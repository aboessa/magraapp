# خطة حماية وتشفير محتوى تطبيق «مجرة» — الإصدار 2.0

> **الحالة:** مرجع تنفيذي ملزم للهاتف والتابلت وTV. تفرّق الخطة بين حماية الوصول، والتشفير المحلي، وDRM؛ ولا تدّعي منع النسخ أو التصوير بنسبة 100%.
>
> **مصفوفة الاستحقاق الموحدة:** ملفات الأطفال `1/4/4`، الأجهزة المسجلة `1/4/8`، التشغيل المتزامن `1/2/4`، وأجهزة التنزيل `0/2/4` لباقات Free/Family/Family Plus على الترتيب.

# 1. القرار المعماري الأساسي

تقسم حماية المحتوى إلى ثلاثة مسارات مترابطة: Online غير الفيديو، فيديو محمي بـDRM، وOffline مشفر ومرخص:

### المحتوى Online غير الفيديو عالي القيمة

لا يحتاج إلى طبقة تشفير مخصصة داخل Flutter، لكنه يحتاج إلى **حماية وصول خادمية** كاملة، بشرط:

* تخزينه داخل Cloudflare R2 Private Bucket.
* عدم وجود روابط عامة أو ثابتة.
* استخدام HTTPS فقط والتحقق من الحساب والاشتراك والطفل والجهاز قبل كل إصدار وصول.
* إصدار Token أو رابط مؤقت قصير الصلاحية من Worker/Backend.
* عدم حفظ المحتوى المدفوع في Disk Cache مكشوف؛ يستخدم Memory Cache أو Cache محلي مشفر.
* عدم اعتبار CORS أو إخفاء المسار وسيلة حماية.

### الفيديو المدفوع أو المرخص Online

لا تكفيه الروابط المؤقتة وحدها، ويستخدم منذ الـMVP:

* HLS أو DASH بجودات متكيفة.
* Widevine على Android/Android TV، وFairPlay على Apple، وPlayReady عند حاجة منصات TV.
* خادم تراخيص DRM وفحص الاستحقاق والجهاز قبل إصدار الترخيص.
* HDCP وSecure Video Surface حسب دعم المنصة.
* Manifest/Playback Token قصير العمر مع تجديد آمن أثناء المشاهدة.

### المحتوى الذي يحمّله المستخدم للعمل دون إنترنت

يجب تشفيره قبل حفظه بصورة دائمة على الجهاز، بحيث:

* لا يُحفظ كصورة أو صوت أو فيديو عادي.
* يكون مخزنًا داخل المساحة الخاصة بالتطبيق.
* لا يستطيع تطبيق آخر قراءته مباشرة.
* يتم فك التشفير أثناء العرض أو التشغيل فقط.
* لا يُكتب المحتوى المفكوك مرة أخرى على القرص.

```text
Online Images/Audio/Stories:
R2 Private → Entitlement Check → Short-lived Token → HTTPS Streaming
              → Memory Cache أو Encrypted Disk Cache

Online Video:
Private Origin → HLS/DASH Packager/CDN → DRM License Check
               → Secure Player + HDCP

Offline Images/Audio/Stories:
R2 Private → Download License → Streaming Download → AES-256-GCM Chunks
           → Device-bound Wrapped Key → Encrypted App Storage

Offline Video:
DRM Offline License حيث تدعم المنصة؛ والتشفير المخصص ليس بديلًا مساويًا لـDRM
```

---

# 2. تصنيف المحتوى حسب الحماية المطلوبة

| نوع المحتوى            | العرض Online      | التحميل Offline          |
| ---------------------- | ----------------- | ------------------------ |
| الصور المصغرة العامة   | يمكن أن تكون عامة | غير مطلوب                |
| أغلفة القصص المجانية   | عامة أو خاصة      | غير مطلوب غالبًا         |
| قصة مجانية Online      | رابط خاص مؤقت     | تشفير عند التحميل        |
| قصة مدفوعة Online      | رابط خاص مؤقت     | تشفير إلزامي             |
| كوميكس مدفوع           | رابط خاص مؤقت     | تشفير إلزامي             |
| ملف صوتي مدفوع         | Streaming خاص     | تشفير إلزامي             |
| فيديو كرتوني مدفوع     | HLS/DASH + Multi-DRM       | DRM Offline License حيث تدعم المنصة |
| ملفات الألعاب المدفوعة | رابط خاص                  | تشفير الحزمة المحلية والتحقق من التوقيع |
| ملفات الترجمة والنصوص  | خاصة حسب المحتوى           | تشفير مع الحزمة          |
| محتوى مرخص من ناشر     | وفق العقد + DRM/Watermark عند اللزوم | وفق حقوق Offline بالعقد |

---

# 3. البنية العامة للنظام

```text
Flutter / TV Application
        ↓
Authentication + Device Token (`auth_epoch`)
        ↓
Content Access API / Cloudflare Worker
        ↓
التحقق من الحساب والاشتراك والطفل والدولة والجهاز وحقوق الأصل
        ↓
        ├── صور/قصص/صوت: Short-lived Access Token → R2 Private
        ├── فيديو: Playback Session → DRM License + HLS/DASH CDN
        └── Offline: Download Session → Signed Manifest/License + Wrapped Key
```

## المكونات

### Cloudflare R2

يستخدم لتخزين:

* الصور.
* ملفات الصوت.
* القصص والكوميكس.
* الفيديوهات أو مقاطع HLS.
* ملفات الترجمة.
* ملفات الألعاب.
* إصدارات المحتوى.

### قاعدة البيانات

تحتوي على:

* بيانات المحتوى.
* الاشتراكات.
* صلاحيات الوصول.
* الأجهزة المسجلة.
* جلسات التحميل.
* تراخيص Offline.
* إصدارات الملفات.
* Hash لكل ملف.
* تاريخ انتهاء الحقوق.

### Cloudflare Worker أو Backend API

مسؤول عن:

* التحقق من JWT.
* التحقق من الاشتراك.
* التحقق من الجهاز.
* منع الوصول غير المصرح.
* إصدار الروابط المؤقتة.
* إنشاء تراخيص Offline.
* تسجيل عمليات التنزيل.
* Rate Limiting.
* إبطال الأجهزة والتراخيص.

### تطبيق Flutter

مسؤول عن:

* تشغيل المحتوى Online.
* إدارة التنزيل.
* تشفير الملفات أثناء التنزيل.
* إدارة مفاتيح الجهاز.
* فك المحتوى أثناء التشغيل.
* إدارة مساحة التخزين.
* حذف المحتوى المنتهي.
* تجديد تراخيص Offline.

---

# 4. إعداد Cloudflare R2

## يجب أن يكون الـBucket خاصًا

ممنوع:

* تفعيل Public Bucket للمحتوى المدفوع.
* استخدام روابط `r2.dev` العامة.
* وضع رابط R2 ثابت داخل التطبيق.
* تضمين R2 Access Key أو Secret Key في Flutter.
* السماح للتطبيق بالوصول المباشر الكامل إلى الـBucket.

## تنظيم الملفات المقترح

```text
majarra-content/
├── public/
│   ├── thumbnails/
│   ├── marketing/
│   └── free-covers/
│
├── private/
│   ├── stories/
│   ├── comics/
│   ├── audio/
│   ├── videos/
│   ├── games/
│   └── activities/
│
└── versions/
    ├── v1/
    ├── v2/
    └── archived/
```

## مثال لمسار قصة

```text
private/stories/story_001/v4/
├── manifest.json
├── cover.webp
├── pages/
│   ├── page_001.webp
│   ├── page_002.webp
│   └── page_003.webp
├── audio/
│   ├── ar/
│   │   ├── page_001.m4a
│   │   └── page_002.m4a
│   └── en/
│       ├── page_001.m4a
│       └── page_002.m4a
└── subtitles/
    ├── ar.json
    └── en.json
```

لا يتم تكرار الصور لكل لغة؛ يتم تخزين الصور مرة واحدة، مع صوت ونص منفصل لكل لغة.

---

# 5. تشغيل الصور والقصص والصوت Online

## خطوات التشغيل

1. يختار الطفل القصة أو الملف الصوتي.
2. يرسل التطبيق طلبًا إلى الـBackend:

```http
POST /api/v1/content/book/story_001/playback-sessions
Authorization: Bearer <JWT>
```

3. يتحقق السيرفر من:

* صحة التوكن وربط الجهاز و`auth_epoch`.
* أن الحساب غير موقوف وأن الطفل تابع له ومفعّل.
* أن الاشتراك فعال وأن المحتوى منشور وداخل الباقة والعمر والمسار.
* أن حد التشغيل المتزامن يسمح بجلسة جديدة.
* أن المحتوى متاح في الدولة وأن حقوقه التجارية لم تنتهِ.

4. يصدر السيرفر جلسة تشغيل وToken/رابطًا مؤقتًا قصير العمر.
5. يستخدم التطبيق الوصول المؤقت للتشغيل دون تخزين دائم مكشوف.
6. بعد انتهاء الصلاحية يجدد التطبيق الجلسة خادميًا إن بقي الاستحقاق صالحًا.

> الفيديو المدفوع لا يعتمد على هذا الرابط وحده؛ يتبع مسار DRM وHLS/DASH المحدد في القسم 15.

## مدة صلاحية الروابط

| نوع الملف          | المدة المقترحة |
| ------------------ | -------------: |
| صورة صفحة          |        5 دقائق |
| ملف صوتي قصير      |     5–10 دقائق |
| قصة كاملة          |       10 دقائق |
| فيديو كبير         |    10–20 دقيقة |
| Manifest           |      3–5 دقائق |
| رابط تنزيل Offline |    10–15 دقيقة |

لا يجب استخدام روابط تستمر عدة ساعات أو أيام إلا عند وجود ضرورة واضحة.

## ملاحظة أمنية مهمة

الرابط المؤقت يعتبر مفتاح وصول مؤقتًا؛ أي شخص يحصل عليه يستطيع استخدامه حتى تنتهي صلاحيته.

لذلك:

* لا يتم تسجيل الرابط كاملًا في Logs.
* لا يرسل إلى خدمات Analytics.
* لا يوضع في إشعار Push.
* لا تتم مشاركته في رسائل الأخطاء.
* لا يتم حفظه بصورة دائمة.
* يجب أن تكون صلاحيته قصيرة.

## التخزين المؤقت Online

يسمح للتطبيق بعمل Cache مؤقت لتحسين الأداء، لكن:

* يكون داخل مساحة التطبيق الخاصة.
* يحذف تلقائيًا حسب سياسة LRU.
* لا يعتبر تنزيل Offline.
* لا يظهر للمستخدم في مدير التنزيلات.
* لا يضمن بقاء المحتوى.
* لا يخزن المحتوى المدفوع مكشوفًا على القرص؛ يكون Memory-only أو مشفرًا بنفس سياسة التخزين المحلي.
* يفضل مسحه عند تسجيل الخروج، ويصبح غير قابل للاستخدام فور إلغاء الجهاز أو انتهاء الترخيص.

---

# 6. حدود حماية المشاهدة Online

بالنسبة للصور والقصص والصوت منخفضي أو متوسطي المخاطر، توفر المنظومة التالية حماية عملية مناسبة للـMVP:

* R2 خاص وHTTPS.
* تحقق خادمي من الاستحقاق والجهاز.
* Token قصير العمر لا يُسجّل أو يُشارك.
* Memory Cache أو Cache مشفر للمحتوى المدفوع.
* منع أي R2 Credentials أو قرار اشتراك داخل Flutter فقط.

هذه **حماية وصول** وليست DRM؛ فالمستخدم المتقدم على جهاز معدل قد يعترض البيانات بعد فك TLS أو أثناء العرض. لذلك:

* الفيديو المدفوع أو المرخص يستخدم Multi-DRM من أول منصة إطلاق مستهدفة.
* المحتوى عالي القيمة يمكن أن يستخدم Watermark توزيعية مجهولة قابلة للتدقيق.
* لا توجد وسيلة تمنع Screenshot أو تسجيل الشاشة أو الكاميرا الخارجية بنسبة 100%.
* الهدف هو تقليل الاستخراج وإبطال الوصول والمساءلة، لا تقديم وعد بحماية مطلقة.

---

# 7. خطة التحميل Offline

عندما يضغط المستخدم على زر «تحميل»، تبدأ عملية مختلفة عن المشاهدة Online.

## الخطوة الأولى: إنشاء جلسة تحميل

يرسل التطبيق:

```http
POST /api/v1/content/book/story_001/download-sessions
Authorization: Bearer <JWT>
```

مع:

```json
{
  "deviceId": "device_456",
  "childProfileId": "child_123",
  "language": "ar",
  "quality": "high",
  "contentVersion": 4
}
```

> لا يثق السيرفر بهذه المعرفات لمجرد وصولها في Body: يستنتج الحساب من JWT، ثم يثبت ملكية الطفل وربط الجهاز وحالته و`auth_epoch` داخل قاعدة البيانات.

## الخطوة الثانية: التحقق

يتحقق السيرفر من:

* الاشتراك.
* الباقة.
* عدد الأجهزة.
* عدد التنزيلات المسموح.
* مساحة التنزيل المسموح بها.
* الدولة.
* صلاحية حقوق Offline.
* إصدار التطبيق.
* حالة الجهاز.
* عدم وجود نشاط تحميل غير طبيعي.

## الخطوة الثالثة: رد جلسة التحميل

يعيد السيرفر:

```json
{
  "downloadSessionId": "ds_83921",
  "contentId": "story_001",
  "version": 4,
  "expiresIn": 900,
  "offlineLicense": "...",
  "assets": [
    {
      "id": "page_001",
      "url": "TEMPORARY_SIGNED_URL",
      "size": 1048576,
      "sha256": "..."
    },
    {
      "id": "audio_001_ar",
      "url": "TEMPORARY_SIGNED_URL",
      "size": 2097152,
      "sha256": "..."
    }
  ]
}
```

صلاحية جلسة التحميل المقترحة: 10–15 دقيقة، ويمكن للتطبيق طلب تجديدها عند انقطاع الإنترنت.

---

# 8. طريقة تشفير المحتوى على الجهاز

## الخوارزمية وصيغة التوثيق

يستخدم التطبيق للمحتوى غير الخاضع لـDRM:

* AES-256-GCM لكل ملف صغير أو Chunk مستقل.
* Content Encryption Key عشوائي 256-bit لكل حزمة/إصدار؛ ويمكن استخدام مفتاح مستقل للأصول الأعلى حساسية.
* GCM Nonce بطول 96-bit **فريد حتميًا لكل مفتاح**؛ يشتق من Package Salt عشوائي + Chunk Counter، أو يولد CSPRNG مع سجل يمنع إعادة الاستخدام.
* Authentication Tag بطول 128-bit للتحقق من السرية والسلامة.
* `AAD` غير مشفرة تربط الجزء بسياقه، وتشمل على الأقل:
  `contentId + version + assetId + chunkIndex + deviceId + licenseId`.
* SHA-256 للأصل كما نشره السيرفر، وHash منفصل للنسخة المشفرة المخزنة محليًا.

> إعادة استخدام Nonce مع مفتاح GCM نفسه خطأ حرج قد يكشف المحتوى؛ لا يكفي وصفه بأنه «عشوائي» دون ضمان التفرد.

## الترتيب الصحيح

```text
Download Stream
      ↓
التحقق من البيانات أثناء الوصول
      ↓
AES-256-GCM Encryption
      ↓
كتابة النسخة المشفرة على الجهاز
```

ممنوع تنفيذ الآتي:

```text
تنزيل ملف مكشوف كامل
      ↓
حفظه على القرص
      ↓
قراءته مرة أخرى
      ↓
تشفيره
```

لأن هذا يترك نسخة غير مشفرة مؤقتًا على الجهاز.

## الطريقة الصحيحة

يتم تنزيل الملف على هيئة Stream:

```text
R2 Response Stream
       ↓
Encryption Sink
       ↓
Encrypted Local File
```

وبذلك لا يتم حفظ النسخة الأصلية المكشوفة على القرص.

---

# 9. تقسيم الملفات إلى Chunks

للملفات الكبيرة يفضل تقسيم التشفير إلى أجزاء.

## الحجم المقترح

* الصور الصغيرة: يمكن تشفير كل صورة كملف.
* الصوت: Chunks بحجم 1–4 MB.
* القصص الكبيرة: Chunks بحجم 2–5 MB.
* الفيديو: Segments يديرها DRM/Packager؛ ولا يستخدم التشفير المخصص إلا للمحتوى منخفض المخاطر المعتمد.
* حزم الألعاب: Chunks بحجم 4–8 MB.

## مثال

```text
story_001_v4/
├── manifest.mjrenc
├── chunk_0001.mjrenc
├── chunk_0002.mjrenc
├── chunk_0003.mjrenc
├── chunk_0004.mjrenc
└── license.mjrlic
```

كل Chunk يمتلك:

```json
{
  "chunkId": "chunk_0001",
  "nonce": "96_BIT_UNIQUE_NONCE",
  "authenticationTag": "128_BIT_GCM_TAG",
  "aad": "contentId|version|assetId|chunkIndex|deviceId|licenseId",
  "encryptedSize": 3145728,
  "plainSize": 3145500,
  "sourceSha256": "...",
  "ciphertextSha256": "..."
}
```

## فوائد التقسيم

* استكمال التحميل بعد الانقطاع.
* إعادة تحميل الجزء التالف فقط.
* تشغيل أول الصفحات قبل انتهاء تحميل القصة.
* عدم فك ملف كبير كاملًا في الذاكرة.
* تحديث بعض الملفات دون إعادة تنزيل كل المحتوى.

---

# 10. إدارة مفاتيح التشفير

## ممنوع وضع مفتاح ثابت داخل التطبيق

مثال خاطئ:

```dart
const encryptionKey = "majarra-secret-key";
```

لأن المفتاح يمكن استخراجه من APK أو IPA، وبذلك يتم فك جميع الملفات.

## النموذج الصحيح

### هوية ومفتاح الجهاز

عند تسجيل التثبيت لأول مرة:

* ينشئ التطبيق مفتاحًا غير قابل للتصدير أو زوج مفاتيح داخل مخزن المنصة الآمن.
* يسجل السيرفر المفتاح العام أو معرف المفتاح مع `account_devices`، ولا يستقبل المفتاح الخاص.
* يستخدم Android Keystore مع Hardware-backed Key عند توفره.
* يستخدم iOS Keychain مع Access Control، وSecure Enclave بمفتاح غير متماثل عند ملاءمة الجهاز والخوارزمية.
* يرتبط الجهاز بـ`auth_epoch`؛ رفعه عند الإلغاء يبطل الرموز والتراخيص الجديدة.
* لا يستخدم IMEI أو Advertising ID أو أي PII للطفل كهوية جهاز.

### مفتاح المحتوى ونموذج Envelope Encryption

1. ينشئ Backend/KMS مفتاح Content Encryption Key عشوائيًا للحزمة/الإصدار، أو يصدره ضمن عملية ترخيص موثوقة.
2. يربط المفتاح بالحساب والطفل والجهاز والمحتوى والإصدار والترخيص.
3. يغلّف CEK للمفتاح الآمن الخاص بالجهاز؛ ولا يرسل مفتاحًا عامًا يصلح لكل المستخدمين.
4. يخزن التطبيق `wrappedKey` فقط مع الترخيص الموقع.
5. يفك المفتاح الخام داخل الذاكرة وقت التشغيل، ثم يمسحه بأفضل جهد ممكن ولا يكتبه على القرص أو Logs/Crash Reports.
6. الفيديو الخاضع لـDRM يحصل على مفاتيحه من DRM License Server، وليس من هذه الآلية المخصصة.

```text
Server/KMS Content Key (CEK)
        ↓ bound to account + child + device + content + version + license
Wrapped for Device Secure Key / Public Key
        ↓
Signed Offline License + Wrapped CEK
        ↓
Raw CEK exists in memory only while decrypting
```

## نموذج بيانات المفتاح

```json
{
  "keyId": "cek_story_001_v4_k2",
  "licenseId": "lic_123",
  "contentId": "story_001",
  "entityType": "book",
  "version": 4,
  "algorithm": "AES-256-GCM",
  "wrappingAlgorithm": "PLATFORM_APPROVED_KEY_WRAP",
  "wrappedKey": "...",
  "deviceId": "device_456",
  "deviceAuthEpoch": 7,
  "createdAt": "2026-08-05T00:00:00Z"
}
```

## عند تسجيل الخروج أو إبطال الجهاز

يمكن:

* حذف Wrapped Key.
* حذف Offline License.
* منع فتح المحتوى.
* الاحتفاظ بالملفات المشفرة مؤقتًا ثم تنظيفها لاحقًا.

دون المفتاح، تصبح الملفات الموجودة على الجهاز غير قابلة للقراءة.

---

# 11. ترخيص Offline

كل محتوى محمل يجب أن يمتلك ترخيصًا موقعًا من السيرفر.

## نموذج الترخيص

```json
{
  "licenseId": "lic_123",
  "keyId": "cek_story_001_v4_k2",
  "accountId": "parent_123",
  "childProfileId": "child_123",
  "deviceId": "device_456",
  "deviceAuthEpoch": 7,
  "entityType": "book",
  "contentId": "story_001",
  "version": 4,
  "manifestSha256": "...",
  "rights": ["offline_read", "audio_ar"],
  "territory": "EG",
  "issuedAt": "2026-08-05T00:00:00Z",
  "expiresAt": "2026-09-04T00:00:00Z",
  "maxOfflineDays": 30,
  "subscriptionRequired": true,
  "signatureAlgorithm": "Ed25519",
  "signatureKeyId": "license-signing-2026-01",
  "signature": "..."
}
```

## القواعد المقترحة

* صلاحية Offline القصوى المقترحة: حتى 30 يومًا، وتُقصر حسب الباقة وحقوق الناشر وحساسية المحتوى.
* يجدد الترخيص تلقائيًا عند الاتصال.
* عند انتهاء الترخيص، يطلب التطبيق الاتصال بالإنترنت.
* يمكن توفير فترة سماح قصيرة في حالة فشل الدفع المؤقت.
* عند إلغاء الاشتراك، يظل المحتوى حتى نهاية الفترة المدفوعة.
* بعد انتهاء الفترة، يتم حذف المفتاح أو تعطيل الترخيص.
* يمكن للسيرفر إبطال جهاز أو ترخيص مسروق عند أول اتصال لاحق.
* لا يمتد أي ترخيص جديد أو مجدد بعد نهاية الاشتراك أو `plan_change_effective_at`.
* كل تنزيل يطابق ترخيص Offline نشطًا لنفس الأسرة والطفل والجهاز و`entityType/contentId/version`.
* حالات الترخيص `expired/revoked` نهائية؛ التجديد يصدر ترخيصًا جديدًا ولا يعيد إحياء السجل القديم.

> الإبطال لا يصل لحظيًا إلى جهاز غير متصل. الحماية العملية تعتمد على قصر مدة الترخيص، وإعادة التحقق الدوري، وانتهاء الترخيص محليًا حتى لو تأخر الاتصال.

## لا تعتمد على ساعة الجهاز فقط

لأن المستخدم يستطيع تغيير تاريخ الهاتف.

يجب حفظ داخل التخزين الآمن:

* آخر وقت موقع وموثوق من السيرفر.
* Monotonic Clock Anchor منذ آخر تحقق حيث تدعم المنصة.
* آخر تاريخ تحقق وفرق الوقت المرصود.
* Hash/توقيع حالة الترخيص ومنع استعادة نسخة أقدم منها قدر الإمكان.
* حالة الاشتراك و`auth_epoch` عند آخر اتصال.
* سياسة تلزم اتصالًا دوريًا بعد مدة محددة للمحتوى الأعلى حساسية.

تغيير ساعة الجهاز أو استعادة Backup قد لا يمكن منعهما مطلقًا، لذلك لا يمنح التطبيق صلاحية Offline مفتوحة بلا تاريخ انتهاء.

---

# 12. تشغيل المحتوى المشفر

عند فتح قصة محملة:

1. يقرأ التطبيق Offline License والـManifest الموقعين.
2. يتحقق من `signatureKeyId` والتوقيع والـCanonical Payload.
3. يطابق الحساب والطفل والجهاز و`auth_epoch` ونوع/معرف الأصل والإصدار.
4. يتحقق من الحقوق والمنطقة وتاريخ الصلاحية باستخدام الوقت الموثوق والـMonotonic Anchor.
5. يرفض حالات `expired/revoked` ولا يعيد إحياء ترخيص نهائي.
6. يفك Wrapped Content Key داخل المخزن/الذاكرة الآمنة قدر الإمكان.
7. يقرأ الـChunk المطلوب ويتحقق من Ciphertext Hash.
8. يفك AES-GCM باستخدام Nonce وAAD المتوقعين ويتحقق من Authentication Tag.
9. يطابق Source Hash أو Manifest Hash حسب مستوى الأصل.
10. يرسل البيانات مباشرة إلى العارض أو مشغل الصوت دون ملف وسيط مكشوف.
11. يمسح Buffers والمفتاح الخام بأفضل جهد عند الانتقال أو الإغلاق.
12. يعيد فحص `child_download_is_usable` أو قرار الاستحقاق الحي قبل إصدار أي Signed URL جديد.

```text
Encrypted File
      ↓
Decrypt Stream
      ↓
Memory Buffer
      ↓
Image / Audio Player
```

ممنوع:

```text
Encrypted File
      ↓
إنشاء page_001.webp مكشوف
      ↓
تشغيله من القرص
```

---

# 13. صيغة الملفات المحلية

يمكن استخدام امتداد داخلي مثل:

```text
.mjrenc
.mjrpack
.mjrlic
```

مثال:

```text
story_001_v4.mjrpack
story_001_v4.mjrlic
```

لكن يجب التأكيد أن الامتداد ليس وسيلة حماية؛ الحماية تعتمد على التشفير والمفتاح والترخيص.

يمكن أن تحتوي `.mjrpack` على:

* Manifest.
* صور.
* صوت.
* نصوص.
* ترجمة.
* Metadata.
* بيانات توقيت.
* Hashes.
* Version Number.

---

# 14. القصص والكوميكس

## القصص المصورة

يمكن أن تتكون الحزمة من:

```text
cover
page images
Arabic audio
English audio
French audio
page text
word timings
questions
activities
```

## الكوميكس

تحتوي الحزمة على:

* صور الصفحات.
* ترتيب اللوحات.
* إحداثيات كل لوحة.
* إحداثيات فقاعات الحوار.
* النصوص.
* أصوات الشخصيات.
* توقيت كل فقاعة.
* مؤثرات صوتية.
* الاختبار المرتبط.

مثال للـManifest:

```json
{
  "page": 1,
  "imageAsset": "page_001",
  "panels": [
    {
      "id": "panel_01",
      "x": 0.04,
      "y": 0.06,
      "width": 0.44,
      "height": 0.38,
      "audioAsset": "voice_panel_01"
    }
  ]
}
```

---

# 15. الفيديو Online وOffline

## الفيديو Online — إلزامي من الـMVP

الفيديو المدفوع أو المرخص يستخدم:

* HLS أو DASH مع Adaptive Bitrate وKey Rotation وفق مزود DRM.
* Widevine لأندرويد وAndroid TV، وFairPlay لـiOS/iPadOS/tvOS، وPlayReady عند احتياج منصة TV المستهدفة.
* DRM License Server يراجع الاشتراك والطفل والجهاز والمنطقة وحقوق الأصل قبل إصدار الترخيص.
* Playback Session قصيرة وHeartbeat لاحتساب حدود التشغيل المتزامن `1/2/4`.
* HDCP وSecure Decoder/Surface حيث تدعم المنصة.
* Manifest/Segment Tokens قصيرة العمر وتجديد سلس حتى لا يتوقف التشغيل الطبيعي.
* R2 كـPrivate Origin عند ملاءمة التصميم، مع Packager/CDN؛ ولا يقدم R2 وحده DRM.

يمنع تقديم MP4 ثابت أو مفتاح تشفير عام داخل Flutter. روابط HLS/DASH المؤقتة وحدها ليست بديلًا عن DRM للمحتوى عالي القيمة.

## الفيديو Offline

* يستخدم DRM Offline License حيث تدعم المنصة والمزود ذلك.
* يرتبط الترخيص بالحساب والطفل والجهاز و`auth_epoch` والإصدار وتاريخ الانتهاء.
* يسحب عند إلغاء الجهاز أو انتهاء الاستحقاق عند أول اتصال، ويتوقف محليًا عند انتهاء صلاحيته.
* Custom Encrypted Segments مسموحة فقط لمحتوى منخفض المخاطر وبعد موافقة مالك الحقوق، وليست حماية مساوية لـDRM.
* لا يتم فك فيديو كامل وكتابته كـMP4 مكشوف على القرص.

## TV وCast/AirPlay

* تطبيق TV المسجل يحتسب جهازًا مستقلًا.
* Cast/AirPlay من جهاز مسجل يحتسب تشغيلًا واحدًا، ولا يسجل المستقبل تلقائيًا كجهاز.
* يطبق TV نفس DRM/HDCP وحدود الجلسة، مع QR pairing بدل إدخال كلمة مرور الطفل.

---

# 16. نصائح مهمة عند عدم استخدام التشفير Online

حتى بدون تشفير مخصص يجب تطبيق الآتي:

## الحماية الأساسية

* R2 Private Bucket.
* HTTPS فقط.
* روابط مؤقتة.
* JWT قصير الصلاحية.
* Refresh Token آمن.
* فحص الاشتراك على السيرفر.
* Device Limit.
* Rate Limiting.
* Audit Logs.
* Hash للملفات.
* عدم كشف مسارات الملفات الأصلية عند الإمكان.

## لا تعتمد على التطبيق وحده

ممنوع أن يكون القرار هكذا:

```dart
if (user.isPremium) {
  openContent();
}
```

لأن بيانات التطبيق يمكن تعديلها.

التحقق الحقيقي يجب أن يتم على السيرفر قبل إصدار الرابط.

## CORS ليس وسيلة حماية

CORS يمنع بعض طلبات المتصفح، لكنه لا يمنع:

* تطبيقًا آخر.
* Postman.
* Scripts.
* الأجهزة المعدلة.
* مشاركة الرابط.

لذلك لا تستخدم CORS كبديل عن Authentication.

## لا تستخدم روابط ثابتة

مثال خاطئ:

```text
https://content.majarra.app/stories/story_001/page_01.webp
```

إذا كان الرابط يعمل دائمًا، يمكن مشاركته خارج التطبيق.

الأفضل:

```text
https://content.majarra.app/access/temporary-token/...
```

مع مدة صلاحية قصيرة.

## لا ترسل أسرار R2 إلى Flutter

ممنوع تضمين:

* Access Key.
* Secret Key.
* Admin Token.
* Bucket Credentials.
* Signing Secret.

كلها تبقى داخل Worker أو Backend فقط.

---

# 17. منع إساءة استخدام التنزيل

يجب إضافة ضوابط مثل:

* حد أقصى للأجهزة.
* حد أقصى للتنزيلات اليومية.
* حد أقصى للمحتوى المحمل.
* تسجيل عدد عمليات التحميل.
* منع 100 تحميل في ثوانٍ.
* منع نفس الحساب من عشرات الدول في وقت واحد.
* إبطال جلسات الأجهزة غير المعروفة.
* إرسال تنبيه لولي الأمر عند تسجيل جهاز جديد.
* إمكانية تسجيل الخروج عن بعد.

## الحدود الملزمة حسب الباقة

| الباقة | ملفات الأطفال | الأجهزة المسجلة | التشغيل المتزامن | أجهزة التنزيل |
| --- | ---: | ---: | ---: | ---: |
| Free | 1 | 1 | 1 | 0 |
| Family | 4 | 4 | 2 | 2 |
| Family Plus | 4 | 8 | 4 | 4 |

* كل تثبيت مستقل، بما فيه تطبيق TV، يحتسب جهازًا مسجلًا.
* Cast/AirPlay من جهاز مسجل يحتسب تشغيلًا واحدًا ولا يسجل المستقبل تلقائيًا.
* حد أجهزة التنزيل مستقل عن عدد العناصر المخزنة؛ يمكن إضافة حصة اختيارية مثل 20/50 عنصرًا أو حد مساحة بعد قرار Product موثق و`policy_version` جديد.
* لا يحذف النظام أقدم جهاز تلقائيًا عند بلوغ الحد؛ يختار ولي الأمر جهازًا لإلغائه خلف PIN/biometric.
* downgrade يستخدم Grace Period واختيار الملفات/الأجهزة المحتفظ بها، ولا يحذف البيانات تلقائيًا.
* جميع الحدود تفرض خادميًا وبشكل ذري، ولا يعتمد فرضها على إخفاء زر داخل Flutter.

---

# 18. إدارة مساحة التخزين

يجب أن يعرض التطبيق:

* حجم كل قصة قبل التنزيل.
* الحجم الإجمالي للتنزيلات.
* المساحة المتاحة.
* حذف محتوى محدد.
* حذف جميع التنزيلات.
* تنزيل عبر Wi-Fi فقط.
* اختيار جودة الفيديو.
* التنزيل في الخلفية.
* إيقاف واستكمال التنزيل.

## التنظيف التلقائي

يمكن حذف:

* الملفات المؤقتة القديمة.
* الإصدارات السابقة.
* التنزيلات التالفة.
* الملفات التي فقدت ترخيصها.
* المحتوى غير المستخدم منذ مدة يحددها ولي الأمر.

لا يحذف التطبيق المحتوى الذي حمّله المستخدم دون إخباره، إلا إذا كان تالفًا أو انتهى ترخيصه.

---

# 19. نموذج البيانات المتوافق مع مخطط المشروع

لا تُنشأ جداول موازية بأسماء مختلفة؛ مصدر الحقيقة هو `DATABASE_V2_SCHEMA.sql`.

## `subscription_plan_limits`

```text
plan
max_child_profiles
max_registered_devices
max_concurrent_streams
max_download_devices
policy_version
updated_at
```

## `account_devices`

```text
id                         -- opaque installation id، وليس IMEI/Advertising ID
parent_id
installation_id_hash
display_name
platform
status
secure_playback_capable
download_enabled
auth_epoch
registered_at
last_seen_at
revoked_at
```

## `media_licenses`

```text
id
parent_id
child_id
device_id
entity_type
entity_id
kind                       -- online/offline
status                     -- active/expired/revoked
provider_license_hash
issued_at
expires_at
revoked_at
```

## `child_downloads`

```text
id
child_id
device_id
entity_type
entity_id
media_license_id
status                     -- queued/downloading/ready/expired/failed
expires_at
created_at
```

كل سجل تنزيل يطابق ترخيص Offline لنفس الأسرة والطفل والجهاز والأصل، ويصبح غير قابل للاستخدام فور انتهاء الترخيص أو إلغاء الجهاز حتى لو تأخر تنظيف الملف المحلي.

## جداول إضافية مطلوبة لمدير التنزيل

### `content_assets`

```text
id
entity_type
entity_id
content_version
asset_type
r2_key
file_size
source_sha256
language
quality
is_downloadable
rights_expire_at
```

### `download_sessions`

```text
id
parent_id
child_id
device_id
entity_type
entity_id
content_version
status
created_at
expires_at
completed_at
```

### `download_events`

```text
id
download_session_id
asset_id
event_type
bytes_downloaded
created_at
ip_hash
```

لا تخزن هذه الجداول Signed URLs أو مفاتيح خام أو nickname/PII للطفل.

---

# 20. نقاط الـAPI المقترحة

```http
POST /api/v1/content/{entityType}/{id}/playback-sessions
POST /api/v1/playback-sessions/{sessionId}/heartbeat
POST /api/v1/playback-sessions/{sessionId}/end

POST /api/v1/content/{entityType}/{id}/download-sessions
POST /api/v1/download-sessions/{sessionId}/refresh
POST /api/v1/download-sessions/{sessionId}/complete
POST /api/v1/downloads/{downloadId}/transition

POST /api/v1/licenses/{licenseId}/renew
POST /api/v1/licenses/{licenseId}/revoke

GET  /api/v1/devices
POST /api/v1/devices/register
POST /api/v1/devices/{deviceId}/revoke
POST /api/v1/devices/{deviceId}/download-access
DELETE /api/v1/downloads/{downloadId}
```

قواعد إلزامية:

* يستنتج `parent_id` من JWT ولا يقبله من Body.
* يعاد التحقق خادميًا من ملكية `childProfileId` وربط الجهاز بالحساب و`auth_epoch`.
* `deviceId` و`childProfileId` المدخلان غير موثوقين حتى يثبت الربط في قاعدة البيانات.
* إلغاء الجهاز يغير حالته إلى `revoked` ويحفظ سجل التدقيق؛ لا يحذف صف الجهاز فعليًا.
* عمليات التسجيل والإصدار والـheartbeat والإلغاء تستخدم ترتيب قفل موحدًا: الحساب ثم الجهاز ثم الصف.
* كل Complete/Transition يستخدم Idempotency Key أو مقارنة بالحالة المتوقعة لمنع التكرار والسباقات.
* روابط وأسرار R2 لا تظهر في Logs أو Analytics أو Push Notifications.

---

# 21. حالات الفشل التي يجب معالجتها

يجب اختبار:

* انقطاع الإنترنت أثناء التنزيل.
* انتهاء الرابط المؤقت.
* انتهاء الاشتراك أثناء التحميل.
* تغيير إصدار المحتوى.
* عدم كفاية مساحة الجهاز.
* تلف Chunk.
* تغيير ساعة الجهاز.
* حذف التطبيق ثم تثبيته.
* تسجيل الخروج.
* إبطال الجهاز.
* فقدان المفتاح.
* فشل KeyStore أو Keychain.
* استخدام Root أو Jailbreak.
* تحميل المحتوى على جهازين في الوقت نفسه.
* انتهاء حقوق المحتوى في دولة معينة.

## عند تلف جزء

لا يتم إعادة تنزيل القصة كاملة؛ يعاد تحميل الـChunk المتضرر فقط.

---

# 22. الحماية من Root وJailbreak

يمكن اكتشاف بعض مؤشرات:

* Root.
* Jailbreak.
* Debugger.
* Emulator.
* Hooking Frameworks.
* App Tampering.

لكن لا يجب الاعتماد عليها بنسبة 100%.

الخيارات:

* السماح بالمشاهدة Online فقط على الأجهزة عالية الخطورة.
* تعطيل Offline Download.
* تقليل مدة الترخيص.
* طلب إعادة تحقق متكرر.
* تسجيل الحدث أمنيًا.

لا يفضل منع كل الأجهزة تلقائيًا دون مراجعة، لأن أدوات الكشف قد تعطي نتائج خاطئة.

---

# 23. التوقيع والتحقق من سلامة المحتوى

كل Manifest وOffline License يجب أن يكونا موقعين رقميًا بتوقيع غير متماثل، مثل Ed25519 أو ECDSA بخوارزمية معتمدة من مزود المنصة.

* المفتاح الخاص يبقى داخل KMS/HSM أو بيئة Backend موثوقة ولا يدخل التطبيق.
* التطبيق يحتوي المفاتيح العامة الحالية والسابقة اللازمة للدوران الآمن.
* تتضمن الرسالة `signatureKeyId` ونسخة الخوارزمية.
* يوقع تمثيل Canonical ثابت مثل JCS JSON أو CBOR deterministic، لا JSON عشوائي الترتيب.
* يشمل التوقيع هوية المحتوى والإصدار والـHashes والحقوق والجهاز والطفل وتاريخ الانتهاء.

يستخدم التوقيع لمنع:

* تعديل ترتيب الصفحات أو Manifest.
* استبدال الملفات أو Chunks.
* تغيير مدة الترخيص أو الحقوق.
* تغيير Content ID أو Version أو ربط الجهاز.
* تركيب حزمة أو ترخيص مزيف.

التشفير يمنع القراءة، وGCM/Hashes تكشف التلف، والتوقيع يمنع التزوير؛ ولا تغني أي طبقة عن الأخرى.

---

# 24. الحماية من Screenshot وتسجيل الشاشة

هذه طبقة منفصلة عن تشفير الملفات وDRM:

* يستخدم Android `FLAG_SECURE` وSecure Surface في الشاشات الحساسة حيث تسمح تجربة المنتج.
* يراقب iOS/iPadOS/tvOS حالة Capture/Mirroring ويوقف أو يغطي المحتوى حيث تسمح الواجهات العامة.
* يطبق الفيديو DRM وHDCP على المسارات والمنصات المدعومة.
* يمكن إضافة Watermark متحركة مجهولة للمحتوى عالي القيمة، مرتبطة بمعرف جلسة pseudonymous قابل للتدقيق.
* لا تحتوي Watermark على nickname أو بريد أو معرف طفل أو أي PII.
* لا تغطي الترجمة أو النص التعليمي، ولا تضاف إلى الأصل الفني؛ تضاف وقت التوزيع أو العرض.
* توثّق المنصة صراحةً أن بعض الأنظمة قد لا تمنع كل Screenshot، وأن الكاميرا الخارجية لا يمكن منعها.

الاستجابة لا تحذف بيانات المستخدم أو تعاقبه تلقائيًا اعتمادًا على إشارة Capture واحدة؛ تسجل الحدث وفق سياسة خصوصية وتطبق الإيقاف أو التغطية المسموحة.

---

# 25. مراحل التنفيذ

## Phase 0 — إثبات المعمارية والأمان

* اعتماد مصفوفة الباقات والحقوق و`policy_version`.
* R2 Private Origin وWorker/Backend تجريبي بلا Credentials في العميل.
* اختيار مزود Packager/CDN وMulti-DRM وإثبات Widevine/FairPlay وPlayReady عند الحاجة.
* تصميم Device Registration و`auth_epoch` وترتيب الأقفال وعمليات Revocation.
* Prototype للهاتف والتابلت وTV يثبت Secure Playback وD-pad/Focus وQR pairing.
* Threat Model واختبار عدم تخزين URL/Token/CEK في Logs أو Crash Reports.

## Phase 1 — MVP للهاتف والتابلت وTV المستهدف

* Entitlement API موحد لكل `entityType/entityId`.
* R2 Private وTokens قصيرة وRate Limiting وAudit Logs.
* Online صور/قصص/صوت مع Memory أو Encrypted Cache.
* HLS/DASH + Multi-DRM + HDCP للفيديو المدفوع أو المرخص.
* حدود `1/4/4` ملفات، `1/4/8` أجهزة، `1/2/4` تشغيلات، `0/2/4` أجهزة تنزيل.
* إدارة الأجهزة خلف PIN/biometric مع Revoke ورفع `auth_epoch`.
* Offline أساسي للقصص والصوت: AES-256-GCM Chunks وWrapped Keys وSigned Licenses.
* استكمال التنزيل والتحقق من Hash وحذف الملفات المنتهية.
* Capture Response وWatermark مجهولة للمحتوى المصنف عالي القيمة.

## Phase 2 — Offline متقدم والكوميكس والألعاب

* حزم `.mjrpack` وبيانات اللوحات والفقاعات والمزامنة الصوتية.
* تنزيل جزئي وتحديث Chunks وإعادة تحميل الجزء التالف فقط.
* تشفير حزم الألعاب والتحقق من سلامتها قبل التشغيل.
* تحسين إدارة المساحة وسياسات Wi-Fi والجودة والخلفية.
* DRM Offline للفيديو على مصفوفة المنصات التي يعتمدها المزود.

## Phase 3 — التوسع

* إضافة متاجر ومصنعي TV جدد، لا تقديم أول دعم TV.
* Key Rotation وSigning Key Rotation آليان واختبارات استرجاع.
* Watermarking جنائي للمحتوى الذي تتطلب عقوده ذلك.
* كشف إساءة استخدام أدق مع مراجعة بشرية وتقليل False Positives.
* اختبارات اختراق وتحميل وسباقات على Staging قبل الإنتاج.

---

# 26. معايير قبول التنفيذ

يعتبر النظام جاهزًا عندما:

1. لا يعمل أي رابط دائم للمحتوى المدفوع، ولا توجد R2 Credentials أو Signing Secrets داخل التطبيق. في الوضع المرحلي غير DRM، يحمل المشغل Authorization قصير المدة إلى مسار Worker فقط ولا يحصل على رابط R2.
2. لا يستطيع مستخدم غير مستحق الحصول على URL أو Playback/Offline License.
3. تفرض الحدود خادميًا وذريًا: ملفات `1/4/4`، أجهزة `1/4/8`، تشغيل `1/2/4`، وأجهزة تنزيل `0/2/4`.
4. تطبيق TV يحتسب جهازًا، وCast/AirPlay يحتسب تشغيلًا واحدًا وفق السياسة الموحدة.
5. يعمل الفيديو المدفوع أو المرخص عالي القيمة عبر HLS/DASH وDRM/HDCP على مصفوفة المنصات المستهدفة، ولا يوجد MP4 أو مفتاح عام ثابت. الوضع المؤقت غير DRM يستثني هذا المحتوى ويقتصر على فيديو أصلي يملكه المنتج.
6. تنتهي Tokens والروابط المؤقتة في الموعد، ويتجدد التشغيل المشروع دون كشفها في Logs/Analytics.
7. لا يحفظ Online Cache المدفوع مكشوفًا على القرص.
8. لا يُحفظ أي محتوى Offline مفكوكًا بصورة دائمة.
9. تختلف CEKs حسب الحزمة/الإصدار، ولا يتكرر GCM Nonce مع المفتاح نفسه.
10. يتحقق كل Chunk من GCM Tag وAAD والـHashes المناسبة.
11. تخزن المفاتيح مغلفة عبر Keystore/Keychain/Secure Enclave حسب المنصة، ولا يظهر المفتاح الخام في القرص أو Logs.
12. كل Offline License موقع ومقيد بالحساب والطفل والجهاز و`auth_epoch` والأصل والإصدار والحقوق والانتهاء.
13. يعمل التطبيق Offline فقط داخل مدة الترخيص، ويتوقف فور الانتهاء حتى لو لم يتصل بالسيرفر.
14. إلغاء الجهاز يرفع `auth_epoch` ويسحب جلساته وتراخيصه وتنزيلاته، ولا يحذف صف الجهاز التاريخي.
15. حالات الجلسة والترخيص النهائية لا تعود Active؛ يصدر سجل جديد عند الحاجة.
16. يستكمل التنزيل بعد الانقطاع، ويعاد تنزيل الجزء التالف فقط، وتستخدم انتقالات Idempotent/CAS.
17. لا يمكن تجاوز الاشتراك أو ملكية الطفل بتعديل Flutter أو Body Parameters محليًا.
18. يتحقق التطبيق من توقيع Canonical Manifest/License مع `signatureKeyId` ودوران مفاتيح صالح.
19. لا تستخدم CORS أو Root Detection أو امتداد الملف كوسيلة حماية أساسية.
20. تختبر `FLAG_SECURE`/Capture Response وWatermark المجهولة بلا PII، مع توثيق عدم ضمان منع التصوير 100%.
21. تختبر حالات Root/Jailbreak وClock Rollback وإعادة تثبيت التطبيق وفقدان المفتاح دون ادعاء حماية مطلقة.
22. توجد Audit Logs لكل جلسة وإلغاء دون URL كامل أو Token أو مفتاح أو PII طفل.
23. يختبر downgrade/grace واختيار الأجهزة والملفات دون حذف بيانات تلقائي.
24. تنفذ اختبارات Race للتسجيل والتشغيل والإلغاء والتنزيل على Staging.
25. تعتمد عقود الناشرين مصفوفة DRM وOffline وWatermark قبل نشر المحتوى المرخص.

---

# القرار النهائي

## وضع مرحلي بلا DRM للفيديو الذي نملكه

يمكن إطلاق طبقة **حماية وصول غير DRM** مؤقتًا للفيديو الأصلي الذي نملك حقوقه ولا يفرض عقده DRM. تظل ملفات R2 في bucket خاص، ويصدر Worker جلسة مشاهدة قصيرة بعد فحص الحساب والاشتراك والطفل والجهاز وحد التشغيل المتزامن. يتلقى المشغل مسار Worker فقط مع Authorization قصير المدة؛ لا يتلقى R2 URL أو R2 credentials، وتُخدم Range requests من Worker مع `Cache-Control: private, no-store`.

هذا يقلل مشاركة الروابط والوصول غير المصرح به ويمكّن الإبطال، لكنه **لا يمنع مستخدمًا مُخوّلًا من استخراج الفيديو أو تسجيل الشاشة**؛ لذلك لا يستخدم للمحتوى المرخص عالي القيمة ولا يسمى DRM. HLS المحمي من دون DRM يحتاج حماية منفصلة للـmanifest والـsegments، ولذلك يبدأ هذا الوضع التجريبي بملف فيديو خاص متدفق عبر Worker ولا يقدّم تنزيل فيديو Offline.

## قرار Google Play

تحديث الخطة من Free إلى Family أو Family Plus لا يأتي من Flutter أو من body يرسله العميل. التطبيق يشتري عبر Google Play، ثم يرسل token إلى endpoint خادمي يُفعّل فقط بعد إعداد Google Play Developer API وservice-account secret. يحتفظ النظام ببصمة SHA-256 للـpurchase token لأغراض idempotency ولا يخزن التوكن الخام. قبل إعداد هذا التحقق، لا يصدر أي entitlement مدفوع.

## الصور والقصص والصوت Online

```text
Private R2 Origin
+ Server-side Entitlement Check
+ Short-lived Token
+ HTTPS Streaming
+ Memory Cache أو Encrypted Cache
```

لا تحتاج طبقة تشفير مخصصة داخل Flutter في أثناء النقل، لكنها تحتاج حماية وصول كاملة ولا يجوز تخزينها مكشوفة طويلًا.

## الفيديو المدفوع أو المرخص Online/Offline

```text
Private Origin
+ HLS/DASH
+ Widevine / FairPlay / PlayReady عند الحاجة
+ DRM License Server
+ HDCP + Secure Playback Surface
+ Short Playback Lease / Offline DRM License
```

الروابط المؤقتة أو Custom AES Segments وحدها ليست بديلًا مساويًا لـDRM.

## القصص والصوت والكوميكس Offline

```text
Private R2
+ Download Authorization
+ Signed Manifest
+ Stream Download
+ AES-256-GCM Chunks with unique Nonces + AAD
+ Device-bound Wrapped CEK
+ Signed Offline License
+ In-memory Decryption
+ Live License Check before content delivery
```

## القاعدة الأساسية

> المحتوى Online يحتاج حماية وصول، والفيديو عالي القيمة يحتاج DRM، وكل محتوى يبقى على الجهاز يحتاج تشفيرًا وترخيصًا مرتبطين بالجهاز والاستحقاق.
>
> لا توجد حماية تمنع الاستخراج أو Screenshot أو الكاميرا الخارجية بنسبة 100%؛ التصميم يقلل المخاطر، يقصر مدة الوصول، ويمكّن الإبطال والتدقيق دون استخدام PII للطفل.
