# ROOT — Flutter is env-driven via --dart-define (see app_environment.dart)
# Base URLs are in app_environment.dart (_productionHost=api.majarra.app).
# Runtime overrides via --dart-define=API_BASE_URL=https://...

# Dashboard API secrets: see dashboard/api/.env.example
# Dashboard front vars:  see dashboard/front/.env.example
