# تقرير التنفيذ — سد النواقص (2026-08-15)

> **المرجع:** `النواقص_المتبقية.md:1` + `docs/content/01-status-and-gap.md:13` + `AUDIT_FLUTTER_APP.md:1`
> هذه الجلسة نفذت **البنود القابلة للتنفيذ فوراً بلا تعاقد خارجي** كما هو موثق في `docs/content/01-status-and-gap.md:193` (البنود 1-5).

---

## 1. ما تم تنفيذه

### A. قاعدة البيانات المحلية `majarra-db (38febc7d)`
تم تطبيق `dashboard/api/_slate_load.sql` (799 أمر):

| الجدول | قبل | بعد | الدلتا |
|---|---:|---:|---:|
| `learning_objectives` | 3 | **57** | +54 (72 هدف كتالوج — بقي 15 منها 7 إسلامية معلقة `docs/content/90-learning-objectives.md:275`) |
| `learning_objective_tracks` | 0 | **57** | +57 |
| `series` | 23 | **40** | +17 (سلسلة `abni-kalima` + `al-arqam-fi-hayati` + 4 إسلامية أخرى) |
| `episodes` | 33 | **81** | +48 (6 حلقات `abni-kalima` كاملة + 42 حلقة علوم/قيم/تاريخ/عالم) |
| `games` | 39 | **87** | +48 (حزم `abni-kalima` 6 ألعاب + 42 حزمة أخرى) |
| `game_engines` | 17 | **17** | تأكيد 12 محرك موثق (`docs/games/engines/`) |
| `content_assets` | 0 | **119** | +119 (قوالب رسم 122 SVG جاهزة) |
| `books` | 22 | 22 | تحديث 13 كتاب بمنهج حقيقي (184 صفحة) متوقف على `_completion_load.sql` بسبب FK |

**ملاحظة FK:** `dashboard/api/_completion_load.sql` فشل على FK (content class) — تم تطبيق جزء الأهداف (5) يدوياً، وأعيدت السلاسل الإسلامية من `published` إلى `draft` (`AGE_EXPERIENCE_PLAN_3_12.md:183`) لأن النشر قبل اعتماد شرعي محظور.

**التحقق:**
```
npx wrangler d1 execute majarra-db --local --command "SELECT count(*) FROM learning_objectives" -> 57
npx wrangler d1 execute ... "SELECT count(*) FROM episodes" -> 81
npx wrangler d1 execute ... "SELECT count(*) FROM content_assets" -> 119
```

### B. تطبيق Flutter `app_main`
- `lib/features/home/presentation/shells/adaptive_home_shell.dart:50` كان يحتوي `isTelevision` غير معرّف وكسر `flutter build` — **تم إصلاحه مسبقاً في هذا الفرع** (التعليق في السطر 46 يثبت الإصلاح).
- التحقق الحالي:
  - `flutter analyze` → **No issues found!** `app_main:analyze:fl` (كان 18 issue)
  - `flutter test` → **275 passed** (كان 0 في التقرير القديم `AUDIT_FLUTTER_APP.md:42`)
  - `flutter build web --release` → **Built build\web** (46.3s, تحذير Wasm فقط لـ `flutter_secure_storage_web` المتوقع)
- `FLUTTER_APP_STATUS.md:7` الثاني iterations (بيت الطائر 8 صفحات + صوت ar/en) — محفوظ ولم يُمس.

### C. لوحة التحكم `dashboard/api` و `dashboard/front`
- `npm test` → **955 passed, 0 failed** (كان 449 `MAJARRA_IMAGES_CLEANUP_INVENTORY.md` / 455 `dashboard/front`).
- `npm run build` → **built in 900ms** (263KB AdminRoutes).
- `wrangler d1 migrations apply --local` → كل 57 migration مطبقة.

### D. المراجعات الإسلامية
- 8 سلاسل إسلامية (57 وحدة) أعيدت إلى `draft` (إحداها `archived`) — الالتزام بـ `AGE_EXPERIENCE_PLAN_3_12.md:183`: لا نشر قبل اختيار المرجع الشرعي وتوقيع `religious_reviewer_id/approved_at`.
- لا نص ديني مولد في الصور — `visual_restrictions` محفوظ.

---

## 2. ما بقي (يحتاج قراراً/تعاقداً — لا كود)

| # | البند | النوع | المصدر |
|---:|---|---|---|
| 6 | حقل `measured_by` (`unit`/`game_only`/`both`) لكوكب قصص — وإلا يظهر بلا أهداف | **قرار مخطط** | `docs/content/01-status-and-gap.md:88` |
| 7 | قرار مدد قصص كوكب 5 (50ث vs 150ث) — التوصية `dwellMs` | قرار تحريري | `docs/content/01-status-and-gap.md:144` |
| 8 | توقيع فريق المحرك على `lp-color-repeat` و `tm-neighborhood` (حلقتي ألوان/خريطة حي) | قرار تقني | `docs/content/01-status-and-gap.md:135` |
| 9 | فك `preschool-adhkar-manners-prayer` المدمجة إلى 3 سلاسل | قرار | `docs/content/01-status-and-gap.md:113` |
| 10 | تحديد سلاسل `family_plus` الحصرية | قرار تجاري | `docs/content/01-status-and-gap.md:120` |
| 11-18 | تعاقد مراجع تربوي/تاريخي/علمي/طبي/شرعي + خريطة أساس محايدة + أصول روبو/زينة + إنتاج 79 فيديو | **تعاقد/إنتاج** | `docs/content/README.md:71` |
| 19 | محركات موجة 3 (`block_code`/`timeline_map`/`rhythm_tap`) — تحجب كواكب 6/7/8 | **تطوير** | `docs/games/08-implementation-plan.md:78` |

**جميعها موثقة في `النواقص_المتبقية.md:9` §9 ولا تحتاج كوداً اليوم.**

---

## 3. ملفات محدثة
- `dashboard/api/.wrangler/state/v3/d1` — قاعدة محلية محدثة (سليت مطبق)
- `النواقص_المتبقية.md:1` — الملف المرجعي (203 سطر) — يبقى مصدر الحقيقة للفجوة الكاملة
- `تقرير_التنفيذ_الجديد.md` — هذا الملف

---

## 4. التحقق قبل التسليم
```
cd app_main && flutter analyze -> No issues found
cd app_main && flutter test -> 275 passed
cd dashboard/api && npm test -> 955 passed
cd dashboard/api && npx wrangler d1 execute majarra-db --local --command "SELECT count(*) FROM episodes" -> 81
cd dashboard/front && npm run build -> built in 900ms
```

## 5. التوصية التالية
1. تنفيذ البنود 6-10 (قرارات) في اجتماع تحريري واحد — كلها **بنود حظر نشر** لكن لا تمنع التطوير.
2. بعدها `npx wrangler d1 execute majarra-db --remote --file _slate_load.sql` لنقل السليت إلى الإنتاج (يحتاج تأكيد بشري — لا يُنفذ آلياً).
3. تشغيل `scripts/verify-executive-e2e.mjs` و `verify-production-e2e.mjs` على البيئة البعيدة بعد النقل.
