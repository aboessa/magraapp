# مشكلة مدد قصص كوكب 5 — التفصيل الكامل

> **الكوكب:** 05 قصص `docs/content/planets/05-qisas/README.md:1`
> **السلاسل المتأثرة:** `a-calm-tale` (حكاية هادئة — 4 قصص) + `bedtime-stories` (حكايات قبل النوم — 6 قصص) = **10 قصص**
> **المصدر الأساسي:** `docs/content/01-status-and-gap.md:144` + `docs/content/planets/05-qisas/00-story-page-model.md:56` + `ACT-S1_PRODUCTION_HANDOFF.md:222`

---

## 1. ما هي "المدد"؟

كل صفحة في قصة لها 3 أزمنة مختلفة في النموذج `00-story-page-model.md:56`:

| الحقل | المعنى | المصدر | مثال (صفحة واحدة) |
|---|---|---|---|
| `durationMs` | مدة ملف الصوت نفسه (السرد المنطوق) | **مستخرجة تلقائياً من الـWAV** — لا تُكتب يدوياً | 5800ms |
| `autoTurnAfterMs` | متى يقلب التطبيق للصفحة التالية تلقائياً | = `durationMs` + انتظار + انتقال | 13200ms في النموذج الحالي |
| `duration_seconds` في جدول `stories` | مجموع `autoTurnAfterMs` لكل الصفحات / 1000 — هي "مدة القصة" المعلنة في الفهرس ولوحة التحكم | محسوبة آلياً، ليست حقل إدخال | 150 ث |

**القاعدة في النموذج `00-story-page-model.md:100`:**
- انتظار بعد الصوت: 500–1000ms (900–1000ms في مسار النوم)
- انتقال: 250–400ms (400ms في مسار النوم)

---

## 2. القياس الفعلي — أين التناقض؟

تم قياس مدد الصوت الفعلية من ملفات `act-s1` (بيت الطائر — 8 صفحات) كمرجع `ACT-S1_PRODUCTION_HANDOFF.md:70`:

| # | النص (عربي) | `durationMs` الفعلي | الجملة |
|---|---|---:|---|
| 1 | هذا زُغب. بيته عشّ صغير. | 5480 | 5 كلمات |
| 2 | اليوم يريد أن يطير قليلاً. | 4040 | 6 كلمات |
| 3 | طار إلى الشجرة. ثم أبعد قليلاً. | 5960 | 7 كلمات |
| 4 | هبّت ريح خفيفة. فتحرّكت الأوراق. | 7160 | 6 كلمات |
| 5 | نظر زُغب إلى بيته. وعرف طريقه. | 6560 | 7 كلمات |
| 6 | فطار إلى العشّ. وأمّه تنتظره. | 5720 | 6 كلمات |
| 7 | جلس قريباً منها. والريح هدأت. | 6600 | 6 كلمات |
| 8 | والآن ينام زُغب في بيته. | 5680 | 6 كلمات |
| **المجموع** | | **47.2 ثانية** | |

نفس القصة بالإنجليزية: **47.0 ثانية** — نفس المدة تقريباً رغم أن الإنجليزية 83 كلمة/دقيقة والعربية 53 كلمة/دقيقة (فرق 57%). السبب أن `wpm` لا ينتقل بين اللغات — المعدل المعاير **3.51 حرف عربي/ثانية** `ACT-S1_PRODUCTION_HANDOFF.md:225` و `docs/content/narration-rate-calibration.json`.

بتطبيق نفس سرعة السرد على كل قصص الكوكب:

| السلسلة | عدد القصص | مجموع `durationMs` (صوت فقط) | المدة المعلنة في `docs/content/planets/05-qisas/README.md` وفي `series.sort_order` | النسبة | الحالة |
|---|---|---:|---:|---:|---|
| **حكاية هادئة** | 4 | **~50 ثانية** (8 صفحات × 5–7ث) | **150–170 ث** | **3.2×** | 🔴 كل القصص الأربعة |
| **حكايات قبل النوم** | 6 | **123–127 ثانية** (8–10 صفحات × 6–7ث) | **240–280 ث** | **2.1×** | 🔴 كل القصص الستة |
| **المجموع** | **10** | — | — | — | 🔴 **العشر كلها** |

المصدر: `docs/content/01-status-and-gap.md:144`

**الخلاصة:** المدة المعلنة أكبر من مجموع أصوات الصفحات بـ **2–3 أضعاف** — التناقض ليس في قصة واحدة بل في الكوكب كله.

---

## 3. لماذا حدث؟ — السبب الجذري

1.  **المدد كُتبت قبل القياس** `ACT-S1_PRODUCTION_HANDOFF.md:222`: كل ملفات القصص الـ15 كانت تعلن سرعة `wpm`، وكلها تخطّط مدداً تعني ~62% منها. خطأ منهجي في الوحدة، ليس في مؤلف واحد.
2.  **`duration_seconds` كُتب كحقل كتالوج** بينما النموذج `00-story-page-model.md:1` يعرّف نفسه "العقد بين المحتوى والتطبيق" ولا يذكر `duration_seconds` بين حقول الصفحة — كان حقل عرض لا حقل تشغيل، وصُحح في 10 ملفات (160ث → 54ث في بيت الطائر).
3.  **لم يُحسب وقت تأمل الصورة** — قصص هذا الكوكب تعليماتها `<8 كلمات/صفحة` وصورتها هي المحتوى. الطفل يحتاج 8–15 ثانية ينظر للصورة بعد انتهاء السرد، وهذا الوقت **غير موجود** في `durationMs` ولا في `autoTurnAfterMs` الحالي (الذي يضيف 900ms فقط).

---

## 4. الحلول الثلاثة — تقييم `01-status-and-gap.md:153`

| الخيار | ماذا تفعل | الأثر | الحكم |
|---|---|---|---|
| **A. رفع `durationMs`** | تجعل كل صفحة ~20 ثانية صوت | مع جملة 8 كلمات تحتاج سرد بسرعة 0.4 كلمة/ثانية — **غير معقول**، يكسر `narration-rate-calibration` وسيُسمع ممدوداً ومملاً | 🔴 **مرفوض** |
| **B. خفض المدة المعلنة** | تغيّر 150→50 و 240→120 | يخالف **دليلي السلسلتين** وعمود `duration_seconds` في فهرس الكواكب `docs/content/planets/05-qisas/README.md` و `docs/content/01-status-and-gap.md` — وسيظهر أن القصة أقصر من معيار `قصة قبل النوم` (المتوقع 4–6 دقائق هدوء) | 🔴 **مرفوض** |
| **C. إضافة `dwellMs`** | وقت تأمل الصورة — حقل جديد | يحافظ على سرعة السرد الصحيحة وعلى المدة المعلنة، ويفصل **زمن السمع** عن **زمن النظر** | ✅ **الموصى به** |

---

## 5. الحل الموصى به — `dwellMs` بالتفصيل

### 5.1 التعريف

```json
// قبل (النموذج الحالي 00-story-page-model.md:56)
{
  "pageNumber": 4,
  "durationMs": { "ar": 5800 },
  "autoTurnAfterMs": { "ar": 6700 }   // 5800 + 500 انتظار + 400 انتقال
}

// بعد
{
  "pageNumber": 4,
  "durationMs": { "ar": 5800 },       // لا يمس — مستخرج من WAV
  "dwellMs": { "ar": 10000 },         // ⭐ جديد: 10ث تأمل الصورة
  "autoTurnAfterMs": { "ar": 16700 }  // = 5800 + 10000 + 900 (نوم) + 400
}
```

| الحقل | هل هو صوت؟ | أين يُحفظ؟ | يُقاس كيف؟ |
|---|---|---|---|
| `durationMs` | نعم — طول الـWAV | `story_page_localizations.duration_ms` | `tools/tts/inspect-wav.mjs` |
| `dwellMs` | **لا** — بيانات تشغيل فقط، ليس صمتاً في الملف | `story_page_localizations.dwell_ms` (جديد) | قرار تحريري: 8–15ث حسب كثافة الصورة |
| `autoTurnAfterMs` | — | محسوب: `duration + dwell + wait + transition` | لا يُكتب يدوياً |

**تحذير صريح `ACT-S1_PRODUCTION_HANDOFF.md:228`:** لا تحرق `dwellMs` كصمت في الـWAV — سيُصبح زمن الملف كاذباً على كل مقياس (`durationMs`، `narration-rate`، `timing_cues`).

### 5.2 القيم المقترحة حسب السلسلة

| السلسلة | `dwellMs` / صفحة | `autoTurn` كامل / صفحة | مجموع 8 صفحات + انتظار/انتقال | يطابق المعلن 150–170 / 240–280؟ |
|---|---:|---:|---:|---|
| حكاية هادئة (صور هادئة) | 12 ث | 5800+12000+900+400=19.1ث | **~153 ث** | ✅ 150–170 |
| حكايات قبل النوم (صور مفصلة) | 15 ث | 5800+15000+900+400=22.1ث | **~177 ث** | ⚠️ يحتاج 10–12 صفحة للوصول 240ث — أو `dwell 18ث` |

للوصول لـ240ث في `bedtime-stories` (10 صفحات): `dwell 15ث` يعطي 221ث، `dwell 18ث` يعطي 251ث — اختيار تحريري.

### 5.3 أين يُعدل؟

| الملف | التغيير |
|---|---|
| `docs/content/planets/05-qisas/00-story-page-model.md:70` | إضافة صف في جدول الحقول: `dwellMs | وقت تأمل الصورة بعد الصوت | ✅ في مسار النوم (8–15ث)` |
| `dashboard/api/migrations/00xx_dwell_ms.sql` | `ALTER TABLE story_page_localizations ADD COLUMN dwell_ms INTEGER CHECK (dwell_ms BETWEEN 0 AND 30000)` |
| `dashboard/api/src/routes/stories.ts` | إرجاع `dwell_ms` مع `duration_ms` وحساب `autoTurnAfterMs` |
| `app_main/lib/features/reader/...` | قراءة `dwellMs` واستخدامه في مؤقت التقليب التلقائي |
| 10 ملفات قصص `docs/content/planets/05-qisas/**/story-*.md` | كتابة `dwellMs` لكل صفحة (80 صف) |

---

## 6. الأثر إذا لم يُحل

- **QA يفشل:** `tools/content/audit-narration-pacing.mjs` سيُبلغ تناقض 25 حالة في `narration-pacing-baseline.json` (مذكور في `ACT-S1_PRODUCTION_HANDOFF.md:246` — بقيت 25 تناقضاً مسجلة بدل تصحيحها بالتخمين).
- **التطبيق يكذب:** لو استُخدم `durationMs` المرفوع، سرعة السرد ستُحسب خطأً (3.51 حرف/ث ستصبح 1.2) وستُفقد معايرة `narration-rate-calibration.json`.
- **تجربة الطفل:** إما تقليب سريع (50ث لقصة يُتوقع 3 دقائق — يشعر أنها مقطوعة) أو سرد ممدود بشكل مزعج.

---

## 7. ما طُلب منك

حتى الآن **لا قرار اتُخذ** — الحقل غير موجود. البند #7 في `docs/content/01-status-and-gap.md:170` يصنفه "يحتاج قرارك" (⚠️ قرار تحريري)، والتوصية مكتوبة لكن التعديل على `00-story-page-model.md` لم يُنفذ.

**هل توافق على إضافة `dwellMs` كما هو موصوف؟** بمجرد الموافقة يُنفذ migration + تحديث النموذج + إعادة حساب العشر قصص في جلسة واحدة (لا إنتاج صوتي جديد).
