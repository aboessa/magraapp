# أودت مجرة الشامل — 2026-09-23

**النوع:** تشغيل فعلي لكل مجموعات الفحص + استعلام قاعدة البيانات + عدّ الأنماط برمجيًّا + قراءة الكود في الطبقات الثلاث.
**الفرع المفحوص:** `chore/wip-batches-1-68` @ `cccb862` · شجرة العمل **غير نظيفة** (250 مسارًا)، والأودت يعكس شجرة العمل لا HEAD.
**يخلف:** `AUDIT_FULL_2026.md` (2026-08-26 + 68 دفعة حتى 08-31). كل ادّعاء فيه أُعيد قياسه، وسبعةٌ منها **لم تصمد** (§13).

---

<!-- section-status: superseded -->
> **على حرس `docs-classified` (`DOCS-201`):** هذا الملف يذكر Supabase وRevenueCat
> وMulti-DRM لأنه **يوثّق غيابها** — §8.2 و§16.3 تقيسان أن صفر مطابقة لها في
> المانيفستات الأربعة. الوسم أعلاه إقرارٌ بذلك، لا وصفٌ لمكدّسٍ مقترح.

## §0 كيف يُقرأ هذا الملف، وحدوده

كل رقم أدناه ناتج أمرٍ شُغِّل في 2026-09-23، لا نقلًا من تقرير. وأوامر إعادة التوليد مذكورة مع كل قسم.

**ما لم أستطع التحقق منه، ولا يجوز افتراضه:**

| ما لم يُفحص | السبب | الأثر على القراءة |
|---|---|---|
| **الإنتاج** (D1 وR2 وKV وDurable Objects على `api.majarra.app`) | لا توكن نشر في المستودع | كل أرقام قاعدة البيانات في §5 **محلية**، والمحلية مبنيّة من الترحيلات لا نسخةً من الإنتاج |
| الأجهزة الحقيقية (هاتف/تابلت/TV) | لا جهاز | كل ملاحظات الوصول والأداء مبنيّة على الكود |
| `flutter build appbundle` | يفشل قبل أن يبدأ — انظر `BUILD-201` | لا حجم حزمة ولا توقيع |
| متاجر التطبيقات | لا وصول | لا حالة مراجعة |

**تنبيه منهجي على قاعدة البيانات:** قاعدة D1 المحلية آخر كتابةٍ فيها **2026-08-31**، وهي نتاج `npm run migrate:local` على قاعدةٍ فارغة — أي أن محتواها هو ما تُدخِله الترحيلات، لا ما في الإنتاج. و`docs/content/CONTENT-COMPLETION-LEDGER-2026-09-03.md` يعلن أن أرقامه من «استعلامات D1 الحية»، وهو يخالف المحلية في ثلاثة أعداد (§7). حيث اختلفا ذكرتُ الاثنين ونسبتُ كلًّا إلى مصدره.

---

## §1 خط الأساس المقيس

### 1.1 المستودع

| البند | القيمة |
|---|---|
| الفرع / HEAD | `chore/wip-batches-1-68` @ `cccb862` — «ci: freeze the two debts that were measurably growing» (2026-09-02) |
| مقابل `master` | **8 commits أمامه، صفر خلفه** — أي أن `master` لا يحمل أيًّا من الدفعات الـ68 |
| الفرع الافتراضي على الأصل | `origin/HEAD → origin/main` |
| شجرة العمل | **250 مسارًا**: 172 معدَّلًا · 77 غير متتبَّع · 1 محذوف |
| آخر commit | 2026-09-02 · **وآخر تعديل ملف: اليوم 2026-09-23 الساعة 16:03** (19 صفحة لوحة) |

### 1.2 نتائج الفحوص — ثلاث مجموعات خضراء وواحدة حمراء

| الفحص | الأمر | النتيجة |
|---|---|---|
| Flutter تحليل | `flutter analyze --fatal-warnings` | 🔴 **18 خطأً** · خروج 1 |
| Flutter اختبارات | `flutter test --coverage` | 🔴 **736 اختبارًا · 6 إخفاقات** |
| Worker أنواع | `npm run typecheck:types` | ✅ نظيف |
| Worker اختبارات | `npm test` | ✅ **1456 · صفر فاشل** |
| اللوحة أنواع | `npx tsc -b` | ✅ نظيف |
| اللوحة اختبارات | `npx vitest run` | ✅ **354 · 27 ملفًا · صفر فاشل** |

### 1.3 بوابات CI — ثلاث حمراء

| البوابة | النتيجة | المقيس |
|---|---|---|
| `docs-classified --check` | ✅ | 36 وثيقة جذر · 36 مُصنَّفة · 16 مصدر + 20 تاريخي |
| `mojibake-scan --check` | ✅ | لا تشويه جديد (الدَّين مُجمَّد: `admin.ts` 43 سطرًا، `adminUsers.ts` 28) |
| `feature-matrix --check` | ✅ | 123 مسارًا · 123 صفحة محلولة · صفر مشاكل |
| `dart-literal-strings --check` | 🔴 | `coloring_home_v2_live.dart` 24 مقابل 23 · `studio_home_view.dart` 11 مقابل 10 |
| `zero-fallback-scan --check` | 🔴 | **127 موضعًا مقابل خطّ أساس 102** · 5 صفحات غير مُسجَّلة أصلًا |
| `assets-declared --check` | 🔴 | عشرات مسارات `Image.asset` في `lib/` بلا ملف مُبندَل |

**النتيجة المباشرة:** وظيفتا `flutter` و`feature-matrix` في `ci.yml` تفشلان، و`deploy` يعتمد عليهما بـ`needs`. أي أن **مسار النشر مغلق اليوم** — لو كان CI يعمل على هذا الفرع أصلًا، وهو لا يعمل (`OPS-201`).

---

## §2 الخلاصة التنفيذية

### الحكم في ثلاث جمل

الهندسة التي أُنجزت في الدفعات الـ68 حقيقية وصامدة: الخادم 1456 اختبارًا أخضر بلا استثناء، واللوحة 354 وبصفر تعطيل أنواع، ومنظومة الترخيص وحدود الباقات والرقابة الأبوية تعمل من طرف إلى طرف في الكود. **لكن ثلاثة أسابيع من العمل غير المُدمَج (2026-09-02 → اليوم) أدخلت انحدارًا يمنع التطبيق من الترجمة أصلًا**، وهو انحدار لم يره أحد لأن العمل يجري على فرعٍ لا يشغّل CI عليه شيء. والقيد الحاكم على الإطلاق لم يتغيّر: ليس الكود، بل **160 وحدة محتوى غير ممثَّلة في الكتالوج الإنتاجي** ثم **التجارة**.

### أعداد البنود

| الأولوية | العدد | المعنى |
|---|---:|---|
| **P0 — موانع** | **7** | بناءٌ مكسور، أو أمان، أو سلامة بيانات |
| **P1 — أساسي** | **11** | |
| **P2 — جودة** | **9** | |
| **مجموع البنود الجديدة** | **27** | |
| قرارات مالك | 4 | لا تُحلّ بالكود |

### الستّة التي تُصلَح أوّلًا، بهذا الترتيب

1. **`BUILD-201`** — سطرٌ واحد: `onboarding_flow_page.dart:233` ينادي دالّة غير موجودة على `AuthGuard`. التطبيق **لا يُترجم**، و4 من الإخفاقات الـ6 أثرٌ لهذا السطر وحده. حجمه XS وبدونه لا معنى لأي «أخضر» في التطبيق.
2. **`SEC-201`** — الخروج لا يمسح بيانات الاعتماد إذا فشل مسحُ حالة الـonboarding. التوكنات تبقى على الجهاز بعد تسجيل الخروج. الاختبار الذي يرصد هذا **موجود ويفشل**.
3. **`GIT-201`** — 460 ميغابايت فيديو في `dashboard/api/private/` **غير مُتجاهَلة**، مع 77 مسارًا غير متتبَّع. أيّ `git add -A` واحد يرفعها.
4. **`OPS-201`** — العمل على فرعٍ لا يشغّل CI، و`master` بلا شيء من الدفعات الـ68. ثلاثة أسابيع بلا بوابة تحقُّق واحدة.
5. **`SEC-202`** — دخول اللوحة على حصّة 600/دقيقة لا 5/دقيقة: مهاجمٌ مجهول يحصل على 600 محاولة كلمة مرور في الدقيقة لكل عنوان.
6. **`DATA-201`** — تسجيل المحتوى في الإنتاج يجري بـSQL يدويٍّ **غير متتبَّع** (`tools/ops/link-episodes.sql`، 66 سطر INSERT) خارج سجلّ الترحيلات — وهو بالضبط ما وُجد السجلّ لمنعه.

### جهوزية الإطلاق

| البُعد | الحالة | الدليل المُوجَز |
|---|---|---|
| بناء التطبيق | 🔴 | `flutter analyze` **18 خطأً**؛ خطأٌ واحد في `lib/` يمنع الترجمة |
| مجموعات الاختبار | 🟡 | الخادم 1456/1456 واللوحة 354/354 خضراء؛ التطبيق 730/736 |
| CI وبوابة الإصدار | 🔴 | ثلاث بوابات حمراء، والفرع لا يشغّل CI أصلًا |
| أمان الـAPI | 🟡 | 211 معالِجًا مُطفِّرًا · 196 بحرس صلاحية · **صفر بلا حساب**؛ ويبقى حدّ دخول اللوحة وكتابة مجهولة بلا حصّة |
| التصاريح (RBAC) | ✅ | حرس خادميّ فائل-مغلق + تطابق 123↔123 مسار/صلاحية بنقطة إنفاذ واحدة |
| الترخيص Offline | 🟡 | Ed25519 موقَّع بحدود ذرّية في الـDO؛ و**`ENC-002` لم يبدأ**: لا مفتاح محتوى، الملف يهبط صريحًا على القرص |
| الرقابة الأبوية | 🟡 | مفروضة للفيديو (بدءًا ونبضةً)، و**غائبة كليًّا عن الصوت والقراءة** |
| محتوى يصل إلى طفل | 🔴 | **160 وحدة من 208 غير ممثَّلة في الكتالوج**؛ صفر حلقة منشورة؛ صفر قصة منشورة محليًّا |
| الوسائط | 🟡 | 33 فيديو أُنتج ورُبط (تقدّم حقيقي) لكنها نسخ 34–44 ثانية لا الحلقات الكاملة؛ 307 أصلًا كلها بحجم صفري |
| التجارة | 🔴 | مسار شراء Android حقيقي بتحقّق خادميّ، و**صفر سعر** في `plan_pricing` |
| التوطين | 🔴 | ~2,250 نصًّا عربيًّا محفورًا مقابل 257 مفتاحًا؛ اللغة مثبَّتة وصفّ الإعداد للقراءة فقط |
| التوثيق | 🟡 | البوابة خضراء، و**وثيقتان `source` تصفان نظامًا آخر** |
| شجرة العمل | 🔴 | 250 مسارًا غير مُدمَج، 460 ميغابايت غير متجاهَلة |

**الحكم: غير قابل للإطلاق، والمانع الأول صار هندسيًّا بعد أن لم يكن.** في الأودت السابق كان القيد إنتاجَ الوسائط؛ اليوم صار قبله **إصلاحُ البناء وإدماج ثلاثة أسابيع من العمل المعلَّق**.

---

## §3 موانع البناء والتحقُّق — `BUILD-*` و `OPS-*`

### BUILD-201 — التطبيق لا يُترجم: دالّة غير معرَّفة في كود الإنتاج

**الأولوية:** P0 · **المنطقة:** تطبيق/بناء · **الحجم:** XS

**الدليل**

```
error - The method 'setOnboardingJourneyInProgress' isn't defined for the type
        'AuthGuard' - lib/features/onboarding/presentation/pages/onboarding_flow_page.dart:233:30
```

و`app_main/lib/app/router/auth_guard.dart` لا يحمل هذه الدالّة. الموجود فيه:

| السطر | العضو |
|---|---|
| 68 | `bool _hasCompletedOnboarding = false;` |
| 95 | `bool get hasCompletedOnboarding` |
| 240 | `void setHasCompletedOnboarding(bool value)` |

**الأثر، وهو أوسع من سطر**

أربعة من الإخفاقات الستّة **أثرٌ لهذا السطر وحده**، لأن أي اختبار يستورد صفحة الـonboarding مباشرةً أو عبر الموجّه يفشل في الترجمة لا في التوكيد:

```
Compilation failed for testPath=.../auth_session_reactivity_test.dart:
  lib/features/onboarding/presentation/pages/onboarding_flow_page.dart:233:30: Error: ...
```
ونفسها لـ`onboarding_flow_page_test.dart` و`orientation_gate_test.dart` و`onboarding_journey_integration_test.dart`.

ويحمل الاختبار التكاملي 17 خطأً خاصًّا به على واجهةٍ أوسع لم تُنفَّذ قط:

| الرمز المطلوب | المواضع في `test/onboarding_journey_integration_test.dart` |
|---|---|
| `AuthGuard.familyOnboardingStatus` | 192, 217, 240, 262, 288, 313 |
| `FamilyOnboardingStatus` (نوع غير موجود) | 192, 217, 240, 262, 288, 313 |
| `AuthGuard.onboardingJourneyInProgress` | 241, 263, 289, 314, 340 |

**التشخيص**

هذه ليست غلطة مطبعية. صفحة الإنتاج والاختبار التكاملي كُتبا مقابل **واجهة `AuthGuard` موسَّعة** (حالة onboarding للأسرة + علَم «الرحلة جارية») لم تُلحَق بـ`auth_guard.dart` قط، أو أُلحقت ثم رُجِعت. والأخطر أن `.kiro/specs/app-foundation-family-journey/tasks.md` يُعلن المهمّتين 32 و33 (صفحة الرحلة + الاختبار التكاملي الكامل) **مُنجَزتين `[x]`** على كودٍ لا يُترجم.

**القرار المطلوب**: إمّا إكمال واجهة `AuthGuard` الموسَّعة (وهي ما تفترضه ثلاثة ملفات)، أو تقليص الصفحة والاختبار إلى `hasCompletedOnboarding` القائمة. الأوّل أوسع والثاني أسرع، والاختيار قرار تصميم لا إصلاح آلي.

---

### OPS-201 — ثلاثة أسابيع عملٍ على فرعٍ لا يشغّل CI، و`master` فارغ منها

**الأولوية:** P0 · **المنطقة:** عمليات/CI · **الحجم:** S

**الدليل**
- `git rev-list --left-right --count master...HEAD` → `0  8`. أي `master` **صفر** أمام الفرع و**8 خلفه**: لا شيء من الدفعات الـ68 في الفرع الرئيسي.
- `origin/HEAD → origin/main`، والفرع الافتراضي على GitHub هو `main`.
- `.github/workflows/ci.yml:8-12`: `push: branches: [master, main]` و`pull_request: branches: [master, main]`. والفرع الحالي `chore/wip-batches-1-68` **ليس منهما**، فلا `push` يُشعِل CI.

**الأثر**
البوابات التسع التي بُنيت بعناية — وفيها بوابات تشغّل `--self-test` أوّلًا لأن «بوابةً حسابها معطوب تُبلّغ رقمًا مريحًا» — لم تُشغَّل على أيّ من تعديلات الأسابيع الثلاثة. وهذا تفسيرٌ كافٍ لكيف مرّ `BUILD-201`: خطأُ ترجمةٍ في كود الإنتاج بقي ثلاثة أسابيع بلا أن يوقعه شيء.

**ملاحظة**: لو كان هناك PR مفتوح إلى `main` فسيشتغل `pull_request`. لم أستطع التحقق (لا `gh` مُصادَق في البيئة)، لكن كون `master` صفرًا أمام الفرع يعني أن شيئًا لم يُدمَج على أي حال.

---

### GIT-201 — 460 ميغابايت فيديو غير متجاهَلة في شجرةٍ فيها 77 مسارًا غير متتبَّع

**الأولوية:** P0 · **المنطقة:** عمليات/git · **الحجم:** XS

**الدليل** — `git check-ignore -v` على كلٍّ:

| المسار | الحجم | الحالة |
|---|---|---|
| `dashboard/api/private/` (33 × `1080p.mp4`) | **460 ميغابايت** | 🔴 **NOT-IGNORED** |
| `content/planets/ep01_mercury.mp4` | — | 🔴 NOT-IGNORED |
| `tools/ops/thumbs-tmp/` | — | 🔴 NOT-IGNORED |
| `dashboard/api/NUL` (ملفٌ باسمٍ محجوز في Windows) | — | 🔴 NOT-IGNORED |
| `.backups/` | — | ✅ IGNORED (`.gitignore:195`) — أُصلح في الدفعة 1 |

`git status --porcelain dashboard/api/private` يُخرج سطرًا واحدًا، أي أن المجلد كلّه يظهر كمسار غير متتبَّع جاهز للإضافة.

**الأثر** `git add -A` واحد يرفع 460 ميغابايت وسائط إلى تاريخ المستودع، وهو ما لا يُنزَع بعدها إلا بإعادة كتابة التاريخ. والدرس نفسه مسجَّل في §15 من الأودت السابق بعد حادثة حذفٍ آليٍّ على شجرةٍ غير متتبَّعة.

**الإصلاح** قواعد `.gitignore` للأربعة، ثم `git check-ignore` للتأكيد. XS.

---

### QA-201 — بوابتان تفشلان على **تحسُّنٍ غير مُسجَّل**، لا على انحدار

**الأولوية:** P2 · **المنطقة:** اختبارات/CI · **الحجم:** XS

بوابتان من الثلاث الحمراء تفشلان في الاتجاه الجيد، وهو تصميمٌ مقصود يستحق التمييز حتى لا يُقرأ الأحمر كعطل:

| البوابة | نوع الفشل |
|---|---|
| `engine_content_separation_test.dart` | `Expected: contains 'reference_catalogue_page_live.dart:_fallbackLocal'` — المدخل **لم يعد موجودًا** في الكود. قائمة السماح تحمل أثرًا لشيءٍ نُظِّف |
| `zero-fallback-scan --check` | يفشل على الانحدار (127 مقابل 102) **و** يفشل على أي تحسُّن غير مُسجَّل، بنصّ الكود: `if (improved.length) ... exit(1)` |

`dart-literal-strings` هو الانحدار الحقيقي من الثلاثة: ملفان نما كلٌّ منهما موضعًا واحدًا.

---

## §4 الأمان والتشفير — `SEC-*` و `ENC-*`

### 4.0 ما هو سليم ولا يجب إعادة بنائه

هذا القسم يبدأ بما صمد، لأن ثلاثة من البنود أدناه صغيرة ويُخطئ من يقرأها حكمًا على الطبقة كلّها.

| ما تحقَّق | الدليل المقيس |
|---|---|
| **صفر معالِج إداريّ مُطفِّر بلا حساب** | 211 معالِجًا `POST/PUT/PATCH/DELETE` في 45 موجّهًا: 196 بـ`requirePermission` · 3 بـ`readOnly` (405) · 12 في قائمة سماح مُبرَّرة · **صفر غير محسوب** |
| المفتاح المشترك مخرجٌ طارئ لا باب | `adminAuth.ts:51-94` — يُرفض بـ401 فور وجود أي مستخدم؛ و`admin_users = 1` في القاعدة أي أن الباب مُغلَق فعلًا |
| `requirePermission` فائل-مغلق وواعٍ بالنطاق | `adminAuth.ts:154` — بلا مستخدم ولا مفتاح ⇒ 401، و`resourceScope` يبني النطاق من المسار والاستعلام والجسم |
| كتابةٌ مجهولة واحدة فقط على المنصّة | `partnerships.ts POST /` (نموذج تواصل)، موسومة `ANONYMOUS_BY_DESIGN` |
| بوابة HLS أُغلقت فعلًا | `episodes.ts:484-560` — مصادقة + `lease_id` إلزاميّ + إتاحة إقليمية + **تحقُّق العقد عبر الـDO** + بلا هبوط إلى الأصل الأساسي |
| حدود التنزيل خادمية وذرّية | `FamilyState.ts:2940-3010` — الباقة ثم إعادة استخدام idempotent ثم `download_devices` ثم `offline_items`، كلّه على تخزينٍ مُسلسَل لكل أسرة |
| لا سرّ في `vars` | كل الأسرار الثمانية المُشار إليها في الكود غائبة عن `wrangler.jsonc` — أي أنها secrets |

---

### SEC-201 — الخروج قد يُبقي بيانات الاعتماد على الجهاز

**الأولوية:** P0 · **المنطقة:** أمان/خصوصية · **الحجم:** S · **الاختبار الذي يرصده موجود ويفشل**

**الدليل**
`app_main/test/logout_tile_test.dart:185` → `expect(storage.cleared, isTrue)` يفشل بـ`Expected: true, Actual: <false>`. والسطر 184 (`api.logoutCalled`) **ينجح** — أي أن النداء الخادميّ يجري ثم يتوقّف التفكيك المحلّي.

**السبب الجذري** — `auth_controller.dart`، داخل `_runLocalSessionTeardown`:

الترتيب هو: قائمة `persistentWipes` تُنفَّذ أوّلًا وتُجمَّع أخطاؤها في `failures`، ثم:

```dart
_throwIfTeardownFailed(failures);   // ← يرمي هنا

// AuthStorage.clear itself attempts every credential key ...
await storage.clear();              // ← فلا يُصل إليه
```

والترتيب **مقصود** وموثَّق بتعليقه: «بيانات الاعتماد تبقى متاحة حتى تُمسح كل البيانات المُنطاقة، فتُعاد محاولة المسح الفاشل بمعرّفَي الوالد والطفل بدل أن تُترك بياناتٌ مُشخَّصة على القرص». المنطق سليم في أصله.

والذي كسره مدخلٌ **جديد** في القائمة نفسها:

```dart
() => _ref.read(onboardingControllerProvider.notifier).complete(),
```

و`onboardingControllerProvider` يقرأ `sharedPreferencesProvider`، وهو مزوّد يلزم تجاوزه (`main.dart` يتجاوزه عند الإقلاع). فحيث لا يُحلّ، يرمي ⇒ يُضاف إلى `failures` ⇒ `_throwIfTeardownFailed` يرمي ⇒ **`storage.clear()` لا يُنفَّذ** ⇒ التوكنات تبقى.

**لماذا هذا P0 وليس عطل اختبار**: الأثر على جهازٍ مُشترك هو أن «تسجيل الخروج» يُعلن نجاحًا ويترك بيانات اعتماد الأسرة السابقة. وهو نفس صنف العطل الذي أُصلح في الدفعة 1 (`ENC-012`: كاش القارئ يبقى بعد الخروج)، وقد عاد من بابٍ آخر.

**الإصلاح المقترح**: إخراج مسح حالة الـonboarding من قائمة الحواجز التي تمنع مسح الاعتماد — إمّا بنقله بعد `storage.clear()`، أو بتصنيفه ضمن `memoryWipes`، أو بجعل فشله غير حاجب. والقرار يحتاج سطرًا في التعليق يقول **أيّ الإخفاقات يجوز أن تحجب مسح الاعتماد وأيّها لا**، لأن غياب هذه القاعدة هو ما سمح للمدخل الجديد أن يوسّع نطاق الحجب بصمت.

---

### SEC-202 — دخول اللوحة على 600 محاولة/دقيقة بينما دخول الأسرة على 5

**الأولوية:** P0 · **المنطقة:** أمان · **الحجم:** XS

**الدليل** — `src/index.ts` مقابل `src/lib/rateLimit.ts`:

| المسار | الوسيط المُطبَّق | الحدّ | المفتاح |
|---|---|---|---|
| `/api/v1/auth/*` | `strictAuthLimit` | 60ث / **5** | العنوان |
| `/api/v1/admin/*` ← ومنه `/admin/auth/login` | `adminLimit` | 60ث / **600** | المُوكَّل، ويهبط إلى العنوان بلا توكن |

`app.use('/api/v1/admin/*', adminLimit)` يُطابق `/api/v1/admin/auth/login`، و`strictAuthLimit` مقصورٌ على `/api/v1/auth/*`. وبلا توكن، `perPrincipal` يهبط إلى مفتاح العنوان (`rateLimit.ts:63-65`) — فمهاجمٌ مجهول يحصل على **600 محاولة كلمة مرور في الدقيقة** على باب اللوحة، مقابل 5 على باب الأسرة.

**الإصلاح**: `app.use('/api/v1/admin/auth/login', strictAuthLimit)` قبل تركيب `adminLimit`. XS.

**وملاحظة ثانية على المحدِّد نفسه (P2):** عند غياب `RATE_LIMITER` أو رميه، يهبط `consumeRateLimit` إلى `Map` داخل الـisolate (`rateLimit.ts:104-133`). الهبوط مُسجَّل لا صامت — وهذا أُصلح — لكن حدًّا يتفرّق على الـisolates قريبٌ من لا حدّ تحت التوزّع.

---

### SEC-203 — عتبةُ مِسحة الحُرّاس أقلّ من الواقع بـ2.3 مرّة

**الأولوية:** P1 · **المنطقة:** اختبارات/أمان · **الحجم:** XS

`test/routeGuards.test.mjs:565` يؤكّد `allMutations.length >= 90` بتعليقٍ يقول «expected ~96 mutating handlers». والمقيس اليوم **211**. ونظيره `>= 80` مقابل 196 فعليًّا.

**الأثر**: نحو 121 معالِجًا يمكن أن يسقط من رؤية المِسحة — بتغيُّر نمطٍ منتظم، أو بإعادة تسمية ملف — وتبقى البوابة خضراء. وهذا بالضبط ما وُجد الاختبار `the guard sweep actually inspected the routers` لمنعه، هزمه ثابتٌ تقادم.

**نقاط عمى أخرى في المِسحة نفسها، مُرتَّبة بما تعني** (وأكثرها موثَّق في الملف نفسه، وهو ما يجعله موثوقًا):

| العمى | حالته اليوم |
|---|---|
| مسارات بعلامات تنصيص مزدوجة أو قوالب نصّية لا تُرى | صفر حالة قائمة — لكن لا شيء يفرض العُرف |
| `.on([...])` و`.all()` خارج `MUTATING` | صفر تسجيل قائم |
| المِسحة الإدارية **اختبارُ اسم ملف** (`name.startsWith('admin')`) | كل موجّهات `/api/v1/admin` الـ20 من ملفات `admin*.ts` — لا فجوة حيّة، وهذه هي العلّة التي أفلتت `recommendations.ts POST /admin` سابقًا |
| وجودُ حرسٍ ≠ صحّته | 20 معالِجًا فقط مُثبَّتة صلاحيتها بالاسم في `CRITICAL_GUARDS` من 196، أي 10% |
| `AUTH_MARKERS` مطابقةٌ نصّية | معالِجٌ ينادي `authenticateParent` ويتجاهل نتيجته يمرّ — وقد وقع فعلًا في `analyticsIngest.ts:140` بإقرار الملف نفسه |
| **كل السطح الإداري خارج التحقُّق المخطَّطي** | `requestSchema.test.mjs:181` يُرشِّح `!name.startsWith('admin')` — أي 45 موجّهًا و211 تطفيرًا تُحلّل باليد |

---

### ENC-002 — لا مفتاح محتوى على الإطلاق: الترخيص توكيدٌ لا مفتاح

**الأولوية:** P1 · **المنطقة:** تشفير · **الحجم:** L · **الحالة: لم يبدأ** (وكان مُسجَّلًا `PARTIAL`)

**الدليل**
- مِسحة `src/` على `content_key|contentKey|wrapKey|dek|kek|CENC|widevine|clearkey`: الـAES-GCM الوحيد في `lib/security.ts:90-130` وهو **لتشفير التوكنات** لا الوسائط.
- `downloads.ts:362` و`:419` يُعيدان `authorization: Bearer <capability>` على **عنوان أصلٍ عاديّ**.
- `books.ts:670` يُعلن العقد بنصّه: `protection: 'access_controlled_no_drm'`.

**ما يعنيه هذا بدقّة**: الترخيص Ed25519 حقيقي وموقَّع ويحمل `exp` و`epoch` و`did` وبصمات `sha256` لكل أصل — لكنه **توكيد تخويلٍ يُؤتمَن العميل على احترامه**، لا مفتاحًا يفكّ شيئًا. والملف المُنزَّل يهبط على القرص **صريحًا**. فكل حدٍّ خادميٍّ في `FamilyState` يحكم *إصدار* الترخيص، ولا يستطيع سحب بايتٍ نزل.

**التصحيح على السجلّ**: `ENC-002` كان مُسجَّلًا «مفتاح واحد للتطبيق بدل CEK مغلَّف للجهاز — PARTIAL». الأدقّ: **لا مفتاح محتوى أصلًا، لا واحدًا ولا مُغلَّفًا.** والفرق مهمّ لأن «PARTIAL» تُقرأ كأن نصف العمل قائم.

**وتدوير المفاتيح**: الصيغة تدعمه (`kid` في المطالبات، و`signature_key_id` عمودٌ في `media_licenses`)، والمنظومة لا: ثابتٌ واحد `CURRENT_KEY_ID = 'majarra-offline-v1'` ومتغيّر بيئة واحد، بلا سجلّ مفاتيح. فالتدوير تغييرُ كودٍ ونشرٌ، ومفتاحٌ واحد يوقّع في كل لحظة.

---

### API-201 — الرقابة الأبوية مفروضة للفيديو وغائبة عن الصوت والقراءة

**الأولوية:** P1 · **المنطقة:** رقابة أبوية · **الحجم:** M

**ما يعمل** — `FamilyState.ts`:
- `/playback/start` (`:3247-3261`): وقت النوم ⇒ 403 `screen_time_bedtime` · الحدّ اليومي ⇒ 403 `screen_time_daily_limit`.
- `/playback/heartbeat` (`:3337-3356`): **يُبطل العقد الحيّ** على `bedtime` أو `session_limit` أو `daily_limit` — فجلسةٌ تعبر وقت النوم تُقطَع لا تُرفَض عند البدء فقط.
- وعمر العقد نفسه مقصوصٌ على ما تسمح به الحدود، فلا يكسب الطفل جلسةً إضافية بين نبضتين.

**وله مُستدعٍ واحد**: `episodes.ts:321`. هذا كلّ التغطية.

**ما لا يُغطّى**

| السطح | الموضع | ما يُفحَص | ما لا يُفحَص |
|---|---|---|---|
| الصوت | `books.ts:597` و`stories.ts:697` (`/audio-sessions`) | الوسائط مُهيّأة · مصادقة · مخطَّط · ملكية الطفل · الباقة | **وقت النوم · الحدّ اليومي · حدّ الجلسة · التزامن** |
| القراءة | `stories.ts:481` (`/:id/pages`) | منشورة · إتاحة · صنف المحتوى | لا مصادقة أصلًا ⇒ **لا هوية طفلٍ تُبنى عليها بوابة** |

`books.ts:649` يقرّ بذلك بتعليقه: «لا عقد للصوت، فمعرّف الأصل يقوم مقام معرّف الارتباط».

**الأثر العمليّ**: طفلٌ يُرفَض له الفيديو في التاسعة مساءً يحصل على الكتب المسموعة في التاسعة مساءً. والرقابة التي يضبطها ولي الأمر تُقرأ كأنها عامّة وهي على ثلث المحتوى.

**وملاحظة على البيانات**: الجدول المستخدَم فعلًا هو `screen_time_daily` **داخل الـDurable Object** (`FamilyState.ts:304`). أمّا جدول D1 `child_screen_time_daily` (من `0001_init.sql:329`) فلا كاتب له — و`parentalControls.ts:12` يقول ذلك في رأسه.

---

### SEC-204 — نقاط عامّة مكلفة بلا حصّة

**الأولوية:** P1 · **المنطقة:** أمان/أداء · **الحجم:** S

مسارات مُركَّبة في `index.ts` بلا `app.use` مقابل: `/planets`، `/series`، `/games`، `/partnerships`، `/app-config`، `/recommendations`، `/home`، `/api/v1` (creative + publicCreativeStudio)، `/site`، والعارض الخادميّ على `/`.

مُرتَّبة بالخطر:

1. **`POST /api/v1/partnerships`** — الكتابة المجهولة الوحيدة على المنصّة، على D1، **بلا حصّة**.
2. **`GET /api/v1/home/resolved`** و**`/recommendations`** — توزيعُ استعلاماتٍ متعدّد بلا مصادقة.
3. **`publicRender` على `/`** — كل عرض صفحة يُصدر 3–4 استعلامات D1 متوازية (`publicRender.ts:335, 453, 576, 672, 779`).

وحصّة التحليلات `analyticsLimit` (240/دقيقة) **بالعنوان لا بالمُوكَّل**، فأسرةٌ خلف NAT تتقاسم الميزانية.

### SEC-205 — كلمات مرور إدارية من `Math.random()`

**الأولوية:** P1 · **المنطقة:** لوحة/أمان · **الحجم:** XS

`dashboard/front/src/pages/TeamAccessPage.tsx:208` و`:237` يولّدان كلمة مرور حساب إداريّ بـ`Math.random().toString(36)` مع لاصقة (`+ 'Aa1!'`). مولِّدٌ غير تشفيريّ لبيانات اعتماد. البديل سطرٌ واحد: `crypto.getRandomValues`.

---

## §5 تطبيق Flutter — `APP-*`

### 5.1 ما تحقَّق فعلًا (تصحيحات على الأودت السابق)

| ما كان مُسجَّلًا | الواقع المقيس اليوم |
|---|---|
| «سياسة الحراسة مكرَّرة في موضعين» | ✅ **مُصلَح**. `_guardRedirect` يستدعي `accessFor(loc)` في `app_router.dart:122`، وكل قرارٍ بعده بلغة `RouteAccess`. القوائم الثلاث القديمة حُذفت |
| «الفوترة معطَّلة» | ✅ **لم تعد كذلك**. `membership_page.dart` يستورد `in_app_purchase`، ويشترك في `purchaseStream`، ويتحقّق خادميًّا عبر `serverVerificationData` مع مجموعة إبطال تكرار. يعمل على Android، وغائب على الويب/iOS **بالبناء** |
| «لا Onboarding إطلاقًا» | 🟡 موجود: 5 ملفات ومسار `/onboarding` وأربعة اختبارات. لكن المتحكّم **محلّي فقط** — يحفظ الخطوة في `SharedPreferences` ولا ينادي الخادم ولا يحمل حمولة موافقة |
| «`/planets` يتجاهل المُعامل ويثبّت `isTelevision: false`» | ✅ مُصلَح (`app_router.dart:239`): يقرأ `planetId` ويقرأ ملف الجهاز الحقيقي |
| «التوجيه بمطابقة نصّ المسار» | ✅ مُصلَح: `routeAllowsLandscape` يقرأ `fullPath` من `go_router` |

### 5.2 APP-201 — `/studio/**` لا تزال محروسة في البانية لا في الموجّه

**الأولوية:** P1 · **الحجم:** S

`route_access.dart:118-124` يصنّف الروابط العميقة الثلاثة `authenticatedFamily`، بينما `/studio` نفسه `childSession` (`:87`). و`_prefixAccess` (`:48-53`) يغطّي `/playback` و`/reader` و`/game` و`/series` فقط — لا `/studio`.

**المُخفِّف**: الأربعة كلّها تمرّ بـ`_touchOnlyRoute`، وكل بانية تفحص `childProvider.activeChildId` وتعرض `_RouteMessage` بدل السطح. فالبوّابة موجودة — **في البانية**، وهي بالضبط الشكل الذي وُجدت جدول التصنيف لإلغائه. والأثر عطلُ صحّةٍ وتجربة لا تسريب بيانات.

**وتعليقٌ صار كاذبًا** (`DOCS-203`): `route_access.dart:11-14` ما زال يقول «لا يمسّ `_guardRedirect` — تلك إعادةُ كتابةٍ منفصلة»، و`:5` ما زال يُحيل إلى `app_router.dart:70-181`. الاثنان يخالفان الكود اليوم ويُضلّلان القارئ التالي.

### 5.3 APP-202 — أصولٌ مُشار إليها في `lib/` ولا وجود لها

**الأولوية:** P1 · **الحجم:** M · **بوابةٌ حمراء**: `assets-declared --check` خروج 1

مسارات `Image.asset` في كود الإنتاج بلا ملفٍ مُبندَل — أي **إطارٌ فارغ أو استثناء على الجهاز**:

| الملف | العدد/الموضع |
|---|---|
| `games/presentation/pages/reference_catalogue_page_live.dart` | نحو 50 مسارًا (`assets/images/draw_like_me/v2-final/*.png`) + `heroes/draw-like-me-hero.webp:535` |
| `games/presentation/pages/studio_v2/image_slot.dart` | `:19` و`:20` → `assets/images/coloring/v2/bird.png` |
| `games/presentation/studio/studio_home_view.dart` | `:207` → `assets/images/studio/studio-main-banner.png` |
| `home/presentation/widgets/home_feed.dart` | `:676` → `assets/images/studio/homebgaart.png` |

السياق الذي يجعل هذا مرجَّحًا لا نظريًّا: `pubspec.yaml` يعلن سياسة **R2/CDN أولًا** مع إعلانات أصولٍ **غير تعاودية** بقصد، فمسارٌ لم يُعلَن لا يُحزَم. والملف الأوّل (`_live`) أحدُ سبعة ملفات مزدوجة الجيل.

### 5.4 APP-203 — سبعة ملفات مزدوجة الجيل، ولا يُعرف أيُّها حيّ

**الأولوية:** P2 · **الحجم:** M

`reference_catalogue_page_live.dart` (33 ك.ب) بجانب `reference_catalogue_page.dart` (31 ك.ب) · `coloring_home_v2.dart` و`coloring_home_v2_live.dart` · `coloring_board_v2.dart` · `trace_board_v2.dart` · `trace_home_v2.dart` · `my_boards_v2.dart`.

زوجان منها يظهران معًا في خطّ أساس النصوص المحفورة، أي أن **كليهما ما زال يُترجَم**. وأيُّهما يُصل إليه فعلًا لم أتحقّق منه — وهو أعلى بندٍ قيمةً في تنظيف الكود الميت، لأن `APP-202` أعلاه كلّه تقريبًا في النسخة `_live`.

### 5.5 APP-204 — أكبر ملف في التطبيق بلا اختبار، وفيه 13 من 33 إسكاتًا صامتًا

**الأولوية:** P1 · **الحجم:** L

`playback_page.dart` = **126,862 بايت**، ولا ملفّ `playback_*_test.dart`. والمحيط مُغطّى (`streaming_contract_test`، `orientation_gate_test`، `screen_capture_guard_test`) والجوهر لا.

وفيه ثلاثة عيوب متجاورة:

1. **مُنتقيا الجودة والمسار الصوتي زينة.** الـDTO يقرأ `audioTrackCodes` (`:134-140`) و`uiQualityRenditions` (`:156-162`)، لكن الأزرار الأربعة كلّها تفتح `_showSettingsSheet` نفسها (`:1678-1681`)، والشيت لا يحمل إلا `onSubtitle` و`onSpeed` و`onAutoPlay` (`:3315-3323`). وتسمية الجودة نصٌّ حرفيّ: `selectedQuality: 'تلقائي'` (`:1651`). والترجمة وحدها تعمل فعلًا.
2. **13 كتلة `catch (_) {}` فارغة** من أصل 33 في التطبيق كلّه. وأخطرها `:1383`: يُسقط قراءة `allow_speed_change` بصمت **ويهبط إلى السماح** — أي أن ضبطًا أبويًّا يفشل قراءتُه يُقرأ «مسموح».
3. ملفٌ بهذا الحجم وبهذه العيوب بلا اختبارٍ واحد هو تفسيرُ بقائها.

**ملفات كبيرة أخرى بلا اختبار**: `series_details_page.dart` (47.8 ك.ب) · `login_page.dart` (39.8 ك.ب، المتحكّم وحده مُغطّى) · `account_data_page.dart` (37.9 ك.ب) · `free_draw_surface.dart` (37.3 ك.ب) · و`app_router.dart` بلا اختبار وحدة لـ`_guardRedirect` نفسه.

### 5.6 APP-205 — خطأ شبكة يُقرأ «لم يُكمل الـonboarding»

**الأولوية:** P2 · **الحجم:** S

`family_children_provider.dart:33-38` يبتلع خطأ الـAPI ويُعيد قائمة فارغة. ومستمع الموجّه (`app_router.dart:73-80`) يشتقّ `setHasCompletedOnboarding` منها. فخطأُ شبكةٍ عابر ⇒ `hasCompletedOnboarding == false` ⇒ أسرةٌ قائمة تُعاد إلى رحلة أوّل استخدام (`:131-135`, `:164-166`).

الحراسة نفسها **فائلة-مغلقة** بشكل صحيح (`auth_guard.dart:139-176` يعدّ فشل التخزين خروجًا)، والعطل في الاتجاه الآخر: لا يفتح ما يجب إغلاقه، بل يُعيد عرض شيءٍ انتهى.

وإسكاتان آخران يستحقّان النظر: `auth_guard.dart:59-61` و`:130-132` — الثاني يُعيد `false` من `_accessTokenExpired` عند فشل التحليل، أي يعدّ توكنًا غير قابل للتحليل **صالحًا**.

### 5.7 APP-206 — الضيف: طفلٌ محفور وكتالوجان محفوران

**الأولوية:** P2 · **الحجم:** M

`family_children_provider.dart:41-43` يحفر `ChildProfile(id: 'demo-child', nickname: 'الضيف', ...)`، ومعه `BillingStatus.guestPreview` (`billing_status.dart:154-166`) و`BillingCatalog.guestPreview` (`billing_catalog.dart:48-50`). والمُحسَن أن `startDemoSession()` لا يكتب بيانات اعتماد مزيّفة قط.

وبجانبه **الكتالوج المُبندَل ما زال في مسار البيانات الحيّ**: `local_catalog.dart` (33 ك.ب، 208 نصًّا عربيًّا) مستوردٌ في `content_repository.dart:5`، و`:142-176` يمرّر الاستجابات عبر `_seriesFallback`/`_episodeFallback`/`_bookFallback` — أي أن صفوفًا مبندلة ما زالت تسند استجابات الخادم.

**والنظيف** الذي يستحق التسجيل: `TODO/FIXME/HACK` في 206 ملف Dart = **واحد** (`core/analytics/analytics.dart:121`). ولا أسماء مطوّرين شاردة؛ الأسماء اللاتينية كلّها طاقم العلامة في `child_avatars.dart:69-134` بأصولٍ حقيقية.

### 5.8 I18N-201 — التوطين مبنيّ ومُقفَل عليه

**الأولوية:** P1 · **الحجم:** XL · **قرار مالك**

| المقياس | القيمة |
|---|---:|
| مفاتيح ARB في `app_ar` / `app_en` / `app_fr` | **257 / 257 / 257** (تطابق تامّ) |
| مواضع `AppLocalizations.of` | **34** في 18 ملفًا من 206 |
| أسطر فيها نصّ عربيّ حرفيّ في كودٍ مكتوب بيد | **~2,250** |
| أسوأ المواضع | `local_catalog.dart` 208 · `reference_catalogue_page.dart` ~90 · `membership_page.dart` ~90 · `playback_page.dart` ~83 |

والمفارقة أن العمل التحضيري **مُنجَز**: 771 مفتاحًا مترجمًا في ثلاث لغات. والمانع سطران:

- `app/majarra_app.dart:107` — `locale: AppLocales.fallback.locale` يُثبّت اللغة ويتجاهل لغة الجهاز، بينما `:108` يمرّر `supportedLocales` كاملة.
- `settings_page.dart:76-82` — صفّ اللغة بلا `onTap`: عنوان وقيمة ولا إجراء.

فـ771 مفتاحًا غير قابلة للوصول من الواجهة بسبب سطرين. وهذا أرخص بندٍ عائدًا في الملف: رفعُ التثبيت وتوصيلُ الصفّ يُفعِّل ما دُفع ثمنه، ثم يبقى الاستخراجُ الـ2,250 عملًا مفتوحًا.

### 5.9 تبعيات ميتة

`chewie` مُعلَنة في `pubspec.yaml` وصفر استيراد `package:chewie/` في 206 ملفات. وكل تبعيةٍ أخرى مستوردةٌ مرّةً على الأقل.

---

## §6 لوحة التحكم — `ADM-*`

### 6.1 ما تحقَّق (وهو الأكثر تحسُّنًا بين الطبقات الثلاث)

| المقياس | القيمة المقيسة |
|---|---|
| `@ts-nocheck` / `@ts-ignore` / `@ts-expect-error` في `src/` | **صفر · صفر · صفر** (المطابقتان الظاهرتان نصٌّ داخل تعليقات اختبارَي الحرس) |
| المسارات / الصفحات | 125 عنصر `<Route>` · 123 مسارًا فريدًا · 119 صفحة |
| تطابق المسار↔الصلاحية | **123 ↔ 123 تامّ** في الاتجاهين، محروسًا باختبار |
| نقطة الإنفاذ | **واحدة**: `PermissionGate` حول `<Outlet/>` داخل `Suspense` في `AdminLayout.tsx:72-74` — فمسارٌ محجوب لا يُنزّل حزمته أصلًا |
| القائمة الجانبية | تشتقّ القرار من الخريطة نفسها (`Sidebar.tsx:282-283`)، لا قائمةً ثانية |
| التقسيم | 116 من 119 صفحة `lazy`؛ والثلاث الساكنة مُبرَّرة |
| تبعيات خارجية | **ثلاث** فقط (`react`, `react-dom`, `react-router-dom`) — فلا مكتبة ثقيلة تتسرّب إلى الحزمة الأولى |
| `TODO`/`not implemented`/`onClick={() => {}}` | **صفر** |

وحرس `typeSuppression.test.ts` مبنيٌّ بحيث لا يُخدَع: تعبيراته مُركَّبة بوصل نصوص حتى لا تُطابق ملفها، ويؤكّد `files.length > 200` لئلّا تمرّ الجولة بجمع صفر ملف — وهو إخفاقٌ وقع فعلًا في هذا المستودع من قبل.

### 6.2 ADM-201 — طابور السرد الصوتي مُفبرك بـ`Math.random()`

**الأولوية:** P0 · **الحجم:** M

`dashboard/front/src/pages/NarrationPage.tsx:150-186` يبني طابور إنتاج السرد كلَّه من مكتبة القصص على العميل:

| السطر | ما يفعله |
|---|---|
| `:156` | `const total = s.pages_total \|\| 4` — يختلق 4 صفحات عند غياب الحقل |
| `:158` | `(s as any).pages_with_narration ?? Math.floor(Math.random() * 2)` — **يقذف عملةً** على مقدار السرد الموجود |
| `:166` | `status: has ? (Math.random() < 0.5 ? 'approved' : 'ready_for_review') : 'missing'` — **يقذف عملةً على حالة الاعتماد** |
| `:163` | `text: \`نص الصفحة ${p} من ${s.title_ar} — نسخة v6\`` — نصٌّ مُختلَق، ثم يُرسَل إلى `api.ttsPreview` (`:190`) كأنه السطر الحقيقي |
| `:168-170` | `sourceVersion: 'v6'` · `owner: 'Audio Team'` · `due: '2026-08-20'` محفورة |
| `:186` | `m.processing = 1` — عدّادٌ محفور في صفّ المقاييس |

**لماذا P0 وليس P2**: هذه شاشة **حكم تشغيليّ**. مشغّلٌ ينظر إليها ليقرّر ما يُنتَج وما يُعتمَد، والشاشة تعرض اعتمادًا لم يحدث ونصًّا لم يُكتَب. وهي الموضع الوحيد في اللوحة كلّها الذي يقود فيه `Math.random()` حالةَ عملٍ معروضة — الخمس المطابقات الأخرى مفاتيح React، واثنتان كلمتا المرور في `SEC-205`.

ولاحظ الرابط بينها وبين النوع: `(s as any)` في `:158` يُخفي بالضبط الحقل الناقص الذي كان رفعُ `@ts-nocheck` ليكشفه. فـ**479 سطرًا فيه `as any`** هو المخرج الباقي بعد إزالة التعطيلات، ولا حرس عليه.

### 6.3 ADM-202 — بوابة `?? 0` حمراء: 127 مقابل 102

**الأولوية:** P1 · **الحجم:** M

`node tools/ci/zero-fallback-scan.mjs --check` → خروج 1. 119 صفحة مفحوصة · 38 فيها `?? 0` · **127 موضعًا** مقابل خطّ أساس **102**.

| فوق حصّتها | جديدة كليًّا (غير مُسجَّلة) |
|---|---|
| `LearningObjectivesPage` 11/9 · `ChildWorkspacePage` 6/3 · `GamesOpsPage` 6/5 · `RolesPage` 5/2 · `AppExperiencePage` 4/3 · `AnalyticsPage` 2/1 · `CampaignsPage` 2/1 · `PackagesPage` 2/1 | `AiProvidersPage` 8 · `CustomersPage` 4 · `PartnershipsPage` 4 · `RevenuePage` 4 · `ProjectsPage` 3 |

والسؤال الذي تفرضه البوابة على كل موضع جديد هو سؤالها الصحيح: **هل الصفر هنا صادق؟** أي هل للجدول كاتب فصفره قياس، أم أن القراءة قد تفشل فالصفر كذبة.

**ونطاق البوابة أضيق مما يبدو**: `ROOTS = ['dashboard/front/src/pages']` فقط. فـ62 موضع `?? 0` في `components`/`lib`/`hooks` و**65 موضع `|| 0`** في كل `src` خارج أي حرس. و`|| 0` مستثنى بقرارٍ مكتوب لأنه عطلٌ مختلف (يُسقط الصفر الحقيقي أيضًا) ويستحق بندًا خاصًّا.

### 6.4 ADM-203 — قراءةٌ فاشلة تُعرَض «لا شيء معلَّق»

**الأولوية:** P1 · **الحجم:** S

`DashboardPage.tsx:29-31` يحوّل ثلاث إخفاقات إلى فراغٍ بشكل نجاح:

```
api.contentReviews(...).catch(() => ({ data: [] }))   // ونفسها لـ api.tasks() و api.rights()
```

والمعروض لا يُميَّز عن «لا مراجعات معلَّقة». هذه صورة `?? 0` في هيئة قائمة، وهي على **الشاشة الأولى بعد الدخول**.

**وبالمقابل**، النمط الصحيح قائمٌ في الملفات المجاورة ويستحق أن يُحتذى: `HeroKpis.tsx:58-61` يعرض `'—'` عند الغياب، و`catch` فيه يضع `{_unavailable:true}`، و`overall_health` يهبط إلى `'unknown'` لا `'healthy'`. و`AttentionPanels.tsx` يعرض حالةً فارغة مُصرَّحة. و`SessionsPage` و`AnalyticsPage` و`StatCard` نظيفة تمامًا — صفر مصفوفة محفورة في سبعة ملفات لوحة فحصتُها.

### 6.5 ADM-204 — ثمانية مسارات تَهبط على شاشة غير التي يعِد بها عنوانها

**الأولوية:** P2 · **الحجم:** S

| المسار | يهبط على |
|---|---|
| `library` و`library-content` | `LibraryHubPage` (نفسها) |
| `creative-studio` و`creative-studio/v2` | `CreativeStudioAdminPage` (نفسها) |
| `ops/alerts` و`ops/telemetry` | `OpsPage` — أي شاشة `ops` نفسها |
| `ops/queues/:name` | `OpsServiceWorkspacePage` — نفس شاشة `ops/services/:id` |
| `ops-sla/policy/:id` | `OpsSlaPage` — نفس `ops-sla` |

وهذه أقرب ما بقي إلى مسارٍ صوريّ؛ `NotImplementedPage` حُذف فعلًا ولا وجود لمكوّن نائب.

**وصفحتان موجودتان بلا مسار**: `LandingPage.tsx` و`StatusPages.tsx` (كلتاهما تُستهلك من `App.tsx` لا من `AdminRoutes`). و`LibraryContentPage.tsx` المحذوف **صفر مرجع** — لا استيراد مكسور.

### 6.6 ADM-205 — ثلاثة عناصر معطَّلة بلا مسارٍ لتفعيلها

**الأولوية:** P2 · **الحجم:** XS

- `RemoteConfigPage.tsx:444` — `<button ... disabled>Rollback where safe</button>`: `disabled` عارية بلا حالة ولا مُعالِج. زرُّ تراجعٍ لا يمكن أن يعمل.
- `VisualStylesPage.tsx:354` — مربّع «تحديد الكلّ» في رأس الجدول `disabled` وبلا `onChange`: تحديدٌ جماعيّ مُعلَن وغير متاح.
- `GamePackForm.tsx:481` — `disabled onChange={() => undefined}`؛ مرجَّح أنه قراءةٌ فقط بقصد، لكن الشكل مُلتبس.
- وبجانبها `RemoteConfigPage.tsx:440` يعرض عمود «السبب» شرطةً لكل صفّ — عنوانُ عمودٍ بلا حقلٍ يسنده.

من 346 موضع `disabled` في اللوحة، هذه الثلاثة وحدها بلا مسارٍ للتفعيل؛ والباقي مربوطٌ بـ`busy` أو تحقُّق أو `hasPermission`.

### 6.7 ADM-206 — CSS لم يُقسَّم مع الـJS

**الأولوية:** P2 · **الحجم:** S

`AdminRoutes.tsx:9-25` يستورد **سبع** صفحات أنماط استيرادًا ساكنًا، وأكبرها `dashboard.css` (نحو 90 ك.ب بإقرار تعليقه). ولأن `AdminRoutes` هو الوحدة التي تحمل نداءات `lazy`، تهبط السبعة كلّها في الحزمة الأولى أيًّا كان المسار المزار — ومنها `gameStudio.css` و`creativeStudioTheme.css` ومُستهلِكاهما مقسَّمان. فالـJS قُسِّم لكل مسار والـCSS لم يُقسَّم.

### 6.8 التوطين في اللوحة: ثنائيّ اللغة يدويًّا

لا إطار i18n إطلاقًا (صفر `useTranslation`/`i18next`). والمنفَّذ **725 موضع `locale === 'ar'`** وقوالب `copy = { ar, en }` لكل ملف. ويبقى **819 سطرًا فيه عربيّ في موضع نصّ JSX** خارج هذا النظام — بلا مقابلٍ إنجليزيّ ولا مسار استخراج. فلغةٌ ثالثة تعني لمس كل واحد منها.

---

## §7 المحتوى والوسائط — `CNT-*` و `DATA-*`

### 7.1 التقدّم الحقيقي: الفيديو وُجد

هذا أهمّ تغيّرٍ إيجابيّ منذ الأودت السابق، وكان مانعَه الأول:

| الدليل | القيمة |
|---|---|
| `dashboard/api/private/**/1080p.mp4` | **33 ملفًا · 460 ميغابايت** · بتاريخ 2026-09-03 |
| `tools/ops/link-episodes.sql` (غير متتبَّع) | **66 سطر INSERT** = 33 حلقة × (`stream` + `thumbnail`) على أصول `ca-episode-*-1080p` |
| `tools/ops/mark-33-episodes-english.sql` (غير متتبَّع) | يوافق العدد نفسه |

فـ«صفر أصل فيديو» لم يبقَ صحيحًا على الإنتاج. **لكن** السجلّ نفسه (`CONTENT-COMPLETION-LEDGER-2026-09-03.md`) يضع الحدّ بدقّة:

> الـ33 فيديو الحاليون نسخ قصيرة قابلة للتشغيل (34–44 ثانية)، لكنهم لا يساوون النصوص الكاملة ذات المدد المستهدفة 2–8 دقائق. لا يُحسب وجود فيديو قصير على أنه اكتمال إنتاج الحلقة الكاملة.

وكلّها **غير منشورة** و**بلا سرد TTS عربيّ**. أي: مسار التشغيل صار قابلًا للإثبات من طرف إلى طرف، والمحتوى القابل للعرض على طفلٍ لم يصل بعد.

### 7.2 D1 المحلية — قياسٌ كامل (2026-08-31)

> **تحذير:** هذه قاعدةٌ مبنيّة من الترحيلات على قاعدةٍ فارغة، لا نسخةً من الإنتاج. تُقرأ كإجابة على «ما يُنشئه المخطَّط والترحيلات» لا «ما في الإنتاج».

| البند | القيمة |
|---|---:|
| الجداول · الترحيلات المُطبَّقة | 159 · **94** (آخرها `0091_drop_dead_parents_fks.sql`) |
| `content_assets` الإجمالي | **307** — 164 صوت/ready/private · 123 صورة/ready/private · 20 صورة/ready/public |
| منها **فيديو** | **صفر** |
| منها بحجم صفري | **307 — أي كلّها** |
| `asset_links` | 88 |
| السلاسل | 22 draft · 1 published |
| الحلقات | 33 draft (`is_published = 0`) · صفر منشورة |
| **حلقات قابلة للتشغيل** (بنفس predicate الكود `episodes.ts:103-104`) | **صفر** |
| القصص | 8 `writing` · صفر منشورة |
| `story_pages` | **صفر** |
| الكتب | 13 draft · صفر منشورة |
| الألعاب | 39 published · 3 ready · 1 draft · 13 archived = 56 |
| `admin_users` | **1** ✅ (`SEC-102` بُذر — كان صفرًا) |
| `learning_objectives` / `questions` | 75 / **صفر** |
| `content_reviews` | 16 |
| `content_rights` / `content_availability` | 125 / 1 |
| `home_experience_blocks` | 14 |
| `audit_logs` | 2 |
| `parents` / `children_profiles` | صفر / صفر (السلطة في الـDurable Object) |
| `analytics_events` / `notifications` / `plan_pricing` | صفر / صفر / **صفر** |
| `episode_renditions` | صفر |
| `media_licenses` / `child_downloads` / `download_events` | صفر / صفر / صفر (إسقاطٌ من الطابور؛ السلطة في الـDO) |

**قراءتان تستحقّان التسمية:**
- **307 أصلًا كلّها بحجم صفري.** فلا تقرير جهوزيةٍ يستطيع أن يفرّق أصلًا حقيقيًّا من صفٍّ مُسجَّل، ولا بوابة نشرٍ تفحص حجمًا.
- **`plan_pricing` صفر** مع مسار شراء Android عامل. أي أن ولي الأمر يلقى مسارَ دفعٍ بلا سعرٍ خلفه.

### 7.3 DATA-201 — تسجيل المحتوى في الإنتاج بـSQL يدويّ غير متتبَّع

**الأولوية:** P0 · **المنطقة:** بيانات/عمليات · **الحجم:** M

خمسة ملفات SQL **غير متتبَّعة** في `tools/ops/` تكتب في المحتوى: `link-episodes.sql` (66 INSERT) · `mark-33-episodes-english.sql` · `thumbs-register.sql` · `update-sizes.sql` · ومجلد `thumbs-tmp/`.

وترحيلان `0090` و`0091` **غير متتبَّعين** كذلك، وهما يعيدان بناء أربعة جداول.

**لماذا هذا P0**: المستودع يملك سجلّ ترحيلات (`migrations/LEDGER.md`) وحرسًا في CI (`migration-ledger.mjs --require-applied`) وبوابةَ بناءٍ من الصفر — وُجدت كلّها لأن بناء القاعدة من الصفر كان مستحيلًا (`DB-104`). ثم صار تسجيلُ المحتوى يجري من ملفات خارج `migrations/` وخارج git. فالقدرة التي دُفع ثمنها — إعادة بناء الإنتاج من المستودع — تُنقَض كلّما شُغِّل أحد هذه الملفات، ولا شيء يسجّل أنه شُغِّل.

**وأثره مُقاس بالفعل**: D1 المحلية فيها `asset_links = 88` و**صفر أصل فيديو** — أي روابطٌ لا تقابلها أصول. المحلية والإنتاج تباعدا، والفرق غير قابل للاشتقاق من المستودع.

### 7.4 CNT-201 — 160 وحدة من 208 غير ممثَّلة في الكتالوج

**الأولوية:** P0 · **قرار مالك + إنتاج** · **الحجم:** XL

المرجع: `docs/content/CONTENT-COMPLETION-LEDGER-2026-09-03.md` (وهو **غير متتبَّع في git** — `DOCS-202`).

| الحالة | الوحدات | الإجراء |
|---|---:|---|
| غير دينيّ مكتوب في الملفات | 142 | لا يُعاد تأليفه |
| منه مُسجَّل قانونيًّا في D1 | **48** | 33 حلقة + 15 قصة |
| مكتوب وغير مُسجَّل | **94** | 84 حلقة + 10 قصص صوتية |
| دينيّ: هيكل بلا نصّ | **66** | تعيين مصدر ومراجع شرعيّ ثم كتابة |
| **غير ممثَّل في الكتالوج الإنتاجيّ** | **160** | **94 تسجيلًا + 66 اعتمادًا وكتابةً وتسجيلًا** |

و**14 سلسلة من 37 غير مُسجَّلة** في D1 أصلًا.

والتمييز الذي يصنعه السجلّ صحيح ويجب الحفاظ عليه: **التأليف مُنجَز في 142 وحدة** (مِسحة الملفات وجدت صفر `pending/TODO/TBD` داخل وحدات المحتوى)، والناقص **تسجيلٌ وإنتاجُ وسائط**، وهما عملان مختلفان بمالكين مختلفين. فـ«94 مكتوبة وغير مُسجَّلة» بندُ هندسةِ بياناتٍ لا بندُ تأليف.

### 7.5 CNT-202 — كوكب الإيمان والآداب: 66 وحدة محجوبة على مراجعةٍ بشرية

**الأولوية:** P1 · **قرار مالك / تعاقد** · **الحجم:** XL

12 سلسلة، كلّها `pending_sharia_review`، و4 منها غير مُسجَّلة في D1. وبوابة النشر **تُنفِّذ** المراجعة الشرعية للكوكب `islamic` في الكود (وقد صُحّح في الدفعة 49 لتكون حاجبةً لا تحذيرًا). أي أن المانع ليس هندسيًّا: هو **تعاقد مراجعٍ شرعيّ وتعيين مصادر** (`religious-source-assignment.csv` موجود وغير متتبَّع).

### 7.6 CNT-203 — 75 هدفًا تعليميًّا وصفر سؤال يقيسها

**الأولوية:** P1 · **الحجم:** L

`learning_objectives = 75` و`questions = 0`. فالإتقان والتوصيات المبنيّة على مهارة لا يمكن أن تعمل، لا لعطلٍ برمجيّ بل لغياب أدوات القياس. والتأليف هنا محتوى لا كود.

---

## §8 التوثيق — `DOCS-*`

### 8.1 البوابة خضراء، وثغرتها بنيوية

`docs-classified --check` → خروج 0: 36 وثيقة جذر · 36 مُصنَّفة · 16 `source` + 20 `historical` · وكلّ تاريخيٍّ يحمل وسمه في أوّل 400 بايت.

والثغرة سطرٌ واحد في `tools/ci/docs-classified.mjs:70-71`:

```js
if (status === 'source' || !present.includes(file)) continue;
```

**التصنيف `source` لا يُتحقَّق منه مقابل أيّ شيء.** المنظومة بُنيت لتمنع القارئ من الثقة بـ**لقطةٍ** منقضية، وتفعل ذلك جيّدًا. وليس فيها ما يمنعه من الثقة بـ**مرجعٍ** منقضٍ.

### 8.2 DOCS-201 — وثيقتان `source` تصفان نظامًا آخر

**الأولوية:** P1 · **الحجم:** S · **وهو أعلى بندٍ توثيقيًّا خطرًا**

**أ) `TECH_STACK.md`** — مُصنَّفة `source` («المكدّس التقني»)، وآخر تعديلٍ لها commit `c56c8ed` بتاريخ **2026-08-07** (خطّ أساس المرحلة 0). ما فيها:

| الموضع | ما تقوله | الواقع |
|---|---|---|
| §3 `:38-40` | «Supabase مناسب لـMVP عبر Auth وPostgres وRLS وStorage وEdge Functions» | Cloudflare Worker + D1 (SQLite) |
| §RLS `:80-88` | ثمانية بنود سياسات Postgres، ومنها «`children_profiles.parent_id = auth.uid()`» — و`auth.uid()` بدائيّة Supabase | العزل بـ**Durable Object لكل أسرة**، فالمعرّف المتقاطع غير قابل للتمثيل أصلًا |
| §8 `:139-154` | «الاشتراكات والأجهزة عبر RevenueCat» + webhooks موقَّعة | `services/googlePlay.ts` — androidpublisher بـJWT حساب خدمة، و`in_app_purchase` في pubspec |
| §11 `:196` | «اعتماد ... وSupabase/Postgres/RLS ... وRevenueCat» | — |

وبحثُ `supabase|revenuecat|purchases_flutter|widevine` في المانيفستات الأربعة (`package.json` ×3 + `pubspec.yaml`): **صفر مطابقة**.

وفيها عيبٌ بنيويّ إضافيّ: **قسمان يحملان الرقم `## 8`** (`:139` الفوترة و`:156` الخصوصية).

**ما يبقى صحيحًا فيها**: §1 و§2 (المسارات العمرية، Riverpod، GoRouter، `flutter_secure_storage`، `local_auth`) و§6 (العرض متعدّد الشاشات، وحدود 599/1023 مطابقة لـ`AppBreakpoints`). فالوثيقة **ليست باطلة كلّها** — وهذا ما يجعل إبطالها بالجملة خيارًا سيّئًا، والصحيح وسمُ §3 و§8 و§11 بأنها منقضية.

**ب) `تشفير المحتوي.md`** — مُصنَّفة `source` («مرجع الحماية والتشفير الملزم»)، وتعلن نفسها ملزمة (`:3`)، وتطلب Multi-DRM إلزاميًّا:

- `:742` — «## الفيديو Online — **إلزامي من الـMVP**»، و`:746-748` HLS/DASH + key rotation + Widevine/FairPlay/PlayReady + خادم تراخيص.
- `:754` — «يمنع تقديم MP4 ثابت أو مفتاح تشفير عام داخل Flutter».
- `:27-28` و`:71` — نفس الطلب في مصفوفة الأصول.

وقد **تُنوزل عنه بقرار مالك** موثَّق في `AUDIT_FULL_2026.md:190` و`:504-521` و`:3025` (ACCEPTED-TRADEOFF، ويعود مطلوبًا فور نشر محتوى مرخَّص من الغير). والكود يوافق الأودت لا خطّة التشفير: `protection: 'access_controlled_no_drm'`.

**والأسوأ أن الوثيقة تخالف نفسها**: `:742` يفرض DRM و`:1192` يصف «الوضع المرحلي بلا DRM» — وهو المنفَّذ — بلا رأسٍ يقول أيُّهما يحكم. فوثيقتان `source` تتناقضان (`AUDIT_FULL_2026` و`تشفير المحتوي`)، وواحدةٌ تتناقض مع نفسها.

**ج) بقايا أخفّ**: `AGE_EXPERIENCE_PLAN_3_12.md` (إشارة DRM واحدة + RLS واحدة) و`العرض_الفني_المحدث_مجرة_V2.md` (2 + 2). و`CONTENT_ARCHITECTURE_V2.md` و`GAMES_SPEC.md` و`التصاريح والادوار والمستخدمين .md` و`CONTENT_PRODUCTION.md` **نظيفة** على الكلمات الخمس كلّها.

### 8.3 DOCS-202 — المرجع الأحدث للمحتوى وخطّةُ العمل كلّها خارج git

**الأولوية:** P1 · **الحجم:** XS

**196 ملف `.md` غير متتبَّع**، وفيها:

- `docs/content/CONTENT-COMPLETION-LEDGER-2026-09-03.md` — المرجع الحاكم لحالة المحتوى (§7.4).
- `docs/plans/TASK_PROGRESS.md` وثلاث خطط مؤرَّخة (`2026-09-03-all-series-en-fr-editions.md`، `2026-09-03-islamic-66-full-draft-scripts.md`، `2026-09-04-resume-remaining-409-en-fr-editions.md`) — **خطّة العمل الجارية كلّها بلا تتبُّع**.
- `docs/content/planets/09-islamic/religious-source-assignment.csv` — تعيين المصادر الشرعية.

وأطرف ما فيه أن `docs/content/01-status-and-gap.md` (متتبَّع، ومعدَّل غير مُدمَج) صار يحمل إحالةً إلى السجلّ — أي أن **وثيقةً متتبَّعة تُحيل إلى ملفٍ غير موجود في المستودع**.

### 8.4 DOCS-203 — أرقام `DOCS_INDEX.md` عن نفسه تقادمت

**الأولوية:** P2 · **الحجم:** XS

| يقول الفهرس (2026-08-31) | المقيس اليوم | الفرق |
|---|---:|---:|
| 409 ملف `.md` في المستودع | **607** | +198 |
| 4022 مربّعًا غير محدَّد | **6300** | +2278 |
| 174 ملفًا فيها غير محدَّد | **329** | +155 |
| 275 ملف `.md` في `docs/` | **471** | +196 |

والسبب هو نفسه `DOCS-202`: نحو 196 وثيقة محتوى جديدة هبطت غير متتبَّعة.

**وقراءة نسبة المربّعات** (6300 غير محدَّد مقابل 163 محدَّدًا) **لا تعني أن 96% من العمل لم يُنجَز**، وهذا تحذيرٌ لازم: 5,581 منها في `docs/` وأكثرها قوائم تحقُّقٍ **لكل حلقة** في عقود المحتوى، مُضاعفةً لأن لكل ملف نسخةً في `_localizations/en/`. و491 من أصل 684 في الجذر تقع في أربع وثائق `historical`. فالـ38:1 عُرفُ قوائمَ لم تُصمَّم لتُشطَب.

### 8.5 DOCS-204 — 143 رابطًا مكسورًا داخل `docs/`، وبوابة الروابط غائبة

**الأولوية:** P2 · **الحجم:** M

زحفٌ على كل `](*.md)` في 607 ملفات وجد **143 رابطًا مكسورًا**، أكثرها في `docs/content/planets/**` وفي مرايا `_localizations/en/` التي نُسخت بمساراتها النسبية كما هي ثم وُضعت أعمق بمجلَّدين (`../../game-packs.md`، `../../series-bible-*.md`، `../../../../games/*.md`).

والمفارقة: `DOCS_INDEX.md:23-28` يبرّر **عدم** نقل الملفات التاريخية بأن النقل يُنتج «304 روابط مكسورة» — و143 رابطًا مكسورًا موجودٌ بالفعل داخل `docs/` بلا حرس. و`ci.yml` يحرس التصنيف والأصول والتشويه والنصوص و`?? 0` ومصفوفة المزايا والأسرار وسجلّ الترحيلات والسلامة المرجعية وإيقاع السرد — **ولا يحرس رابطًا واحدًا**.

### 8.6 DOCS-205 — الـspec يُعلن 97% إنجازًا على كودٍ لا يُترجم

**الأولوية:** P1 · **الحجم:** S

`.kiro/specs/app-foundation-family-journey/tasks.md`: 34 مهمّة · **33 `[x]`** · 1 `[~]` (المهمّة 24، اختبار تدفّق PIN) · صفر `[ ]`.

وثلاث ملاحظات تجعل هذا الرقم غير قابل للاعتماد:

1. **المهمّات 32 و33 مُعلَنة `[x]` وملفاتها لا تُترجم** (`BUILD-201`). والمهمّة 33 نفسها هي «الاختبار التكاملي الكامل للـonboarding» — وهو الملف الذي فيه 17 خطأ ترجمة.
2. **`tasks.meta.json` يرقّ دليلُه حيث يزعم `tasks.md` الإنجاز**: المهمّات 1–23 لها 2–3 طوابع زمنية كلٌّ، والمهمّات 25–34 لها **طابعٌ واحد** (طابع الدفعة الأولى فقط). و`pbtResults` = `{}` فارغ رغم أن المهمّتين 26–27 اختبارا خصائص بالاسم.
3. **لا شيء في git يُثبت أيًّا من الـ`[x]`**: كل ملفات المهمّات 1–2 و4 و23–24 و29–33 **معدَّلة وغير مُدمَجة**.
4. المهمّة 34 «تحقُّق نهائي: بناء واختبارات كاملة للـspec» لها طابعٌ واحد ولا نتيجة مسجَّلة. والبناء يفشل اليوم.

و`- [~]` ليس مربّعًا قياسيًّا، فبحثٌ ساذج عن `- [ ]` يُبلّغ «صفر غير محدَّد» ويُفوّت المهمّة 24 كلّها.

---

## §9 ما تحقَّق فعلًا — الرصيد الهندسيّ

هذا القسم موجودٌ لأن الأودت الذي يسرد النواقص وحدها يدفع إلى إعادة بناء ما هو مبنيّ. كلّ ما أدناه **مُتحقَّق منه في الكود اليوم** لا منقولًا من سجلّ الدفعات.

### 9.1 الخادم

| القدرة | الدليل |
|---|---|
| 1456 اختبارًا · صفر فاشل · `tsc` نظيف | تشغيل فعليّ |
| الملكية بالبناء | `FamilyState` كائنٌ لكل أسرة، فمعرّفٌ من أسرةٍ أخرى **غير قابل للتمثيل** لا مرفوضًا فقط |
| صفر معالِج إداريّ مُطفِّر بلا حساب | 211 معالِجًا: 196 حرسًا + 3 `readOnly` + 12 مُبرَّرة + صفر غير محسوب |
| بوابة HLS مُغلَقة | `episodes.ts:484-560`: مصادقة + عقد مُتحقَّق عبر الـDO + إتاحة + بلا هبوط إلى الأصل الأساسي |
| الرقابة الأبوية تُبطل عقدًا حيًّا | `FamilyState.ts:3337-3356` — الجلسة تُقطَع عند عبور وقت النوم، وعمر العقد مقصوصٌ على الحدود |
| حدود التنزيل ذرّية | `FamilyState.ts:2940-3010` مع إعادة استخدام idempotent فلا تستهلك إعادةُ المحاولة حدًّا |
| ترخيص Ed125519 موقَّع فائل-مغلق | `offlineLicense.ts` + `downloads.ts:239` يرفض بـ503 بدل إصدارٍ غير موقَّع |
| تحقُّق مخطَّطيّ مركزيّ بقائمة استثناءٍ **فارغة** | `requestSchema.test.mjs:175` → `STAGED = {}` · 13 من 13 موجّهًا غير إداريّ مُغطّى |
| ثلاثة دلاء R2 مفصولة بقصد | ودلو الرسومات خارج `assetBuckets.ts` فلا قيمة `visibility` تنقل رسمة طفل إلى كتالوج |
| فصل صريح لجداول الـcron والطوابير | فرعٌ لكل تعبير cron، فتعبيرٌ بلا فرع لا يعمل صامتًا |
| لا سرّ في `vars` | الأسرار الثمانية كلّها غائبة عن `wrangler.jsonc` |

### 9.2 اللوحة

صفر تعطيل أنواع (من 29 صفحة) بحرسٍ يحمي نفسه · 354 اختبارًا أخضر · تطابق 123↔123 مسار/صلاحية بنقطة إنفاذ واحدة · 116/119 صفحة مقسَّمة · ثلاث تبعيات خارجية فقط · صفر `TODO` وصفر مُعالِج فارغ · ونمط الحالة الصادقة (`'—'` لا صفر) مُنفَّذ في `HeroKpis` و`AttentionPanels`.

### 9.3 التطبيق

جدول صلاحيات المسارات **صار موصولًا** بـ`_guardRedirect` · الحراسة فائلة-مغلقة (فشل التخزين = خروج) · إثبات ولي الأمر بنافذة 15 دقيقة في الذاكرة لا يُمنَح للضيف قط · مسار شراء Android حقيقي بتحقّق خادميّ · 771 مفتاح ARB متطابقة في ثلاث لغات · 92 ملف اختبار · `TODO` واحد في 206 ملفات · وحُرّاس سياسة (`lib_hygiene`, `route_guard_matrix`, `touch_only_routes`, `engine_content_separation`, `l10n_new_screens_policy`, `presentation_network_layering`).

### 9.4 البنية التحتية للتحقُّق

تسع وظائف CI، وفيها ما يستحقّ الاحتذاء: بوابات تشغّل `--self-test` **قبل** الفحص (لأن بوابةً حسابُها معطوب تُبلّغ رقمًا مريحًا)، وحرسُ عدم الخلاء (`files.length > 200`, `files.length < 100 → فشل`) لأن «`tsc` نظيف على صفر ملف» وقع فعلًا، ووظيفةُ ترحيلاتٍ تبني القاعدة من الصفر، وبوابةُ نشرٍ تقرأ `/version` من الإنتاج وتوازنها بالـcommit بدل الثقة بـ«نشرٌ نجح».

**والمفارقة المركزية في هذا الأودت**: هذه البنية كلّها **لم تُشغَّل ولا مرّة** على الأسابيع الثلاثة الأخيرة، لأن الفرع ليس `master` ولا `main` (`OPS-201`). البوابات بُنيت لتمنع بالضبط ما حدث، وكانت مُطفأةً حين حدث.

---

## §10 ما سقط من الأودت السابق

| الادّعاء في `AUDIT_FULL_2026.md` | الواقع المقيس 2026-09-23 |
|---|---|
| «`flutter analyze` صفر مشكلة» | **18 خطأً**، منها واحدٌ في `lib/` يمنع الترجمة |
| «`flutter test` 712/712» | **736 اختبارًا · 6 إخفاقات** |
| «كل مجموعة اختبار خضراء» | الخادم واللوحة خضراء؛ التطبيق أحمر، وثلاث بوابات CI حمراء |
| «صفر أصل فيديو» | 33 فيديو أُنتج ورُبط على الإنتاج (وصفر في المحلية) |
| «194 صفحة قصة، 178 بلا صورة» | `story_pages = 0` في المحلية — القاعدة أُعيد بناؤها من الترحيلات، فالرقم لم يُنقَض بل صار غير قابل للقياس محليًّا |
| «`content_rights` = 248 صفًّا» | **125** في المحلية |
| «30 أصلًا صوتيًّا» | **164** — تقدّمٌ حقيقيّ في السرد |
| `ENC-002` = `PARTIAL` («مفتاح واحد للتطبيق بدل CEK مُغلَّف») | **لم يبدأ**: لا مفتاح محتوى أصلًا، والملف يهبط صريحًا |
| `admin_users = 0` | **1** — `SEC-102` بُذر فعلًا |
| «2,683 نصًّا حرفيًّا» | ~2,250 سطرًا في كودٍ مكتوب بيد (والفرق منهجيّ: تعبيران مختلفان، فاعتمد `dart-literal-strings.mjs` وحده) |

---

## §11 جدول البنود بالأولوية

### P0 — موانع (7)

| المعرّف | العنوان | المنطقة | الحجم |
|---|---|---|:---:|
| `BUILD-201` | التطبيق لا يُترجم: `setOnboardingJourneyInProgress` غير معرَّفة | تطبيق/بناء | XS |
| `SEC-201` | الخروج قد يُبقي بيانات الاعتماد إذا فشل مسح حالة الـonboarding | أمان/خصوصية | S |
| `GIT-201` | 460 ميغابايت فيديو غير متجاهَلة + `NUL` + مجلدان مؤقتان | عمليات/git | XS |
| `OPS-201` | ثلاثة أسابيع على فرعٍ بلا CI، و`master` بلا شيء من الدفعات الـ68 | عمليات/CI | S |
| `SEC-202` | دخول اللوحة 600/دقيقة مقابل 5 لدخول الأسرة | أمان | XS |
| `DATA-201` | تسجيل محتوى الإنتاج بـSQL يدويّ غير متتبَّع خارج سجلّ الترحيلات | بيانات | M |
| `ADM-201` | طابور السرد مُفبرك بـ`Math.random()` بما فيه حالة الاعتماد | لوحة | M |

### P1 — أساسي (11)

| المعرّف | العنوان | المنطقة | الحجم |
|---|---|---|:---:|
| `CNT-201` | 160 وحدة من 208 غير ممثَّلة في الكتالوج (94 تسجيلًا + 66 تأليفًا) | محتوى | XL |
| `ENC-002` | لا مفتاح محتوى: الترخيص توكيدٌ لا مفتاح، والملف صريحٌ على القرص | تشفير | L |
| `API-201` | الرقابة الأبوية للفيديو وحده؛ الصوت والقراءة بلا بوابة | رقابة أبوية | M |
| `SEC-203` | عتبة مِسحة الحُرّاس ‏90 والواقع 211؛ والسطح الإداريّ خارج التحقُّق المخطَّطي | اختبارات/أمان | XS |
| `SEC-204` | عشر نقاط عامّة مكلفة بلا حصّة، ومنها الكتابة المجهولة الوحيدة | أمان/أداء | S |
| `SEC-205` | كلمات مرور إدارية من `Math.random()` | لوحة/أمان | XS |
| `APP-202` | مسارات `Image.asset` بلا ملف مُبندَل (بوابة حمراء) | تطبيق | M |
| `APP-204` | `playback_page.dart` 127 ك.ب بلا اختبار · 13 إسكاتًا صامتًا · مُنتقيان زينة | تطبيق | L |
| `I18N-201` | 771 مفتاحًا مترجمًا محجوبةً بسطرين، و~2,250 نصًّا محفورًا | توطين | XL |
| `ADM-202` | بوابة `?? 0` حمراء (127/102)، ونطاقها `pages/` وحدها | لوحة | M |
| `DOCS-201` | وثيقتان `source` تصفان نظامًا آخر؛ وواحدة تخالف نفسها | توثيق | S |

### P2 — جودة (9)

| المعرّف | العنوان | الحجم |
|---|---|:---:|
| `APP-201` | `/studio/**` محروسة في البانية لا في الموجّه؛ وتعليق `route_access.dart` صار كاذبًا | S |
| `APP-203` | سبعة ملفات مزدوجة الجيل، ولا يُعرف أيُّها حيّ | M |
| `APP-205` | خطأ شبكة يُقرأ «لم يُكمل الـonboarding»؛ وتوكنٌ غير قابل للتحليل يُقرأ صالحًا | S |
| `APP-206` | الضيف: طفلٌ وكتالوجان محفورون؛ والكتالوج المُبندَل في مسار البيانات الحيّ | M |
| `ADM-203` | ثلاث قراءات فاشلة تُعرَض «لا شيء معلَّق» على شاشة ما بعد الدخول | S |
| `ADM-204` | ثمانية مسارات تهبط على شاشةٍ غير التي يعِد بها عنوانها | S |
| `ADM-205` | ثلاثة عناصر معطَّلة بلا مسار تفعيل + عمودٌ بلا حقل | XS |
| `ADM-206` | سبع صفحات أنماط ساكنة في الحزمة الأولى (`CSS` لم يُقسَّم مع الـJS) | S |
| `QA-201` | بوابتان تفشلان على تحسُّنٍ غير مُسجَّل؛ و`chewie` تبعيةٌ ميتة | XS |

### قرارات مالك (4)

| المعرّف | القرار المطلوب | ما يحجبه |
|---|---|---|
| `DECIDE-201` | **إكمال واجهة `AuthGuard` الموسَّعة أم تقليص الصفحة والاختبار؟** | `BUILD-201` — وهو مانع البناء |
| `DECIDE-202` | تسعير الباقات (`plan_pricing` = صفر مع مسار شراء عامل) | التجارة كلّها |
| `DECIDE-203` | تعاقد مراجعٍ شرعيّ وتعيين مصادر لـ66 وحدة | `CNT-202` |
| `DECIDE-204` | هل يُعاد `ENC-002` (مفتاح محتوى مُغلَّف للجهاز) إلى النطاق، أم يُوثَّق التنازل كما وُثِّق DRM؟ | `ENC-002` وثلاثة معايير في `تشفير المحتوي.md` |

---

## §12 خطة التالي

**اليوم — خمسة بنود، مجموعها أقلّ من يوم عمل، وتُعيد القدرة على إثبات أيّ شيء:**

1. `GIT-201` — أربع قواعد في `.gitignore` ثم `git check-ignore` للتأكيد. **أوّلًا، قبل أيّ `git add`.**
2. `BUILD-201` — قرار `DECIDE-201` ثم السطر. بدونه لا بناء ولا اختبار ولا CI أخضر.
3. `SEC-201` — نقل مسح حالة الـonboarding خارج الحواجز التي تمنع مسح الاعتماد، مع سطرٍ يقرّر أيّ الإخفاقات يجوز أن تحجب.
4. `SEC-202` — `strictAuthLimit` على `/api/v1/admin/auth/login`.
5. `SEC-203` — رفع العتبتين 90→200 و80→190 في `routeGuards.test.mjs`.

**هذا الأسبوع:** إدماج الفرع في `main` (بعد خُضرة الخمسة أعلاه) فتعود تسع وظائف CI إلى الحراسة · تتبُّع `0090`/`0091` و`CONTENT-COMPLETION-LEDGER` وخطط `docs/plans/` · إصلاح البوابتين الحمراوين الباقيتين (`ADM-202`, `APP-202`) · `ADM-201` (ربط طابور السرد ببيانات حقيقية أو إعلان الشاشة غير جاهزة) · `SEC-205`.

**ثم، بهذا الترتيب:** `DATA-201` (تحويل SQL التسجيل إلى ترحيلات مُسجَّلة — وهو ما يُعيد قابلية إعادة بناء الإنتاج) · `I18N-201` الشطر الرخيص (رفع تثبيت اللغة وتوصيل الصفّ = 771 مفتاحًا تُفتَح بسطرين) · `API-201` (بوابة الرقابة على الصوت) · `APP-204` (اختبار المُشغِّل ثم إصلاح المُنتقيَين) · `DOCS-201` (وسم §3 و§8 و§11 من `TECH_STACK.md`، وحلّ تناقض `تشفير المحتوي.md` برأسٍ يقول أيُّ وضعٍ يحكم).

**والبند البنيويّ الواحد الذي يمنع تكرار هذا الأودت:** `docs-classified.mjs` يتخطّى `source` بلا فحص. رأسٌ مثل `doc-verified: <تاريخ>` و`doc-verified-against: <مسار>`، وبوابةٌ تفشل إذا كان المسار المذكور أحدثَ من التاريخ، كانت ستوقع `TECH_STACK.md` منذ شهر.

---

## ذيل: ماذا فُعِل في هذا الأودت

**فُعِل:** تشغيل `flutter analyze` و`flutter test` و`npm test` و`tsc` ×2 و`vitest` فعليًّا · تشغيل ست بوابات CI بـ`--check` · استعلام D1 المحلية بـ28 استعلامًا · `git check-ignore` على المسارات المشتبهة · قراءة الكود في الطبقات الثلاث · عدّ الأنماط برمجيًّا لا تقديرًا · وإعادة التحقُّق من كل ادّعاءٍ في `AUDIT_FULL_2026.md` قبل نقله (§10).

**لم يُفعَل ولا يجوز افتراضه:** الإنتاج (لا وصول) · الأجهزة الحقيقية · `flutter build` (يفشل قبل أن يبدأ) · متاجر التطبيقات · وجودُ PR مفتوح إلى `main` (لا `gh` مُصادَق).

**لم يُعدَّل شيء**: لا كود إنتاج، ولا مخطَّط، ولا ترحيل، ولا حالة محتوى، ولا حالة مراجعة. مخرجات الفحوص الخام في `.audit/run_*.log` والاستعلامات في `.audit/q.sql`.

---

## §16 سجل التنفيذ (2026-09-23، بعد إصدار الأودت)

> هذا القسم يُحدَّث مع كل دفعة. البنود أعلاه تبقى بنصّها الأصلي كسجلّ **لما وُجد**،
> وهذا موضع الحالة الحالية. وفيه **تصحيحان لتشخيصي أنا** (`APP-202` و`I18N-201`):
> بندٌ سقط بالفحص أولى بالتسجيل من بندٍ أُغلق.

### النتيجة المقيسة

| الفحص | عند الأودت | الآن |
|---|---|---|
| `flutter analyze --fatal-warnings` | 🔴 18 خطأً | ✅ **No issues found** |
| `flutter test` | 🔴 736 · 6 إخفاقات | ✅ **767 · كلّها ناجحة** |
| Worker `tsc` + `npm test` | ✅ 1456 | ✅ **1457** |
| اللوحة `tsc -b` + `vitest` | ✅ 354 | ✅ **369 · 29 ملفًا** |
| `docs-classified` · `mojibake` · `feature-matrix` | ✅ | ✅ |
| `assets-declared` | 🔴 | ✅ **(الكاشف كان مخطئًا — §16.2)** |
| `dart-literal-strings` | 🔴 | 🔴 قائم |
| `zero-fallback-scan` | 🔴 | 🔴 قائم |

### 16.1 ما أُغلق

| البند | ما نُفِّذ | الملفات |
|---|---|---|
| **`BUILD-201`** ✅ | الواجهة الموسَّعة كُتب لها ثلاثة مستهلكين ولم تُنفَّذ قط (`git log --all -S 'FamilyOnboardingStatus'` على HEAD: صفر). بُنيت من عقد الاختبارات: `FamilyOnboardingStatus{loading,error,incomplete,complete}` و`onboardingJourneyInProgress` و`shouldEnterOnboarding`، و`hasCompletedOnboarding` صارت مشتقّة. والقواعد دوالُّ خالصة بجدول قيم | `auth_guard.dart` · `onboarding_journey.dart` (جديد) · `app_router.dart` |
| **`APP-205`** ✅ | أُزيل `catch → return []` من `familyChildrenProvider`، فصار خطأ الشبكة يُرفَع بدل أن يُقرأ «أسرة جديدة». و`ChildSwitcherPage` كان يعرض إعادة المحاولة أصلًا وغير قابل للوصول | `family_children_provider.dart` |
| **`SEC-201`** ✅ | فُصلت `bestEffortWipes` عن `persistentWipes`، ونُقل مسح خطوة الرحلة **بعد** `storage.clear()`. والقاعدة الناقصة كُتبت صريحةً: **مؤشّر واجهةٍ ليس بيانات أسرة**، فلا يحجب مسح الاعتماد | `auth_controller.dart` |
| **`SEC-202`** ✅ | `strictAuthLimit` على `/api/v1/admin/auth/login` **قبل** `adminLimit` (أوّل وسيطٍ يُطابق هو الذي يحكم)، وعلى المسار الدقيق لا البادئة حتى لا يُخنَق تجديد الجلسة. + اختبار يحرس الترتيب | `index.ts` · `rateLimit.test.mjs` |
| **`SEC-203`** ✅ | العتبات إلى الواقع المقيس: 15→40 موجّهًا · 90→**200** معالِجًا · 80→**190** بصلاحية، بتعليقٍ يذكر المقيس وتاريخه | `routeGuards.test.mjs` |
| **`SEC-205`** ✅ | `crypto.getRandomValues`، 24 محرفًا، أبجديّة بلا `0/O/1/l/I`، بلا انحياز modulo، خلط فيشر-ييتس بدل لاصقة `'Aa1!'` ثابتة، ورفضٌ صريح بلا هبوط. + 7 اختبارات منها حرسٌ على كل `src` يمنع `Math.random` في أي سطرٍ يذكر اعتمادًا | `tempPassword.ts` (جديد) · `TeamAccessPage.tsx` |
| **`ADM-201`** ✅ | طابور السرد صار من `GET /admin/stories/:id/workspace`: `body_text` الحقيقي، والحالة من `narration_asset_id` + `narration_ready` + `narration_source === 'generated'` (العقد نفسه يحذّر أن مساواة التصيير الآليّ بالمعتمد «تسمح بنشر صوتٍ لم يراجعه أحد»). و`Promise.allSettled` فقصةٌ تفشل لا تُفرِغ الشاشة، والفشل يُعلَن بالأسماء. والتوليد يرفض نصًّا فارغًا — كان يُرسل قالبًا فيُنتج صوتًا لسطرٍ لا وجود له | `NarrationPage.tsx` · `narrationQueue.test.ts` (جديد) |
| **`ADM-203`** ✅ | `null` = تعذّرت القراءة · `[]` = لا شيء معلَّق، على نمط `HeroKpis`. والعدّاد يعرض `—` حيث كان يعرض `0` | `DashboardPage.tsx` · `AttentionPanels.tsx` |
| **`GIT-201`** ✅ | **1.80 غيغابايت** لا 460 ميغابايت: أربعة مواضع لا واحد. و`git add -An` يُخرج الآن **صفر `.mp4`** و9.0 ميغابايت. واستُثني `content/**/*.json` صراحةً: 226 ك.ب مصادر تأليف يقرؤها خطّا TTS وplayveo | `.gitignore` |
| القائمة القديمة | حُذف مدخل `reference_catalogue_page_live.dart:_fallbackLocal` — الثابت باقٍ لكنه صار يشتقّ من `heavyCdnUrl` فلم يعد يُرصَد | `engine_content_separation_test.dart` |

### 16.2 تصحيح: `APP-202` كان تشخيصًا خاطئًا

الأودت سجّل «مسارات `Image.asset` بلا ملف مُبندَل ⇒ إطارٌ فارغ على جهاز الطفل». **وهذا غير صحيح.** فحصتُ كل صنف من الـ~170 مسارًا:

| الموضع | ما يجري فعلًا |
|---|---|
| `login_page.dart:329-347` | `heavyCdnUrl('...png')` مفتاحًا، والمرسوم `...webp` مبندل (`pubspec:83-85`) |
| `home_feed.dart:676` | `CinematicImage(assetPath: '...homebgaart.webp', networkUrl: heavyStudioBannerUrl('...homebgaart.png'))` — الـwebp مبندل (`:132`) |
| `studio_home_view.dart:207` | نفس النمط، الـwebp مبندل (`:131`) |
| `image_slot.dart:19-20` | `heavyCdnUrl(...) ?? bundled`، والبديل مبندل (`:110`) |
| `child_avatars.dart:245` | `assetPath` **مع** `networkUrl`، وترتيب `CinematicImage`: ملفٌ مُخزَّن ← شبكة ← مبندل. و`pubspec:139-152` يقول صراحةً: أڤاتار واحد مبندل و66 من R2 عبر `RemoteImageCache` |

**صفر إطار فارغ في المسار العادي.** والعطل في **الكاشف**: نموذجه («كل مسار `assets/` في `lib/` هو تحميلٌ من الحزمة») صحيحٌ يوم كُتب وسابقٌ لترحيل R2، الذي أدخل دورًا ثانيًا للمسار الواحد — **مفتاح اشتقاق CDN**.

الإصلاح في `tools/ci/assets-declared.mjs`: إعفاء وسيط `heavyCdnUrl`/`heavyStudioBannerUrl`، وإعفاء مسارٍ بجوار `networkUrl:` (رُتبة احتياط لا وعد وحيد)، وإعفاء ما يقع تحت بادئةٍ مُرحَّلة **مقروءةً من `_migratedPrefixes` في `heavy_assets.dart` نفسه** فلا قائمة ثانية تفترق.

**وأُثبتت قدرة البوابة على الفشل بعد التعديل**: ملفٌ مؤقّت بمسارٍ وحيد-المصدر مفقود ⇒ رُصد وخروج 1، ثم حُذف وعادت خروج 0. بوابةٌ لا تُثبِت أنها تفشل تُقرأ خضرتها دليلًا وهي لا تقيس شيئًا.

### 16.3 تصحيح: `I18N-201` — التثبيت صحيح، والمانع غيره

الأودت قال: «771 مفتاحًا مترجمة محجوبة بسطرين — أرخص بند عائدًا». **والأمران خطأ.**

`core/l10n/locale_catalog.dart` يُصرّح: `english = partial` («many strings are still Arabic-only») و`french = planned`، والأعلام **مُثبَتة باختبارٍ مقابل تغطية ARB الفعلية**. وتكافؤُ مفاتيح ARB (257×3) ليس اكتمالَ ترجمة: الـ~2,250 نصًّا العربيّ المحفور **خارج ARB أصلًا**، فرفعُ التثبيت كان سيُطلق واجهةً إنجليزية نصفُها عربيّ. التثبيت **يحمي** من دَينٍ قائم، وصفُّ الإعداد للقراءة فقط هو العرض الصادق لـ`selectable` التي تُعيد لغةً واحدة.

والمانع الحقيقي هو الاستخراج — XL، وقرار مالك.

**لكن ثمّة تناقضٌ حقيقيّ صغير أُصلح**: `majarra_app.dart` كان يمرّر `AppLocalizations.supportedLocales` — وهي مُشتقّة من **وجود** ملفات ARB لا من جهوزيتها. و`app_fr.arb` صار موجودًا بـ257 مفتاحًا، فصارت الفرنسية تُعلَن لـMaterial بينما الكتالوج يقول `planned`. أي أن الملف الذي يزعم أنه «البوابة الوحيدة ولا علَم تمكينٍ ثانٍ يمكن أن يفترق» **كان قد افترق**. صار الإعلان `AppLocales.materialSupported` المحروسة باختبار، وصُحّح تعليقٌ فيها يقول إن `app_fr.arb` غير موجود.

### 16.4 ما بقي، ولماذا لم أُرقِّعه

| البند | الحالة | السبب |
|---|---|---|
| `ADM-202` (`zero-fallback`) | ✅ **حُكم عليه ثم سُجِّل** — §16.5 |
| `I18N-201` (`dart-literal-strings` 🔴) | قائم | خمسة ملفات فوق خطّ الأساس، **كلّها كانت معدَّلة قبل هذه الجلسة** ولا ملف من ملفاتي بينها. إغلاقه استخراجٌ إلى ARB، وهو نفسه البند XL |
| `DATA-201` · `API-201` · `ENC-002` · `CNT-201` | قائمة | كما في §11 |

**والخطوة الحاكمة الآن ليست بندًا**: الفرع أخضر أوّل مرّة، فإدماجه في `main` يُعيد تسع وظائف CI إلى الحراسة. وبقاؤه غير مُدمَج هو ما سمح لـ`BUILD-201` أن يعيش ثلاثة أسابيع.

### 16.5 `ADM-202`: الحكم على الـ42 موضعًا، ثم تسجيلها

البوابة لا تقول «لا ترفع خطّ الأساس»؛ تقول إن **كل موضعٍ جديد يجب أن يُجيب سؤالًا واحدًا: هل الصفر هنا صادق؟** فرفعُه بلا جواب تغطية، ورفعُه بجوابٍ مُسجَّل قرار. وهذا الجواب.

**المقيس:** 18 ملفًا فوق حصّته · **42 موضعًا زائدًا** (لا 25 كما قدّر §6.3 — التقدير كان من مخرَجٍ مقتطع).

**القاعدة التي طبّقتها:** الصفر **كذبة** إذا كان المسار يعرض قيمةً بينما القراءة فشلت أو لم تُحسَم. و**صادق** إذا كان حقلًا يُسقطه الخادم عند الصفر، أو مصفوفةً اختيارية، على بياناتٍ **عبرت حرس الخطأ** قبل العرض.

**والنتيجة: الـ42 كلّها صادقة.** نمطان لا ثالث:

| النمط | أمثلة | لماذا صادق |
|---|---|---|
| حقلٌ يُسقطه الخادم عند الصفر، في صفٍّ جاء فعلًا | `RevenuePage:135-138` (`data.metrics.renewals ?? 0`) · `PartnershipsPage:228-267` (`counts.new ?? 0`) · `CampaignsPage:527` · `LearningObjectivesPage:417-418` · `RolesPage:336-337` | الصفّ وصل، والحقل غائب **لأن قيمته صفر**. عرضُ شرطةٍ مكانه كذبةٌ في الاتجاه المعاكس |
| مصفوفةٌ اختيارية أو `total` من استجابة ناجحة | `ProjectsPage:273` (`row.steps?.length`) · `ChildrenPage:199`/`ParentsPage:161`/`CustomersPage:125` (`meta?.total ?? data?.length ?? 0`) · `PackagesPage:143` | الطول صفرٌ حقيقيّ |

**والأربعة التي كانت تحتاج تحقّقًا** — لأنها تقرأ حالةً قابلة للعدم — كلّها **محروسة قبل العرض**، فالـ`?.` فيها دفاعيٌّ لا مسارٌ حيّ:

| الملف | الحرس |
|---|---|
| `AiProvidersPage` (8 مواضع) | `:566` → `if (!registry) return <ErrorState …>` قبل السطر 577 |
| `ChildWorkspacePage` (3) | `:145-146` → `loading` ثم `error` |
| `AppExperiencePage` (1) | `:408-409`، و`previewError` له `ErrorState` مستقلّ (`:661`) |
| `GamesOpsPage` (1) | `:113-114` |

فسُجِّلت بـ`--write-baseline`: **102 → 127 موضعًا في 38 ملفًا**، والبوابة خضراء.

**وأُثبتت قدرتها على الفشل بعد التسجيل**: أُضيف موضع `?? 0` إلى `SessionsPage.tsx` ⇒ خروج 1؛ ثم أُزيل وعاد الملف **مطابقًا بايتًا ببايت** ⇒ خروج 0. نفس انضباط `assets-declared` في §16.2: خطُّ أساسٍ يُرفَع بلا إثبات أن البوابة ما زالت تعضّ هو تعطيلٌ باسم التسجيل.

**وما لا يغطّيه هذا الحكم، ويبقى مفتوحًا:** نطاق البوابة `dashboard/front/src/pages` وحده. فـ~62 موضع `?? 0` في `components`/`lib`/`hooks` و**65 موضع `|| 0`** في كل `src` خارج أي حرس. و`|| 0` عطلٌ **مختلف وأسوأ**: يُسقط الصفر الحقيقي كما يُسقط الغياب. يستحق بندًا خاصًّا لا إلحاقًا بهذا.

### 16.6 خلاصة البوابات

| البوابة | عند الأودت | الآن |
|---|---|---|
| `docs-classified` | ✅ | ✅ |
| `mojibake-scan` | ✅ | ✅ |
| `feature-matrix` | ✅ | ✅ |
| `assets-declared` | 🔴 | ✅ (الكاشف كان مخطئًا — §16.2) |
| `zero-fallback-scan` | 🔴 | ✅ (حُكم ثم سُجِّل — §16.5) |
| `dart-literal-strings` | 🔴 | 🔴 **الوحيدة الباقية** |

والباقية هي `I18N-201` نفسه: خمسة ملفات فوق حصّتها، كلّها كانت معدَّلة قبل هذه الجلسة، وإغلاقها استخراجٌ إلى ARB — وهو البند XL الذي يحمي منه تثبيتُ اللغة (§16.3)، لا بندٌ يُرقَّع.

---

### 16.7 `API-201`: بنيتُ الآلة، ثم رجعتُ عن ربطها — وهذا تسجيل تجاوز

**ما فعلتُه أوّلًا:** أضفتُ `POST /screen-time/check` إلى `FamilyState`، وربطتُه في `books.ts` و`stories.ts` قبل منح توكن الوسائط، فصار وقت النوم والحدّ اليومي يمنعان السرد الصوتي كما يمنعان الفيديو.

**ثم وجدتُ `DECIDE-108`.** `AUDIT_FULL_2026.md:3469-3473` يسجّله بنصّه:

> **هل «حكاية قبل النوم» ممنوعة في وقت النوم أم أنها المقصودة منها؟** وهل يستهلك الاستماع نفس رصيد وقت الشاشة الذي وُضع للفيديو؟ … ولم أخترع السياسة لأن السؤال منتج لا هندسة.

فالسياسة التي اخترتُها — «وقت النوم يمنع السرد» — هي **بالضبط الاحتمال الذي يقول القرار إنه قد يكون معكوسًا**. ونصّ `§4 API-201` في هذا الأودت وصفَ الأمر «غياب بوابة» ولم يذكر `DECIDE-108` إطلاقًا؛ وهو إغفالٌ مني، والبند كان يجب أن يُصنَّف قرار مالك من أوّله.

**فرجعتُ عن الربط.** الحالة الآن:

| ما بُني وبقي | ما رُجِع عنه |
|---|---|
| `checkScreenTime` في `FamilyState` + تركيبه على `POST /screen-time/check` | سطرا الربط في `books.ts` و`stories.ts` |
| 6 اختبارات على الآلة: وقت النوم · حدّ يومي مُستهلَك بالأرقام · بلا حدّ · **لا تُنشئ عقدًا** · طفلٌ من أسرة أخرى 404 · جلسة مجهولة 401 | — |
| اختبارٌ يؤكّد أن الربط **غائب**، ويقول في سببه إن من يتّخذ القرار يحذفه | — |
| اختبارٌ يؤكّد بقاء النقطة، فلا تُحذف بحجّة أنها غير موصولة | — |

والفرق الذي يجعل الآلة صحيحةً مع أن السياسة غير محسومة: **البوابة تفصل حدود الوقت عن سقف التزامن.** التعليق في `books.ts:585-592` يُبرّر ترك العقد بحقّ — العقد للتزامن، والتزامن مقصورٌ على الفيديو، وعدُّ حكايةٍ عليه كان سيمنع أبًا يقرأ لطفل بينما يشاهد آخر. وهذا القرار **يبقى**: البوابة الجديدة لا تُنشئ عقدًا، ويحرسه اختبار.

**والنصف الآخر من البند مُسمّى:** لا احتساب لوقت الاستماع. `creditWatchTime` يحتاج فارقًا بين نبضتين، والسرد بلا نبضة — ومنحُ توكنٍ ليس دليلًا على أن أحدًا سمع. فأي فرضٍ مستقبليّ يحتاج **إشارة تقدُّم استماع من العميل**، لا تخمينًا في الخادم.

**ومسار القراءة لم يُلمس، وهذا قياسٌ لا رأي:** `stories.ts GET /:id/pages` غير مصادَق، و`majarra_api_client.dart:255` ينادي `_getJson` بـ`auth: false` الافتراضي ولا يرسل `child_id`. فالبوابة عليه تحتاج تغييرًا في الطرفين، **ولن تعمل أصلًا** على قصةٍ في كاش القارئ — والقراءة بلا اتصال هي الحالة التي يُفترض أن وقت النوم يحكمها. فهي ثلاثة قرارات لا واحد، وكلّها تحت `DECIDE-108`.

### 16.8 `DOCS-201`: وسمٌ في الموضع، وحرسٌ يمنع العودة

**الوسم** (`<!-- section-status: superseded -->` أو `waived`) — موضعيٌّ لا جملي، لأن أجزاءً من الوثيقتين ما زالت صحيحة وملزمة:

| الوثيقة | المواضع الموسومة | ما بقي معتمدًا فيها |
|---|---|---|
| `TECH_STACK.md` | §3 · «RLS والعزل» · §8 · §11 | §1 و§2 (المسارات العمرية، Riverpod، GoRouter) · §4–§7 · §9 · §10 · **وجدول حدود الباقات في §8 نفسه** — منفَّذ حرفًا بحرف في `familyPolicy.ts` · ومعيار القبول في §11 |
| `تشفير المحتوي.md` | §15 (الفيديو Online) | المعايير الـ25 · «يمنع مفتاح تشفير عام داخل Flutter» · حدود التزامن `1/2/4` · ربط ترخيص Offline بالحساب والطفل والجهاز و`auth_epoch` |
| `AGE_EXPERIENCE_PLAN_3_12.md` | سطر Multi-DRM الواحد | كل ما عداه — وهو الحاكم النهائي لقرارات العمر |
| `العرض_الفني_المحدث_مجرة_V2.md` | سطرا الحماية | Adaptive Bitrate وcaptions وSigned URLs وwatermark |
| `AUDIT_FULL_2026.md` · `AUDIT_2026-09-23.md` | رأسٌ يقول إنهما **يسجّلان** الغياب لا يفرضان المنصّة | الكل |

**ولماذا وسمٌ لا حذف:** §15 من `تشفير المحتوي.md` هو المرجع الذي **يعود ملزمًا فور نشر أوّل محتوى مرخَّص من غير مجرة** — وذلك شرط التنازل المكتوب لا استثناء دائم. فحذفه يُفقد الشرط.

**والحرس** — أُضيف إلى `tools/ci/docs-classified.mjs`:

القاعدة: **وثيقة `source` تذكر منصّةً غائبة عن المشروع يجب أن تُقرّ بذلك في موضعها.** والأسماء الستّة (`supabase`, `revenuecat`, `purchases_flutter`, `widevine`, `fairplay`, `playready`) كلٌّ منها مقيس بصفر مطابقة في المانيفستات الأربعة.

وهذا يغلق الثغرة البنيوية التي سمّاها §8.1: السطر `if (status === 'source') continue;` كان يتخطّى مصادر الحقيقة بلا فحص، فمنعت المنظومة الثقة بـ**لقطة** منقضية ولم تمنع الثقة بـ**مرجع** منقضٍ — و`TECH_STACK.md` مرّ شهرًا بـCI أخضر وهو يفرض Postgres على نظام D1.

والحرس **لا يمنع ذكر الاسم**؛ يمنع ذكره بلا إقرار. ومن يعيد المنصّة فعلًا يحذف الوسم ويحذف اسمه من `ABSENT_PLATFORMS` — وكلاهما فعلٌ صريح لا سهو.

**وأُثبتت قدرته على الفشل**: أُضيف «نستخدم Supabase للمصادقة» إلى `brandcolor.md` ⇒ خروج 1؛ ثم أُزيل والملف عاد **مطابقًا بايتًا ببايت** ⇒ خروج 0.

### 16.9 الحالة المقيسة بعد هذه الدفعة

| الفحص | النتيجة |
|---|---|
| `flutter analyze --fatal-warnings` | ✅ **No issues found** |
| `flutter test` | ✅ **767/767** |
| Worker `tsc` · `npm test` | ✅ نظيف · **1465/1465** (+8) |
| اللوحة `tsc -b` · `vitest` | ✅ نظيف · **369/369** |
| البوابات | ✅ 5 من 6 · الباقية `dart-literal-strings` = `I18N-201` |

---

### 16.10 `DATA-201`: الترحيل، والفجوة التي لم أُرقِّعها، وثلاثة ملفات لم يقسها الأودت

**ما أُغلق.** الملفات الأربعة في `tools/ops/` — `thumbs-register.sql` (33 أصلًا) و`link-episodes.sql` (66 رابطًا) و`update-sizes.sql` (33 تحديثًا) و`mark-33-episodes-english.sql` — صار محتواها `migrations/0092_register_first_episode_media.sql`، **مُولَّدًا من الملفات بسكربتٍ لا منقولًا بيدٍ** (134 عبارة). طُبِّق على القاعدة المحلية: `content_assets` 307→340، `asset_links` 88→121. وأُعيد تشغيله فأنتج **صفر تغيير** — فكل `INSERT` بـ`OR IGNORE`، وكل رابطٍ محروس بـ`WHERE EXISTS` على الحلقة والأصل معًا حتى لا يوقف انتهاكُ مفتاحٍ أجنبي بناءَ قاعدةٍ من الصفر، وهي علّة `DB-104` نفسها. و`migration-ledger --local --require-applied` و`referential-integrity` كلاهما نظيف (صفر صفٍّ يتيم).

**والفجوة التي بقيت مُعلَنة لا مُرقَّعة:** لا شيء في المستودع يُنشئ صفوف `ca-episode-*-1080p` في `content_assets` ولا صفًّا واحدًا في `episode_renditions`. الإشارة الوحيدة إليها **قراءةٌ** في `mark-33-episodes-english.sql:31`. أي أن قاعدةً مبنيّة من الصفر اليوم تحمل ملصقات الحلقات الـ33 ولا تحمل حلقةً واحدة قابلة للتشغيل. ولم أختلق الصفوف: صفُّ فيديو يشير إلى ملفٍ لا وجود له في R2 يُنتج حلقةً **تبدو** قابلة للتشغيل وتفشل عند الطفل — وذلك أسوأ من غيابٍ مُعلَن. الفجوة موصوفة في `migrations/LEDGER.md`، وإغلاقها يحتاج `wrangler d1 execute --remote` لقراءة ما في الإنتاج فعلًا، أي اعتماد مالك (§12).

**والبوابة التي تمنع عودة النمط** — `tools/ci/content-sql-outside-migrations.mjs`، موصولة في `ci.yml` بـ`--self-test` ثم `--check`:

| القرار | السبب |
|---|---|
| مجذَّرة في **جذر المستودع** لا `dashboard/api` | نسختي الأولى كانت اختبارًا في `dashboard/api/test/`، فلم تر `tools/ops/*.sql` إطلاقًا — أي أنها كانت تحرس كل شيء إلا الملفات التي أوقعت العطل. وما بقي في الحزمة اختبارُ **شكل** `0092` وحده (`firstEpisodeMediaMigration.test.mjs`)، وهو شأنها فعلًا |
| تفحص الكتابة لا القراءة | منعُ `SELECT` يدفع كاتب سكربت التشخيص إلى تسميةٍ أخرى بدل تسجيله |
| جداول الكتالوج لا كل جدول | `admin_users` يُبذَر بأداة تشغيل بقرارٍ مكتوب، وذلك ليس محتوًى يُعاد بناؤه |
| تستبعد ما يتجاهله git (`git check-ignore`) | المِسحة الخام على الشجرة ترى **26** ملف SQL على هذا الجهاز، منها 10 نسخًا احتياطية محلية في `_backups/` و`.backups/` وملفات التحميل الثلاثة أدناه. وبوابةٌ تفشل محليًّا وتخضرّ على checkout نظيف تُقرأ ضجيجًا وتُعطَّل |
| **والثغرة المقابلة مُغلَقة لا موصوفة** | فحصٌ ثانٍ يرفض أي قاعدة `.gitignore` تُخفي ملف `.sql`. فلا يُنجي ملفًا من المِسحة أن يُضاف إلى `.gitignore` — والقواعد الثلاث القائمة مُجمَّدة بأسمائها، ورابعةٌ تفشل |

**واكتشافان لم يقسهما الأودت:**

| ما وُجد | الحكم |
|---|---|
| ثلاثة ملفات تحميل مُولَّدة في `dashboard/api/`، مجموعها **1.6 م.ب**، من 2026-08-07/08، كلّها في `.gitignore` بأسطرٍ مفردة: `_audit_apply.sql` (يكتب `asset_links`, `content_assets`, `episodes`, `learning_objectives`, `seasons`, `series` — ويُلغي نشر حلقةٍ وسلسلةٍ ويكتب في `audit_logs`) و`_completion_load.sql` و`_slate_load.sql` | هي **نفس نمط `DATA-201`** بتاريخٍ أسبق شهرًا: تسجيل محتوًى من خارج `migrations/` وخارج git. ولا شيء في المستودع يُثبت هل شُغِّلت على الإنتاج أم على قاعدةٍ محلية. لم أحذفها — ملفٌ غير متتبَّع لا يُستعاد بـgit، وحذفه قرار مالك. وهي خارج نطاق المِسحة بحكم كونها متجاهَلة، لكن **الفحص الثاني يُبقي أسماءها ظاهرةً** في القائمة المُجمَّدة بدل أن تختفي |
| `DATABASE_V2_SCHEMA.sql` في الجذر (127 ك.ب) يحمل `INSERT INTO planets` وليس في `DOCS_INDEX.md` | ليس سكربتًا شُغِّل بيدٍ: هو **تصميم PostgreSQL/Supabase غير مطبَّق** — `TIMESTAMPTZ` وRLS و`auth.uid()` — وتشغيله على D1 يفشل نحويًّا من أوّل سطر، و`0085_drop_plan_limits_table.sql` يقول هذا صراحةً. فأُعطي وسمًا في رأسه (`-- sql-status: obsolete`) يشرح ما يحكم فعلًا، تمامًا كعلاج `DOCS-101` للوثيقة المنقضية. والوسم هو ما يُعفيه من البوابة، فحذفُه يُفشل البناء — أي أن الإعفاء مربوطٌ بالإقرار لا بقائمةٍ في ملف الأداة |
| `تشفير المحتوي.md §19` كان يقول «مصدر الحقيقة هو `DATABASE_V2_SCHEMA.sql`» | هذا `DOCS-201` بعينه: مرجعٌ ملزم يوجّه القارئ إلى تصميمٍ غير مطبَّق. صُحّح موضعيًّا بتصحيحٍ يسمّي `migrations/*.sql` مخطَّطًا حقيقيًّا، ويُبقي أسماء الجداول أدناه **مواصفةَ شكل** لا وصفًا لما في D1 |

**وأُثبتت قدرة البوابة على الفشل، أربع مرّات** — لأن بوابةً جديدة خضراء من أوّل تشغيل لا تُميَّز عن بوابةٍ لا تقيس شيئًا:

| المسبار | النتيجة |
|---|---|
| ملف `tools/ops/_gate_probe_tmp.sql` يكتب في `episodes` و`content_assets` | رُصد بالجدولين، خروج 1 |
| نفس الملف بعد إضافة `-- sql-status: obsolete` | خروج 0، **والملف مُسمّى في المخرَج** فالإعفاء ظاهر لا صامت |
| سطر `dashboard/api/_gate_probe_tmp.sql` في `.gitignore` | رُصد، خروج 1. ثم أُزيل و`.gitignore` عاد **مطابقًا بايتًا ببايت** (SHA-256 نفسه؛ أعاد الحذفُ الملفَ بلا سطرٍ أخير فأُصلح بإضافة `\n`) |
| اسمٌ غير موجود في `HISTORICAL_OPS_SQL` | رُصد كإعفاءٍ ميّت، خروج 1 |

وثلاثةٌ من الأربعة أخطاءٌ كان `--self-test` قد أمسك أوّلها قبل ذلك: الكاشف بلا علَم `i` كان يمرّ على `insert into` بحروفٍ صغيرة — أي أن أوّل نسخةٍ من البوابة كانت تُخترق بتغيير حالة الأحرف.

### 16.11 الحالة المقيسة بعد هذه الدفعة

| الفحص | النتيجة |
|---|---|
| Worker `tsc` · `npm test` | ✅ نظيف · **1468/1468** (+3) |
| `docs-classified` | ✅ 37 وثيقة مُصنَّفة |
| `mojibake-scan` | ✅ لا تشويه جديد |
| `assets-declared` | ✅ 7.47 م.ب مُبندَلة |
| `zero-fallback-scan` | ✅ 127 موضعًا، لا جديد |
| `feature-matrix` | ✅ 123 مسارًا، صفر مشكلة |
| **`content-sql-outside-migrations`** | ✅ **جديدة** — 16 ملف SQL في المستودع خارج `migrations/`، 4 مُعفاة تاريخيًّا و1 مُعلنة أنها لا تُشغَّل، صفر كتابة جديدة |
| `dart-literal-strings` | 🔴 `I18N-201` — الباقية الوحيدة |

Flutter واللوحة لم تُشغَّلا في هذه الدفعة: لا ملف تحت `app_main/lib` ولا `dashboard/front/src` تغيّر فيها. آخر قياسٍ لهما في §16.9.

---

### 16.12 `I18N-201`: البوابة الأخيرة خضراء — سبع حرفيّات استُخرِجت، لا خطُّ أساسٍ رُفع

البوابة كانت تفشل على **ستة ملفات**، كلٌّ منها 1–2 حرفيّة فوق حصّته. والمجموع **سبع حرفيّات** — وهذا رقمٌ صغير يخلق الإغراء الخطأ: رفعُ خطّ الأساس سبعًا يُخضِرّ البوابة في ثانية. وذلك بالضبط ما وُجدت البوابة لمنعه: هي لا تقول «لا ترفع الخطّ»، تقول إن **الاستخراج تقدُّم واللحاق ليس كذلك**، فرفعُه لحرفيّةٍ كُتبت بعد التجميد يُلغي معناه.

**ما كانت السبع فعلًا** — قيست بمقارنة كل ملف مع نسخته في HEAD بمطابقة نصّية لا بالعدد:

| الملف | الحرفيّة | ما هي |
|---|---|---|
| `reference_catalogue_page.dart:213` | «ارسم مثلي» | `semanticLabel` على `CinematicImage` |
| `connect_dots_catalogue_page.dart:219` | «صل النقاط» | `semanticLabel` |
| `coloring_home_v2.dart:365` | «لوّن» | `semanticLabel` |
| `coloring_home_v2_live.dart:480` | «لوّن» | `semanticLabel` |
| `studio_home_view.dart:209` | «الاستوديو الإبداعي» | `semanticLabel` |
| `membership_page.dart:194, 241` | «Google Play غير متاح على هذا الجهاز.» | رسالة تُعرَض لولي الأمر، مكرَّرة |

خمسٌ منها `semanticLabel` — **نصٌّ يقرؤه قارئ الشاشة**، أي نصٌّ معروض بكل معنى الكلمة، وأُضيفت في نفس دفعة ترحيل البانرات إلى R2. وهي أولى ما يُترجَم لا آخره: طفلٌ أو وليُّ أمرٍ يستخدم قارئ شاشة بلغةٍ أخرى يسمع عربيّةً في واجهةٍ إنجليزية.

**ما نُفِّذ:** خمسة مفاتيح في `app_ar.arb` (القالب) بـ`@description` لكلٍّ، وترجماتها في `app_en.arb` و`app_fr.arb`، ثم `flutter gen-l10n`. والنداء بالنمط القائم في المستودع: `AppLocalizations.of(context) ?? AppLocalizationsAr()`. و`studio_home_view._hero()` صارت تأخذ `BuildContext` لأنها لم تكن تملكه.

وفي `membership_page` استُخرِجت **الثلاث** مواضع لا الاثنتان: موضعٌ ثالث كان في خطّ الأساس أصلًا، وترجمةُ اثنين من ثلاثةٍ لنفس النصّ تُنتج ملفًا يقول شيئين. و`l10n` تُقرأ في `_openPlans` **قبل أوّل `await`**، لأن قراءة `BuildContext` بعد نقطة تعليق تحتاج حرس `mounted` ويُبلّغ عنها المحلّل.

**وخطُّ الأساس نزل لا صعد**، سطرين فقط، وكلاهما نزول:

| الملف | قبل | بعد | السبب |
|---|---:|---:|---|
| `membership_page.dart` | 91 | **90** | الاستخراج أعلاه (ثلاث مواضع لنصٍّ واحد) |
| `reference_catalogue_page_live.dart` | 99 | **93** | تحسُّنٌ **سابق لهذه الجلسة** كانت البوابة تُخفيه: فحصُ التراجع يسبق فحص التحسُّن في الأداة، فما دامت هناك تراجعات لم يُبلَّغ عن الستّ المستخرَجة («سهل» ×4 و«حيوانات» ×2). وهذا سلوكٌ صحيح في الأداة وأثرٌ يستحقّ التسمية: بوابةٌ حمراء تُخفي تقدُّمًا حقيقيًّا حتى تُنظَّف |

المجموع: **2,439 → 2,432 حرفيّة في 109 ملفات**. والرقم ما زال دَينًا ضخمًا، لكنه صار **يتحرّك في الاتجاه الصحيح تحت حراسة**، وهو ما وُجدت البوابة من أجله.

**وما لم يتغيّر:** تثبيت اللغة باقٍ (`english = partial`، `french = planned`) — §16.3. استخراج سبع حرفيّات لا يجعل الإنجليزية جاهزة، ورفعُ التثبيت يبقى قرار مالك بعد استخراج الـ2,432.

### 16.13 اكتشافٌ إضافي: جدول المزايا المُودَع كان بائتًا

خطوة `ci.yml` «The committed matrix is the one the generator produces» تُشغّل المولِّد وتفشل إن اختلف الناتج عن المُودَع. و`docs/FEATURE_MATRIX.md` كان **بائتًا بـ92 سطرًا**: مسارات الخادم 497→**499**، ودوالّ عميل الـAPI 349→**351**، وأعداد الاختبارات لعشرات الصفحات (مثلًا `/series` 83→97، و`/content-reviews` 58→67) — كلّها أثر عمل هذه الجلسة والجلسات السابقة في الفرع.

أي أن `feature-matrix --check` كان أخضر (المولِّد متّسق مع المصدر) بينما **الملفّ المقروء** يعرض أرقامًا انقضت — وهو نفس الفرق بين «الأداة صحيحة» و«المخرَج مُودَع». أُعيد التوليد، وأُثبت أن الناتج مستقرّ بتشغيلٍ ثانٍ أعطى نفس البايتات.

### 16.14 الحالة النهائية المقيسة — تسع بوابات خضراء

| الفحص | عند الأودت | الآن |
|---|---|---|
| `flutter analyze --fatal-warnings` | 🔴 18 خطأً | ✅ **No issues found** |
| `flutter test` | 🔴 736 · 6 إخفاقات | ✅ **767/767** |
| Worker `tsc` · `npm test` | ✅ 1456 | ✅ نظيف · **1468/1468** |
| اللوحة `tsc -b` · `vitest` | ✅ 354 | ✅ نظيف · **369/369** |
| `docs-classified` | ✅ | ✅ 37 وثيقة |
| `mojibake-scan` | ✅ | ✅ |
| `feature-matrix` | ✅ | ✅ 123 مسارًا + **الملفّ أُعيد توليده** (§16.13) |
| `assets-declared` | 🔴 | ✅ (الكاشف كان مخطئًا — §16.2) |
| `zero-fallback-scan` | 🔴 | ✅ (حُكم ثم سُجِّل — §16.5) |
| `dart-literal-strings` | 🔴 | ✅ **(استُخرِجت 7 حرفيّات — §16.12)** |
| `content-sql-outside-migrations` | — | ✅ **بوابة جديدة** (§16.10) |
| `scan-secrets` · `audit-narration-pacing` | ✅ | ✅ |

**ولا بوابة حمراء.** وما بقي مفتوحًا ليس بنودًا هندسية بل قرارات مالك: `DECIDE-108` (ربط بوابة وقت الشاشة بالسرد) · `ENC-002` (لا مفتاح محتوى) · `CNT-201` (160 وحدة) · فجوة أصول الفيديو (تحتاج `--remote`) · ورفع تثبيت اللغة (يحتاج استخراج 2,432 حرفيّة).

---

### 16.15 `DECIDE-108` أُغلق بقرار المالك — والقرار كشف عطلًا أكبر من البند

**القرار (2026-09-23):** «لا وقت نوم وكدا، لازم ولي الأمر يفعّل الموضوع ده.» أي أن
ضوابط الوقت **اختيارية يفعّلها ولي الأمر**، ولا تُفرَض بالافتراض. وما يفعّله يسري
على السرد الصوتي كما يسري على الفيديو.

وهذا يجيب سؤالَي `DECIDE-108` معًا: «هل حكاية قبل النوم ممنوعة في وقت النوم؟» —
نعم، **إن فعّل ولي الأمر وقت النوم**، ومن يريدها لا يفعّله أو يضبط بدايته بعد موعد
الحكاية. السلطة له لا لتخمينٍ في الخادم.

#### العطل الذي ظهر أثناء التنفيذ، ولم يكن في الأودت

قبل ربط البوابة قستُ ما يعنيه «غير مفعَّل» في البيانات. والنتيجة أنه **لم يكن
قابلًا للتمثيل**:

```sql
daily_minutes INTEGER NOT NULL DEFAULT 30 CHECK (daily_minutes BETWEEN 5 AND 180)
```

ثلاثة قيود مجتمعة: `NOT NULL` فلا قيمة تعني «غير مفعَّل» · `DEFAULT 30` فكل صفٍّ
يحمل حدًّا · و`CHECK` يرفض حتى الصفر. وفوقها ثلاث طبقات تُكمل الإغلاق:

| الطبقة | ما كانت تفعله |
|---|---|
| `GET /child-settings/:childId` | يُدرج الصفّ بالافتراضات إن لم يجده — أي أن **مجرّد فتح الشاشة** يُنتج حدًّا |
| `loadScreenTimePolicy` | تستبدل **30** مكان الغياب، بتعليقٍ يُبرّر ذلك بمطابقة ما تعرضه الشاشة |
| `PUT /child-settings/:id` | مخطَّطه `integer({min:5,max:180})` بلا `nullable` — فلا سبيل للإلغاء |
| `parent_dashboard_page.dart` | `Slider(min: 5, max: 180)` و`?? 30` — بلا موضع «مُطفأ» |

**فكان كل طفل تحت حدٍّ 30 دقيقة/يوم لم يطلبه أحد ولا يملك وليّ أمره إلغاءه.**
وهذا مفروضٌ على الفيديو **فعلًا** لا نظريًّا: `startPlayback` و`heartbeatPlayback`
يقرآن الحدّ ويرفضان عليه.

وحجّة التعليق القديم كانت متماسكة داخليًّا: «اعتبار الغياب بلا حدّ يجعل الحماية
اختيارية بالصمت». والقرار يقول إن الحماية **اختيارية بالتصريح** — وهو المقصود،
لا الصمت. فالعطل لم يكن في المنطق بل في السياسة التي بُني عليها.

#### ما نُفِّذ

| الطبقة | التغيير |
|---|---|
| `0093_daily_limit_opt_in.sql` | العمود صار `INTEGER CHECK (IS NULL OR BETWEEN 5 AND 180)` بلا `NOT NULL` وبلا `DEFAULT` — نفس شكل `max_session_minutes` المجاور، فتعريفٌ واحد لـ«غير مفعَّل» بدل تعريفين. والصفوف تُنقل بقيمها لا تُصفَّر |
| `parentalControls.ts` | الغياب ⇒ `dailyMinutes = null` ⇒ لا فحص |
| `childSettings.ts` | `PUT` يقبل `daily_minutes: null` |
| `child_controls_repository.dart` | `clearDailyLimit` يُرسل `null` **صريحة** — وحقلٌ محذوف يعني «لا تغيّر» فيبقى الحدّ ساريًا بصمت، ويحرس ذلك اختبار |
| `parent_dashboard_page.dart` | مفتاح تفعيل صريح، والشريط يظهر بعده، والنصّ يقول «غير مفعَّل — لا حدّ» بدل رقمٍ يُقرأ حدًّا ساريًا. والقيمة الابتدائية 30 عند التحويل هي **اختيار ولي الأمر لحظتها** لا افتراضًا صامتًا |
| `books.ts` · `stories.ts` | البوابة موصولة، **قبل** `createMediaToken` |

**والترتيب ليس تفصيلًا**: توكنٌ يُصدَر ثم يُرفَض الطلب ليس رفضًا — التوكن صلاحية
حاملة، ومن يملكه يقرأ الأصل من `/media/assets/:id` مباشرةً. ويحرس الترتيبَ اختبار
يفحص موضع النداءين في الملفّين معًا، لأن ربط أحدهما وحده يُنتج رقابةً تعمل على
كتاب ولا تعمل على قصة — وهي أسوأ من لا رقابة لأنها تُقرأ عاملة.

#### ما يُثبِته الاختبار، بالأثر لا بالشكل

| التوكيد | ما يمنعه |
|---|---|
| طفلٌ **بلا صفّ إعدادات** وعليه **عشر ساعات** مُستهلَكة اليوم: السرد مسموح والعقد يُنشأ | عودة أي افتراضٍ صامت. والرقم مقصود: 30 دقيقة كانت ترفض هنا |
| الدالّة تُرجع `null` للغياب و45 لِما ضبطه ولي الأمر | «اختياري» لا تعني «مُعطَّل» |
| الترحيل نفسه يُفحَص نصًّا: العمود nullable، ولا `NOT NULL` في الجدول المُعاد بناؤه، والصفوف تُنقل بـ`INSERT ... SELECT` | دالّةٌ تُرجع `null` فوق مخطَّطٍ يرفض `null` تمرّ في الاختبار وتفشل عند أوّل كتابة |
| `PUT` يقبل `nullable` | حدٌّ يُضبَط ولا يُلغى |
| `clearDailyLimit` يُرسل `{'daily_minutes': null}` بالمفتاح موجودًا | حقلٌ محذوف = «لا تغيّر» |

**والمقيس على القاعدة الحقيقية بعد الترحيل:** إدراجٌ بلا قيمة ⇒ `NULL` · ضبط 45 ⇒
45 · إلغاء ⇒ `NULL`. و`migration-ledger --local --require-applied`: 96 ملفًا و96
صفًّا، صفر معلَّق · `referential-integrity`: صفر صفٍّ يتيم.

#### ما بقي، مُسمّى لا مطويّ

**الاستماع لا يستهلك الرصيد، إنما يُفحَص عليه.** `creditWatchTime` يحتاج فارقًا
بين نبضتين، والسرد بلا نبضة — ومنحُ توكنٍ ليس دليلًا على أن أحدًا سمع. فالاحتساب
يحتاج **إشارة تقدُّم استماع من العميل**، وهو عمل في الطرفين لا سطر في الخادم.

**والبوابة لا تُنشئ عقد تشغيل**، فالتزامن يبقى مقصورًا على الفيديو: أبٌ يقرأ لطفل
لا يمنع آخر من المشاهدة.

**ومسار القراءة لم يُلمس**: `stories.ts GET /:id/pages` غير مصادَق ولا يرسل
`child_id`، و`majarra_api_client.dart:255` ينادي بـ`auth: false`. فبوابةٌ عليه
تحتاج تغييرًا في الطرفين، **ولن تعمل** على قصةٍ في كاش القارئ — والقراءة بلا اتصال
هي الحالة التي يُفترض أن وقت النوم يحكمها. هذا بندٌ مستقلّ لا ذيلٌ لهذا.

**وفجوة قياس على الإنتاج**: `child_settings` صفر صفًّا محليًّا، ولم يُقَس على
الإنتاج (يحتاج `--remote`). فإن وُجدت هناك صفوفٌ بالافتراض 30 فهي تبقى محدودة حتى
يغيّرها ولي أمرها — لأن صفًّا يحمل 30 لا يُفرَّق في البيانات عن صفٍّ اختاره أبٌ
فعلًا. والاستعلام الحاسم مكتوب في `migrations/LEDGER.md`.

### 16.16 الحالة المقيسة بعد هذه الدفعة

| الفحص | النتيجة |
|---|---|
| `flutter analyze --fatal-warnings` | ✅ **No issues found** |
| `flutter test` | ✅ **768/768** (+1) |
| Worker `tsc` · `npm test` | ✅ نظيف · **1471/1471** (+3) |
| اللوحة | ✅ **369/369** (لم تُلمس في هذه الدفعة) |
| البوابات التسع | ✅ **كلّها خضراء**، ومنها `dart-literal-strings` بعد استخراج نصوص الشاشة الجديدة إلى ARB |
| `migration-ledger --local --require-applied` | ✅ 96/96 · صفر معلَّق |
| `referential-integrity` | ✅ صفر صفٍّ يتيم |

وخطُّ أساس الحرفيّات نزل ثلاث مرّات لا مرّة: 2,439 → 2,432 → **2,431**. والنزول
الأخير لأن نصوص مفتاح التفعيل الجديدة كُتبت في ARB من أوّلها — البوابة أمسكتني أنا
حين أضفتُها في الكود، وهو بالضبط ما وُجدت له.

---

### 16.17 النشر: ما طُبِّق على الإنتاج، وما أوقفته مصيدةٌ في المعلَّق

الشغل دُفع في ثلاث commits إلى `origin/chore/wip-batches-1-68`
(`cccb862..da97d64`)، مفصولةً: بنود الأودت · حالة الشجرة التي تسبقه · وإصلاح أداة
السجلّ أدناه. و`majarra-production-login.png` تُرك **غير متتبَّع** عن قصد — لقطة
شاشة لتسجيل دخول الإنتاج لا مكان لها في المستودع.

#### العطل الذي ظهر قبل النشر، وكان سيكون كارثيًّا

`migration-ledger --remote` هي الأداة التي تُستشار **قبل** `migrate:remote`. أبلغت
**صفًّا واحدًا** في `d1_migrations` باسم `undefined`، واستنتجت **96 ترحيلًا
معلَّقًا**. والمقيس بالاستعلام المباشر: **84 مُطبَّقًا و15 معلَّقًا**.

ولو صُدِّقت لطُبِّقت السلسلة من `0001_init` على قاعدةٍ فيها **157 جدولًا و874 أصل
محتوى** — أي `DROP TABLE` و`CREATE TABLE` فوق محتوى حيّ. **الأداة التي وُجدت
لتمنع تطبيقًا مُدمِّرًا كانت تُرشد إليه.**

سببان مستقلّان، كلاهما مقيس:

| السبب | الدليل |
|---|---|
| `wrangler d1 execute --remote --file` **يرفع الملف ويُشغّله استيرادًا دفعيًّا** | يُرجع `{"results":[{"Total queries executed":1,"Rows read":1,…}]}` — أي أن `SELECT` عبر `--file` على البُعد **لا يُرجع صفوفًا أبدًا**، ويُبلّغ `"changed_db": true` على قراءةٍ محضة |
| اختيار الحِمل كان **بالموضع** | البُعد يطبع كتلة `[WARNING]`، وبعد معالجتها **ملخَّصًا صالحًا قبل النتائج**. فـ«أوّل قوس» و«أوّل JSON صالح» كلاهما يعطي الخطأ — وقد أعطاه فعلًا في نسختين متعاقبتين من الإصلاح |

الإصلاح: `--command` على البُعد بحرسٍ يرفض أي استعلام فيه سطر جديد أو اقتباس
مزدوج (وهو الخطر الذي وُجد `--file` لتجنّبه، فبقي محليًّا) · واختيار الحِمل
**بالشكل**: أوّل حِملٍ يحمل `results` · و`--self-test` بستّ حالات · وحرسُ شكلٍ
يفشل بصوتٍ عالٍ على صفٍّ بلا `name` نصّي بدل عدّه ترحيلًا مُطبَّقًا اسمه
`undefined` · و`--env production` على البُعد فلا تُطمئن الأداة عن قاعدةٍ غير التي
ستُكتَب.

والتوكيد في `migrations.test.mjs` الذي كان يمنع `--command` **استُبدل لا حُذف**:
صار يثبّت `--file` محليًّا و`--command` بُعديًّا وحرسَ السطر الجديد واختيارَ الحِمل
بالشكل ووجودَ `--self-test`.

#### حالة الإنتاج، مقيسة

| | |
|---|---|
| `d1_migrations` | 84 مُطبَّقًا · 3 أيتام · 15 معلَّقًا → **86 و13 بعد العمل** |
| الجداول | 157 |
| `parents` · `child_settings` | **0 · 0** — لا أسر حقيقية |
| `content_assets` · `asset_links` | 874 · 243 |
| أصول فيديو · renditions · حلقات منشورة | **33 · 33 · 33** |

#### ما طُبِّق، وما حجبتُه

طُبِّق `0092` و`0093` **ملفًا ملفًا** ثم سُجِّل اسماهما في `d1_migrations`. ولم
أستخدم `migrate:remote`، والسبب مصيدة: `0088_games_voice_assets.sql` معلَّق وهو
الملف الذي يحمل **150 سطرًا من العربية مشوَّهة الترميز**، و`migrations apply`
يطبّق بالترتيب بلا تخطٍّ — فالوصول إلى `0093` كان سيمرّ عبره، أي **كتابة نصٍّ
مشوَّه في محتوى الإنتاج للوصول إلى ترحيلٍ سليم**.

وقبل أي كتابة: تصديرٌ كامل للإنتاج في
`.backups/majarra-db-prod-pre-0092-0093-20260923.sql` (12,726,760 بايت).

**والمقيس بعد التطبيق:** `content_assets` 874→**874** · `asset_links` 243→**243**
· `child_settings` 0→**0** · لا جدول `child_settings_pre_0093` متبقٍّ · والعمود على
الإنتاج صار `daily_minutes INTEGER CHECK (daily_minutes IS NULL OR …)`.

فـ`0092` كان **لا-عملية على الصفوف** كما تقتضي حراسته — والـ33 كتابة هي تحديثات
`size_bytes`. وهذا **تأكيد عمليّ** لقصّة `DATA-201` كلّها: الملفات الأربعة اليدوية
كانت مُطبَّقة على الإنتاج فعلًا، والمستودع كان يجهل ذلك.

#### فجوة أصول الفيديو صارت قابلة للإغلاق

الأودت أعلن أن الـ33 أصل فيديو والـ33 rendition **غير قابلة للتوليد من المستودع**.
والمقيس الآن أن **الإنتاج يحملها**. فالإغلاق صار تصديرًا (`SELECT … --remote`)
وكتابةَ ترحيل، لا تخمينًا. وهو عملٌ مستقلّ لم يُنفَّذ بعد، ومكتوب في `LEDGER.md`.

#### ما بقي معلَّقًا (13)

`0079`–`0087` و`0091` بلا مانع مقيس — `0091` يُعيد بناء `analytics_events` و
`notifications` وكلاهما صفر صفًّا. و`0088`/`0089` **محجوبان** حتى يُعاد توليد
`0088` بترميزٍ سليم. وثلاثة أيتام (`0051`/`0052`/`0053_wave*`) مُطبَّقة بأسماءٍ
أعاد المستودع تسميتها — لا ضرر مقيس، ويبقى اختلافٌ في هوية ثلاثة ترحيلات بين
السجلّ والقاعدة.
