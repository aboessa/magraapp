# تقرير حالة لوحة التحكم (Admin Dashboard)

> تاريخ الإعداد: 2026-08-09
> المصادر: `docs/DASHBOARDv2.md`، `dashboard/front`، `dashboard/api`، `KIRO_LAST_REPORT.md`، `التصاريح والادوار والمستخدمين .md`

## ملاحظة مهمة أولاً

يوجد في المشروع نظامان تحت مسمى "لوحة التحكم":

1. **`admin-dashboard.html`** (في جذر المشروع) — ملف HTML/CSS/JS واحد، مجرد نموذج تصميم أولي (mockup) بمتغيرات CSS ووضع dark/light وشريط جانبي وبطاقات إحصائيات. **لا يحتوي على منطق حقيقي متصل بأي API**، وليس هو التطبيق الفعلي.
2. **`dashboard/`** — هذا هو **التطبيق الحقيقي والمُنفَّذ فعليًا**:
   - `dashboard/front`: React + TypeScript + Vite + react-router
   - `dashboard/api`: Cloudflare Workers + D1 + R2 + Queues + Durable Objects

كل ما يلي يتحدث عن `dashboard/` الحقيقي.

---

## ما تم تنفيذه فعليًا

### البنية التقنية
- **Frontend**: `dashboard/front/src`، توجيه عبر `AdminRoutes.tsx`، حارس جلسة (session guard) يتحقق من `/admin/auth/me` عند التحميل.
- **Backend**: `dashboard/api/src/routes/` — أكثر من 25 مسار (`admin.ts`, `adminAuth.ts`, `adminUsers.ts`, `adminTeams.ts`, `adminContent.ts`, `adminCatalogue.ts`, `adminAssets.ts`, `adminAnalytics.ts`, `adminMastery.ts`, `adminBilling.ts`, `adminBackup.ts`, `adminAppExperience.ts`, `adminSiteMode.ts`, `adminPartnerships.ts`, `adminFamilyProjection.ts`, `adminTts.ts`، وغيرها).
- **التخزين**: D1 لبيانات الإدارة، R2 للصور/الصوت/ملفات ZIP، Durable Objects (`FamilyState`) لكل أسرة — الإدارة تقرأ منها فقط (قرار معماري سليم موثّق في ملف الصلاحيات).

### الشاشات المُنفَّذة (45 صفحة مسجّلة في `AdminRoutes.tsx`)

| الفئة | الشاشات |
|---|---|
| المحتوى | السلاسل، المواسم، الحلقات، الشخصيات، القصص، مكتبة المحتوى (تشمل الألعاب)، مكتبة الوسائط، الأنماط البصرية، التصنيف (Taxonomy) |
| التعليم | المهارات، الأهداف التعليمية، Mastery، بناء الاختبارات (Quiz Builder) |
| المستخدمون | أولياء الأمور، الأطفال، الأجهزة، مركز الدعم |
| الفرق والصلاحيات | الفرق، الأدوار، صلاحيات الفريق، مهامي، Workflow، مراجعات المحتوى، سجل التدقيق (Audit Log) |
| التجارة | الفواتير، الباقات، الحقوق، الإيرادات، المالية المتقدمة، حسابات المدارس، الشراكات |
| تجربة التطبيق | بناء الصفحة الرئيسية، Remote Config، التوصيات |
| التسويق | الحملات |
| التشغيل والنظام | العمليات، SLA، الأحداث الفاشلة، السرد الصوتي، الجودة، مركز الترجمة، التحليلات، الإعدادات |

### نظام الصلاحيات والأدوار
مطابق لنموذج "الدور + النطاق + نوع المحتوى + اللغة" الموثق في `التصاريح والادوار والمستخدمين.md`. توجد شاشات `TeamsPage`, `RolesPage`, `TeamAccessPage` مع مسارات API فعلية (`adminTeams.ts`, `adminUsers.ts`) وسجل تدقيق (`AuditLogPage`). **لم يتم التحقق بعمق** من مدى تطبيق مبدأ "التحقق من الصلاحية داخل الـ Worker لكل طلب" على كل مسار على حدة.

### أمثلة على استبدال بيانات مزيّفة (mock) ببيانات حقيقية
هذا نمط موثّق داخل الكود نفسه (تعليقات JSDoc تسجّل "قبل/بعد"):

- **الأجهزة (`DevicesAdminPage`)**: كان يعرض جهازين وهميين دائمًا "نشطين". الآن `POST /admin/devices/:id/revoke` حقيقي مع تسجيل في `audit_logs`. **لكن إشعار ولي الأمر عند الإلغاء غير مُنفَّذ** (الاستجابة تعيد `parent_notified: false` صريحًا)، **ولا يوجد عرض للتنزيلات إطلاقًا** (لا يوجد جدول تنزيلات في قاعدة البيانات).
- **Workflow**: كان يعرض تشغيلين وهميين وقرار مراجعة عبر `prompt()` نصي حر. الآن القرار عبر أزرار محددة مع استجابة حقيقية مفحوصة.
- **Remote Config**: كانت **أسوأ حالة سابقًا** — المسار لم يكن موجودًا بالخادم إطلاقًا وكانت الأعلام مزيّفة دائمًا. الآن جدول `remote_config` مُهاجَر فعليًا (migration 0015) على الإنتاج، مع مسار `PUT` حقيقي وتسجيل تدقيق.
- **المهارات (`SkillsPage`)**: كان الـ backend جاهزًا منذ فترة لكن الواجهة لم تكن تستدعيه (عنصر معطّل بلافتة "قريبًا"). تم ربطه الآن فعليًا.
- **مركز الدعم**: البحث بمعرّف الأسرة يعمل، لكن أزرار الإجراءات الحساسة (إعادة مزامنة الاشتراك، استعادة شراء، إعادة ضبط PIN، إنشاء تذكرة) **غير منفَّذة بالخادم** وتمت إزالتها صراحة من الواجهة بدل التظاهر بعملها.

---

## المميزات الحالية (خلاصة)

- تنقل وهيكلة شاملة تقريبًا لكل الأقسام المخطط لها (45 مسار).
- مصادقة وجلسة إدارية حقيقية.
- إدارة كاملة CRUD للمحتوى الأساسي (سلاسل، مواسم، حلقات، شخصيات، قصص، وسائط).
- فرق وأدوار وصلاحيات وسجل تدقيق مربوطة بـ API حقيقي.
- Remote Config حقيقي (بعد إصلاحه من الصفر).
- إلغاء الأجهزة (Revoke) يعمل بشكل حقيقي مع تسجيل تدقيق.
- محرر Story Builder قوي نسبيًا (بحسب `KIRO_LAST_REPORT.md`).

## النواقص

### نواقص موثقة رسميًا في `docs/DASHBOARDv2.md`

**أولوية P0 (قبل التشغيل التجاري)**:
- مركز بناء الصفحة الرئيسية الكامل (معاينة Mobile/Tablet/TV، اعتماد، جدولة، Rollback)
- إدارة الأجهزة والتنزيلات الكاملة (Revoke + إعادة مزامنة + إشعار ولي الأمر)
- مركز دعم فعلي بإجراءات حقيقية
- محرك Workflow فعلي (لا نموذج حالة ثابتة فقط)
- الحقوق والتراخيص
- Kill Switch إلى جانب Remote Config
- إدارة الباقات والأسعار
- مراقبة النظام والـ Queues
- Audit Log كامل
- نسخ احتياطي / استعادة
- فحص جودة المحتوى
- أمان الدخول و MFA

**أولوية P1 (بعد الإطلاق)**:
- الحملات والإشعارات
- تقارير الإيرادات والتحويل
- الترجمة والدبلجة المتقدمة
- تذاكر دعم فعلية (Support Tickets)
- Quiz Builder كامل
- تقارير ولي الأمر
- البحث والتوصيات
- Content Calendar
- عمليات جماعية (Bulk Operations)

**أولوية P2 (عند التوسع)**:
- تحرير متزامن (Presence + Soft Lock)
- A/B Testing
- تكامل Slack/Teams
- مساعدة بالذكاء الاصطناعي

### نواقص ملموسة من فحص الكود مباشرة

- محرر Story Builder: بلا رفع ZIP مع تقرير مطابقة، بلا Template ZIP، بلا Import من Excel، بلا استكمال رفع بعد الانقطاع، بلا مقارنة صوت قديم/جديد.
- التحقق من صحة محتوى الألعاب (`lib/catalogueValidation.ts`) سطحي جدًا (يتحقق فقط من عدم الفراغ) دون تطبيق القواعد الـ12 الموثقة في مخطط بيانات الألعاب.
- لا يوجد `game_localizations`، لا محرر مسارات رسم بصري (trace-path editor)، لا نظام تحقق مخصص لكل محرك لعبة.
- محرك ألعاب واحد فقط (`memory_flip`) من 12 محركًا مخطَّطًا مُنفَّذ في تطبيق Flutter، وهو hardcoded وليس pack-driven.

### أجزاء غير مؤكدة الاكتمال (تحتاج فحصًا أعمق)
الصفحات التالية موجودة كهياكل واجهة لكن عمق تنفيذها الفعلي بالخادم غير مؤكد بالكامل من هذا الفحص: `RevenuePage`, `RightsPage`, `CampaignsPage`, `OpsPage`, وتفاصيل `adminBilling.ts` و `adminPartnerships.ts` وآلية MFA الفعلية.

---

## الخلاصة

لوحة التحكم قوية ومكتملة نسبيًا في: **المحتوى الأساسي، القصص، الفرق والصلاحيات، والتنقل العام**. لكنها لا تزال ناقصة في: **تجربة التطبيق المتقدمة (بناء الصفحة الرئيسية بمعاينة كاملة)، خدمة العملاء الفعلية، الأسعار والإيرادات المتقدمة، الحقوق والتراخيص، محرك Workflow المؤسسي، النسخ الاحتياطي/الاستعادة، ومراقبة الأنظمة بعمق**. هناك نمط إيجابي واضح في المشروع: استبدال تدريجي وصادق للواجهات المزيّفة (mock) بربط حقيقي بالخادم، مع الإفصاح الصريح عن الميزات غير المنفذة بدل التظاهر بعملها.
