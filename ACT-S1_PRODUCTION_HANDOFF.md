# بيت الطائر — تسليم الإنتاج

`act-s1` · سلسلة **حكاية هادئة** · 8 صفحات · لغتان

> **حالة الرفع: لم يُرفع شيء.** كل ما في هذا الملف موجود **على القرص المحلي فقط**.
> لا صفوف في D1، ولا كائنات في R2. الرفع يحتاج قرارًا بشريًا — انظر [ما لم يُنفَّذ ولماذا](#ما-لم-يُنفَّذ-ولماذا).

---

## 1. القصة

| الحقل | القيمة |
|---|---|
| `slug` | `bird-home` |
| `title_ar` | بيت الطائر |
| `title_en` | The Bird's Home |
| `series_id` | `a-calm-tale` |
| `type` | 🔴 `illustrated` *(قصة مصورة — **ليست** `audio_story`)* |
| `page_count` | 8 |
| `duration_seconds` | **54** *(0:54)* |
| `age_min` / `age_max` | 3 / 5 |
| `reading_level` | `pre_reader` |
| `default_mode` | `read_to_me` |
| `is_free` | 1 |
| `bedtime_track` | ✅ |
| `status` | `draft` |
| المصدر التحريري | `docs/content/planets/05-qisas/a-calm-tale/story-01-bird-home.md` |

**الشخصيات:** زُغب — طائر صغير رمادي فاتح بعينين كبيرتين ودّيتين · وأمّه، طائر أكبر في العشّ.
بالإنجليزية الاسم **Fluff** — استبدال محليّ لا نقل حرفيّ، لأن «زُغب» تعني الريش الناعم و`Zughb` غير قابلة للنطق عند الطفل الإنجليزي.

**القوس:** طائر صغير يطير أبعد قليلًا، فتهبّ ريح خفيفة، فيعود إلى عشّه وينام. بلا ضياع ولا خوف.

### 🔴 تحذير التباس — اقرأه قبل الرفع

في قاعدة البيانات قصة اسمها **«الطائر يجد عشه»** `story-bedtime-05-bird-finds-nest`.
الاسم والموضوع قريبان جدًا، لكنها **قصة أخرى**: نوعها `audio_story` ومن سلسلة `bedtime-stories`.

**بيت الطائر قصة جديدة تُنشأ.** ربط أصول هذه القصة بتلك يُفسد الاثنتين: القصة الصوتية تحصل على 11 صورة ليست لها، والقصة المصورة تبقى فارغة.

---

## 2. الصور — 11 ملفًا

المجلد: `assets/images/stories/act-s1-playveo/`

| # | المعرف | الملف | النسبة | `brightness` | الحجم |
|---:|---|---|---|---:|---:|
| 1 | `asset-act-s1-page-001` | `page-001.jpg` | 16:9 | 1.00 | 791 KB |
| 2 | `asset-act-s1-page-002` | `page-002.jpg` | 16:9 | 0.97 | 793 KB |
| 3 | `asset-act-s1-page-003` | `page-003.jpg` | 16:9 | 0.94 | 727 KB |
| 4 | `asset-act-s1-page-004` | `page-004.jpg` | 16:9 | 0.90 | 650 KB |
| 5 | `asset-act-s1-page-005` | `page-005.jpg` | 16:9 | 0.86 | 773 KB |
| 6 | `asset-act-s1-page-006` | `page-006.jpg` | 16:9 | 0.82 | 952 KB |
| 7 | `asset-act-s1-page-007` | `page-007.jpg` | 16:9 | 0.76 | 699 KB |
| 8 | `asset-act-s1-page-008` | `page-008.jpg` | 16:9 | 0.70 | 725 KB |
| — | `asset-act-s1-cover` | `cover.jpg` | 1:1 | — | 790 KB |
| — | `asset-act-s1-hero` | `hero.jpg` | 16:9 | — | 632 KB |
| — | `asset-act-s1-thumb` | `thumb.jpg` | 3:4 | — | 653 KB |

**المجموع 7.99 MB** · JPEG بجودة ~98 · الصفحات 1376×768 · الغلاف 1024×1024 · المصغّرة 896×1200.

**التصنيف للرفع:** `kind='image'` · `visibility='public'` · بَكِت `majarra-thumbs` · مفاتيح تبدأ بـ `public/`.

---

## 3. الصوت العربي — 8 ملفات

المجلد: `assets/audio/stories/act-s1/ar/` · الصوت **Leda** · الموديل `gemini-3.1-flash-tts-preview`

| # | الملف | النص | `durationMs` | `autoTurnAfterMs` | `preRollMs` |
|---:|---|---|---:|---:|---:|
| 1 | `page-001-ar.wav` | هذا زُغب. بيته عشّ صغير. | 5480 | 6480 | — |
| 2 | `page-002-ar.wav` | اليوم يريد أن يطير قليلًا. | 4040 | 5040 | — |
| 3 | `page-003-ar.wav` | طار إلى الشجرة. ثم أبعد قليلًا. | 5960 | 6960 | — |
| 4 | `page-004-ar.wav` | هبّت ريح خفيفة. فتحرّكت الأوراق. | 7160 | 8160 | — |
| 5 | `page-005-ar.wav` | نظر زُغب إلى بيته. وعرف طريقه. | 6560 | 7560 | — |
| 6 | `page-006-ar.wav` | فطار إلى العشّ. وأمّه تنتظره. | 5720 | 6720 | — |
| 7 | `page-007-ar.wav` | جلس قريبًا منها. والريح هدأت. | 6600 | 7600 | — |
| 8 | `page-008-ar.wav` | والآن ينام زُغب في بيته. | 5680 | 🔴 **بلا** | **800** |

**المجموع 47.2 ث** · 24 kHz · 16-bit · mono · جهارة موحّدة **−20.0 dBFS** · صفر clipping.

---

## 4. الصوت الإنجليزي — 8 ملفات

المجلد: `assets/audio/stories/act-s1/en/` · الصوت **Leda** · نفس الموديل

| # | الملف | النص | `durationMs` | `autoTurnAfterMs` | `preRollMs` |
|---:|---|---|---:|---:|---:|
| 1 | `page-001-en.wav` | This is Fluff. His home is a small nest. | 6520 | 7520 | — |
| 2 | `page-002-en.wav` | Today he wants to fly a little. | 4080 | 5080 | — |
| 3 | `page-003-en.wav` | He flew to the tree. Then a little farther. | 6600 | 7600 | — |
| 4 | `page-004-en.wav` | A soft wind blew. The leaves moved. | 5280 | 6280 | — |
| 5 | `page-005-en.wav` | Fluff looked at his home. He knew the way. | 5560 | 6560 | — |
| 6 | `page-006-en.wav` | He flew to the nest. His mother was waiting. | 6080 | 7080 | — |
| 7 | `page-007-en.wav` | He sat close beside her. The wind grew still. | 6360 | 7360 | — |
| 8 | `page-008-en.wav` | And now Fluff sleeps in his home. | 6560 | 🔴 **بلا** | **800** |

**المجموع 47.0 ث** · جهارة موحّدة **−21.0 dBFS** · صفر clipping.

> **الهدف −21 لا −20 في الإنجليزية بقصد.** أداة التطبيع رفضت −20 لأن صفحة 5 كان `peak` عندها −1.7 dBFS فقط، فالهدف الأعلى كان سيُحدث clipping. الأداة تحسب أعلى هدف آمن للمجموعة وترفض ما يتجاوزه بدل أن تشوّه الصوت.

**التصنيف للرفع:** `kind='audio'` · `visibility='private'` **دائمًا** · بَكِت `majarra-media` · مفاتيح `private/`.
السرد لا يُخدَم من CDN عام أبدًا — هذا مفروض في `adminTts.ts` ولا يُترك للمتصل.

---

## 5. الربط المطلوب في D1

```
stories                     صف واحد جديد: slug=bird-home, type=illustrated
└── story_pages             8 صفوف: page_number 1..8
    │                       image_asset_id ← صورة الصفحة
    │                       duration_ms, transition='fade'
    │                       motion: 1-2,5-6 kenburns_slow · 3-4 pan_slow · 7-8 static
    └── story_page_localizations
        ├── (page_id, 'ar') body_text + narration_asset_id ← page-00N-ar
        └── (page_id, 'en') body_text + narration_asset_id ← page-00N-en

content_assets              27 صفًا: 11 صورة + 16 صوت
asset_links                 غلاف/hero/مصغّرة → entity_type='story', entity_id=<story id>
                            بأدوار role='cover' / 'hero' / 'thumb'
```

`timing_cues` تبقى `'[]'` في المرحلة الأولى — الحقل موجود جاهزًا للمرحلة الثانية *(توقيت الجمل)*.

---

## 6. ما لم يُنفَّذ ولماذا

### الرفع

**البنية جاهزة بالكامل:**

| المكوّن | الحالة |
|---|---|
| `majarra-api-prod` | ✅ منشور · `api.majarra.app/*` · نسخة `318a3526-ba50-4c7d-b0e8-9fc96389fa19` |
| D1 `majarra-db` | ✅ 78 جدولًا · 21 migration · آخرها `0021_dlq_family_events.sql` |
| R2 | ✅ `majarra-media` · `majarra-thumbs` · `majarra-creations` |
| الأسرار | ✅ `ADMIN_API_KEY` · `AUTH_TOKEN_SECRET` · `GOOGLE_TTS_API_KEY` · `MEDIA_TOKEN_SECRET` |

**العائق الوحيد: جلسة أدمن.**

`GET /api/v1/admin/tts/config` يرد `401` بلا توكن. والمفتاح المشترك **مرفوض تقنيًا**: `requireAdmin` يرفضه بمجرد وجود مستخدم أدمن واحد، وفي القاعدة **مستخدمان**. والتوكن غير قابل للاستخراج من D1 لأن `admin_sessions` تحفظ `token_hash` فقط لا التوكن.

**ولماذا لم أكتب في D1 و R2 مباشرة** رغم أن صلاحية `d1:write` وR2 متاحتان لي:

`lib/assetBuckets.ts` يوثّق عيبًا **حدث فعلًا في هذا المشروع**: 18 أصلًا كانت `visibility='public'` بمفاتيح `public/` لكنها في بَكِت `media`. النتيجة أن الـ CDN يرجع 404 لها، والحل الوحيد لتشغيلها — توجيه الـ CDN إلى `MEDIA_BUCKET` — **يعرّض كل فيديو خاص للتحميل المجهول**. ولهذا صار `bucket` دالة محسوبة من `visibility` لا حقلًا يبعثه المتصل.

الكتابة المباشرة تعيدني إلى الحالة التي كُتب هذا الكود لمنعها: أصير أنا المتصل الذي يحدّد البَكِت بيده. وخطأ من هذا النوع **صامت** — يظهر متأخرًا كصورة مكسورة، أو أسوأ، كمحتوى خاص مكشوف عند توجيه الـ CDN.

### طرق الرفع الثلاث

| # | الطريقة | من يحسب البَكِت والمفتاح | الخطورة |
|---:|---|---|---|
| 1 | **من الداشبورد يدويًا** | التطبيق | الأدنى — موصى بها |
| 2 | عبر الـ API بتوكن جلسة | التطبيق | منخفضة · التوكن يُكشف في المحادثة فيلزم `revoke` بعدها |
| 3 | كتابة مباشرة في D1 و R2 | **أنا** | الأعلى — خطأ صامت محتمل |

للطريقة 2، الأنضف إنشاء مستخدم مخصّص للإنتاج الآلي بصلاحيات `upload_images` و`upload_audio` و`create` فقط، فلو تسرّب التوكن يكون أقصى ضرره رفع أصول لا لمس مستخدمين أو فواتير.

### الصيغ

**الصور: لا تحويل مطلوب.** مواصفة الكاتالوج تطلب «ماستر PNG + WebP» وهذا **غير قابل للتحقيق** من مخرجات PlayVeo: الأصل JPEG، فتحويله إلى PNG يلفّ بيانات مفقودة الجودة في علبة بلا فقدان — حجم أكبر بصفر تحسين، أي ماستر وهمي.

والأصل بجودة **~98**، أي أعلى من النطاق 85–95 الذي توصي به Cloudflare للأصل. فالصحيح **حفظ الـ JPEG كماستر** وترك التحويل إلى WebP/AVIF للحافة حسب `Accept` header — أصل واحد يخدم كل الصيغ، وAVIF يأتي مجانًا. والتحويل المسبق محليًا **ثم** عند الحافة يعني ثلاثة أجيال ضغط فوق بعضها، وهو ضرر لا تحسين.

🔴 **مطلوب تحديث `IMAGE_PROMPTS_CATALOG.md`:** قاعدة «ماستر PNG» كُتبت على افتراض خط إنتاج بلا فقدان *(رسم يدوي أو تصدير)*، والمولِّد يخرج JPEG. القاعدة مبنية على افتراض غير قائم.

**الصوت: `.m4a` مؤجّل.** المواصفة تطلبه، والمخرجات WAV. لا تُثبَّت `ffmpeg`: `googleTts.ts` فيه نقل `cloud_tts` يعيد **MP3 جاهزًا من جوجل** بلا تحويل محلي. ضبط `GOOGLE_TTS_SERVICE_ACCOUNT_EMAIL` و`_PRIVATE_KEY` و`_PROJECT_ID` يحلّ المشكلة من جذرها لكل قصة قادمة. و`googleTts.ts` ينصّ على أن **Workers لا تُحوّل صيغًا**، فالتحويل يجب أن يسبق الرفع.

### الدقة

الصفحات 1376×768 والكاتالوج يطلب 1920×1080 لنسبة 16:9. النسبة صحيحة (1.79) والدقة أقل. **غير قابل للإصلاح بالتحويل** — التكبير يخترع بيكسلات. ولم أجد في PlayVeo API معاملًا للدقة.

### المراجعات

🔴 **مراجعة لغوية للإنجليزية غير موقّعة.** النص معتمد للتوليد الصوتي لا للنشر.
🔴 **مراجعة نوم** و**تربوية** و**صوتية** غير موقّعة — مطلوبة في معايير قبول القصة.

### ما لا يمكن التحقق منه آليًا

**النبرة والتيمبر ونطق العربية.** لم يسمع أحد الملفات بعد. الفحص الآلي يغطي الجهارة والصمت وclipping والمدد فقط.

مقارنة الأصوات الستة في `assets/audio/_audition/` للحكم على اختيار **Leda**، ومعها `_audition.json`.

---

## 7. الأدوات

| الملف | الوظيفة |
|---|---|
| `tools/playveo/generate.mjs` | مولّد الصور · `--validate` `--only <id>` `--rest` `--reftest` |
| `tools/playveo/act-s1.manifest.json` | مشاهد الصور + قفل الشخصية + الإضاءة لكل صفحة |
| `tools/playveo/probe-i2i.mjs` · `probe-cache.mjs` | إثبات عطل image-to-image |
| `tools/tts/narrate.mjs` | مولّد السرد · `--manifest` `--page` `--all` `--voice` |
| `tools/tts/act-s1.narration.locked.json` | برومت + نصوص العربية |
| `tools/tts/act-s1.narration.en.json` | برومت + نصوص الإنجليزية |
| `tools/tts/audition.mjs` | مقارنة الأصوات |
| `tools/tts/inspect-wav.mjs` | فحص RMS · peak · clipping · صمت · تفاوت الجهارة |
| `tools/tts/normalize-wav.mjs` | توحيد الجهارة · يرفض أي هدف يُحدث clipping |
| `tools/tts/remeasure-durations.mjs` | إعادة بناء `_durations.json` من الـ WAVs |
| `tools/content/audit-narration-pacing.mjs` | فحص تناقضات الإيقاع · `--check` في CI |
| `tools/content/calibrate-narration-rate.mjs` | معايرة معدل السرد من الصوت الفعلي |
| `tools/content/fix-story-durations.mjs` | تصحيح `duration_seconds` من المدد |
| `tools/content/fix-narration-rate.mjs` | تحويل أرقام wpm إلى حرف/ثانية |
| `tools/content/write-measured-durations.mjs` | كتابة المدد المقيسة في المواصفة |
| `tools/build-story-preview.mjs` | بناء `act-s1-preview.html` |

**المعاينة:** افتح `act-s1-preview.html` — كل صفحة بصورتها ونصّها العربي وصوته، وتحته الإنجليزي وصوته **على الصورة نفسها**.

---

## 8. دروس تقنية تخصّ كل قصة قادمة

**PlayVeo image-to-image معطوب.** ستة نداءات حية بمصادر وبرومتات مختلفة أعادت **البايتات نفسها** (`sha256 963168c7cbe5cb0b`). حتى برومت «سيارة رياضية حمراء في صحراء» أعاد صورة العشّ. و`strength: 1.0` قُبل وأُعيد في الرد بلا أثر. و`padding` بعد علامة JPEG EOI لم يفوّت الـ cache، أي أن المفتاح مبني على الصورة المفكوكة لا بايتات الملف. **ويحاسب 0.15 credit لكل نداء.**

فتثبيت الشخصية **بالنص** لا بصورة مرجعية: حقل `character_lock` مستخرج من `page-001` المعتمدة ويُصدَّر في كل برومت. نجح — صفحة 3 خرجت طائرًا طائرًا والعشّ صغير في الخلف، بتكوين مختلف تمامًا وشخصية ثابتة.

**الـ wpm لا ينتقل بين اللغات.** كل ملفات القصص الـ15 كانت تعلن سرعة بالكلمة/الدقيقة، وكلها تخطّط مددًا تعني **~62%** منها. انتظام الخطأ في ملفات كُتبت في أوقات مختلفة يشير إلى الوحدة لا إلى المؤلفين.

والدليل القاطع من هذا الإنتاج: **نفس القصة، نفس الصوت، ونفس المدة تقريبًا** (47.2 ث عربي · 47.0 ث إنجليزي) — و**53 كلمة/دقيقة مقابل 83**. فرق 57%. الكلمة العربية 3.95 حرف. المعدل المعاير **3.51 حرف عربي/ثانية**.
انظر `docs/content/narration-rate-calibration.json` و`-en.json`.

**قاعدة `autoTurn` هي المرجع لا `duration_seconds`.** `00-story-page-model.md` يعرّف نفسه «العقد بين المحتوى والتطبيق ولوحة التحكم» ويحدّد فترة الانتظار **أربع مرات متسقة** (900–1000ms في مسار النوم)، ولا يعرف مفهوم «زمن تفرّج» أصلًا، ولا يذكر `duration_seconds` بين حقول الصفحة. فهو حقل كتالوج لا حقل تشغيل — وصُحّح في 10 ملفات *(160 ث → 54 ث في هذه القصة)*.

**الوسوم الصوتية تنقل الإيقاع، والوصف النصّي لا ينقله.** «الصفحة 8 أبطأ صفحة» في توجيه الأداء أعطى 3.66 حرف/ثانية مقابل هدف 2.92 — أي من **أسرع** الصفحات. الحل `[very slow, drowsy, one gentle word at a time]` + الوقفة 800ms التي تنصّ عليها المواصفة. والوقفة تبقى **بيانات تشغيل** لا صمتًا محروقًا في الـ WAV، وإلا صار زمن الملف كاذبًا على كل ما يقيسه.
والإنجليزية خرجت صحيحة **من أول مرة** لأن الدرس انتقل.

**الموديل يعيد ردودًا فارغة عشوائيًا** (`finishReason=OTHER`). حدث في العربية صفحة 4 وفي الإنجليزية صفحة 6. المعالجة الموثّقة هي إعادة المحاولة، وهي مضافة في `synthesizeWithRetry`.

**الإضاءة لكل صفحة لا مشتركة.** `style_tail` المنقول من AutoFlow فيه «deep navy sky» من لوحة العلامة، فأخرج `page-001` ليلًا بنجوم رغم `brightness = 1.00`. ولأنها الصورة المرجعية كان الخطأ سينتشر في 11 صورة. والوصف الإيجابي وحده لم يكفِ — النجوم اختفت فقط عند كتابة **«no stars, no moon»** صريحة.

**بوابة تتخطّى ملفًا بصمت تعطي نجاحًا كاذبًا.** تكرّر هذا ثلاث مرات في هذا العمل: فحص الإيقاع مرّر ملفًا صار غير مقروء، ومحوّل الـ wpm تخطّى ملفات محوّلة جزئيًا، وكاتب المدد عدّل 7 صفوف من 16. الثلاثة ظهرت لأن كل أداة تطبع **العدد المتوقع** وتقارنه. أُزيلت البوابات المسبقة وصار القرار على مقارنة المحتوى الفعلي.

**`narrate.mjs` كان يفقد بيانات.** كتابة `pages: report` تعني أن توليد صفحة واحدة يمسح مدد الباقي. حدث فعلًا: الملف صار يحمل صفحة واحدة بدل ثمانية. أُصلح بالدمج، وأُضيف `remeasure-durations.mjs` لأن الصوت هو المصدر الذي لا ينحرف.

---

## 9. متبقٍّ

- [ ] 🔴 **`rotate` لمفتاح PlayVeo** — كُتب صريحًا في محادثة. أعلى بند أمني.
- [ ] رفع الأصول وإنشاء صفوف القصة والصفحات.
- [ ] معايرة `bedtime-stories` و`qisas-min-alhayat` — صفحة صوت واحدة لكل سلسلة تكفي. **لا تُكتب لهما أرقام قبل القياس**، وإلا تكرّرت الغلطة الأصلية: رقم غير مقيس يُقال كأنه حقيقة. لهذا بقيت **25** تناقضًا مسجّلة في `narration-pacing-baseline.json` بدل تصحيحها بالتخمين.
- [ ] تحديث قاعدة «ماستر PNG» في `IMAGE_PROMPTS_CATALOG.md`.
- [ ] ضبط `GOOGLE_TTS_SERVICE_ACCOUNT_*` للحصول على MP3 من المصدر.
- [ ] السماع البشري وتوقيع المراجعات الأربع.
- [ ] `INCLUDE_TEST_FIXTURES` غائب عن `env.production.vars` — **الغياب مقصود وصحيح** لأن `shouldServeTestFixtures()` يرفضه في الإنتاج. أضِف تعليقًا يوضح ذلك وإلا أضافه أحدهم مستقبلًا ظنًّا أنه سهو.
- [ ] الأصول المشتركة غير المنتَجة: `asset-sfx-soft-wind` *(≤300ms)* · `asset-sfx-page-turn` *(≤200ms)* · `asset-music-act-lullaby` *(−24 dB)* · `asset-act-frame-end`.

---

## 10. التكلفة

**PlayVeo ~2.65 credit:** 13 نداء text-to-image *(0.1 لكل واحد)* · 7 نداءات image-to-image *(0.15)* منها **6 مهدرة على الـ endpoint المعطوب** · 1 فيديو *(0.4)* في الاختبار الأول.

**Google TTS:** 22 نداءً — 6 مقارنة أصوات · 8 عربي *(+1 إعادة محاولة)* · 2 إعادة توليد للصفحتين 1 و8 · 8 إنجليزي *(+1 إعادة محاولة)*.
