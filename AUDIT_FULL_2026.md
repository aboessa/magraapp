# أودت مجرة الشامل — كل المشاكل والنواقص

> **تاريخ الأودت:** 2026-08-26 · **النوع:** قراءة كاملة للكود + تشغيل فحوص فعلي + استعلام قاعدة البيانات المحلية
> **النطاق:** تطبيق Flutter، Worker API، Durable Objects، D1، R2، Queues، Cron، لوحة التحكم، المحتوى، الوثائق، الأدوات، CI
> **لم يُعدَّل أي كود إنتاج أو مخطط أو ترحيل أو حالة محتوى أو حالة مراجعة أثناء هذا الأودت.**

---

## §0 كيف تستخدم هذا الملف

- هذا الملف هو **المخرج القابل للتنفيذ**. كل بند فيه تاسك مستقل بمعرّف ثابت.
- **المعرّفات لا تُعاد ترقيمها.** عند إضافة بنود جديدة تُضاف في نهاية تسلسل منطقتها.
- **سبب جذري واحد = تاسك واحد**، مع ذكر كل المناطق المتأثرة. لا يُقسَّم عيب إعداد واحد إلى تاسك لكل شاشة.
- كل بند يحمل دليلًا بصيغة `path:line`. إن لم تستطع إعادة إنتاج الدليل، أعد التدقيق قبل التنفيذ.
- **الحالة** من هذه القائمة: `MISSING` / `PARTIAL` / `COMPLETE` / `HARDCODED` / `PLACEHOLDER` / `DEAD-UNUSED` / `DUPLICATED` / `DISCONNECTED` / `TEST-FIXTURE-ONLY` / `SPEC-DOC-ONLY` / `REGRESSION` / `OWNER-DECISION` / `EXTERNAL-DEP` / `HUMAN-REVIEW`.
- **الأولوية:** `P0` مانع إطلاق (أمان، سلامة بيانات، أو مسار أساسي مكسور من طرف إلى طرف) · `P1` أساسي قبل إطلاق جدّي · `P2` جودة وقابلية تشغيل · `P3` مؤجَّل.
- **الحجم** تقدير هندسي نسبي `XS/S/M/L/XL`، وليس وعدًا زمنيًا.
- سطر **«لا تفعل»** يوثّق مسارًا خاطئًا معروفًا. تجاهله يعيد إنتاج العيب.

### علاقته بالملفات القديمة

| الملف | الحالة |
|---|---|
| `PROJECT_AUDIT_TASKS.md` (2026-08-15، 99 تاسك) | **لم يُمسّ.** §13 هنا يصنّف كل معرّف فيه: مُنجَز / قائم / مُبطَل |
| `KIRO_LAST_REPORT.md` | مرجع سردي تاريخي؛ عدة ادعاءات فيه سقطت بالفحص، انظر §1.4 |
| `النواقص_المتبقية.md` | مرجع محتوى مفيد، لكن أقسامه التقنية متقادمة بشدة (يقول إن التطبيق لا يُبنى وبلا اختبارات — كلاهما خطأ اليوم) |
| `docs/FEATURE_MATRIX.md` | أرقامه خاطئة (67 مسارًا مقابل الواقع) — انظر §9 |

---

## §1 خط الأساس المُتحقَّق منه

كل رقم في هذا القسم نتيجة أمر شُغِّل فعلًا في 2026-08-26، لا نقلًا من تقرير.

### 1.1 المستودع

| البند | القيمة |
|---|---|
| الفرع / HEAD | `master` @ `3ee03a38` — `feat: add ACT-S2 story assets and narration import` (2026-08-19) |
| شجرة العمل | **566 مسارًا معدَّلًا/غير متتبَّع** — الشجرة غير نظيفة، والأودت يعكس حالة شجرة العمل لا HEAD |
| Flutter `lib/` | 184 ملف Dart |
| Flutter `test/` | 63 ملف اختبار |
| Worker `src/` | 146 ملف TS، منها 69 موجّهًا في `routes/` |
| Worker `test/` | 69 ملف `*.test.mjs` |
| ترحيلات D1 | 80 ملف `.sql` في `migrations/` (+ ملف في `migrations/catalog/` خارج `migrations_dir`) |
| صفحات اللوحة | 118 ملف صفحة، منها **29 صفحة تحمل `@ts-nocheck`** |
| اختبارات اللوحة | 19 ملفًا |
| ملفات `.md` في الجذر | 35 |

### 1.2 نتائج الفحوص — أربعة إخفاقات حقيقية

> **هذا القسم لقطة وقت الأودت ولا يُحدَّث.** الحالة بعد الدفعة الأولى من الإصلاح في **§15**: كل المجموعات خضراء و`flutter analyze` بصفر مشكلة.

| الفحص | الأمر | النتيجة |
|---|---|---|
| Flutter تحليل | `flutter analyze` | **43 مشكلة** (تحذيرات + infos)، صفر أخطاء — يُبنى |
| Flutter اختبارات | `flutter test` | **13 اختبارًا فاشلًا**، ثم **تعليق دائم** عند `test/tmp_render_png_test.dart` — الجولة لا تنتهي أصلًا |
| Worker أنواع | `npm run typecheck:types` | نظيف |
| Worker اختبارات | `npm test` | 1164 اختبارًا · 1162 ناجح · **2 فاشل** |
| اللوحة أنواع | `tsc --noEmit -p tsconfig.app.json` | نظيف (مع 29 صفحة تتخطّى الفحص بـ`@ts-nocheck`) |
| اللوحة اختبارات | `vitest run` | 303 اختبارًا · 301 ناجح · **2 فاشل** |

هذا يُبطل مباشرةً ما يقوله `PROJECT_AUDIT_TASKS.md` من أن كل المجموعات خضراء (304 + 960 + 284). المجموعات ليست خضراء اليوم، وجولة Flutter لا تُكمل حتى.

**الإخفاقات الأربعة، بأسمائها:**

1. `dashboard/api/test/routeGuards.test.mjs:151` — «every mutating admin handler carries an authorization guard» → `['adminBilling.ts POST /billing/refunds', 'adminBilling.ts POST /billing/grant']`. مسارَا **استرداد الأموال ومنح الاستحقاق** بلا فحص صلاحية. → `SEC-101`
2. `dashboard/api/test/rateLimit.test.mjs:212` — «the login limit refuses the sixth attempt» → 4 من 6 محاولات مرفوضة بدل 1، مع ترويسة تعلن الحد 5. → `SEC-104`
3. `dashboard/front/src/test/paletteCalendar.test.tsx:71` — مراجع (`review`) يرى أمر «سلسلة جديدة» في لوحة الأوامر. → `ADM-103`
4. `dashboard/front/src/test/dashboardPresets.test.tsx:112` — `content_manager` يحصل على تخطيط `support` بدل `content`. → `ADM-104`

**الـ13 إخفاقًا في Flutter** (سبعة أسباب جذرية):

| # | الاختبار | ما يقوله الإخفاق |
|---|---|---|
| 1 | `app_environment_test.dart` | قائمة السماح لم تعد تقبل مضيف staging المُعلَن |
| 2-3 | `catalog_cache_test.dart:82` | مدخل كاش **منتهي الصلاحية** ومدخل بطابع زمني **مستقبلي** كلاهما يُقبل بدل أن يُرفض |
| 4-6 | `deep_link_smoke_test.dart` | عدد عناصر الكاتالوج المبندل لا يطابق المتوقع (30) في ثلاثة سيناريوهات منها «المحتوى غير المنشور لا يتسرّب» |
| 7 | `creative_studio_e2e_test.dart` | ترتيب خطوات المراجع لا يطابق العدد المتوقع (29) |
| 8-10 | `engine_content_separation_test.dart` | ملفات المحركات تحمل محتوى مؤلَّفًا، وإيموجي يقوم مقام الرسوم، وقائمة السماح فيها مدخلات قديمة |
| 11 | `logout_tile_test.dart` | **تسجيل الخروج لا يمسح كاش صفحات القارئ**: `majarra_reader_pages_v1.story.story-bird-home.ar` يبقى بعد الخروج |
| 12 | `studio_home_layout_test.dart` | أخطاء تخطيط في رئيسية الاستوديو |
| 13 | `tutorial_gate_test.dart` | إكمال الجولة التعليمية لا يُحفَظ بعد الخطوة الأخيرة |

### 1.3 قاعدة البيانات — D1 المحلية

المصدر: `dashboard/api/.wrangler/state/v3/d1/miniflare-D1DatabaseObject/18cae7f5….sqlite`، آخر كتابة **2026-08-23**. **الإنتاج غير مُتحقَّق منه** — لا توكن نشر في المستودع، فكل رقم أدناه محلي.

**150 جدولًا · 81 صف ترحيل مُطبَّق.**

| أصول الوسائط `content_assets` | العدد |
|---|---:|
| `audio` / ready / private | 30 |
| `image` / ready / public | 161 |
| `image` / ready / private | 8 |
| **`video`** | **0** |
| **الإجمالي** | **199** |

| النوع | published | ready | draft | archived |
|---|---:|---:|---:|---:|
| السلاسل | 24 | — | 14 | 2 |
| الحلقات | 20 | — | 61 | — |
| القصص | **0** | 12 | 3 | 8 |
| الكتب | **0** | — | 22 | — |
| الألعاب | 39 | 3 | 49 | 14 |

| مقياس حرِج | القيمة |
|---|---:|
| **حلقات قابلة للتشغيل فعلًا** (بنفس predicate الكود في `episodes.ts:64-80`) | **0** |
| حلقات منشورة بمدة زمنية فارغة | 20 من 20 |
| `episode_renditions` / `episode_audio_tracks` / `episode_subtitle_tracks` | 0 / 0 / 0 |
| صفحات قصص | 194 · **178 بلا صورة** |
| ترجمات صفحات القصص | 194 صفًا · **16 فقط فيها سرد صوتي** |
| **`admin_users`** | **0** — لا حساب مسؤول واحد، فاللوحة لا يمكن الدخول إليها إلا بالمفتاح المشترك |
| `parents` / `children_profiles` / `child_settings` | 0 / 0 / 0 |
| `child_screen_time_daily` / `watch_progress` / `mastery` / `attempts` | 0 / 0 / 0 / 0 |
| `store_products` / `plan_pricing` / `subscription_entitlements` / `google_play_purchases` | 3 / **0** / 0 / 0 |
| `learning_objectives` / `questions` / `translation_units` | 57 / 0 / 0 |
| `game_engines` | 17 |
| `home_experience_blocks` / `home_recommendations` | 15 / 3 |
| `analytics_events` / `notifications` / `feature_flags` | 0 / 0 / 0 |
| `audit_logs` | 32 |
| `content_availability` / `content_rights` | 1 / 0 |

**الجداول الأربعة التي تفترضها خطة التشفير غير موجودة إطلاقًا:** `media_licenses`، `child_downloads`، `download_sessions`، `download_events`. الموجود منها: `content_assets` و`subscription_plan_limits` و`account_devices` (والأخير مُعلَن ميتًا في `migrations/0010_cleanup_dead_d1_tables.sql:1-5` وصفوفه 0).

كذلك: **لا جدول `book_pages`**. صفحات الكتب مخزَّنة كعمود JSON نصي `books.pages`، وهو ما لا يمكن ربط أصل به ولا استعلامه لجهوزية النشر.

### 1.4 ادعاءات سقطت بالفحص

| الادّعاء | المصدر | الواقع المُتحقَّق منه |
|---|---|---|
| «كل المجموعات خضراء: 304 + 960 + 284» | `PROJECT_AUDIT_TASKS.md` | 4 إخفاقات + جولة Flutter معلَّقة |
| «التطبيق لا يُبنى — `isTelevision` خطأ ترجمة» | `النواقص_المتبقية.md:3.1` | `flutter analyze` صفر أخطاء |
| «لا اختبارات `test/` ولا CI» | `النواقص_المتبقية.md` T29/T30 | 63 ملف اختبار + `ci.yml` بسبع وظائف |
| «`google_fonts` يجلب الخط وقت التشغيل» | `النواقص_المتبقية.md` T13 | الخط مُبندل محليًا في `app_main/pubspec.yaml:53-73` |
| «PIN مكسور: أي 4 أرقام تفتح» | `النواقص_المتبقية.md` T14 | PBKDF2-HMAC-SHA256 مع verifier وsalt في `parent_pin_store.dart` |
| «لا refresh للتوكن» | `النواقص_المتبقية.md` T15 | تدوير مُسلسَل مع تجميع الطلبات في `majarra_api_client.dart:1379-1467` |
| «`installation_id` مزيف `dev-install-$email`» | `النواقص_المتبقية.md` T16 | UUIDv4 عشوائي في `installation_identity.dart:16-40` |
| «3 نقاط نهاية غير مصادَقة» | `KIRO_LAST_REPORT.md` | الثلاث مُصلَحة — انظر §5.5. لكن **ثغرتان جديدتان** ظهرتا في الفوترة الإدارية |
| «0 صوت في قاعدة البيانات» | `KIRO_LAST_REPORT.md` | 30 أصل صوتي اليوم؛ **0 فيديو** ما زال قائمًا |
| «`dwellMs` قرار معلَّق» | `مشكلة_مدد_قصص_كوكب_5` | مُنفَّذ: عمود + ترحيلات + وقت تشغيل + اختبارات |

### 1.5 ما لم أستطع التحقق منه

- **الإنتاج:** D1 وR2 وKV والـDurable Objects على `api.majarra.app` — لا وصول. كل أرقام §1.3 محلية.
- **الأجهزة الحقيقية:** لا تحقق على هاتف أو تابلت أو TV؛ كل ملاحظات a11y والأداء مبنية على الكود.
- **`flutter test` كاملة:** الجولة تتعلّق ولا تنتهي (`QA-101`)، فلا يوجد عدد نهائي للناجح ولا نسبة تغطية.
- **متاجر التطبيقات:** لا حالة مراجعة ولا بناء موقَّع تم فحصه.

---

## §2 الخلاصة التنفيذية

### الحكم في ثلاث جمل

الهندسة أفضل بكثير من كل ما يقوله التوثيق عنها، والمحتوى والعمليات أسوأ بكثير مما تفترضه الخطط. **تشفير المحتوى تحديدًا ليس منفَّذًا**: المنفَّذ تشفير ملفات محلي سليم خوارزميًّا، لكن منظومة الترخيص الخادمية التي تمنح هذا التشفير معناه — جلسة تنزيل، ترخيص موقَّع، مفتاح مغلَّف للجهاز، إبطال — غائبة بالكامل، وما يُشفَّر اليوم هو محتوى عام يمكن تنزيله من CDN مباشرة. وأكثر بند مستعجل في هذا الملف ليس أمنيًّا: ملف اختبار مؤقت واحد يمنع `flutter test` من الانتهاء، فيُبطل كل بوابة تحقق في المشروع.

### أعداد البنود

| الأولوية | العدد | المعنى |
|---|---:|---|
| **P0 — موانع إطلاق** | **21** | أمان، سلامة بيانات، أو مسار أساسي مكسور من طرف إلى طرف |
| **P1 — أساسي قبل إطلاق جدّي** | **42** | |
| **P2 — جودة وقابلية تشغيل** | **22** | |
| **P3 — مؤجَّل** | **1** | |
| **مجموع البنود الهندسية** | **86** | |
| قرارات مالك `DECIDE-*` | 7 | لا تُحلّ بالكود |
| تبعيات خارجية `EXT-*` | 7 | تعاقد أو حساب أو موارد |
| مراجعات بشرية `HUMAN-*` | 8 | إنتاج فني، مراجعة شرعية/تاريخية/تربوية، خصوصية، وصول |
| **الإجمالي** | **108** | |

### الخمسة التي تُصلَح أولًا، بهذا الترتيب

1. **`QA-101`** — احذف `test/tmp_render_png_test.dart`. حجمه XS، وبدونه لا تنتهي جولة اختبار Flutter أبدًا، فلا CI ولا بوابة إصدار ولا معنى لأي «أخضر». (والملف يكتب `_studio_home.png` في جذر المستودع.)
2. **`SEC-101`** — احرس `POST /admin/billing/refunds` و`/billing/grant`. أي حساب لوحة بأي دور يمنح اليوم باقة مدفوعة سنة كاملة أو يُنشئ استردادًا. الاختبار المكتوب لهذا **موجود ويفشل**.
3. **`OPS-103`** — أضف `.backups/` إلى `.gitignore`. نسخة من قاعدة الإنتاج (1.38 ميغابايت) في شجرة العمل، غير مُتجاهَلة، مع 292 مسارًا غير متتبَّع — أي أن `git add -A` واحدًا يرفع بيانات إنتاج.
4. **`QA-102` + `ENC-012`** — الإخفاقات الـ13 في Flutter، وأخطرها أن كاش صفحات القارئ **يبقى بعد تسجيل الخروج** وأن كاش الكاتالوج يقبل مدخلات منتهية ومستقبلية.
5. **`ENC-001`** — ابدأ منظومة الترخيص الخادمية (الجداول الأربعة + نقاط جلسة التنزيل + التوقيع). كل بنود التشفير الأخرى معلَّقة عليه، ولا يمكن إغلاق 12 معيار قبول بدونه.

### جهوزية الإطلاق

| البُعد | الحالة | الدليل المُوجَز |
|---|---|---|
| البناء والتحليل | 🟡 | `flutter analyze` صفر أخطاء / 43 مشكلة · `tsc` نظيف في الطرفين (مع 29 صفحة `@ts-nocheck`) |
| مجموعات الاختبار | 🔴 | 13 إخفاقًا في Flutter + **جولة لا تنتهي** · 2 في الـWorker · 2 في اللوحة |
| CI وبوابة الإصدار | 🔴 | مُصلَح تصميمًا، أحمر واقعًا؛ `deploy-gate` لن يعمل، والنشر يدوي |
| أمان الـAPI | 🟡 | ثغرات P0 الثلاث السابقة مُصلَحة؛ وظهرت **ثغرتان جديدتان** في الفوترة الإدارية |
| **تشفير المحتوى والترخيص** | 🟡 | **11 من 25** معيار قبول مستوفى و9 جزئيًّا (كان 2 و11 — انظر §4 و§15). المنظومة تعمل من طرف إلى طرف: الخادم يُصدر ترخيصًا Ed25519 موقَّعًا بحدود ذرّية، والعميل يتحقّق منه بوقت موثوق ويتحقّق من بصمة ما نزّله، والتشفير مقسَّم بـAAD ومفتاح لكل حزمة، ولا بايت صريح على القرص. ويبقى **`ENC-002`** (مفتاح المحتوى مغلَّفًا للجهاز لا مشتقًّا من مفتاح التطبيق) و`ENC-013` و`ENC-015` |
| DRM للفيديو | ✅ **غير مطلوب بقرار مالك** | كل المحتوى من إنتاج «مجرة» والملكية مسجَّلة (`content_rights` = 248 صفًّا). تنازل موثَّق بشرطه في `§15` (الدفعة 10)، ويعود مطلوبًا فور نشر محتوى مرخَّص |
| التنزيل Offline | 🟡 | المحرّك حقيقي ويعمل، والتشفير صار مقسَّمًا بـAAD ويجري **أثناء التدفق**، والتشغيل من مصدر محلي بلا ملف صريح (الدفعات 7 و8 و11). ويبقى: للمحتوى **العام** فقط، وبترخيص يحسبه العميل نفسه (`ENC-001`, `ENC-008`) |
| محتوى يصل إلى طفل | 🔴 | **0 حلقة قابلة للتشغيل** · 0 قصة منشورة · 0 كتاب · ثلاثة كواكب فارغة |
| الوسائط | 🔴 | 0 أصل فيديو · 30 صوتيًّا (14 يتيم) · 157 من 199 أصلًا بحجم صفري |
| التجارة | 🔴 | لا مسار شراء عامل · لا أسعار · لا استحقاق مدفوع |
| الرقابة الأبوية | 🔴 | تُضبط وتُحفَظ ولا تُقرأ في أي مسار تشغيل |
| الأجهزة وTV | 🟡 | العرض والسحب يعملان؛ لا إقران ولا اعتماد TV |
| الخصوصية | 🟡 | تنقيح واحتفاظ وتصدير/حذف حقيقية؛ لا تدقيق لأي عملية على مسار العميل |
| التوطين | 🔴 | 2526 نصًّا حرفيًّا مقابل 255 مفتاحًا؛ المنتج عمليًّا أحادي اللغة |
| إمكانية الوصول | 🟡 | 199 إشارة semantics · **إشارة واحدة** لتكبير النص · صفر تحقق على جهاز |
| الرصد | 🔴 | 12 خدمة معرَّفة · صفر فحص صحة · صفر تنبيه · **24 حدث عائلة فاشل** بلا إشعار |
| البيئات | 🔴 | لا staging؛ dev وstaging يوجَّهان إلى الإنتاج في الطرفين |
| اللوحة | 🟡 | RBAC خادمي قوي (196 حرسًا)؛ حجب الواجهة في 17 ملفًا من 240 |

**الحكم: غير قابل للإطلاق.** والقيد الحاكم ليس جودة الكود — بل ثلاثة أشياء بهذا الترتيب: **إنتاج الوسائط**، ثم **منظومة الترخيص والتجارة**، ثم **إعادة CI إلى الأخضر** حتى يصبح أي تقدّم قابلًا للإثبات.

### ملاحظة على قراءة هذا الملف

هذا الأودت يخالف التقارير السابقة في عشر نقاط موثَّقة في `§1.4`، منها ادعاءات كانت **متشائمة** (التطبيق «لا يُبنى»، «لا اختبارات»، PIN مكسور) وأخرى كانت **متفائلة** («كل المجموعات خضراء»). لا تُبنَ خطة على أي رقم في ملفات الجذر القديمة بلا إعادة توليده من استعلام أو أمر.

---

## §3 التشفير والترخيص والحماية عند التخزين — `ENC-*`

### 3.0 الحكم الموجز

المنفَّذ اليوم هو **تشفير محلي للملفات**، لا **منظومة ترخيص محتوى**. الفرق ليس لفظيًا:

- التشفير الموجود حقيقي وسليم كخوارزمية: AES-256-GCM عبر حزمة `cryptography` المدعومة بـBoringSSL، nonce عشوائي 96-bit لكل ملف، وتحقق سلامة بـtag 128-bit (`app_main/lib/core/crypto/file_crypto.dart:36-101`). لا تشفير مكتوب باليد.
- لكن **كل الطبقة الخادمية التي تجعل هذا التشفير ذا معنى غائبة**: لا جلسة تنزيل، ولا ترخيص موقَّع، ولا CEK لكل حزمة، ولا مفتاح مغلَّف مربوط بالجهاز، ولا إبطال، ولا تحقق سلامة من المصدر. أقسام **7، 8 (جزئيًا)، 9، 10، 11، 13، 19، 20** من `تشفير المحتوي.md` غير منفَّذة.
- والنتيجة العملية الأهم: **التنزيل لا يعمل إلا للمحتوى العام**. طلب التنزيل لا يحمل أي ترويسة تخويل (`download_manager.dart:387-395`)، ومسار الوسائط الخاص يشترط توكن قدرة (`dashboard/api/src/routes/media.ts:17-56`). أي أن الطبقة المشفَّرة تحمي اليوم ملفات يمكن لأي أحد تنزيلها مباشرة من CDN، بينما المحتوى المدفوع لا يملك مسار offline إطلاقًا.

ما هو سليم ولا يجب إعادة بنائه: الخوارزمية ومصدرها، مسار Online بتوكن القدرة وخدمة البايتات من Worker مع `private, no-store` ودعم Range، منع أي سرّ R2 في العميل، قائمة السماح لعنوان الـAPI، بوابات التنزيل الثلاث (استحقاق/شبكة/مساحة)، وسياج دورة حياة `DownloadManager` (generation fence وإلغاء التدفقات).

---

### ENC-001 — لا توجد منظومة ترخيص Offline على الخادم: صفر نقاط نهاية وصفر جداول

**الأولوية:** P0 · **المنطقة:** API / DB / التشفير · **الحالة:** 🟡 **نصف الخادم مُنفَّذ في الدفعة الثانية عشرة** — السلطة والحدود والإصدار الموقَّع والإبطال قائمة؛ ويبقى استهلاكها في العميل (`ENC-005`, `ENC-008`) وتغليف المفتاح (`ENC-002`). انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
خطة التشفير تبني كل حمايتها على جلسة تنزيل خادمية تُصدر ترخيصًا موقَّعًا ومفتاحًا مغلَّفًا. لا شيء من ذلك موجود.

**الدليل**
- `تشفير المحتوي.md:20` يعرّف نقاط: `POST /content/{entityType}/{id}/download-sessions`، `/download-sessions/{id}/refresh`، `/complete`، `/downloads/{id}/transition`، `/licenses/{id}/renew`، `/licenses/{id}/revoke`، `/devices/{id}/download-access`. **لا واحدة منها موجودة** في 69 موجّهًا تحت `dashboard/api/src/routes/`.
- استعلام D1 المحلية: الجداول `media_licenses`، `child_downloads`، `download_sessions`، `download_events` **غير موجودة** (150 جدولًا، ولا واحد منها). المخطط المرجعي يطلبها في `تشفير المحتوي.md:19`.
- لا إشارة إلى أي من الأسماء الأربعة في `dashboard/api/src/**/*.ts`.

**السلوك الحالي**
العميل يقرر بنفسه أن التنزيل مسموح، ويشفّر بمفتاحه، ويمنح نفسه صلاحية 30 يومًا. الخادم لا يعرف أن تنزيلًا حدث، ولا يستطيع عدّه ولا تحديده ولا إبطاله.

**السلوك المتوقع**
جلسة تنزيل خادمية تتحقق من الاشتراك والباقة والطفل والجهاز و`auth_epoch` والدولة وحقوق Offline وعدد أجهزة التنزيل، ثم تُصدر ترخيصًا موقَّعًا وقائمة أصول بـ`sha256` وأحجام، وتسجّل السجل في `download_sessions` و`child_downloads` و`media_licenses` مع أحداث في `download_events`.

**نطاق التنفيذ**
ترحيل D1 للجداول الأربعة؛ موجّه `downloads.ts` بالنقاط أعلاه؛ توقيع Ed25519 للترخيص مع `signatureKeyId` ودوران مفاتيح؛ ربط `child_downloads` بـ`media_licenses` بقيد؛ انتقالات حالة idempotent بـ`Idempotency-Key`.

**التبعيات** ENC-002 (تغليف المفتاح)، ENC-005 (التحقق في العميل)، API-101 (حدود الباقة خادميًا).

**الحجم** XL

**معايير القبول**
- [ ] `POST /content/book/:id/download-sessions` يرفض حسابًا بلا استحقاق بلا إصدار أي رابط أو مفتاح.
- [ ] الترخيص المُصدَر موقَّع، ويحمل `licenseId/keyId/accountId/childId/deviceId/deviceAuthEpoch/entityType/contentId/version/rights/expiresAt/signatureKeyId`.
- [ ] كل صف `child_downloads` يطابق ترخيصًا نشطًا لنفس الأسرة والطفل والجهاز والأصل والإصدار.
- [ ] `expired/revoked` حالتان نهائيتان؛ التجديد يُصدر سجلًا جديدًا ولا يُحيي القديم.
- [ ] تجاوز حدّ أجهزة التنزيل يُرفض خادميًا وذريًا، لا بإخفاء زر.

**لا تفعل**
لا تعتمد `playback_leases` كبديل عن جدول تراخيص: عمر الـlease دقائق، والترخيص Offline عمره أيام، والاثنان لهما دورتا حياة مختلفتان.

---

### ENC-002 — مفتاح واحد للتطبيق كله بدل CEK لكل حزمة/إصدار مغلَّف للجهاز

**الأولوية:** P0 · **المنطقة:** التطبيق / التشفير · **الحالة:** PARTIAL

**المشكلة**
`FileCrypto` يولّد مفتاحًا رئيسيًا واحدًا على الجهاز ويشفّر به **كل** التنزيلات، ويعيد استخدامه بعد إعادة التشغيل. خطة §10 تفرض Envelope Encryption: CEK عشوائي لكل حزمة/إصدار يولّده الخادم/KMS، مغلَّف لمفتاح الجهاز الآمن، ولا يُخزَّن على الجهاز إلا مغلَّفًا.

**الدليل**
`app_main/lib/core/crypto/file_crypto.dart:37` (`_masterKeyStorageKey = 'majarra_download_master_key_v1'`)، و`:52-72` (`_masterKey()` يولّد محليًا ويحتفظ به)، و`:74-81` (`encryptBytesToFile` يستخدم نفس المفتاح لكل ملف). مقابل `تشفير المحتوي.md:10` الذي يفرض `wrappedKey` وربطًا بـ`account + child + device + content + version + license`.

**السلوك الحالي**
استخراج مفتاح واحد من keystore جهاز واحد يفكّ **كل** ما نزّله ذلك الجهاز. ولا يمكن سحب الوصول لعنصر واحد أو لإصدار واحد، ولا تدوير المفاتيح، ولا ربط الملف بالطفل أو الترخيص.

**السلوك المتوقع**
CEK لكل حزمة/إصدار يصل مغلَّفًا مع الترخيص؛ يُفتح داخل الذاكرة وقت التشغيل فقط؛ يُمسح بأفضل جهد بعده؛ ولا يُكتب خامًا على القرص ولا في السجلات.

**نطاق التنفيذ**
مفتاح جهاز غير قابل للتصدير (Android Keystore hardware-backed، iOS Keychain/Secure Enclave)؛ تسجيل المفتاح العام مع الجهاز خادميًا؛ تغليف CEK؛ تخزين `wrappedKey` مع الترخيص؛ نموذج بيانات المفتاح كما في `تشفير المحتوي.md:10`.

**التبعيات** ENC-001، ENC-005.

**الحجم** L

**معايير القبول**
- [ ] لكل حزمة/إصدار CEK مختلف، ويثبت اختبار أن ملفَّي محتوى مختلفين لا يفكّهما مفتاح واحد.
- [ ] المفتاح الخاص للجهاز لا يخرج من مخزن المنصة ولا يُرسل إلى الخادم.
- [ ] حذف الترخيص يجعل الملف غير قابل للفك دون حذف بقية التنزيلات.
- [ ] لا يظهر CEK خامًا في أي سجل أو تقرير انهيار.

**لا تفعل**
لا تكتفِ بتوليد مفتاح لكل ملف على الجهاز: ذلك يحل تعدد المفاتيح ولا يحل ربط المفتاح بالاستحقاق ولا الإبطال.

---

### ~~ENC-003~~ — الملف يُنزَّل كاملًا **مكشوفًا** على القرص ثم يُشفَّر — الترتيب المحرَّم صراحةً في §8

**الأولوية:** P0 · **المنطقة:** التطبيق / التشفير · **الحالة:** ✅ **مُنفَّذ في الدفعة الثامنة** — انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
الخطة تمنع بالنص: «تنزيل ملف مكشوف كامل → حفظه على القرص → قراءته → تشفيره»، وتفرض Encryption Sink على التدفق. الكود ينفّذ المسار الممنوع حرفيًا.

**الدليل**
`app_main/lib/features/downloads/application/download_manager.dart:607-612` يفتح `IOSink` على `{id}.part` **بلا تشفير**، و`:629` يكتب كل chunk خامًا، ثم `:669` `partFile.readAsBytes()` و`:674` `_repo.writeEncrypted(...)`. مقابل `تشفير المحتوي.md:8` («الطريقة الصحيحة: R2 Response Stream → Encryption Sink → Encrypted Local File»).

**السلوك الحالي**
نسخة كاملة غير مشفَّرة من المحتوى تبقى على القرص طوال مدة التنزيل، وتبقى بعده إن مات التطبيق قبل `_deletePartIfPresent` (والملف `.part` يُستأنف لاحقًا، أي أنه مصمَّم للبقاء بين الجلسات). أي مدير ملفات على جهاز مفتوح الجذر ينسخها.

**السلوك المتوقع**
تشفير أثناء التدفق: كل chunk يُشفَّر قبل الكتابة، والاستئناف يعمل على chunks مشفَّرة بترقيم nonce حتمي.

**نطاق التنفيذ**
استبدال `IOSink` الخام بـsink مشفِّر لكل chunk (يستلزم ENC-006 للتقسيم)؛ إعادة تصميم الاستئناف على حدود chunk بدل بايتات خام؛ حذف مسار `readAsBytes` كليًا.

**التبعيات** ENC-006 (chunking) — لا يمكن إصلاح هذا بشكل نظيف مع تشفير الملف ككتلة واحدة.

**الحجم** L

**معايير القبول**
- [ ] لا يوجد في أي لحظة ملف مؤقت يحتوي بايتات محتوى غير مشفَّرة.
- [ ] قتل التطبيق في منتصف التنزيل ثم فحص المجلد لا يُظهر بايتات قابلة للقراءة.
- [ ] الاستئناف بعد الانقطاع يكمل من آخر chunk مكتمل ويعيد تنزيل التالف وحده.

**لا تفعل**
لا تكتفِ بحذف `.part` عند نجاح التنزيل: النافذة الزمنية هي المشكلة، لا بقاء الملف.

---

### ~~ENC-004~~ — فك التشفير إلى ملف مكشوف على القرص للتشغيل — ممنوع صراحةً في §12

**الأولوية:** P0 · **المنطقة:** التطبيق / التشفير · **الحالة:** ✅ **مُنفَّذ في الدفعة الحادية عشرة** — انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
التشغيل يكتب نسخة **مفكوكة كاملة** بامتداد الوسائط الحقيقي على القرص ثم يُسلّمها للمشغّل.

**الدليل**
`app_main/lib/features/downloads/data/download_repository.dart:66-72` (`decryptForPlayback` ينشئ `.play_{id}{ext}` ويكتب المحتوى الصريح)، و`_extensionFor` في نهاية الملف يمنحه `.mp4` أو `.m4a`، ثم `playback_page.dart:609` و`audio_player_page.dart:196` يشغّلانه بـ`VideoPlayerController.file`. مقابل `تشفير المحتوي.md:12` («ممنوع: Encrypted File → إنشاء page_001.webp مكشوف → تشغيله من القرص»).

**السلوك الحالي**
كل تشغيل offline ينتج ملف `.mp4`/`.m4a` كامل الصلاحية داخل مجلد التطبيق. اسم الملف يبدأ بنقطة فقط، وهو ليس حماية. والتنظيف يعتمد على `deletePlayFile`/`cleanupPlayFiles`، فأي إغلاق قسري يتركه.

**السلوك المتوقع**
فك التشفير في الذاكرة أو عبر مصدر بيانات مخصص (proxy محلي على loopback، أو `DataSource` مخصص، أو Platform View مع pipe) بلا ملف وسيط، ومحو الـbuffers عند الانتقال.

**نطاق التنفيذ**
تقييم البدائل الثلاثة على `video_player`/`just_audio`؛ الأرجح خادم HTTP محلي على 127.0.0.1 يخدم بايتات مفكوكة من chunks عند الطلب مع توكن جلسة داخلي؛ حذف `decryptForPlayback` و`deletePlayFile` بعده.

**التبعيات** ENC-006.

**الحجم** L

**معايير القبول**
- [ ] لا يُنشأ أي ملف قابل للتشغيل مباشرة أثناء أو بعد التشغيل offline.
- [ ] إنهاء التطبيق قسريًا أثناء التشغيل لا يترك بايتات مفكوكة.
- [ ] التشغيل offline ما زال يدعم التقديم والتأخير (seek) — أي أن الحل يخدم Range.

**لا تفعل**
لا تعتمد على البادئة `.` أو امتداد مخصص كحماية؛ `تشفير المحتوي.md:13` ينص أن الامتداد ليس وسيلة حماية.

---

### ~~ENC-005~~ — الترخيص محلي بالكامل: ساعة الجهاز هي السلطة، والصلاحية مخزَّنة في مكان غير آمن

**الأولوية:** P0 · **المنطقة:** التطبيق / الترخيص · **الحالة:** ✅ **مُنفَّذ في الدفعة الثالثة عشرة** — انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
مدة الـ30 يومًا ثابتة في الكود، وتاريخ الانتهاء يُحسب على الجهاز، ويُخزَّن في `shared_preferences` كنص JSON، ويُقارن بـ`DateTime.now()`. لا توقيع، ولا ربط بالحساب/الطفل/الجهاز/`auth_epoch`/الإصدار، ولا وقت موثوق، ولا Monotonic Anchor.

**الدليل**
- `download_manager.dart:63` — `offlineLicenseDuration = const Duration(days: 30)`.
- `download_manager.dart:679-682` — `expiresAt = DateTime.now().add(offlineLicenseDuration).millisecondsSinceEpoch`.
- `download_models.dart:84-88` — `isExpired()` يقارن بـ`DateTime.now()`.
- `download_repository.dart:26` — الميتاداتا كلها في `shared_preferences` تحت `majarra_downloads_v1`، لا في `flutter_secure_storage`.
- مقابل `تشفير المحتوي.md:11` («لا تعتمد على ساعة الجهاز فقط»، Monotonic Clock Anchor، توقيع حالة الترخيص، منع استعادة نسخة أقدم).

**السلوك الحالي**
تأخير ساعة الجهاز يمدّد الصلاحية بلا حد. وعلى جهاز مفتوح الجذر، تحرير قيمة `expires_at` في `shared_preferences` (ملف XML/plist عادي) يجعل الترخيص أبديًا. ولا شيء يمنع نسخ ملف الميتاداتا واستعادته بعد انتهاء الصلاحية.

**السلوك المتوقع**
ترخيص موقَّع من الخادم يُتحقَّق من توقيعه و`signatureKeyId` قبل كل تشغيل؛ مقارنة بوقت موثوق آخر مرة + Monotonic Anchor؛ حالة الترخيص وتوقيعها في التخزين الآمن؛ إعادة تحقق دورية إلزامية للمحتوى الأعلى حساسية.

**نطاق التنفيذ**
نقل حالة الترخيص إلى `flutter_secure_storage`؛ تنفيذ تحقق التوقيع بمفاتيح عامة مُبندلة (حالية وسابقة)؛ تخزين آخر وقت موثوق من ترويسة `Date` الموقَّعة أو نقطة نهاية وقت؛ Monotonic Anchor عبر `Stopwatch`/`elapsedRealtime` حيث تتوفر؛ رفض تراجع الحالة.

**التبعيات** ENC-001، ENC-002.

**الحجم** L

**معايير القبول**
- [ ] تأخير ساعة الجهاز 60 يومًا لا يُعيد تشغيل محتوى انتهى ترخيصه.
- [ ] تحرير الميتاداتا يدويًا يُبطل التشغيل بدل أن يمدّده (التوقيع يفشل).
- [ ] استعادة نسخة أقدم من حالة الترخيص تُرفض.
- [ ] مدة الترخيص تأتي من الخادم لكل باقة/محتوى، لا من ثابت في العميل.

**لا تفعل**
لا تعتمد على `DateTime.now()` كمصدر وحيد، ولا تنقل الاعتماد إلى `DateTime.now().toUtc()` وتعتبره إصلاحًا.

---

### ~~ENC-006~~ — لا تقسيم إلى Chunks ولا AAD ولا nonce مربوط بالسياق، والملف كله في الذاكرة

**الأولوية:** P1 · **المنطقة:** التطبيق / التشفير · **الحالة:** ✅ **مُنفَّذ في الدفعة السابعة** — انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
كل ملف يُشفَّر ككتلة واحدة، وتحميل الملف كله في الذاكرة، وبلا `AAD` تربط الجزء بسياقه.

**الدليل**
- `file_crypto.dart:74-81` — `encryptBytesToFile(Uint8List plainBytes, ...)` يأخذ الملف كاملًا في الذاكرة، ولا يمرّر `aad`.
- `file_crypto.dart:83-96` — `decryptFile` يقرأ الملف كله (`readAsBytes`) ثم يفك.
- `download_manager.dart:65` — `maxItemBytes = 512 MiB`، أي أن هذا المسار قد يطلب نصف جيجابايت في الذاكرة على جهاز طفل.
- مقابل `تشفير المحتوي.md:8` (AAD تشمل `contentId + version + assetId + chunkIndex + deviceId + licenseId`) و`:9` (chunks 1–8 MB بحسب النوع، مع `nonce` و`tag` و`sourceSha256` و`ciphertextSha256` لكل chunk).

**السلوك الحالي**
- خطر OOM على أجهزة منخفضة الذاكرة عند 512 MiB.
- لا يمكن إعادة تنزيل جزء تالف وحده؛ التلف يعني إعادة الملف كله.
- لا يمكن بدء التشغيل قبل اكتمال التنزيل.
- غياب AAD يعني أن ciphertext صالح من سياق آخر (محتوى آخر، جهاز آخر، ترخيص آخر) لا يُرفض تشفيريًا؛ الرفض يعتمد على منطق التطبيق فقط.

**السلوك المتوقع**
تشفير لكل chunk بحجم مناسب للنوع، nonce فريد حتميًا مشتقًا من Package Salt + Chunk Counter، AAD كما في الخطة، وmanifest يحفظ لكل chunk حجمه وnonce وtag وhashes.

**نطاق التنفيذ**
تصميم صيغة الحزمة (manifest + chunks)؛ تعديل `FileCrypto` لواجهة chunk؛ تعديل `DownloadRepository` و`DownloadManager` لكتابة/قراءة chunks؛ تشغيل تدفقي يقرأ chunk عند الحاجة.

**التبعيات** ENC-003 و ENC-004 يعتمدان عليه.

**الحجم** L

**معايير القبول**
- [ ] تنزيل ملف 400 MB لا يرفع استهلاك الذاكرة بمقدار حجم الملف.
- [ ] إتلاف بايت واحد في chunk يجعل ذلك الـchunk فقط يفشل ويُعاد تنزيله.
- [ ] فك chunk بـAAD لا تطابق السياق يفشل حتى مع المفتاح الصحيح.
- [ ] لا يتكرر nonce مع المفتاح نفسه — يثبته اختبار على عدد كبير من chunks.

**لا تفعل**
لا تكتفِ بوصف الـnonce بأنه «عشوائي»؛ الخطة تشترط تفرّدًا حتميًا مع سجل يمنع إعادة الاستخدام.

---

### ~~ENC-007~~ — لا تحقق سلامة من المصدر: لا `sha256` للأصل ولا manifest موقَّع

**الأولوية:** P1 · **المنطقة:** التطبيق / API / التشفير · **الحالة:** ✅ **مُنفَّذ في الدفعة الرابعة عشرة** (حدّان مُعلَنان: الأصول القديمة بلا بصمة، والتنزيل المستأنف لا يُتحقَّق منه) — انظر §15.

**المشكلة**
العميل لا يتحقق أن ما نزّله هو ما نشره الخادم، والخادم لا يرسل بصمة أصلًا.

**الدليل**
لا ظهور لـ`sha256` في `app_main/lib/features/downloads/**` (البحث الوحيد الناجح على `sha256` في التطبيق هو في `pin_kdf.dart` و`playback_watermark.dart`). ولا حقل بصمة في أي استجابة تنزيل لأن نقطة التنزيل غير موجودة (ENC-001). عمود `content_assets.source_sha256` مطلوب في `تشفير المحتوي.md:19` — والجدول موجود لكن لا مسار يستخدمه للتنزيل.

**السلوك الحالي**
الاعتماد على TLS وETag فقط. أصل مستبدل على المنشأ، أو استجابة مقطوعة يقبلها الطرفان كـ200، تُخزَّن مشفَّرة وتُعتبر سليمة.

**السلوك المتوقع**
كل أصل في جلسة التنزيل يحمل `sha256`؛ العميل يتحقق منه ومن `manifestSha256` الموقَّع؛ وأي عدم تطابق يُرفض بلا كتابة.

**نطاق التنفيذ**
حساب وتخزين `source_sha256` عند الاستيراد؛ إعادته في جلسة التنزيل؛ التحقق التدفقي في العميل؛ توقيع الـmanifest (`تشفير المحتوي.md:23`).

**التبعيات** ENC-001، ENC-006.

**الحجم** M

**معايير القبول**
- [ ] أصل بـhash غير مطابق يُرفض ويُسجَّل، ولا يظهر كتنزيل جاهز.
- [ ] manifest بتوقيع غير صالح أو `signatureKeyId` غير معروف يُرفض.
- [ ] تعديل ترتيب صفحات الحزمة يكتشفه التوقيع.

---

### ~~ENC-008~~ — التنزيل يعمل للمحتوى العام فقط: الطلب بلا أي ترويسة تخويل

**الأولوية:** P0 · **المنطقة:** التطبيق / API · **الحالة:** ✅ **مُنفَّذ في الدفعة الثالثة عشرة** (حدّ مُعلَن: أصل واحد لكل عنصر — الحزمة متعدّدة الأصول تحت `ENC-015`) — انظر §15.

**المشكلة**
كل الطبقة المشفَّرة مبنية على تنزيل مجهول من رابط عام، فهي تحمي ما لا يحتاج حماية، بينما المحتوى المدفوع لا يملك مسار offline.

**الدليل**
- `download_manager.dart:387-395` — ترويسات الطلب هي `Range` و`If-Match` فقط؛ لا `Authorization` ولا `X-Parent-Proof`.
- `download_manager.dart:24-33` — `DownloadRequest` لا يحمل حقل توكن إطلاقًا.
- `audio_player_page.dart:352-366` — التعليق صريح: «Private narration cannot be persisted without an offline licence»، وزر التنزيل يُبنى من `widget.audioUrl` العام.
- `dashboard/api/src/routes/media.ts:17-56` — الأصل الخاص لا يُخدم إلا بتوكن قدرة في ترويسة.

**السلوك الحالي**
الحلقات والقصص المدفوعة لا يمكن تنزيلها. الميزة المعلنة «تنزيل للعمل دون إنترنت» تنطبق على عيّنات صوتية عامة.

**السلوك المتوقع**
جلسة تنزيل تُصدر قدرة قصيرة العمر لكل أصل خاص، والعميل يحملها في ترويسة، ويجدّد الجلسة عند انتهاء صلاحية القدرة أثناء تنزيل طويل.

**نطاق التنفيذ**
ENC-001 ثم تمرير القدرة في `DownloadRequest`؛ منطق تجديد أثناء التنزيل؛ منع تسجيل الرابط أو التوكن في أي سجل.

**التبعيات** ENC-001.

**الحجم** M

**معايير القبول**
- [ ] تنزيل قصة مدفوعة ينجح لحساب مستحق ويفشل لغيره.
- [ ] انتهاء القدرة أثناء تنزيل طويل يجدّد خادميًا ولا يفشل التنزيل.
- [ ] لا يظهر رابط أو توكن كامل في أي سجل أو تحليلات أو إشعار.

---

### ~~ENC-009~~ — لا DRM ولا HLS/DASH متعدد DRM ولا HDCP، ولا تنزيل فيديو

**الأولوية:** ~~P0~~ → **مؤجَّل بشرط** · **المنطقة:** التطبيق / البث · **الحالة:** **تنازل موثَّق بقرار مالك (`ACCEPTED-TRADEOFF`)** — انظر §15 (الدفعة العاشرة). النصّ أدناه يبقى كما وُجد، ويعود البند نشطًا فور نشر أي محتوى **مرخَّص من طرف ثالث**.

**المشكلة**
الخطة تفرض Multi-DRM من الـMVP للفيديو المدفوع أو المرخَّص. الموجود `video_player` عادي.

**الدليل**
- لا Widevine/FairPlay/PlayReady في أي مكان بالتطبيق؛ `app_main/lib/core/security/screen_capture_guard.dart:12` و`android/app/src/main/kotlin/.../MainActivity.kt:26` يعترفان صريحًا بغياب Widevine.
- `episode_renditions` = 0 و`content_assets.kind='video'` = 0 في D1 المحلية، فمسار HLS لا يُنتج إلا manifest فارغًا (`episodes.ts:493`).
- لا نقطة تنزيل فيديو في التطبيق: زر التنزيل الوحيد في `audio_player_page.dart:357`.
- مقابل `تشفير المحتوي.md:15`.

**السلوك الحالي**
الوضع المطابق فعلًا هو «الوضع المرحلي بلا DRM» في `تشفير المحتوي.md:1192`: بايتات مخدومة من Worker بتوكن قصير. مقبول للفيديو الذي نملكه، وغير مقبول لأي محتوى مرخَّص، ولا يقدّم تنزيل فيديو.

**السلوك المتوقع**
قرار موثَّق: إمّا حصر الإطلاق على محتوى نملكه بلا DRM (مع بيان صريح في الوثائق والعقود)، أو تعاقد مع مزوّد Packager/Multi-DRM قبل نشر أي محتوى مرخَّص.

**نطاق التنفيذ**
`DECIDE-101` أولًا؛ ثم تكامل مشغّل يدعم DRM (استبدال `video_player` أو قناة native)، وخادم تراخيص يفحص الاستحقاق، وHDCP وSecure Surface، وOffline DRM License.

**التبعيات** DECIDE-101، EXT-101.

**الحجم** XL

**معايير القبول**
- [ ] لا يوجد MP4 خاص يُخدَم لمحتوى مرخَّص.
- [ ] الترخيص لا يُصدر إلا بعد فحص الاشتراك والطفل والجهاز والمنطقة.
- [ ] HDCP وSecure Decoder مفعّلان حيث تدعمهما المنصة، والفشل يُبلَّغ لا يُتجاهل.

**لا تفعل**
لا تسمِّ توكن القدرة الحالي «DRM»، ولا تعتبر HLS بروابط مؤقتة بديلًا؛ الخطة تنص على ذلك في `تشفير المحتوي.md:15`.

---

### ~~ENC-010~~ — الإبطال لا يصل: سحب الجهاز أو التنزيلات لا يعطّل ملفًا واحدًا على جهاز

**الأولوية:** P0 · **المنطقة:** API / التطبيق / الترخيص · **الحالة:** ✅ **مُنفَّذ في الدفعة الخامسة عشرة** — انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
توجد نقطة إدارية لسحب التنزيلات، لكنها تسحب Leases، والعميل لا يسأل الخادم عن أي ترخيص قبل التشغيل offline.

**الدليل**
- `dashboard/api/src/routes/adminDevices.ts:121-141` → `dashboard/api/src/do/FamilyState.ts:1152-1162`، والتعليق نفسه يقول: «leases are what authorise offline media in this architecture; there is no separate downloads table».
- `download_manager.dart:900-935` (`_preparePlayback`) يفحص `isExpired()` والملف والمفتاح فقط — **لا نداء شبكة، ولا فحص استحقاق حي**.
- مقابل `تشفير المحتوي.md:12` البند 12 («يعيد فحص `child_download_is_usable` أو قرار الاستحقاق الحي قبل إصدار أي Signed URL جديد») و§11 («يمكن للسيرفر إبطال جهاز أو ترخيص مسروق عند أول اتصال لاحق»).

**السلوك الحالي**
جهاز مسروق أو حساب أُلغي اشتراكه يواصل تشغيل كل ما نزّله حتى تنتهي الـ30 يومًا التي منحها العميل لنفسه. سحب الجهاز من اللوحة لا يُنتج أي أثر على الجهاز.

**السلوك المتوقع**
عند كل اتصال: تجديد/تحقق ترخيص، وسحب فوري للتراخيص عند رفع `auth_epoch`؛ وعند عدم الاتصال: انتهاء محلي مضمون بمدة قصيرة وإعادة تحقق دورية إلزامية.

**نطاق التنفيذ**
ENC-001 و ENC-005؛ فحص ترخيص عند الإقلاع وعند كل تشغيل offline؛ حذف المفتاح والترخيص عند الإبطال؛ ربط `auth_epoch` بحالة الترخيص المحلية.

**التبعيات** ENC-001، ENC-005.

**الحجم** M

**معايير القبول**
- [ ] سحب الجهاز من اللوحة يجعل التنزيلات غير قابلة للتشغيل عند أول اتصال.
- [ ] انتهاء الاشتراك يمنع تجديد الترخيص ولا يمدّد أي ترخيص قائم بعد `plan_change_effective_at`.
- [ ] بقاء الجهاز غير متصل مدة أطول من نافذة إعادة التحقق يوقف التشغيل محليًا.

---

### ~~ENC-011~~ — فشل مخزن المفاتيح مكتوم: تنزيلات تصبح غير قابلة للفك بلا أي إشعار

**الأولوية:** P1 · **المنطقة:** التطبيق / التشفير · **الحالة:** ✅ **مُنفَّذ في الدفعة السابعة** — انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
كتابة المفتاح الرئيسي في المخزن الآمن مغلَّفة بـ`try/catch` صامت، فإن فشلت يستمر التطبيق بمفتاح موجود في الذاكرة فقط.

**الدليل**
`app_main/lib/core/crypto/file_crypto.dart:39-45` (`_safeRead` يبتلع كل خطأ) و`:66-70` (`try { await _storage.write(...) } catch (_) {}` ثم `return _cachedKey = SecretKey(raw)`). نمط مشابه في `auth_storage.dart:46-60`.

**السلوك الحالي**
تُنزَّل ملفات وتُشفَّر بمفتاح لم يُحفَظ. بعد إعادة تشغيل التطبيق يُولَّد مفتاح جديد، فكل ما نُزِّل قبله يصبح ciphertext لا يُفك أبدًا، ويظهر للمستخدم كتنزيل «جاهز» يفشل عند التشغيل.

**السلوك المتوقع**
فشل كتابة المفتاح يمنع بدء أي تنزيل ويظهر كخطأ صريح قابل للفهم («التخزين الآمن غير متاح على هذا الجهاز»)، ويُسجَّل بلا أي مادة مفاتيح.

**نطاق التنفيذ**
تمييز أخطاء المخزن عن غيابه؛ إرجاع فشل بدل مفتاح ذاكرة؛ رسالة مستخدم؛ اختبار يحاكي فشل الكتابة.

**الحجم** S

**معايير القبول**
- [ ] فشل الكتابة في المخزن الآمن يمنع التنزيل بدل إنتاج تنزيل غير قابل للفك.
- [ ] الرسالة للمستخدم تشرح السبب ولا تكشف تفاصيل تشفيرية.
- [ ] اختبار يغطّي الحالة.

---

### ENC-012 — الكاش المحلي يخالف سياسة §5: يبقى بعد الخروج، ويقبل مدخلات منتهية ومستقبلية

**الأولوية:** P1 · **المنطقة:** التطبيق / الكاش / الخصوصية · **الحالة:** REGRESSION

**المشكلة**
ثلاثة اختبارات فاشلة تُثبت أن كاش القارئ والكاتالوج لا يحترمان سياسة «الكاش يُمسح عند الخروج ويصبح غير قابل للاستخدام فور الإبطال».

**الدليل**
- `app_main/test/logout_tile_test.dart` — «logout must wipe cached reader pages»، والفعلي: `['majarra_reader_pages_v1.story.story-bird-home.ar']` باقٍ بعد الخروج.
- `app_main/test/catalog_cache_test.dart:82` — «an entry older than the TTL is refused» يفشل: `Expected: null, Actual: Instance of 'CachedCatalog'`.
- نفس الملف — «a future timestamp is refused rather than trusted forever» يفشل بنفس الشكل.
- مقابل `تشفير المحتوي.md:5` («يفضل مسحه عند تسجيل الخروج، ويصبح غير قابل للاستخدام فور إلغاء الجهاز أو انتهاء الترخيص»).

**السلوك الحالي**
صفحات قصة مقروءة تبقى على الجهاز بعد خروج المستخدم؛ وكاتالوج منتهي أو بطابع زمني مزوَّر يُستخدم إلى الأبد.

**السلوك المتوقع**
تفكيك الجلسة يمسح كل كاش محتوى؛ ورفض أي مدخل كاش أقدم من TTL أو بطابع زمني مستقبلي.

**نطاق التنفيذ**
إضافة كاش القارئ إلى مسار تفكيك الجلسة؛ إصلاح تحقق TTL والطابع المستقبلي في `core/cache/catalog_cache.dart`؛ الإبقاء على الاختبارات الثلاثة كشبكة انحدار.

**الحجم** S

**معايير القبول**
- [ ] الاختبارات الثلاثة تنجح.
- [ ] لا مفتاح `majarra_reader_pages_v1*` يبقى بعد الخروج.
- [ ] مدخل بطابع زمني مستقبلي يُرفض ويُسجَّل.

---

### ENC-013 — حدود التنزيل والاستحقاق مفروضة في العميل فقط

**الأولوية:** P1 · **المنطقة:** التطبيق / API · **الحالة:** PARTIAL

**المشكلة**
سقف المساحة وسقف العنصر ثابتان في الكود، وفحص الاستحقاق يقرأ حالة فوترة في العميل، ولا يوجد فرض خادمي لحدّ أجهزة التنزيل `0/2/4`.

**الدليل**
- `download_manager.dart:64-65` — `maxTotalBytes = 2 GiB`، `maxItemBytes = 512 MiB` ثابتة.
- `app_main/lib/features/downloads/application/download_providers.dart:62-66` — `_isEntitledToDownload` يقرأ `billingStatusProvider` ويقارن `downloadDevices > 0`، والتعليق نفسه يقول إن الخادم يبقى السلطة — لكن لا يوجد نداء خادمي في مسار التنزيل ليكون سلطة.
- `dashboard/api/src/lib/familyPolicy.ts:10-12` يعرّف الحدود في الكود، وجدول `subscription_plan_limits` يعرّفها أيضًا — مصدرا حقيقة (انظر `API-102`).

**السلوك الحالي**
عميل معدَّل يتجاوز الحدود كلها؛ والخادم لا يعرف عدد أجهزة التنزيل المستخدمة لأنه لا يسجّل تنزيلات.

**السلوك المتوقع**
الحدود تصل من الخادم مع الاستحقاق وتُفرض خادميًا وذريًا عند إنشاء جلسة التنزيل؛ والعميل يستخدمها للعرض فقط.

**التبعيات** ENC-001، API-102.

**الحجم** M

**معايير القبول**
- [ ] محاولة تنزيل على جهاز ثالث في باقة Family تُرفض خادميًا.
- [ ] حدود المساحة تأتي من الخادم لكل باقة.
- [ ] لا قرار استحقاق نهائي يُتخذ في العميل.

---

### ~~ENC-014~~ — حماية العرض ناقصة على iOS وغير مُتحقَّق منها على أي جهاز

**الأولوية:** P2 · **المنطقة:** التطبيق / الأمان · **الحالة:** ✅ **الرصد مُنفَّذ في الدفعة التاسعة** (يبقى التحقّق على جهاز حقيقي) — انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
`FLAG_SECURE` مطبَّق على Android أثناء التشغيل فقط، ولا يوجد أي رصد لحالة Capture/Mirroring على iOS/iPadOS/tvOS كما تطلب §24.

**الدليل**
`app_main/lib/core/security/screen_capture_guard.dart:20-38` (قناة `com.majarra/device`) و`MainActivity.kt:28`. لا مقابل iOS في الشجرة. العلامة المائية موجودة (`core/security/playback_watermark.dart`) وبلا PII — وهذا مطابق للخطة.

**السلوك المتوقع**
رصد Capture/Mirroring على منصات Apple وتغطية أو إيقاف المحتوى حيث تسمح الواجهات العامة، مع توثيق صريح أن المنع الكامل غير ممكن.

**الحجم** M

**معايير القبول**
- [ ] تسجيل الشاشة على iOS يُرصد ويُطبَّق عليه سلوك موثَّق.
- [ ] السلوك مُتحقَّق منه على جهاز حقيقي لكل منصة، بدليل مرفق.
- [ ] لا تُحذف بيانات مستخدم ولا يُعاقَب اعتمادًا على إشارة Capture واحدة.

---

### ENC-015 — نموذج الحزمة والصيغ (`.mjrpack` / `.mjrlic`) غير منفَّذ

**الأولوية:** P2 · **المنطقة:** التطبيق / التشفير · **الحالة:** MISSING

**المشكلة**
الخطة تصف حزمة محتوى واحدة تضم manifest وصورًا وصوتًا ونصوصًا وتوقيتات وhashes وإصدارًا (§13، §14). الموجود ملف واحد لكل عنصر تنزيل، بلا manifest ولا إصدار.

**الدليل**
`download_repository.dart:47-51` (`fileFor` = ملف واحد باسم `item.fileName`) و`_extensionFor` (`.mp4`/`.m4a`/`.bin`). لا manifest ولا حقل `version` في `DownloadItem` (`download_models.dart:33-60`).

**الأثر**
لا يمكن تنزيل قصة كحزمة (صفحات + صوت لكل لغة + توقيتات)، ولا تحديث جزء من الحزمة، ولا معرفة أن إصدار المحتوى تغيّر — وهي كلها متطلبات §9 و§21.

**الحجم** L

**معايير القبول**
- [ ] حزمة قصة واحدة تضم الصفحات والصوت والنصوص والتوقيتات وmanifest موقَّعًا.
- [ ] تغيير إصدار المحتوى يُكتشف ويُعرض للمستخدم كتحديث لا كإعادة تنزيل كاملة.

---

## §4 مطابقة معايير القبول الـ25 في `تشفير المحتوي.md` §26

الحالة: ✅ مستوفى · 🟡 جزئي · 🔴 غير مستوفى. لا صف بلا دليل ولا بلا تاسك يغلقه.

| # | المعيار (مختصر) | الحالة | الدليل | يغلقه |
|---:|---|:---:|---|---|
| 1 | لا رابط دائم للمحتوى المدفوع، ولا أسرار R2 في التطبيق، والمشغّل يحمل Authorization قصير لمسار Worker فقط | ✅ | `routes/media.ts:17-56` يخدم البايتات بتوكن قدرة مع `private, no-store`؛ لا presigned URL في المستودع؛ `app_environment.dart:161-215` قائمة سماح للمضيف | — |
| 2 | لا يحصل غير المستحق على URL أو Playback/Offline License | ✅ | Playback مُحكَم، وOffline License يُصدره الخادم بعد فحص الاستحقاق والحدود (الدفعة 12)، والتنزيل صار يمرّ على مسار Worker بقدرة في ترويسة لا على رابط عام (الدفعة 13). لا رابط R2 يصل العميل في أي مسار | — |
| 3 | الحدود `1/4/4` ملفات، `1/4/8` أجهزة، `1/2/4` تشغيل، `0/2/4` تنزيل تُفرض خادميًا وذريًا | ✅ | الأربعة كلها في الـDO الآن: الأجهزة والأطفال والتشغيل كما كانت، و**حدّ أجهزة التنزيل وحدّ العناصر** أُضيفا في الدفعة 12 (`offlineItems` و`downloadDevices` يُقرأان في `issueOfflineLicense` داخل معاملة واحدة). الباقة المجانية تُرفض صراحةً بـ`offline_not_in_plan` | — |
| 4 | تطبيق TV يحتسب جهازًا، وCast/AirPlay تشغيلًا واحدًا | 🔴 | لا نقطة إقران أو اعتماد TV في كل الـAPI — منصوص عليه في `lib/parentAuth.ts:57-60`؛ `tv_pairing_page.dart:12-16` صفحة بلا خادم؛ لا معالجة Cast/AirPlay | `API-103` |
| 5 | الفيديو المدفوع عبر HLS/DASH + DRM/HDCP، ولا MP4 ولا مفتاح عام ثابت | ✅ **بالوضع المرحلي** | المعيار نفسه يستثني «فيديو أصلي يملكه المنتج» من DRM. قرار مالك 2026-08-26: كل المحتوى من إنتاج «مجرة»، والملكية مسجَّلة في `content_rights` (248 صفًّا). البايتات تُخدَم من Worker بتوكن 3 دقائق (`media.ts`) لا برابط دائم ولا مفتاح ثابت. **الشرط:** أول محتوى مرخَّص يُعيد `ENC-009` نشطًا | `HUMAN-101` (شرطي) |
| 6 | Tokens تنتهي في موعدها ويتجدد التشغيل دون كشفها في السجلات | ✅ | `parentAuth.ts:12-16` (access 15د، media 3د، proof 5د)؛ `auditLog.ts` `redactForAudit` يحجب `token/secret/authorization`؛ heartbeat يجدّد | — |
| 7 | لا يحفظ Online Cache المدفوع مكشوفًا على القرص | 🟡 | كاش القارئ يُمسح عند الخروج والكاش المنتهي/المستقبلي يُرفَض (`ENC-012`، الدفعة 1). ويبقى أن صفحات الكاش نفسها تُكتب على القرص **غير مشفَّرة** ما دامت الجلسة قائمة | `ENC-012` (متابعة) |
| 8 | لا يُحفَظ أي محتوى Offline مفكوكًا بصورة دائمة | ✅ | لا بايت صريح يُكتب على القرص إطلاقًا: التنزيل يُشفَّر أثناء التدفق (`ENC-003`، الدفعة 8)، والتشغيل يقرأ من مصدر محلي يفكّ في الذاكرة عند الطلب (`ENC-004`، الدفعة 11). حذف `.play_*` يبقى مستدعى لتنظيف ما كتبته نسخ أقدم من التطبيق | — |
| 9 | CEK يختلف حسب الحزمة/الإصدار، ولا يتكرر nonce مع المفتاح | 🟡 | مفتاح مشتقّ لكل حزمة بـHKDF (ملح لكل ملف)، وnonce **حتمي** `salt‖chunkIndex` لا يتكرّر داخل الحزمة ببنائه (`ENC-006`، الدفعة 7). ويبقى أن المفتاح يُشتَقّ من مفتاح تطبيق لا يأتي مغلَّفًا من الخادم لكل إصدار | `ENC-002` |
| 10 | كل chunk يتحقق من GCM tag وAAD والhashes | ✅ | أجزاء 1 ميغابايت، ووسم GCM لكل جزء، وAAD تربط السياق والفهرس وعلامة `final` (الدفعة 7)، وبصمة المصدر تأتي موقَّعة في الترخيص ويقارنها العميل أثناء التدفّق (الدفعة 14). **حدّ مُعلَن:** الأصول المستوردة بلا بصمة تُقبل (16 من 30 صوتيًّا لها بصمة)، والتنزيل المستأنف لا يُتحقَّق منه | — |
| 11 | المفاتيح مغلَّفة عبر Keystore/Keychain/Secure Enclave، ولا مفتاح خام على القرص أو في السجلات | 🟡 | المفتاح في `flutter_secure_storage` بخيارات مقوّاة (`file_crypto.dart:29-34`)، لكنه **مفتاح مولَّد محليًا لا مغلَّف من الخادم**، وفشل الحفظ مكتوم (`:66-70`) | `ENC-002` `ENC-011` |
| 12 | كل Offline License موقَّع ومقيَّد بالحساب/الطفل/الجهاز/`auth_epoch`/الأصل/الإصدار/الحقوق/الانتهاء | ✅ | الترخيص يُوقَّع بـEd25519 في الخادم ويحمل الحقول الثمانية كلها (الدفعة 12)، والتطبيق يتحقّق من التوقيع ومن كل قيد قبل التشغيل (الدفعة 13). تحرير أي حقل يُفشل التوقيع — يثبّته اختبار يمدّد `exp` ستين يومًا | — |
| 13 | يعمل Offline داخل مدة الترخيص فقط ويتوقف فور الانتهاء دون اتصال | ✅ | الانتهاء يُقاس بـ«أفضل وقت موثوق»: الأكبر بين ساعة الجهاز وآخر علامة خادمية + ساعة أحادية الاتجاه (الدفعة 13). تأخير الساعة لا يُعيد الزمن إلى الوراء. **حدّ مذكور:** الإغلاق الكامل يحتاج تحققًا خادميًّا دوريًّا | — |
| 14 | إلغاء الجهاز يرفع `auth_epoch` ويسحب جلساته وتراخيصه وتنزيلاته دون حذف صف الجهاز | ✅ | صار يُبطل التراخيص أيضًا في الـDO (الدفعة 12)، والعميل يرفض ترخيصًا بعهد أقدم من عهد الجلسة الحاضرة (الدفعة 13) — فلا حاجة إلى قائمة إبطال على الجهاز | — |
| 15 | حالات الجلسة والترخيص النهائية لا تعود Active | 🟡 | مستوفى للجلسات والـleases (`WHERE status='active'` في كل انتقال). غير قابل للتطبيق على التراخيص لعدم وجودها | `ENC-001` |
| 16 | يستكمل التنزيل، ويعاد الجزء التالف وحده، وانتقالات Idempotent/CAS | 🟡 | الاستكمال يعمل بـ`Range`+`ETag` (`download_manager.dart:387-395`)، لكن **إعادة الجزء التالف وحده غير ممكنة** بلا chunks؛ ولا Idempotency-Key على مسار التنزيل لعدم وجوده | `ENC-006` `ENC-001` |
| 17 | لا يمكن تجاوز الاشتراك أو ملكية الطفل بتعديل Flutter أو Body | 🟡 | مستوفى لمسارات Online: `parent_id` من التوكن وملكية الطفل تُثبَت في الـDO. **غير مستوفى للتنزيل**: القرار كله في العميل | `ENC-008` `ENC-013` |
| 18 | التحقق من توقيع Canonical Manifest/License مع `signatureKeyId` ودوران مفاتيح | 🔴 | لا توقيع ولا مفاتيح عامة مُبندلة ولا manifest في التطبيق | `ENC-007` |
| 19 | لا CORS ولا Root detection ولا الامتداد كحماية أساسية | 🟡 | لا يُعتمد على CORS كحماية (المصادقة Bearer)، والامتداد ليس حماية اسمًا — لكن الأثر العملي أن `.play_*`+امتداد الوسائط هو **كل** ما يفصل بين المفكوك والمستخدم (`ENC-004`) | `ENC-004` |
| 20 | `FLAG_SECURE`/Capture Response وWatermark بلا PII، مع توثيق عدم الضمان | 🟡 | Android مستوفى (`screen_capture_guard.dart:20-38`)، وWatermark بلا PII (`playback_watermark.dart:166-170`). **iOS بلا رصد Capture**، ولا تحقق على جهاز | `ENC-014` |
| 21 | اختبار Root/Jailbreak وClock Rollback وإعادة التثبيت وفقدان المفتاح | 🔴 | لا كشف Root/Jailbreak إطلاقًا؛ لا اختبار لتراجع الساعة؛ فقدان المفتاح مكتوم لا مُختبَر | `ENC-005` `ENC-011` `SEC-107` |
| 22 | Audit Logs لكل جلسة وإلغاء دون URL أو Token أو مفتاح أو PII طفل | 🟡 | التنقيح المركزي قوي (`lib/auditLog.ts` قائمة منع تعاودية تشمل `nickname/birth_month/birth_year`)، والإلغاء الإداري مُدقَّق. **لا تدقيق لجلسات التشغيل ولا للتنزيل** | `ENC-001` `PRIV-103` |
| 23 | اختبار downgrade/grace واختيار الأجهزة والملفات دون حذف تلقائي | 🔴 | لا مسار شراء عامل، ولا `plan_pricing`، ولا سيناريو downgrade قابل للتنفيذ (`plan_pricing`=0، `subscription_entitlements`=0) | `API-104` `DECIDE-102` |
| 24 | اختبارات Race للتسجيل والتشغيل والإلغاء والتنزيل على Staging | 🔴 | **المعيار بنصّه غير قابل للاستيفاء**: قرار المالك أن لا توجد بيئة staging (`§15`). البديل المقبول تنفيذها محليًّا مقابل `wrangler dev` مع Durable Objects حقيقية — كما تفعل `test/rateLimit.test.mjs` للحدود — أو توثيقها صراحةً كغير مُختبَرة | `ENC-001` (سباقات الترخيص) |
| 25 | عقود الناشرين تعتمد مصفوفة DRM وOffline وWatermark قبل نشر المحتوى المرخَّص | ✅ **غير منطبق بقرار مالك** | لا محتوى مرخَّص من طرف ثالث: كل المحتوى من إنتاج «مجرة»، والملكية مسجَّلة الآن في `content_rights` (248 صفًّا، الترحيل `0082`). `HUMAN-101` يبقى شرطًا **مسبقًا** لأول محتوى مرخَّص يُضاف | `HUMAN-101` (شرطي) |

**المحصلة عند كتابة الأودت: 2 مستوفى من 25 · 11 جزئي · 12 غير مستوفى.**

**المحصلة بعد الدفعات 1–14: 11 مستوفى · 9 جزئي · 5 غير مستوفى** (المعايير 2 و12 و13 و14 في الدفعة 13، والمعيار 3 في الدفعة 12، والمعيار 10 في الدفعة 14).

**المحصلة بعد الدفعات 1–12: 6 مستوفى · 12 جزئي · 7 غير مستوفى.** ما تحوّل: المعيار 5 و25 بقرار المالك (الدفعة 10)، والمعيار 8 بالدفعتين 8 و11 (لا بايت صريح على القرص لا في التنزيل ولا في التشغيل)، والمعيار 3 بالدفعة 12 (الحدود الأربعة كلها مفروضة خادميًّا وذريًّا)، و7 و9 و10 من 🔴 إلى 🟡 بالدفعات 1 و7. وكل صفّ يذكر ما بقي منه ولا يدّعي إغلاقًا كاملًا.

المستوفى بالكامل هو ما يخص مسار Online غير الفيديو: منع الروابط الدائمة وأسرار R2، وأعمار التوكنات وعدم كشفها. كل ما يخص **Offline والترخيص والفيديو** غير مستوفى، وكل ما يخص **الحدود والإبطال** جزئي لأنه ينقطع عند التنزيل.

---

## §5 الأمان غير التشفيري — `SEC-*`

### SEC-101 — استرداد الأموال ومنح الباقات المدفوعة بلا أي فحص صلاحية

**الأولوية:** P0 · **المنطقة:** API / الأمان / الفوترة · **الحالة:** MISSING · **مكشوف باختبار فاشل**

**المشكلة**
`adminBilling.ts` يفرض `requireAdmin` على كل مساراته، لكن أخطر مسارَين فيه بلا `requirePermission`. أي حساب لوحة مُصادَق — بأي دور، حتى `viewer` أو مراجع لغوي — يستطيع منح باقة `family_plus` لسنة كاملة أو إنشاء صف استرداد.

**الدليل**
- `dashboard/api/src/routes/adminBilling.ts:17` — `route.use('*', requireAdmin)` وحده.
- `:68` — `route.post('/billing/refunds', async (c) => {` بلا حرس صلاحية.
- `:98` — `route.post('/billing/grant', async (c) => {` بلا حرس صلاحية؛ و`:107-110` يُنشئ استحقاقًا مدته 365 يومًا.
- `dashboard/api/test/routeGuards.test.mjs:151` **فاشل الآن**: `['adminBilling.ts POST /billing/refunds', 'adminBilling.ts POST /billing/grant']`.
- للمقارنة، نفس الملف يحرس مسارات أخف خطرًا: `adminTeams.ts:177` يفرض `manage_permissions` على `POST /grants`.

**السلوك الحالي**
منح استحقاق مدفوع وإنشاء استرداد عمليتان ماليتان بلا تفويض ولا فصل مهام. والأسوأ أن الاختبار المخصص لمنع هذا موجود ويفشل، أي أن الحماية سقطت بعد كتابتها.

**السلوك المتوقع**
`requirePermission('manage_billing')` أو ما يعادلها على كلا المسارين، مع سجل تدقيق يذكر الفاعل، ومع مبدأ فصل المهام (من ينشئ الاسترداد لا يعتمده).

**نطاق التنفيذ**
إضافة الحرس؛ تعريف الصلاحية في `permissions` وربطها بالأدوار؛ كتابة صف `audit_logs` لكل منح واسترداد؛ إبقاء `routeGuards.test.mjs` أخضر كشبكة انحدار.

**الحجم** S

**معايير القبول**
- [ ] `POST /admin/billing/grant` بحساب بلا صلاحية الفوترة يُعيد 403 ولا يكتب شيئًا.
- [ ] `POST /admin/billing/refunds` كذلك.
- [ ] كل عملية ناجحة تُنتج صف تدقيق باسم الفاعل المُصادَق.
- [ ] `routeGuards.test.mjs` أخضر، ويفشل إن أُزيل أي حرس.

**لا تفعل**
لا تكتفِ بإضافة الحرس على `grant` وحده لأنه «الأخطر»؛ الاسترداد يمسّ المال أيضًا.

---

### SEC-102 — لا حساب مسؤول واحد في قاعدة البيانات، فالمفتاح المشترك هو الباب الوحيد

**الأولوية:** P0 · **المنطقة:** الأمان / العمليات · **الحالة:** PARTIAL

**المشكلة**
`admin_users` = **0 صفًا** في D1 المحلية. تصميم الحرس يقبل `ADMIN_API_KEY` **فقط قبل بذر أول مستخدم**، ما يعني أن النظام اليوم في الحالة التي يكون فيها المفتاح المشترك مقبولًا: هوية واحدة للفريق كله، وسجل تدقيق يسجّل `legacy-admin-key` بدل شخص.

**الدليل**
- استعلام D1: `SELECT COUNT(*) FROM admin_users` = 0.
- `dashboard/api/src/lib/adminAuth.ts:76-95` — إن لم يوجد مستخدمون يُقبل المفتاح؛ وإن لم يوجد مفتاح ولا مستخدمون يمرّ الطلب في `development` بلا أي تحقق (`:97-104`).
- `dashboard/api/src/lib/adminAuth.ts:230-234` — `auditActor` يعيد `'legacy-admin-key'` في هذه الحالة.
- `dashboard/api/.secrets.local.txt` يحتوي زوج `ADMIN_API_KEY` للإنتاج (القيم غير معروضة هنا).

**السلوك الحالي**
لا يمكن الدخول إلى اللوحة بحساب شخصي، ولا سحب وصول فرد، ولا تمييز فاعل في التدقيق. وأي تشغيل محلي بلا مفتاح يفتح كل مسارات الإدارة.

**السلوك المتوقع**
بذر حساب مالك واحد على الأقل، ثم إبطال `ADMIN_API_KEY` من أسرار الإنتاج نهائيًا، وإجبار تغيير كلمة المرور أول دخول (`must_change_password` موجود في النموذج).

**نطاق التنفيذ**
سكربت/إجراء بذر موثَّق؛ حذف السرّ من الإنتاج بعد التأكيد؛ التحقق أن المسار `development` المفتوح لا يمكن الوصول إليه بأي إعداد إنتاج.

**التبعيات** OPS-102 (إدارة الأسرار).

**الحجم** S

**معايير القبول**
- [ ] يوجد حساب مالك واحد على الأقل في الإنتاج، ودخوله يعمل.
- [ ] بعد البذر، أي طلب بـ`ADMIN_API_KEY` يُرفض بـ401 — بدليل نداء فعلي.
- [ ] `audit_logs` لم يعد يحمل `legacy-admin-key` في أي صف جديد.

---

### ~~SEC-103~~ — منح الأدوار بلا مقارنة صلاحيات، و`granted_by` بلا قيد مرجعي مفروض

**الأولوية:** P1 · **المنطقة:** API / RBAC · **الحالة:** ✅ **مُنفَّذ في الدفعة الحادية والعشرين** (ستة مسارات، لا مسار واحد) — انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
`POST /admin/grants` محروس بـ`manage_permissions`، لكنه لا يقارن صلاحية المانح بالصلاحية الممنوحة: من يملك `manage_permissions` يستطيع منح نفسه أو غيره دور `owner`. وD1 لا يفرض المفاتيح الأجنبية افتراضيًا، فقيمة `granted_by` غير مضمونة أن تشير إلى مستخدم حقيقي.

**الدليل**
`dashboard/api/src/routes/adminTeams.ts:19` (التعليق نفسه: «من يصل إلى `POST /grants` يمنح نفسه الملكية»)، `:177-190` (الحرس موجود، والمقارنة غائبة)، و`:183-184` (تعليق يشرح أن `granted_by` كان نصًا حرفيًا وأن D1 لم يمنعه).

**السلوك المتوقع**
منع منح دور أو صلاحية لا يملكها المانح؛ وقيد مرجعي مُفعَّل أو تحقق تطبيقي على `granted_by`؛ وسجل تدقيق لكل منح وسحب.

**الحجم** M

**معايير القبول**
- [ ] حامل `manage_permissions` بلا دور `owner` لا يستطيع منح `owner`.
- [ ] منح دور يحمل صلاحية لا يملكها المانح يُرفض بـ403.
- [ ] `granted_by` يُرفض إن لم يطابق مستخدمًا قائمًا.

---

### SEC-104 — حدّ معدّل الدخول يرفض من المحاولة الثالثة بينما يعلن حدًّا من خمس

**الأولوية:** P1 · **المنطقة:** API / الأمان / تجربة المستخدم · **الحالة:** REGRESSION · **مكشوف باختبار فاشل**

**المشكلة**
`strictAuthLimit` معرَّف بـ`max: 5` لكل دقيقة، لكن ست محاولات دخول متتالية أنتجت **أربع** استجابات 429 بدل واحدة، أي أن المحاولتين الأوليين فقط تمرّان.

**الدليل**
`dashboard/api/src/lib/rateLimit.ts:166` (`max: 5`) مقابل `dashboard/api/test/rateLimit.test.mjs:212-235` الفاشل: `Expected values to be strictly equal: 4 !== 1`. الترويسة `X-RateLimit-Limit` ما زالت تعلن `5`.

**السلوك الحالي**
ولي أمر يخطئ كلمة المرور مرتين يُحجب دقيقة كاملة، والرسالة تخبره أن له خمس محاولات. السبب الجذري غير محدَّد بعد: إمّا استهلاك أكثر من وحدة لكل طلب، أو تطابق أكثر من middleware على المسار.

**نطاق التنفيذ**
تشخيص عدد الزيادات لكل طلب (سجل مؤقت داخل `RateLimiter`)؛ إصلاح السبب؛ إبقاء الاختبار أخضر؛ التأكد أن الترويسة والجسم يتفقان مع السلوك الفعلي.

**الحجم** S

**معايير القبول**
- [ ] خمس محاولات تمرّ والسادسة تُرفض، بدليل تشغيل الاختبار.
- [ ] `X-RateLimit-Remaining` ينقص بمقدار 1 لكل طلب.
- [ ] لا مسار آخر يتأثر بنفس السبب الجذري (فحص باقي الحدود).

---

### ~~SEC-105~~ — لا Certificate Pinning في التطبيق

**الأولوية:** P1 · **المنطقة:** التطبيق / الأمان · **الحالة:** ✅ **مُنفَّذ في الدفعة الخامسة** (يبقى فحص اعتراض يدوي) — انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
كل الاتصالات تعتمد على مخزن شهادات النظام. على جهاز بشهادة جذر مثبَّتة (شائع في أدوات التحليل وفي أجهزة مُدارة)، يمكن اعتراض التوكنات ومحتوى الاستجابات وفكّها.

**الدليل**
لا `badCertificateCallback` ولا `SecurityContext` ولا `HttpClient` مخصص في أي ملف تحت `app_main/lib/`؛ العميل الوحيد هو `http.Client()` الافتراضي (`majarra_api_client.dart:82` وما بعده).

**السلوك المتوقع**
تثبيت مفتاح عام (SPKI pinning) لمضيف الـAPI مع مفتاح احتياطي واحد على الأقل وخطة دوران موثَّقة، وفشل مغلق عند عدم التطابق.

**نطاق التنفيذ**
تثبيت على مستوى `HttpClient` أو قناة native؛ مفتاحان (حالي واحتياطي)؛ توثيق إجراء الدوران؛ سلوك واضح عند الفشل لا يقفل المستخدم أبديًا.

**الحجم** M

**معايير القبول**
- [ ] اعتراض بشهادة جذر مثبَّتة يدويًا يفشل الاتصال بدل تمريره.
- [ ] دوران شهادة الخادم المخطَّط لا يعطّل التطبيق (المفتاح الاحتياطي يعمل).
- [ ] الفشل يُعرض كخطأ شبكة مفهوم لا كتعليق أبدي.

**لا تفعل**
لا تثبّت شهادة ورقية واحدة بلا احتياطي؛ ذلك يحوّل تجديدًا روتينيًا إلى انقطاع كامل.

---

### ~~SEC-106~~ — رمز جلسة اللوحة في `localStorage`، ويُنسَخ تلقائيًا بين المخزنين

**الأولوية:** P1 · **المنطقة:** اللوحة / الأمان · **الحالة:** ✅ **تنازل موثَّق بتعويضات مُنفَّذة (الدفعة السادسة)** — `HttpOnly` غير ممكن بالاستضافة الحالية؛ القرار المتبقّي `DECIDE-109`. انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
رمز الجلسة الإدارية يُحفَظ في `localStorage` و`sessionStorage` معًا، والقراءة تنسخه تلقائيًا من أحدهما للآخر. أي ثغرة XSS في اللوحة تقرأه، وهو يمنح صلاحيات إدارية كاملة.

**الدليل**
`dashboard/front/src/lib/adminSession.ts:26-27` (المفاتيح)، `:41-70` (`readFromStorages` مع مزامنة تلقائية في الاتجاهين)، والتعليق `:19-24` يشرح أن السبب تجربة استخدام (تبويب جديد يطلب تسجيل دخول).

**السلوك المتوقع**
كوكي `HttpOnly; Secure; SameSite` يديره الخادم للجلسة الإدارية، أو على الأقل تقليص العمر بشدة مع تجديد صامت وترويسات أمان تمنع XSS (SEC-108).

**نطاق التنفيذ**
تحويل جلسة اللوحة إلى كوكي `HttpOnly` مع حماية CSRF مناسبة؛ أو توثيق قبول الخطر صراحةً مع تعويضات (CSP صارمة + عمر قصير + إبطال فوري).

**التبعيات** SEC-108.

**الحجم** M

**معايير القبول**
- [ ] الرمز غير قابل للقراءة من JavaScript، أو قرار مقبول موثَّق مع تعويضاته.
- [ ] فتح تبويب جديد لا يطلب تسجيل دخول (لا انحدار في التجربة).
- [ ] تسجيل الخروج يُبطل الجلسة خادميًا لا محليًا فقط.

---

### ~~SEC-107~~ — لا كشف Root/Jailbreak ولا سياسة تخفيف

**الأولوية:** P2 · **المنطقة:** التطبيق / الأمان · **الحالة:** ✅ **مُنفَّذ في الدفعة السادسة عشرة** — انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
`تشفير المحتوي.md:22` يطلب مؤشرات Root/Jailbreak/Debugger/Emulator/Hooking مع سياسة تخفيف (offline فقط، أو تقليل مدة الترخيص). لا شيء منها موجود، ولا حزمة ولا قناة native.

**الدليل** لا نتائج لأي من هذه المفاهيم في `app_main/lib/` ولا في `android/`/`ios/`.

**السلوك المتوقع**
إشارات إرشادية (لا حاجز مطلق) تُستخدم لتقليل مدة الترخيص أو تعطيل التنزيل على الأجهزة عالية الخطورة، مع تسجيل الحدث وبلا حجب تلقائي شامل.

**التبعيات** ENC-001 (لا معنى لتقليل مدة ترخيص غير موجود).

**الحجم** M

**معايير القبول**
- [ ] الإشارة تُرصد وتُسجَّل بلا PII.
- [ ] السياسة المطبَّقة موثَّقة ولا تحجب كل الأجهزة تلقائيًا.
- [ ] نتيجة إيجابية خاطئة لا تمنع المشاهدة Online.

---

### ~~SEC-108~~ — صفحات HTML العامة تُخدَم بلا أي ترويسة أمان

**الأولوية:** P1 · **المنطقة:** API / الموقع العام · **الحالة:** ✅ **مُنفَّذ في الدفعة السادسة** — انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
عارض الصفحات العامة يُصدر مستندًا كاملًا بلا `Content-Security-Policy` ولا `X-Frame-Options` ولا `Strict-Transport-Security` ولا `X-Content-Type-Options`.

**الدليل**
`dashboard/api/src/routes/publicRender.ts:208-214` — الترويسات هي `Content-Type` و`Cache-Control` فقط. المقابل الصحيح موجود في المستودع: `routes/creations.ts:252-253` يضبط `nosniff` و`default-src 'none'; sandbox`، و`routes/media.ts` يضبط `no-store` و`nosniff` و`no-referrer`.

**السلوك المتوقع**
CSP مناسبة للصفحة المُخدَّمة، ومنع التأطير، وHSTS على النطاق، وnosniff، وسياسة إحالة.

**الحجم** S

**معايير القبول**
- [ ] كل استجابة HTML عامة تحمل CSP وX-Frame-Options وnosniff وReferrer-Policy.
- [ ] HSTS مضبوط على النطاق (أو موثَّق أنه على مستوى Cloudflare).
- [ ] اختبار يفشل إن سقطت أي ترويسة.

---

### ~~SEC-109~~ — التوكنات بصيغة مخصصة بلا معرّف مفتاح، فلا يمكن تدوير السرّ بلا إخراج كل المستخدمين

**الأولوية:** P2 · **المنطقة:** API / الأمان · **الحالة:** ✅ **مُنفَّذ في الدفعة السابعة عشرة** — انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
`createSignedToken` ينتج `base64url(payload).base64url(HMAC-SHA256)` بلا رأس ولا `kid`. التحقق سليم ومقارنته بزمن ثابت، لكن غياب معرّف المفتاح يعني أن تدوير `AUTH_TOKEN_SECRET` يُبطل فورًا كل التوكنات القائمة: كل ولي أمر يُخرَج من جلسته، وكل توكن وسائط جاري يفشل.

**الدليل**
`dashboard/api/src/lib/security.ts:147-153` (الإصدار) و`:154-160` (التحقق) — لا `kid` ولا `alg` ولا دعم مفتاحين متزامنين. مقابل ذلك، خطة التشفير تفرض `signatureKeyId` ودوران آمن للتراخيص (`تشفير المحتوي.md:23`).

**السلوك المتوقع**
حقل `kid` في الحمولة، وقبول مفتاحين (الحالي والسابق) أثناء نافذة الدوران، وإجراء دوران موثَّق.

**الحجم** S

**معايير القبول**
- [ ] تدوير السرّ لا يُخرج أي مستخدم أثناء نافذة الدوران.
- [ ] توكن موقَّع بمفتاح مسحوب يُرفض.
- [ ] الإجراء موثَّق في `SETUP_SECRETS.md`.

---

### SEC-110 — لا تحقق مخطَّطي مركزي للمدخلات على مسارات العميل

**الأولوية:** P1 · **المنطقة:** API / سلامة البيانات · **الحالة:** ✅ **مُنفَّذ في الدفعتين الثامنة عشرة والثالثة والعشرين** — الطبقة وثلاثة عشر موجّه عميل، والقائمة المُدرَجة فارغة. انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
508 نقطة نهاية تتحقق من المدخلات يدويًا داخل كل معالج. لا `zod` ولا أي مُصادِق مشترك لأجسام الطلبات (الموجود `lib/jsonSchema.ts` مخصص لحزم الألعاب لا لأجسام الـAPI).

**الدليل**
البحث عن `from 'zod'` في `dashboard/api/src/**/*.ts` = صفر نتائج. أمثلة على التحقق اليدوي المتفرّق: `routes/childSettings.ts:51-72`، `routes/adminBilling.ts:70-74`، `routes/adminTeams.ts:180`.

**السلوك الحالي**
التحقق يعتمد على انتباه كاتب كل معالج. الفرق مرئي في الكود نفسه: بعض المسارات تفحص المدى والنوع بدقة، وأخرى تكتفي بوجود الحقل.

**السلوك المتوقع**
مُصادِق مشترك يعلن مخطط كل جسم طلب، مع رفض الحقول غير المعروفة، ورسائل خطأ موحّدة.

**نطاق التنفيذ**
اختيار مُصادِق خفيف يناسب حجم Worker؛ تطبيقه تدريجيًا على مسارات العميل أولًا (auth, account, family, child-settings, creations, analytics) ثم الإدارة؛ اختبار عقد لكل مخطط.

**الحجم** L

**معايير القبول**
- [ ] كل مسار كتابة على مسار العميل يعلن مخططًا ويرفض الحقول غير المعروفة.
- [ ] رسائل الأخطاء موحّدة الشكل ولا تكشف بنية داخلية.
- [ ] اختبار يفشل إن أُضيف مسار كتابة بلا مخطط.

---

### SEC-111 — توكن الوسائط ما زال مقبولًا في سلسلة الاستعلام

**الأولوية:** ~~P1~~ → **P2** · **المنطقة:** API / الأمان / الوسائط · **الحالة:** تنازل موثَّق (`ACCEPTED-TRADEOFF`)

> **تصحيح على هذا البند (2026-08-26).** صيغته الأولى طلبت حصر القبول على الترويسة. المحاولة كشفت أن للفرع **مستهلكَين شرعيَّين** لا يستطيعان ضبط ترويسة لكل طلب:
> `routes/episodes.ts:525` يكتب روابط متغيّرات HLS داخل الـmaster playlist والمشغّل يجلبها بنفسه، و`app_main/lib/features/games/engine/media_audio_player.dart` + `game_route.dart:113` يبنيان الرابط لمشغّل يقبل رابطًا فقط (`GameAudioPlayer` لا يقبل ترويسات).
> إسقاط الفرع كان سيكسر تشغيل HLS وصوت الألعاب صامتًا. المُنفَّذ: توثيق التنازل داخل `media.ts` مع مخفّفاته، وبقاء بند مفتوح واحد هو توسيع `GameAudioPlayer` لقبول ترويسات حتى يبقى الفرع لـHLS وحده.

**المشكلة**
مسار خدمة الأصول يقبل القدرة في ترويسة `Authorization` **أو** في `?token=`. سلسلة الاستعلام هي أسوأ مكان لسرّ: تُسجَّل في سجلات الوسيط وسجلات CDN وتاريخ المتصفح وترويسة `Referer`.

**الدليل**
`dashboard/api/src/routes/media.ts:19` — `const authHeader = c.req.header('Authorization') ?? (c.req.query('token') ? `Bearer ${c.req.query('token')}` : undefined);`

وهذا يخالف نصًّا صريحًا في خطة التشفير: `تشفير المحتوي.md:5` («لا يتم تسجيل الرابط كاملًا في Logs… لا يرسل إلى خدمات Analytics… لا تتم مشاركته في رسائل الأخطاء») والمعيار 6 في §26.

**السياق العادل**
العميل نفسه يفعل الصواب: يرسل القدرة في ترويسة دائمًا (`audio_player_page.dart:247-253` بتعليق يشرح ذلك، و`playback_page.dart:614-620`). أي أن المسار الخطر مفتوح على الخادم بلا مستخدم شرعي له.

**السلوك المتوقع**
حصر القبول على الترويسة. إن كان المسار موجودًا لحالة لا يمكنها ضبط ترويسة (مثل `<video src>` مباشر)، فيُوثَّق الاستخدام ويُقصَّر عمر التوكن لثوانٍ ويُمنع تسجيل سلسلة الاستعلام على الحافة.

**الحجم** XS

**معايير القبول**
- [ ] `GET /media/assets/:id?token=…` يُرفض بـ401.
- [ ] اختبار يمنع عودة مسار الاستعلام.
- [ ] لا سلسلة استعلام تحتوي توكنًا في أي سجل، بفحص فعلي.

---

### 5.5 ما ثبت أنه مُصلَح — لا تعيدوا فتحه

| الادّعاء السابق | الحالة المُتحقَّق منها |
|---|---|
| `POST /recommendations/admin` بلا مصادقة (`SEC-001` القديم) | **مُصلَح.** نُقل إلى `routes/adminRecommendations.ts` ويُركَّب قبل `adminRoute`؛ والتعليق في `routes/recommendations.ts:79-87` يوثّق السبب وأن الموجّه العام كان خارج مسح الحرَس |
| HLS يمنح توكنات بلا استحقاق (`SEC-002` القديم) | **مُصلَح.** `routes/episodes.ts:441-470` يشترط lease قائمًا، ويعيد التحقق عبر heartbeat، ويفحص الإقليم (451)، وأزال fallback الأصل الأساسي (`:493`) |
| ingest التحليلات يقبل معرّفات مزوَّرة بلا حصة (`SEC-003` القديم) | **مُصلَح.** `routes/analyticsIngest.ts:107-125` يشتق الهوية من الجلسة، و`ownsChild` يتحقق من الملكية عبر الـDO، وقائمة سماح للمفاتيح بدل regex، والحصة مُسجَّلة في `index.ts:90` |
| `X-Admin-Actor` مصدر هوية التدقيق | **مُصلَح.** `lib/adminAuth.ts:230-234` يستخدم هوية الجلسة المُصادَقة أولًا |
| `requirePermission` يفشل مفتوحًا ويتجاهل النطاق | **مُصلَح.** `lib/adminAuth.ts:196-222` يفشل مغلقًا ويبني نطاق المورد من المسار والاستعلام والجسم |

---

## §6 الباكند وقاعدة البيانات — `API-*` و `DB-*`

### API-101 — الرقابة الأبوية تُكتب ولا تُقرأ: كل حدود الوقت زينة

**الأولوية:** P0 · **المنطقة:** API / الرقابة الأبوية · **الحالة:** **DONE للفيديو** (2026-08-26)

> **مُنفَّذ على مسار الفيديو.** وقت النوم والحدّ اليومي وحدّ الجلسة تُفرض الآن داخل `FamilyState` مع احتساب ذرّي في `screen_time_daily`، وعمر عقد التشغيل نفسه محدود بالرصيد المتبقي. 13 اختبارًا في `test/parentalControls.test.mjs`. التفصيل والقرارات الأربعة في `§15` (الدفعة الثالثة).
>
> **ما يبقى:** الصوت والقراءة بلا فرض — سؤال منتج لا هندسة (`DECIDE-108`): هل «حكاية قبل النوم» ممنوعة في وقت النوم أم أنها المقصودة، وهل يستهلك الاستماع نفس رصيد وقت الشاشة؟ وإسقاط `child_screen_time_daily` في D1 للتقارير الإدارية تحت `API-105`.
>
> **تغيير سلوك:** الصف الغائب في `child_settings` يُطبَّق عليه افتراضي المخطط (30 دقيقة يوميًّا) لأنه ما تعرضه شاشة الإعدادات نفسها. النص أدناه يبقى كسجل لما كان.

**المشكلة**
ولي الأمر يضبط `daily_minutes` و`max_session_minutes` و`bedtime_start/end`، وتُحفَظ بتحقق دقيق من المدى والصيغة، ثم **لا يقرؤها أي مسار تشغيل**. وجدول التتبّع اليومي `child_screen_time_daily` موجود بلا كاتب واحد.

**الدليل**
- الكتابة: `dashboard/api/src/routes/childSettings.ts:44-76` (تحقق `5-180` دقيقة، وصيغة `HH:MM`، وكتابة `UPDATE child_settings`).
- **القراءة:** البحث عن `child_settings` و`max_session_minutes` في `dashboard/api/src/**/*.ts` لا يعطي أي نتيجة خارج `childSettings.ts` نفسه. لا `episodes.ts` ولا `books.ts` ولا `stories.ts` ولا الـDO يقرأها.
- `INSERT INTO child_screen_time_daily` = صفر نتائج في المستودع؛ والجدول 0 صفًا في D1.

**السلوك الحالي**
شاشة إعدادات تُعطي ولي الأمر إحساسًا بالتحكم لا يقابله أي فرض. الطفل يشاهد بلا حدّ يومي وبلا حدّ للجلسة وفي وقت النوم.

**السلوك المتوقع**
قرار التشغيل (منح lease أو audio session) يقرأ إعدادات الطفل ويرفض بسبب واضح عند تجاوز الحد اليومي أو حدّ الجلسة أو دخول نافذة النوم؛ والاستهلاك اليومي يُحتسب ويُكتب؛ والتطبيق يعرض السبب لا خطأً عامًا.

**نطاق التنفيذ**
نقل الإعدادات إلى سلطة الـDO أو قراءتها من D1 قبل منح الجلسة؛ احتساب الاستهلاك عبر heartbeat؛ كتابة `child_screen_time_daily`؛ رموز رفض مخصصة؛ شاشة «انتهى وقتك اليوم» في التطبيق.

**التبعيات** التطبيق (شاشة الرفض)، ENC-010 (نفس نقطة إعادة التحقق).

**الحجم** L

**معايير القبول**
- [ ] تجاوز الحد اليومي يمنع منح جلسة جديدة برمز رفض مخصص.
- [ ] الدخول في نافذة وقت النوم يمنع التشغيل ويُعرض السبب للطفل بلغة مناسبة.
- [ ] الاستهلاك يُحتسب من heartbeat لا من تقرير العميل.
- [ ] اختبارات تغطّي الحالات الثلاث.

**لا تفعل**
لا تُفرض الحدود في Flutter؛ عميل معدَّل يتجاوزها، والأهم أن الإعداد وعدٌ لولي الأمر لا تلميح واجهة.

---

### ~~API-102~~ — حدود الباقات لها مصدرا حقيقة

**الأولوية:** P1 · **المنطقة:** API / DB · **الحالة:** ✅ **مُنفَّذ في الدفعة الثانية والعشرين** (الكود هو المصدر، والجدول أُسقط في `0085`) — انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
الحدود مكتوبة في الكود **وفي** جدول D1، والمفروض فعلًا هو الكود. تغيير الجدول لا يغيّر شيئًا، وهو فخ صامت لأي عملية تسويق أو باقة جديدة.

**الدليل**
`dashboard/api/src/lib/familyPolicy.ts:10-12` (`free/family/family_plus` بأرقامها) مقابل جدول `subscription_plan_limits` المزروع في `migrations/0006_parent_auth_entitlements.sql:18-34`. الجدول غير مذكور في قائمة الجداول الميتة في `0010`، أي أنه يبدو حيًّا وهو ليس كذلك.

**السلوك المتوقع**
مصدر واحد. الأبسط: الجدول هو المصدر مع `policy_version`، والكود يقرأه ويحتفظ به في الذاكرة بعمر قصير؛ أو الاعتراف صراحةً بأن الكود هو المصدر وإسقاط الجدول في ترحيل.

**الحجم** M

**معايير القبول**
- [ ] تغيير حدّ في المصدر الواحد يظهر أثره في رفض فعلي بدون نشر كود (إن كان الجدول هو المصدر).
- [ ] لا يبقى تعريفان للأرقام في المستودع.
- [ ] `policy_version` يُسجَّل مع كل قرار حدّ في التدقيق.

---

### API-103 — لا إقران أجهزة ولا اعتماد TV في كل الـAPI

**الأولوية:** P1 · **المنطقة:** API / الأجهزة / TV · **الحالة:** MISSING

**المشكلة**
إدارة الأجهزة تعرض وتسحب فقط. لا مسار لتسجيل جهاز TV أو إقرانه برمز QR، مع أن التطبيق يحتوي شاشة إقران والخطة تحتسب TV جهازًا مستقلًا.

**الدليل**
- `dashboard/api/src/lib/parentAuth.ts:57-60` — نص صريح: الغرض `approve_tv` أُزيل «لأنه لا يوجد أي endpoint لاعتماد TV أو إقران جهاز في كل الـAPI».
- `app_main/lib/features/tv/presentation/tv_pairing_page.dart:12-16` — صفحة بلا كود عمل.
- `تشفير المحتوي.md:15` و`:17` يفرضان احتساب TV جهازًا وCast/AirPlay تشغيلًا واحدًا.

**السلوك المتوقع**
`POST /devices/pairing-codes` على الجهاز الكبير، و`POST /devices/pair` من الهاتف خلف PIN/biometric، مع احتساب الجهاز ضمن حدّ الأجهزة، وسلوك موثَّق لـCast/AirPlay.

**التبعيات** API-101 (نفس مسار قرار الجلسة)، ENC-001 (حدّ أجهزة التنزيل).

**الحجم** L

**معايير القبول**
- [ ] إقران TV يُنشئ جهازًا مسجَّلًا يُحتسب في الحدّ ويظهر في قائمة الأجهزة.
- [ ] الإقران يتطلّب إثبات ولي أمر ولا يقبل كلمة مرور طفل.
- [ ] Cast/AirPlay من جهاز مسجَّل يُحتسب تشغيلًا واحدًا ولا يسجّل المستقبل جهازًا.

---

### API-104 — لا مسار شراء عامل: باقات بلا أسعار، وتحقق بلا عميل، وdowngrade غير قابل للاختبار

**الأولوية:** P0 · **المنطقة:** API / التطبيق / الفوترة · **الحالة:** PARTIAL

**المشكلة**
لا يمكن لأي عائلة أن تشتري أي شيء. نقاط Google Play موجودة وبلا عميل ينادِيها، ولا Apple IAP إطلاقًا، و`plan_pricing` صفر صفًا، و`store_products` ثلاثة منتجات بلا أسعار، و`subscription_entitlements` صفر.

**الدليل**
- `dashboard/api/src/lib/parentAuth.ts:50-56` — نص صريح أن `google-play/context` و`verify` بلا عميل، وأنهما ضمن `BILLING-001` المعلَّق على قرار مالك.
- استعلام D1: `plan_pricing` = 0، `store_products` = 3، `subscription_entitlements` = 0، `google_play_purchases` = 0.
- التطبيق يعرض واجهة الباقات (`membership_page.dart`، 1416 سطرًا) ويقرأ `getBillingCatalog`.
- `تشفير المحتوي.md` القرار النهائي: «قبل إعداد هذا التحقق، لا يصدر أي entitlement مدفوع».

**السلوك الحالي**
ولي الأمر يصل إلى جدار دفع بلا منتج خلفه، والطفل يرى قفلًا لا يُفتح. وكل معايير القبول التي تخص الباقات والحدود وdowngrade/grace غير قابلة للاختبار أصلًا.

**السلوك المتوقع**
منتجات وأسعار مزروعة لكل دولة/عملة، وتحقق شراء عامل من طرف إلى طرف، واستحقاق يُصدر خادميًا فقط بعد التحقق، وسيناريو downgrade مع فترة سماح واختيار ما يُحتفظ به.

**التبعيات** DECIDE-102، EXT-102 (حساب Google Play Developer API)، EXT-103 (Apple).

**الحجم** XL

**معايير القبول**
- [ ] شراء حقيقي في بيئة اختبار يمنح استحقاقًا، ويُخزَّن بصمة SHA-256 للتوكن لا التوكن.
- [ ] إعادة إرسال نفس الشراء لا تمنح استحقاقًا مضاعفًا (idempotency).
- [ ] downgrade يستخدم فترة سماح ولا يحذف ملفات أطفال أو تنزيلات تلقائيًا.
- [ ] لا استحقاق مدفوع يُصدر من جسم طلب يرسله العميل.

---

### ~~API-105~~ — ثلاثة جداول تُقرأ ولا يكتبها شيء، فمزايا كاملة فارغة دائمًا

**الأولوية:** P1 · **المنطقة:** API / DB / المنتج · **الحالة:** ✅ **مُنفَّذ في الدفعة الرابعة والعشرين** (إسقاط تقدّم يكتبه الطابور، وجدولان مُسقَطان، ولا قارئ لجدول بلا كاتب) — انظر §15. يبقى بلوكا التوصيات مخفيَّين بقرار موثَّق. النصّ أدناه يبقى كما وُجد.

**المشكلة**
`children_profiles` و`watch_progress` و`child_screen_time_daily` كلها 0 صفًا، وبحث المستودع لا يجد لها أي `INSERT`. سلطة الحقيقة في الـDurable Object، والإسقاط إلى D1 لم يُكتب.

**الدليل**
- `INSERT INTO children_profiles` / `INSERT INTO watch_progress` / `INSERT INTO child_screen_time_daily` = صفر نتائج في `dashboard/api/src/**/*.ts`.
- `dashboard/api/src/routes/analyticsIngest.ts:99-104` — تعليق صريح: «the D1 `children_profiles` table it is projected into has no writer yet».
- المقابل الموجود: `home_recommendations` له كاتب واحد (`adminRecommendations.ts:141-144`) وفيه 3 صفوف — وهو تحريري لا سلوكي.

**الأثر**
«تابع المشاهدة» و«الأكثر مشاهدة» والتوصيات السلوكية والتقارير الأسبوعية لولي الأمر وشاشات اللوحة التي تقرأ هذه الجداول ستبقى فارغة في الإنتاج بصرف النظر عن جودة الكود. وهذا هو سبب إخفاء بلوكات كاملة في التطبيق (`home_feed.dart:1573-1575`، `feed_blocks.dart:261-264`).

**السلوك المتوقع**
مستهلك طابور أحداث العائلة يكتب الإسقاطات، أو الشاشات تقرأ من الـDO عبر واجهة مخصّصة، مع قرار صريح أيّ الاتجاهين.

**الحجم** L

**معايير القبول**
- [ ] إكمال طفل لحلقة يُنتج صفًا في إسقاط التقدّم خلال ثوانٍ.
- [ ] «تابع المشاهدة» يعرض عنصرًا حقيقيًا في بيئة اختبار.
- [ ] لا شاشة تقرأ جدولًا بلا كاتب؛ اختبار يمنع عودة الحالة.

---

### ~~API-106~~ — نشر السلاسل والحلقات يتخطّى بوابة الجهوزية عند تعذّر تقييمها

**الأولوية:** P1 · **المنطقة:** API / النشر · **الحالة:** ✅ **مُنفَّذ في الدفعة الخامسة والعشرين** (الأنواع الستّة تستدعي `publishEntity` واحدة تفشل مغلقًا) — انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
موجّه النشر الجديد للقصص والكتب والألعاب والمشروعات **يفشل مغلقًا** بشكل صحيح، لكن معالجَي السلاسل والحلقات الأقدم يعتبران نتيجة البوابة الفارغة «غير مُقيَّم» وينشران على أي حال.

**الدليل**
`dashboard/api/src/routes/adminPublish.ts:81-88` — التعليق يقول ذلك حرفيًا ويسجّله كمتابعة لم تُنفَّذ: «those treat a null gate result as `'not evaluated'` and publish anyway… The two older handlers should be aligned to this».

**الأثر المُقاس**
20 حلقة منشورة اليوم، **بلا أصل فيديو واحد** وبمدة زمنية فارغة (`§1.3`). أي أن المسار الذي نُشرت به لم يمنع نشر محتوى غير قابل للتشغيل.

**السلوك المتوقع**
مسار نشر واحد لكل الأنواع الستة، يفشل مغلقًا، ويسجّل `publish_blocked` مع أسباب المنع.

**الحجم** M

**معايير القبول**
- [ ] نشر حلقة بلا أصل فيديو جاهز يُرفض بـ409 مع سبب.
- [ ] المسارات الستة تستخدم نفس دالة النشر والبوابة.
- [ ] كل رفض ونشر يُسجَّل في `audit_logs` مع تحذيرات الجهوزية.

---

### API-107 — صفحات الكتب مخزَّنة كعمود JSON، فلا ربط أصول ولا قياس جهوزية

**الأولوية:** P2 · **المنطقة:** DB / المحتوى · **الحالة:** PARTIAL

**المشكلة**
لا جدول `book_pages`. الصفحات نص JSON داخل `books.pages`، بينما القصص لها جدول صفحات كامل مع ربط أصول وتوطين وتوقيتات.

**الدليل**
`PRAGMA table_info(books)` يُظهر `pages TEXT NOT NULL DEFAULT '[]'`؛ ولا جدول `book_pages` بين 150 جدولًا. المقابل للقصص: `story_pages` + `story_page_localizations` مع `image_asset_id` و`narration_asset_id`.

**الأثر**
لا يمكن ربط صورة أو سرد بصفحة كتاب، ولا استعلام «كم كتابًا صفحاته بلا رسوم»، ولا تشغيل بوابة الجهوزية على الكتب بمعنى حقيقي — و22 كتابًا كلها `draft`.

**التشخيص كان معكوسًا (الدفعة 68)** الأثر المذكور **غير صحيح**: صفحات الكتب في `story_pages` بمفتاح `story_id = book.id`، بأصولها وتوطيناتها وسردها. و`GET /books/:id/pages` يقرأها ويحسب `translated_pages` و`narrated_pages` و`renderable` و`with_audio`. و`books.ts` يقول عن العمود صريحًا: «legacy JSON blob … deliberately not selected».

**والعطل الحقيقي أخطر: البوابة كانت تقرأ العمود الميت.** `adminPublishGate.ts` كان يقرأ `b.pages`، و`evaluateBook` يحكم على طوله، و`bookPublishError` على مسار الكتابة كذلك. والمقيس: العمود `'[]'` في **الاثنين والعشرين** كتابًا. فالحكم خاطئٌ في الاتجاهين — **لا كتاب يُنشَر** وإن كانت صفحاته كاملة، وكتابٌ تُكتب في عموده مصفوفةٌ يُنشَر و**لا صفحة له في المصدر الذي يقرأه التطبيق**.

**الحجم** M

**معايير القبول**
- [ ] جدول صفحات للكتب بربط أصول وتوطين، مع ترحيل ينقل الـJSON القائم.
- [ ] بوابة الجهوزية تستطيع الإجابة عن نقص الرسوم أو السرد لكل كتاب.

---

### API-108 — لا إشعارات دفع: صندوق داخلي واستقصاء فقط

**الأولوية:** P2 · **المنطقة:** API / التطبيق / التسويق · **الحالة:** MISSING

**المشكلة**
لا FCM ولا APNs. الحملات محدودة صراحةً بـ`in_app` و`website_banner` و`email`، والتطبيق يستقصي صندوق إشعارات.

**الدليل**
`dashboard/api/src/routes/adminCampaigns.ts:49` — رسالة الخطأ نفسها: «Only in_app, website_banner, email without FCM are supported». `notifications` = 0 صفًا في D1.

**الأثر**
لا تنبيه لولي الأمر عند تسجيل جهاز جديد (وهو مطلب في `تشفير المحتوي.md:17`)، ولا إشعار انتهاء ترخيص، ولا استرجاع مستخدمين.

**الحجم** L

**معايير القبول**
- [ ] تنبيه «جهاز جديد سُجِّل» يصل فعلًا إلى ولي الأمر.
- [ ] لا يُرسَل أي إشعار يحتوي رابطًا أو توكنًا للمحتوى.

---

### ~~DB-101~~ — سجل الترحيلات غير متسق: أرقام مكرَّرة وفجوات وصيغ فرعية وملف خارج المجلد

**الأولوية:** P1 · **المنطقة:** DB / العمليات · **الحالة:** ✅ **مُنفَّذ في الدفعة السادسة والعشرين** (`LEDGER.md` يحرسه اختبار، والملف الخارج حُذف). **وانقسم البند:** «البناء النظيف» انتقل إلى `DB-104` لأنه عطل مستقلّ مُتحقَّق منه. انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
الترقيم لم يعد يمثّل تسلسلًا موثوقًا، وهو أساس أي استرجاع أو مطابقة بين بيئتين.

**الدليل** (عدّ فعلي في `dashboard/api/migrations`)
- **رقمان مكرَّران:** `0018_content_class_and_project_links.sql` و`0018_site_mode.sql`؛ `0051_home_builder_resolved.sql` و`0051_identity_account_lifecycle.sql`.
- **ثلاث فجوات:** 0020، 0052، 0053 غير موجودة.
- **صيغ فرعية:** `0074`, `0074b`, `0074c`, `0074d`.
- 80 ملفًا بأعلى رقم 78، مقابل **81 صفًا** في `d1_migrations` محليًا.
- `migrations/catalog/0001_catalog_init.sql` خارج `migrations_dir` المُعلَن في `wrangler.jsonc:173-181`، فلا يطبّقه `wrangler d1 migrations apply` — ومع ذلك يُنشئ `episodes` و`series` مرة ثانية.

**الأثر**
لا يمكن الجواب بثقة عن «ما المُطبَّق على الإنتاج؟»، ولا إعادة بناء قاعدة من الصفر بترتيب معروف، ولا مقارنة بيئتين.

**السلوك المتوقع**
سجل خطّي واحد بلا تكرار ولا فجوات غير موثَّقة؛ ملف `catalog` إمّا يدخل السجل أو يُحذف؛ وتوثيق ما هو مُطبَّق على الإنتاج فعليًا.

**الحجم** M

**معايير القبول**
- [ ] لا رقم مكرَّر، وكل فجوة موثَّقة بسبب.
- [ ] إعادة بناء قاعدة نظيفة من الترحيلات وحدها تنجح وتُنتج نفس المخطط.
- [ ] `wrangler d1 migrations list` على الإنتاج موثَّق في المستودع.

**لا تفعل**
لا تُعِد ترقيم ترحيلات مُطبَّقة على الإنتاج؛ الحل يكون بترحيل جديد وتوثيق، لا بتحرير التاريخ.

---

### ~~DB-104~~ — بناء قاعدة نظيفة من الترحيلات يتوقّف عند `0074`

**الأولوية:** P1 · **المنطقة:** DB / العمليات · **الحالة:** ✅ **مُنفَّذ في الدفعة السابعة والعشرين** (٩٠/٩٠ ترحيلًا على قاعدة فارغة، ومهمّة `migrations` في CI تحرسه)

**المشكلة**
قاعدة محلية جديدة + `wrangler d1 migrations apply` تتوقّف عند
`0074_wave4_closure_36_games.sql` بـ`FOREIGN KEY constraint failed`. **٧١ ترحيلًا
مُطبَّقًا و١٨ معلَّقًا.**

أي أن **مخطَّط الإنتاج لا يمكن إعادة إنتاجه من المستودع**: لا بيئة اختبار تُبنى من
الصفر، ولا مقارنة بين بيئتين، ولا استرداد يبدأ من الترحيلات وحدها.

**الدليل** (مُتحقَّق منه في الدفعة 26)
نُقلت حالة D1 المحلية جانبًا وأُعيد التطبيق من الصفر. المخرَج:
`[ERROR] FOREIGN KEY constraint failed: SQLITE_CONSTRAINT_FOREIGNKEY`،
و`wrangler d1 migrations list --local` يُظهر 0074 أوّل المعلَّقات.

**السبب** (بالتنصيف)
الملف يُدرج ترجمةً لـ`game-trace-color-advanced-3` في البيان السابع، وصفّ اللعبة
نفسها في البيان الثامن. و`INSERT OR IGNORE` **لا يبتلع مخالفة مفتاح أجنبي**: خيار
`ON CONFLICT` في SQLite لا يشمل `FOREIGN KEY`. فالبيان يفشل ويسقط الملف كلّه.

**ما كتبه هذا البند أوّلًا وكان مُضِلًّا**
قيل هنا إن مفاتيح `game_localizations.game_id` الثمانية عشر «مُدرَجة داخل الملف»،
وهو صحيح **وغير كافٍ**: المهم موضعها لا وجودها. وذلك الفحص جرى على قاعدة مكتملة
كان الصفّ فيها موجودًا سلفًا، فأجاب عن سؤال آخر. الدرس: مفاتيح الترحيل تُفحَص على
**الحالة عند البيان**.

وأداة التنصيف نفسها كانت تكذب: كانت تُسقط كل قطعة تبدأ بتعليق — أي بيانها معه —
فأعلنت «بيانًا واحدًا» في ملفٍ فيه تسعة، وسمّت البيان الأخير السليم فاشلًا. ومعها
كان الاستعلام على Windows يُقتبَس يدويًّا ويمرّ بـ`cmd.exe` فيتشوّه كل استعلام فيه
سطر جديد. أُصلح العطلان (التعليق يُقشَّر سطرًا سطرًا، والاستعلام يُمرَّر بـ`--file`).

**ما نُفِّذ**
`0074` عُدِّل: أُسقط صفّ الترجمة المُبكِّر (وكانت حالته `draft` أصلًا)، فترجمة اللعبة
النهائية بحالة `ready` صارت البيان الأخير بعد إدراجها. **استثناءٌ مُعلَن** لقاعدة «لا
يُحرَّر ترحيل مُطبَّق»، وسببه أن انهيارًا في منتصف السلسلة **لا يصلحه ترحيل لاحق لأن
اللاحق لا يُشغَّل**. الاستثناء وشرطاه مكتوبان في `migrations/LEDGER.md`.

و`0087_db104_trace_localization_convergence.sql` يقارب البيئات القائمة. فالترجمة
كانت مكتوبة مرّتين بقيمتين — `0074` بمفتاح `game.trace.word.amal`، و`0074d`
بمفتاح `game.trace.free` — وكلتاهما `INSERT OR IGNORE`، فالفائز **من يصل أوّلًا**.
ولمّا كان `0074` يتوقّف، فازت قيمة `0074d` في كل بيئة قائمة. والصحيح مُحدَّد لا
مُختار: حزمة اللعبة لها ثلاثة مستويات بمفاتيح `letter.alif` و`letter.baa`
و`word.amal`، فـ`game.trace.free` مفتاح **لا يطلبه أي مستوى** (منقول عن
`game-shape-trace-3`)، ومفتاح المستوى الثالث **غائب** — أي نصّ مفقود يُعرَض للطفل.

**الحجم** M

**معايير القبول**
- [x] `wrangler d1 migrations apply` على قاعدة فارغة يطبّق **٩٠/٩٠** (مُتحقَّق منه
      ببناءَين نظيفَين: قبل `0087` وبعده).
- [x] مخطَّط القاعدة المبنية من الصفر قُوبل بمخطَّط القاعدة المُطبَّقة تدريجيًّا
      (`sqlite_master`، ٣٣٣ كائنًا مقابل ٣٣٠). الفرق الوحيد الجوهري **في القاعدة
      القائمة لا في المستودع**: تنقصها `family_deletion_watermarks` و
      `child_deletion_watermarks` وفهرسها — يُنشئها `0051_identity_account_lifecycle.sql`
      ويقرأها `familyEvents.ts` في ١٩ موضعًا. و`creative_drawings` يختلف نصّ
      تعريفه بالفراغات وحدها (متطابق بعد التطبيع). **مقابلة الإنتاج فعلٌ للمالك**
      (تحتاج بيانات اعتماد): `migration-ledger.mjs --remote`.
- [x] مهمّة `migrations` في `.github/workflows/ci.yml` تبني قاعدة من الصفر على كل
      دفعة، و`--require-applied` تُفشلها إن بقي ملفٌ معلَّق. ومضافة إلى `needs`
      بوابة النشر.

---

### DB-102 — جداول مُعلَنة ميتة ما زالت في المخطط، ووثيقة التشفير تبني عليها

**الأولوية:** P2 · **المنطقة:** DB / التوثيق · **الحالة:** DEAD-UNUSED

**المشكلة**
`migrations/0010_cleanup_dead_d1_tables.sql:1-5` يعلن سبعة جداول ميتة نُقلت ملكيتها إلى الـDurable Objects (`account_devices`، `parent_credentials`، `parent_auth_sessions`، `google_play_purchases`، `subscription_entitlements`، `playback_leases`، `used_refresh_tokens`) — وكلها ما زالت موجودة في المخطط بصفوف صفرية. وفي الوقت نفسه `تشفير المحتوي.md:19` يقدّم `account_devices` كنموذج البيانات المرجعي.

**الأثر**
مطوّر يقرأ الوثيقة يكتب استعلامًا على جدول لا يُكتب أبدًا، فيحصل على نتيجة فارغة بلا خطأ. هذا بالضبط ما يفسّر جداول «تُقرأ بلا كاتب» في `API-105`.

**الحجم** S

**والأثر لم يكن احتمالًا (الدفعة 65)** البند قال «مطوّر **سيكتب** استعلامًا على جدول لا يُكتب أبدًا فيحصل على نتيجة فارغة بلا خطأ». والمقيس: **قد كُتب فعلًا** — `account_devices` مقروءٌ في ثلاثة مسارات إدارية و`google_play_purchases` في واحد. فكانت اللوحة تعرض «صفر جهاز» لكل أسرة في قائمة العملاء، وقائمة أجهزة **المنصّة كلّها** فارغة بـ`total: 0`. والمقيس أيضًا: **صفر كاتب** لكل منهما في `src` و`scripts` و`migrations`.

**معايير القبول**
- [x] الجداول الميتة تُسقَط بترحيل، أو تُوسَم بتعليق مخطط لا يمكن تفويته. (الدفعة 65) **بحرس لا بتعليق**: الإسقاط في الإنتاج يمحو صفوفًا قد تكون من قبل انتقال الملكية ولا أرى حالة الإنتاج؛ والتعليق **يمكن تفويته** — والدليل أن `0010` نفسه تعليقٌ فُوِّت تسعًا وسبعين ترحيلة. فـ`test/deadTables.test.mjs` يُفشل أي استعلام D1 على السبعة، ويستثني `src/do/` لأن الأسماء هناك **مِلكٌ شرعي** لمخطَّط `FamilyState`، وقائمته مُشتقّة من سطر الإعلان في `0010` لا مكتوبة يدويًّا.
- [x] `تشفير المحتوي.md` يُحدَّث ليصف سلطة الـDO لا جداول D1 الميتة. (الدفعة 65) قسم `account_devices` صار يعلن أن سلطته الكائن الدائم، وأن قراءة D1 تُعيد فراغًا بلا خطأ، ومعه إشارة إلى الحرس. وأُصلح موضعٌ ثانٍ (سطر تسجيل المفتاح العام) كان يحيل إلى الجدول نفسه.

---

### DB-103 — المفاتيح الأجنبية غير مفروضة، والقيود المنطقية تعتمد على انتباه المعالج

**الأولوية:** P2 · **المنطقة:** DB / سلامة البيانات · **الحالة:** PARTIAL

**المشكلة**
D1/SQLite لا يفرض `FOREIGN KEY` افتراضيًا، والمخطط يعتمد عليها في مواضع كثيرة. المستودع نفسه يوثّق أثر ذلك: `granted_by` كان نصًا حرفيًا `'admin-api-key'` ولا مفتاح أجنبي منعه (`adminTeams.ts:183-184`).

**السلوك المتوقع**
تفعيل `PRAGMA foreign_keys` حيث يدعمه D1، أو تحقق تطبيقي صريح على كل مرجع حساس، مع اختبار سلامة مرجعية دوري.

**الحجم** M

**الفرضية نُقضت بالقياس (الدفعة 66)** «D1/SQLite لا يفرض `FOREIGN KEY` افتراضيًّا» **غير صحيح لـD1**. المقيس على القاعدة المحلية: `PRAGMA foreign_keys` = **1** · وإدراجُ مرجعٍ غير موجود يُرفض بـ`SQLITE_CONSTRAINT_FOREIGNKEY` · و`PRAGMA foreign_keys=OFF` ثمّ إدراج **رُفض أيضًا** (الإنفاذ غير قابل للتعطيل من SQL) · وجدولٌ يرجع إلى جدولٍ غير موجود يُرفض عند الإنشاء. ويؤكّده حادث `DB-104`: ترحيلةٌ **توقّفت** على انتهاك مفتاح أجنبي.

**والمثال المُستشهَد به يُقرأ الآن على وجهه** `granted_by TEXT REFERENCES admin_users(id)` — المفتاح **مُعلَن**. فمرورُ `'admin-api-key'` لم يكن لغياب الإنفاذ بل لأن العمود قابل للعدم والقيد لا يُفحَص على `NULL`. وقد أُصلح أصلًا في الدفعة 21 بـ`verifiedGrantedBy`، والتعليق الذي يشرحه كان يحمل الفرضية الخاطئة فصُحّح.

**معايير القبول**
- [x] كل مرجع حساس (grants، أصول، تراخيص) يُتحقَّق منه قبل الكتابة. (الدفعة 66) **القيد نفسه هو المتحقِّق**، وهو مفروض ومقيس. ويعلوه تحقّقٌ تطبيقي حيث تكون القيمة قابلة للعدم فلا يبلغها القيد: `verifiedGrantedBy` يختار بين هويةٍ موجودة و`NULL` صريحة. ولم يُضَف تحقّقٌ تطبيقي مكرِّر لكل مرجع: فحصٌ ثانٍ إلى جانب قيدٍ يفرض الشيء نفسه ينحرف عنه، والانحراف أخطر من غيابه.
- [x] فحص سلامة مرجعية دوري يُبلّغ عن أي صف يتيم. (الدفعة 66) `tools/ops/referential-integrity.mjs` — يقيس الإنفاذ (`PRAGMA foreign_keys`) والصفوف اليتيمة (`PRAGMA foreign_key_check`)، ويقبل `--remote`. مربوطٌ بوظيفة `migrations` في CI **بعد** بناء قاعدةٍ من الصفر، ومحروسٌ ربطُه وترتيبُه في `ciGates`. المقيس اليوم: **صفر يتيم**.
- [x] وجردٌ لم يطلبه البند: `--coverage` يُخرِج **120** عمودًا يبدو مرجعًا وبلا قيد — وهو ما **لا يراه أي إنفاذ**. للقراءة البشرية لا للبوابة: بعضها مرجعٌ إلى كائن دائم، وبعضها معرّف خارجي (متجر، مزوّد).

---

### 6.5 ما ثبت أنه سليم في هذه المنطقة

| البند | الدليل |
|---|---|
| ملكية الأسرة غير قابلة للتمثيل عبر الحدود | Durable Object لكل أسرة (`do/FamilyState.ts`)، والملكية تُثبَت داخله لا بشرط `WHERE` |
| سياسة احتفاظ فعلية للبيانات السلوكية | `scheduled/cleanup.ts:13,19` — 180 يومًا للتحليلات، 30 يومًا لسجل إبطال التكرار، بـcron يومي `0 3 * * *` |
| نشر القصص والكتب والألعاب والمشروعات صار ممكنًا | `routes/adminPublish.ts:136-139` مع بوابة جهوزية تفشل مغلقة |
| فصل ثلاث دلاء R2 بمنطق واضح | `wrangler.jsonc:42-66` + `lib/assetUrls.ts:79-108` يفشل مغلقًا على تطابق البادئة مع `visibility` |
| توجيه الطوابير يفصل مصنع المحتوى عن أحداث العائلة | `src/index.ts:242-256` |
| صفر مواضع حقن SQL في المراجعة السابقة، ولم يظهر جديد في هذه | استعلامات مُعامَلة عبر `queryAll/queryFirst/prepare().bind()` |

---

## §7 البيئات والنشر والأسرار والتشغيل — `OPS-*`

### OPS-101 — لا بيئة staging: كل تشغيل غير إنتاجي يلمس الإنتاج

**الأولوية:** ~~P0~~ · **المنطقة:** العمليات / البنية · **الحالة:** **CLOSED — قرار مالك 2026-08-26**

> **مُغلَق بقرار، لا بعمل هندسي.** المالك قرّر أن المنتج في مرحلة تطوير وأن كل شيء يعمل على الإنتاج مباشرة، ولا نية لبيئة وسطى. وبناءً على ذلك أُزيل أثر staging من الكود بدل انتظاره: عضو التعداد، والمضيف، والشارة، وثلاثة فروع `switch` ميتة، وسكربتا نشر وترحيل يخرجان بخطأ. التفصيل في `§15` (الدفعة الثانية).
>
> **ما يبقى صحيحًا من التحليل أدناه:** أن `failed_family_events` = 24 صفًا مرّت بلا إشعار (`OPS-106`)، وأن المعيار 24 في `§4` يحتاج بديلًا — تنفيذ اختبارات السباق محليًّا مقابل `wrangler dev`، أو توثيقها كغير مُختبَرة. والنص أدناه يبقى كسجل لما كان.

**المشكلة**
لا بيئة وسطى في الطرفين. الـWorker يعرّف بيئة واحدة مسمّاة (`production`)، والتطبيق يوجّه `development` و`staging` كلتيهما إلى مضيف الإنتاج.

**الدليل**
- `dashboard/api/package.json:15-16` — `deploy:staging` و`migrate:staging` يطبعان رسالة ويخرجان بـ`exit 1`.
- `dashboard/api/wrangler.jsonc:185-196` — `env.production` هي البيئة المسمّاة الوحيدة؛ والطبقة العليا موصوفة صراحةً بأنها للتطوير المحلي.
- `app_main/lib/core/env/app_environment.dart:120-133` — `development` و`staging` كلتاهما تُرجعان `_productionHost`، بتعليق يسمّي ذلك `A3 — EXTERNAL BLOCKER`.
- **الأثر المُقاس:** `failed_family_events` = **24 صفًا** في القاعدة المحلية، أي أن أحداثًا فشلت معالجتها فعلًا — وهذا نوع الخلل الذي يُكتشف عادةً في staging.

**الأثر**
معيار القبول 24 في خطة التشفير (اختبارات سباق للتسجيل والتشغيل والإلغاء والتنزيل على Staging) غير قابل للتنفيذ. وكل اختبار فوترة أو إبطال أو DRM إمّا يُجرى على الإنتاج أو لا يُجرى.

**نطاق التنفيذ**
بيئة `staging` في `wrangler.jsonc` بروابطها الخاصة (D1 وKV وR2 وقوائم منفصلة)؛ نطاق فرعي؛ سكربتات نشر وترحيل حقيقية؛ تحديث `allowedHosts` في التطبيق؛ بيانات بذر غير حقيقية.

**التبعيات** EXT-104 (تكلفة/حساب Cloudflare)، OPS-104.

**الحجم** L

**معايير القبول**
- [ ] `npm run deploy:staging` ينشر فعلًا إلى بيئة منفصلة.
- [ ] بناء التطبيق بـ`MAJARRA_ENV=staging` يصل إلى مضيف staging لا الإنتاج.
- [ ] لا رابط بين قواعد بيانات أو دلاء أو طوابير البيئتين.
- [ ] سيناريو سباق واحد على الأقل (تسجيل جهازين متزامنين) يُنفَّذ على staging ويُوثَّق.

**لا تفعل**
لا تُعِد استخدام قاعدة الإنتاج أو دلائها لبيئة staging «مؤقتًا».

---

### OPS-102 — CI لا يمكن أن يكون أخضر اليوم، ووظيفة Flutter ستتعلّق حتى المهلة القصوى

**الأولوية:** P0 · **المنطقة:** CI · **الحالة:** REGRESSION

**المشكلة**
`ci.yml` مُصلَح ومحكم في تصميمه (يعمل على `master`، والوظائف صريحة بلا fallback يخفي فشلًا، و`deploy-gate` يعتمد على الست السابقة). لكن الواقع أن أربع وظائف منه ستفشل الآن، وواحدة ستتعلّق.

**الدليل**
- `.github/workflows/ci.yml:29` — `flutter analyze --fatal-warnings`، والتحليل يُنتج **43 مشكلة** فيها تحذيرات فعلية (`unused_import` في `story_reader_page.dart:13`، `unreachable_switch_case` في `home_feed.dart:1460`، `unused_element` متعدد) → الوظيفة تفشل عند هذه الخطوة.
- `:31` — `flutter test --no-pub`، والجولة محليًا تُنتج 13 إخفاقًا ثم **تتعلّق عند `test/tmp_render_png_test.dart`** ولا تنتهي → الوظيفة تستهلك مهلة GitHub القصوى (ست ساعات افتراضيًا) لكل دفعة.
- `:47-48` — `npm test` للـWorker: إخفاقان.
- `:64` — `npx vitest run` للوحة: إخفاقان.
- `:161` — `deploy-gate` يعتمد على الست، فلن يعمل أبدًا.

**الأثر**
CI في حالة «مُصلَح على الورق، أحمر في الواقع». والأسوأ من الفشل هو التعليق: وظيفة لا تنتهي تحجب الطابور وتستهلك دقائق التشغيل بلا أي إشارة مفيدة.

**نطاق التنفيذ**
`QA-101` (حذف اختبار الـPNG المؤقت) أولًا لأنه المانع المطلق؛ ثم `QA-102` (الإخفاقات الـ13)؛ ثم `SEC-101` و`SEC-104` و`ADM-103` و`ADM-104`؛ ثم `DEBT-101` (تحذيرات التحليل)؛ وإضافة `timeout-minutes` لكل وظيفة حتى لا يتكرر التعليق غير المحدود.

**الحجم** M

**معايير القبول**
- [ ] الوظائف الست خضراء على `master`، بدليل تشغيل فعلي في GitHub.
- [ ] كل وظيفة لها `timeout-minutes` صريح.
- [ ] `deploy-gate` يعمل ويسجّل ما كان سيُنشَر.

---

### OPS-103 — نسخة من قاعدة الإنتاج على القرص، غير متتبَّعة و**غير مُتجاهَلة**

**الأولوية:** P0 · **المنطقة:** العمليات / الخصوصية / Git · **الحالة:** PARTIAL

**المشكلة**
`.backups/majarra-db-prod-pre-0076-20260823.sql` بحجم **1.38 ميغابايت** موجود في شجرة العمل. `git check-ignore` يؤكد أنه **ليس مُتجاهَلًا**، و`git status` يعرضه كـ`?? .backups/`. أي أن أمر `git add -A` واحدًا يرفع نسخة من قاعدة بيانات الإنتاج إلى المستودع.

**الدليل**
- `git check-ignore -v ".backups/majarra-db-prod-pre-0076-20260823.sql"` → لا مطابقة (رمز خروج 1).
- `.gitignore` يتجاهل `_backups/` (بشرطة سفلية) ولا يتجاهل `.backups/` (بنقطة) — الاسمان متشابهان والفرق حرف واحد.
- `git status --porcelain` → **292 مسارًا غير متتبَّع** و255 معدَّلًا و16 مُضافًا و8 محذوفة. أي أن `git add -A` في هذه الشجرة عملية غير آمنة أصلًا.

**السلوك المتوقع**
`.backups/` في `.gitignore`؛ ونقل النسخ إلى مسار خارج المستودع؛ وسياسة موثَّقة لعمر نسخ الإنتاج المحلية وحذفها.

**الحجم** XS (والأثر كبير)

**معايير القبول**
- [ ] `git check-ignore .backups/...` يطابق قاعدة.
- [ ] لا نسخة إنتاج داخل جذر المستودع.
- [ ] `tools/ci/scan-secrets.mjs` أو قاعدة مكافئة تفشل إن ظهر ملف `.sql` بحجم كبير في مسار مُتتبَّع.

**لا تفعل**
لا تُصلح هذا بـ`git rm --cached` وحده؛ القاعدة الناقصة في `.gitignore` هي السبب.

---

### OPS-104 — أسرار الإنتاج في ملف نصي محلي، وسجلات تطوير بميغابايتات قد تحمل رموزًا

**الأولوية:** P1 · **المنطقة:** العمليات / الأسرار · **الحالة:** PARTIAL

**المشكلة**
الأسرار **غير متتبَّعة في git** — وهذا صحيح ومُتحقَّق منه — لكنها موجودة على القرص في ملف نصي عادي إلى جانب سجلات ضخمة وملفات استجابة قد تحتوي رموزًا حيّة.

**الدليل** (أسماء وأحجام فقط، بلا أي قيمة)
- `dashboard/api/.secrets.local.txt` (668 بايت) يحمل أزواج محلي/إنتاج لثلاثة أسرار: `AUTH_TOKEN_SECRET`، `MEDIA_TOKEN_SECRET`، `ADMIN_API_KEY`.
- `dashboard/api/.dev.vars` (269 بايت) يحمل `AUTH_TOKEN_SECRET`، `PLAYVEO_API_KEY`، `GOOGLE_TTS_API_KEY`، `GOOGLE_AI_API_KEY`.
- سجلات تطوير على القرص: `devA.log` **3.96 ميغابايت**، `dev2.log` 365 كيلوبايت، وعشرة ملفات أخرى؛ و`probe-login.json` (139 بايت) و`probe-resend.json` — ملفات استجابة لنقطة دخول.
- `dashboard/api/.tmp/` فيه 12 ملفًا على الأقل منها `created-ids.json` و`bulk_phase1_assets.sql` (496 كيلوبايت).
- الحماية القائمة صحيحة: `.gitignore` يتجاهل `.secrets*` و`.env*` و`*.log` و`.wrangler/` و`*.rar`، و`dashboard/api/.gitignore:26` يتجاهل `.tmp/`، ووظيفة `secrets` في CI تشغّل ماسحًا بقواعد المستودع مع `--self-test` وتاريخ كامل.

**الأثر**
سرّ إنتاج في ملف نصي على جهاز مطوّر يتسرّب بطرق لا يراها git: نسخة احتياطية، أرشيف `.rar`، مشاركة شاشة، أو نسخ المجلد. وسجل بحجم 4 ميغابايت من جلسة تطوير حقيقية قد يحتوي رموز دخول كاملة.

**السلوك المتوقع**
الأسرار في `wrangler secret` أو مدير أسرار، لا في ملف؛ حذف السجلات وملفات الاستجابة القديمة؛ وتدوير أي سرّ ظهر في سجل.

**التبعيات** SEC-102 (إبطال `ADMIN_API_KEY` بعد بذر مستخدم)، SEC-109 (دوران بلا إخراج المستخدمين).

**الحجم** S

**معايير القبول**
- [ ] لا سرّ إنتاج في أي ملف داخل المستودع، ولو غير متتبَّع.
- [ ] السجلات وملفات `probe-*` محذوفة، وقاعدة تمنع عودتها.
- [ ] كل سرّ ظهر في سجل تطوير مُدوَّر، والإجراء موثَّق في `SETUP_SECRETS.md`.

---

### ~~OPS-105~~ — النشر لا يحدث من CI، فلا رابط بين بناء مُتحقَّق وما يعمل في الإنتاج

**الأولوية:** P1 · **المنطقة:** CI / النشر · **الحالة:** ✅ **مُنفَّذ في الدفعة الثامنة والعشرين** — يبقى فعلان للمالك: إضافة السرَّين، وتنفيذ تراجعٍ واحد مقصود بعد أوّل نشر.

**المشكلة**
`deploy-gate` يتحقق أن الحزمة تُبنى (`--dry-run`) ولا ينشر. النشر الحقيقي يدوي وغير موثَّق، ولا يوجد تتبّع لأي إصدار يعمل الآن.

**الدليل**
`.github/workflows/ci.yml:176-178` (`npx wrangler deploy --dry-run --outdir .wrangler-ci --env production`) والتعليق `:150-160` يشرح أن التوكن غير موجود وأن تمكينه قرار مالك.

**السلوك المتوقع**
نشر آلي من `master` بعد خضرة كل الوظائف، مع وسم إصدار وسجل «ما نُشِر ومتى ومن أي commit»، وإجراء تراجع موثَّق.

**التبعيات** EXT-104 (توكن Cloudflare)، DECIDE-103 (سياسة النشر).

**الحجم** M

**ما نُفِّذ**
مهمّة `deploy` تنشر **فعلًا حين يوجد التوكن**، والتوكن شرطٌ في وقت التشغيل لا سطرٌ
في ملف: يضيف المالك السرَّين فيُنشَر، ولا تحرير. والصياغة القديمة كانت مثبَّتة على
`--dry-run` مع تعليق «كيف تجعله حقيقيًّا» — أي طلبُ تغييرٍ متنكّرًا في صورة مسار.

وبصمة الـcommit تُخبَز في الحزمة (`tools/ci/write-release.mjs` → `src/lib/release.ts`)،
فتُجيب النقطة `GET /version` من **الإنتاج نفسه**. ولم تُمرَّر بـ`--var` لأن دلالة
دمجها مع `vars` المُعلَنة ليست شيئًا يُقامَر عليه في مسار النشر: لو استبدلت الخريطة
بدل أن تضيف إليها، سقطت `ENVIRONMENT` و`API_VERSION` وانكسر الإنتاج **بعد** خضرة كل
الفحوص.

**معايير القبول**
- [x] نشر يحدث فقط من CI على `master` بعد خضرة كل الوظائف (بما فيها `migrations`).
- [x] كل نشر يترك سجلًا يربط الإصدار بالـcommit: وسم `release/<تاريخ>-<sha7>` رسالته
      فيها معرّف إصدار Cloudflare ورابط الجولة، **ومعه تحقّق فعلي**: المسار يسأل
      `https://api.majarra.app/version` ويوازن البصمة، فلا «نشرٌ نجح» ونسخةٌ قديمة
      تخدم الطلبات.
- [ ] **إجراء التراجع موثَّق بأوامره ومُختبَر بصريًّا في مسار النشر، ولم يُنفَّذ على
      الإنتاج** — لا يمكن تنفيذه بلا نشرٍ حقيقي. فعلٌ للمالك بعد تمكين السرَّين
      (`SETUP_SECRETS.md §8`).

**قرار: الترحيلات لا تُطبَّق مع النشر**
نشرٌ سيّئ يُلغيه `wrangler rollback` في ثوانٍ، **وترحيلٌ سيّئ لا يُلغيه شيء** يملكه
هذا المسار. وربطهما يعني أن تغييرًا في المخطَّط يركب مع إصلاح نصّ عابر فيُطبَّق بلا أن
ينظر إليه أحد. فبقي `migrate:remote` فعلًا منفصلًا، ومعه `migration-ledger --remote`
للفحص قبله.

---

### ~~OPS-106~~ — لا رصد فعلي: 12 خدمة معرَّفة وصفر فحص صحة وصفر تنبيه

**الأولوية:** P1 · **المنطقة:** العمليات / الرصد · **الحالة:** ✅ **مُنفَّذ في الدفعة العشرين** — انظر §15. يبقى فعلان للمالك: ضبط `OPS_ALERT_EMAIL` وتشغيل أداة التشخيص على الإنتاج. النصّ أدناه يبقى كما وُجد.

**المشكلة**
البنية موجودة (`observability.enabled` في `wrangler.jsonc:8-10`، وجداول `ops_*`، وست سياسات SLA، و12 خدمة معرَّفة) لكن لا بيانات ولا تنبيهات.

**الدليل** (استعلام D1)
`ops_services` = 12، `sla_policies` = 6، لكن `ops_health_checks` = **0**، `ops_alerts` = **0**، `ops_incidents` = **0**. و`failed_family_events` = **24** بلا أي تنبيه أنتجها.

**الأثر**
24 حدث عائلة فاشل مرّت بلا إشعار أحد. شاشة العمليات ستعرض خدمات «بلا حالة»، وهذا أسوأ من عدم وجود الشاشة لأنه يُقرأ كـ«لا مشاكل».

**السلوك المتوقع**
فحوص صحة دورية تكتب نتائجها، وتنبيه فعلي على DLQ وعلى الأحداث الفاشلة وعلى زمن الاستجابة، بقناة تصل إلى شخص.

**الحجم** L

**معايير القبول**
- [ ] فحص صحة يكتب صفًا كل دورة، والشاشة تعرض آخر حالة حقيقية.
- [ ] صف في `failed_family_events` يُنتج تنبيهًا يصل فعلًا.
- [ ] الـ24 صفًا القائمة مُشخَّصة ومُعالَجة أو مُصنَّفة صراحةً كضجيج تطوير.

---

### OPS-107 — شجرة العمل والمستودع محمَّلان بمخلَّفات ثقيلة

**الأولوية:** P2 · **المنطقة:** المستودع / النظافة · **الحالة:** DEBT

**المشكلة**
566 مسارًا في حالة غير نظيفة، وأرشيفات ونسخ وسجلات بحجم مئات الميغابايتات في الجذر.

**الدليل**
`git status --porcelain`: 292 غير متتبَّع + 255 معدَّل + 16 مُضاف + 8 محذوف. أرشيفات في الجذر: `majarra_images.rar`، `app_main.rar`، `plans.rar`، `admin-dashboard html.rar`، `majarra-landing.v3.rar` (مُتجاهَلة بقاعدة `*.rar` لكن حاضرة). ثلاث نسخ من صفحة الهبوط (`majarra-landing.backup.html`، `.v2.full-backup.html`، `.v3.html`) و35 ملف `.md` في الجذر بعضها متناقض.

**الأثر**
لا يمكن مراجعة تغيير بثقة في شجرة بهذا الحجم من الضجيج، ولا معرفة ما هو مصدر وما هو مخلَّف. وهذا هو السياق الذي أنتج `OPS-103`.

**الحجم** S

**معايير القبول**
- [ ] شجرة عمل نظيفة أو قائمة موثَّقة بما هو متروك عن قصد ولماذا.
- [ ] الأرشيفات والنسخ منقولة خارج المستودع.
- [ ] ملفات الجذر `.md` مُصنَّفة: مصدر حقيقة / تاريخي / مُبطَل (انظر `DOCS-101`).

---

### 7.5 ما ثبت أنه سليم في هذه المنطقة

| البند | الدليل |
|---|---|
| CI صار يعمل على الفرع الصحيح ولا يخفي فشلًا | `ci.yml:7-11` (`master, main`) والتعليقات `:30`, `:44-47` توثّق أن الصيغة السابقة كانت تتخطّى المجموعة كلها |
| بوابة إصدار تعتمد على كل الوظائف | `ci.yml:161-163` (`needs:` الستة + شرط `master` وpush فقط) |
| ماسح أسرار بقواعد المستودع مع اختبار ذاتي وتاريخ كامل | `ci.yml:126-140` |
| فشل التبعيات على `critical` فقط، مع تقرير كامل | `ci.yml:142-159` — تصميم واعٍ لتجنّب بوابة يتعلّم الناس تجاوزها |
| الأسرار غير متتبَّعة فعلًا في git | `git ls-files --error-unmatch` يفشل على `.dev.vars` و`.secrets.local.txt` و`.env.local`؛ و`.gitignore` يغطّي `.secrets*` و`.env.*` و`*.log` و`.wrangler/` |
| دلو إبداعات الأطفال مفصول بين dev والإنتاج | `wrangler.jsonc:52-66` — الرابط المحلي يسمّي `majarra-creations-dev` عن قصد |

---

## §8 تطبيق Flutter — `APP-*`

### APP-101 — أربعة ملفات آلهة تحمل أخطر منطق في التطبيق، وأكبرها بلا اختبار

**الأولوية:** P1 · **المنطقة:** التطبيق / المعمارية · **الحالة:** DEBT

**المشكلة**
منطق التشغيل والحقوق والقارئ والاستوديو مكتوب داخل ملفات واجهة ضخمة، بلا طبقة بيانات ولا دومين، وبلا تغطية اختبار.

**الدليل** (عدّ أسطر فعلي)

| الملف | الأسطر | تغطية |
|---|---:|---|
| `lib/features/games/presentation/pages/creative_studio_page.dart` | **4373** | لا اختبار |
| `lib/features/playback/presentation/playback_page.dart` | **3434** | لا اختبار |
| `lib/features/home/presentation/widgets/home_feed.dart` | **2768** | لا اختبار |
| `lib/features/reader/presentation/pages/story_reader_page.dart` | **2225** | لا اختبار |
| `lib/features/home/data/majarra_api_client.dart` | 1535 | اختبار واحد بـ45 سطرًا |
| `lib/features/profile/presentation/pages/membership_page.dart` | 1480 | لا اختبار |

`playback_page.dart` وحده يخلط: إنشاء جلسة القدرة، فحوص الحقوق، تجديد التوكن، التقارير، حفظ التقدّم، فك التنزيل المشفَّر، العلامة المائية، وواجهة المشغّل الكاملة.

**السلوك المتوقع**
استخراج طبقة بيانات/تطبيق لكل من التشغيل والقارئ (على نمط `features/downloads/` الذي هو المرجع الأنظف في المشروع: `application/` + `data/` + `domain/` + `presentation/`)، ثم تغطية المنطق المستخرج باختبارات.

**نطاق التنفيذ**
تفكيك تدريجي: أولًا `PlaybackRepository` (الجلسة والتجديد والتقارير)، ثم `ReaderRepository`، ثم تقسيم `home_feed` إلى widgets مستقلة. لا إعادة كتابة شاملة.

**الحجم** XL

**معايير القبول**
- [ ] منطق جلسة التشغيل قابل للاختبار بلا widget، ومغطّى باختبارات.
- [ ] لا ملف تحت `presentation/` يتجاوز 800 سطر بعد التفكيك.
- [ ] لا انحدار في سلوك المشغّل، بدليل اختبارات جديدة.

---

### APP-102 — الطبقات غير متسقة: **تسع** ميزات بلا `data/` ولا `domain/`

**الأولوية:** P1 · **المنطقة:** التطبيق / المعمارية · **الحالة:** PARTIAL — **الأخطر منه أُغلق في الدفعة التاسعة والعشرين**، والباقي ترتيبيّ ومحدود بأسمائه أدناه.

**المشكلة**
`downloads` و`home` و`auth` تتبع المعمارية المعلنة، وبقية الميزات لا.

**الدليل**
`lib/features/playback` و`reader` و`planets` و`shorts` و`audio` و`tv` و`details` كلها `presentation/` فقط (مع `application/` جزئي في `reader`). و`features/profile` فيه `data/` بلا `domain/` ولا `application/`.

**تصحيح العدد** (بالجرد في الدفعة 29): **تسع** ميزات بلا `data/` ولا `domain/` لا سبع — يُضاف `child` و`parent` إلى القائمة. و`downloads` و`home` وحدهما تملكان الطبقات الأربع؛ `auth` بلا `domain/`.

**الأثر**
كل نداء API في هذه الميزات يُكتب داخل `State` بيدين مختلفتين، فتظهر أنماط مختلفة لنفس العملية (فك الظرف، معالجة الخطأ، إعادة المحاولة)، ولا يمكن اختبار أي منها بلا `pumpWidget`.

**الحجم** L

**ما وجدته داخل هذا البند وكان أخطر من عنوانه** (الدفعة 29)
البند يُقرأ كترتيب مجلّدات. والقياس أظهر أن عدم الاتساق أنتج **ثقبًا أمنيًّا**:
موضعان ينفّذان `http.get` عاريًا — بمخزن ثقة النظام لا بالعميل المثبَّت
(`SEC-105`) — أحدهما يحمّل **نصّ الترجمة الذي يُعرَض على الطفل**
(`playback_page.dart:790`)، والآخر صورة التلوين (`coloring_board_v2.dart:178`).
فأي جذر مزروع على الجهاز (أداة تحليل، وكيل مؤسسي، اعتراض على شبكة عامّة) يستبدل
ما يقرأه الطفل. وأحدهما **بلا مهلة** فيعلّق إلى الأبد، والآخر بمهلة ثالثة لا تشبه
مهلة العميل. والأسوأ: الخدمة التي يحتاجها الثاني كانت موجودة، **مثبَّتةً وبمهلة** —
فكان تطبيقان لعملٍ واحد، وانحرف أحدهما إلى ما هو أقل أمنًا.

ومعه: ثلاث صفحات تبني `CreativeRemoteAssetsService()` بنفسها، فكل زيارة تُنشئ
عميلًا مثبَّتًا جديدًا **لا يُغلَق أبدًا**.

**معايير القبول**
- [ ] كل ميزة تنادي الشبكة تملك `data/` و`domain/` — **جزئيًّا**: `playback` كسبت
      `data/`+`application/` (أوّل طبقتين لها)، و`child` كسبت `domain/`، و`parent`
      كسبت `data/`+`application/`، و`profile` و`games` كسبتا `application/`.
      فالميزات ذوات الطبقات الأربع صارت أربعًا بدل اثنتين. وبقيّة الميزات
      (`reader`, `audio`, `planets`, `shorts`, `tv`, `details`) لم تُلمَس.
- [x] **لا `http` مُستدعى مباشرة من `presentation/`** — صفر ملف، ويحرسه
      `test/presentation_network_layering_test.dart` فأي ملف عرضٍ جديد يفتح
      مسارًا شبكيًّا بنفسه يُفشل الجولة.
- [x] **لا مزوّد جلبٍ (`FutureProvider`/`StreamProvider`) مُعلَنٌ في ملف صفحة** —
      صفر، ويحرسه نفس الاختبار. ستّة نُقلت إلى `application/`.
- [ ] لا `MajarraApiClient` مُستدعى مباشرة من `presentation/` — **باقٍ**: ١٥ ملفًا
      (‏`auth` ٩، `profile` ٤، `playback` ٢، `reader`، `audio`، `onboarding`).
      كلّها تمرّ بالعميل المثبَّت، فالمكسب من نقلها **ترتيبيّ وقابلية اختبار، لا
      أمن**، وهو مشروط بتغطية اختبارية لصفحاتٍ بلا اختبار.

**ما بقي، مُرتَّبًا بالضرر لا بالطبقة**
1. ~~ستّ كتابات `updateChildSettings` بلا `catch`~~ ✅ الدفعة 30: مستودع بدالّة
   مسمّاة لكل ضبط، ومتحكّم يعرض الفشل ويسجّله ولا يُبطل الكاش إلا عند النجاح.
2. ~~`'فشل الحذف: $e'`~~ ✅ الدفعة 30: من `AppFailure`، والخطأ مُسجَّل.
3. ~~ثلاثة مزوّدات في ملفات صفحات~~ ✅ الدفعة 30 — وصارت **ستّة**: الحرس كشف
   ثلاثة أخرى في `profile` لم تكن في الجرد.
4. `jsonDecode` لجسم ردٍّ داخل عنصر واجهة (`account_data_page.dart:256`).
5. ثلاث صيغ لفكّ الظرف، وثلاث لمعالجة الخطأ، بلا `reportIgnoredError` في معظمها.
6. `EarnedSticker` و`AccountProfile` و`FamilyDevice` نماذج نطاق ما زالت مُعلَنة في
   ملفات صفحات، فتستوردها طبقة `application/` بـ`show` صريح — دَينٌ **مرئيّ**
   بقصد، لا تصميم.
7. `playback_page.dart.new` — ملف مخلَّف بجانب صفحة المُشغِّل (`APP-105`).

ونقل نداءات `MajarraApiClient` من صفحات بحجم ٣٥٣٨ و٢٢٢٤ و١٤٨٠ سطرًا **بلا اختبار
واحد** يغطّيها هو `APP-101` بثوب آخر: مقايضة عطلٍ صامت بعطلٍ صريح. فالترتيب أعلاه
يبدأ بما يضرّ فعلًا.

---

### APP-103 — كاتالوج مبندل داخل الكود يحلّ محل الكاتالوج الحقيقي عند أي انقطاع

**الأولوية:** P1 · **المنطقة:** التطبيق / المحتوى · **الحالة:** PARTIAL — الدفعة 31 أوقفت الحشر الدائم ووحّدت نطاق الأصول؛ ويبقى أن الحزمة نفسها لا تحمل حالة نشر.

**تصحيحان لهذا البند** (بالقياس في الدفعة 31، والنصّ الأصلي يبقى أدناه كما وُجد):

1. **«الفشل صامت» لم يكن صحيحًا على الرئيسية.** `home_feed.dart:206` يعرض
   `_FallbackNotice` عند `usesLocalFallback` بنصّ صريح: «نعرض المكتبة المحلية
   الآمنة حتى يصبح المحتوى المنشور متاحًا»، مع زرّ تحديث و`Semantics(liveRegion)`.
   الصامتة كانت **شاشة «العب»** وحدها.
2. **الاختبارات الثلاثة المذكورة كدليل تنجح، ولا تخصّ هذا الملف.** هي في
   `deep_link_smoke_test.dart` وتقيس **كاتالوج الأنشطة المرجعية بصيغة JSON**
   (`_bundledReferenceActivities = 32`) لا `local_catalog.dart`. فالدليل قديم،
   والبند يبقى قائمًا لأسبابه الأخرى.

وما كان أخطر ممّا وصفه البند: البند يقول «عند أي انقطاع»، والقياس أظهر أن
`play_page.dart` كان يحشر `LocalCatalog.experiences` **بلا شرط**، فيرى الطفل ألعابًا
من الحزمة والشبكة سليمة تمامًا — أي تجاوزٌ لسياسة النشر في **كل** جولة.

**المشكلة**
`local_catalog.dart` كاتالوج كامل مكتوب في الكود (كواكب، سلاسل، حلقات، كتب، قصص، وروابط CDN حرفية) يُستخدم كبديل ثالث بعد الشبكة والكاش. أي انقطاع يجعل الطفل يرى كاتالوجًا لا يعكس ما هو منشور فعلًا.

**الدليل**
- `lib/features/home/data/local_catalog.dart` (948 سطرًا) مع روابط `https://cdn.majarra.app/public/...` مكتوبة حرفيًا.
- `lib/features/home/data/content_repository.dart:36-88` — سلسلة `network → cache → bundled` (`usedBundled`).
- `lib/features/home/data/content_dtos.dart:317` — `const _cdnBase = 'https://cdn.majarra.app'` مستقل عن `AppConfig`، فتغيير النطاق يتطلّب تعديل ملفين.
- **ثلاثة اختبارات فاشلة** تخص هذا المسار: `deep_link_smoke_test.dart` سيناريوهات «cold start — bundled JSON loads» و«unpublished/archived not leaked» و«normal production load does not require Dart literal fallback» — كلها تتوقع 30 عنصرًا ولا تجدها.

**الأثر**
سياسة النشر تُتجاوز في وضع الانقطاع: مسودة أو محتوى مؤرشف قد يظهر (وهو ما يقيسه أحد الاختبارات الفاشلة بالاسم). والأخطر أن الفشل صامت — الطفل لا يعرف أنه يرى نسخة قديمة.

**السلوك المتوقع**
البديل المبندل يقتصر على هيكل واجهة فارغ ورسالة «لا اتصال»، أو يُحدَّد صراحةً كعرض تجريبي معلَّم للمستخدم. ونطاق الـCDN يأتي من `AppConfig` وحده.

**الحجم** M

**معايير القبول**
- [ ] لا محتوى غير منشور يظهر في أي وضع، بدليل الاختبار المسمّى بذلك —
      **جزئيًّا**: المبندل لم يعد يُحشَر مع البيانات الحيّة، ولم يعد يُستخدم إلا حين
      يفشل الطلب فعلًا. لكن عناصر الحزمة **لا تحمل حالة نشر ولا عمرًا** أصلًا،
      فالضمان الكامل يقتضي إمّا حصر البديل في هيكل فارغ، وإمّا حمل الحزمة حالةَ
      نشرٍ تُراجَع. وذلك **قرار مالك** (ما يراه طفل بلا إنترنت)، مُسجَّل أدناه.
- [x] استخدام البديل المبندل مرئي للمستخدم لا صامت — الرئيسية كانت تُعلنه أصلًا،
      وشاشة «العب» تُعلنه الآن بنفس النصّ ونفس زرّ التحديث.
- [x] نطاق CDN واحد يأتي من `AppConfig` — `AppConfig.assetBaseUrl`/`assetHost`،
      ويحرسه اختبار يرفض أي نصّ حرفي في `lib/` خارج ملف الإعداد. كان في **أربعة**
      مواضع مستقلّة لا اثنين.

**قرار معلَّق للمالك** (`DECIDE`): ماذا يرى طفلٌ بلا إنترنت؟
1. **هيكل فارغ + «لا اتصال»** — أصدق، ويُلغي كل خطر عرض غير المنشور، وأقسى على
   طفل في سيارة.
2. **مكتبة محلية معلَّمة** (الوضع الحالي بعد الدفعة 31) — يرى محتوًى، ويعرف أنه
   محليّ، ويبقى احتمال أن عنصرًا سُحب من النشر ولم يُسحب من الحزمة.
3. **حزمة تحمل حالة نشر** — تُراجَع مع كل إصدار. أدقّ، وكلفتها إجراءُ مراجعة دائم.

---

### ~~APP-104~~ — واجهات تعتمد على جداول بلا كاتب، فبلوكات كاملة مخفية دائمًا في الكود

**الأولوية:** P2 · **المنطقة:** التطبيق / المنتج · **الحالة:** ✅ **مُنفَّذ في الدفعة الثانية والثلاثين** — الأنواع الثلاثة سُحبت من الطرفين الثلاثة، والباقي (شارة تاريخ الإصدار، وأفاتار المسار) مُسجَّل أدناه.

**المشكلة**
بلوكات الرئيسية مبنية ثم مُعطَّلة بثابت في الكود لأن البيانات التي تُغذّيها غير موجودة.

**الدليل**
- `lib/features/home/presentation/widgets/home_feed.dart:1573-1575` و`lib/features/home/domain/feed_blocks.dart:261-264` — `mostWatched` و`becauseYouWatched` يعيدان `false` دائمًا.
- `home_feed.dart:1424-1426` — بلوك «رحلة التعلم» يعيد `SizedBox.shrink()` لعدم وجود نقطة نهاية.
- `home_feed.dart:2251-2255` — لا حقل تاريخ إصدار، فالشارة محيَّدة.
- `home_destination_spec.dart:307-311` — أفاتار طفل placeholder لأن البروفايل لا يُحمَّل في هذا المسار.
- السبب الجذري في الخادم: `API-105` (`watch_progress` و`children_profiles` بلا كاتب).

**السلوك المتوقع**
إمّا إكمال المصدر (API-105) وتشغيل البلوكات، أو حذف الكود المُعطَّل. الوضع الحالي أسوأ الاثنين: كلفة صيانة بلا قيمة.

**التبعيات** API-105.

**الحجم** S

**ما وجدته أوسع ممّا وصفه البند** (الدفعة 32)
البند يصف الأمر كـ«كود مُعطَّل في التطبيق». والحقيقة أنه **وعدٌ عبر الطبقات
الثلاث**: لوحة الإدارة تعرض الأنواع الثلاثة بأسماء عربية (`AppExperiencePage.tsx`)،
والخادم يقبلها في `HOME_BLOCK_TYPES` ويصنّفها في `SYSTEM_BLOCK_TYPES`، والتطبيق
يخطّطها إلى أنواع تُعيد `false` دائمًا.

أي أن محرّرًا يستطيع إضافة «الأكثر مشاهدة»، وجدولتها، واستهدافها، ونشرها — **ولا
تظهر على أي جهاز، ولا خطأ في أي مكان**. وهذا ليس دَينًا داخليًّا: هو واجهة تَعِد بما
لا يحدث.

**ما نُفِّذ**
- **التطبيق:** حُذفت الثلاثة من `BlockType`، ومن التخطيط الافتراضي (ثلاثة صفوف كانت
  فيه)، ومن `_blockTypeMap`، ومن `home_feed.dart` — ومعها **عنصرا عرض ميتان**
  (`_LearningJourneyCard` و`_MostWatchedRail`، ٢٢٢ سطرًا) كانا مُبقيَين بـ
  `// ignore: unused_element`.
- **الخادم:** `HOME_BLOCK_TYPES` تبقى كما هي لأنها **مرآة قيد CHECK** في القاعدة،
  وأُضيفت `RETIRED_BLOCK_TYPES` و`OFFERED_BLOCK_TYPES` **مشتقّة** منها. الكتابة
  تُفحَص على المعروض، والقراءة تبقى على المقبول — فالصفوف المحفوظة في الإنتاج تُقرأ،
  ولا يُنشأ صفٌّ جديد لا يعرضه أحد. ورسالة رفض مستقلّة: «نوعٌ سُحب» لا «نوع غير
  صالح»، وإلا بحث المشغّل عن خطأ إملائي في اسم كان صحيحًا.
- **اللوحة:** المُنتقي يُبنى من `meta.block_types` فتوقّف عرضها تلقائيًّا؛ والأسماء
  العربية **بقيت** كي يُقرأ صفٌّ قائم بعنوانه لا بمعرّفه، ومعها شارة `role="alert"`
  تقول إنه نوع مسحوب لا يعرضه أي إصدار.

**السبب الجذري لم يكن `API-105`**
`API-105` أُنجز في الدفعة 24 (‏`child_progress_projection` بكاتب فعلي). ومع ذلك
الأنواع الثلاثة تبقى بلا مصدر: «الأكثر مشاهدة» يحتاج **تجميعًا بين الأسر** — قرارُ
خصوصية لا نقطة نهاية · «رحلة التعلّم» لا مصدر لها أصلًا · و«لأنك شاهدت» يقرأ **نفس**
مصدر `recommended` العاملة، فصفٌّ ثانٍ من مصدر واحد اختيارُ منتج لا فجوة.

**ما بقي من البند** (أصغر، ومسجَّل بدقّة)
- `home_feed.dart` — لا حقل تاريخ إصدار في الكاتالوج، فشارة «جديد» محيَّدة. يحتاج
  عمودًا في المخطَّط وكاتبًا له.
- `home_destination_spec.dart:307-311` — أفاتار طفل placeholder. صار قابلًا للحلّ بعد
  الدفعة 30: `familyChildrenProvider` في `child/application/` يحمل `avatarId`.

---

### ~~APP-105~~ — مخلَّفات وتكرار داخل `lib/`

**الأولوية:** P2 · **المنطقة:** التطبيق / النظافة · **الحالة:** ✅ **مُنفَّذ في الدفعة الثالثة والثلاثين** — المخلَّفات والكود الميت حُذفت؛ وشطر «الصفحات المكرَّرة» **صُحِّح**: ليست مكرَّرة، انظر الجدول أدناه.

**الدليل** (فحص فعلي لشجرة `lib/`)
- **ملف متروك داخل الكود:** `lib/features/playback/presentation/playback_page.dart.new` (7 بايت).
- **وثيقة داخل الكود:** `lib/features/games/presentation/pages/coloring/README_V2.md` (6.4 كيلوبايت).
- **اعتمادية ميتة:** `hive_flutter` معلنة في `pubspec.yaml:22` و**صفر** استخدام في 184 ملفًا.
- **صفحات متوازية مكرَّرة** في `features/games/presentation/pages/`: `coloring/coloring_home_v2.dart` و`coloring_home_v2_live.dart`؛ `reference_catalogue_page.dart` و`reference_catalogue_page_live.dart`؛ `my_boards_page.dart` و`studio_v2/my_boards_v2.dart`؛ `studio_tracing/trace_home_v2.dart` و`studio_v2/trace_home_page.dart`. وأربع شجرات مسارات متجاورة: `pages/`، `pages/studio_v2/`، `pages/studio_tracing/`، `presentation/studio/`.
- **تحذيرات التحليل تؤكد الموت:** `unused_import` في `creative_studio_page.dart:36` و`story_reader_page.dart:13` و`trace_home_v2.dart:8`؛ و`unused_element` لـ`_isMultiSection` و`_CatalogColoringSection` و`_ColoringPagesSection` و`_StarsPainter` و`_kCyan`.

**الأثر**
لا يمكن معرفة أي نسخة هي الحقيقية بلا قراءة الاستدعاءات، وأي تعديل على تجربة التلوين يجب تكراره مرتين أو يُنسى في إحداهما.

**الحجم** M

**تصحيح: «الصفحات المتوازية المكرَّرة» ليست مكرَّرة** (بالقياس في الدفعة 33)
عُدّت المراجع لكل ملف من الثمانية، فلا واحد منها مهجور:

| الزوج | ما هو فعلًا |
|---|---|
| `coloring_home_v2.dart` · `_live` | الأوّل يحمل `FeaturedColoringSpec` و`kFeaturedColoringV2` — **يستورده ٨ مواضع**؛ والثاني مُغلِّف شبكي حوله |
| `reference_catalogue_page.dart` · `_live` | الأوّل يحمل الأنواع المشتركة، ويستورده الثاني و`reference_drawing_page` و`creative_deep_links` |
| `trace_home_v2.dart` · `trace_home_page.dart` | الثاني **يستورد الأوّل** ويُغلِّفه — لا نسختان |
| `my_boards_page.dart` · `studio_v2/my_boards_v2.dart` | **كلاهما** يستورده `creative_studio_page.dart` في موضعين مختلفين |

فالبنية طبقةٌ حيّة (`_v2` = نماذج وعرض مشترك، `_live` = مُغلِّف شبكي) لا نسخٌ
مهجورة. ودمجها **إعادة هيكلة** في شيفرة استوديو بلا تغطية اختبارية، لا حذف — أي
`APP-101` بثوب آخر. المعيار أُعيد صياغته أدناه على هذا الأساس.

**معايير القبول**
- [x] لا ملف غير `.dart` ولا ملف `.new` داخل `lib/` — حُذف `playback_page.dart.new`
      (٧ بايتات فيها «PENDING») ونُقل `README_V2.md` إلى `docs/COLORING_V2_DESIGN.md`.
      ويحرسه `test/lib_hygiene_test.dart`، ويستثني `lib/l10n/*.arb` وحدها لأنها
      **مصدر** الترجمة باصطلاح Flutter لا مخلَّف.
- [ ] ~~نسخة واحدة لكل شاشة استوديو~~ → **أُعيد تحديده**: لا نسخة مهجورة أصلًا
      (الجدول أعلاه). المطلوب دمج طبقتَي `_v2`/`_live`، وهو إعادة هيكلة تحتاج
      تغطية اختبارية أوّلًا — تبقى مع `APP-101`.
- [x] `hive_flutter` محذوفة من `pubspec.yaml` (صفر استخدام في الشجرة)، ويحرسه اختبار.
- [x] صفر `unused_import` و`unused_element` في `flutter analyze` — **وصفر إسكاتٍ
      لها**: أحد عشر `// ignore: unused_*` حُذفت مع ما كانت تُسكته (≈٦٠٠ سطر).
      ويحرسه اختبار يستثني ملفات `l10n` المُولَّدة وحدها.

---

### ~~APP-106~~ — 56 كتلة `catch (_) {}` صامتة، بعضها في مسارات حرِجة

**الأولوية:** P1 · **المنطقة:** التطبيق / معالجة الأخطاء · **الحالة:** ✅ **مُنفَّذ في الدفعة التاسعة عشرة** (`core/` و`data/` و`application/` محروسة؛ `presentation/` متروكة بقرار موثَّق) — انظر §15. النصّ أدناه يبقى كما وُجد.

**المشكلة**
البنية العامة لمعالجة الأخطاء قوية (`runZonedGuarded` + `FlutterError.onError` + `PlatformDispatcher.onError` + `ErrorWidget.builder` في `main.dart:17-77`، و`core/failures/app_failure.dart`، و`MajarraApiException` تحمل الرمز والحالة). لكن **56 كتلة استثناء فارغة في 21 ملفًا** تلتقط الأخطاء وتُهملها، وبعضها في مواضع تُنتج فشلًا صامتًا يظهر للمستخدم كسلوك غامض.

**الدليل** (عدّ فعلي: 56 مطابقة لـ`catch (_) {}` في `lib/`)
أخطرها:
- `core/crypto/file_crypto.dart:66-70` — فشل حفظ مفتاح التشفير (انظر `ENC-011`).
- `features/auth/data/auth_storage.dart:46-60` — فشل قراءة/كتابة/حذف التوكن.
- `features/home/data/majarra_api_client.dart:128-131` — فشل جلب الكتب يُخفى كليًا ويُعاد مصفوفة فارغة، فرفّ المكتبة يبدو فارغًا بدل معطوب.
- `features/downloads/data/download_repository.dart:172-174, 236-239` — فشل تنظيف ملفات التشغيل والتفكيك.

**السلوك المتوقع**
كل التقاط صامت يصبح إمّا فشلًا مُبلَّغًا (`AppFailure`) أو التقاطًا مع تعليق يشرح لماذا الإهمال صحيح هنا. القاعدة: الإهمال يحتاج تبريرًا مكتوبًا.

**الحجم** M

**معايير القبول**
- [ ] صفر `catch (_) {}` بلا تعليق مبرِّر في `core/` و`features/*/data/`.
- [ ] فشل جلب الكتب يُميَّز عن «لا كتب».
- [ ] فشل المخزن الآمن يصل إلى المستخدم (يشترك مع `ENC-011`).

---

### ~~APP-107~~ — تنازلات دورة الحياة وتوثيق يخالف الكود في بوابة ولي الأمر

**الأولوية:** P2 · **المنطقة:** التطبيق / الأمان · **الحالة:** ✅ **مُنفَّذ في الدفعة الرابعة والثلاثين**

**المشكلة**
إلغاء إثبات ولي الأمر مقصور على حالة `detached` بسبب مشكلة تركيز على الويب، فيبقى الوصول الأبوي حيًّا على الجوال أثناء وجود التطبيق في الخلفية حتى نهاية المؤقّت. والتعليق الذي يشرح ذلك يذكر مدة خمس دقائق بينما القيمة الفعلية **15 دقيقة**.

**الدليل**
`lib/app/majarra_app.dart:41-50` (التنازل موثَّق) مقابل `lib/app/router/auth_guard.dart:30` (`parentAccessDuration = Duration(minutes: 15)`).

**الأثر**
طفل يمسك الجهاز بعد أن فتح ولي الأمر منطقة الوالدين وأغلق التطبيق يملك نافذة 15 دقيقة، لا 5 كما يفترض قارئ الكود.

**الحجم** S

**معايير القبول**
- [x] الإلغاء يعمل على `paused` في الجوال، مع استثناء الويب بتعليق يشرحه —
      `!kIsWeb && paused` مع `detached` في كل مكان. و`inactive` مستثناة قصدًا:
      تُطلقها إشعارات النظام ومركز التحكّم على iOS والتطبيق في يد وليّ الأمر، وهي
      التي سبّبت حلقة شاشة الرمز على الويب أصلًا.
- [x] التعليق والقيمة متطابقان، واختبار يربطهما —
      `test/parent_access_lifecycle_test.dart` يستخرج كل مدّة بالدقائق من
      `majarra_app.dart` ويوازنها بـ`AuthGuard.parentAccessDuration`، فتعليقٌ
      يخالف الثابت يُفشل الجولة.

---

### ~~APP-108~~ — حاجز الاتجاه يعتمد على مطابقة نص المسار

**الأولوية:** P2 · **المنطقة:** التطبيق / التوجيه · **الحالة:** ✅ **مُنفَّذ في الدفعة الخامسة والثلاثين**

**المشكلة**
الاتجاه مقفول على portrait في `main.dart:22-36`، ثم يُضاف حاجز ثانٍ يقرّر السماح بالأفقي بمطابقة نصية على اسم المسار.

**الدليل**
`lib/app/majarra_app.dart:100-116` — `ModalRoute.of(context)?.settings.name?.contains('playback')`. مع `go_router` قد لا يكون `settings.name` مأهولًا، فالشرط هش وقد يفشل في الاتجاهين (منع الأفقي في المشغّل، أو السماح به خارجه).

**الحجم** S

**تصحيح: الشرط لم يكن هشًّا، كان مكسورًا في اتجاه واحد ثابت** (الدفعة 35)
`MaterialApp.router(builder:)` يعمل **فوق الـNavigator**، فلا `ModalRoute` أعلاه —
و`ModalRoute.of(context)` هناك تُعيد `null` **دائمًا**، لا «قد لا تكون مأهولة». فـ
`isVideo` كانت `false` في كل جولة، وأثرها **واحد ومحدَّد**: شاشة المُشغِّل تُغطّى
بدعوة «أدِر الجهاز» في الوضع الأفقي على هاتف مضغوط — وهو الوضع الذي يفتحه المُشغِّل
بنفسه في `initState` لمشاهدة الفيديو.

وعطلٌ ثانٍ لم يذكره البند: القرار كان معلَّقًا على `MediaQuery` وحده، فلا يُعاد
حسابه عند التنقّل.

**معايير القبول**
- [x] قرار الاتجاه يأتي من بيانات المسار في `go_router` لا من مطابقة نص —
      `RouteMatchList.fullPath` (نمط المسار الذي طابقه الموجّه) يُقابَل بمجموعة
      `landscapeCapableRoutes` المُعلَنة في `app_router.dart`. ويثبّت الاختبار
      **الافتراض نفسه**: أن `fullPath` يُعطي `'/playback/:episodeId'` لا الموقع.
- [x] اختبار يغطّي دخول المشغّل والخروج منه — ومعه: أي معرّف حلقة يعمل بلا تحليل
      نصّي، والحاجز يُعاد بناؤه على تغيّر المسار (`ListenableBuilder` على
      `routerDelegate`).

---

### ~~APP-109~~ — مهلة شبكة واحدة ثابتة 8 ثوانٍ لكل الطلبات

**الأولوية:** P2 · **المنطقة:** التطبيق / الشبكة · **الحالة:** ✅ **مُنفَّذ في الدفعة السادسة والثلاثين**

**المشكلة**
`_timeout = Duration(seconds: 8)` مطبَّقة على كل نداء في العميل، بما فيها العمليات الثقيلة (تصدير بيانات الحساب، جلسات الوسائط، الاستجابات الكبيرة) وعلى شبكات ضعيفة.

**الدليل** `lib/features/home/data/majarra_api_client.dart:85` وتطبيقها في `_getList`/`_getJson`/`_postJson` (`:1271-1355`).

**السلوك المتوقع**
مهلة لكل نوع عملية، وإعادة محاولة بتراجع أسّي للقراءات الآمنة، ورسالة مختلفة لانقطاع الشبكة عن بطئها.

**الحجم** S

**ما نُفِّذ** (الدفعة 36)
- **مهلتان مُصرَّحتان** لا واحدة: `_timeout` ثمانِ ثوانٍ للطلب الذي يقف المستخدم
  أمامه، و`_extendedTimeout` خمس وأربعون لعمليةٍ يعرف أنها ليست فورية. والموضعان
  الثقيلان الوحيدان في العميل — **تصدير بيانات الحساب كلّها**، و**رفع رسمة الطفل**
  — انتقلا إليها. وكل طلب يبقى محدودًا بمهلة، ويحرسه اختبار يعدّ الطلبات مقابل
  المهلات: الحدّ الغائب يُنتج انتظارًا أبديًّا (وهو ما كان في تحميل الترجمة قبل
  `APP-102`).
- **إعادة المحاولة بتراجع أسّي** (`retryBackoff`: ٤٠٠ ثم ٨٠٠ ثم ١٦٠٠ مللي)،
  للقراءات (`GET`) وحدها، وعلى **فشل النقل** وحده.
- **الرسالة تفرّق البطء عن الانقطاع** — كان موجودًا في `AppFailure`، وصار محروسًا
  باختبار: لو وحّد أحدٌ الرسالتين صار «الإنترنت مقطوع» جوابًا لشبكةٍ بطيئة.

**قرارٌ يخالف ظاهر المعيار: الإعادة مطلوبة صريحةً، لا افتراضًا**
`attempts: 1` هو الافتراض، أي أن السلوك العام لم يتغيّر. لأن مسار الكاتالوج له
**بديلٌ أرخص من الانتظار**: `ContentRepository` يقرأ الكاش ثم الحزمة عند الفشل.
فإعادة محاولةٍ هناك تُجلس الطفل أمام دوّارة سبع عشرة ثانية بدل أن يرى رفًّا محفوظًا
بعد ثمانٍ — أي أن «إعادة المحاولة للقراءات الآمنة» على عمومها كانت ستُسيء حيث يوجد
بديل.

فالإعادة مُعلَنة حيث **لا بديل**: `fetchDevices` (وليّ أمر يحاول سحب جهاز)،
و`getGooglePlayBillingContext` (تسبق دفعًا). وهناك بديلُ الفشل رسالةُ خطأ، فمحاولةٌ
ثانية أرخص منها.

**ما لا يُعاد، ولماذا**
- **أي حالة HTTP**: 4xx طلبٌ مرفوض يُرفَض ثانيةً، و5xx خادمٌ يترنّح تضاعف المحاولةُ
  حمله.
- **كل كتابة**: إعادة إرسالها قد تُنشئ طفلًا ثانيًا أو تخصم مرّتين. ومسارات الحذف
  تحمل مفتاح تكرار لكنها **مدمِّرة**، فلا تُعاد آليًّا بحال.

**دَينٌ مُعلَن**: `getAccountDeletionStatus` قراءةُ حالة لكنها تُنقَل بـ`POST` (تحمل
سرّ الإيصال في الجسم)، فلا تدخل في مسار الإعادة. القاعدة «‏`GET` وحده» أدقّ من
استثناءٍ يُكتب لها.

---

### ~~APP-110~~ — نواقص معروفة موسومة في الكود بانتظار الخادم

**الأولوية:** P2 · **المنطقة:** التطبيق · **الحالة:** ✅ **مُعالَج في الدفعة السابعة والثلاثين** — **والعنوان كان خطأً**: الخادم شحن العقد، والعميل يحلّله ويستخدمه؛ المُظلم **بيانات** (`CNT-101`) لا كود.

**تصحيح بالقياس** (الدفعة 37)

| ما قاله البند | ما هو فعلًا |
|---|---|
| «`previewSpriteUrl` و`introStartMs` غير موجودين على الخادم» | **موجودان**: `0047_episode_streaming_contract.sql` أضاف `intro_start_ms`/`intro_end_ms`/`recap_*`/`credits_start_ms`/`preview_sprite_url`/`preview_sprite_vtt_url`، و`routes/episodes.ts` يُرسلها، و`EpisodeDto.fromJson` يحلّلها |
| «فلا معاينة تمرير ولا تخطّي مقدمة حقيقي» | **مُنفَّذان في العميل**: `introRange` تُظهر زرّ «تخطي المقدمة» (`_showSkipIntro`/`_skipIntro`)، و`previewSpriteUrl` تُغذّي معاينة التمرير |
| «الجداول الثلاثة صفر صفًا» | **صحيح**: صفرٌ في `episode_renditions` و`episode_audio_tracks` و`episode_subtitle_tracks`، وصفرُ حلقةٍ لها `preview_sprite_url` أو `intro_start_ms`، من **٣٣ حلقة**. وللثلاثة CRUD كامل في `adminEpisodeStreaming.ts` |
| «شاشة إقران TV بلا أي منطق» | صحيح، **وسلوكها سليم**: تعرض «خدمة الاقتران غير متاحة بعد» و`— — —` بدل رمز ثابت يبدو صالحًا. تبقى مع `API-103` |

فالصياغة «بانتظار الخادم» خاطئة في نصفها الأوّل: **الخادم واللوحة والعميل جاهزون،
والغائب محرّرٌ يضع علامة مقدمة أو يرفع مسار جودة**. وهو `CNT-101` بعينه.

**ما نُفِّذ**
حُذف تعليقٌ في `playback_page.dart` كان يسرد الحقول ويطلب إضافتها «عندما يشحنها
الخادم» — وقد شُحنت وحُلِّلت. وذلك التعليق أسوأ من غيابه: يقرؤه المطوّر فيستنتج أن
الميزة غير مُنفَّذة، فيعيد بناء ما هو مبنيّ، أو يخبر صاحب المنتج أنها «تنتظر الخادم»
وهي تنتظر محرّرًا. ومعه صُحِّح تعليق `[AC7]`.

ويحرس ذلك `test/streaming_contract_test.dart` (8): كل حقل من العقد يُحلَّل كما
يُرسَل · مدى المقدمة يُشتقّ · **ومدًى مقلوب لا يُشتقّ** (بيانات محرِّر خاطئة لا تُنتج
زرًّا يقفز إلى الخلف) · وحلقةٌ بلا أي من هذه الحقول تبقى صالحة للتشغيل — وهي حالة
الإنتاج اليوم · ولا يعود التعليق المُضلِّل.

**ما بقي، بأصحابه**
- **تعبئة البيانات** (علامات المقدمة، مسارات الجودة والصوت والترجمة، أوراق
  المعاينة) → `CNT-101`، وهي عمل تحرير على لوحةٍ جاهزة.
- **إقران التلفزيون** → `API-103` (لا نقطة نهاية)، والشاشة تفشل بصدق حتى ذلك.
- **نقل الأحداث التحليلية** (`analytics.dart`: `TODO(backend)`) → قرار مزوّد،
  مرتبط بـ`OPS-106`/`PRIV-101`. الشكل الآمن للحدث مكتوب، والناقل وحده غائب.
- **فحوص الإقليم ونافذة العرض** placeholders تعتمد 451/403 من الخادم — وهذا
  **تصميم صحيح** لا نقص: السلطة للخادم، والعميل يعرض ما يقوله.

**الدليل**
- `playback_page.dart:115-118` — `Backend TODO`: `previewSpriteUrl` و`introStartMs` غير موجودين على الخادم، فلا معاينة تمرير ولا تخطّي مقدمة حقيقي.
- `playback_page.dart:384-388` — فحوص الإقليم ونافذة العرض placeholders، والاعتماد على 451/403 من الخادم.
- `features/tv/presentation/tv_pairing_page.dart:12-16` — شاشة إقران TV بلا أي منطق (السبب في `API-103`).
- لا اختيار جودة، ولا مسارات صوت، ولا ترجمات في المشغّل — والجداول الثلاثة المقابلة (`episode_renditions`، `episode_audio_tracks`، `episode_subtitle_tracks`) صفر صفًا.

**الحجم** M (تابع لـ`API-103` و`CNT-101`)

---

### 8.5 ما ثبت أنه سليم في هذه المنطقة

| البند | الدليل |
|---|---|
| التطبيق يُبنى ويحلّل بلا أخطاء | `flutter analyze` صفر أخطاء (43 تحذيرًا/info) |
| قائمة سماح صارمة لعنوان الـAPI | `core/env/app_environment.dart:140-215` — ترفض السكيمات غير المتوقعة والـcredentials في الرابط وplain http لغير loopback وأي مضيف خارج القائمة |
| تدوير توكن مُسلسَل يجمع الطلبات المتوازية | `majarra_api_client.dart:1379-1401`, `1429-1467` — ومحو الجلسة فقط عند 401 على الـrefresh نفسه |
| PIN أبوي بـPBKDF2-HMAC-SHA256 مع verifier وsalt لكل جهاز | `features/auth/data/pin_kdf.dart`, `parent_pin_store.dart` — والبصمة طبقة راحة لا سلطة تخويل |
| سياج دورة حياة لمدير التنزيل | `download_manager.dart:79-141` — generation fence وإلغاء التدفقات وإغلاق العميل قبل عودة أول future |
| خطوط مُبندلة بلا نداء خارجي عند الإقلاع | `pubspec.yaml:53-73` مع مصدر الترخيص |
| سياسة أصول صارمة تمنع تضخّم الحزمة | `pubspec.yaml:76-135` — إعلان غير تعاودي مقصود وR2-first |
| دعم TV/remote حقيقي | `majarra_app.dart:90-98` (`select`/`gameButtonA` → `ActivateIntent`)، `FocusableScale`، `InputModeTracker`، `NumericFocusOrder` في الرئيسية |

---

## §9 لوحة التحكم — `ADM-*`

### ADM-101 — 508 نقطة نهاية خلفية مقابل 123 مسار واجهة، وثلاثة موجّهات بلا أي شاشة

**الأولوية:** P1 · **المنطقة:** اللوحة / التغطية · **الحالة:** PARTIAL

**المشكلة**
قدرات مبنية ومحروسة على الخادم لا يمكن الوصول إليها من أي شاشة، فتبقى إمّا معطَّلة عمليًا أو مُستخدَمة بنداءات يدوية.

**الدليل** (عدّ فعلي)
- الخادم: **508** تسجيل مسار في `dashboard/api/src/routes/*.ts` (69 موجّهًا).
- الواجهة: **123** `<Route>` في `dashboard/front/src/AdminRoutes.tsx`، و118 ملف صفحة.
- موجّهات كاملة بلا شاشة مقابلة: `adminBackup.ts` (**3** نقاط: النسخ الاحتياطي والاستعادة)، `adminAvailability.ts` (**4** نقاط: الإتاحة والإقليم)، `adminPublishGate.ts` (**1**). البحث في `AdminRoutes.tsx` عن `backup` و`availability` و`publish` لا يعطي أي نتيجة.

**الأثر**
النسخ الاحتياطي والاستعادة — أخطر عملية تشغيلية — بلا واجهة، أي أنها تُنفَّذ بـ`curl` أو لا تُنفَّذ. والإتاحة الإقليمية (`content_availability` = صف واحد فقط) بلا شاشة تشرحها، بينما مسار التشغيل يرفض بـ451 اعتمادًا عليها.

**السلوك المتوقع**
شاشة لكل قدرة تشغيلية حرِجة، أو قرار موثَّق بأن هذه النقاط للاستخدام البرمجي فقط مع سكربت مُوثَّق يستدعيها.

**الحجم** L

**تصحيح الأرقام وطريقة العدّ** (الدفعة 38)

| القياس | البند | المُقاس فعلًا |
|---|---|---|
| نقاط النهاية | 508 | **497** (`node tools/dashboard-audit/feature-matrix.mjs` → `serverRoutes`). ~~370~~ في الدفعة 38 كان **خطأً**: طريقة العدّ طابقت موجّهًا اسمه `route` حرفيًّا فقط، فأسقطت `adminUsersRoute` و`familyRoute` و`episodesRoute` وغيرها — فالرقم الأصلي 508 كان أقرب |
| الموجّهات | 69 | **70** |
| مسارات الواجهة | 123 | **124** |
| ملفات الصفحات | 118 | **118** ✅ |

**وطريقة الدليل نفسها كانت معطوبة.** البند استنتج «بلا أي شاشة» من أن البحث في
`AdminRoutes.tsx` عن `availability`/`publish` لا يعطي نتيجة. لكن هذه السطوح
**تبويبات وحوارات داخل مساحات العمل** لا مسارات عليا:

| الموجّه | الحكم | الواقع |
|---|---|---|
| `adminAvailability.ts` | «بلا شاشة» | **له شاشة**: `AvailabilityPanel` مركّب في خمس مساحات عمل (سلسلة، حلقة، قصة، كتاب، كوكب) ويستخدم ثلاثًا من نقاطه الأربع |
| `adminPublishGate.ts` | «بلا شاشة» | **مُستخدَم بكثافة**: `QualityPage` و`PublishReadinessDialog` (السلاسل والحلقات) و`PublishReadinessPanel` (الألعاب) |
| `adminBackup.ts` | «بلا شاشة — أخطر عملية» | ثلاث نقاط: `GET /backup/:type/:id` **بلا مُستهلِك** · `POST /restore` يُعيد **501 صراحةً** بقرار سابق (كان يكذب بنجاح استعادة لم تحدث) · `GET /quality/:type/:id` **بلا مُستهلِك** (تجاوزته `publish-readiness` الموحّدة) |

**معايير القبول**
- [ ] ~~شاشة نسخ احتياطي/استعادة تنفّذ استعادة خلف تأكيد~~ → **أُعيد تحديده**:
      الاستعادة **غير منفَّذة في الخادم** (501 بقرار موثَّق). وبناء زرّ لها اليوم هو
      بالضبط العطل الذي يُصلحه هذا الأودت في كل دفعة: واجهة تَعِد بما لا يحدث.
      المطلوب أوّلًا تنفيذ الاستعادة خادميًّا، ثم شاشتها. ويبقى `GET /backup`
      (يعمل) بلا مُستهلِك — بندٌ صغير مفتوح.
- [x] شاشة إتاحة تعرض الأقاليم ونوافذ العرض — **كانت موجودة لكل عنصر**
      (`AvailabilityPanel`)، وأُضيفت في الدفعة 38 الشاشةُ التي تُجيب السؤال المعاكس:
      `availability` تسرد **كل قيد مضبوط على المنصّة** مع نمطه ودوله ونافذته وسببه.
      وكانت النقطة `GET /admin/availability` بلا أي مُستهلِك، والسؤال «ما المحجوب
      وأين؟» لا يُجاب إلا باستعلام D1 — ومسار التشغيل يرفض بـ451 اعتمادًا عليه.
- [ ] لكل نقطة نهاية إمّا شاشة أو تصنيف «برمجي فقط» في وثيقة واحدة — **باقٍ**، وهو
      الشطر الأكبر (**497** نقطة، لا 370: انظر التصحيح في الجدول أعلاه). ويحتاج
      أداةً تعدّ وتُقابل لا وثيقةً تُكتب بيدٍ، وإلا تقادمت كما تقادم هذا البند.
      **والأداة موجودة**: `tools/dashboard-audit/feature-matrix.mjs` تقابل بالفعل
      كل نداء عميل بموجّه خادم، وصارت تعمل صحيحًا ومربوطةً بـCI في الدفعة 42 —
      فالباقي هو الاتجاه المعاكس: نقاطٌ لا ينادِيها أي عميل.

---

### ~~ADM-102~~ — التصاريح مفروضة في الخادم و**غائبة عن الواجهة** في 101 من 118 صفحة

**الأولوية:** P1 · **المنطقة:** اللوحة / التصاريح / التجربة · **الحالة:** ✔ منجَز (الدفعة 39)

**المشكلة**
`hasPermission` مستخدمة في **17 ملفًا فقط** (47 استدعاءً) من أصل 240 ملف TS/TSX و118 صفحة. بقية الشاشات تُفتح لأي حساب مُصادَق، ثم تفشل نداءاتها بـ403 من الخادم.

**الدليل**
- `dashboard/front/src/lib/adminSession.ts` يعرّف `hasPermission(action)`؛ و17 ملفًا فقط يستدعيها (منها `Sidebar`, `CommandPalette`, `PlanetsPage`, `StoriesPage`, `TaxonomyPage`, `VisualStylesPage`, `BooksPage`, `GamesPage`).
- الخادم يفرض بجدية: **196** استدعاءً لـ`requirePermission` في 36 موجّهًا إداريًا.

**السلوك الحالي**
مراجع لغوي يرى قائمة كاملة بشاشات لا يملكها، يفتحها، يملأ نموذجًا، ثم يفقد عمله على 403. الأمان سليم؛ التجربة هي المكسورة، والنتيجة العملية أن المستخدمين يتعلّمون تجاهل الأخطاء.

**السلوك المتوقع**
حجب على مستوى المسار (حرس في `AdminRoutes`) لا على مستوى كل زر، مع قائمة جانبية تُخفي أو تُعطِّل غير المسموح وتشرح السبب.

**نطاق التنفيذ**
مكوّن `RequirePermission` يغلّف مسارات `AdminRoutes`؛ خريطة مسار→صلاحية مشتقّة من نفس أسماء صلاحيات الخادم؛ اختبار يمنع إضافة مسار بلا صلاحية.

**الحجم** M

**معايير القبول**
- [x] فتح مسار بلا صلاحية يعرض شاشة «لا تملك صلاحية» لا نموذجًا يفشل لاحقًا. (`PermissionGate` في `AdminLayout`، والصلاحية المطلوبة مسمّاة في الرسالة)
- [x] القائمة الجانبية تعكس صلاحيات الحساب. (**مشتقّة** من نفس الخريطة بعد حذف الحقل اليدوي الذي خالفها في خمسة مواضع)
- [x] اختبار يفشل إن أُضيف مسار إداري بلا صلاحية معلَنة. (ومعه العكس: مفتاحٌ بلا مسار)

**ما نُفِّذ خلافًا للنطاق المقترح:** الإنفاذ **نقطة واحدة** لا تغليفًا لكل مسار — مئة وثلاثة وعشرون موضعًا تعني أن المسار التالي يُكتب بلا حرس ولا يلاحظه أحد، لأن غياب الحرس يظهر كشاشة تعمل.

---

### ADM-103 — لوحة الأوامر تعرض أوامر إنشاء لمن لا يملك صلاحية الإنشاء

**الأولوية:** P1 · **المنطقة:** اللوحة / التصاريح · **الحالة:** REGRESSION · **مكشوف باختبار فاشل**

**المشكلة**
حساب بدور `review` يرى أمر «سلسلة جديدة» في لوحة الأوامر، مع أن الاختبار المكتوب لهذا الغرض ينص أن المراجع يرى «مراجعة محتوى جديدة» فقط.

**الدليل**
`dashboard/front/src/test/paletteCalendar.test.tsx:71` **فاشل**: `expect(element).not.toBeInTheDocument()` والعنصر الموجود `<span class="palette__label">سلسلة جديدة</span>`.

**الأثر**
`CommandPalette` من الملفات الـ17 التي **تستخدم** `hasPermission`، ومع ذلك سقط الحجب فيها. أي أن الخلل ليس «لم نكتب الحجب» بل «الحجب مكتوب ولا يعمل» — وهو أخطر لأن المراجعة البشرية تراه محميًّا.

**الحجم** S

**معايير القبول**
- [ ] الاختبار أخضر.
- [ ] لكل أمر في اللوحة صلاحية معلَنة، ولا أمر بلا صلاحية.

---

### ADM-104 — التخطيط المقترح لا يتبع دور الحساب

**الأولوية:** P2 · **المنطقة:** اللوحة / التجربة · **الحالة:** REGRESSION · **مكشوف باختبار فاشل**

**الدليل**
`dashboard/front/src/test/dashboardPresets.test.tsx:112` فاشل: `content_manager` أنتج `support` بدل `content`. الأدوار المجاورة في نفس الاختبار تنجح، فالخلل في مطابقة دور واحد أو في ترتيب الأولوية.

**الأثر** مدير محتوى يفتح لوحة موجَّهة للدعم؛ إزعاج لا خطر.

**الحجم** XS

---

### ~~ADM-105~~ — 29 صفحة من 118 تتخطّى فحص الأنواع بـ`@ts-nocheck`

**الأولوية:** P1 · **المنطقة:** اللوحة / الجودة · **الحالة:** ✔ منجَز (الدفعة 40)

**المشكلة**
`tsc --noEmit` نظيف — لكن ربع صفحات اللوحة معلَّق خارج الفحص، فالنتيجة الخضراء تصف 89 صفحة لا 118.

**الدليل** عدّ فعلي: **29** ملفًا في `dashboard/front/src/pages/*.tsx` يحمل `@ts-nocheck`. وCI يشغّل `npx tsc --noEmit -p tsconfig.app.json` (`ci.yml:64`) فيمرّ.

**الأثر**
أخطاء الأنواع في هذه الصفحات تظهر فقط في المتصفح عند التشغيل. وهي غالبًا الصفحات الأكثر تعقيدًا، أي الأحق بالفحص.

**نطاق التنفيذ**
إزالة `@ts-nocheck` صفحة بصفحة مع إصلاح الأخطاء؛ ثم قاعدة lint تمنع إعادته؛ يمكن التدرّج على دفعات مع بوابة تمنع الزيادة.

**الحجم** L

**معايير القبول**
- [x] صفر `@ts-nocheck` في `src/pages`. (والفحص صار يشمل الـ118 صفحة: 48 خطأً ظهرت، كلّها مُصلَحة)
- [x] قاعدة أو اختبار يفشل إن أُضيف مرة أخرى. (`src/test/typeSuppression.test.ts` على **كل** `src`، ومُتحقَّقٌ أنه يفشل فعلًا لا أنه أخضر)

**ما ظهر خلافًا للتقدير:** الحجم كان أصغر من **L** بكثير — **عشرة** من الـ29 ملفًا لم يكن فيها خطأ واحد: تعطيلٌ نُسخ بلا سبب. وما كان تحت البقية ليس أسلوبًا: نداءان زائدان إلى الإنتاج في كل فتح صفحة، ونداءٌ ثالث بمُعرَّف مُختلق، وشرطٌ أبدًا غير صحيح يُفرغ مسار ٩–١٢، ورسالة خطأ غير موجودة تجعل الفشل يظهر كصفحة فارغة ناجحة، ورقمُ جلسات **مُختلَق** في تأكيد عملية أمنية.

---

### ADM-106 — شاشات تقرأ جداول فارغة، فتعرض صفرًا يُقرأ كـ«لا مشاكل»

**الأولوية:** P2 · **المنطقة:** اللوحة / الرصد · **الحالة:** PARTIAL — **الدليل نُقض والبند أُعيد تحديده** (الدفعة 41)

> **تصحيح (الدفعة 41).** دليل هذا البند كان عدد الصفوف، وهو قياسٌ للشيء الخطأ: جدولٌ فارغ ليس جدولًا بلا كاتب. بقياس الكاتب (`INSERT INTO`) تبيّن أن **أحد عشر من الاثني عشر لها كاتب** يبلغه إنسان من موجّه إداري، و`ops_health_checks` يكتبه جدول كل خمس دقائق مربوطٌ فعلًا — فصفرها **صادق**: «لم يفعلها أحد بعد». والوحيد بلا كاتب `translation_units`، وشاشته لا تعرض أصفارًا لأن `adminTranslation.ts` يتفرّع صريحًا ويحسب الطابور من `story_page_localizations`. والمثال الذي بُني عليه البند («صفر حوادث مقابل 24 حدثًا فاشلًا») كان **قد أُصلح في `OPS-106`**.
>
> والمبدأ نفسه كان مكسورًا في موضع آخر: `/admin/ops/overview` كانت عدّاداته تبدأ من `0` وكل استعلام في `catch {}` فارغة، فقراءةٌ فاشلة تُشحَن كصفر و`overall_health` يبقى `'healthy'` — سلامةٌ مُعلَنة على فشلٍ صامت. أُصلح على الطرفين مع سبب مُسمّى لكل مسبار فاشل.

**المشكلة**
عدة شاشات مبنية على جداول لا يكتبها شيء بعد، فتعرض حالة فارغة لا تُميَّز عن حالة سليمة.

**الدليل** (استعلام D1 المحلية)
`support_tickets` = 0 · `campaigns` = 0 · `workflow_runs` = 0 · `content_factory_runs` = 0 · `rights_licenses` = 0 · `ops_alerts` = 0 · `ops_incidents` = 0 · `ops_health_checks` = 0 · `access_grants` = 0 · `questions` = 0 · `translation_units` = 0 · `plan_pricing` = 0 — مقابل بنية كاملة لكل منها (12 خدمة معرَّفة، 6 سياسات SLA، 12 دورًا، 22 صلاحية).

**الأثر**
مركز العمليات يعرض «صفر حوادث» بينما `failed_family_events` = 24. الفرق بين «لا يوجد» و«لا نعرف» غير ظاهر للمشغّل. والاختبارات نفسها تعرف هذا المبدأ وتفرضه في مكان واحد فقط («an unavailable metric prints a dash and its reason, never a zero» في `dashboardPresets.test.tsx`) — المطلوب تعميمه.

**السلوك المتوقع**
كل مقياس بلا مصدر بيانات حقيقي يُعرض كشرطة مع سبب («لا يوجد كاتب لهذا الجدول بعد»)، لا كصفر.

**التبعيات** API-105، OPS-106.

**الحجم** M

**معايير القبول**
- [x] لا شاشة تعرض `0` لمقياس لا يوجد له كاتب. **المعيار لاغٍ كما كُتب**: أحد عشر جدولًا من الاثني عشر له كاتب، فصفره قياسٌ صادق. وما حلّ محلّه ومُنجَز: لا شاشة تعرض `0` لمقياس **تعذّرت قراءته**.
- [ ] كل حالة فارغة تفرّق بين «لا نتائج» و«لا مصدر». مُنجَز في `/ops/overview` طرفًا لطرف؛ ويبقى **85 موضع `?? 0`** في 32 ملفًا في `src/pages` — وأكثرها صادق، فسحبها بالجملة يقلب العطل إلى ضدّه (شرطةٌ مكان صفرٍ حقيقي). تحتاج قاعدةً تُميّز المصدر القابل لأن يكون مجهولًا ثم مرورًا موضعًا بموضع.

---

### ~~ADM-107~~ — مكوّن `NotImplementedPage` ميت

**الأولوية:** P3 · **المنطقة:** اللوحة / النظافة · **الحالة:** ✔ منجَز (الدفعة 42) — **والحجم كان XS والأثر لم يكن**

> حذف المكوّن سطران. لكن اسمه كان **دليلًا** في أربع وثائق على أن ثماني وجهات «غير مُنفَّذة»، وكان حُجّةَ استثناءٍ معماريّ (تسع صفحات غير `lazy`). وتتبُّع ذلك كشف أن `tools/dashboard-audit/feature-matrix.mjs` — المولِّد المكتوب لمنع تقادم الأوصاف — كان **يحلّ صفرًا من 123 صفحة** بعد أن صارت الصفحات `lazy`، فنصفه الاستدلاليّ فارغٌ وهو يُقرأ كدليل، و`--check` فيه **لم يكن مربوطًا بـCI**. أُصلح المُحلِّل (163 مشكلة → صفر) ورُبط `--check`.

**الدليل** `dashboard/front/src/components/PageState.tsx:92` هو الظهور **الوحيد** للاسم في كل الواجهة، أي أنه معرَّف وغير مستخدَم.

**الملاحظة المهمة**
هذا يُبطل ما يقوله `docs/FEATURE_MATRIX.md` و`النواقص_المتبقية.md:5.1` من وجود «8 وحدات MISSING تعرض `NotImplementedPage`». لم تبقَ أي شاشة كذلك: `campaigns` و`revenue` و`translation` و`quiz` و`recommendations` و`school` و`finance-advanced` و`ops-sla` كلها مسارات حقيقية اليوم في `AdminRoutes.tsx`. **وقد صُحّحت الوثائق الأربع في الدفعة 42**، وصارت حالة الثمانية `PARTIAL` لا `COMPLETE` (النداءات موجودة، والاكتمال لم يُدقَّق).

**الحجم** XS (والأثر L: مولِّد الجدول كان عاطلًا وحرسه غير مُشغَّل)

**ما أُنجز**
- [x] حذف المكوّن، وتصحيح الوثائق الأربع التي استشهدت به.
- [x] تسع صفحات كانت ساكنة بحُجّته صارت `lazy`: حزمة `AdminRoutes` من **297.91 ك.ب إلى 152.82**.
- [x] إصلاح ثلاث علل في `feature-matrix.mjs`: شكل `lazy`، و`import X, { a }` في خريطة التركيب (ثلاثة موجّهات مركَّبة كانت غير مرئية)، ودالّة عميل مسارها من صفّ الحدث (كانت تُتَّهم بأنها غير مُعرَّفة).
- [x] ربط `--check` وإعادة التوليد بـCI، وإزالة تاريخ التوليد من الناتج لئلّا يفشل الحرس بالساعة.
- [ ] **39 مسارًا `UNVERIFIED`** بلا حُكم مسجَّل — حُكمٌ بشريّ لكل مسار، لا استنتاجٌ من تعبير نمطي.

---

### 9.5 ما ثبت أنه سليم في هذه المنطقة

| البند | الدليل |
|---|---|
| RBAC خادمي جدّي وفاشل مغلقًا | 196 استدعاءً لـ`requirePermission` في 36 موجّهًا؛ `lib/adminAuth.ts:196-222` يبني نطاق المورد ويرفض بلا مستخدم |
| تصادم المسارات مُغطّى باختبار على التطبيق المُركَّب | ثلاث مجموعات تُحمّل `src/index.ts` فعلًا (`entrypoint`, `homeBuilderE2E`, `rateLimit`)، و`entrypoint.test.mjs:176,194` يؤكّد حلّ `/admin/games/ops` — وهي العلّة التي كسرت `/admin/games/*` و`/admin/billing/*` سابقًا |
| الجلسة لا تُمسح على 429 أو 5xx | `lib/adminSession.ts:verifySession` — 401/403 فقط تعني «خارج الجلسة»، والتعليق يوثّق أن الاجتياح السابق كان يرمي المسؤول لشاشة الدخول بعد ست شاشات |
| لا بيانات وهمية في صفحات الإنتاج | كل نتائج `mock|fakeData|placeholderData` محصورة في `src/test/**` |
| تحميل مؤجَّل لكل الصفحات وحدود Suspense | `AdminRoutes.tsx` — 123 مسارًا كلها `lazy` |
| اختبارات سلوكية حقيقية لا لقطات | 303 اختبارًا تفحص التصاريح والحالات الفارغة وسلوك الفلاتر وإمكانية الوصول (`axe-core` في `contentFactory.test.tsx`) |

---

## §10 المحتوى والوسائط والنشر — `CNT-*`

> كل رقم في هذا القسم من استعلام على D1 المحلية (2026-08-23)، وكل استعلام قابل لإعادة التشغيل. الإنتاج غير مُتحقَّق منه.

### CNT-101 — صفر أصل فيديو، فصفر حلقة قابلة للتشغيل — مع 20 حلقة معروضة كمنشورة

**الأولوية:** P0 · **المنطقة:** المحتوى / الإنتاج · **الحالة:** MISSING

**الدليل**
- `content_assets` حسب النوع: **audio = 30 · image = 169 · video = 0** (الإجمالي 199).
- الاستعلام بنفس predicate الكود (`routes/episodes.ts:64-80`) → **0 حلقة قابلة للتشغيل**.
- `episode_renditions` = 0 · `episode_audio_tracks` = 0 · `episode_subtitle_tracks` = 0.
- 33 حلقة لها صفوف في `asset_links`، لكن لا واحد منها من نوع `video`.

**الأثر**
كل زر «شاهد» في التطبيق يفشل. ولأن 20 حلقة **منشورة**، الطفل يراها في الرفوف ويضغطها ويحصل على خطأ — وهذا أسوأ من عدم ظهورها.

**نطاق التنفيذ**
إنتاج/استيراد الفيديو، ثم ربطه بـ`asset_links` بدور `stream`، ثم إعادة تقييم بوابة النشر (`API-106`) قبل النشر لا بعده.

**التبعيات** HUMAN-102 (إنتاج فني)، API-106، DECIDE-101 (DRM قبل أي محتوى مرخَّص).

**الحجم** XL (إنتاج، لا كود)

**معايير القبول**
- [ ] الاستعلام أعلاه يعيد عددًا يساوي عدد الحلقات المنشورة. — **إنتاجٌ لا كود: قرار مالك.**
- [x] لا حلقة منشورة بلا أصل `stream` جاهز. **للمستقبل مضمون** (الدفعة 43): مسار نشر الحلقات واحدٌ (`publishEntity(c,'episode')`) والبوابة تحجب حلقةً بلا فيديو (`publishGate.ts:598`). **وللماضي مرئيّ**: `GET /admin/publish-readiness/sweep` تُعيد تشغيل البوابة على كل صفّ منشور — لأن البوابة تعمل لحظةَ النشر ولا تُعاد — ولها قسم في `QualityPage`.
- [ ] تشغيل حلقة واحدة من طرف إلى طرف على جهاز حقيقي، بدليل. — **قرار مالك.**

**ملاحظة قياس (الدفعة 43)** رقم «20 حلقة منشورة» حقيقةٌ **إنتاجية** لا يمكن تأكيدها محليًّا: قاعدة البيانات المحلية فيها 33 حلقة كلّها `draft` وصفرٌ لها رابط فيديو. وتأكيده فعلُ مالكٍ مُعلَّق (تشغيل الأدوات بـ`--remote`).

---

### CNT-102 — صفر قصة وصفر كتاب منشور، وقصتان فقط مكتملتان فنيًّا

**الأولوية:** P0 · **المنطقة:** المحتوى / النشر · **الحالة:** PARTIAL

**الدليل**
- القصص: **0 منشورة** · 12 بحالة `ready` · 3 `draft` · 8 `archived`.
- الكتب: **0 منشورة** · 22 `draft`.
- قصص مكتملة الرسوم لكل صفحاتها: **2**. قصص مكتملة السرد العربي لكل صفحاتها: **2**.
- مسار النشر متاح الآن: `routes/adminPublish.ts:136-139` ينشر القصص والكتب والألعاب والمشروعات ببوابة جهوزية تفشل مغلقة.

**الأثر**
رفّ القصص والمكتبة فارغان تمامًا للطفل. الفرق عن الأودت السابق أن السبب لم يعد «لا يوجد مسار نشر» — المسار موجود — بل أن **12 قصة تقف عند `ready` ولم يُنقر نشرها، وعشرة منها لن تعبر البوابة أصلًا** لنقص الرسوم أو السرد.

**نطاق التنفيذ**
نشر القصتين الجاهزتين فورًا (قرار محتوى لا كود)؛ وإكمال الرسوم والسرد للعشر الباقية؛ ثم الكتب بعد `API-107`.

**الحجم** M (نشر) + XL (إنتاج)

**معايير القبول**
- [ ] القصتان المكتملتان منشورتان وتظهران للطفل. — **قرار محتوى على الإنتاج، لا كود.**
- [x] كل قصة `ready` لها تقرير بوابة يوضح ما ينقصها بالاسم. (الدفعة 44) `GET /admin/publish-readiness/sweep?type=story&status=ready` يُعيد تشغيل **البوابة نفسها** على كل صفّ ويُعيد حواجبه مسمّاة، وله قسم في `QualityPage`. وكانت القدرة غائبة: النقطة الأخرى تفحص عنصرًا واحدًا، والشاشة تفحص عيّنة من ثمانية أيًّا كانت حالتها.

**ملاحظة قياس (الدفعة 44)** أرقام البند **إنتاجية**: القاعدة المحلية فيها 8 قصص `writing` و13 كتابًا `draft` و**صفر قصة `ready`**. وتأكيدها فعلُ مالكٍ مُعلَّق.

---

### CNT-103 — 178 من 194 صفحة قصة بلا رسم، و16 سردًا فقط من 194

**الأولوية:** P0 · **المنطقة:** المحتوى / الإنتاج · **الحالة:** PARTIAL

**الدليل**
`story_pages` = 194 · بلا `image_asset_id` = **178** · صفوف `story_page_localizations` = 194 · منها ما له `narration_asset_id` = **16**. قصص لها أي سرد = **2**.

**التقدّم مقابل الأودت السابق**
الأودت السابق قال «صفر صوت وكل الـ194 صفحة بلا سرد». اليوم 30 أصل صوتي و16 صفحة مسرودة — أي أن `feat: add ACT-S2 story assets and narration import` (HEAD) أنجز شيئًا حقيقيًا. النسبة ما زالت 8% من الصفحات.

**الحجم** XL (إنتاج)

**معايير القبول**
- [ ] لكل قصة تُنشَر: 100% من صفحاتها لها رسم وسرد عربي. — **XL إنتاج: قرار مالك.**
- [x] تقرير جهوزية لكل قصة يُنتَج آليًا من الاستعلام أعلاه. (الدفعة 45) وهو **أدقّ** من الاستعلام المقترح: `evaluateStory` تفصل «بلا رسم» عن «رسمها غير جاهز» — وأصلٌ مرتبطٌ غير جاهز يُقرأ في الاستعلام كأنه رسمٌ موجود — وتُرجع المواضع بالاسم («صفحة 3: بلا رسم»). ويُقرأ للدفعة كلّها عبر `sweep?type=story&status=ready`، **وصار العرض يُظهر العدد والمواضع** بعد أن كان يعرض اسم الفحص وحده.

**ملاحظة قياس (الدفعة 45)** أرقام البند **إنتاجية**: القاعدة المحلية فيها صفر صفحة قصة وصفر ترجمة و14 أصلًا صوتيًّا.

---

### CNT-104 — 14 أصلًا صوتيًا يتيمًا: موجودة في المخزن وغير مرتبطة بأي محتوى

**الحالة:** PARTIAL — معيار التقرير مُنجَز (الدفعة 46)، والتصنيف قرار محتوى.

**الأولوية:** P1 · **المنطقة:** المحتوى / سلامة البيانات · **الحالة:** DISCONNECTED

**المشكلة**
30 أصلًا صوتيًا في `content_assets`، و**صفر** منها في `asset_links`، و16 فقط مرجَّعة عبر `story_page_localizations.narration_asset_id`. الباقي (14) غير مرتبط بأي كيان بأي مسار.

**الدليل**
`SELECT COUNT(*) FROM asset_links al JOIN content_assets ca ON ca.id=al.asset_id WHERE ca.kind='audio'` = **0**؛ و`COUNT(DISTINCT narration_asset_id)` = 16؛ و`COUNT(*) FROM content_assets WHERE kind='audio'` = 30.

**الأثر**
تكلفة تخزين وإنتاج بلا استخدام، ولا طريقة لمعرفة ماذا كان مقصودًا بها. والأهم أنه يكشف أن **مسارَي ربط مختلفين** يتعايشان (`asset_links` للحلقات، وعمود مباشر للقصص)، فأي تقرير «أصول غير مستخدمة» يجب أن يعرف الاثنين أو يعطي نتيجة خاطئة.

**الحجم** S

**معايير القبول**
- [ ] كل أصل صوتي إمّا مرتبط بكيان أو مُصنَّف صراحةً كأرشيف. — **قرار محتوى على الإنتاج**، وصار له الآن ما يسبقه: قائمةٌ صحيحة بما هو يتيم حقًّا.
- [x] تقرير «أصول غير مستخدمة» يفحص كلا مساري الربط. (الدفعة 46) **وهما ستة عشر لا اثنان**: 16 مفتاحًا أجنبيًّا في 13 جدولًا. و`GET /admin/assets/unreferenced` **يكتشفها من كتالوج SQLite** في كل نداء لا من قائمة مكتوبة، فعمودٌ جديد يُغطّى لحظةَ وصول ترحيله؛ ويرفض بـ500 إن لم يكتشف مسارًا (تقريرٌ بلا مسارات يسمّي كل أصل يتيمًا، وهي دعوةٌ إلى حذفٍ جماعي)؛ وله قسم في `MediaLibraryPage` **يعرض عدد المسارات المفحوصة** لأن تغطيةً غير مرئية هي كيف صار «مسارَان» وصفًا مقبولًا لثلاثة عشر.

**تصحيح المقدار (الدفعة 46)** «مسارَا ربط» كان وصفًا ناقصًا: القياس على المخطَّط أعطى **16 عمودًا في 13 جدولًا**. وتقريرٌ يُبنى على وصف البند يبقى خاطئًا — في الاتجاه الذي يُحذَف فيه محتوى مستخدَم. وبتشغيل الاستعلام على D1 المحلية: 14 أصلًا صوتيًّا و**87 صورة** بلا إشارة (وأرقام الإنتاج فعلُ مالكٍ مُعلَّق؛ المحلية بلا صفحات قصص).

---

### CNT-105 — 36 من 39 لعبة منشورة بلا تعليمات، و21 بلا هدف تعليمي

**الأولوية:** P1 · **المنطقة:** المحتوى / الألعاب · **الحالة:** PARTIAL

**الدليل**
`SELECT ... FROM games WHERE status='published'` → **39 منشورة · 36 بلا `instructions_ar` · 21 بلا `learning_objective_id`**. و`game_engines` = 17 محرّكًا مسجَّلًا.

**الأثر**
طفل في الثالثة أو الرابعة يفتح لعبة بلا تعليمات ولا صوت يشرح. و21 لعبة لا تقيس أي هدف تعليمي، أي أنها ترفيه محض داخل منصّة تعليمية — وهو خيار مقبول إن كان مقصودًا وموثَّقًا، لا إن كان نتيجة نقص بيانات.

**التبعيات** CNT-107 (المراجعة)، ~~DECIDE-104~~ (حُسم هندسيًّا: التصنيف مُشتَقّ من عقد المحرّك)، و`DECIDE-110` الجديد (مصير `instructions_ar`).

**الحجم** L

**تصحيح الأثر (الدفعة 47)** «ولا صوت يشرح» **غير صحيح**: قاعدة 5 في `gamePackValidation.ts` تجعل `vo.intro` و`vo.instruction` و`vo.instruction_repeat` مفاتيح **إلزامية**، وغيابها **خطأ يحجب النشر**، ويحرسه اختبار قائم بالاسم. و`repeat_instructions_button` مثبَّت `const: true` في المخطَّط. فالتعليمات المسموعة مضمونة ومحروسة.

**معايير القبول**
- [ ] ~~كل لعبة منشورة لها تعليمات عربية.~~ → **المعيار مؤجَّل بقرار، لا بعمل.** `instructions_ar` يُشحَن في حِزمة اللعبة و**تطبيق Flutter لا يحلّله في أي موضع** (صفر إشارة، مقابل خمس تستهلك الصوت). فحشوه في 36 لعبة أو جعله حاجب نشر عملٌ يُقاس بعدد الصفوف لا بأثرٍ على طفل → `DECIDE-110`.
- [x] كل لعبة إمّا لها هدف تعليمي أو وسم «ترفيهية» صريح. **مُنجَزٌ وأفضل من الصياغة**: فحص `objective` مشتقٌّ من عقد المحرّك (`writesMastery`) — «ترفيهية» تصنيفٌ **مُشتَقّ** لا وسمًا يُكتب بيد، وربطُ هدفٍ بمحرّكٍ لا يكتب إتقانًا **حاجب** (عكس القاعدة الساذجة). ووسمٌ يدويّ كان سيصير مصدر حقيقة ثانيًا يكذب حين يتغيّر المحرّك، فرُفض.

---

### CNT-106 — ثلاثة كواكب لا يصل إليها طفل: `islamic` و`maharat` و`tarikh`

**الأولوية:** P0 · **المنطقة:** المحتوى / المنتج · **الحالة:** MISSING

**الدليل** (سلاسل وحلقات منشورة لكل كوكب)

| الكوكب | سلاسل منشورة | حلقات منشورة |
|---|---:|---:|
| `abjad` | 2 | 2 |
| `alam` | 3 | 2 |
| `arqam` | 3 | 6 |
| `oloom` | 5 | 4 |
| `qisas` | 3 | 2 |
| `qiyam` | 3 | 4 |
| `maharat` | 3 | **0** |
| `tarikh` | 2 | **0** |
| **`islamic`** | **0** | **0** |

**الأثر**
تسعة كواكب معروضة في الواجهة، وثلاثة منها غرف فارغة. وكوكب الإيمان — وهو أكبر الشرائح المخطَّطة والأكثر حساسية — ليس فيه سلسلة منشورة واحدة، وهو محجوب على بوابة اعتماد شرعي لم تُفتح (`HUMAN-103`).

**التبعيات** HUMAN-103 (مراجع شرعي)، HUMAN-104 (مراجع تاريخي)، CNT-101.

**الحجم** XL (إنتاج ومراجعة)

**تصحيح الدليل (الدفعة 48)** الجدول أعلاه يعدّ **السلاسل والحلقات**، والسلسلة مجلَّد لا يفتح نشرُها شيئًا، والقصص والألعاب والكتب تتفرّع من `series` لا من الحلقات. وبقياس «ما يمكن فتحه» على D1 المحلية: `maharat` = 3 و`tarikh` = 3 (أي **ليسا** فارغين)، و`qiyam` = **صفر** (وقد وصفه البند بـ3 سلاسل و4 حلقات). فعدُّ الحلقات وحده يخطئ في الاتجاهين.

**معايير القبول**
- [x] لا كوكب معروض للطفل بلا محتوى قابل للوصول، أو يُعرض بحالة «قريبًا» صريحة. (الدفعة 48) داخل الكوكب كانت الأقسام الفارغة مُعلَنة بصراحة أصلًا؛ والناقص كان أن **الاختيار أعمى**. الآن `GET /api/v1/planets` تشحن `published_openable` (حلقات + قصص + ألعاب + كتب)، والبطاقة تعرض «قريبًا» عند **صفرٍ مقيس** فقط — و`null` (النسخة المحزومة أو خادم أقدم) ليست صفرًا، وإلّا طُبعت العلامة على كل كوكب لحظة انقطاع الشبكة. والعلامة في تسمية `Semantics` أيضًا لا في النصّ وحده.
- [x] كوكب الإيمان لا يُنشر أي عنصر منه قبل توقيع المراجع الشرعي المعتمد. **مُنفَّذ في الكود** (تصحيح في الدفعة 49 لِما كُتب هنا في الدفعة 48): `religiousChecks` في `lib/publishGate.ts` تحجب المحتوى الديني بثلاثة فحوص — `religious_source` بلا `source_type`، و`religious_review` بلا تاريخ موافقة، و`review_sharia` بلا قرارٍ معتمد — وهي **حواجب لا تحذيرات**، خلافًا لكل أدوار المراجعة الأخرى. التوقيع بشريّ (`HUMAN-103`)، والإنفاذ في الكود.

---

### CNT-107 — 43 مراجعة محتوى كلها `pending`: لا شيء اعتُمد

**الأولوية:** P1 · **المنطقة:** المحتوى / سير العمل · **الحالة:** PARTIAL

**الدليل**
`SELECT status, COUNT(*) FROM content_reviews GROUP BY status` → **`pending` = 43**، ولا حالة أخرى. و`workflow_runs` = 0 و`workflow_step_reviews` بلا نشاط.

**الأثر**
المحتوى المنشور (24 سلسلة، 20 حلقة، 39 لعبة) نُشر **بلا اعتماد مراجعة مكتمل واحد**. وهذا يفسّر `CNT-105`: التعليمات الناقصة كانت ستُرصد في مراجعة تربوية.

**السلوك المتوقع**
لا نشر بلا مراجعة معتمدة للنوع المطلوب؛ وبوابة النشر تقرأ حالة المراجعة لا حالة الحقول فقط.

**الحالة بعد القياس (الدفعة 49)** الشطر الثاني **قائم**: البوابة تقرأ حالة المراجعة فعلًا — مراجعة مرفوضة أو «تحتاج تغييرًا» **حاجب** («النشر فوقه تجاوزٌ صامت لمراجع»)، ومعتمدة نجاح، وغائبة أو معلّقة **تحذير** بقرارٍ موثَّق: تحويل الغياب إلى حاجب «يجعل المكتبة كلّها غير قابلة للنشر بين ليلة وضحاها فيتعلّم الناس تجاوز البوابة لا تعبئتها». والشطر الأوّل («لا نشر بلا مراجعة معتمدة») **يخالف ذلك القرار**، والقرار أصحّ ومنطقه منطق هذا الأودت: حرسٌ يحجب كل شيء يُطفَأ. فلم يُحوَّل.

**وما كان مكسورًا فعلًا** أن التحذير المُتجاهَل **غير مرئي**: مسح البوابة (الدفعتان 43 و44) كان يُعيد الحواجب فقط، فمحتوى منشور و43 مراجعة كلّها معلّقة يعود «لا شيء محجوب». أُضيف `warned`/`warned_count` **منفصلًا** (طيُّه في `blocked` يسمّي التحذير رفضًا، وإسقاطه يُخفيه)، وصفٌّ محجوب لا يُعدّ مرّتين، وعبارة اللوحة «**يمرّ** ويحمل تحذيرًا» لا «سليم».

**التبعيات** API-106، HUMAN-105 (تعاقد المراجعين).

**الحجم** M

---

### CNT-108 — 20 حلقة منشورة بمدة زمنية فارغة

**الأولوية:** P1 · **المنطقة:** المحتوى / سلامة البيانات · **الحالة:** PARTIAL

**الدليل** `SELECT COUNT(*) FROM episodes WHERE status='published' AND (duration_seconds IS NULL OR duration_seconds=0)` = **20 من 20**.

**الأثر**
كل بطاقة حلقة تعرض مدة فارغة، ولا يمكن حساب تقدّم أو «تابع المشاهدة» أو تقرير وقت شاشة بلا مدة. وهذا يتفاعل مع `API-101`: حدّ الوقت اليومي يحتاج مدة معروفة.

**تصحيح الأثر (الدفعة 50)** لم تكن تعرض «مدة فارغة»: `durationLabel` كانت تُعيد **«قصيرة»** عند غياب المدة، فكل بطاقة **تُقرّ بأن الحلقة قصيرة** — دعوى عن المحتوى لم يقِسها أحد، تُعرض لوليّ أمرٍ يبحث عن شيءٍ قصير قبل النوم. والكلمة كانت تخدم معنيين («مقيسٌ وقصير» و«لا أعرف»)، وأيّ كلمة تخدم معنيين لا تُبلّغ أيًّا منهما.

**ما أُنجز (الدفعة 50)** النوع صار `String?`: المجهول `null`، وستة مواضع استهلاك تُسقط التسمية بدل اختلاقها — ومنها التسمية الوصفية لقارئ الشاشة. وما دون الدقيقة مقيسٌ فله تسمية دقيقة. والنمط الصحيح كان في `home_feed.dart` أصلًا، فكانت هذه الدالّة الشاذّة.

**ما بقي — محجوبٌ لا مؤجَّل** الاشتقاق من الأصل مستحيل اليوم: `content_assets` **لا يحمل عمود مدة**، وعدد أصول الفيديو **صفر** (`CNT-101`)، ولا استخراج وسائط في Worker. والبوابة تُحذِّر على المدة الغائبة، وذلك التحذير صار مرئيًّا في مسح البوابة (الدفعة 49).

**الحجم** S (يُشتق من الأصل عند الاستيراد)

---

### CNT-109 — 57 هدفًا تعليميًّا وصفر سؤال يقيسها

**الأولوية:** P1 · **المنطقة:** المحتوى / التعلّم · **الحالة:** PARTIAL

**الدليل**
`learning_objectives` = **57** (تحسّن حقيقي: الأودت السابق قال 0) · `skills` = 25 · لكن `questions` = **0** و`question_localizations`/`question_usage` فارغة، و`attempts` = 0 و`mastery` = 0.

**الأثر**
البنية الكاملة لبنك الأسئلة والإتقان مبنية (جداول، موجّهات، شاشات) وبلا محتوى، فكل شاشة إتقان ستعرض «لا أدلة». والمنصّة لا تستطيع إثبات أي نتيجة تعليمية.

**وما وُجد في الشاشة (الدفعة 51)** تبويب التشخيص في `MasteryPage` — المكتوب ليجيب «لماذا لا دليل لهذا الهدف؟» — كان يكتب **عناوين أعمدته مكان قيمها**: ثلاث عبارات ثابتة تتكرّر في كل صفٍّ من السبعة والخمسين، بلا قراءة حرفٍ من قاعدة البيانات. وذلك **أسوأ من خليةٍ فارغة**: يُقرأ منه أن لكل هدفٍ محتوًى وألعابًا قادرة، وهو عكس ما يوجد التبويب لكشفه.

**ما أُنجز (الدفعة 51)** `by-objective` يُعيد أربعة أعداد مقيسة: `linked_episodes`، `linked_games`، `questions_count`، و`evidence_capable_games` — والأخير **ليس عدد الألعاب المرتبطة**: محرّكٌ لا يكتب إتقانًا (`memory_flip`, `rhythm_tap`) لا يُنتج دليلًا أبدًا، فلعبةٌ عليه مرتبطةٌ بهدفٍ خطأُ ربطٍ لا مصدر قياس. والفرق بين العددين هو الجواب العمليّ: «ثلاث ألعاب وصفر قياس» تعني أعِد الربط لا أنتِج محتوى. والصفر يُقال بالكلمات لا برقمٍ يُقرأ قياسًا ناجحًا.

**التبعيات** CNT-105 (الألعاب هي مصدر الأدلة).

**الحجم** L (محتوى)

---

### CNT-110 — صفر حقوق مسجَّلة، وصف إتاحة واحد

**الأولوية:** P1 · **المنطقة:** المحتوى / الحقوق · **الحالة:** 🟡 **نصفه مُغلَق في الدفعة العاشرة** — الملكية مسجَّلة الآن: `content_rights` = **248 صفًّا** (الترحيل `0082`، كل السلاسل والحلقات والكتب والألعاب بمالك `majarra`). ويبقى قائمًا: `content_availability` بصفّ واحد فالرفض الإقليمي غير مُهيَّأ، ولا بوابة نشر تفحص وجود صفّ الحقوق.

**الدليل** `content_rights` = **0** · `rights_licenses` = **0** · `content_availability` = **1** صف.

**الأثر**
لا يمكن معرفة أي محتوى نملكه وأيّه مرخَّص، وهو **الشرط الأول** لقرار DRM (`DECIDE-101`) ولمعيار القبول 25 في خطة التشفير. ومسار التشغيل يرفض بـ451 اعتمادًا على `content_availability` الذي يحمل صفًا واحدًا، أي أن الرفض الإقليمي فعليًا غير مُهيَّأ.

**التبعيات** HUMAN-101، DECIDE-101.

**الحجم** M

**تصحيح (الدفعة 52)** «ولا بوابة نشر تفحص وجود صفّ الحقوق» **قديم**: `rightsChecks` تفحص بثلاث درجات — ترخيصٌ منتهٍ **يحجب** بلا شرط، وغيابُ السجلّ يُحذِّر، وأقاليمٌ فارغة تُحذِّر.

**معايير القبول**
- [ ] لكل أصل مالك حقوق ونوع ترخيص وتاريخ انتهاء مسجَّل. المالك والتراخيص **مسجَّلة** (125 صفًّا محليًّا، مالكٌ واحد، تراخيص غير فارغة)، و`expiry` فارغ في كل الصفوف — وهو صحيحٌ لمحتوًى مملوك بلا انتهاء، ويصير واجبًا عند أوّل ترخيصٍ خارجي.
- [x] المحتوى المرخَّص لا يُنشر قبل توثيق مصفوفة DRM/Offline/Watermark لعقده. **مُنفَّذ بمعناه بعد قرار «بلا DRM»** (الدفعة 52): `licenses` تحمل الاستعمالات المسموح بها (`streaming`/`offline`) — أي أن المصفوفة في البيانات — و`routes/downloads.ts` **لم يكن يقرؤها أصلًا**. صار التنزيل يُرفَض بـ403 وسببٍ مُسمّى **حين تُعلَن الحقوق وتستثني `offline`**؛ والغياب ليس منعًا (ولو كان، لتعطّل تنزيل القصص كلّها: قيد CHECK يمنع أن تحمل صفّ حقوق). **والتجديد محروس كالإصدار**، وإلّا بقي حقٌّ سُحب بعد التنزيل ممتدًّا شهرًا بعد شهر.

---

### 10.5 ما تحسّن فعلًا منذ الأودت السابق

| البند | كان | الآن |
|---|---|---|
| أصول صوتية | 0 | **30** (16 مستخدمة كسرد) |
| أهداف تعليمية في القاعدة | 0 | **57** |
| مسار نشر للقصص والكتب والألعاب والمشروعات | غير موجود | موجود ويفشل مغلقًا (`adminPublish.ts`) |
| صفحات قصص لها رسوم | 0 | 16 من 194 |
| محركات ألعاب مسجَّلة | 5 | **17** |
| ثغرات أمان P0 الثلاث | مفتوحة | مُصلَحة (§5.5) |
| CI | لم يعمل قط (فرع خاطئ) | يعمل على `master` بسبع وظائف (لكن أحمر اليوم — `OPS-102`) |

---

## §11 الخصوصية والجودة والوصول والأداء والدَين التقني

### QA-101 — اختبار PNG مؤقت داخل المجموعة يعلّق `flutter test` نهائيًا ويكتب في جذر المستودع

**الأولوية:** P0 · **المنطقة:** الاختبارات / CI · **الحالة:** DEAD-UNUSED

**المشكلة**
`test/tmp_render_png_test.dart` ليس اختبارًا؛ إنه سكربت لقطة شاشة مؤقت التُزم داخل مجموعة الاختبارات. يرسم رئيسية الاستوديو ويحفظ `../_studio_home.png` في **جذر المستودع**، ولا ينتهي.

**الدليل**
- الملف نفسه: `app_main/test/tmp_render_png_test.dart` — اسم الاختبار «render studio home to png for visual review»، و`File('../_studio_home.png').writeAsBytesSync(...)`، و`print('WROTE _studio_home.png')`.
- الملف الناتج موجود فعلًا في الجذر: `_studio_home.png`.
- في التشغيل الفعلي: الجولة تصل إلى هذا الملف وتتوقف عنده أكثر من 90 ثانية بلا تقدّم ثم تظل معلَّقة (`pumpAndSettle` على واجهة فيها حركة مستمرة لا تسكن).

**الأثر**
`flutter test` **لا يمكن أن ينتهي**. أي أن وظيفة `flutter` في CI ستستهلك مهلة GitHub القصوى لكل دفعة، و`deploy-gate` لن يعمل أبدًا. وهذا هو أول ما يجب إصلاحه في المشروع كله: بلا جولة اختبار تنتهي، لا معنى لأي بوابة.

**نطاق التنفيذ**
حذف الملف. إن كانت المعاينة البصرية مطلوبة فتكون سكربتًا تحت `tools/` أو اختبار golden حقيقي بمقارنة صورة مرجعية داخل `test/goldens/`، وبمهلة محدودة، ولا يكتب خارج مجلده.

**الحجم** XS (والأثر أكبر من كل بند آخر في الملف)

**معايير القبول**
- [ ] `flutter test` ينتهي ويطبع عددًا نهائيًا.
- [ ] لا اختبار يكتب ملفًا خارج مجلد مؤقت خاص به.
- [ ] `_studio_home.png` محذوف من الجذر.

**لا تفعل**
لا تكتفِ بوسم الاختبار بـ`skip`؛ الملف نفسه لا مكان له في `test/`.

---

### QA-102 — 13 اختبارًا فاشلًا في Flutter، اثنان منها يكشفان عيوبًا حقيقية في الأمان والكاش

**الأولوية:** P0 · **المنطقة:** الاختبارات / التطبيق · **الحالة:** REGRESSION

**المشكلة**
الجدول الكامل في `§1.2`. الإخفاقات ليست ضجيج اختبارات؛ ثلاثة منها تصف سلوكًا خطأً في الإنتاج:

| الاختبار | ما يكشفه فعلًا | البند المرتبط |
|---|---|---|
| `logout_tile_test.dart` | كاش صفحات القارئ يبقى بعد تسجيل الخروج | `ENC-012` |
| `catalog_cache_test.dart:82` (اختباران) | كاش منتهي الصلاحية وكاش بطابع زمني مستقبلي كلاهما يُقبل | `ENC-012` |
| ~~`deep_link_smoke_test.dart` (ثلاثة)~~ | **تنجح، والوصف كان خطأً**: تقيس كاتالوج الأنشطة المرجعية بصيغة JSON (`_bundledReferenceActivities = 32`) لا `local_catalog.dart` — مُتحقَّق منه في الدفعة 31 | ~~`APP-103`~~ |
| `engine_content_separation_test.dart` (ثلاثة) | ملفات المحركات تحمل محتوى مؤلَّفًا وإيموجي مكان الرسوم | `CNT-105` |
| `app_environment_test.dart` | قائمة السماح لم تعد تقبل مضيف staging المُعلَن | `OPS-101` |
| `studio_home_layout_test.dart` · `tutorial_gate_test.dart` · `creative_studio_e2e_test.dart` | أخطاء تخطيط، وعدم حفظ إكمال الجولة التعليمية، وترتيب خطوات المراجع | `APP-101` |

**نطاق التنفيذ**
`QA-101` أولًا (وإلا لا يمكن رؤية النتيجة النهائية)، ثم إصلاح كل إخفاق عند سببه الجذري المذكور أعلاه لا بتعديل التوقّع في الاختبار.

**الحجم** L

**معايير القبول**
- [ ] `flutter test` أخضر بالكامل.
- [ ] لم يُعدَّل أي `expect` لملاءمة السلوك الخطأ — التغييرات في كود الإنتاج.

**لا تفعل**
لا تُسكِت اختبارًا يكشف بقاء كاش محتوى بعد الخروج.

---

### QA-103 — لا اختبارات تكامل على جهاز ولا قياس تغطية

**الأولوية:** P1 · **المنطقة:** الاختبارات · **الحالة:** MISSING

**الدليل**
- لا مجلد `app_main/integration_test/` ولا استخدام لـ`flutter_driver`. الاختبارات المسمّاة «integration» (`onboarding_journey_integration_test.dart`, `creative_studio_e2e_test.dart`) هي widget tests داخل `test/`.
- لا مجلد `coverage/` ولا خطوة تغطية في `ci.yml`، فلا نسبة معروفة لأي طبقة.
- `dev_dependencies` في `pubspec.yaml` عنصران فقط: `flutter_test` و`flutter_lints 5.0.0` — لا `integration_test` ولا `mocktail` ولا أدوات golden.

**الأثر**
مسارات لا يمكن التحقق منها إلا على جهاز — `FLAG_SECURE`، مخزن المفاتيح، التنزيل الحقيقي، المشغّل، تكبير النص، D-pad على TV — ليست مغطّاة بأي شكل. وهذا يتقاطع مع أن أخطر الأقسام (التشفير، التنزيل) تعتمد على سلوك منصّة لا يظهر في widget test.

**الحجم** L

**معايير القبول**
- [ ] `integration_test/` يحتوي مسار تشغيل offline كاملًا (تنزيل → تشفير → تشغيل → انتهاء ترخيص) على جهاز/محاكي في CI. — **محجوب** (الدفعة 53): محاكي CI استثمارٌ بطيء ومتقلّب، و**محتوى الفيديو غير موجود** (`CNT-101`) فلا شيء يسير في المسار. ولم يُبنَ هيكلٌ يمرّ بلا قياس.
- [x] تقرير تغطية يُنتَج في CI مع حدّ أدنى معلَن لا ينخفض. (الدفعة 53) مقيسًا: **31.92%** من 28,834 سطرًا في 193 ملفًا (من 203) على 689 اختبارًا، والحدّ **31%** بتاريخ قياسه مكتوبًا. ويُحرَس **عدد الملفات** أيضًا لأن `lcov` لا يذكر إلا ما حمّله اختبار — فخروجُ ملفٍ كبيرٍ ضعيف التغطية **يرفع** النسبة والتغطية الحقيقية تنخفض. وتقريرٌ غائب **يُرفَض** لا يُمرّ. والحرس نفسه محروس بخمسة اختبارات وبفحصٍ ذاتي يسبق القياس في CI.

**ما يبقى غير مقيس** تغطية الـWorker واللوحة: لم تُدرَج في الحدود، لأن حدًّا بلا قياس رقمٌ مُختار — وهو ما يرفضه هذا البند نفسه.

---

### ~~QA-105~~ — اختبار Flutter يتقلّب: أخضر منفردًا وأحمر في الجولة الكاملة

**الأولوية:** P2 · **المنطقة:** الاختبارات / التطبيق · **الحالة:** ✔ منجَز (رُصد في الدفعة 40، أُصلح في الدفعة 43)

> **السبب، مقيسًا:** `settle` كان ينتظر **120 مللي ثانية من زمن الحائط** ويفترض أن فكّ ترميز الصورة انتهى — فحُكم الاختبار كان رهنَ حِمل الجهاز. صار الانتظار على الشرط لا على الساعة.
>
> **وما أخفاه:** بعد التحويل فشل اختبارٌ آخر بصراحة. انتظارُ الحدث لازمٌ وغير كافٍ: زرّ «إعادة» يبقى `onTap: null` حتى إعادة البناء التي تلي الالتزام، والحدث يصل قبلها، و`tester.tap` على زرّ معطَّل **ينجح ولا يفعل شيئًا**. فكان الاختبار يمرّ لأن النومة صادفت أن تسمح بإعادة البناء — لا لأن الميزة تعمل. صار كل انتظار على شرط الخطوة التالية: الحدث **و**أن يكون الزرّ قابلًا للنقر.
>
> وأُفرد لإثبات **الغياب** مساعدٌ باسمه (`quiet`): لا شرطَ يُنتظَر هناك، فالمدّة هي الأداة الصحيحة — ولا تُخفى داخل `settle` حيث يظنّها القارئ محدودةً بحقيقة.

**المشكلة**
`test/coloring_board_test.dart` — «فرشاة تُلوّن والتراجع يستردّ» — يفشل أحيانًا في الجولة الكاملة وينجح دائمًا منفردًا.

**الدليل**
في جولةٍ كاملة: `+674 -1` مع `Expected: [true, false, true] / Actual: [true, false]` («at location [2] is shorter than expected»). ثم `flutter test test/coloring_board_test.dart` وحده: 12/12. ثم جولة كاملة تالية: **675/675**. ولا سطر Dart تغيّر في تلك الدفعة، فالتقلُّب سابقٌ لها.

**الأثر**
اختبارٌ يتقلّب هو **حرسٌ لا يُوثَق به**: الأحمر يُنسَب إلى «التقلّب» فيُعاد التشغيل، فيمرّ انحدارٌ حقيقي في نفس الملف بلا أن يُلاحظ. وهي عائلة العطل التي يعالجها هذا الأودت كلّه — حرسٌ يبدو قائمًا وليس كذلك.

**رصدٌ ثانٍ (الدفعة 42) يُرجّح السبب** فشل هذه المرّة اختبارٌ **آخر** في الملف نفسه — «تُحمّل الصورة الحقيقية وتعرض الأدوات» — بـ`Expected: no matching candidates / Actual: Found 1 widget with type "CircularProgressIndicator"`. أي أن الشاشة **كانت ما تزال تُحمّل** عند التأكيد. وهذا يرفع «إطارٌ غير منتظَر» من فرضية إلى الأرجح: الملف لا ينتظر انتهاء تحميل الصورة انتظارًا مضمونًا، فالنتيجة رهنُ سرعة الجولة. والجولة التالية مرّت `675/675`.

**الفرضية** الشكل `[true, false]` بدل `[true, false, true]` يعني أن الضربة الثالثة (أو أثر التراجع) لم تُرصَد بعد: انتظارُ إطارٍ غير مضمون، أو حالةٌ ساكنة يتقاسمها الملف مع غيره في نفس العملية (الجولة الكاملة تشترك في عمليات، والمنفرد لا).

**الحجم** S

**معايير القبول**
- [x] السبب مُسمّى: **إطارٌ غير منتظَر** — نومة 120 مللي ثانية من زمن الحائط بدل انتظار الشرط. بدليل: بعد التحويل ظهر الفشل الحقيقي بجملة صريحة، ومُسبّبه الثاني (زرّ معطَّل يُنقَر بلا أثر) لم يكن ليظهر لولاه.
- [x] الاختبار يمرّ في **عشر** جولات كاملة متتابعة (ومعها خمس منفردة)، بعد أن كان يفشل قريبًا من جولةٍ من كل جولتين.
- [x] لا `pumpAndSettle` بمهلة تُخفي المشكلة. ولا نومةً مخفيّة: الانتظار على شرط، والموضع الوحيد الذي يُثبت غيابًا له مساعدٌ باسمه.

---

### QA-104 — بعض الاختبارات الخادمية تتحقق من نص المصدر بدل السلوك

**الأولوية:** P2 · **المنطقة:** الاختبارات / الباكند · **الحالة:** PARTIAL

**المشكلة**
عدة مجموعات تقرأ `src/index.ts` كنص وتؤكد بتعبير نمطي أن سطرًا موجود، بدل تشغيل الطلب والتحقق من النتيجة.

**الدليل**
`test/cors.test.mjs` و`test/analyticsIngest.test.mjs` و`test/contentFactory.test.mjs` كلها تستخدم `readFileSync(... '../src/index.ts')` ثم `assert.match(source, ...)`. والتعليق في `analyticsIngest.test.mjs` يقرّ بالسبب: «The quota lives in index.ts middleware, which this suite cannot mount».

**الأثر**
اختبار كهذا يبقى أخضر إن أُعيد ترتيب الوسائط أو تغيّر السلوك مع بقاء النص. وهو نفس نمط الخلل الذي أخفى الحرَس الناقص سابقًا. والدليل أن البديل ممكن: **ثلاث مجموعات تُحمّل التطبيق المُركَّب فعلًا** (`entrypoint`, `homeBuilderE2E`, `rateLimit` عبر `await import('../src/index.ts')`).

**الحجم** M

**القاعدة المُعلَنة (الدفعة 54)** الفرق الذي يحدّده البند بعبارة «لإثبات سلوك وقت التشغيل» هو كل شيء: **سلوك وقت التشغيل** (هل عمل الوسيط؟ هل فُرضت الحصة؟) يُثبَت بطلبٍ حقيقي — وتأكيدُ المصدر عليه يبقى أخضر لو غُيّر مسار التركيب إلى ما لا يطابق طلبًا حقيقيًّا. أمّا **الخاصّية البنيوية الشاملة** (أن كل موجّه متحوّل في 36 ملفًا يُعلن `requirePermission`، أو ألّا توجد نسخة ثانية من عقد CORS) فمسحُ المصدر أداتها الصحيحة: تحويلها إلى سلوك يعني ضرب 497 نقطة واحدةً واحدة، أي **خسارة** في الصرامة.

**معايير القبول**
- [x] كل تأكيد على وسيط أو حصة أو CORS يجري عبر طلب حقيقي على التطبيق المُركَّب. (الدفعة 54) `cors.test.mjs` كان **مُصلَحًا أصلًا** (preflight حقيقي + العقد مُؤكَّد على القائمة المستوردة + تأكيد بنيويّ واحد يمنع نسخة ثانية). وحصّة التحليلات كانت مُؤكَّدة على نصّ `index.ts` بعُذرٍ **غير صحيح** («المجموعة لا تستطيع تركيب التطبيق») — وثلاث مجموعات تُركّبه. صارت طلبًا حقيقيًّا يقرأ `X-RateLimit-Limit` (240 = إعداد التحليلات بعينه) واسم الدلو، **ومعه حالة ضابطة** تُثبت أن قراءة كاتالوج لا تُنفق الميزانية.
- [ ] لا `assert.match` على نص مصدر لإثبات سلوك وقت التشغيل. **مُستوفى للحالات المرصودة**، ومحروسٌ بمنع العُذر الذي يُبرّره. ويبقى مسحُ الـ123 تأكيدًا الباقية واحدًا واحدًا للتأكّد من أن أيًّا منها لا يخفي خاصّية وقت تشغيل — عملٌ يحتاج قائمة مُصنَّفة، من فئة «تصنيف 370 نقطة» في `ADM-101`.

---

### I18N-101 — التوطين بنية بلا استخدام: 2526 نصًّا عربيًّا حرفيًّا مقابل 255 مفتاحًا

**الأولوية:** P1 · **المنطقة:** التطبيق / التوطين · **الحالة:** PARTIAL

**الدليل** (عدّ فعلي على 180 ملفًا، باستثناء الملفات المولَّدة)
- **102 من 180 ملفًا** تحتوي نصوصًا عربية بين علامتَي اقتباس، بمجموع **2526** ظهورًا.
- **77** إشارة فقط إلى `AppLocalizations` في كل الكود.
- ملفات ARB الثلاثة `app_ar.arb` و`app_en.arb` و`app_fr.arb` بـ**255 مفتاحًا لكل منها** — أي أنها متطابقة في عدد المفاتيح (وهذا **يُبطل** ما تقوله التقارير القديمة عن فجوة 346 مقابل 258).
- اللغة مثبَّتة على العربية في `lib/app/majarra_app.dart:80`، وعنوان التطبيق نص حرفي `'مجرة'` (`:70`).

**الأثر**
إطلاق لغة ثانية يتطلب استخراج ~2500 نص، وهو عمل بحجم إعادة كتابة الواجهة لا بحجم ترجمة. والقرار الأصدق اليوم هو **الإقرار بأن المنتج أحادي اللغة** حتى يُتخذ قرار توسّع بميزانية حقيقية.

**نطاق التنفيذ**
إمّا (أ) تثبيت السياسة: العربية فقط، وحذف `app_en.arb` و`app_fr.arb` حتى الحاجة، وإزالة إيحاء تعدّد اللغات من الواجهة؛ أو (ب) استخراج تدريجي بأولوية الشاشات العامة (`membership_page`, `account_data_page`, `privacy_page`) مع بوابة تمنع نصًّا حرفيًّا جديدًا.

**التبعيات** DECIDE-105 (سياسة اللغات).

**الحجم** XL

**تصحيح العدّ (الدفعة 55)** النصوص الحرفية **2,683** في **147 من 198** ملفًا (لا 2,526 في 102) — أي أن العدد **نما** لأن لا بوابة تمنعه. وملفات ARB الثلاثة 255 مفتاحًا لكل منها و**صفر قيمة عربية** في `app_en.arb`: فتغطية ARB **100%** والشاشات عربية، لأن 2,683 نصًّا لا تمرّ بأي كتالوج.

**معايير القبول**
- [ ] قرار موثَّق: أحادي اللغة أو متعدّد، بميزانية. — `DECIDE-105`، **قرار مالك**.
- [ ] إن كان متعدّدًا: بوابة CI تفشل على نص حرفي جديد في `lib/`. — **لم تُبنَ بقرار** (الدفعة 55): تحت سياسة «العربية وحدها» هي احتكاكٌ بلا مقابل، وبناؤها قبل القرار افتراضٌ له. والعدد مقيسٌ في اختبارٍ يُظهره في سبب الفشل.
- [x] لا واجهة تعرض خيار لغة لا تعمل فعلًا. (الدفعة 55) مُستوفًى في السطوح الثلاثة، وكلٌّ بحرسه: التطبيق (اللغة مثبَّتة + `locale_catalog_test`) · نموذج ملفّ الطفل (حقل للقراءة فقط، لأن «عرضًا مُنتقٍ بخيار واحد يزعم اختيارًا لا وجود له») · صفحة الهبوط (فاحص متصفّح يتحقّق أن المبدّل **يترجم** لا أن الزرّ يستجيب).

**والعطل كان في الحرس نفسه (الدفعة 55)** `locale_catalog_test` وضع التأكيد **داخل** `if (cov < 0.99)` تحت تعليقٍ يقول «إن بلغت الإنجليزية التغطية الكاملة سيفشل هذا الاختبار». والعكس ما يحدث: عند بلوغها يُتخطّى الجسم فيمرّ بلا تأكيد — و**التغطية 100% اليوم**، فالاختبار كان قد توقّف عن التأكيد بصمت. والمقياس نفسه خطأ: قلبُ الراية على تغطية ARB كان سيعرض لغةً لا يستطيع التطبيق رسمها. فصارت دالّة الإجبار على **المانع الحقيقي** (النصوص الحرفية)، ومُتحقَّقٌ أنها تفشل فعلًا بقلب الراية مؤقّتًا.

---

### A11Y-101 — 199 إشارة Semantics مقابل إشارة واحدة لتكبير النص

**الأولوية:** P1 · **المنطقة:** التطبيق / إمكانية الوصول · **الحالة:** PARTIAL

**الدليل** (عدّ فعلي)
`Semantics(` / `semanticLabel` / `excludeSemantics` = **199** إشارة — تغطية جيدة لقارئ الشاشة. مقابل `textScaler`/`textScaleFactor` = **1** إشارة في كل التطبيق.

**الأثر**
مستخدم رفع حجم الخط في نظامه (وهو إعداد شائع جدًّا لدى الأهل) سيرى نصوصًا مقطوعة أو تخطيطًا مكسورًا، ولا شيء في الكود يتعامل مع ذلك. والاختبار الفاشل `studio_home_layout_test.dart` («renders every section without layout errors») مؤشّر على أن التخطيط هشّ أصلًا عند حدوده.

**السلوك المتوقع**
اختبار كل شاشة على `textScaler` 1.0 / 1.3 / 2.0 مع تخطيط لا يقطع النص، وسقف معلَن للتكبير حيث يلزم.

**الحجم** L

**العطل المقيس (الدفعة 56)** الإشارة الواحدة لم تكن مجازًا: أوّل مِسحة تكبير على شاشتين أوقعت تجاوزًا حقيقيًّا — `Column` في `planets_page.dart:446` **يتجاوز 29 بكسل** عند 200%. السبب أن حجمَي خطّ التسمية في `_PlanetChoice` رقمان حرفيّان، وارتفاع شريط الاختيار في `_PlanetChooser` ثابتٌ **مستقلّ** عنهما، فنما النصّ ولم ينمُ الشريط. صار الارتفاع يُحسب من الحجمين نفسيهما (نموّ كتلة النصّ وحدها، فالرمز والحواشي لا تتأثّر بحجم الخط).

**معايير القبول**
- [~] كل شاشة رئيسية تعمل على تكبير 200% بلا نص مقطوع. — **مقيسٌ على شاشتين لا على الكلّ** (الدفعة 56): `PlanetsPage` كانت تتجاوز فأُصلحت، و`StudioHomeView` نظيفة على النِّسَب الثلاث. باقي الشاشات تحتاج مضيفًا يحقن مزوّديها، وهي بقيّة البند.
- [x] اختبارات widget تغطّي ثلاث نسب تكبير على الأقل. (الدفعة 56) `test/text_scaling_layout_test.dart` — 1.0 / 1.3 / 2.0، وحرسٌ على القائمة نفسها يمنع تقليصها إلى نسبةٍ سهلة.
- [ ] تحقق واحد على جهاز حقيقي مع TalkBack وVoiceOver، بدليل مرفق. — **فعلٌ بشري** (`HUMAN-107`).

> ملاحظة: التحقق الكامل من مطابقة WCAG يتطلّب اختبارًا يدويًا بتقنيات مساعدة ومراجعة خبير وصول؛ لا يمكن إثباته بعدّ إشارات في الكود.

---

### A11Y-102 — الاستوديو والتلوين مبنيان على الإيماءات والرسم على Canvas بلا بديل وصول

**الأولوية:** P2 · **المنطقة:** التطبيق / إمكانية الوصول · **الحالة:** MISSING

**المشكلة**
أكبر ملفات التطبيق (`creative_studio_page.dart` 4373 سطرًا، `free_draw_surface.dart`، محركات التلوين والتتبّع) تعتمد على السحب والرسم على `Canvas`، وهي أصعب مسارات الوصول وأقلّها تغطية semantics، ولا يوجد لها بديل بلوحة مفاتيح أو D-pad.

**الأثر**
طفل يستخدم TV بريموت، أو طفل بإعاقة حركية، لا يستطيع استخدام الاستوديو مطلقًا — وهو أحد أبرز أقسام المنتج.

**الحجم** L

**تصحيح شقّ التلفاز (الدفعة 57)** «لا يستطيع مطلقًا» صحيح، لكن «بلا شيء في الكود يتعامل مع ذلك» **غير صحيح**: التلفاز محجوبٌ بثلاث بوابات مقيسة — `/studio` يعرض «الاستوديو يحتاج شاشة لمس`، وبطاقة الاستوديو مُخفاة على التلفاز (`if (!isTelevision)`)، و`evaluateAvailability` يرفض المحرّكات اللمسية بسبب `requiresTouch`، وكل حزمة استوديو تُعلن `supports_dpad: false`.

**والعطل الحقيقي كان في الرابعة الغائبة** ثلاثة روابط عميقة — `/studio/coloring/:id` و`/studio/reference/:id` و`/studio/trace/:id` — تفتح **الأسطح نفسها** ولم تفحص شيئًا، ولا تمرّ بـ`GameScreen` فلا يعصمها `requiresTouch`. فرابطٌ عميق على التلفاز كان يُنزل الطفل على لوحةٍ تُدار بالإصبع وحده: لا رسالة ولا سبيل. صارت الأربعة تُعلَن عبر `_touchOnlyRoute` والبوابة **داخل** بانيَة المسار.

**ما بقي: بديل الإدخال نفسه — قرار مالك (`DECIDE-111`)**
الجرد المقيس في `features/games/**`: إشارة **واحدة** إلى لوحة المفاتيح في المنطقة كلّها (`CallbackShortcuts` لإغلاق طبقة مقارنة في `reference_drawing_page.dart:488`)، وصفر `FocusableActionDetector`/`Shortcuts`/`Actions`/`onKeyEvent`، وصفر `Semantics(onTap:)` على أي لوحة، ولا مفهوم «مؤشّر حاليّ» في الحالة. الأدوات والألوان **مخدومة** (`Semantics(button:, selected:)` + `InkResponse`)؛ اللوحة وحدها ليست كذلك. وسطح الأفعال الأدنى لبديل لوحة مفاتيح مقيسٌ ومحدود: تحريك مؤشّر، وإنزال (`_begin`)، ومدّ (`_extend`)، ورفع (`_end`) — أو نقطة تعبئة واحدة في التلوين. القرار المطلوب: هل «الرسم الحرّ بالريموت» منتَجٌ يُراد أصلًا، أم البديل نشاطٌ آخر للطفل الذي لا يرسم بإصبعه؟

**فرصة مقيسة إن حُسم القرار** `ColorRegion` في `coloring_regions.dart` نموذجٌ قائم بمعرّفات ثابتة و`label` و`hitRegionAt` و`regionPaths`، فتلوينُ مناطق بعُقَد `Semantics` قابلة للتركيز **رخيص** — لكن اللوحتين الشاغلتين (`coloring_board.dart` و`coloring_board_v2.dart`) نقطيّتان (`_floodFill` على بكسل) ولا تستعملان النموذج، فلا قائمة مناطق في وقت التشغيل تُعدَّد.

---

### PERF-101 — 358 ميغابايت أصول على القرص بصيغ مزدوجة

**الأولوية:** P1 · **المنطقة:** التطبيق / الأداء / حجم الحزمة · **الحالة:** DEBT

**الدليل** (قياس فعلي لـ`app_main/assets`)
الإجمالي **358.18 ميغابايت**: `.jpg` 243 ملفًا = **157.6 MB** · `.png` 212 ملفًا = **152.0 MB** · `.webp` 401 ملفًا = **47.9 MB** · `.ttf` 4 = 0.38 MB.

**التحليل**
سياسة `pubspec.yaml:76-135` واعية جدًّا وتُعلن أصولًا محدّدة لتجنّب التضخّم (وهذا محسوب لها). لكن وجود 401 ملف WebP بجانب 455 ملف JPG/PNG يعني أن الصيغ الأصلية والمحوَّلة تتعايش في نفس الشجرة، فأي إعلان مجلد جديد بالخطأ يضيف مئات الميغابايتات، والمستودع نفسه ثقيل بلا سبب.

**السلوك المتوقع**
الأصول المصدرية خارج `app_main/assets`، والمجلد يحتوي فقط ما يُبندل فعلًا؛ وقياس حجم الـAAB في CI مع حدّ أعلى يفشل عند تجاوزه.

**الحجم** M

**تصحيح الأرقام (الدفعة 58، بأداة `tools/ci/assets-declared.mjs`)**
الـ358 MB صحيحة **على القرص** وليست ما يُبندَل. إعلان المجلد في Flutter **غير تعاودي**، والسياسة تعتمد على ذلك صراحةً. المقيس: **المُبندَل فعلًا 389 ملفًا = 116.12 MB** · وغير المُعلَن على القرص **630 ملفًا = 242.06 MB** (جُلّها `assets/images/stories/` = 222 MB).

**و«المستودع ثقيل بلا سبب» غير صحيح** `.gitignore` ينفّذ قائمة سماح صريحة (`app_main/assets/**` مُستثنًى إلا ما يُسمَح بالاسم). المقيس: المستودع يتعقّب **104 ملفات أصول = 8.85 MB**، ومن 496 ملف قصص على القرص **22 فقط متعقَّب**. فالـ242 MB مخرَجاتُ خطّ إنتاج محلية قابلة للتوليد، لا حِمل مستودع.

**معايير القبول**
- [~] `app_main/assets` يحتوي فقط الملفات المعلَنة في `pubspec.yaml`. — **المعيار نفسه محلّ نظر** (الدفعة 58): غرضه المُعلَن («الأصول المصدرية خارج `assets`») **مُستوفًى للمستودع** بقائمة سماح `.gitignore`؛ وشجرة العمل مساحةُ إنتاج مقصودة. والخطر الحقيقي ليس وجود الملفات بل **إعلان مجلدٍ بالخطأ** — وهذا صار محروسًا بحدٍّ مقيس على الحِمل. نقلُ 242 MB من ملفات غير متعقَّبة على جهاز المالك عملٌ بلا مقابل، وموضعُها قرارُه.
- [x] CI يطبع حجم الـAAB ويفشل عند تجاوز حدّ معلَن. (الدفعة 58) `tools/ci/aab-size.mjs` في وظيفة `flutter` **بعد** `flutter build appbundle`، والحدّ **200 MB** ليس رقمًا مُختارًا: هو عتبة Google Play التي يرى فوقها المستخدم على بيانات الجوّال تحذيرَ حجمٍ عند التثبيت ([مرجع](https://support.google.com/googleplay/android-developer/answer/9859372) — الصياغة أُعيدت بإيجاز للامتثال لقيود الترخيص). وغيابُ ملف الحزمة **فشل** لا تجاوُز.
- [x] حرسٌ إضافي لم يطلبه البند: حدٌّ **مقيس** على حِمل الأصول المُبندَلة (116.12 → 125 MB بهامش صغير مقصود)، ورفضُ إعلانٍ لا يقابله ملف، وفحصُ أن كل مسار `assets/…` حرفيّ في `lib/` مُبندَلٌ فعلًا.

---

### PERF-102 — فك ترميز الصور بأحجامها الكاملة: 56 `Image.asset` مقابل 5 استخدامات لـ`cacheWidth`

**الأولوية:** P2 · **المنطقة:** التطبيق / الأداء · **الحالة:** PARTIAL

**الدليل** عدّ فعلي: `Image.asset` = **56** · `Image.network`/`CachedNetworkImage` = 13 · `cacheWidth`/`cacheHeight` = **5**.

**الأثر**
صورة بوستر بأبعاد كاملة تُفكّ في الذاكرة لتُعرض في مربع 58 بكسل. على أجهزة أطفال منخفضة الذاكرة هذا سبب مباشر لتقطيع التمرير ولإغلاق النظام للتطبيق.

**الحجم** S

**العدّ المُحدَّث والمُصحَّح (الدفعة 59)** `Image.asset` = **53** (لا 56) · `Image.network` = **12** و`CachedNetworkImage` = **صفر** (لا 13 مجموعًا) · `cacheWidth` = **3** (لا 5) · و`cacheHeight` = **صفر** · و`ResizeImage` = **صفر**.

**والقدرة كانت موجودة وغير مستعملة** `CinematicImage` يقبل `decodeWidth` ويحوّله إلى `cacheWidth` بالكثافة الحقيقية منذ ما قبل هذه الدفعة. المقيس: **21 موضع نداء، سبعة تُمرّرها**. أي أن العطل لم يكن غياب الأداة بل عدم استعمالها.

**الأثر مُقاسًا بالميغابايت** أفاتارات الشخصيات: **50 صورة** حتى 768×1376، نحو **4 ميغابايت مفكوكة للواحدة** و**200 لو عاشت كلّها**، تُعرض في خلايا 72–78 بكسل. والكواكب: **10 صور** 768×768 = **22.5 ميغابايت** في دوائر 58–72. وشبكات التلوين و«ارسم مثلي»: أصول 1024×1024، خمسون منها = **200 ميغابايت**.

**ما أُنجز (الدفعة 59)** `decodeCapFor` مصدرًا واحدًا للقاعدة، مطبَّقًا في **أربعة أسطح مشتركة** — `PlanetSymbol` و`ChildAvatarView` و`DrawingAsset` و`smartImage` — وثلاثة أحواض صغيرة معلومة العرض (120، 120، 84). فصار `cacheWidth` في **عشرة** مواضع و`decodeWidth` في **عشرة**. والتأكيد على `ResizeImage` في الشجرة لا على نصّ المصدر.

---

### PRIV-101 — مدة الاحتفاظ اختيار هندسي ينتظر مراجعة خصوصية لم تحدث

**الأولوية:** P1 · **المنطقة:** الخصوصية / الامتثال · **الحالة:** HUMAN-REVIEW

**الدليل**
`dashboard/api/src/scheduled/cleanup.ts:5-13` — التعليق نفسه يقول إن 180 يومًا رقم هندسي «ينتظر تأكيدًا من مراجعة خصوصية الطفل (HUMAN-009 في سجل الأودت السابق) وينبغي أن ينتقل إلى الإعداد إن قرّرت المراجعة رقمًا آخر». المراجعة لم تحدث.

**الأثر**
منصّة أطفال تحتفظ ببيانات سلوكية 180 يومًا بقرار مهندس. هذا سؤال قانوني (COPPA / GDPR-K / لوائح محلية) لا هندسي، ولا توجد في المستودع سياسة خصوصية مكتوبة أو DPIA تقابله.

**التبعيات** HUMAN-106.

**الحجم** S (كود) + HUMAN

**معايير القبول**
- [~] مدة الاحتفاظ معتمدة كتابةً ومصدرها إعداد لا ثابت. — **الشقّ الهندسي مُنجَز** (الدفعة 60): `ANALYTICS_RETENTION_DAYS` في الإعداد، والافتراض **180 كما هو** لأن تغييره هو القرار القانوني نفسه. و«معتمدة كتابةً» فعلٌ بشري (`HUMAN-106`).
- [~] سياسة خصوصية أطفال مكتوبة في المستودع تطابق ما ينفّذه الكود. — كُتب `docs/DATA_RETENTION.md`: **جردٌ مقيسٌ من الكود** لا سياسة. صياغةُ السياسة القانونية واعتمادُها خارج الهندسة، والجرد هو الحقيقة التي يجب أن تطابقها.

**ووُجد في الطريق (الدفعة 60)** جدول `notifications` يحمل `parent_id` و`child_id` ونصًّا وارتباطًا عميقًا، و**لا نافذة احتفاظ له**: لا شيء في الكود يحذفه بمرور الوقت، فتتراكم صفوفه بلا حدّ لحسابٍ نشط (تُحذف فقط بحذف الأب أو الطفل عبر `ON DELETE CASCADE`). ولم يُقترَح له رقم: اختيار المدّة هو القرار المطلوب من المراجعة، واختراعه هنا يُنتج سياسةً مصدرها مهندس — عين ما رصده هذا البند.

---

### ~~PRIV-102~~ — لا تدقيق لأي عملية على مسار العميل: 32 صف تدقيق كلها إدارية

**الأولوية:** P1 · **المنطقة:** الخصوصية / التدقيق · **الحالة:** ✅ **مُنفَّذ في الدفعة الرابعة** — انظر §15. النصّ أدناه يبقى كما وُجد.

**الدليل**
`audit_logs` = **32 صفًا** في القاعدة المحلية، وكلها من عمليات إدارية (`lib/auditLog.ts` يُكتب من موجّهات `admin*`). لا صف واحد لجلسة تشغيل، ولا لتنزيل، ولا لإبطال جهاز بادره ولي الأمر، ولا لتصدير بيانات، ولا لحذف طفل — مع أن هذه أخطر ما يمسّ بيانات الأسرة.

**المطلوب في الخطة**
`تشفير المحتوي.md:26` المعيار 22: «توجد Audit Logs لكل جلسة وإلغاء دون URL كامل أو Token أو مفتاح أو PII طفل».

**الأثر**
سؤال «من فتح هذا المحتوى، ومن ألغى هذا الجهاز، ومتى انتهى هذا الترخيص؟» لا جواب له. وفي حادثة أمنية أو شكوى أسرة، لا سجل يُراجع.

**السلوك المتوقع**
تدقيق لكل: منح/تجديد/إبطال ترخيص، بدء/إنهاء جلسة تشغيل، تسجيل/إبطال جهاز، تصدير/حذف بيانات — بنفس تنقيح `redactForAudit` الحالي.

**التبعيات** ENC-001.

**الحجم** M

**معايير القبول**
- [ ] كل عملية من القائمة أعلاه تُنتج صف تدقيق.
- [ ] لا صف يحتوي رابطًا كاملًا أو توكنًا أو مفتاحًا أو `nickname`/تاريخ ميلاد.
- [ ] اختبار يفشل إن أُضيفت عملية حسّاسة بلا تدقيق.

---

### PRIV-103 — الملكية سلطتها الـDO، والتحقق منها يكلّف نداءً كاملًا لكل حدث

**الأولوية:** P2 · **المنطقة:** الخصوصية / الأداء · **الحالة:** PARTIAL

**المشكلة**
`ownsChild` يستدعي حالة الأسرة الكاملة من الـDurable Object للتحقق من ملكية طفل واحد قبل قبول حدث تحليلات.

**الدليل** `dashboard/api/src/routes/analyticsIngest.ts:99-104` — يقرأ `/state` كاملًا ثم يبحث في القائمة. والسبب موثَّق: `children_profiles` في D1 بلا كاتب (`API-105`).

**الأثر**
التحقق صحيح أمنيًّا (وهذا هو المهم) لكنه مكلف: كل حدث تحليلات يعني نداء DO كامل. مع حصّة 240 حدثًا/دقيقة لكل عميل، هذا حِمل غير ضروري.

**التبعيات** API-105.

**الحجم** S

**التبعية انحلّت، والتعليق كان متخلّفًا (الدفعة 61)** `API-105` أُنجز في الدفعة 24: الجدول بلا كاتب هو `children_profiles`، أمّا `child_projection` فيكتبه مستهلك الطابور (`queue/familyEvents.ts`) على الإنشاء والتحديث والأرشفة، ويحمل `parent_id` و`status`. و**أربعة مسارات تتحقّق من الملكية به بالفعل** — `recommendations.ts` و`childSettings.ts` و`adminMastery.ts` و`adminAnalytics.ts` — وهي تُقدّم بيانات طفل حقيقية، أي أشدّ حساسية من كتابة صفّ قياس. فبقي هذا المسار وحده على النداء الكامل بتعليقٍ يشرح سببًا لم يعد قائمًا.

**ما أُنجز** الإسقاط أوّلًا (قراءة D1 واحدة على مفتاح أوّليّ)، والكائن الدائم **عند غياب الصفّ وحده**. وهذا لا يمكن أن يكون أغلى من قبل: كان **كل** حدث يجرّ `/state`. والاحتياطي مقصود لا احتياط: الإسقاط قد يتأخّر، وأوّلُ أحداث طفلٍ جديد هي ما يقيس رحلة التهيئة، فالرفض على تأخّرٍ كان سيفقد أنفعَ القياسات. ولا ذاكرة مؤقتة: قرارُ تخويلٍ من ذاكرةٍ قديمة يقبل طفلًا نُقل أو أُرشِف.

---

### DEBT-101 — 43 مشكلة تحليل تحجب CI، منها تحذيرات فعلية

**الأولوية:** P1 · **المنطقة:** التطبيق / الجودة · **الحالة:** DEBT

**الدليل**
`flutter analyze` = **43** مشكلة (صفر أخطاء)، و`ci.yml:29` يشغّلها بـ`--fatal-warnings`. أمثلة فعلية: `unused_import` في `creative_studio_page.dart:36` و`story_reader_page.dart:13` و`trace_home_v2.dart:8`؛ `unreachable_switch_case` في `home_feed.dart:1460`؛ `unused_local_variable` في `connect_dots_catalogue_page.dart:419`؛ `unused_element` لخمس بنى؛ وعشرات `deprecated_member_use` لـ`withOpacity`.

**ملاحظة على `unreachable_switch_case`**
هذا ليس تنظيفًا شكليًّا: حالة غير قابلة للوصول في `switch` تعني أن فرعًا كُتب ولن ينفَّذ أبدًا — سلوك مقصود مفقود.

**الحجم** S

**معايير القبول**
- [ ] `flutter analyze --fatal-warnings` أخضر.
- [ ] كل `unreachable_switch_case` مُشخَّص (هل الفرع كان مقصودًا؟) لا محذوف بلا فحص.

---

### DEBT-102 — قواعد lint مُسكَتة بدين معلَن لم يُسدَّد

**الأولوية:** P2 · **المنطقة:** الجودة · **الحالة:** DEBT

**الدليل**
`app_main/analysis_options.yaml:5-11` يُسكِت ست قواعد بتعليق صريح: «Do not fail CI on style lints until the 58 current infos are fixed (C3 follow-up)» — ومنها `avoid_print` (طباعة في تطبيق إنتاج) و`use_key_in_widget_constructors` و`prefer_const_constructors`.

**الأثر**
`avoid_print` المُسكَت هو ما يسمح بأسطر مثل `print('WROTE _studio_home.png')` في `QA-101`. والدين المعلَن (58 info) لم يُسدَّد، والرقم لم يُراجَع منذ كتابته.

**الحجم** M

**تصحيح الرقم (الدفعة 62)** «58 info» لم تكن قائمة. بإعادة تفعيل القواعد الأربع الأسلوبية كانت المحصّلة **مشكلتان** فقط — كلتاهما `use_key_in_widget_constructors` في ملفٍ واحد — و`prefer_const_constructors` و`prefer_const_declarations` و`avoid_print` **صفر**. أي أن الدين سُدِّد ضمنًا في دفعاتٍ سابقة وبقي الإسكات وحده يحمي لا شيء. وهذا هو خطر الإسكات المؤجَّل: لا أحد يعرف ثمن إعادته، فلا يُعاد.

**والستّ ليست ستًّا** اثنتان من المُسكَتات ليستا دينًا أسلوبيًّا: `invalid_annotation_target` إنذارٌ كاذب على وسوم يولّدها `freezed`/`json_serializable`، و`dangling_library_doc_comments` تخالف أسلوب رؤوس الملفات المتَّبع في مئات الملفات. بقيتا بسببٍ مكتوب وتاريخ مراجعة.

**معايير القبول**
- [x] `avoid_print` مُعاد تفعيله، ولا طباعة في مسار إنتاج. (الدفعة 62) والثلاث `print` في `lib/` كانت مُسكَتة **سطرًا بسطر** بـ`// ignore: avoid_print`: اثنتان داخل `kDebugMode` (مشروعتان) صارتا `debugPrint`، و**الثالثة كانت تطبع بلا شرط في الإنتاج** (`reportFallback`) فصارت محروسة بـ`kDebugMode`. فلم يبقَ إسكاتٌ سطريّ لهذه القاعدة.
- [x] كل استثناء باقٍ له سبب مكتوب وتاريخ مراجعة. (الدفعة 62) ومعه حرس `lint_suppressions_test.dart`: مجموعة المُسكَتات مُعلَنة في الاختبار، وكل مدخل يجب أن يسبقه تعليق.

---

### DOCS-101 — 405 ملف توثيق و4026 مربع غير مُنجَز، وتقارير تُبطل بعضها

**الأولوية:** P1 · **المنطقة:** التوثيق · **الحالة:** DEBT

**الدليل** (عدّ فعلي، باستثناء `node_modules` و`_backups` و`_archive` و`.wrangler`)
**405** ملف `.md` · **4026** مربع `[ ]` غير محدَّد في **174** ملفًا · 112 مربعًا محدَّدًا فقط.

**الأثر الملموس**
قسم `§1.4` في هذا الملف يعدّ عشرة ادعاءات في التقارير الجذرية سقطت بالفحص. مطوّر جديد يقرأ `النواقص_المتبقية.md` سيصدّق أن التطبيق لا يُبنى وأنه بلا اختبارات، ويعيد إصلاح ما هو مُصلَح، أو يبني على أرقام محتوى قديمة.

**السلوك المتوقع**
تصنيف كل ملف `.md` في الجذر: **مصدر حقيقة** / **تاريخي (لا يُعتمد)** / **مُبطَل (يُحذف)**، برأس واضح في أعلى كل ملف تاريخي. ونقل التقارير التاريخية إلى `_archive/`.

**العدّ المُحدَّث (الدفعة 63)** **409** ملف `.md` (لا 405) · **4022** مربّعًا غير محدَّد (لا 4026) في **174** ملفًا · و**158** محدَّدًا (لا 112) · و**36** ملفًا في الجذر · و`docs/` فيه **275**.

**ما أُنجز** `DOCS_INDEX.md`: التصنيف الكامل لملفات الجذر الستّة والثلاثين — **16 مصدر حقيقة** و**20 لقطة منقضية** — ولكلٍّ سببٌ وما يُقرأ بدلًا منه. ووسمٌ في **رأس** كل ملف تاريخي (`<!-- doc-status: historical -->` مع تحذير عربي)، ومعه تحذيرٌ محدَّد في `النواقص_المتبقية.md` لأنها ما استشهد به هذا البند: تقول «الكود لا يُبنى — P0» وتقدّر التطبيق بـ«20-25%»، والمقيس نظافة التحليل و712 اختبارًا. وحُذف `KIRO_IDE_LAST.md` (صفر بايت). وحرس `tools/ci/docs-classified.mjs` مربوطٌ بـCI.

**والنقل إلى `_archive/` لم يُنفَّذ بقياس** خمسة عشر من هذه الملفات لها **304 مراجع داخلة** من ملفاتٍ أخرى (`KIRO_LAST_REPORT.md` وحده 48، و`FLUTTER_APP_STATUS.md` 37، و`النواقص_المتبقية.md` 32). فالنقل يستبدل توثيقًا مُضلِّلًا بـ304 روابط مكسورة، والوسم يحلّ الأثر المذكور بلا كسر شيء. والنقل مع إعادة كتابة المراجع عملٌ مستقلّ يستحق قرارًا.

**وما لم يُلمس** الـ4022 مربّعًا. أكثرها قوائم عمل **داخل** وثائق صارت تاريخية موسومة، وشطبها بالجملة يزعم إنجازًا لم يحدث.

**الحجم** M

**معايير القبول**
- [ ] كل ملف في الجذر مُصنَّف، والتاريخي يحمل رأسًا «تقرير تاريخي — لا يُعتمد، انظر `AUDIT_FULL_2026.md`».
- [ ] `docs/FEATURE_MATRIX.md` مُصحَّح أو مُبطَل (أرقامه خاطئة: 67 مسارًا مقابل 123، و«8 وحدات MISSING» لم يبقَ منها شيء).
- [ ] مصدر حقيقة واحد لأرقام المحتوى، مُشتق من استعلام لا مكتوب يدويًا.

---

### 11.5 ما ثبت أنه سليم في هذه المنطقة

| البند | الدليل |
|---|---|
| تنقيح مركزي للتدقيق يحمي بيانات الطفل | `lib/auditLog.ts` — `redactForAudit` بقائمة منع تعاودية على أسماء المفاتيح تشمل `nickname`, `birth_month`, `birth_year`, `token`, `secret`, `pin` |
| قائمة سماح لمعاملات التحليلات بدل قائمة منع | `routes/analyticsIngest.ts:31-49` — والتعليق يشرح بدقة لماذا فشلت قائمة المنع في الاتجاهين |
| احتفاظ فعلي مُنفَّذ بـcron | `scheduled/cleanup.ts` — 180 يومًا للتحليلات، 30 للسجل الوسيط |
| تصدير وحذف بيانات مع إثبات ولي أمر أحادي الاستخدام | `routes/account.ts` + `lib/parentAuth.ts:79-89` (`export_data`, `delete_child`, `delete_account` كلها single-use) |
| موافقات مُسجَّلة لا محذوفة | `do/FamilyState.ts:420-424` — تعليق صريح: الإبطال سجل، وحذف الصف يجعل «لم يُسأل» و«قال لا» شيئًا واحدًا |
| علامة مائية بلا أي PII | `core/security/playback_watermark.dart:166-170` — hash من معرّف جلسة pseudonymous ونافذة ساعة |
| 199 إشارة semantics ودعم D-pad حقيقي | `majarra_app.dart:90-98`, `FocusableScale`, `InputModeTracker` |
| 63 ملف اختبار Flutter و69 خادميًّا و19 للوحة | تغطية حقيقية للمحركات والأمان والسياسات، وليست لقطات شكلية |

---

## §12 قرارات المالك والتبعيات الخارجية والمراجعات البشرية

هذه البنود **لا تُحلّ بالكود**. كل بند منها يحجب تاسكات هندسية مذكورة بالاسم، فتركه بلا قرار يعني أن الفريق يبني على افتراض.

### قرارات المالك — `DECIDE-*`

| المعرّف | القرار المطلوب | ما يحجبه | السياق المُتحقَّق منه | من يقرر |
|---|---|---|---|---|
| ~~**DECIDE-101**~~ | هل نطلق بمحتوى نملكه بلا DRM، أم نتعاقد على Multi-DRM قبل أي نشر؟ — **✅ حُسم بقرار مالك 2026-08-26: الإطلاق بلا DRM.** كل المحتوى من إنتاج «مجرة»، فلا جهة ترخيص تفرضه، والخطة نفسها تسمح بالوضع المرحلي للفيديو الأصلي (المعيار 5). التفصيل وشروط القرار في `§15` (الدفعة العاشرة) | ~~`ENC-009`~~ · `CNT-110` · المعيارين 5 و25 | كان `content_rights` = 0 صفًّا فلا سبيل لإثبات الملكية؛ الترحيل `0082` سجّل **248 صفّ حقوق** لكل ما هو منشور | ~~المالك +~~ الشؤون القانونية (لأي محتوى مرخَّص لاحقًا) |
| **DECIDE-102** | مصفوفة الباقات والأسعار لكل دولة وعملة، وما يميّز `family_plus` فعلًا | `API-104` · `ENC-013` · المعيار 23 | `plan_pricing` = 0 · `store_products` = 3 بلا أسعار · لا محتوى حصري يبرّر فرق السعر | المالك + المنتج |
| **DECIDE-103** | تمكين النشر الآلي من CI (توكن Cloudflare) وسياسة التراجع | `OPS-105` | ✅ **القرار صار فعلًا لا قرارًا**: مهمّة `deploy` تنشر بمجرّد وجود السرَّين، وإجراء التراجع موثَّق بأوامره في `SETUP_SECRETS.md §8`. يبقى للمالك: إضافة السرَّين، وتنفيذ تراجعٍ واحد مقصود بعد أوّل نشر | المالك |
| ~~**DECIDE-104**~~ | أي الألعاب ترفيهية عن قصد وأيها يجب أن يقيس هدفًا تعليميًّا — **حُسم هندسيًّا (الدفعة 47): التصنيف مُشتَقّ لا مُختار**. فحص `objective` يقرأ عقد المحرّك (`writesMastery`): محرّكٌ لا يكتب إتقانًا «ترفيهي أولًا» بسببٍ مكتوب، وربطُ هدفٍ به **حاجب**، ومحرّكٌ يكتب إتقانًا بلا هدف **حاجب**. فلا قائمة تُختار بيد | `CNT-105` · `CNT-109` | ~~21 من 39 لعبة منشورة بلا `learning_objective_id`~~ — ومن كان بلا هدف وله محرّكٌ يكتب إتقانًا فهو محجوبٌ عن النشر أصلًا | ~~المنتج + التعليم~~ |
| **DECIDE-110** | `instructions_ar`: هل يقرؤه التطبيق فيصير حقلًا واجبًا، أم يُسقَط من عقد التسليم؟ | `CNT-105` | الخادم يشحنه في حِزمة اللعبة و**تطبيق Flutter لا يحلّله في أي موضع** (صفر إشارة إلى `instructions`، مقابل خمس تستهلك `vo.instruction`/`vo.intro` المسموعة). والتعليمات المسموعة إلزامية ومحجوبة النشر بدونها، فالحقل ليس فجوةً في تجربة الطفل. وحشوه في 36 لعبة عملٌ يُقاس بعدد الصفوف؛ وإلزامُه بلا قارئ هو «واجهةٌ تَعِد بما لا يحدث» معكوسة | المنتج |
| **DECIDE-111** | بديل الإدخال في أسطح الرسم: هل يُبنى **رسمٌ بلوحة مفاتيح/D-pad** (مؤشّر افتراضي: تحريك، إنزال، مدّ، رفع) للطفل الذي لا يرسم بإصبعه، أم يكون البديل **نشاطًا آخر** يُقدَّم له بدل الرسم؟ | `A11Y-102` | التلفاز محجوبٌ بأربع بوابات ورسالةٍ صريحة (الدفعة 57)، فالفجوة الباقية هي الطفل ذو الإعاقة الحركية **على الهاتف**. سطح الأفعال الأدنى مقيسٌ ومحدود (أربعة أفعال في الرسم الحرّ، فعلٌ واحد في التلوين)، لكن «الرسم الحرّ بمؤشّرٍ منطقي» تجربةُ منتَج تُصمَّم لا تُستنتَج: قد يكون النشاط البديل أنفع من محاكاة إصبعٍ بأربعة أزرار. و`ColorRegion` يجعل تلوين المناطق بالتركيز رخيصًا **إن** حُوِّلت اللوحتان النقطيّتان إليه | المنتج + المالك |
| **DECIDE-105** | سياسة اللغات: أحادي اللغة (عربي) أم متعدّد، وبأي ميزانية | `I18N-101` | 2526 نصًّا حرفيًّا مقابل 255 مفتاحًا، واللغة مثبَّتة على العربية في الكود | المالك + المنتج |
| ~~**DECIDE-106**~~ | نموذج تخزين جلسة اللوحة: كوكي `HttpOnly` أم قبول خطر `localStorage` مع تعويضات — **حُسم هندسيًّا (الدفعة السادسة): `HttpOnly` غير ممكن بالاستضافة الحالية** (اللوحة على `majarra.app` والـAPI على `api.majarra.app`، فالكوكي طرف ثالث تحجبه المتصفّحات). فُعِّل مسار التعويضات، والقرار المتبقّي هو `DECIDE-109` | `SEC-106` | الرمز في `localStorage` وحده الآن، وسطح التعرّض عُوِّض بـCSP و`script-src 'self'` | ~~الهندسة +~~ المالك |
| **DECIDE-109** | هل تُقدَّم اللوحة من نفس الموقع الذي يقدّم الـAPI — إمّا وكيل `/api/*` على أصل اللوحة، وإمّا تقديمها من مسار تحت `api.majarra.app` — حتى يصبح كوكي `HttpOnly` ممكنًا؟ | `SEC-106` | الفصل الحالي يجعل كوكي الجلسة طرفًا ثالثًا: تحجبه Safari اليوم وChrome في طريقه، فلا مسار `HttpOnly` بلا تغيير استضافة. الوكيل يعني نداءً إضافيًّا لكل طلب، والدمج يعني نطاقًا واحدًا لسطحين | المالك + الهندسة |
| **DECIDE-107** | حصص التنزيل: عدد العناصر وحدّ المساحة لكل باقة، و`policy_version` جديد | `ENC-013` | الحدود اليوم ثابتة في العميل (2 GiB / 512 MiB) وحدّ أجهزة التنزيل غير مفروض خادميًّا | المنتج |
| **DECIDE-108** | هل تُفرض حدود وقت الشاشة على **الصوت والقراءة** كما على الفيديو؟ وتحديدًا: هل «حكاية قبل النوم» ممنوعة داخل نافذة النوم أم أنها المقصودة منها؟ وهل يستهلك الاستماع نفس الرصيد اليومي الموضوع للفيديو؟ | استكمال `API-101` على `books.ts` و`stories.ts` | الفيديو مُفروض عليه بالكامل (`§15` الدفعة الثالثة). الصوت لا يُنشئ عقدًا فلا يوجد ما يُحتسَب منه بلا مسار إبلاغ من العميل — والعميل ليس سلطة. الجوابان يحدّدان تصميمين مختلفين تمامًا، فلم تُخترَع السياسة | المنتج + ولي الأمر النموذجي |

### تبعيات خارجية — `EXT-*`

| المعرّف | التبعية | ما تحجبه | حالة اليوم |
|---|---|---|---|
| **EXT-101** | مزوّد Packager/CDN وMulti-DRM (Widevine + FairPlay + PlayReady عند الحاجة) وخادم تراخيص | `ENC-009` · المعيار 5 | لا تعاقد ولا إثبات مفهوم؛ ولا كود DRM في التطبيق |
| **EXT-102** | حساب Google Play Developer API وسرّ حساب خدمة للتحقق من الشراء | `API-104` | النقاط موجودة بلا عميل، وموسومة `BILLING-001` في `lib/parentAuth.ts:50-56` |
| **EXT-103** | Apple IAP وStoreKit وحساب المطوّر | `API-104` | لا وجود لها في المستودع إطلاقًا (`in_app_purchase_android` فقط في `pubspec.yaml`) |
| **EXT-104** | توكن نشر Cloudflare (`CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID`) كأسرار مستودع | `OPS-105` | ~~وموارد بيئة staging~~ — أُسقط شطر staging بقرار المالك؛ يبقى توكن النشر وحده |
| **EXT-105** | مزوّد إشعارات دفع (FCM/APNs) | `API-108` | `adminCampaigns.ts:49` يعلن صراحةً أن FCM غير مدعوم |
| **EXT-106** | تراخيص خطوط عربية وموسيقى للألعاب | `CNT-105` | `rights_licenses` = 0؛ ومحرّك `rhythm_tap` مؤجَّل لحقوق موسيقى في وثائق الألعاب |
| **EXT-107** | مزوّد TTS مُهيَّأ لتوليد السرد | `CNT-103` · `CNT-105` | `GOOGLE_TTS_API_KEY` معلَن في `.env.example`؛ والسرد الفعلي 16 صفحة من 194 |

### مراجعات بشرية — `HUMAN-*`

| المعرّف | المراجعة | ما تحجبه | حالة اليوم |
|---|---|---|---|
| **HUMAN-101** | اعتماد مصفوفة DRM/Offline/Watermark في عقود الناشرين — صار **شرطًا مسبقًا لأول محتوى مرخَّص** لا مطلبًا للإطلاق، بعد قرار المالك أن كل المحتوى من إنتاجه (`§15` الدفعة 10) | `ENC-009` (شرطيًّا) | `content_rights` = **248** بمالك `majarra` · `rights_licenses` = 0 (لا ترخيص من طرف ثالث) |
| **HUMAN-102** | إنتاج فني: فيديو الحلقات، ورسوم 178 صفحة قصة، وأصول الألعاب | `CNT-101` · `CNT-103` · `CNT-105` | 0 أصل فيديو · 16 صفحة مرسومة من 194 |
| **HUMAN-103** | مراجع شرعي معتمد + مدقّق قرآني + بوابة اعتماد لكوكب الإيمان | `CNT-106` | `islamic` = **0 سلاسل منشورة**؛ وهو أكبر شريحة مخطَّطة |
| **HUMAN-104** | مراجع تاريخي معتمد + خريطة أساس محايدة لكوكب التاريخ | `CNT-106` | `tarikh` = 2 سلسلتان منشورتان و**0 حلقة** |
| **HUMAN-105** | تعاقد مراجعين تربوي/لغوي/علمي/سلامة وإغلاق 43 مراجعة معلَّقة | `CNT-107` · `CNT-105` | `content_reviews` = 43 كلها `pending`، ولا اعتماد واحد مكتمل |
| **HUMAN-106** | مراجعة خصوصية أطفال: مدة الاحتفاظ، والامتثال (COPPA / GDPR-K)، وسياسة مكتوبة | `PRIV-101` | 180 يومًا قرار هندسي معلَن بانتظار المراجعة (`cleanup.ts:5-13`) |
| **HUMAN-112** | **إعادة توليد الملفات المُشوَّهة الترميز** بترميز UTF-8 صحيح، وأوسعها `0088_games_voice_assets.sql` (150 صفّ `title_ar`) ومعه `scripts/seed-demo-family.mjs` و`fix-arabic-names.mjs` و`fix-avatars.mjs` و`_audit_gen_sql.mjs` و`_audit_apply.sql` (الدفعة 67). التشويه **مزدوج** فلا يُفكّ آليًّا: قِستُ أربع طرق، أفضلها استعاد 37% ورفض الباقي، و`0088` وحدها أنتجت 1097 محرف إبدال. والدَّين مُجمَّد ببوابة `mojibake-scan --check` فلا ينمو | `I18N-102` · `DB-101` | 26 رسالة خطأ في `adminUsers.ts` أُصلحت يدويًّا في الدفعة 67؛ والباقي بيانات مولَّدة | المالك / خط الإنتاج |
| ~~**HUMAN-112 (الصياغة السابقة)**~~ | ~~إعادة توليد `0088_games_voice_assets.sql`~~ — دُمجت في الصفّ أعلاه. نصوصه العربية (`title_ar` لـ150 صفّ أصل) مُشوَّهة (mojibake مزدوج)، والمولِّد `tools/gen_0082.py` يكتب `utf-8` سليمًا فالعطل في الـmanifest الذي يقرأه. لم تُصلَح بالتخمين: فكُّ الترميز بفرضية Latin-1 أنتج 1097 محرف إبدال (الدفعة 64) | `DB-101` | الملف مُدرَج في `LEDGER.md` بتحفّظٍ مكتوب، وغير مُطبَّق محليًّا | المالك / خط الإنتاج |
| **HUMAN-107** | تحقق وصول على أجهزة حقيقية مع TalkBack وVoiceOver وتكبير 200% | `A11Y-101` · `A11Y-102` | صفر تحقق على جهاز؛ وإشارة واحدة لتكبير النص في كل الكود |
| **HUMAN-108** | اختبار قابلية استخدام مع أطفال 3-5 و6-8 و9-12 بموافقة أولياء الأمور | بوابة الانتقال في `PLAN_PHASE_0.md:143` | لم يُجرَ؛ وكل قرارات التجربة العمرية اليوم افتراضات فريق |

---

## §13 مطابقة الأودت السابق — `PROJECT_AUDIT_TASKS.md` (2026-08-15)

الملف السابق يحمل 117 بندًا بمعرّفات مستقرة. التصنيف هنا: **مُنجَز** (بدليل) · **قائم** (يُتابَع بمعرّف جديد في هذا الملف) · **مُبطَل** (الادّعاء نفسه لم يصمد للفحص) · **غير مُتحقَّق** (يحتاج جولة تدقيق خاصة لم تُجرَ اليوم).

### الأمان

| القديم | الحالة | الدليل / المعرّف الجديد |
|---|---|---|
| SEC-001 نقطة إدارية بلا مصادقة تكتب توصيات | **مُنجَز** | نُقلت إلى `adminRecommendations.ts` بحرس؛ `recommendations.ts:79-87` يوثّق الإصلاح |
| SEC-002 مانيفست HLS يمنح توكنات بلا استحقاق | **مُنجَز** | `episodes.ts:441-470` يشترط lease ويعيد التحقق ويفحص الإقليم |
| SEC-003 ingest تحليلات بلا مصادقة | **مُنجَز** | `analyticsIngest.ts:107-125` + `ownsChild` + حصة في `index.ts:90` + احتفاظ في `cleanup.ts` |
| SEC-004 تصاعد صلاحيات في المنح | **قائم جزئيًّا** | الحرس أُضيف (`adminTeams.ts:177`) لكن لا مقارنة صلاحيات → `SEC-103` |
| SEC-005 حدود المعدّل إرشادية وغائبة عن الأخطر | **مُنجَز مع انحدار** | ثمانية إعدادات مطبَّقة على DO (`index.ts:82-118`) — لكن حدّ الدخول صار أقسى من المُعلَن → `SEC-104` |
| SEC-006 أغراض إثبات ولي الأمر تُصدر ولا تُتحقَّق | **مُنجَز** | `parentAuth.ts:20-89` — جدول غرض↔موضع تحقق، وحُذف غرضان بدل تركهما، ويحرسه `parentProof.test.mjs` |
| SEC-007 توكن الوسائط مقبول في سلسلة الاستعلام | **قائم** | `media.ts:19` ما زال يقبل `?token=` → `SEC-111` |
| SEC-008 مخططات الـDO تتطوّر بـ`try { ALTER } catch {}` | **غير مُتحقَّق** | يحتاج مراجعة `do/*.ts` سطرًا بسطر؛ يوجد `doSchema.test.mjs` لكن لم أفحص كفايته |
| SEC-009 Durable Object مربوط وغير قابل للوصول | **مُنجَز** | `StoryCollab` مُصدَّر ومُركَّب في `index.ts`، وله جدول migrations tag v3 |
| SEC-010 مُرسِلا بريد يختلفان في الأولوية | **غير مُتحقَّق** | لم أفحص `services/email.ts` في هذه الجولة |

### العمليات

| القديم | الحالة | الدليل / المعرّف الجديد |
|---|---|---|
| OPS-001 CI لم يعمل قط | **مُنجَز شكلًا، أحمر واقعًا** | `ci.yml:7-11` على `master`، وكل الوظائف صريحة — لكن أربع منها تفشل والخامسة تتعلّق → `OPS-102` `QA-101` |
| OPS-002 سجل الترحيلات لا يطابق المجلد | **قائم** | تكرار 0018 و0051، فجوات 20/52/53، و80 ملفًا مقابل 81 صفًا → `DB-101` |
| OPS-003 لا حساب مسؤول | **قائم** | `admin_users` = 0 اليوم أيضًا → `SEC-102` |
| OPS-004 لا مسح أسرار ولا تدقيق تبعيات ولا بوابة إصدار | **مُنجَز** | `ci.yml:126-159` (ماسح بقواعد المستودع مع اختبار ذاتي) + `deploy-gate` |
| OPS-005 نظافة المستودع: 354 مسارًا غير نظيف | **قائم وأسوأ** | 566 مسارًا اليوم، ونسخة قاعدة إنتاج غير مُتجاهَلة → `OPS-103` `OPS-107` |
| OPS-006 النشر والتراجع يدويان | **مُعالَج** | الدفعة 28: `deploy` ينشر بوجود السرَّين ويتحقّق من `/version` ويوسم الإصدار؛ والتراجع موثَّق بأوامره → `OPS-105` |
| OPS-007 الـcron وفحوص الصحة لا تفعل شيئًا | **مُنجَز جزئيًّا** | الاحتفاظ حقيقي الآن (`cleanup.ts`)، لكن `ops_health_checks` = 0 → `OPS-106` |
| OPS-008 لا جرد لأعلام الميزات | **قائم** | `feature_flags` = 0 و`remote_config` = 3 بلا جرد موثَّق |
| OPS-009 303 استيرادًا بلا امتداد يمنع تحميل نقطة الدخول | **مُنجَز** | ثلاث مجموعات تُحمّل `src/index.ts` فعلًا اليوم (`entrypoint`, `homeBuilderE2E`, `rateLimit`) |

### المحتوى والوسائط

| القديم | الحالة | الدليل / المعرّف الجديد |
|---|---|---|
| CONTENT-001 لا أصل صوتي إطلاقًا | **مُنجَز جزئيًّا** | **30** أصلًا صوتيًّا اليوم، منها 16 مستخدم كسرد → `CNT-103` `CNT-104` |
| CONTENT-002 لا أصل فيديو، 20 حلقة غير قابلة للتشغيل | **قائم كما هو** | `kind='video'` = 0 · حلقات قابلة للتشغيل = 0 → `CNT-101` |
| CONTENT-003 مواسم تعلن حلقات لا توجد، ولا موسم منشور | **قائم** | `seasons` = 39 صفًا · **منشور = 0** |
| STORY-001 لا قصة قابلة للنشر | **قائم جزئيًّا** | قصتان مكتملتا الرسوم والسرد الآن، و178 صفحة بلا رسم → `CNT-102` `CNT-103` |
| STORY-002 مسار ما قبل النوم ومنحنى الإضاءة والحركة | **غير مُتحقَّق** | لم أفحص القارئ مقابل مواصفته في هذه الجولة |
| STORY-004 نصف الصفحات بلا مدة سرد ونظامان متعارضان | **غير مُتحقَّق** | `dwell_ms` مُنفَّذ، لكن مطابقة الإيقاع تحتاج جولة محتوى |
| BOOK-001 لا كتاب قابل للنشر | **قائم** | 22 كتابًا كلها `draft`، ولا جدول صفحات أصلًا → `CNT-102` `API-107` |
| GAME-001 ثلاث ألعاب مكتملة من 87 | **تغيّر** | 39 لعبة منشورة اليوم، لكن 36 بلا تعليمات و21 بلا هدف → `CNT-105` |
| MEDIA-001 115 من 119 أصلًا بحجم صفري | **قائم وأوسع** | **157 من 199** أصلًا بـ`size_bytes` صفري/فارغ |
| MEDIA-002 أصول يتيمة ومراجع مفقودة | **قائم** | 30 أصلًا صوتيًّا و**صفر** في `asset_links` → `CNT-104` |
| DATA-004 35 مراجعة معلَّقة ولا اعتماد | **قائم وأوسع** | **43** مراجعة كلها `pending` → `CNT-107` |
| DATA-006 صور الكواكب غائبة وثلاثة كواكب لا تؤدي لشيء | **قائم** | `islamic` 0 سلاسل · `maharat` و`tarikh` 0 حلقات → `CNT-106` |
| DATA-007 نموذج الحقوق فارغ ولا يُستشار عند النشر | **نصفه مُغلَق** | `content_rights` صار **248 صفًّا** (الترحيل `0082`، الدفعة 10)؛ ويبقى أنه لا يُستشار عند النشر → `CNT-110` |
| DATA-005 أهداف تعليمية منحلّة (ست ألعاب بهدف واحد) | **مُبطَل** | 57 هدفًا مزروعًا و**50 هدفًا مميّزًا** مستخدمًا في الألعاب اليوم؛ الوصف لم يصمد |
| DATA-009 الموقع والمدونة فارغان بنيويًّا | **قائم** | `web_pages` 19 صفًا و**0 منشور** · `blog_posts` = 0 |

### التطبيق

| القديم | الحالة | الدليل / المعرّف الجديد |
|---|---|---|
| APP-001 وضع الضيف بطفل تجريبي ثابت في بناء الإنتاج | **قائم جزئيًّا** | 33 إشارة `isDemo`/`demoChild` في `lib/`؛ الوضع صار جلسة محلية معلَّمة لا طفلًا وهميًّا — يحتاج قرار (`DECIDE-002` القديم) |
| APP-002 انقطاع الخادم يستبدل كاتالوجًا مزيفًا | **قائم** | `content_repository.dart:36-88` + `local_catalog.dart` → `APP-103` |
| APP-003 اسم شخص حقيقي مثبَّت في التنقّل | **مُنجَز** | الاسم لم يبقَ إلا داخل تعليق يشرح الإصلاح: `majarra_bottom_navigation.dart:13` |
| APP-004 بوابة التحديث الإلزامي لا تعمل (إصدار ثابت) | **مُنجَز** | `package_info_plus` مثبَّت ومُستخدَم، و`X-App-Version` يرسل `AppVersion.current` |
| APP-005 كود ميت واعتماديات غير مستخدمة | **مُعالَج** الدفعة 33 | `hive_flutter` صفر استخدام · `playback_page.dart.new` · 4 استيرادات غير مستخدمة → `APP-105` |
| APP-006 اثنا عشر مسارًا صامتًا يجعل الانقطاع يبدو نقص محتوى | **قائم** | 56 كتلة `catch (_) {}` في 21 ملفًا → `APP-106` |
| APP-009 إقران TV placeholder صادق | **قائم** | لا نقطة نهاية أصلًا → `API-103` `APP-110` |
| OFFLINE-001 لا نقطة دخول لتنزيل الفيديو | **قائم** | زر التنزيل الوحيد في `audio_player_page.dart:357` → `ENC-008` `ENC-009` |
| OFFLINE-002 كاش القارئ والكاتالوج يبقى بعد الخروج | **قائم ومُثبَت باختبار فاشل** | `logout_tile_test.dart` + `catalog_cache_test.dart:82` → `ENC-012` |
| PARENT-001 الرقابة الأبوية تُضبط ولا تُفرض | **قائم كما هو** | صفر قراءة لـ`child_settings` خارج ملفها → `API-101` |
| DATA-001 جداول الطفل والتقدّم بلا كاتب | **قائم كما هو** | `children_profiles` · `watch_progress` · `child_screen_time_daily` كلها 0 وبلا `INSERT` → `API-105` |
| ANALYTICS-001 لا ناقل تحليلات ولا تقارير انهيار | **مُنجَز جزئيًّا** | `MajarraAnalytics` بـ28 إشارة وناقل إلى `analytics/events`؛ `analytics_events` = 0 صفًا فلم يُثبَت وصوله |
| ANALYTICS-002 19 لوحة أرقام مفبركة | **مُنجَز جزئيًّا** | اختبارات اللوحة تفرض «شرطة وسبب لا صفر» للمقاييس غير المتاحة؛ وعُمِّم على `/ops/overview` طرفًا لطرف في الدفعة 41 (قراءةٌ فاشلة = `null` + اسم المسبار، والحالة `unknown` لا `healthy`)؛ ويبقى 85 موضع `?? 0` تحتاج قاعدة → `ADM-106` |
| BILLING-001 لا شيء قابل للشراء | **قائم كما هو** | `plan_pricing` = 0 · `store_products` = 3 بلا أسعار → `API-104` |
| STREAM-001/002/003 الجودة والمسارات والترجمات وفحص الحقوق | **قائم** | الجداول الثلاثة 0 صفًا وفحوص الحقوق placeholders → `APP-110` |
| STREAM-004 DRM وHDCP وتراخيص Offline | **قائم كما هو** | لا DRM في المستودع → `ENC-009` |
| I18N-001/002 التوطين موجود بنيويًّا وغير مستخدم | **قائم ومُقاس** | 2526 نصًّا حرفيًّا في 102 ملف مقابل 255 مفتاحًا → `I18N-101` |
| A11Y-001/002 تكبير النص وعدم التحقق على جهاز | **قائم** | إشارة واحدة لـ`textScaler` في كل الكود → `A11Y-101` `HUMAN-107` |
| PERF-001/002/003 صفحات بحدّ 100، واستعلامات لا تتوسّع، ولا قياس أداء | **قائم** | `limit=100` ثابت في `majarra_api_client.dart`؛ ولا قياس → `PERF-101` `PERF-102` |

### اللوحة والاختبارات والتوثيق

| القديم | الحالة | الدليل / المعرّف الجديد |
|---|---|---|
| ADMIN-001 لا نشر للقصص والكتب والألعاب والمشروعات | **مُنجَز** | `adminPublish.ts:136-139` ببوابة تفشل مغلقة |
| ADMIN-015 مواءمة نشر السلاسل والحلقات مع البوابة | **قائم** | `adminPublish.ts:81-88` يسجّلها كمتابعة لم تُنفَّذ → `API-106` |
| ADMIN-003 شاشة الجلسات تعرض مصفوفة وهمية | **مُنجَز** | لا نتائج `mock` في صفحات الإنتاج؛ كلها في `src/test/**` |
| ADMIN-005 28 صفحة تعطّل فحص الأنواع | ~~قائم وأوسع بواحدة~~ **مُنجَز** (الدفعة 40) | كانت **29**؛ رُفع التعطيل عنها كلّها وصار الفحص يشمل الـ118 صفحة، وحرسٌ يمنع عودته |
| ADMIN-009 أزرار متحوّلة بلا حجب صلاحية | **مُنجَز على مستوى المسار** (الدفعة 39) و`ADM-103` قبله | حجب المسارات صار بخريطة معلَنة ونقطة إنفاذ واحدة؛ وحجب **كل زرّ داخل** الشاشة يبقى موزّعًا على الصفحات، والخادم هو السلطة عليه |
| ADMIN-011 مكوّنات ميتة ونقاط خادم يتيمة | **مُنجَز جزئيًّا** (الدفعة 42) | `NotImplementedPage` حُذف؛ و`adminAvailability` صارت لها شاشة (الدفعة 38)؛ و`adminPublishGate` **لم تكن يتيمة** — `publishReadiness` تنادِيها، وإنما خريطة تركيب المولِّد لم تكن تراها. ويبقى `adminBackup` بلا شاشة بقرار موثَّق (`POST /restore` يُعيد 501) → `ADM-101` |
| ADMIN-012 الاستجابة مُتحقَّقة في 7 من 114 مسارًا خارج CI | **غير مُتحقَّق** | سكربتات المتصفح موجودة في `front/test/browser/`؛ لم أشغّلها |
| TEST-001 الاختبارات التي كان يجب أن تكشف ثغرات P0 عمياء بنيويًّا | **مُنجَز جزئيًّا وكشف ثغرة** | مسح الحرَس صار يغطي كل الموجّهات المتحوّلة — **ولذلك يفشل الآن** على `adminBilling` → `SEC-101` |
| TEST-002 أخطر سطحين في Flutter بلا اختبار | **قائم** | `playback_page` 3434 سطرًا و`creative_studio_page` 4373 بلا تغطية → `APP-101` |
| TEST-003 تغطية اللوحة ~15% | **غير مُتحقَّق** | لا قياس تغطية مُهيَّأ في أي طبقة → `QA-103` |
| TEST-004 لا تغطية بصرية أو golden | **قائم وبأثر عكسي** | لا اختبار golden؛ وما يوجد هو سكربت PNG يعلّق المجموعة → `QA-101` |
| DOCS-001…006 تصحيح التقارير وإعادة توليد الأرقام | **قائم** | 405 ملف md و4026 مربعًا غير محدَّد → `DOCS-101` |
| DECIDE-001…010 عشرة قرارات مالك | **قائم** | أُعيد صياغتها وتقليصها إلى سبعة قرارات فعّالة في `§12` مع ما يحجبه كل قرار. أضافت الدفعات بعدها `DECIDE-108` (حدود وقت الشاشة على الصوت والقراءة) و`DECIDE-109` (استضافة اللوحة على نفس الموقع)، وحُسم `DECIDE-106` هندسيًّا |

### الخلاصة العددية للمطابقة

| التصنيف | العدد |
|---|---:|
| **مُنجَز** بالكامل | 12 |
| **مُنجَز جزئيًّا** (تقدّم حقيقي، بقية مُتابَعة) | 8 |
| **قائم** (يُتابَع بمعرّف جديد هنا) | 33 |
| **مُبطَل** (الادّعاء لم يصمد) | 1 (`DATA-005`) |
| **غير مُتحقَّق** في هذه الجولة | 6 (`SEC-008`, `SEC-010`, `STORY-002`, `STORY-004`, `ADMIN-012`, `TEST-003`) |
| بنود مؤجَّلة عن قصد (P3 في الملف القديم) | 11 — لم يُعَد فتحها |

> بقية بنود الملف القديم (وأكثرها P2 في اللوحة والمحتوى) لم تُفحَص بندًا ببند في هذه الجولة، ويجب اعتبارها **غير مُتحقَّقة** لا مُنجَزة.

---

## §14 جدول الملخص الكامل

86 بندًا هندسيًّا مرتَّبة بالأولوية ثم بالمعرّف.

### P0 — موانع إطلاق (21)

| المعرّف | العنوان | المنطقة | الحالة | الحجم |
|---|---|---|---|:---:|
| ~~`QA-101`~~ | ~~اختبار PNG مؤقت يعلّق `flutter test` نهائيًّا~~ &#10004; الدفعة 1 | اختبارات/CI | DEAD-UNUSED | XS |
| ~~`SEC-101`~~ | ~~استرداد ومنح باقات بلا فحص صلاحية~~ &#10004; الدفعة 1 | API/فوترة | MISSING | S |
| ~~`OPS-103`~~ | ~~نسخة قاعدة إنتاج غير مُتجاهَلة في الشجرة~~ &#10004; الدفعة 1 | عمليات/git | PARTIAL | XS |
| `SEC-102` | لا حساب مسؤول، فالمفتاح المشترك هو الباب — 🟡 الأداة والإجراء جاهزان (الدفعة 3)، **والبذر فعلٌ بشري** (`SETUP_SECRETS.md §0`) | أمان | PARTIAL | S |
| ~~`QA-102`~~ | ~~13 اختبارًا فاشلًا في Flutter~~ &#10004; الدفعة 1 | اختبارات | REGRESSION | L |
| ~~`OPS-101`~~ | لا بيئة staging — **مُغلَق بقرار مالك: كله إنتاج** (`§15`) | بنية | CLOSED | — |
| ~~`OPS-102`~~ | ~~CI أحمر ووظيفة Flutter تتعلّق~~ &#10004; الدفعة 1 | CI | REGRESSION | M |
| `ENC-001` | ~~لا منظومة ترخيص Offline: صفر نقطة وصفر جدول~~ 🟡 نصف الخادم مُنفَّذ (الدفعة 12): سلطة + حدود + إصدار موقَّع + إبطال؛ يبقى العميل | API/DB/تشفير | PARTIAL | XL |
| `ENC-002` | مفتاح واحد للتطبيق بدل CEK مغلَّف للجهاز | تشفير | PARTIAL | L |
| ~~`ENC-003`~~ | ~~تنزيل مكشوف كامل ثم تشفير (ممنوع في §8)~~ ✅ الدفعة 8 | تشفير | DONE | L |
| ~~`ENC-004`~~ | ~~فك التشفير إلى ملف مكشوف للتشغيل (ممنوع في §12)~~ ✅ الدفعة 11 | تشفير | DONE | L |
| ~~`ENC-005`~~ | ~~ترخيص محلي بساعة الجهاز وبلا توقيع~~ ✅ الدفعة 13 | ترخيص | DONE | L |
| ~~`ENC-008`~~ | ~~التنزيل للمحتوى العام فقط: بلا ترويسة تخويل~~ ✅ الدفعة 13 | تشفير/API | DONE | M |
| ~~`ENC-009`~~ | لا DRM ولا HDCP ولا تنزيل فيديو — **تنازل موثَّق بقرار مالك** (`§15` الدفعة 10)؛ يعود نشطًا لأي محتوى مرخَّص | بث | ACCEPTED-TRADEOFF | XL |
| ~~`ENC-010`~~ | ~~الإبطال لا يصل إلى أي ملف على جهاز~~ ✅ الدفعة 15 | ترخيص | DONE | M |
| ~~`API-101`~~ | الرقابة الأبوية تُكتب ولا تُقرأ — **مُنفَّذ للفيديو** (`§15` الدفعة الثالثة)؛ الصوت والقراءة على `DECIDE-108` | رقابة أبوية | DONE (فيديو) | L |
| `API-104` | لا مسار شراء عامل ولا أسعار | فوترة | PARTIAL | XL |
| `CNT-101` | صفر فيديو → صفر حلقة قابلة للتشغيل. الشطر البرمجي مُنجَز (الدفعة 43): مسار النشر واحدٌ والبوابة تحجب حلقةً بلا فيديو، وأُضيفت نقطة **مسح البوابة** تُعيد تشغيل البوابة على كل صفّ حيّ ولها شاشة. والباقي إنتاجٌ لا كود: **قرار مالك** | محتوى | MISSING | XL |
| `CNT-102` | صفر قصة وصفر كتاب منشور. الشطر البرمجي مُنجَز (الدفعة 44): مسح البوابة صار يقبل `status=ready` فيُعيد لكل صفٍّ منتظرٍ **ما ينقصه بالاسم** — وهي القدرة التي تحوّل «12 قصة عند جاهزة» إلى خطوة. والباقي نشرٌ وإنتاج: **قرار مالك** | نشر | PARTIAL | M |
| `CNT-103` | 178 من 194 صفحة قصة بلا رسم. معيار التقرير الآلي مُنجَز (الدفعة 45): البوابة تُنتجه وهي أدقّ من الاستعلام المقترح، وصار العرض يُظهر العدد والمواضع بعد أن كان يعرض اسم الفحص وحده. والباقي إنتاجٌ: **قرار مالك** | محتوى | PARTIAL | XL |
| `CNT-106` | ~~ثلاثة كواكب لا يصل إليها طفل~~ الدفعة 48: معيار «قريبًا» **مُنجَز** (البطاقة تعرض الحقيقة عند صفرٍ مقيس، و`null` ليست صفرًا)؛ والدليل كان يعدّ الحلقات فأخطأ في الاتجاهين — `maharat` و`tarikh` فيهما ما يُفتح، و`qiyam` فارغ. **ومعيار المراجعة الشرعية مُنفَّذ في الكود** (تصحيح في الدفعة 49: حواجب لا تحذيرات). والباقي إنتاجٌ XL | محتوى | PARTIAL | XL |

### P1 — أساسي قبل إطلاق جدّي (42)

| المعرّف | العنوان | المنطقة | الحجم |
|---|---|---|:---:|
| ~~`ENC-006`~~ | ~~لا chunking ولا AAD والملف كله في الذاكرة~~ ✅ الدفعة 7 | تشفير | L |
| ~~`ENC-007`~~ | ~~لا `sha256` للأصل ولا manifest موقَّع~~ ✅ الدفعة 14 | تشفير | M |
| ~~`ENC-011`~~ | ~~فشل مخزن المفاتيح مكتوم~~ ✅ الدفعة 7 | تشفير | S |
| ~~`ENC-012`~~ | ~~الكاش يبقى بعد الخروج ويقبل المنتهي والمستقبلي~~ &#10004; الدفعة 2 | كاش/خصوصية | S |
| `ENC-013` | حدود التنزيل والاستحقاق في العميل فقط | تشفير/API | M |
| ~~`SEC-103`~~ | ~~منح الأدوار بلا مقارنة صلاحيات~~ &#10004; الدفعة 21 | RBAC | M |
| ~~`SEC-104`~~ | ~~حدّ الدخول يرفض من الثالثة ويعلن خمسًا~~ &#10004; الدفعة 2 | أمان/تجربة | S |
| ~~`SEC-105`~~ | ~~لا certificate pinning~~ ✅ الدفعة 5 | تطبيق | M |
| ~~`SEC-106`~~ | ~~رمز جلسة اللوحة في `localStorage`~~ ✅ الدفعة 6 (تنازل موثَّق + تعويضات؛ `DECIDE-109`) | لوحة | M |
| ~~`SEC-108`~~ | ~~صفحات HTML عامة بلا ترويسات أمان~~ ✅ الدفعة 6 | موقع | S |
| ~~`SEC-110`~~ | ~~لا تحقق مخطَّطي مركزي للمدخلات~~ &#10004; الدفعتان 18 و23 (13 موجّهًا) | API | L |
| ~~`SEC-111`~~ | توكن الوسائط مقبول في سلسلة الاستعلام — **تنازل موثَّق P2** (الدفعة 6)؛ يبقى توسيع `GameAudioPlayer` للترويسات | وسائط | ACCEPTED-TRADEOFF | XS |
| ~~`API-102`~~ | ~~حدود الباقات بمصدرَي حقيقة~~ &#10004; الدفعة 22 | API/DB | M |
| `API-103` | لا إقران أجهزة ولا اعتماد TV | أجهزة | L |
| ~~`API-105`~~ | ~~ثلاثة جداول تُقرأ ولا يكتبها شيء~~ &#10004; الدفعة 24 | API/DB | L |
| ~~`API-106`~~ | ~~نشر السلاسل والحلقات يتخطّى البوابة~~ &#10004; الدفعة 25 | نشر | M |
| ~~`DB-101`~~ | ~~سجل ترحيلات غير متسق~~ &#10004; الدفعة 26 (البناء النظيف → DB-104) | DB | M |
| ~~`DB-104`~~ | ~~بناء قاعدة نظيفة يتوقّف عند 0074~~ &#10004; الدفعة 27 (٩٠/٩٠ + بوابة CI) | DB | M |
| ~~`OPS-104`~~ | ~~أسرار إنتاج في ملف نصي وسجلات ضخمة~~ &#10004; الدفعة 2 | أسرار | S |
| ~~`OPS-105`~~ | ~~النشر لا يحدث من CI~~ &#10004; الدفعة 28 (ينشر بمجرّد وجود السرَّين + `/version` + وسم) | نشر | M |
| ~~`OPS-106`~~ | ~~لا رصد فعلي و24 حدثًا فاشلًا بلا تنبيه~~ &#10004; الدفعة 20 (يبقى ضبط OPS_ALERT_EMAIL) | رصد | L |
| `APP-101` | أربعة ملفات آلهة بلا اختبار | معمارية | XL |
| `APP-102` | **تسع** ميزات بلا `data/` ولا `domain/` — الدفعتان 29 و30 أغلقتا الأخطر (مسارا شبكة بلا تثبيت، وضوابط رقابية تفشل صامتة، وستّة مزوّدات في ملفات صفحات)؛ وبقي نقل ١٥ نداء عميل | معمارية | L |
| `APP-103` | كاتالوج مبندل يحلّ محل الحقيقي — الدفعة 31 أوقفت حشره مع البيانات الحيّة ووحّدت نطاق الأصول؛ وبقي قرار مالك عن تجربة الانقطاع | محتوى | M |
| ~~`APP-106`~~ | ~~56 كتلة `catch (_) {}` صامتة~~ &#10004; الدفعة 19 (presentation متروكة بقرار) | أخطاء | M |
| `ADM-101` | ~~508 نقطة مقابل 123 مسارًا، وثلاثة موجّهات بلا شاشة~~ → **497** نقطة (~~370~~ في الدفعة 38 كان خطأ عدّ، صُحّح في الدفعة 42)، والموجّهات الثلاثة **لها سطوح**. الدفعة 38 أضافت قائمة قيود الإتاحة؛ والدفعة 42 أصلحت أداة العدّ وربطتها بـCI؛ وبقي التصنيف المعاكس: نقاطٌ بلا مُستهلِك | لوحة | L |
| ~~`ADM-102`~~ | ~~حجب التصاريح غائب عن 101 من 118 صفحة~~ &#10004; الدفعة 39: خريطة مسار→صلاحية للمسارات الـ123، ونقطة إنفاذ واحدة في `AdminLayout`، والقائمة الجانبية تشتقّ منها بدل قائمة ثانية خالفتها في خمسة مواضع | لوحة | M |
| ~~`ADM-103`~~ | ~~لوحة الأوامر تعرض أوامر إنشاء لغير المصرَّح~~ &#10004; الدفعة 1 | لوحة | S |
| ~~`ADM-105`~~ | ~~29 صفحة تتخطّى فحص الأنواع~~ &#10004; الدفعة 40: التعطيل مرفوع عن الـ29 (عشرةٌ منها كانت بلا خطأ واحد)، و48 خطأً مُصلَحة منها أربعة أعطال مرئية، وحرسٌ على كل `src` مُتحقَّق من قدرته على الفشل | لوحة | L |
| `CNT-104` | ~~14 أصلًا صوتيًّا يتيمًا~~ معيار التقرير مُنجَز (الدفعة 46): «مسارَا ربط» كانا **16 عمودًا في 13 جدولًا**، والتقرير يكتشفها من المخطَّط ويرفض إن لم يكتشف شيئًا، وله شاشة تُظهر تغطيته. والتصنيف قرار محتوى | محتوى | S |
| `CNT-105` | ~~36 من 39 لعبة بلا تعليمات و21 بلا هدف~~ الدفعة 47: «ولا صوت يشرح» **غير صحيح** (التعليمات المسموعة إلزامية وتحجب النشر، ومحروسة باختبار)؛ ومعيار الهدف **مُنجَزٌ بأفضل** (تصنيف مُشتَقّ من عقد المحرّك لا وسم يدويّ)؛ وحقل `instructions_ar` **لا قارئ له في التطبيق** → `DECIDE-110`. وعُثر على عطلٍ حقيقيّ في المسار نفسه: سقوطُ لغةٍ لحقلٍ واحد كان صامتًا، فصار مُسمّى (`text_fell_back`) | ألعاب | L |
| `CNT-107` | ~~43 مراجعة كلها معلَّقة~~ الدفعة 49: البوابة **تقرأ حالة المراجعة أصلًا** (الرفض حاجب، والغياب تحذير بقرارٍ موثَّق لأن حجب الكل يُعلِّم التجاوز)؛ والمكسور كان أن **التحذير المُتجاهَل غير مرئي** في مسح البوابة الذي كتبتُه في الدفعتين 43–44 — أُضيف `warned` منفصلًا عن `blocked`. والباقي: تعاقد المراجعين (`HUMAN-105`) | سير عمل | M |
| `CNT-108` | ~~20 حلقة منشورة بمدة فارغة~~ الدفعة 50: لم تكن «فارغة» بل تُقرّ بأن الحلقة **«قصيرة»** — دعوى لم يقِسها أحد. صار المجهول `null` وستة مواضع تُسقط التسمية (ومنها قارئ الشاشة). والاشتقاق من الأصل **محجوب** لا مؤجَّل: لا عمود مدة في `content_assets` وصفر أصل فيديو | بيانات | S |
| `CNT-109` | 57 هدفًا وصفر سؤال يقيسها. الدفعة 51: تبويب التشخيص كان يكتب **عناوين أعمدته مكان قيمها** (بلا قراءة حرفٍ من القاعدة)، فصار يقيس أربعة أعداد — ومنها «ألعاب قادرة على القياس» التي تستثني المحرّكات التي لا تكتب إتقانًا، فيُفرَّق بين «أعِد الربط» و«أنتِج محتوى». والتأليف نفسه محتوى | تعلّم | L |
| `CNT-110` | ~~صفر حقوق مسجَّلة~~ الملكية سُجّلت (الدفعة 10)؛ والدفعة 52: دعوى «لا بوابة تفحص الحقوق» **قديمة**، ومعيار المصفوفة **مُنفَّذ بمعناه بعد قرار بلا DRM** — التنزيل صار يقرأ `licenses` ويرفض ما يستثني `offline` (والتجديد محروس كالإصدار)، والغياب ليس منعًا وإلّا تعطّلت القصص بقيدٍ هندسيّ. ويبقى `content_availability` بصفٍّ واحد: **بيانات** | حقوق | M |
| `QA-103` | ~~لا اختبارات تكامل ولا **قياس تغطية**~~ الدفعة 53: التغطية مقيسة (31.92%) وحدُّها 31% في CI، ويُحرَس عدد الملفات أيضًا لأن النسبة قد ترتفع بانخفاض التغطية الحقيقية، والتقرير الغائب يُرفَض. ويبقى مسار التكامل على محاكٍ: **محجوب** على محتوى الفيديو | اختبارات | L |
| `I18N-101` | ~~2526~~ **2,683** نصًّا حرفيًّا في 147 ملفًا مقابل 255 مفتاحًا (العدد نما، فلا بوابة). الدفعة 55: معيار «لا خيار لغة لا يعمل» **مُستوفًى** في السطوح الثلاثة؛ و«دالّة الإجبار» في `locale_catalog_test` كانت **داخل شرطٍ يُلغيها** وقد توقّفت عن التأكيد بصمت (تغطية ARB 100%)، فصارت على المانع الحقيقي ومُتحقَّقٌ أنها تفشل. والقرار والاستخراج: **مالك** | توطين | XL |
| `I18N-102` | **بندٌ جديد (الدفعة 67، ليس من الأودت الأصلي)**: نصٌّ عربي مُشوَّه الترميز في **15 ملفًا**، منها بيانات تُعرَض لمستخدم — و**26 رسالة خطأ** في `adminUsers.ts` أُصلحت. الباقي مُجمَّد بخطّ أساسٍ مقيس وبوابةٍ في CI لأن التشويه **مزدوج** فلا يُفكّ آليًّا (37% استعادة و1097 محرف إبدال). وإعادة التوليد → `HUMAN-112` | توطين/بيانات | S |
| `A11Y-101` | إشارة واحدة لتكبير النص. الدفعة 56: مِسحة 1.0/1.3/2.0 مبنيّة، وكشفت تجاوزًا حقيقيًّا **29 بكسل** في شريط كواكب `planets_page.dart:446` فأُصلح بربط ارتفاع الشريط بحجمَي خطّ التسمية. المِسحة تغطّي **شاشتين** لا كلّ الشاشات، والتحقّق على جهاز حقيقي `HUMAN-107` | وصول | L |
| `PERF-101` | ~~358 ميغابايت أصول بصيغ مزدوجة~~ الدفعة 58: **المُبندَل فعلًا 116.12 MB** (الإعلان غير تعاودي)، والمستودع يتعقّب **8.85 MB** فقط فـ«ثقل المستودع» غير قائم. وحُرِس ما يهمّ: حدٌّ مقيس على الحِمل + حدّ عتبة Play على الحزمة + رفض مسار أصلٍ في `lib/` غير مُبندَل — وقد أوقع **11 مسارًا ميتًا** منها هيرو شاشة التتبّع | أداء | M |
| `PRIV-101` | مدة الاحتفاظ قرار هندسي بلا مراجعة — 🟡 الدفعة 60: الرقم صار **إعدادًا** (`ANALYTICS_RETENTION_DAYS`) بلا تغييره، وكُتب `docs/DATA_RETENTION.md` جردًا مقيسًا. **والاعتماد الكتابي والسياسة القانونية فعلٌ بشري** (`HUMAN-106`). ووُجد `notifications` يحمل `child_id` **بلا نافذة احتفاظ** | خصوصية | S |
| ~~`PRIV-102`~~ | ~~لا تدقيق لأي عملية على مسار العميل~~ ✅ الدفعة 4 | تدقيق | M |
| ~~`DEBT-101`~~ | ~~43 مشكلة تحليل تحجب CI~~ &#10004; الدفعة 2 | جودة | S |
| `DOCS-101` | ~~405 ملف توثيق و4026 مربعًا~~ **409** و**4022** — 🟡 الدفعة 63: ملفات الجذر الـ36 مُصنَّفة في `DOCS_INDEX.md` (16 مصدر · 20 لقطة منقضية)، ووسمٌ في رأس كل تاريخي، وحرسٌ في CI. و**النقل إلى `_archive/` مرفوض بقياس**: 304 مراجع داخلة إلى 15 ملفًا. والـ4022 مربّعًا لم تُلمس بقصد | توثيق | M |

### P2 — جودة وقابلية تشغيل (23)

| المعرّف | العنوان | الحجم |
|---|---|:---:|
| ~~`ENC-014`~~ | ~~حماية العرض ناقصة على iOS وبلا تحقق على جهاز~~ ✅ الدفعة 9 (الرصد؛ التحقّق على جهاز يبقى) | M |
| `ENC-015` | نموذج الحزمة `.mjrpack`/`.mjrlic` غير منفَّذ | L |
| ~~`SEC-107`~~ | ~~لا كشف Root/Jailbreak ولا سياسة تخفيف~~ ✅ الدفعة 16 | M |
| ~~`SEC-109`~~ | ~~توكنات بلا `kid`، فالتدوير يُخرج كل المستخدمين~~ ✅ الدفعة 17 | S |
| ~~`API-107`~~ | ~~صفحات الكتب عمود JSON بلا ربط أصول~~ &#10004; الدفعة 68: **التشخيص كان معكوسًا** — الصفحات في `story_pages` بأصولها وتوطينها، والعمود ميت لا يُقرأ في مسار القراءة. والعطل الحقيقي أن **بوابة النشر** كانت تحكم على العمود الميت في ثلاثة مواضع، فلا كتاب يُنشَر وكتابٌ بلا صفحة يُنشَر. صار المصدر واحدًا، ومعه فحصُ رسم كل صفحة ونصّها | M |
| `API-108` | لا إشعارات دفع | L |
| ~~`DB-102`~~ | ~~جداول ميتة في المخطط ووثيقة تبني عليها~~ &#10004; الدفعة 65: الأثر **كان واقعًا لا احتمالًا** — أربعة استعلامات إدارية على جدولين بلا كاتب، فاللوحة تعرض «صفر جهاز» لكل أسرة. أُعلن عدم التوفّر بدل الصفر، وحرس `deadTables` يمنع العودة، والوثيقة صُحّحت. **ووُجد أن `tsc --noEmit` في اللوحة يفحص صفر ملف** | S |
| ~~`DB-103`~~ | ~~المفاتيح الأجنبية غير مفروضة~~ &#10004; الدفعة 66: **الفرضية نُقضت** — `PRAGMA foreign_keys` = 1، والانتهاك مرفوض، و`OFF` لا يُطاع، وصفر يتيم. والمثال المُستشهَد به سببه عمودٌ قابل للعدم لا غيابُ إنفاذ. ومِسحةٌ دورية في CI بعد بناءٍ من الصفر، وجردٌ يقول إن **120** عمودًا يبدو مرجعًا وبلا قيد | M |
| `OPS-107` | مخلَّفات ثقيلة في الشجرة والمستودع | S |
| ~~`APP-104`~~ | ~~بلوكات مخفية دائمًا تعتمد جداول بلا كاتب~~ &#10004; الدفعة 32 (سُحبت من التطبيق والخادم واللوحة) | S |
| ~~`APP-105`~~ | ~~مخلَّفات وتكرار داخل `lib/`~~ &#10004; الدفعة 33 (‏≈٦٠٠ سطر ميت + إسكاتاته؛ وشطر «التكرار» صُحِّح ورُبط بـ`APP-101`) | M |
| ~~`APP-107`~~ | ~~تنازلات دورة الحياة وتوثيق يخالف الكود~~ &#10004; الدفعة 34 (الإبطال على `paused`، والتعليق مربوط بالثابت باختبار) | S |
| ~~`APP-108`~~ | ~~حاجز الاتجاه بمطابقة نص المسار~~ &#10004; الدفعة 35 (من `fullPath`؛ وكان الشرط `null` دائمًا فيُغطّى المُشغِّل) | S |
| ~~`APP-109`~~ | ~~مهلة شبكة واحدة 8 ثوانٍ لكل الطلبات~~ &#10004; الدفعة 36 (مهلتان مُصرَّحتان + إعادة محاولة حيث لا بديل) | S |
| ~~`APP-110`~~ | ~~نواقص موسومة في الكود بانتظار الخادم~~ &#10004; الدفعة 37 — **العنوان كان خطأً**: العقد مشحون ومُحلَّل ومُستخدَم؛ الباقي بيانات (`CNT-101`) وإقران (`API-103`) | M |
| ~~`ADM-104`~~ | ~~التخطيط المقترح لا يتبع الدور~~ &#10004; الدفعة 1 | XS |
| `ADM-106` | ~~شاشات تعرض صفرًا يُقرأ كـ«لا مشاكل»~~ الدليل نُقض (11 من 12 جدولًا لها كاتب) والبند أُعيد تحديده؛ الدفعة 41 أصلحت الموضع الحقيقي: قراءةٌ فاشلة في `/ops/overview` كانت تُشحَن صفرًا وسلامةً. وبقي 85 موضع `?? 0` تحتاج قاعدة | M |
| `QA-104` | ~~اختبارات تتحقق من نص المصدر بدل السلوك~~ الدفعة 54: `cors` كان مُصلَحًا أصلًا؛ وحصّة التحليلات كانت على نصّ المصدر بعُذرٍ **غير صحيح** فصارت طلبًا حقيقيًّا مع حالة ضابطة؛ وحرسٌ يمنع عودة العُذر. والـ123 الباقية **بنيويّة** بقاعدة مُعلَنة — تحويلها يُخسر صرامة | M |
| ~~`QA-105`~~ | ~~اختبار `coloring_board` يتقلّب~~ &#10004; الدفعة 43: السبب نومة 120 مللي ثانية بدل انتظار شرط؛ وأخفت عطلًا ثانيًا (نقرة على زرّ معطَّل تنجح ولا تفعل شيئًا). عشر جولات كاملة متتابعة | S |
| `A11Y-102` | الاستوديو بلا بديل وصول. الدفعة 57: شقّ التلفاز **كان مخدومًا بثلاث بوابات** والعطل في الرابعة الغائبة — ثلاثة روابط عميقة تفتح الأسطح نفسها بلا فحص، فصارت الأربعة تُعلَن عبر `_touchOnlyRoute` والبوابة داخل بانيَة المسار. وبديل الإدخال للإعاقة الحركية → `DECIDE-111` | وصول | L |
| ~~`PERF-102`~~ | ~~فك ترميز الصور بأحجامها الكاملة~~ &#10004; الدفعة 59: القدرة كانت موجودة وغير مستعملة (`decodeWidth` في **7 من 21** موضعًا). سقفٌ واحد `decodeCapFor` في أربعة أسطح مشتركة: أفاتارات **200 ميغابايت** لو عاشت كلّها → 216 بكسلًا، وكواكب 22.5 → 174. والتأكيد على `ResizeImage` في الشجرة لا على النصّ | أداء | S |
| ~~`PRIV-103`~~ | ~~التحقق من الملكية بنداء DO كامل لكل حدث~~ &#10004; الدفعة 61: التبعية `API-105` كانت **منحلّة منذ الدفعة 24** والتعليق متخلّفًا؛ وأربعة مسارات أشدّ حساسية تتحقّق من `child_projection` أصلًا. صار الإسقاط أوّلًا والسلطة عند غياب الصفّ وحده، فلا يمكن أن يكون أغلى من قبل. والحرس يعدّ نداءات `/state` **مفرّقًا** عن `/sessions/resolve` الذي يستهلكه التوثيق أصلًا | خصوصية/أداء | S |
| ~~`DEBT-102`~~ | ~~قواعد lint مُسكَتة بدين معلَن~~ &#10004; الدفعة 62: «58 info» لم تكن قائمة — المحصّلة **مشكلتان**، والدين سُدِّد ضمنًا وبقي الإسكات يحمي لا شيء. أُعيدت الأربع الأسلوبية، وبقيت اثنتان **ليستا أسلوبيتين** بسببٍ مكتوب وتاريخ مراجعة، وحرسٌ يمنع إسكاتًا بلا سبب. ووُجد `print` **بلا شرط في مسار إنتاج** | M |

### P3 — مؤجَّل (1)

| المعرّف | العنوان | الحجم |
|---|---|:---:|
| ~~`ADM-107`~~ | ~~مكوّن `NotImplementedPage` ميت~~ &#10004; الدفعة 42: حُذف، وصُحّحت الوثائق الأربع التي استشهدت به، وتسع صفحات صارت `lazy` (297.91 ك.ب → 152.82). وكشف أن مولِّد جدول الميزات كان يحلّ صفرًا من 123 صفحة و`--check` غير مربوط بـCI — أُصلح ورُبط (163 مشكلة → صفر) | XS |

---

## ذيل: نطاق هذا الأودت وحدوده

**ما فُعِل:** قراءة الكود في الطبقات الثلاث؛ تشغيل `flutter analyze` و`flutter test` و`npm test` و`tsc` و`vitest` فعليًّا؛ استعلام D1 المحلية؛ عدّ الأنماط برمجيًّا لا تقديرًا؛ وإعادة التحقق من كل ادّعاء في التقارير السابقة قبل نقله.

**ما لم يُفعَل ولا يجوز افتراضه:**

- **الإنتاج غير مُتحقَّق منه.** كل أرقام قاعدة البيانات من نسخة محلية بتاريخ 2026-08-23. الإنتاج قد يختلف في المحتوى وفي `admin_users` وفي الترحيلات المُطبَّقة.
- **لا تحقق على أجهزة حقيقية.** كل ما يخص `FLAG_SECURE` ومخزن المفاتيح والمشغّل وتكبير النص وD-pad وTalkBack/VoiceOver مستنتج من الكود.
- **`flutter test` لم تُكمل جولة واحدة** (`QA-101`)، فلا عدد نهائي للناجح ولا نسبة تغطية.
- **ستة بنود من الأودت السابق لم تُعَد فحصها** (`§13`)، وبنود P2 المتفرقة في اللوحة والمحتوى لم تُفحَص بندًا ببند.
- **مطابقة WCAG لم تُقَس.** التحقق الكامل يحتاج اختبارًا يدويًّا بتقنيات مساعدة ومراجعة خبير وصول.
- **لم يُعدَّل أي كود إنتاج أو مخطط أو ترحيل أو حالة محتوى أو حالة مراجعة.** الأوامر المُشغَّلة كلها للقراءة.

---

## §15 سجل التنفيذ — الدفعات (2026-08-26)

> هذا القسم يُحدَّث مع كل دفعة. البنود أعلاه تبقى بنصّها الأصلي كسجل لما وُجد، وهذا هو موضع الحالة الحالية.
>
> **قرار مالك (2026-08-26): لا توجد بيئة staging.** المنتج في مرحلة تطوير وكل شيء يعمل على الإنتاج مباشرة. هذا يُغلق `OPS-101` كقرار لا كعمل هندسي، ويُبطل المعيار 24 في `§4` (اختبارات سباق على Staging) — تُنفَّذ محليًّا مقابل `wrangler dev` أو تُوثَّق كغير مُختبَرة. الأثر على `§7` و`§4` مسجَّل في الدفعة الثانية أدناه.

### النتيجة المقيسة: كل مجموعة اختبار خضراء

| الفحص | قبل | بعد |
|---|---|---|
| `flutter analyze --fatal-warnings` | **43 مشكلة**، خروج 1 | **صفر مشكلة**، خروج 0 |
| `flutter test` | **13 إخفاقًا + تعليق دائم** (لا تنتهي) | **499 اختبارًا · كلها ناجحة** |
| `npm test` (Worker) | 1164 · **2 فاشل** | 1164 · **صفر فاشل** |
| `npm run typecheck:types` | نظيف | نظيف |
| `tsc --noEmit` (اللوحة) | نظيف | نظيف |
| `vitest run` (اللوحة) | 303 · **2 فاشل** | 303 · **صفر فاشل** |

أي أن `OPS-102` (استحالة خضرة CI) مُغلَق: الوظائف الست التي يعتمد عليها `deploy-gate` تمرّ محليًّا الآن.

### ما أُنجز

| البند | ما نُفِّذ | الملفات |
|---|---|---|
| **`QA-101`** ✅ | حذف `test/tmp_render_png_test.dart` ومخرجه `_studio_home.png`. الجولة صارت تنتهي وتطبع عددًا نهائيًّا | حذف ملفين |
| **`SEC-101`** ✅ | `requirePermission('manage_billing')` على `POST /billing/refunds` و`/billing/grant`، وصفّا تدقيق لكل عملية، و`created_by` صار الفاعل المُصادَق بدل ترويسة `X-Admin-Actor`، وترحيل يعرّف الصلاحية ويمنحها لـ`owner` و`system_admin` فقط | `routes/adminBilling.ts` · `migrations/0079_billing_permission.sql` |
| **`SEC-104`** ✅ | السبب الجذري: **استهلاك مزدوج للحدّ**. `index.ts` يركّب `strictAuthLimit` على `/api/v1/auth/*` و`auth.ts` كان يركّبه أيضًا على خمسة مسارات، فكل طلب دخول يستهلك وحدتين — الحدّ المُعلَن 5 والفعلي 2. حُذفت التسجيلات المحلية (واثنان منها كانا لمسارات غير موجودة أصلًا) | `routes/auth.ts` |
| **`OPS-103`** ✅ | قاعدة `.backups/` في `.gitignore` — الفرق عن القاعدة القائمة `_backups/` حرف واحد. تحقّق بـ`git check-ignore` | `.gitignore` |
| **`ADM-103`** ✅ | السبب الجذري: مزامنة ثنائية الاتجاه بين `localStorage` و`sessionStorage` تُبقي هوية المستخدم السابق فتفوز على الحالية. صار `localStorage` مصدر الحقيقة و`sessionStorage` ترقية لمرة واحدة بلا كتابة رجعية | `front/src/lib/adminSession.ts` + مساعدا الاختبار |
| **`ADM-104`** ✅ | نفس السبب الجذري أعلاه | `front/src/test/dashboardPresets.test.tsx` |
| **`ENC-012`** ✅ | `ReaderPageCache.clearAll` و`CatalogCache.clear` يمسحان **كل** إصدارات المفاتيح لا الإصدار الحالي وحده: ترقية `_v1` → `_v2` كانت تُخلّف مدخلات لا يقرؤها ولا يمسحها شيء، فتبقى مادة قراءة الأسرة على الجهاز بعد الخروج إلى الأبد. وتحقّق TTL في `CatalogCache` كان سليمًا أصلًا — الاختباران كانا يعبثان بمفتاح قديم لا يقرؤه أحد | `core/cache/reader_page_cache.dart` · `core/cache/catalog_cache.dart` |
| **`QA-102`** ✅ | الإخفاقات الـ13 كلها مُغلَقة. تفصيل الأسباب أدناه |
| **`DEBT-101`** ✅ | صفر مشكلة تحليل. حُذف كود ميت فعليًّا (لا `// ignore`) | تسعة ملفات |
| **`APP-105`** 🟡 | تقدّم جزئي: حُذفت `_TopCardV2` و`_TopCardV2Live` و`_V2AppBar` و`_V2Pill` و`_V2Circle` و`_StarsPainter` و`_CatalogColoringSection` و`_ColoringPagesSection` و`_coloringItems` (≈980 سطرًا) و`_ComingSoonRail` و`_starDot` و`_dot` و`_kCyan` و`_isMultiSection` وستة استيرادات غير مستخدمة. يبقى: النسخ المتوازية للصفحات نفسها و`hive_flutter` | — |
| **`APP-104`** 🟡 | بلوك «قريبًا» كان له **فرعان** في نفس الـ`switch` (الثاني ميت)، و**كلاهما** يعرض سلاسل منشورة بوصفها قادمة. صار مخفيًّا كما `learningJourney` و`mostWatched`، وحُذف الفرع الميت وواجهته | `home_feed.dart` |

### ما وجدته أثناء التنفيذ ولم يكن في الأودت

| الاكتشاف | الدليل | الحالة |
|---|---|---|
| `_stagingHost` كانت قيمته `'api.majarra.app'` — نسخة من الإنتاج بتعليق يقول إن التوحيد «هو الحل الصحيح». والأثر: `validateBaseUrl` يرفض `https://staging-api.majarra.app`، و`baseUrl` يردّ على الرفض بالهبوط إلى **الإنتاج**، فمن وجّه بناءً إلى staging وصل إلى بيانات أسر حقيقية وهو يظن غير ذلك | `core/env/app_environment.dart` | ✅ أُعيد اسمًا مستقلًّا وأُضيف لقائمة سماح غير الإنتاج فقط |
| سياسة `engine_content_separation_test` تمنع محارف الرموز التصويرية في كل `features/games` — ووجدت إيموجي في رسالتَي نجاح يقرؤهما قارئ الشاشة، وخمسة أخرى في تعليق | `connect_dots_board_page.dart` · `reference_catalogue_page.dart` | ✅ أُزيلت |
| تسع قوائم نصوص مُصرَّفة في صفحات الاستوديو خارج قائمة السماح، منها **48 نصًّا** ككتالوج بديل مبندل | `reference_catalogue_page_live.dart` وغيرها | 🟡 أُدرجت بأسبابها، والثلاث الفعلية موسومة `TEMPORARY` ومربوطة بـ`APP-103` |
| زر «استكمال» في كتالوج وصل النقاط كان جسمه `try { final list = context as dynamic; } catch (_) {}` — لا شيء بمظهر كود عامل | `connect_dots_catalogue_page.dart` | ✅ صار معطَّلًا صريحًا |
| ثلاثة توكيدات محتوى بأعداد حرفية مكرّرة (`30` في ثلاثة مواضع) تسقط كلما أُضيف محتوى | `deep_link_smoke_test.dart` | ✅ ثابت واحد مسمّى وموثَّق |

### أسباب الإخفاقات الـ13، مُصنَّفة

**عيوب إنتاج حقيقية (3):** بقاء كاش القارئ بعد الخروج · مسح ناقص للإصدارات القديمة في كاشين · مضيف staging الذي يهبط إلى الإنتاج.

**سياسة انتُهكت في الإنتاج (3):** إيموجي في `features/games` (رسالتان + تعليق) · قوائم نصوص خارج قائمة السماح · استثناء ميت في القائمة.

**توكيدات تقادمت على تغيير مقصود (7):** أعداد محتوى مبندل (4) · نص عنوان قُصِّر لإصلاح فائض 78 بكسل · عناصر أزالها إعادة تصميم رئيسية الاستوديو · محرف زخرفي لم يوجد في الإنتاج قط.

في كل حالة من الفئة الثالثة، عُدِّل التوقّع **مع تعليق يذكر التغيير المقصود ودليله**، وبقيت التوكيدات التي تحمل ضمانًا حقيقيًّا (غياب استثناءات الإطار، ظهور كل التصنيفات، العدد الحقيقي في شريط الإنجاز).

### حادثة أثناء التنفيذ — تُسجَّل لأنها تخصّ سلامة العمل

أثناء حذف الكود الميت استخدمت سكربتًا يحذف تصريحًا حتى أول قوس مُغلِق في العمود صفر. على `const _kCyan = Color(...)` — سطر واحد لا ينتهي بقوس — امتد الحذف 34 سطرًا فأزال `TraceCategorySpec` و`TraceFilterAge` و`TraceFilterDiff` من `trace_home_v2.dart`. والملف **غير متتبَّع في git** (`??`)، فلم يكن للاستعادة مصدر.

أُعيد بناء الثلاثة من مواضع استخدامها في `studio_v2/trace_home_page.dart` و`_TraceCard` و`_Filters`، وأكّد `flutter analyze` صفر أخطاء و`flutter test` 499 ناجحًا. لكن الدرس يُسجَّل: **292 مسارًا غير متتبَّع في هذه الشجرة** (`OPS-107`) يعني أن أي حذف آلي عمل بلا شبكة أمان. الحذف اليدوي المُدقَّق هو الطريق الوحيد حتى تُنظَّف الشجرة.

### لم يُلمَس في هذه الدفعة

`ENC-001` منظومة الترخيص · `ENC-002`…`ENC-011` · `ENC-013`…`ENC-015` · `API-101` الرقابة الأبوية · `API-104` الفوترة · `CNT-*` كل بنود المحتوى · `OPS-101` staging · `SEC-102` بذر حساب مسؤول · `SEC-103` · `SEC-105`…`SEC-110` · `ADM-101` · `ADM-105` · `I18N-101` · `A11Y-*` · `PERF-*` · `PRIV-*` · وكل `DECIDE-*` و`EXT-*` و`HUMAN-*`.

**التالي المنطقي:** `SEC-102` (بذر حساب مالك ثم إبطال المفتاح المشترك) و`OPS-104` (نقل الأسرار وحذف السجلات) — كلاهما صغير وأثره أمني مباشر — ثم `ENC-001` بعد قرار `DECIDE-101`.

---

### الدفعة الثانية (2026-08-26) — قرار staging، والأسرار، وباب اللوحة

الفحوص بعد الدفعة: `flutter analyze` **صفر مشكلة** · `flutter test` **499/499** · Worker `npm test` **1164/1164** · `tsc` نظيف في الطرفين · `vitest` 303/303.

#### `OPS-101` — مُغلَق بقرار: لا staging

القرار: **كل شيء على الإنتاج**، ولا بيئة وسطى. والأثر أن staging لم يكن «ناقصًا» بل كان **دَينًا يشير إلى لا شيء**، فأُزيل من الكود:

| ما كان | ما صار |
|---|---|
| `enum AppEnvironment { development, staging, production }` | `{ development, production }` — وأي قيمة غير `development` (ومنها `staging`) تُحلّ إلى الإنتاج |
| `_stagingHost` (كانت قيمته نسخة من الإنتاج، ثم صحّحتها في الدفعة الأولى) | محذوف. المضيف السابق `staging-api.majarra.app` صار مثل أي مضيف غير مُدرَج ويُرفض في كل البيئات |
| `isStaging` و`'STAGING'` وشارة بلون خاص | محذوفة؛ الشارة بلون واحد لأن البيئة غير الإنتاجية الوحيدة هي DEV |
| فرع `case AppEnvironment.staging:` في ثلاثة `switch` | محذوف — كان فرعًا ميتًا في كل مرة |
| `deploy:staging` و`migrate:staging` في `package.json` — سكربتان يطبعان رسالة ويخرجان بـ`exit 1` | محذوفان |
| اختبار «accepts the staging host» | صار «rejects the former staging host» بتعليق يذكر القرار |

قائمة سماح الإنتاج تبقى **مضيفًا واحدًا** (`api.majarra.app`)، والتطوير يضيف loopback ومضيف `workers.dev` القديم. الملفات: `core/env/app_environment.dart` · `app/majarra_app.dart` · `features/home/data/majarra_api_client.dart` · `test/app_environment_test.dart` · `dashboard/api/package.json`.

#### `SEC-102` — الأداة جاهزة، والتنفيذ قرار مالك

`admin_users` = 0، وما دام كذلك فالمفتاح المشترك `ADMIN_API_KEY` هو الباب الوحيد. أُضيف `tools/ops/seed-admin.mjs`:

- يقرأ المفتاح من **البيئة** لا من سطر الأوامر (سطر الأوامر يُسجَّل في تاريخ الصدفة وقائمة العمليات).
- يرفض العمل إن وُجد مستخدم — يُبذَر الأول فقط.
- يولّد كلمة مرور مؤقتة قوية، والخادم يضبط `must_change_password`.
- **يعيد الفحص ويؤكد أن المفتاح صار يُرفض بـ401.** بلا هذه الخطوة لا أحد يعرف أن الباب أُغلق، وهي ما يجعل البذر إجراءً أمنيًّا لا مجرد إنشاء حساب.

**لم يُنفَّذ التنفيذ نفسه:** كتابة على الإنتاج تحتاج المفتاح وقرار المالك. البند يبقى مفتوحًا حتى يُشغَّل السكربت ويُحذف السرّ (`wrangler secret delete ADMIN_API_KEY --env production`).

#### `OPS-104` — نُفِّذ التنظيف

حُذف من `dashboard/api`: **22 ملفًا بحجم 4.49 ميغابايت** (سجلات `wrangler dev` وأثقلها `devA.log` بـ3.87 MB، وملفا `probe-*.json`) ومجلد `.tmp/` بكامله. كلها كانت غير متتبَّعة في git ومغطّاة بـ`.gitignore` — الخطر كان القرص لا المستودع.

**فحص قبل الحذف:** مسحت الملفات الـ22 عن أنماط بيانات الاعتماد (`Authorization: Bearer`، `access_token`/`refresh_token`/`purchase_token`/`api_key`/`secret`، وبادئة JWT `eyJ`) — **صفر مطابقة**. أي أن المخاوف من رموز حيّة في السجلات لم تتحقّق في هذه النسخة، وهذا يُسجَّل كما هو: البند كان صحيحًا كمخاطرة نظافة، ولم يكن تسريبًا فعليًّا. لا حاجة لتدوير أسرار بسبب هذه السجلات.

بقي `.dev.vars` و`.secrets.local.txt` — الأول مطلوب لتشغيل `wrangler dev`، والثاني يُقلَّص بحذف `ADMIN_API_KEY` بعد `SEC-102`.

#### `DOCS-101` — تقدّم جزئي

أُعيدت كتابة `SETUP_SECRETS.md` بالكامل. كان يحمل أرقامًا خاطئة (حدّ الإدارة 30/دقيقة والواقع 600) ويشير إلى مضيف staging وإلى إجراءات مُنجَزة كأنها معلَّقة. الصيغة الجديدة: إجراءات مُتحقَّقة مقابل الكود، وجدول الحدود الفعلية السبعة، وربط كل فجوة بمعرّفها في هذا الملف، وقاعدة «الحدّ يُعلَن في `index.ts` فقط» التي يشرحها `SEC-104`.

#### أثر القرار على `§4`

المعيار **24** («اختبارات سباق للتسجيل والتشغيل والإلغاء والتنزيل على Staging») لم يعد قابلًا للاستيفاء بنصّه. البديل المقبول: تنفيذها محليًّا مقابل `wrangler dev` مع Durable Objects حقيقية — وهو ما تفعله `test/rateLimit.test.mjs` فعلًا للحدود — أو توثيقها صراحةً كغير مُختبَرة. الحالة تبقى 🔴 حتى تُنفَّذ إحدى الطريقتين.

#### التالي

`API-101` (فرض الرقابة الأبوية — الإعدادات تُكتب ولا تُقرأ) هو أكبر بند P0 قابل للتنفيذ بلا قرار مالك ولا تبعية خارجية.

---

### الدفعة الثالثة (2026-08-26) — `API-101` فرض الرقابة الأبوية

الفحوص بعد الدفعة: `flutter analyze` **صفر مشكلة** · `flutter test` **499/499** · Worker `npm test` **1177/1177** (13 اختبارًا جديدًا) · `tsc` نظيف · `vitest` 303/303.

#### ما صار يُفرض فعلًا

كانت الإعدادات تُكتب بتحقق دقيق ولا يقرؤها أي مسار تشغيل، وجدول التتبّع بلا كاتب. الآن:

| الحدّ | أين يُفرض | ماذا يحدث |
|---|---|---|
| **وقت النوم** | `FamilyState.startPlayback` وكل نبضة | لا يُنشأ عقد أصلًا؛ والعقد القائم يُسحب في أول نبضة داخل النافذة |
| **الحدّ اليومي** | نفس الموضعين | يُرفض البدء عند نفاد الرصيد، ويُسحب العقد عند نفاده أثناء المشاهدة |
| **حدّ الجلسة** | كل نبضة | يُسحب العقد عند تجاوز عمر الجلسة الحدّ |
| **الاحتساب** | كل نبضة | الفارق منذ آخر نبضة يُضاف إلى `screen_time_daily` داخل الـDO |

#### أربعة قرارات تصميم، كل واحد يغلق ثغرة

**1. السياسة تُقرأ داخل الـDurable Object، لا تُمرَّر إليه.** أول تصميم قرأها في المسار ومرّرها في جسم النداء. المشكلة ظهرت عند النبضة: **المسار لا يعرف معرّف الطفل** — العقد هو من يحمله — فكان الحل الوحيد أن يرسله العميل، وعميل معدَّل يرسل معرّف طفل آخر حدّه 180 دقيقة فيقرأ الخادم سياسة الطفل الخطأ. القراءة داخل الكائن تُغلق ذلك: المعرّف من العقد أو الجلسة، وولي الأمر من صف الأسرة، ولا شيء من السياسة يسافر على الشبكة.

**2. اليوم هو اليوم المحلي للأسرة، لا يوم UTC.** المفتاح يُحسب من `parents.timezone` عبر `Intl.DateTimeFormat` (متاح في وقت تشغيل Workers، ويتعامل مع التوقيت الصيفي صحيحًا). استخدام يوم UTC كان سيُصفّر رصيد طفل في القاهرة الساعة الثانية صباحًا، وفي طوكيو في منتصف مسائه.

**3. النبضة المتأخرة لا تحرق الرصيد.** الفارق مقصوص عند خمس دقائق: فارق أكبر يعني جهازًا نام أو فقد الشبكة أو ساعة تحرّكت، لا طفلًا شاهد. بلا القصّ كانت نبضة واحدة بعد ساعتين تحرق حدّ اليوم كله — خطأ في غير مصلحة الطفل.

**4. عمر العقد نفسه محدود بالرصيد المتبقي.** العقد كان يُجدَّد خمس عشرة دقيقة دائمًا، فيبقى صالحًا بعد نهاية الوقت وتأتي النبضة التالية بعد فوات الأوان — أي جلسة إضافية مجانية بعد كل حدّ. الآن `expiresAt = min(سقف العقد، ما بقي من اليوم، ما بقي من الجلسة)`.

وأيضًا: **الوقت المُشاهَد يُحتسَب قبل قرار الرفض في نفس النبضة**، وإلا صارت آخر فترة قبل كل رفض مجانية فيتعلّم العميل أن يتجاهل الرفض.

#### جانب التطبيق

`PlaybackErrorKind.screenTime` نوع رفض جديد منفصل عن `forbidden`. الأهمية عملية: ثلاث حالات مختلفة تشترك في 403 (اشتراك ناقص، وقت نوم، انتهاء الوقت)، وعرض «هذا المحتوى يتطلب اشتراكًا» لطفل انتهى وقته يدفع ولي الأمر إلى صفحة دفع لا تحلّ شيئًا. فصار الرمز `screen_time_*` من الخادم هو ما يقرّر الرسالة، لا رمز الحالة.

وتغييران في التجربة:

- **النبضة لم تعد تكتم الرفض.** كانت `catch (_) {}` تلتقط كل شيء، فيواصل الطفل المشاهدة حتى تنتهي صلاحية توكن الوسائط — أي أن الحدّ يتأخر دقائق أو لا يظهر أبدًا في تشغيل تقدّمي. الآن يتوقف التشغيل فورًا برسالة الحدّ. وما عدا حدود وقت الشاشة يبقى مكتومًا عن قصد: خطأ شبكة عابر في نبضة لا يجوز أن يقطع المشاهدة.
- **زر «إعادة المحاولة» يُخفى عند الحدّ**، ويبقى «رجوع» وحده. زر إعادة المحاولة يعلّم الطفل أن يضغطه حتى يمرّ الحدّ، فيصير الحدّ تحديًا لا قاعدة. والأيقونة ساعة لا علامة تعجّب: الحدّ قاعدة اتفق عليها ولي الأمر، لا خطأ في التطبيق.

#### تغيير سلوك يجب أن تعرفه

الصف الغائب في `child_settings` يعني أن ولي الأمر لم يفتح الإعدادات، ويُطبَّق عليه **افتراضي المخطط: 30 دقيقة يوميًّا**. هذا ما تعرضه شاشة الإعدادات نفسها عند إنشاء الصف، فالسلوك الوحيد المتماسك أن يطابق الفرض ما تعرضه الشاشة. البديل — اعتبار الغياب «بلا حدّ» — كان سيجعل الحماية اختيارية بالصمت.

**الأثر:** كل طفل لم يضبط له وليه إعدادات صار محدودًا بـ30 دقيقة مشاهدة فيديو يوميًّا، ويُرفعه ولي الأمر إلى 180 من الشاشة. إن أردت افتراضًا مختلفًا فهو تغيير سطر واحد في `lib/parentalControls.ts` وقرار منتج لا هندسة.

#### ما لم يُفرض ولماذا — `DECIDE-108` جديد

**الصوت والقراءة بلا فرض بعد.** `books.ts` و`stories.ts` لا تُنشئان عقدًا (وهذا مقصود: حدّ التشغيل المتزامن للفيديو)، فلا يوجد ما يُحتسَب منه بلا مسار إبلاغ من العميل — والعميل ليس سلطة.

ولم أخترع السياسة لأن السؤال منتج لا هندسة: **هل «حكاية قبل النوم» ممنوعة في وقت النوم أم أنها المقصودة؟** وهل يستهلك الاستماع نفس رصيد وقت الشاشة الذي وُضع للفيديو؟ الجوابان يحدّدان تصميمًا مختلفًا تمامًا. `DECIDE-108`.

كذلك لم يُكتَب إسقاط D1 لـ`child_screen_time_daily` بعد: السلطة في الـDO، والإسقاط للتقارير الإدارية ويحتاج حدث outbox ومستهلكًا — بند متابعة تحت `API-105`.

#### التالي

`PRIV-102` (تدقيق عمليات مسار العميل — 32 صف تدقيق كلها إدارية) أو `SEC-105` (certificate pinning). كلاهما متوسط وبلا تبعية خارجية.

---

### الدفعة الرابعة (2026-08-26) — `PRIV-102` سجل تدقيق مسار العميل

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` **صفر مشكلة** · `flutter test` **499/499** · Worker `npm test` **1194/1194** (17 اختبارًا جديدًا) · `tsc` نظيف في الطرفين · `vitest` **303/303**.

#### ما صار مُسجَّلًا

جدول جديد `family_audit_logs` (الترحيل `0081`) يستقبل صفًّا لكل فعل مؤهَّل على مسار الأسرة:

| المجموعة | الأحداث |
|---|---|
| **الترخيص** | `playback.started` (المنح) · `playback.revoked` (الإبطال) · `playback.ended` |
| **المصادقة والأجهزة** | `session.created` · `session.revoked` · `device.revoked` · `downloads.revoked` · `parent_pin.enrolled/changed` |
| **دورة حياة الحساب** | `family.initialized/updated/resynced` · `family.deletion_requested` · `family.deleted` · `data.exported` |
| **الأطفال** | `child.created/updated/track_transitioned/deleted` |
| **الاستحقاق** | `entitlement.updated` |

كل صف يحمل `actor_kind` ∈ {`parent`, `operator`, `system`} و`actor_id`، فسؤال «هل ولي الأمر ألغى الجهاز أم المسؤول؟» صار له جواب في نفس الصف.

#### خمسة قرارات تصميم

**1. جدول مستقل، لا إعادة استخدام `audit_logs`.** ذاك سجل اللوحة: كل صفوفه فاعلها مسؤول، وشاشة `GET /admin/audit-logs` تقرؤه كما هو **بلا عمود نوع فاعل**. لو كُتبت فيه أفعال أولياء الأمور لاختلط سجل الرقابة بسجل المُراقَبين: يصير «من فعل ماذا في اللوحة» غير قابل للإجابة إلا بتصفية هشّة على `entity_type`، وتُسرَّب حركة كل أسرة إلى كل من يملك `view_audit_log`. الفصل يعطي أيضًا صلاحية مستقلّة: `view_family_audit`.

**2. الكتابة في مستهلك الطابور، لا في المسار الساخن.** كل عملية مؤهَّلة تُصدر أصلًا حدث outbox داخل نفس المعاملة التي تُغيّر الحالة. فالكتابة من المستهلك تعني: لا فقدان (الحدث محفوظ قبل أن يرى العميل الاستجابة، والفشل النهائي يذهب إلى `failed_family_events`)، ولا تكلفة على زمن استجابة `POST /playback/start`، و**idempotency مجانًا** لأن `event_id` هو مفتاح الجدول. البديل — كتابة D1 من داخل الـDO — يضيف نداءً شبكيًا داخل معاملة، وفشله يترك الحالة متغيّرة بلا سجل، أي الفشل الصامت نفسه الذي نغلقه.

**3. الأثر في نفس `DB.batch` مع الإسقاط.** كتابة منفصلة تعني احتمال نجاح الإسقاط وفشل الأثر: حالة متغيّرة بلا سجل. الاختبار يثبّت الترتيب داخل الدفعة الواحدة.

**4. ثلاثة أنواع أحداث لا تُدقَّق عن قصد.** `progress.updated` و`content.completed` و`favorite.updated`: سلوك تعلّمي عالي الحجم لا فعل أمني. تدقيقها يجعل الجدول أكبر من كل ما عداه فيطمر ما يُراجَع فعلًا، **ويحوّل سجل التدقيق إلى سجل مشاهدة طفل** — أي يخالف الخصوصية التي أُنشئ لحمايتها. القائمة صريحة لا استبعادية: نوع جديد لا يُدقَّق حتى يُضاف بقرار.

**5. تجديد الترخيص عند كل نبضة لا يُسجَّل.** النبضة كل دقائق لكل جلسة نشطة، فصفٌّ لكل تجديد يعني آلاف الصفوف لطفل واحد في مساء واحد. التجديد ليس قدرة جديدة بل تمديد لمنحة مُدقَّقة، و`playback.started`/`ended` يحدّان المظروف، وكل إبطال بسبب السياسة يُسجَّل بـ`playback.revoked` ومعه سببه. **متابعة:** إن طُلب أثر لكل تمديد فهو صفّ تجميعي (عدد التجديدات وآخر وقت انتهاء) لا صفّ لكل نبضة.

#### علّة ثانية اكتُشفت أثناء المعالجة: سبعة أنواع أحداث كانت تُدمَّر صامتة

الـoutbox يقبل أي نوع (`addOutbox` لا يتحقق منه)، والمسلِّم يرسل كل صف معلَّق إلى الطابور، ثم `parseFamilyEvent` يرفض ما ليس في `FAMILY_EVENT_TYPES` فيُسجَّل `console.warn` ثم **`message.ack()`** — أي يُستهلَك ويُفقَد.

والقائمة كانت أقصر من قائمة ما يُصدره الـDO فعلًا بسبعة أنواع: `device.revoked` و`downloads.revoked` و`child.updated` و`child.track_transitioned` و`family.resynced` و`parent_pin.changed` و`parent_pin.enrolled`. أي أن **إبطال جهاز وإبطال تنزيلات وتغيير رمز وليّ الأمر** — أهم ثلاثة أفعال أمنية على المسار — كانت أول ما يُفقَد، ولا إسقاط ولا أثر ولا صف في `failed_family_events`.

القائمة الآن مطابقة للمصدر، و`test/familyAudit.test.mjs` يمسح `FamilyState.ts` ويفشل إن أُضيف `addOutbox` بنوع غير معلَن.

#### تصدير البيانات صار له أثر

`GET /account/export` كان أوسع قراءة على المسار كلّه — الأطفال وإعداداتهم والمحاولات والتقدّم والإتقان والموافقات وسجلات التشغيل — **وبلا أي أثر**. الآن يُصدر `data.exported` بعدد أقسامه وعدد صفوف كل قسم، ولا شيء من محتواه: نسخ المحتوى إلى السجل يخلق نسخة ثانية من البيانات نفسها في جدول أطول عمرًا، وهو عكس غرض السجل.

#### الخصوصية

`details` يُكتب عبر **نفس** منقّح مسار اللوحة (`serializeAuditDetails` صُدِّر من `lib/auditLog.ts` بدل نسخة ثانية تنحرف): لا توكن، ولا JWT، ولا سلسلة استعلام في أي رابط، ولا `nickname`، ولا شهر/سنة ميلاد. اختباران يثبّتان ذلك على `child.created` (الذي يحمل `nickname` في حمولته فعلًا) وعلى حمولة تحوي توكنًا ورابطًا مُوقَّعًا. `child_id` معرّف مستعار ومسموح، وهو ما يجعل السجل مفيدًا.

#### القراءة

القسم مُعرَّض في Customer 360 (`GET /admin/customers/:id` → `family_audit`، مصدره مُعلَن `d1_history` لا إسقاطًا) وتبويب جديد «سجل عمليات الأسرة» في `CustomerDetailPage`، منفصل عن تبويب «سجل اللوحة»: دمجهما كان سيخلط «من فعل هذا بالحساب» بـ«ماذا فعل الحساب»، وهما سؤالان لمراجعتين مختلفتين. سجل لا يقرؤه أحد ليس سجلًا.

#### الملفات

`migrations/0081_family_audit_logs.sql` (جديد) · `src/lib/familyAudit.ts` (جديد) · `src/contracts/familyEvents.ts` · `src/queue/familyEvents.ts` · `src/lib/auditLog.ts` · `src/do/FamilyState.ts` · `src/routes/adminCustomer.ts` · `front/src/types/api.ts` · `front/src/pages/CustomerDetailPage.tsx` · `test/familyAudit.test.mjs` (جديد، 17 اختبارًا) · `test/adminDeviceOperations.test.mjs`.

#### التالي

`SEC-105` (certificate pinning) — متوسط وبلا تبعية خارجية.

---

### الدفعة الخامسة (2026-08-26) — `SEC-105` تثبيت الشهادات

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` **صفر مشكلة** · `flutter test` **512/512** (13 اختبارًا جديدًا) · Worker `npm test` 1194/1194 · `tsc` نظيف · `vitest` 303/303.

#### ما كان

صفر `SecurityContext` وصفر `HttpClient` مخصص في `lib/` كلها: كل اتصال يثق بمخزن شهادات الجهاز. على جهاز فيه جذر مزروع — أداة تحليل، وكيل مؤسسي، جهاز مُدار من مدرسة — تُقرأ كل الطلبات: توكن الوصول، توكن الوسائط، ردود الترخيص. **ولا خطأ يظهر**، لأن الشهادة المزروعة صالحة في نظر النظام فلا يُستدعى `badCertificateCallback` أصلًا.

#### القرار المحوري: تثبيت الجذر، لا تثبيت مفتاح الورقة

بند الأودت طلب SPKI pinning. **غير قابل للتنفيذ في `dart:io`**: `badCertificateCallback` يستقبل شهادة الخادم وحدها — لا السلسلة — ولا يُستدعى حين تكون السلسلة صالحة. فالمتاح للمقارنة هو الورقة فقط، وورقة Cloudflare تتجدّد كل ~90 يومًا (الحالية تنتهي 2026-11-04)، فتثبيتها يعني تعطّل كل نسخة منشورة عند تجديد روتيني — وهو ما يحذّر منه البند نفسه في «لا تفعل».

الوسيلة الصحيحة هي العكس: `SecurityContext(withTrustedRoots: false)` مع حزمة جذور نحن نحدّدها، فيتحقّق BoringSSL من السلسلة كاملة مقابلها وحدها. جذر مزروع على الجهاز لا تُبنى منه سلسلة صالحة → يفشل الاتصال، وهو التهديد المقصود بالضبط. والورقة والوسيط يتجدّدان بحرّية داخل الهرم.

#### الجذور، ومن أين جاءت

قرأتُ السلسلة الحيّة من `api.majarra.app` و`cdn.majarra.app`؛ المضيفان على نفس الهرم:

```
leaf (CN=majarra.app / *.sni.cloudflaressl.com)
  └── CN=WE1, O=Google Trust Services        (ينتهي 2029-02-20)
        └── GTS Root R4 (مُوقَّعة تقاطعيًّا)   (ينتهي 2028-01-28)
              └── GlobalSign Root CA          (ينتهي 2028-01-28)
```

الحزمة تحوي خمسة جذور موقَّعة ذاتيًّا: GTS R1..R4 (تنتهي 2036-06-22) وGlobalSign Root CA. خمسة لا واحد لسببين: (1) **مسارَا بناء** — السلسلة المُقدَّمة تنتهي عند GlobalSign ويمكن أن تُختصر عند R4 نفسه، وإدراج الاثنين يجعل التحقّق ينجح في الحالتين؛ (2) **دوران داخل الهرم** — GTS تنقل وسطاءها بين R1..R4، وتثبيت الأربعة يستوعب ذلك بلا تحديث تطبيق.

بصمات SHA-256 للخمسة مسجَّلة في `pinnedRootFingerprints`، واختبار يعيد حسابها من الحزمة نفسها: **أي استبدال أو إضافة أو حذف يُفشل الاختبار**، فلا يمكن إدخال جذر «مؤقّت للتشخيص» بلا مراجعة.

#### تاريخ انتهاء للتثبيت — القرار الذي يمنع كارثة توافر

التثبيت خطر توافر بطبيعته: خطأ في الحزمة، أو تغيير جهة إصدار لم نلحقه، يمنع كل نسخة منشورة من الوصول إلى الخادم — **ولا علاج من جهة الخادم، لأن قناة الإصلاح هي نفس القناة المعطَّلة**. لذلك `pinningEnforcedUntil = 2027-08-26`: بعده يعود البناء المتقادم إلى ثقة النظام بدل أن يبقى معطَّلًا إلى الأبد. هذا نفس نهج الجذور الثابتة في المتصفّحات (تنتهي مع كل إصدار)، والتاريخ **قبل** انتهاء أقرب جذر (2028-01-28) عن قصد ويثبّته اختبار: تثبيت ساري على جذر منتهٍ = فشل مؤكَّد للكل.

#### فشل مفهوم لا تعليق ولا نص خام

`HandshakeException`/`TlsException` تُترجَم إلى `TlsVerificationException` بنوع **مستقل عن `SocketException`** عن قصد: انقطاع الشبكة يُعالَج بإعادة المحاولة، وفشل التحقّق **لا يجوز** أن تُعاد المحاولة عليه بصمت لأنه يعني اعتراضًا فعليًّا أو حزمة متقادمة. الرسالة عربية صالحة للعرض («تعذّر التحقّق من هوية الخادم… قد تكون الشبكة التي تستخدمها تعترض الاتصال») والتفصيل التقني يبقى في `toString()` للسجل. ومهلة اتصال 10 ثوانٍ صريحة، فلا تعليق أبدي.

#### أين طُبِّق

كل موضع كان يُنشئ `http.Client()` صار يستدعي `createAppHttpClient()`: `home_providers.dart` (كل طلبات الـAPI)، `download_providers.dart` (وسائط مُرخَّصة برابط مُوقَّع)، `creative_catalogue_provider.dart` (خمسة مواضع)، `creative_remote_assets.dart`. مسار واحد غير مثبَّت يكفي لقراءة ما يمرّ به.

الاستثناء الوحيد المقصود: `http` إلى loopback (عامل wrangler محلي بلا TLS، بناء التطوير وحده). أي `https` يُثبَّت — حتى في بناء التطوير — وإلا كان أول اختبار حقيقي للتثبيت هو أول بناء إصدار.

#### ما لا يشمله التثبيت

- **مشغّل الفيديو والصوت** (`video_player`, `just_audio`): يفتح اتصاله في ExoPlayer/AVPlayer ولا يمرّ بـ`dart:io`؛ تثبيته يحتاج قناة native — بند منفصل. الأثر محدود: مسار الوسائط يحمل توكنًا عمره 3 دقائق لا توكن الحساب.
- **`Image.network`**: يستخدم `HttpClient` داخليًّا من `ImageCache`؛ الأغلفة عامة ولا تحمل سرًّا.

#### معيار قبول واحد لم يُتحقَّق منه آليًّا

«اعتراض بشهادة جذر مثبَّتة يدويًا يفشل الاتصال» **يحتاج فحصًا يدويًّا**: جهاز/محاكي بجذر Charles أو mitmproxy مثبَّت، ثم تشغيل التطبيق والتأكّد من ظهور رسالة فشل التحقّق لا تمرير الطلب. لا اختبار وحدة يستطيع إثبات ذلك بلا شبكة، فلا أدّعيه. الاختبارات تثبّت كل ما عداه: صحّة الحزمة ومطابقتها للبصمات، وقبول BoringSSL لها، وأن السياق يُبنى بلا جذور النظام، وأن `badCertificateCallback` لا يملك مسار `return true`، ونافذة الفرض، وترجمة الفشل.

#### الملفات

`lib/core/network/pinned_certificates.dart` (جديد — الحزمة والبصمات وإجراء الدوران) · `lib/core/network/secure_http_client.dart` (جديد) · `home_providers.dart` · `download_providers.dart` · `creative_catalogue_provider.dart` · `creative_remote_assets.dart` · `test/tls_pinning_test.dart` (جديد، 13 اختبارًا).

#### التالي

`SEC-106` (رمز جلسة اللوحة في `localStorage`) أو `ENC-012`+`ENC-013` من منظومة التشفير، أو `API-102`. `ENC-001` يبقى موقوفًا على `DECIDE-101`.

---

### الدفعة السادسة (2026-08-26) — `SEC-108` ترويسات الأمان و`SEC-106` سطح تعرّض جلسة اللوحة

الفحوص بعد الدفعة: Worker `npm test` **1204/1204** (10 اختبارات جديدة) · `vitest` **310/310** (7 اختبارات جديدة) · `tsc` نظيف في الطرفين · `flutter analyze` صفر مشكلة · `flutter test` 512/512.

#### `SEC-108` — الترويسات

عارض الصفحات العامة كان يُصدر مستندًا كاملًا، **مبنيًّا من محتوى يكتبه بشر في اللوحة**، بلا `Content-Security-Policy` ولا `X-Frame-Options` ولا `Strict-Transport-Security`. والمقابل الصحيح كان في المستودع نفسه (`creations.ts` يضبط `default-src 'none'; sandbox`)، أي أن المعرفة كانت حاضرة والتطبيق ناقصًا في أخطر سطح: المستند.

الآن `lib/securityHeaders.ts` مصدرًا واحدًا، وكل توجيه مبنيّ على ما تحتاجه الصفحة فعلًا مُتحقَّقًا من المصدر لا منسوخًا من قائمة:

| التوجيه | لماذا هذه القيمة |
|---|---|
| `default-src 'none'` | الصفحة **لا تحمل أي سكربت**: لا ملف خارجي ولا سطر مضمَّن. (`<script type="application/ld+json">` كتلة بيانات لا تُنفَّذ ولا يحجبها CSP.) فالمنع الكامل توصيف للواقع لا تقييد زائد — وهو ما يحوّل ثغرة هروب في `publicHtml.ts` من تنفيذ إلى نصّ معروض |
| `img-src 'self' https: data:` | المحرّر قد يضع صورة من مصدر خارجي؛ حصرها في نطاقنا كان سيكسر محتوى مشروعًا، والصورة لا تُنفَّذ |
| `style-src 'unsafe-inline'` | المستند يضمّن ورقة أنماط ثابتة واحدة. البديل بصمة `'sha256-…'` يحتاج حسابًا غير متزامن لكل استجابة أو ثابتًا يُنسى تحديثه فتتعطّل كل الصفحات بلا أنماط. والأثر محدود: بلا تنفيذ سكربت، أقصى ما يفعله حقن CSS تشويه شكلي |
| `base-uri`/`form-action`/`frame-ancestors` = `'none'` | ثلاثة منافذ لا تحتاجها الصفحة: إعادة تأصيل الروابط، نموذج مُدرَج يسرّب، تأطير |

**HSTS على كل استجابة لا على المستندات وحدها.** ترويسة على مستوى المضيف: المتصفّح يخزّنها للنطاق كلّه، فوضعها على HTML وحده يعني أن أول اتصال بالـAPI — وهو الاتصال الأول لكل عميل تطبيق — لا يثبّت السياسة. الوسيط في `index.ts` يضيفها مع `nosniff` إلى كل استجابة.

والوسيط **يرفع الأرضية ولا يخفض سقفًا**: `Referrer-Policy` لا يُكتب فوق قيمة قائمة، لأن `routes/media.ts` يضبط `no-referrer` وهي أصرم مما نضعه — والكتابة فوقها كانت ستُرخي حماية مسار الوسائط بينما نظنّ أننا نشدّدها. `preload` غير مُدرَج في HSTS عن قصد: الإدراج في قائمة المتصفّحات المسبقة قرار شبه دائم وإزالته تأخذ أشهرًا، فهو قرار مالك لا إعداد ترويسة.

#### `SEC-106` — لماذا `HttpOnly` **غير ممكن** اليوم

السلوك المتوقع في البند هو كوكي `HttpOnly` يديره الخادم. فحص الاستضافة يقول إن ذلك غير قابل للتنفيذ الآن: اللوحة تُخدَم من `majarra.app` (مشروع Cloudflare Pages باسم `majarra-dashboard`، مثبَّت في `front/wrangler.jsonc`) والـAPI من `api.majarra.app` (Worker مستقل، مثبَّت في `front/.env.production`). كوكي تضعه `api.majarra.app` ويُرسَل من صفحة على `majarra.app` هو **كوكي طرف ثالث**: تحجبه Safari اليوم بـITP وChrome في طريقه إلى الحجب. أي أن التحويل كان سيبدو إصلاحًا أمنيًّا ثم يُخرج المسؤولين من اللوحة على متصفّحات بعينها.

فسُلك المسار الثاني الذي يسمح به البند صراحةً — **تنازل موثَّق مع تعويضات** — وسُجِّل القرار الفعلي المطلوب من المالك كـ`DECIDE-109`: تقديم اللوحة من نفس الموقع (وكيل `/api/*` على أصل اللوحة، أو تقديمها تحت `api.majarra.app`)، وحينها يصير `HttpOnly` سطرًا واحدًا.

#### التعويضات، وكلها مُنفَّذة لا موعودة

1. **`script-src 'self'`** في `front/public/_headers`. هذا هو التعويض الفعلي: رمز مقروء من JavaScript لا يُقرأ إن لم يُنفَّذ سكربت غريب أصلًا. تحقّقتُ قبل الفرض أن لا سكربت مضمَّن في `index.html`، ولا `eval`، ولا `new Function`، ولا وسم `script` يُنشأ وقت التشغيل — فلا استثناء يحتاجه. ومعه `connect-src 'self' https://api.majarra.app` فحتى في أسوأ حالة لا مستقبل خارجي للرمز، و`frame-ancestors 'none'` فلا clickjacking على أزرار إدارية، و`base-uri`/`object-src` = `'none'`. الخطّان الخارجيان (`fonts.googleapis.com` و`fonts.gstatic.com`) مسموحان بالتحديد لا بفتح `https:` كلها، لأن `index.css:1` يستوردهما.
2. **الكتابة في `localStorage` وحده.** كان `saveSession` يكتب الرمز والمستخدم في `localStorage` **و**`sessionStorage`، بينما مسار القراءة صار — في الدفعة الأولى (`ADM-103/104`) — يقرأ الأول وحده. أي أن النسخة الثانية لم يكن أحد يقرؤها: رمز إداري كامل الصلاحيات في موضع بلا قارئ. المسح يبقى شاملًا للمخزنين حتى تزول جلسات ما قبل التغيير.
3. **الخروج يُبطل الجلسة في الخادم** (`POST /admin/auth/logout` ثم `revokeSession`)، ولها عمر في `admin_sessions.expires_at`. هذا كان قائمًا؛ الاختبار الجديد يثبّته فلا يعود مسحًا محليًّا يترك رمزًا صالحًا في الخادم.

**تغيير سلوك صغير:** كان الدخول ينجح لو فشل `localStorage` ونجح `sessionStorage`. الآن يفشل برسالة «التخزين غير متاح» بدل أن ينجح ظاهريًّا ثم تفشل كل صفحة بـ401 — حالة نادرة (حجب تخزين كامل) والفشل الصريح فيها أفضل.

#### ما لا يثبّته أي اختبار

أن اللوحة تعمل فعلًا تحت هذه الـCSP. `_headers` يقرؤه Cloudflare Pages وقت التقديم، لا `vite preview` ولا jsdom، فلا اختبار محلي يستطيع تشغيله. الاختبارات تثبّت وجود التوجيهات وعدم وجود `unsafe-eval` وأن الملف في `public/` (الموضع الذي ينسخه Vite إلى جذر `dist/`). **الفحص المطلوب بعد النشر:** فتح اللوحة ومراجعة تبويب Console لأي انتهاك CSP — الأرجح مورد لم أره في المصدر. التصحيح سطر في `_headers`.

#### الملفات

`api/src/lib/securityHeaders.ts` (جديد) · `api/src/routes/publicRender.ts` · `api/src/index.ts` · `api/test/securityHeaders.test.mjs` (جديد، 10 اختبارات) · `front/public/_headers` (جديد) · `front/src/lib/adminSession.ts` · `front/src/test/adminSessionExposure.test.ts` (جديد، 7 اختبارات).

#### التالي

`SEC-110` (تحقق مخطَّطي مركزي على مسارات العميل، P1) أو `ENC-013` (حصص التنزيل، موقوف على `DECIDE-107`) أو `API-102`.

---

### الدفعة السابعة (2026-08-26) — `ENC-006` الصيغة المقسَّمة و`ENC-011` فشل المخزن الآمن

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` **صفر مشكلة** · `flutter test` **529/529** (17 اختبارًا جديدًا) · Worker `npm test` 1204/1204 · `vitest` 310/310 · `tsc` نظيف في الطرفين.

المسار هنا **بالترتيب** من رأس §5. ما قبل `ENC-006` مُخطًّى بسبب مذكور لا بإهمال:

| البند | لماذا لم يُلمَس |
|---|---|
| `ENC-001` | XL، وكان موقوفًا على `DECIDE-101`. **تنبيه: القرار حُسم في الدفعة العاشرة فصار البند غير محجوب** — يبقى حجمه XL وحده سببًا لتأجيله إلى دفعة مخصَّصة |
| `ENC-002` | CEK مغلَّف للجهاز يحتاج خادمًا يُصدره — أي `ENC-001`. **تقدّم جزئي:** المفتاح صار يُشتَقّ لكل حزمة بـHKDF، فلا حزمتان تتشاركان مفتاحًا؛ ما يبقى هو أن يأتي المفتاح من الخادم مغلَّفًا لا أن يُشتَقّ من مفتاح تطبيق |
| `ENC-003` | يعتمد على `ENC-006` (كان تبعيةً مصرَّحة)، وقد أُنجزت الآن → صار قابلًا للتنفيذ وهو **التالي** |
| `ENC-004` | يحتاج مشغّلًا يقرأ من تدفق مفكوك أو خادمًا محليًّا — تصميم منفصل |
| `ENC-005` | جزء من منظومة الترخيص (`ENC-001`) |

#### `ENC-006` — الصيغة الجديدة

كان كل ملف يُشفَّر **ككتلة واحدة**، والملف كله في الذاكرة عند التشفير وعند الفك — حتى 512 ميغابايت لعنصر واحد على جهاز طفل. والذاكرة كانت أهون العلل الأربع: لا AAD، ولا ربط ترتيب، ولا وحدة تلف أصغر من الملف.

```text
الترويسة (18 بايت):
  0..3   'MJR1'
  4      version = 1
  5      flags   = 0
  6..9   chunkSize (uint32 BE)
  10..17 packageSalt (8 بايت عشوائية لكل ملف)
ثم لكل chunk: ciphertext || tag(16)
```

أربعة قرارات:

**1. مفتاح لكل حزمة، لا مفتاح واحد لكل شيء.** `HKDF-SHA256(master, salt, info=context)`. فملفان لا يتشاركان مفتاحًا، فلا خطر إعادة استخدام nonce بينهما أصلًا. وهذا أيضًا نصف الطريق إلى `ENC-002`.

**2. الـnonce حتمي لا عشوائي**: `salt(8) || chunkIndex(uint32 BE)`. البند يحذّر صراحةً من الاكتفاء بـ«عشوائي»: العشوائي 96-بت يصطدم احتماليًّا تحت المفتاح نفسه بعد أعداد كبيرة، والحتمي لا يصطدم **أبدًا** داخل الحزمة لأن العدّاد لا يتكرّر، وبين الحزمات المفتاح مختلف.

**3. AAD تربط كل جزء بسياقه وبموضعه**: `context` و`chunkIndex` وعلامة `final`. النتائج قابلة للاختبار وكلها مُغطّاة: فكّ جزء بسياق طفل آخر أو محتوى آخر يفشل **بالمفتاح الصحيح نفسه**؛ وقصّ آخر الملف يفشل لأن آخر جزء متبقٍّ ليس `final` (وهذا سبب كتابة جزء أخير حتى لو كان فارغًا)؛ وتبديل ترتيب جزأين يفشل لأن الفهرس داخل AAD.

**4. طول النصّ الصريح لا يُخزَّن** — يُحسب من طول الملف وعدد الأجزاء. ما لا يُخزَّن لا يُزوَّر، وترويسة تحمل طولًا غير موثَّق كانت ستحتاج حماية إضافية بلا فائدة.

`PackageContext` يقبل `deviceId` و`licenseId` (كما تطلب `تشفير المحتوي.md:8`) ولا يملأهما اليوم لأنهما لا يوجدان في التطبيق (`ENC-001`/`ENC-002`). إضافتهما لاحقًا تُبطل فكّ ما نُزِّل قبلها — ولذلك تحمل الصيغة `version`.

**قارئ الصيغة القديمة يبقى.** على أجهزة المستخدمين تنزيلات كُتبت قبل هذا التغيير؛ رفضها كان سيُظهر كل تنزيل قائم كتالف. القارئ للقراءة وحدها ولا مسار كتابة يستخدمه.

**والذاكرة:** `partFile.readAsBytes()` قبل التشفير حُذف، والقراءة صارت `openRead()` تدفقيًّا إلى `writeEncryptedStream`، والفكّ للتشغيل صار `decryptToFile` تدفقيًّا. معيار القبول الأول («400 ميغابايت لا ترفع الاستهلاك بمقدار حجم الملف») **لا يُقاس في اختبار وحدة**، فما ثُبِّت هو غياب المسار الذي كان يسبّبه: فحص يفشل إن عاد أي من الملفين إلى قراءة الملف كاملًا.

#### `ENC-011` — الفشل الصامت الذي كان يُنتج تنزيلات لا تُفَك

`_safeRead` كان يبتلع كل خطأ ويُعيد `null`، والكتابة `try { … } catch (_) {}` ثم **تُكمل بمفتاح في الذاكرة**. حالتان، كلتاهما صامتة:

- **فشل قراءة** يُقرأ كـ«لا مفتاح» → يُولَّد مفتاح جديد ويُكتب فوق القديم → كل ما نُزِّل سابقًا ciphertext بلا مفتاح، **بلا رجعة**. هذه أخطر من الأخرى، ويثبّتها اختبار يتأكّد أن المفتاح المحفوظ لا يُلمَس عند فشل القراءة.
- **فشل كتابة** → تشفير بمفتاح يزول مع إنهاء العملية → عنصر يظهر «جاهزًا» ويفشل عند التشغيل.

الآن كل خطأ في المخزن يُرفَع كـ`SecureStorageUnavailableException`، و«غياب المفتاح» (قراءة **ناجحة** تُعيد `null`) هي الحالة الوحيدة التي تُولّد مفتاحًا، والتوليد لا يُعتمد إلا بعد كتابة ناجحة. والفحص يجري **قبل أول بايت من الشبكة**: `DownloadRejection.secureStorageUnavailable` جديد، ورسالته لوليّ الأمر تقول إن المشكلة في الجهاز — بلا ذكر مفتاح ولا تشفير (يثبّته اختبار).

**أثر جانبي على الاختبارات، ويستحق ذكره:** الاختبارات كانت تمرّ عبر هذا المسار المكتوم نفسه — الإضافة غير مسجَّلة في اختبار الوحدة فترفع استثناءً، فيُقرأ كـ«لا مفتاح». أي أنها كانت تختبر مسار العلّة. أُضيف `test/support/fake_secure_storage.dart` مخزنًا حقيقيًّا في الذاكرة يستطيع محاكاة الفشل أيضًا.

#### الملفات

`lib/core/crypto/file_crypto.dart` (أُعيد بناؤه) · `lib/features/downloads/data/download_repository.dart` · `lib/features/downloads/application/download_manager.dart` · `lib/features/downloads/presentation/download_button.dart` · `test/file_crypto_chunked_test.dart` (جديد، 17 اختبارًا) · `test/support/fake_secure_storage.dart` (جديد) · `test/download_engine_test.dart`.

#### التالي

`ENC-007` (بصمة `sha256` للأصل وmanifest موقَّع) — وقد صارت تبعيته `ENC-006` منجزة، لكن نصفه على الخادم موقوف على `ENC-001`. والأقرب عمليًّا `ENC-003` (إلغاء الملف الصريح على القرص أثناء التنزيل): الصيغة المقسَّمة تجعله ممكنًا الآن بتشفير على الطاير مع استئناف من حدود الأجزاء.

---

### الدفعة الثامنة (2026-08-26) — `ENC-003` التشفير أثناء التدفق

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` **صفر مشكلة** · `flutter test` **532/532** (3 اختبارات جديدة فوق الدفعة السابعة) · Worker `npm test` 1204/1204 · `vitest` 310/310 · `tsc` نظيف.

#### ما كان

الترتيب المحرَّم بالنصّ في الخطة (§8): «تنزيل ملف مكشوف كامل → حفظه على القرص → قراءته → تشفيره». الكود كان ينفّذه حرفيًّا: `IOSink` خام على `<id>.part`، ثم `readAsBytes()`، ثم تشفير.

والنافذة ليست لحظة: `.part` **مصمَّم للبقاء بين الجلسات** (هو أساس الاستئناف)، فنسخة كاملة غير مشفَّرة تبقى على القرص طوال التنزيل — وتبقى بعده إن مات التطبيق قبل الحذف.

#### ما صار

`ChunkedPackageWriter`: الملف الجزئي هو **نفسه** الحزمة النهائية ناقصةَ أجزائها الأخيرة. كل جزء يُشفَّر في الذاكرة ويُكتب مشفَّرًا، وعند الاكتمال يُكتب الجزء الأخير بعلامته ثم **يُعاد تسمية** الملف إلى اسمه النهائي — لا إعادة قراءة ولا إعادة تشفير ولا مضاعفة مساحة لحظيًّا.

#### الاستئناف — وهو موضع القرارات

**1. الحدود هي حدود الأجزاء لا البايتات.** موضع الاستئناف لم يعد طول الملف الجزئي: صار الملف مشفَّرًا فطوله أكبر من النصّ الصريح بترويسة ووسم لكل جزء. المستودع يحسب النصّ الصريح المثبَّت (`أجزاء كاملة × حجم الجزء`) وهو ما يُطلَب به `Range`.

**2. الملح يُقرأ من الترويسة القائمة لا يُولَّد.** ملح جديد يعني مفتاحًا مشتقًّا مختلفًا بين نصفَي الملف — أي حزمة لا تُفَك. يثبّته اختبار يكتب حزمة على جلستين ويفكّها كأنها جلسة واحدة.

**3. الإطار الناقص يُقصّ.** انقطاع أثناء كتابة إطار يترك بايتات معلَّقة. إكمالها مستحيل (حالة GCM الوسطى لا تُخزَّن)، وتركها يُفشل حساب الفهرس من طول الملف — وهو أساس الفكّ كلّه. فتُقصّ عند الفتح، ويُعاد تنزيل ذلك الجزء.

**4. تغيير حجم الجزء بين إصدارين يبدأ من الصفر.** ملف بأجزاء مختلطة الأحجام لا يُفَك، لأن الفهرس يُحسب بافتراض حجم واحد.

**تكلفة مقبولة ومُعلَنة:** الإيقاف يفقد ما دون الجزء الواحد من بايتات مجمَّعة في الذاكرة (أقل من ميغابايت)، فيُعاد تنزيله. البديل — كتابة جزء ناقص ثم إكماله — يكسر الصيغة.

**وحماية من الخياطة:** إن لم يطابق موضع الحزمة الجزئية الموضع الذي طُلب به `Range`، يُحذف الملف ويفشل الجري ليبدأ نظيفًا. ملف مخيط من نصفين لا يُكتشَف إلا عند التشغيل.

#### معيار قبول لا يُقاس هنا

«قتل التطبيق في منتصف التنزيل ثم فحص المجلد لا يُظهر بايتات قابلة للقراءة» فحص يدوي على جهاز. ما ثُبِّت آليًّا: لا `openWrite` ولا `readAsBytes` في مسار التنزيل (فحص مصدر يفشل عند الانحدار)، وأن الحزمة المكتوبة على جلستين تُفَك صحيحة، وأن الإطار الناقص يُقصّ.

#### ما يبقى في المسار

`ENC-004` (فكّ إلى ملف صريح للتشغيل) قائم: الفكّ صار تدفقيًّا بذاكرة محدودة، لكن المشغّل ما زال يحتاج ملفًا. إغلاقه يحتاج خادمًا محليًّا يقدّم تدفقًا مفكوكًا أو تكاملًا مع طبقة native — تصميم منفصل.

#### الملفات

`lib/core/crypto/file_crypto.dart` (`ChunkedPackageWriter` جديد) · `lib/features/downloads/data/download_repository.dart` · `lib/features/downloads/application/download_manager.dart` · `test/file_crypto_chunked_test.dart` (+3 اختبارات، 20 إجمالًا).

#### التالي

`ENC-004` (يحتاج تصميم مشغّل) أو `ENC-007` (نصفه على الخادم موقوف على `ENC-001`). فبالترتيب، البند التالي القابل للإنجاز كاملًا هو `ENC-014` (حماية العرض على iOS) ثم `SEC-107`/`SEC-109`.

---

### الدفعة التاسعة (2026-08-26) — `ENC-014` رصد تسجيل الشاشة على منصّات Apple

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` **صفر مشكلة** · `flutter test` **546/546** (14 اختبارًا جديدًا) · Worker `npm test` 1204/1204 · `vitest` 310/310 · `tsc` نظيف.

بقيّة §5 قبل هذا البند موقوفة بأسباب مذكورة: `ENC-004` يحتاج مشغّلًا يقرأ تدفقًا مفكوكًا (تصميم منفصل)، و`ENC-005`/`ENC-008`/`ENC-010` جزء من منظومة الترخيص (`ENC-001`)، و`ENC-007` نصفه على الخادم داخل تلك المنظومة، و`ENC-009` XL على `DECIDE-101`، و`ENC-013` على `DECIDE-107`، و`ENC-015` صيغة الحزمة الكاملة (`.mjrpack`) تستلزم manifest موقَّعًا من الخادم.

#### ما كان

`FLAG_SECURE` على Android وحده، و**لا ملف Swift في الشجرة أصلًا** خارج القالب: صفر رصد لتسجيل الشاشة أو المرآة على iOS/iPadOS/tvOS، مع أن §24 تطلبه.

#### القرار: رصد لا منع، والحدّ مُعلَن

لا واجهة عامة على منصّات Apple تمنع تسجيل الشاشة كما يفعل `FLAG_SECURE`. المتاح `UIScreen.isCaptured` (iOS 11+) — إشارة ترتفع عند التسجيل وعند المرآة وعند AirPlay — و`UIScreen.screens.count > 1` للمرآة السلكية التي لا يرفعها بعض الأجهزة. فالسلوك المنفَّذ هو ما تسمح به المنصّة: **يُرصد فيُوقَف المحتوى**، مع رسالة تقول ما جرى وما يحلّه.

والحدّ صريح في الكود والتوثيق: كاميرا خارجية لا تُرصد على أي منصّة، والمنع الحقيقي للتدفق يحتاج DRM بخادم تراخيص (`ENC-009`).

#### أربعة قرارات في السلوك

**1. لا عقوبة على إشارة رصد.** معيار قبول صريح في البند، وله سبب: الإشارة ترتفع عند عرض **مشروع تمامًا** — أب يعرض حلقة على تلفاز عبر AirPlay. فلا حذف بيانات، ولا إبطال جلسة، ولا تسجيل مخالفة. الأثر الوحيد إيقاف العرض ما دامت الحالة قائمة.

**2. الحجب يُرفَع تلقائيًّا، والتشغيل لا يُستأنف من نفسه.** بقاء الحجب بعد أن يوقف المستخدم التسجيل يجعل الإجراء الصحيح بلا نتيجة ظاهرة. والاستئناف التلقائي كان سيُعيد الصوت في غرفة صار فيها أحد آخر، فالطفل يضغط زرّ التشغيل.

**3. نوع خطأ مستقل لا `forbidden` ولا `screenTime`.** ليست نقصًا في الاشتراك ولا حدًّا وضعه ولي الأمر، والإجراء الذي يحلّها بيد المستخدم. ولا زرّ «إعادة المحاولة»: يُوهم بأن الحلّ عندنا، والحجب يُرفَع تلقائيًّا أصلًا. والأيقونة عين لا علامة تعجّب — الحالة رصد لا عُطل.

**4. الحالة الأولى تُرسَل عند الاشتراك، لا أول تغيّر بعده.** التشغيل قد يبدأ والتسجيل **جارٍ**؛ انتظار إشعار تالٍ كان سيترك المحتوى معروضًا بلا حدّ. وقناة أحداث لا استقصاء دوري: الاستقصاء إمّا متأخّر فتُسجَّل ثوانٍ قبل أن نلاحظ، وإمّا مكلف على البطارية أثناء التشغيل.

**وصدق العقد بين الطرفين:** `setSecureFlag` على iOS يردّ `FlutterMethodNotImplemented` صراحةً بدل نجاح كاذب يجعل طبقة Dart تظنّ النافذة محميّة وهي معروضة. وطبقة Dart لا تُرسله على iOS أصلًا. و`supportsSecureWindow` (منع) صارت منفصلة عن `supportsCaptureDetection` (رصد) — دمجهما في `isSupported` واحدة كان يخفي أن المنصّتين تفعلان شيئين مختلفين.

**والفشل يُقرأ «غير مسجَّل»:** بناء مضيف أقدم لا يعرف الطريقة، أو قناة غائبة، لا يجوز أن يمنع طفلًا من مشاهدة ما يحقّ له. الرصد إجراء تخفيف، وتحويل فشله إلى منع يعاقب المستخدم على عيب فينا.

#### معيار قبول لا يُغلَق هنا

«السلوك مُتحقَّق منه على جهاز حقيقي لكل منصّة، بدليل مرفق» يحتاج جهازًا وتسجيلًا فعليًّا — **يبقى مفتوحًا**، ولا اختبار Dart يستطيع تشغيل Swift. المُثبَّت آليًّا: قدرات كل منصّة، وأن العلامة الآمنة لا تُرسَل على iOS، وأن غياب القناة يُقرأ «غير مسجَّل»، وعقد الطرفين بفحص مصدر `AppDelegate.swift` (القناتان، ومعالج لكل طريقة تستدعيها Dart، و`notImplemented` لعلامة النافذة، والمرآة السلكية، والحالة الأولى، وإزالة المراقبين عند الإلغاء).

#### الملفات

`ios/Runner/AppDelegate.swift` (القناتان + الرصد) · `lib/core/security/screen_capture_guard.dart` · `lib/features/playback/presentation/playback_page.dart` · `test/screen_capture_guard_test.dart` (جديد، 14 اختبارًا).

#### التالي

بقيّة §5 موقوفة كما بيّن الجدول أعلاه، فالتالي بالترتيب من §6: `SEC-107` (كشف Root/Jailbreak، P2) ثم `SEC-109` (توكنات بلا `kid`، S) ثم `SEC-110` (تحقق مخطَّطي مركزي، P1).

---

### الدفعة الثانية عشرة (2026-08-26) — `ENC-001` سلطة الترخيص على الخادم

الفحوص بعد الدفعة: Worker `npm test` **1232/1232** (28 اختبارًا جديدًا) · `tsc` نظيف · `flutter analyze` صفر مشكلة · `flutter test` 562/562 · `vitest` 310/310.

البند XL، وهذه **دفعة الخادم منه**: السلطة والحدود والإصدار الموقَّع والإبطال. ما يبقى مذكور في آخر القسم.

#### ما كان

صفر نقطة نهاية وصفر جدول. العميل يقرّر بنفسه أن التنزيل مسموح، ويشفّر بمفتاحه، ويمنح نفسه ثلاثين يومًا محسوبة على ساعة الجهاز. الخادم لا يعرف أن تنزيلًا حدث، فلا يعدّه ولا يحدّه ولا يبطله. و`PLAN_LIMITS.downloadDevices` كان معلَنًا منذ البداية **ولا يقرؤه أحد**.

#### أين تعيش السلطة، ولماذا

في `FamilyState` لا في D1: الإصدار **عدٌّ ثم إدراج** على حدود مشتركة، وهذا الكائن هو الموضع الوحيد المُسلسَل لكل أسرة. عدٌّ في الـWorker يسمح لطلبين متوازيين بتجاوز الحدّ، والاختبار يفرض ذلك بفحص أن الموجّه **لا يذكر `PLAN_LIMITS` أصلًا**.

جدولان جديدان في الكائن (`offline_licenses`, `offline_license_assets`)، وثلاثة إسقاطات في D1 يكتبها الطابور (`media_licenses`, `child_downloads`, `download_events`).

#### انحراف مقصود عن الخطة: ثلاثة جداول لا أربعة

الخطة تطلب `download_sessions` أيضًا. الجلسة والترخيص هنا **شيء واحد**: الجلسة تُصدر ترخيصًا وتنتهي، والحالة التي تُتابَع بعدها هي حالة الترخيص (`pending` = يُنزَّل الآن → `active` = اكتمل). جدول جلسات منفصل كان سيحمل نفس المفتاح ونفس دورة الحياة وصفًّا واحدًا لكل ترخيص — أي حقيقة ثانية عن الشيء نفسه، وهو ما يُنتج تناقضًا لا معلومة.

#### التوقيع: Ed25519، وهو أول توقيع لامتناظر مملوك لنا في الـWorker

كل توكن في المنظومة HMAC، وهو الصحيح لتوكن يتحقّق منه الخادم. لكن الترخيص دون إنترنت **يتحقّق منه العميل وهو غير متصل**: لو كان HMAC لاحتاج التطبيق نفس السرّ، أي أن مفتاح إصدار التراخيص يُوزَّع على كل جهاز فيكتب كل جهاز ترخيصًا أبديًّا لنفسه.

وكل ترخيص يحمل `signature_key_id`، فالتدوير لا يُبطل ما هو قائم: التطبيق يبندل المفاتيح العامة بمعرّفاتها، وتُوقَّع الجديدة بمعرّف جديد. الترتيب الإلزامي (انشر التطبيق ثم بدّل السرّ) موثَّق في `SETUP_SECRETS.md §4.5` مع أمر التوليد.

**غياب المفتاح يرفض الإصدار بـ503 ولا يُصدر ترخيصًا بلا توقيع.** ترخيص بلا توقيع يقبله العميل، فالغياب عطل تهيئة لا حالة تشغيل صالحة.

#### الحدود التي صارت تُفرض فعلًا

| الحدّ | القيمة | ما يحدث |
|---|---|---|
| أجهزة التنزيل | 0 / 2 / 4 | الجهاز الثالث على باقة `family` يُرفض بـ409 `download_device_limit`، والجهاز المُرخَّص أصلًا لا يُحسب مرتين |
| عدد العناصر | 1 / 4 / 4 | العنصر الخامس يُرفض بـ409 `offline_item_limit` (`offlineItems` مُضاف إلى `PLAN_LIMITS`، وهذا أول قارئ له) |
| الباقة المجانية | صفر أجهزة | رفض صريح بـ402 `offline_not_in_plan` لا إخفاء زرّ في العميل |
| باقة المحتوى | `planAllows` | محتوى `family_plus` على باقة `family` يُرفض **قبل** إصدار أي شيء |

#### أربعة قرارات في دورة الحياة

**1. `pending` يُحسَب في الحدود من لحظة الإصدار.** لو حُسب المكتمل وحده لاستطاع عميل بدء عشرة تنزيلات متوازية وتجاوز الحدّ بلا مخالفة واحدة.

**2. إعادة الطلب لنفس (طفل، جهاز، عنصر، إصدار) تُعيد الترخيص نفسه.** انقطاع شبكة بعد الإصدار يجعل العميل يعيد الطلب؛ ترخيص ثانٍ كان سيستهلك حدًّا بلا تنزيل جديد.

**3. `expired`/`revoked`/`superseded` نهائية.** الاكتمال لا يُحيي مسحوبًا، والتجديد يُصدر **سجلًّا جديدًا** يحمل `renewed_from` ويجعل القديم `superseded`. والتجديد يُرفض بـ402 إن سقط الاشتراك بعد الإصدار.

**4. الأصول تُسجَّل في `child_downloads` عند الاكتمال لا عند الإصدار.** صفٌّ يعني «هذا الملف على الجهاز»، وكتابته قبل اكتمال التنزيل تجعله يكذب. والإدراج يقرأ الأعمدة `SELECT`ًا من `media_licenses` نفسه، فصفٌّ بلا ترخيص لا يُكتب أصلًا — وهو معيار القبول «كل صف يطابق ترخيصًا».

#### تصحيح أمر كان لا يفعل ما يسمّيه

`POST /admin/downloads/revoke` كان يُبطل `playback_leases` وحدها، وتعليقه يقول إن العقود «هي ما يُرخّص الوسائط دون إنترنت في هذه المعمارية، ولا جدول تنزيلات، وإنشاء واحد يخلق حقيقة ثانية». كان ذلك وصفًا صحيحًا للواقع وأمرًا خاطئًا: عقد التشغيل عمره دقائق ويجدّده نبض، فإبطاله **لا يمسّ ملفًا على جهاز غير متصل**. الآن يُبطل الاثنين ويُعيد `licences_revoked`.

وكذلك **إبطال الجهاز** صار يُبطل تراخيصه: بلا ذلك يبقى ما على الجهاز مرخَّصًا أيامًا بعد سحبه — وهو بالضبط الانفصال الذي يرصده `ENC-010`، وقد أُغلق نصفه هنا.

#### ما يبقى في العميل — والحدّ الذي لا يُتجاوز بلا كوده

`ENC-005` (التحقّق من التوقيع ومقاومة تلاعب الساعة) و`ENC-008` (تمرير القدرة في التنزيل) و`ENC-002` (مفتاح المحتوى مغلَّفًا للجهاز بدل مشتقّ من مفتاح التطبيق). أي أن اليوم: **الخادم يعرف ويحدّ ويبطل، والعميل لا يقرأ ذلك بعد** — والتنزيل الفعلي ما زال يمرّ على روابط عامة. البند يبقى 🟡 لهذا السبب، ولا يُعلَن مغلقًا.

#### الملفات

`migrations/0083_offline_licensing.sql` (جديد) · `src/lib/offlineLicense.ts` (جديد) · `src/routes/downloads.ts` (جديد) · `src/do/FamilyState.ts` (جدولان + 5 نقاط + تصحيح الإبطال) · `src/lib/familyPolicy.ts` (`offlineItems`) · `src/lib/db.ts` (`OFFLINE_LICENSE_SIGNING_KEY`) · `src/contracts/familyEvents.ts` · `src/queue/familyEvents.ts` · `src/lib/familyAudit.ts` · `src/index.ts` · `test/offlineLicensing.test.mjs` (جديد، 28 اختبارًا) · `SETUP_SECRETS.md §4.5`.

#### التالي

`ENC-005` (تحقّق العميل من الترخيص الموقَّع) هو التالي منطقيًّا: بلا احتياج إليه في العميل تبقى الطبقة الخادمية غير مستهلَكة. ويحتاج بندلة المفتاح العام في التطبيق — أي أنه يبدأ بتوليد المفتاح ونشر السرّ (خطوة تشغيل في `SETUP_SECRETS.md §4.5`).

---

### الدفعة الثالثة عشرة (2026-08-26) — `ENC-005` + `ENC-008`: العميل صار يستهلك الترخيص

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` **صفر مشكلة** · `flutter test` **581/581** (19 اختبارًا جديدًا) · Worker `npm test` 1234/1234 · `tsc` نظيف · `vitest` 310/310.

الدفعة السابقة بنت السلطة على الخادم، وهذه تجعل العميل يسألها ويتحقّق منها. البندان معًا لأن أحدهما بلا الآخر بلا معنى: ترخيص لا يُطلَب لا يُتحقَّق منه، وتحقّق بلا ترخيص يمنع كل شيء.

#### `ENC-005` — الجهاز لم يبقَ سلطةً على صلاحيته

ثلاث طرق كانت لتمديد الصلاحية بلا مقابل، وكلها من أن القرار كان محليًّا:

| الطريقة | ما كان | ما صار |
|---|---|---|
| تأخير ساعة الجهاز | `isExpired()` يقارن بـ`DateTime.now()` | الوقت المستعمل هو **الأكبر** بين ساعة الجهاز وآخر علامة موثوقة + ساعة أحادية الاتجاه |
| تحرير الميتاداتا | `expires_at` في `shared_preferences` — ملف XML/plist عادي | الانتهاء **موقَّع** داخل الترخيص؛ تحريره يُفشل Ed25519 |
| استعادة نسخة أقدم | لا شيء يمنعها | عدّاد حالة يرتفع مع كل كتابة، وحالة بعدّاد أقلّ من أعلى ما رُئي تُرفَض |

**الوقت الموثوق — لماذا «الأكبر» لا «الخادم دائمًا»:** تقديم الساعة لا يضرّ (يُعجّل الانتهاء على صاحبه)، وتأخيرها لا ينفع لأن العلامة الأخيرة تبقى مرجعًا. والساعة الأحادية (`Stopwatch`) تُقاس منها المدّة داخل الجلسة، فتعديل ساعة النظام أثناء التشغيل لا يُفيد. **والحدّ مذكور في الكود لا مُدَّعى عكسه:** بعد إعادة تشغيل التطبيق تزول الساعة الأحادية فيعود المرجع إلى آخر علامة محفوظة — أي أن أقصى ما يكسبه من يؤخّر ساعته هو **ألّا يتقدّم** الزمن عنده، لا أن يعود إلى الوراء. والإغلاق الكامل يحتاج تحققًا خادميًّا دوريًّا.

**والتراجع الكامل للتخزين الآمن نفسه غير قابل للمنع على الجهاز** — مكتوب في الملف صراحةً، ويكشفه التحقّق الخادمي عند أول اتصال. عدّاد الحالة يمنع استعادة نسخة أقدم من ملف واحد، وهذا ما يُدَّعى ولا أكثر.

#### المفاتيح العامة من البناء لا من المصدر

`--dart-define=OFFLINE_LICENSE_PUBLIC_KEYS=kid:base64,...`. سببان:

1. **مفتاح مكتوب في المصدر يُشحن كما هو.** بناء إنتاج يحمل مفتاح تطوير — نصفه الخاص في ملف على جهاز مطوّر — يعني أن من يملك ذلك الملف يكتب تراخيص مقبولة في الإنتاج.
2. **التدوير يحتاج مفتاحين معًا**، فالخريطة تقبل عدّة معرّفات.

وغياب المفاتيح **يُفشل** التحقّق ولا يتجاوزه: القبول لغياب الإعداد يعيد الجهاز سلطةً على نفسه. ويُميَّز «معرّف مفتاح مجهول» (تطبيق أقدم من التدوير → **حدّث التطبيق**) عن «توقيع فاسد» (→ **أعد التنزيل**): إجراءان مختلفان لا رسالة واحدة.

#### `ENC-008` — التنزيل صار مخوَّلًا

`POST /downloads/sessions` قبل أول بايت، والقدرة في **ترويسة** لا في سلسلة استعلام (فلا تظهر في سجل ولا تحليلات ولا تاريخ وسيط)، والرابط مسار Worker لا رابط R2. ورموز الرفض تُترجَم: 402 → «التنزيل في الباقات المدفوعة»، 409 → حدّ بلغ منتهاه.

**والقدرة عمرها ثلاث دقائق والتنزيل قد يطول** — وهذا معيار قبول صريح. 401/403 في المنتصف يُجدّد القدرة **بلا** ترخيص جديد ويُعيد الطلب من الموضع المحفوظ، مرة واحدة لا حلقة (خادم يردّ 401 دائمًا لأن الترخيص سُحب كان سيُنتج تجديدًا لا نهائيًّا). و«إعادة المحاولة» بعد إعادة تشغيل التطبيق تُعيد التخويل من الصفر: التخويل في الذاكرة، وفتح جلسة لنفس المحتوى idempotent على الخادم (يعيد الترخيص نفسه بقدرة جديدة).

#### البوابة عند التشغيل

`preparePlayback` يتحقّق قبل أن يُقدّم أي بايت: التوقيع، والكيان والإصدار، والطفل، والجهاز، وعهد المصادقة، والانتهاء بالوقت الموثوق. **والرفض لا يحذف الملف**: قد يكون السبب انتهاءً يُجدَّد باتصال واحد، وحذف المحتوى عقوبةً على ذلك يُهدر بيانات الأسرة.

#### حدّ مُعلَن: أصل واحد لكل عنصر

جلسة التنزيل قد تُعيد عدّة أصول، والعميل يستعمل الأول. الحزمة متعدّدة الأصول (صفحات قصة + صوت لكل لغة + توقيتات) صيغة مستقلّة تحت `ENC-015`. تجاهل البقية بصمت كان سيُنتج تنزيلًا ناقصًا يظهر مكتملًا، فالحدّ مكتوب في موضع القرار.

#### الملفات

`lib/core/licensing/offline_license.dart` (جديد) · `lib/core/licensing/license_guard.dart` (جديد) · `lib/features/home/data/majarra_api_client.dart` (أربع نقاط) · `lib/features/downloads/application/download_manager.dart` · `lib/features/downloads/application/download_providers.dart` · `test/offline_license_test.dart` (جديد، 19 اختبارًا).

#### التالي

`ENC-002` (مفتاح المحتوى مغلَّفًا للجهاز بدل مشتقّ من مفتاح التطبيق) هو ما يبقى من جوهر التشفير، ويأتي بعده `ENC-007` (بصمة المصدر — والخادم يرسلها الآن في الترخيص، فبقي أن يتحقّق منها العميل أثناء التنزيل). وبالترتيب من §6: `SEC-107`.

---

### الدفعة الرابعة عشرة (2026-08-26) — `ENC-007` سلامة ما نُزِّل

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` **صفر مشكلة** · `flutter test` **586/586** (5 اختبارات جديدة) · Worker `npm test` 1234/1234 · `tsc` نظيف · `vitest` 310/310.

`ENC-002` مُخطًّى لا مُهمَل: تغليف المفتاح للجهاز يحتاج **سجلّ مفاتيح أجهزة** (زوج مفاتيح يُولَّد على الجهاز، ونقطة تسجيل، وعمود في الـDO، وترحيل يُعيد تنزيل ما هو محفوظ لأن مفتاحه سيتغيّر). دفعة مستقلّة بحجمها، وبدؤها قبل إغلاق `ENC-007` كان سيترك بندًا صغيرًا مفتوحًا وراء بند كبير.

#### ما كان

الاعتماد على TLS وETag وحدهما. أصل مستبدل على المنشأ، أو استجابة مقطوعة يقبلها الطرفان كـ200، تُخزَّن مشفَّرة وتُعتبر سليمة — ثم تفشل عند التشغيل بعد أيام بلا سبب ظاهر. ولا بصمة في أي استجابة تنزيل لأن نقطة التنزيل لم تكن موجودة.

#### الـmanifest الموقَّع موجود أصلًا — وهو الترخيص

البند يطلب «manifest موقَّعًا» يحمل بصمات الأصول. هذا ما بُني في الدفعتين 12 و13: الترخيص موقَّع بـEd25519 ويحمل `assets[{id, sha256, bytes}]` و`signature_key_id`، والعميل يتحقّق من التوقيع قبل التشغيل. فلا manifest ثانٍ: مستند موقَّع ثانٍ يحمل نفس القائمة كان سيكون حقيقة ثانية عن الشيء نفسه.

وبذلك معياران من معايير القبول مُغطّيان سابقًا: «manifest بتوقيع غير صالح أو `signatureKeyId` غير معروف يُرفض» (الدفعة 13).

#### التحقّق في العميل: أثناء التدفّق لا بعده

البصمة تُحسب على النصّ الصريح **قبل** التشفير وأثناء وصول الأجزاء. حسابها بعد الكتابة كان يعني قراءة الملف كاملًا مرة ثانية — وهو ما أُزيل في `ENC-006` لأنه يحمل نصف جيجابايت في الذاكرة.

وعند عدم التطابق: يُفشل التنزيل، **وتُحذف الحزمة الجزئية** — البناء عليها في محاولة تالية يعني استئنافًا فوق بايتات خطأ.

#### تصحيح على الخادم كان لازمًا لجدوى التحقّق كلّه

`checksum_sha256` كان يُخزَّن **كما أعلنه العميل الرافع** من ترويسة `X-File-SHA256`، بلا أي تحقّق. أي أن العمود كان «ما ادّعاه الرافع» لا «ما في الملف» — وعميل ينزّل ويقارن ببصمة كاذبة يرفض أصلًا سليمًا، أو يقبل أصلًا مبتورًا إن كانت البصمة تصفه.

الآن البصمة تُمرَّر إلى `R2.put({ sha256 })`، فـR2 نفسه يحسبها أثناء الكتابة **ويرفض العنصر** إن لم تطابق. والعمود صار يصف البايتات المخزَّنة فعلًا. والقيمة تُتحقَّق شكلًا أولًا (64 حرفًا ست عشريًّا، وإلا 400)، وفشل الكتابة يعود 422 برسالة تقول السبب بدل 500 عام.

#### ثلاثة حدود مُعلَنة لا مُدَّعى عكسها

1. **الأصول بلا بصمة تُقبل.** `null` تعني «لم تُحسب عند الاستيراد» وهي مختلفة عن «لا تطابق». الواقع المقيس: **30 أصلًا صوتيًّا جاهزًا، 16 منها فقط له بصمة** (والصور 22 من 169). رفض ما لا بصمة له كان سيمنع تنزيل محتوى سليم منشور.
2. **التنزيل المستأنف لا يُتحقَّق منه.** ما وصل في جلسة سابقة ليس في هذه، فلا يمكن حساب بصمة الملف كاملًا بلا قراءته من جديد. التحقّق يجري على ما اكتمل في جلسة واحدة.
3. **رفع المتعدّد الأجزاء (multipart) يبقى على بصمة يعلنها العميل**: R2 لا يتحقّق من بصمة الكائن الكامل عند `complete`. مسار الفيديو الكبير يمرّ منه، فهو متابعة مسجَّلة.

#### `close_sinks` — لماذا أُسكِتت، وأين

البصمة التدفّقية تُغلَق في دالة أخرى من صفّها، والقاعدة تبحث عن الإغلاق في نطاق الإنشاء نفسه فلا تراه. فُصلت البصمة إلى `core/crypto/streaming_digest.dart` وأُسكِتت القاعدة **في ذلك الملف وحده** (سطران وصفّ واحد، نطاق يُراجَع بنظرة). إسكاتها في `download_manager.dart` — 1400 سطر — كان سيُخفي تسريبًا حقيقيًّا لاحقًا. والإغلاق مضمون: مرة واحدة، وفي `finally` لكل مسار فشل أو إلغاء.

#### الملفات

`lib/core/crypto/streaming_digest.dart` (جديد) · `lib/features/downloads/application/download_manager.dart` · `lib/features/downloads/application/download_providers.dart` · `dashboard/api/src/routes/adminAssets.ts` · `test/download_engine_test.dart` (+5 اختبارات).

#### التالي

`ENC-002` كدفعة مستقلّة (سجلّ مفاتيح أجهزة + تغليف CEK + إعادة تنزيل ما هو محفوظ)، أو بالترتيب من §6: `SEC-107` ثم `SEC-109` ثم `SEC-110`.

---

### الدفعة الخامسة عشرة (2026-08-26) — `ENC-010` الإبطال يصل إلى الملف

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` **صفر مشكلة** · `flutter test` **588/588** (اختباران جديدان) · Worker `npm test` 1234/1234 · `tsc` نظيف · `vitest` 310/310.

#### ما كان

سحب جهاز من اللوحة **بلا أي أثر على الجهاز**. العميل لا يسأل الخادم عن أي ترخيص قبل التشغيل، فيواصل جهاز مسروق — أو حساب أُلغي اشتراكه — تشغيل كل ما نزّله حتى تنتهي الثلاثون يومًا التي منحها العميل لنفسه.

الدفعتان 12 و13 بنتا نصف الطريق: الخادم صار يُبطل التراخيص عند سحب الجهاز، والعميل يتحقّق من ترخيص موقَّع. لكن **الإبطال ما زال لا يصل**: العميل لا يسأل، والترخيص المحفوظ يبقى صالحًا حتى انتهائه.

#### آليتان، وكل منهما تسدّ ما لا تسدّه الأخرى

**1. المصالحة عند أول اتصال.** عند إقلاع التطبيق يُسأل `GET /api/v1/downloads`: أي التراخيص نشطة؟ وما ليس في الجواب **يُنسى محليًّا** فيصير الملف غير قابل للفك. وهذا يغطّي كل مسارات الإبطال بجواب واحد: سحب جهاز، وأمر «إبطال التنزيلات» من اللوحة، وانتهاء اشتراك، وسحب وليّ أمر لترخيص واحد.

**والنسيان لا يحذف الملف:** الحزمة المشفَّرة بلا ترخيص لا تُفَك، وحذفها عقوبةً على إبطال قد يكون خطأً — يُصحَّح بترخيص جديد — يُهدر بيانات الأسرة وحصّة تنزيلها.

**وفشل المصالحة صامت عن قصد:** لا اتصال ليس إبطالًا. ولو فُهم كذلك لصار انقطاع الشبكة يمحو ما نزّلته الأسرة.

**2. نافذة إعادة تحقّق إلزامية.** المصالحة تحتاج اتصالًا، ومن لا يتصل أبدًا لا تصله. فالتشغيل يُرفض إن مضى على آخر تأكيد خادمي أكثر من **سبعة أيام** — داخل مدّة الترخيص (ثلاثين يومًا)، أي أنها **تشدّد ولا تُرخي**: جهاز مسروق يتوقف بعد أسبوع بلا اتصال بدل شهر.

الرفض بنوع مستقل `staleVerification` لا `expired`: إجراؤه **اتصال واحد** لا إعادة تنزيل ولا تحديث تطبيق، ورسالته تقول ذلك بالضبط. وهذا أثر أُشير إليه في `تشفير المحتوي.md:12` («إعادة تحقّق دورية إلزامية») بلا قيمة محدّدة، والقيمة هنا في **موضع واحد** (`LicenseGuard.maxOfflineGap`) لأنها موازنة منتج: أسرة في سفر طويل تفقد محتواها المحفوظ بعدها، وتقصيرها يزيد الأمان ويضرّ الاستخدام المشروع.

#### معايير القبول الثلاثة

| المعيار | الحالة |
|---|---|
| سحب الجهاز من اللوحة يجعل التنزيلات غير قابلة للتشغيل عند أول اتصال | ✅ المصالحة + إبطال التراخيص في الـDO (الدفعة 12) |
| انتهاء الاشتراك يمنع التجديد ولا يمدّد ترخيصًا قائمًا | ✅ التجديد يُرفض بـ402 عند سقوط الاشتراك (الدفعة 12، مُختبَر) |
| بقاء الجهاز غير متصل أطول من نافذة إعادة التحقّق يوقف التشغيل محليًّا | ✅ سبعة أيام، بنوع رفض مستقل |

#### الملفات

`lib/core/licensing/license_guard.dart` (`recordVerification`, `licenceIdFor`, `revokeLocally`, `maxOfflineGap`) · `lib/core/licensing/offline_license.dart` (`staleVerification`) · `lib/features/downloads/application/download_manager.dart` (`_reconcileLicences`) · `lib/features/downloads/application/download_providers.dart` · `lib/features/home/data/majarra_api_client.dart` · `test/offline_license_test.dart` (+2).

#### التالي

لم يبقَ في §5 إلا `ENC-002` (تغليف المفتاح للجهاز — دفعة مستقلّة: سجلّ مفاتيح أجهزة X25519، وتغليف CEK لكل حزمة، وترحيل يُعيد تنزيل ما هو محفوظ لأن مفتاحه سيتغيّر) و`ENC-013` (موقوف على `DECIDE-107`) و`ENC-015` (صيغة الحزمة الكاملة). فبالترتيب من §6: `SEC-107`.

---

### الدفعة السادسة عشرة (2026-08-26) — `SEC-107` سلامة الجهاز: رصدٌ بلا حجب

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` **صفر مشكلة** · `flutter test` **596/596** (+8) · Worker `npm test` **1252/1252** (+18) · `tsc` نظيف (الطرفان) · `vitest` 310/310.

#### ما كان

`تشفير المحتوي.md:22` يطلب مؤشرات Root/Jailbreak/Debugger/Emulator/Hooking «مع سياسة تخفيف». لم يكن منها شيء: لا حزمة، ولا قناة، ولا سياسة.

#### القرار الذي حكم التنفيذ: الحجب مرفوض

كل إشارة في هذا الباب **يهزمها الجهاز الذي تعمل عليه**، ولإيجابياتها الخاطئة أسباب مشروعة: هاتف مطوّر، نظام مخصَّص، مضاهٍ يستخدمه فريقنا. فالحجب يعني أسرةً تدفع ولا تشاهد بسبب استدلال، مقابل خصم يتجاوز الاستدلال في دقيقة.

فالسياسة المطبَّقة **تخفيف** بمعناه الحرفي:

| الطبقة | الأثر |
|---|---|
| المشاهدة المتّصلة | **لا شيء.** لا فحص سلامة في مسار قدرات الوسائط أصلًا، ومُختبَر بأن `routes/media.ts` لا يذكر الإشارات |
| التنزيل | مسموح كما هو. لا رفض واحد مبنيّ على الخطورة، ومُختبَر بأن لا `risk` في أي `return` رافض |
| عمر الترخيص | **ثلاثة أيام بدل ثلاثين** على جهاز عالي الخطورة |
| السجلّ | سطر في أودت الأسرة مع `offline_license.issued` |

وتقصير الترخيص هو الأثر الوحيد لأنه الشيء الوحيد الذي يمنح جهازًا سلطةً أيامًا بلا رجوع إلينا: على جهاز مكسور، المفتاح في المخزن الآمن قابل للاستخراج، فمنحه شهرًا هو منح شهر لمن استخرجه. والأسرة على هاتف مطوَّر تُكمل استخدامها **بلا أن تعرف أن شيئًا اختلف**.

#### تصنيف الإشارات: كسر امتياز ≠ إشارة بيئة

| عالية (تقصّر الترخيص) | مرتفعة (تُسجَّل فقط) |
|---|---|
| `root_binaries`, `root_manager_app`, `jailbreak_paths`, `sandbox_escape`, `hooking` | `test_keys`, `emulator`, `debugger` |

المضاهي والمنقّح ومفاتيح التطوير **إشارات بيئة لا كسر امتياز**، وتقصير الترخيص عليها كان سيعاقب فريقنا نفسه. واختبار يفرض أن كل إشارة معلَنة لها تصنيف، فلا تُرصد إشارة ولا تعني شيئًا.

#### السياسة في ملف واحد، والرصد في ثلاثة

القرار كلّه في `lib/deviceIntegrity.ts` على الخادم — لا في التطبيق ولا في الكائن الدائم — لسببين: تغيير عتبة بلا تحديث تطبيق، وأن **جهازًا مكسورًا هو أسوأ من يُؤتمن على سياسة عن نفسه**. واختبار يفرض أن `FamilyState.ts` لا يذكر اسم إشارة ولا عتبة: تفرّق السياسة يعني تشدّدًا في موضع وتساهلًا في آخر.

والدرجة تُحسَب في الخادم من الإشارات ولا تُقرأ من العميل: قراءتها كانت ستجعل جهازًا مكسورًا يعلن نفسه «سليمًا» بكلمة واحدة بدل أن يكذب في كل إشارة على حدة.

والقائمة **مغلقة** على الطرفين: ما ليس فيها يُهمَل بلا خطأ. المفتوحة كانت ستجعل من `details` حقلَ نصّ حرّ يكتبه العميل — أي قناة PII بلا حساب — والرفضُ بـ400 كان سيمنع نسخة تطبيق أحدث من التنزيل.

#### بلا PII، وبلا استعلام عن المستخدم

قيَم منطقية فقط: لا طراز، ولا إصدار نظام، ولا بصمة تثبيت، ولا `FINGERPRINT`. و`root_manager_app` يفحص ثلاثة مسارات معروفة بدل تعداد الحزم المثبَّتة، لأن قائمة الحزم وصفٌ للمستخدم وهذا فحصٌ لجهازه. وعلى iOS **رُفض** `canOpenURL("cydia://")`: يحتاج تسجيل مخطَّطات في `Info.plist`، وهو استعلام عن تطبيقات المستخدم. و`sysctl(P_TRACED)` مرفوض أيضًا: بعض مراجعات المتجر تعدّه مضادًّا للتنقيح.

والغياب يُقرأ **«لا شيء» لا «خطر»**: منصّة بلا تنفيذ أصلي، أو `PlatformException`، أو `SecurityException` على فحص مسار — كلّها تُنتج مجموعة فارغة. ولو قُرئ الغياب اشتباهًا لصار كل جهاز لا نعرفه مشتبهًا به، وهو عكس السياسة.

#### معايير القبول الثلاثة

| المعيار | الحالة |
|---|---|
| الإشارة تُرصد وتُسجَّل بلا PII | ✅ قيَم منطقية في `details`، ومُختبَر أنها تنجو من منقّح الأودت |
| السياسة موثَّقة ولا تحجب كل الأجهزة تلقائيًّا | ✅ لا رفض واحد؛ الأثر تقصير مدّة |
| إيجابية خاطئة لا تمنع المشاهدة Online | ✅ مسار الوسائط لا يعرف الإشارات، ومُختبَر |

#### الملفات

`dashboard/api/src/lib/deviceIntegrity.ts` (جديد) · `routes/downloads.ts` · `do/FamilyState.ts` · `test/deviceIntegrity.test.mjs` (جديد، 18) · `app_main/lib/core/security/device_integrity.dart` (جديد) · `android/.../MainActivity.kt` · `ios/Runner/AppDelegate.swift` · `download_providers.dart` · `majarra_api_client.dart` · `test/device_integrity_test.dart` (جديد، 8).

#### حدّ موثَّق

الفحوص الأصلية **غير مُختبَرة على جهاز مكسور فعلًا** — لا جهاز Root ولا Jailbreak في متناولنا. المُختبَر هو العقد: أسماء الإشارات متطابقة في اللغات الثلاث، والسياسة، والتسجيل، وأن الغياب لا يُقرأ خطرًا. أمّا «هل يرصد Magisk الحديث؟» فسؤال يحتاج جهازًا، وجوابه سلبيّ اليوم بحكم أن Magisk يُخفي نفسه — وهذا مقبول لأن الإشارة إرشادية بالتعريف.

#### التالي

بالترتيب من §6: `SEC-109` (توكنات بلا `kid`، S) ثم `SEC-110` (تحقق مخطَّطي مركزي، P1).

---

### الدفعة السابعة عشرة (2026-08-26) — `SEC-109` تدوير سرّ التوقيع بلا إخراج أحد

الفحوص بعد الدفعة: Worker `npm test` **1267/1267** (+15) · `tsc` نظيف · `flutter analyze` صفر مشكلة · `flutter test` 596/596 · `vitest` 310/310.

#### ما كان

`createSignedToken` ينتج `base64url(payload).base64url(HMAC)` بلا معرّف مفتاح. التحقّق سليم ومقارنته بزمن ثابت، لكن **تبديل `AUTH_TOKEN_SECRET` كان يُبطل كل توكن قائم في اللحظة نفسها**: كل وليّ أمر يُخرَج من جلسته، وكل توكن وسائط جارٍ يفشل في منتصف مشاهدة.

فكان التدوير عمليًّا لا يحدث. **وسرٌّ لا يُدوَّر هو سرٌّ يبقى إلى الأبد** — وهذا هو العطل، لا الحرف الناقص في الصيغة.

#### `kid` مشتقّ من السرّ لا مُسمّى في الإعداد

القرار الذي شكّل التنفيذ. البديل المألوف — حقل `AUTH_TOKEN_KEY_ID` بجانب السرّ — **مرفوض**: يجب أن يُغيَّر الحقلان في نفس اللحظة، وهذه فرصة خطأ بشري كاملة. مشغّل يبدّل السرّ وينسى المعرّف فيُوقَّع بمفتاح جديد تحت معرّف قديم، وهو **أسوأ من غياب المعرّف أصلًا**: يجعل التحقّق يختار المفتاح الخطأ بثقة.

والمشتقّ — `HMAC('majarra:token-key-id:v1', secret)` مقطوعًا إلى اثني عشر حرفًا — لا يمكن أن يفترق عن سرّه: هو دالّة منه. وسحب سرّ من الإعداد يُسقط معرّفه معه، فتُرفض توكناته **بلا أي خطوة إضافية**: لا قائمة سحب، ولا جدول، ولا نشر ثانٍ.

ولا يفشي شيئًا: HMAC باتجاه واحد، و٧٢ بتًا من مُخرَجه لا يُستدلّ منها على مفتاح، ولا تُفيد إلا في **اختيار** واحد من مجموعة معلومة سلفًا.

و`kid` في الحمولة لا في رأس ثالث: الشكل `payload.signature` يقرؤه العميل والتراخيص معًا، وتغييره كان سيغيّر عقدًا يعرفه طرفان مقابل صفر مكسب — الحمولة موقَّعة أيضًا، فالمعرّف فيها ليس أقلّ حمايةً.

#### حلقة مفاتيح للقراءة، وسرّ واحد للكتابة

`secretRing(env, name)` = `[الحالي, السابق]`، والتوقيع **بالحالي وحده دائمًا**. ولو وُقِّع بالسابق لصار الدوران بلا نهاية ولما انقضت نافذته أبدًا.

و`kid` يُقرأ **قبل** التحقّق — أي من مدخل غير موثوق — فلا يُصدَّق: يُستخدم للاختيار وحده، **والتوقيع هو ما يحكم**. ومعرّف لا يقابل مفتاحًا في الحلقة يُرفض بلا حساب HMAC واحد.

وتوكن بلا `kid` — أُصدر قبل هذا التغيير — يُجرَّب على الحلقة كلّها. تسامح **مؤقّت** بمقدار عمر أطول توكن، وحذفه اليوم كان سيُخرج كل مستخدم قائم: أي نفس العطل الذي كُتب هذا البند لإصلاحه.

#### الإجراء الموثَّق (`SETUP_SECRETS.md §4.7`)

الترتيب **ملزم**: يُنقل السرّ الحالي إلى `*_PREVIOUS` **قبل** تبديل الحالي. لو بُدِّل الحالي أوّلًا لوُجدت لحظة لا يقبل فيها الخادم توكنات أحد — أي العطل نفسه في نافذة أضيق.

| السرّ | أطول ما يُوقَّع به | النافذة الدنيا |
|---|---|---|
| `AUTH_TOKEN_SECRET` | توكن التحديث (٣٠ يومًا) | ٣١ يومًا |
| `MEDIA_TOKEN_SECRET` | قدرة وسائط (٣ دقائق) | ساعة |

وحذف `*_PREVIOUS` بعد النافذة ليس تنظيفًا اختياريًّا: **هو ما يُبطل** توكنات السرّ القديم. سرٌّ يبقى في الخانة السابقة إلى الأبد يعني تدويرًا لم يكتمل.

وللسرّ **المسروق** إجراء مختلف موثَّق: تُتجاوَز خانة السابق فيُخرَج كل مستخدم — وهذا هو المطلوب. الحلقة لتدوير مُخطَّط لا لاحتواء تسريب.

#### معايير القبول الثلاثة

| المعيار | الحالة |
|---|---|
| تدوير السرّ لا يُخرج أي مستخدم أثناء النافذة | ✅ مُختبَر: توكن السابق يُقبل مع الحلقة |
| توكن موقَّع بمفتاح مسحوب يُرفض | ✅ مُختبَر: حذف السابق يُبطل توكناته |
| الإجراء موثَّق في `SETUP_SECRETS.md` | ✅ §4.7، ومُختبَر أن الاسمين مذكوران فيه |

واختباران يحرسان الانزلاق: أن **كل** مسارات التحقّق الخمسة تستخدم الحلقة (مسار واحد بالسرّ المفرد يُخرج مستخدميه عند كل دوران — عطل يظهر مرّة كل تدوير، أي حين لا أحد ينظر)، وأن `createSignedToken` لا يُستدعى بحلقة أبدًا.

#### الملفات

`src/lib/security.ts` (`tokenKeyId`, حلقة في `verifySignedToken`) · `src/lib/parentAuth.ts` (`secretRing` + خمسة مسارات) · `src/lib/db.ts` (متغيّران اختياريان) · `.env.example` · `SETUP_SECRETS.md §4.7` · `test/tokenRotation.test.mjs` (جديد، 15) · `test/architecture.test.mjs` (تحديث عقد الحمولة).

#### التالي

بالترتيب من §6: `SEC-110` (تحقق مخطَّطي مركزي للمدخلات، P1، 508 نقطة تتحقّق يدويًّا).

---

### الدفعة الثامنة عشرة (2026-08-26) — `SEC-110` تحقّق مخطَّطي مركزي (الطبقة + أربعة موجّهات)

الفحوص بعد الدفعة: Worker `npm test` **1286/1286** (+19) · `tsc` نظيف · `flutter analyze` صفر مشكلة · `flutter test` 596/596 · `vitest` 310/310.

#### ما كان

التحقّق دالّةً على انتباه كاتب كل معالج. والفرق مرئي في الكود نفسه: `Number(body.daily_minutes)` يقبل `'30'` نصًّا، و`body.autoplay ? 1 : 0` يقبل **أي** قيمة صادقة فيصير `"no"` تشغيلًا تلقائيًّا مفعَّلًا، و`{ ...value }` يمرّر كل حقل زائد عبر حدّ الكائن الدائم.

#### مُصادِق مكتوب لا `zod`

`zod` أداة ممتازة، ورُفضت لثلاثة أسباب قابلة للقياس: **الحجم** (سقف حزمة Worker، مقابل ستة أنواع نستخدمها فعلًا)، و**الاعتماد** (سطح توريد يُصان ويُدقَّق ويُرقَّى)، و**أن المطلوب ليس أنواعًا في وقت الترجمة بل رفضًا في وقت التشغيل برسالة موحّدة** — وهو مئتا سطر لا مكتبة.

وما ليس في `lib/requestSchema.ts` مقصود: لا تحويلات، ولا قيَم افتراضية، ولا اتحادات مركَّبة. المخطَّط **يصفّي ولا يُنشئ**.

#### ثلاثة قرارات تحكم السلوك

**١. رفض الحقل غير المعروف افتراضيًّا، لا خيارًا.** حقل زائد يُقبل صامتًا هو إمّا خطأ إملائي في العميل يُهمَل بلا أثر — فيُرسل `child_Id` ويُقرأ الطلب كأنه بلا طفل — أو حقل أُزيل من الخادم وما زال العميل يرسله. والحالتان تستحقّان جوابًا.

**٢. الخارج من التصفية منسوخ لا مُمرَّر.** ما بعد المخطَّط لا يرى حقلًا لم يُعلَن، فلا يمكن أن يقرأ معالجٌ حقلًا نسي المخطَّط إعلانه. وهذا ما جعل حذف `async function body(c)` من الموجّهات الأربعة ممكنًا — ومُختبَرًا: لم يبقَ في الملف جسمٌ غير مصفّى يُنشَر.

**٣. الرسالة تقول أسماء الحقول ولا تقول القاعدة.** «`birth_year` غير صالح» يكفي من يبني عميلًا شريفًا، و«يجب أن يكون بين 2008 و2024» يرسم لمن يستكشف حدود القاعدة خريطةً مجّانًا. والرمز **واحد** (`invalid_body`) لكل الأسباب: التفريق بين «مجهول» و«ناقص» يجعل من المسار أوراكل لمعرفة الحقول المعروفة. واختبار يفرض ألّا تظهر في الردّ كلمة `min` ولا `max` ولا `pattern` ولا أي حدّ.

#### عطلان حقيقيان كشفهما التحويل

هذا أهمّ ما في الدفعة، ولم يكن في نيّتها:

**١. حفظ تقدّم المشاهدة من المشغّل لم يعمل قطّ.** التطبيق يرسل `childId` و`contentId` بصيغة camelCase (`majarra_api_client.dart:542`)، والكائن الدائم يقرأ `child_id` وحده — فكان **كل** نداء يُرفض بـ400. والنداء «أطلق وانسَ» مع كتم الفشل (`playback_page.dart:836`: «must never interrupt playback») هو ما جعل العطل صامتًا شهورًا. الإصلاح ترجمةٌ في الموجّه لا في العميل: نسخة مثبَّتة على جهاز لا تُصلَح بنشر خادم.

**٢. `POST /auth/register` يستقبل `installation_id` و`platform` و`device_name` من التطبيق ويهملها كلّها.** إعلانها في المخطَّط يوثّق أن العميل يرسلها؛ وحذفها من العميل أو استخدامها قرار مستقلّ.

**وثالثة أصغر:** شاشة وليّ الأمر ترسل `allow_speed_change: v ? 1 : 0` رقمًا لا منطقيًّا. صُحِّح العميل، وبقي القبول الانتقالي للقيمتين `0`/`1` وحدهما — لا `"yes"` ولا `2` — لأن نسخة مثبَّتة لا تُصلَح بنشر خادم.

#### ما غُطّي وما لم يُغطَّ

| مُحوَّل | نقاط الكتابة |
|---|---|
| `auth.ts` | ٦ (تسجيل، دخول، تحديث، إعادة إرسال، تحقّق بريد، إعادة كلمة مرور) |
| `family.ts` | ١١ (كلّها) |
| `account.ts` | ٤ |
| `childSettings.ts` | ١ |

وتسعة موجّهات **مُدرَجة بأسبابها المكتوبة** في `STAGED` داخل `test/requestSchema.test.mjs`: `creations` (رفع ثنائي وحمولات مشاريع)، `downloads` (مُحقَّق يدويًّا بقائمة مغلقة في الدفعة 12)، `billing` (موقوف على `BILLING-001`)، `episodes`/`books`/`stories` (جلسات لها شكل واحد يُوحَّد معًا)، `notifications`، `partnerships`، `analyticsIngest`.

والحرس **هيكلي**: ملف كتابة جديد على مسار العميل يُفشل الاختبار حتى يُعلن مخطَّطه أو يُدرَج بسببه — والإدراج مرئي في مراجعة. وقائمة `STAGED` **تنقص ولا تنمو**.

#### معايير القبول

| المعيار | الحالة |
|---|---|
| كل مسار كتابة على مسار العميل يعلن مخطَّطًا ويرفض غير المعروف | 🟡 ٢٢ نقطة في ٤ موجّهات؛ ٩ موجّهات مُدرَجة بأسبابها |
| رسائل الأخطاء موحّدة ولا تكشف بنية داخلية | ✅ ومُختبَر أن الحدود لا تظهر في الردّ |
| اختبار يفشل إن أُضيف مسار كتابة بلا مخطَّط | ✅ حرس هيكلي بقائمة إدراج مرئية |

#### الملفات

`src/lib/requestSchema.ts` (جديد) · `routes/{auth,family,account,childSettings}.ts` · `test/requestSchema.test.mjs` (جديد، 19) · `test/parentProof.test.mjs` (تحديث موضع البوابة) · `app_main/lib/features/parent/presentation/pages/parent_dashboard_page.dart`.

#### التالي

إكمال `SEC-110` على الموجّهات التسعة المُدرَجة، أو بالترتيب من §6: `API-102` وما بعده.

---

### الدفعة التاسعة عشرة (2026-08-26) — `APP-106` الإهمال الصامت

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` **صفر مشكلة** · `flutter test` **607/607** (+11) · Worker `npm test` 1286/1286 · `tsc` نظيف · `vitest` 310/310.

#### لماذا هذا البند بعد الدفعة 18 لا قبلها

الدفعة 18 كشفت أن حفظ تقدّم المشاهدة كان يفشل بـ400 عند **كل** نداء، شهورًا، بلا أن يعرف أحد. والسبب لم يكن قرار «لا تُقاطع المشاهدة» — بل أن القرار كان **يمحو** الخطأ بدل أن يسجّله. فالسؤال الذي فتح هذه الدفعة: كم عطلًا آخر تحت الكتل الباقية؟

#### القاعدة

**الإهمال مسموح. الإهمال الصامت ممنوع.** كل التقاط لا يعالج شيئًا يجب أن يمرّ بقناة تسجيل أو يحمل تعليقًا يشرح لماذا الإهمال صحيح في موضعه.

و`core/diagnostics/ignored_errors.dart` هي القناة: عدّاد لكل موضع، وآخر ثلاثين خطأً في الذاكرة، وطبعٌ في التطوير وحده. **ولا ترفع ولا تعرض**، لأنها تُستدعى من مسارات لا تحتمل مقاطعة — بثّ وسائط، وتنظيف ملفات، وقراءة كاش. وظيفتها أن تجعل الخطأ **موجودًا**. ورفعه إلى خدمة رصد خارجية بند مستقلّ (`OPS-106`) لأنه يحتاج قرار مزوّد.

والعدّاد أهمّ من النصّ: «أربعمئة مرة» تعني عطلًا، و«مرة» تعني حادثة. والسجلّ محدود بثلاثين لأن مسار بثّ يُهمل خطأً لكل قفزة مشاهدة، وسجلٌّ بلا حدّ في عملية طويلة العمر يتسرّب.

#### ثلاث فئات، ثلاث معالجات

| الفئة | المعالجة | مثال |
|---|---|---|
| فشل يعني عطلًا مؤجَّلًا | **يُرفَع** | كتابة توكن، كتابة رمز وليّ الأمر |
| فشل مقبول لكن تكراره عطل | **يُسجَّل** | قراءة المخزن الآمن، قراءة كاش، إبلاغ اكتمال التنزيل |
| فشل هو المسار المتوقّع | **تعليق فقط** | إلغاء اشتراك أُلغي، `firstWhere` بمعنى «ليس هنا» |

**والكتابة في المخزن الآمن صارت ترفع** `SecureStorageUnavailableException`: كان فشلها مكتومًا، ومعناه أن الجلسة لا تنجو من إغلاق التطبيق — دخول ناجح ظاهرًا، ثم خروج بلا سبب معلَن في الإقلاع التالي. ورفعه يجعل المسار يفشل **وقتَ حدوثه** فتظهر رسالة صحيحة.

**والقراءة تبقى تُعيد `null`** — رفعها هناك يمنع فتح التطبيق أصلًا — لكن `keystoreUnavailable` صار يفرّق بين «لا جلسة محفوظة» و«المخزن لا يُقرأ». والفرق ليس تجميليًّا: في مخزن رمز وليّ الأمر كان الثاني يُقرَأ كالأوّل، فتُدعى أسرةٌ لها رمزٌ إلى **إنشاء رمز جديد**.

#### عطل ثانٍ كشفته الدفعة: كتمان رفّ الكتب كان يمحو المكتبة

`fetchBookRows` كانت تُعيد `const []` عند أي فشل «حتى لا يُسقِط رفٌّ اختياريٌّ الشاشة كلّها». لكن `ContentRepository._fetchRows` يفرّق أصلًا بين نجاح وفشل، **ويتعامل مع النجاح الفارغ كحقيقة**: يخزّنه في الكاش ويعلن المجموعة «متاحة».

أي أن الكتمان لم يحمِ الشاشة، بل حوّل **انقطاعًا مؤقّتًا في الخادم إلى حذف دائم لمكتبة الأسرة من الكاش** — والطفل يرى رفًّا فارغًا لا رفًّا قديمًا. حُذف الكتمان، فصار المستودع هو من يقرّر: يُبقي صفوف الكاش، ولا يعلن المجموعة متاحة، ويرفع `ContentSource.mixed`.

**وتعليق يزعم ما لا يحدث:** `_fetchRows` كان يحمل «send technical failure to telemetry» ولا يُرسل شيئًا — يُسقِط الخطأ ويعود. توثيق ما لا يحدث أسوأ من غياب التوثيق، لأنه يُطمئن قارئه فيمتنع عن الفحص.

#### النطاق: ما حُرس وما تُرك بقرار

| الطبقة | الحالة |
|---|---|
| `core/` | ✅ صفر كتلة صامتة |
| `features/*/data/` | ✅ صفر |
| `features/*/application/` | ✅ صفر (أُضيفت للحرس: هي موضع العطل الذي فتح البند) |
| `features/*/presentation/` | 🟡 **متروكة بقرار** — ٤٩ كتلة، ثلاثة عشر منها في `playback_page.dart` |

و`presentation/` قرارٌ لا سهو: تحويلها دفعةً واحدة تغييرٌ واسع في ملفّات آلهة **بلا اختبار** (`APP-101`) — أي مقايضة عطلٍ صامت بعطلٍ صريح في مسار المشاهدة. تُحوَّل مع تفكيك تلك الملفّات.

والحرس اختبارٌ يقرأ المصدر: كتلة جوفها فارغ تمامًا تُفشِل الجولة، والتعليق جوفٌ لأنه التبرير الذي يطلبه المعيار.

#### معايير القبول الثلاثة

| المعيار | الحالة |
|---|---|
| صفر `catch (_) {}` بلا تبرير في `core/` و`features/*/data/` | ✅ ومحروس باختبار، وامتدّ إلى `application/` |
| فشل جلب الكتب يُميَّز عن «لا كتب» | ✅ مُختبَر في الاتجاهين |
| فشل المخزن الآمن يصل إلى المستخدم | ✅ الكتابة ترفع، والقراءة تُعلن `keystoreUnavailable` |

#### الملفات

`lib/core/diagnostics/ignored_errors.dart` (جديد) · `lib/core/failures/secure_storage_failure.dart` (جديد — نُقل من `file_crypto.dart` ويُصدَّر منه) · `auth_storage.dart` (+حقن للاختبار) · `parent_pin_store.dart` · `content_repository.dart` · `majarra_api_client.dart` · `content_dtos.dart` · `download_repository.dart` · `download_manager.dart` · `creative_catalogue_provider.dart` · `local_media_source.dart` · `test/ignored_errors_test.dart` (جديد، 11).

#### التالي

`OPS-106` (لا رصد فعلي و٢٤ حدثًا فاشلًا بلا تنبيه) — وهو المتلقّي الطبيعي لما تسجّله هذه القناة اليوم في الذاكرة وحدها.

---

### الدفعة العشرون (2026-08-26) — `OPS-106` رصد يرصد فعلًا

الفحوص بعد الدفعة: Worker `npm test` **1301/1301** (+15) · `tsc` نظيف (الطرفان) · ترحيل `0084` طُبِّق محليًّا · `flutter analyze` صفر · `flutter test` 607/607 · `vitest` 310/310.

#### ما كان

بنية كاملة بلا كاتب واحد: `ops_services` فيه اثنتا عشرة خدمة، و`sla_policies` ستّ سياسات، و`ops_health_checks` **صفر صفّ**، و`ops_alerts` **صفر صفّ** — وبحثتُ في المصدر كلّه فلم أجد `INSERT` واحدًا في `ops_alerts`. وأربعة وعشرون حدث عائلة فاشلًا مرّت بلا أن يعرف أحد.

**وشاشة رصدٍ لا ترصد أسوأ من غياب الشاشة:** «صفر تنبيه نشط» و«خدمات بحالة unknown» تُقرأ في لمحة **«لا مشاكل»**.

وأسوأ ما وجدته فيها سطرٌ واحد: `/ops/overview` كان يُرجع `api: { status: 'healthy', checked_at: new Date() }` — **حالة مثبَّتة في الكود، وطابع زمني من لحظة قراءة الشاشة لا من لحظة فحص**. أي أنه كان يختلق الشيء الوحيد الذي جاء المشغّل يسأل عنه.

#### ما يُفحَص، وما لا يُفحَص عن قصد

| الخدمة | الإشارة الحقيقية |
|---|---|
| `d1` | استعلام فعليّ وزمنه |
| `admin_api` | تشغيل هذه الدورة نفسه + زمن رحلة D1 |
| `queue_family_events` | حداثة آخر حدث مُسقَط |
| `queue_dlq` | عدد الأحداث الفاشلة المعلَّقة |

وثمانية خدمات **لا يُكتب لها صفّ**: `r2_media` و`cdn` تحتاج طلبًا خارجيًّا كل خمس دقائق (تكلفة وهشاشة — انقطاع شبكة عابر يصير «عطل CDN»)، و`familystate` تحتاج مسار تفتيش لا وجود له، والباقي منطق تطبيقي لا خدمة تُستقصى.

تركها `unknown` **أصدق** من كتابة `healthy` مُفترَضة. وهذا هو الفرق بين رصد وواجهة تُطمئن.

#### التنبيه: ثلاث مراحل كلّها إلزامية

**١. يُرفَع بلا تكرار — والقيد في القاعدة لا في الكود.** فهرس فريد **جزئي** على `fingerprint` حيث `status IN ('open','acknowledged')`. والفحص في الكود (`SELECT` ثم `INSERT`) لا يكفي: دورتان متقاربتان — أو مستهلك DLQ ودورة فحص في اللحظة نفسها — تمرّان من الفحص كلتاهما. وجزئي عن قصد: تنبيه أُغلق قبل شهر وعادت حالته يجب أن يكون صفًّا جديدًا بتاريخ جديد، لا إحياءً يجعل `started_at` كذبة.

فعطلٌ يعيش يومًا يُنتج **رسالة واحدة لا ٢٨٨**.

**٢. يُسلَّم، ويُسجَّل ما جرى للتسليم.** عمودان جديدان: `notified_at` و`notify_outcome` (`sent` / `unconfigured` / `provider_error`). و`unconfigured` قيمة **صريحة** لأنها الحالة الأخطر — النظام يعمل والتنبيهات تُكتب ولا أحد يستقبلها؛ تركها `NULL` كان سيخلطها بـ«لم يُحاول بعد».

والقناة `sendOpsAlertEmail` مُغلِّف لا `send` مُصدَّرة: تصدير `send` كان سيجعل كل مسار قادرًا على إرسال بريد باسم المنصّة. والمستقبِل من `OPS_ALERT_EMAIL` لا من المتصل.

**ولم أستعمل `emailIsConfigured`**: هي تشترط `EMAIL_VERIFICATION_URL` — رابطٌ لا شأن له بتنبيه عمليات. استعمالها كان سيُسكِت الرصد كلّه حين يتغيّر إعدادٌ لمسار آخر تمامًا.

**٣. يُغلَق تلقائيًّا.** والنداء **غير مشروط** في كل دورة تجد الحالة سليمة — لا عند «زوال» مرصود. الفرق أن الإغلاق يصير دالّةً على الواقع الحالي لا على تذكّر النظام أنه رفع تنبيهًا، فدورةٌ فاتت أو نشرٌ أعاد التشغيل لا يُخلّف تنبيهًا خالدًا. وتنبيه يبقى مفتوحًا بعد زوال سببه **يُدرِّب المشغّل على تجاهل الشاشة**.

#### قرارات صغيرة يهمّ سببها

- **العتبة `1` لا `10`.** حدث عائلة فاشل واحد يعني أسرةً بعينها إسقاطها متأخّر: طفل قد لا يظهر، أو تقدّم لا يُحفَظ. والأربعة والعشرون كانت ستُرصد من أوّلها.
- **`60` دقيقة لتأخّر الطابور لا `30`.** الثلاثون في `sla_policies.queue-family` سياسة **حلّ**؛ وهذه عتبة **رصد**. لو تساويا لرُفع التنبيه في لحظة خرق السياسة — أي بلا وقت لأحد أن يتصرّف.
- **«لا حدث قطّ» ليس تأخّرًا.** منصّة لم تُنتج حدثًا بعد تُعطي `unknown` بلا تنبيه. وأوّل تنبيه كاذب هو الذي يُعلّم المشغّل أن الشاشة لا تُقرأ.
- **الدورة لا تفتح حادثة.** الحادثة قرار إنسان: إعلان أن شيئًا يستحقّ تحقيقًا ومالكًا. وفتحها آليًّا يُنتج حوادث بلا أصحاب تُغلَق كسلًا، فتفقد الكلمة معناها. واختبار يفرض أن `healthChecks.ts` لا يذكر `INSERT INTO ops_incidents`.
- **التنبيه في مستهلك الـDLQ بعد الـack لا قبله.** الحفظ هو الالتزام والإشعار خدمة عليه؛ ولو كان قبله لصار مزوّد بريد ساقط سببًا في تدوير أحداث فاشلة إلى ما لا نهاية. وهو مرفوع هناك **إضافةً** إلى الدورة: الدورة تكتشف التراكم بعد دقائق، والمستهلك يكتشف أوّل حدث في ثانيته — والبصمة واحدة فالفهرس يمنع رسالتين.

#### جدولان لا واحد، والتوزيع صريح

`*/5 * * * *` للفحص و`0 3 * * *` للتنظيف. وكان الحرس داخل `cleanup.ts` بتعبيره الحرفي، فصار التوزيع في `index.ts` مع `console.error('unhandled_cron')`: **تعبيرٌ في الإعداد بلا فرع في الكود يعمل ولا يفعل شيئًا — عطل صامت لا يُنتج خطأً في أي سجلّ.** واختبار يفرض أن كل تعبير مُعلَن في `wrangler.jsonc` له فرع، وأنه مُعلَن **مرّتين** (البيئات المُسمّاة لا ترث المشغّلات).

وبالمناسبة صار `queue_health` يُحدَّث و`telemetry_sources.last_data_at` يُختَم: كانا بذرتين مجمَّدتين تقرؤهما الشاشة كأنهما قياس.

#### الأربعة والعشرون صفًّا

`tools/ops/triage-failed-events.mjs` يجمعها ويصنّفها إلى `replayable` / `placeholder` / `unknown_type` ويطبع أمر القرار لكل مجموعة. **ولا يكتب شيئًا**: الإعادة والاستبعاد عبر مسارَي الإدارة اللذين يفرضان الصلاحية ويكتبان `resolved_by` — أداةٌ تكتب مباشرة كانت ستُنتج صفوفًا بلا فاعل معروف.

**وحدّ صريح:** لم أُشخّص الصفوف الأربعة والعشرين بنفسي. لا وصول لي إلى قاعدة الإنتاج من هنا (القاعدة المحلية غير موجودة أصلًا حتى شغّلتُ الترحيل الآن)، فالرقم في هذا الأودت جاء من استعلام سابق لا مني. المُسلَّم أداةٌ تُنتج التصنيف والقرار بأمر واحد، وتشغيلها على الإنتاج فعلٌ للمالك.

#### معايير القبول الثلاثة

| المعيار | الحالة |
|---|---|
| فحص صحة يكتب صفًّا كل دورة والشاشة تعرض آخر حالة حقيقية | ✅ أربع خدمات بإشارات حقيقية، والشاشة لم تبقَ تختلق `healthy` |
| صفّ في `failed_family_events` يُنتج تنبيهًا يصل فعلًا | ✅ عتبة ١، ومُختبَر أن الرسالة تصل بجسم فيه الحالة والخدمة والبصمة · **يحتاج `OPS_ALERT_EMAIL`** |
| الـ٢٤ صفًّا مُشخَّصة ومُعالَجة أو مُصنَّفة | 🟡 الأداة والتصنيف والإجراء جاهزة؛ التشغيل على الإنتاج فعل مالك |

#### الملفات

`migrations/0084_ops_alert_delivery.sql` (جديد) · `src/lib/opsAlerts.ts` (جديد) · `src/scheduled/healthChecks.ts` (جديد) · `src/scheduled/cleanup.ts` · `src/index.ts` · `src/queue/dlq.ts` · `src/routes/adminOpsReliability.ts` · `src/services/email.ts` · `src/lib/db.ts` · `wrangler.jsonc` · `tools/ops/triage-failed-events.mjs` (جديد) · `SETUP_SECRETS.md §4.8` · `.env.example` · `test/opsMonitoring.test.mjs` (جديد، 15) · `test/dlqFamilyEvents.test.mjs` (تحديث: الفرز بالجدول لا بعدّ الكتابات).

#### التالي

`SEC-103` (منح الأدوار بلا مقارنة صلاحيات، P1) أو إكمال `SEC-110` على الموجّهات التسعة.

---

### الدفعة الحادية والعشرون (2026-08-26) — `SEC-103` تصعيد الصلاحيات

الفحوص بعد الدفعة: Worker `npm test` **1315/1315** (+14) · `tsc` نظيف (الطرفان) · `vitest` 310/310 · Flutter 607/607 بلا تغيير.

#### ما كان

`manage_permissions` كانت **مفتاح المنصّة كلّها**: من يملكها يمنح نفسه `owner`. والحرس كان موجودًا (`requirePermission('manage_permissions')`) والمقارنة غائبة — أي أن السؤال المطروح كان «هل تستطيع منح الأدوار؟» لا «هل تستطيع منح **هذا** الدور؟».

وكانت مقارنة واحدة قائمة في `POST /users/:id/grants` وحدها. والبحث كشف **خمسة أبواب** تتجاوزها:

| الباب | كيف كان يُصعّد |
|---|---|
| `POST /users` | يُنشئ مستخدمًا جديدًا بدور `owner` مباشرة — **وبكلمة مرور يعرفها المُنشئ** |
| `POST /grants` | يمنح أي دور لأي أحد، بلا مقارنة ولا تحقّق من وجود الدور أو المستفيد |
| `POST /roles` | يخلق دورًا يحمل `manage_billing` ثم يمنحه لنفسه |
| `PATCH /roles/:id` | يمنح نفسه دورًا مخصَّصًا ثم يرفع صلاحياته من هناك |
| `POST /teams` | يضيف أعضاء بلا فحص إلى فريق (والعضوية تمنح منح الفريق) |

وهذا ما يفعله حرسٌ يعيش في ملف المسار الذي كتبه أوّل من احتاجه: يحمي بابه ويترك الباقي.

#### القرار: مجموعة فرعية لا رتبة

**رُفض اختراع عمود `rank` على `roles`.** يحتاج قيمة لكل دور مخصَّص يُنشأ لاحقًا، ويصير رقمًا يُصان بيد إنسان **بجانب** الحقيقة الفعلية (`role_permissions`). ورقمان يفترقان يعني حرسًا يسمح بما تمنعه الحقيقة — وهو أسوأ من غياب الحرس لأنه يُطمئن.

والقاعدة المطبَّقة: **لا تُعطِ ما لا تملك**، بمقارنة مجموعتَي صلاحيات تُقرأ من نفس الجدول الذي يُنفَّذ به التصريح. فلا موضع لافتراقهما.

والمقارنة على `permissions` المسطّحة عن قصد لا على النطاق: تضييقها بالنطاق كان سيمنع قائد قسم من منح دوره لعضو في قسمه — وهو الاستخدام المشروع الوحيد لتلك الشاشة.

#### `granted_by`: `NULL` لا نصّ مختلق

العمود مُعلَن `REFERENCES admin_users(id)`، و**D1 لا يفرض المفاتيح الأجنبية** بلا `PRAGMA foreign_keys` لكل اتصال. فكان يستقبل `'legacy-admin-key'`، **وقيمة ترويسة يكتبها المتصل** (`X-Admin-Actor` عبر `auditActor`) — أي أن سجلّ «من منح هذه الصلاحية» كان **قابلًا للتلفيق من الطلب نفسه**.

الآن يُتحقَّق من وجود الهوية في `admin_users`، وإلا `NULL`. و«غير منسوب» أصدق من «منسوب إلى كائن غير موجود»: الثانية كذبة يصعب اكتشافها في مراجعة. ونسبةُ الفعل تبقى في `audit_logs` حيث يظهر `legacy-admin-key` صريحًا.

#### وبابٌ خلفيّ ثالث لم يكن في نصّ البند

`DELETE /grants/:id` في موجّه الفرق كان **يحذف بالمعرّف بلا أي فحص**، بينما نظيره في `adminUsers.ts` يحمي آخر مالك. أي طلبٌ واحد يُفرغ المنصّة من مالكها — ثم لا يبقى من يستطيع إعادة منح الملكية، لأن `manage_permissions` مقصورة على `owner` و`system_admin`.

**وكان بلا صفّ تدقيق أيضًا**: أخطر عملية في المنصّة تحدث بلا أثر. صار المنح والسحب يكتبان في `audit_logs`.

وثلاثة تصحيحات أصغر في نفس المسار: التحقّق من وجود الدور والمستفيد وصحّة النطاق (D1 لا يفرض `CHECK` فكان الصفّ يُكتب بقيَم لا تقابل شيئًا)، و`crypto.randomUUID()` بدل `grant-${Date.now()}` الذي يتصادم لطلبين في نفس المليّة.

#### معايير القبول الثلاثة

| المعيار | الحالة |
|---|---|
| حامل `manage_permissions` بلا `owner` لا يستطيع منح `owner` | ✅ مُختبَر بالحالة نفسها |
| منح دور يحمل صلاحية لا يملكها المانح يُرفض بـ403 | ✅ في **ستة** مسارات، وحرسٌ يفرض أن كل مسار كتابةٍ يقارن **قبل** أن يكتب |
| `granted_by` يُرفض إن لم يطابق مستخدمًا قائمًا | ✅ وحرسٌ يفرض ألّا يعود `auditActor` إلى أي `INSERT INTO access_grants` |

والحرس في `routeGuards.test.mjs` أُعيدت كتابته: كان يثبّت **موضع** الدالّة في `adminUsers.ts`، فصار يثبّت **الخاصيّة** — كل مسار يكتب منحًا أو يكتب صلاحيات دور يستدعي المقارنة قبل الكتابة. وهذا ما كان سيمنع الأبواب الخمسة من الوجود أصلًا.

#### الملفات

`src/lib/privilegeGuard.ts` (جديد) · `src/routes/adminTeams.ts` (منح، سحب، دور جديد، تعديل صلاحيات دور، أعضاء فريق) · `src/routes/adminUsers.ts` (إنشاء مستخدم، منح، `granted_by`) · `test/privilegeGuard.test.mjs` (جديد، 12) · `test/routeGuards.test.mjs` (ثلاثة حروس أقوى).

#### التالي

إكمال `SEC-110` على الموجّهات التسعة المُدرَجة، أو `API-102` (حدود الباقات بمصدرَي حقيقة) بالترتيب من §6.

---

### الدفعة الثانية والعشرون (2026-08-26) — `API-102` مصدر حقيقة واحد لحدود الباقات

الفحوص بعد الدفعة: Worker `npm test` **1323/1323** (+8) · `tsc` نظيف · ترحيل `0085` طُبِّق محليًّا · `vitest` 310/310 · Flutter 607/607.

#### ما كان

الأرقام مكتوبة في `lib/familyPolicy.ts` **وفي** جدول `subscription_plan_limits` (المهاجرة 0006). والمفروض فعلًا هو الكود: بحثتُ في `src/` كلّه فلا سطر واحد يقرأ الجدول — لا `FamilyState` الذي يفرض الحدود، ولا `routes/billing.ts` الذي يعرضها، ولا `adminPlans.ts` (وهو يُعلن `source: 'family_policy'` صراحةً).

**والجدول يبدو حيًّا:** أعمدته صحيحة، وصفوفه مزروعة، وفيه `policy_version` — ولم يُدرَج في قائمة الجداول الميتة في `0010`. فأي عملية تسويق تعدّل صفًّا فيه وتظنّ أنها غيّرت حدًّا، والحدّ لا يتغيّر. وفخٌّ صامتٌ كهذا **أسوأ من غياب الجدول**.

#### القرار: الكود هو المصدر، والجدول يُسقَط

البند عرض بديلين. اخترت الكود، والسبب في موضع الفرض لا في الذوق:

**`FamilyState` هو من يفرض الحدّ**، بعدٍّ-ثم-إدراج مُسلسَل لكل أسرة. قراءة الحدّ من D1 داخل ذلك المسار تُدخل **فشلًا شبكيًّا في اللحظة التي يجب أن تكون قاطعة**: انقطاع D1 يصير إمّا فتحًا للباب — أسرة تتجاوز حدّها — أو منعًا لأسرة تدفع. والحدّ في الكود لا يفشل.

و«تغيير حدّ بلا نشر كود» ليس حاجةً قائمة: الحدود قرار تسعير مرتبط بـ`API-104` الموقوف على قرار مالك، وكل تغيير فيها يستحقّ مراجعةً واختبارًا — لا صفًّا يُعدَّل في قاعدة بلا أثر.

#### `PLAN_POLICY_VERSION`، وبصمة تمنع نسيانه

رقم إصدار لا يتغيّر مع ما يوصفه **أسوأ من غيابه**: يجعل سجلًّا قديمًا يبدو مُفسَّرًا وهو ليس كذلك. فلم أكتفِ بالثابت.

`planLimitsFingerprint()` يبني نصًّا حتميًّا من `PLAN_LIMITS` نفسها، واختبارٌ يثبّته. فأي تغيير في رقم واحد يُفشل الجولة برسالة تقول ما يجب فعله: ارفع الإصدار وحدّث البصمة. **الخطوتان معًا أو لا شيء.**

#### الإصدار مع كل قرار حدّ

كان الرفض نصًّا وحده — `'Child profile limit reached'` — فلا العميل يعرف أي رقم بلغه، ولا من يقرأ سجلًّا بعد شهر يعرف **على أي سياسة رُفض**. و«رُفض على سياسة 2» جواب، و«رُفض» ليس جوابًا.

فصار كل رفض حدٍّ يمرّ بـ`limitRefusal` ويحمل: اسم الحدّ، والباقة، وقيمة الحدّ، والمُستهلَك منه، و`policy_version`. والحدود الخمسة كلّها: `children`, `devices`, `concurrent_streams`, `download_devices`, `offline_items`.

**والقبول كذلك**: `child.created` و`playback.started` و`offline_license.issued` تحمل `policyVersion` إلى سجلّ أودت الأسرة. سجلٌّ يقول «أُنشئ» بلا سياسته لا يُفسِّر لماذا رُفض التالي.

#### معايير القبول

| المعيار | الحالة |
|---|---|
| تغيير حدّ في المصدر الواحد يظهر أثره بلا نشر كود | ⛔ **لا ينطبق** — المعيار مشروط بـ«إن كان الجدول هو المصدر»، والقرار أن الكود هو المصدر |
| لا يبقى تعريفان للأرقام في المستودع | ✅ الجدول مُسقَط، ومُختبَر أن لا سطر في `src/` يذكره |
| `policy_version` يُسجَّل مع كل قرار حدّ | ✅ في الرفض وفي القبول، ومُختبَر للحدود الخمسة |

#### حدّ موثَّق

`DATABASE_V2_SCHEMA.sql` يذكر الجدول بكثافة (زنادات، وسياسات RLS، ودوالّ تخفيض باقة). ذلك المستند **تصميم Postgres غير مطبَّق** لا مخطَّط D1 الحالي، وإسقاط الجدول هنا لا يمسّه. وإن نُفِّذ ذلك التصميم يومًا فقرار «أين تعيش الأرقام» يُعاد فتحه مع بنيته المختلفة (زنادٌ في القاعدة يفرض الحدّ ذرّيًّا، وهو ما يجعل الجدول مصدرًا معقولًا هناك).

#### الملفات

`migrations/0085_drop_plan_limits_table.sql` (جديد) · `src/lib/familyPolicy.ts` (`PLAN_POLICY_VERSION` + البصمة) · `src/do/FamilyState.ts` (`limitRefusal` + خمسة رفوض + ثلاثة أحداث) · `src/routes/adminPlans.ts` · `test/planPolicy.test.mjs` (جديد، 8).

#### التالي

إكمال `SEC-110` على الموجّهات التسعة، أو `API-105` (ثلاثة جداول تُقرأ ولا يكتبها شيء) بالترتيب من §6.

---

### الدفعة الثالثة والعشرون (2026-08-26) — `SEC-110` اكتمل: القائمة المُدرَجة صارت فارغة

الفحوص بعد الدفعة: Worker `npm test` **1324/1324** (+1 صافي، مع تحويل اختبارين) · `tsc` نظيف · `vitest` 310/310 · Flutter 607/607.

#### ما كان معلَّقًا

الدفعة 18 سلّمت الطبقة وأربعة موجّهات، و**تسعة مُدرَجة بأسبابها**. وقلتُ حينها إن القائمة «تنقص ولا تنمو» — وهذه الدفعة تُفرغها: ثلاثة عشر موجّه عميل، كلّها بمخطَّط.

#### `bodyOr400` — نسخةٌ خامسة كانت ستعني خمسة مواضع للافتراق

المُغلِّف كان مكتوبًا في أربعة موجّهات بأربع نسخ متطابقة. انتقل إلى `requestSchema.ts`، ومعه `NO_FIELDS` لمسارٍ كل معلوماته في مساره.

#### ما كشفه التحويل هذه المرّة

**١. المُصرِّف وجد ثلاثة حقول تُقرأ ولا تُعلَن.** حين صار جسم `POST /episodes/:id/progress` مُصنَّفًا، رفض المُصرِّف `duration_seconds` و`event_id` و`is_completed`: المعالج يقرؤها ولم يكن أحد يعلنها. وهذا بالضبط ما يفعله المخطَّط — يجعل «ما يُقرأ» و«ما يُعلَن» شيئًا واحدًا لا شيئين يفترقان بمرور الوقت.

**٢. `POST /notifications/test` كان يقرأ الجسم بلا التقاط.** جسمٌ مشوّه يرفع استثناءً فيصير **500** بدل 400. وكانت القيَم تُكتب كما جاءت: `kind` كائنًا، أو عنوانًا بميغابايت.

**٣. `Number(value.score)` كان يقبل ما ليس رقمًا.** `'7'` نصًّا يمرّ، و`'7abc'` تصير `NaN` التي تمرّ من فحص `< 0` لأن كل مقارنة مع `NaN` كاذبة.

#### تشديد مقصود على التتبّع

`POST /analytics/events` كان **يُهمِل** `parent_id` من الجسم: يكتب الصفّ بأب فارغ ويعيد 201. فيصدّق العميل أن نسبته نجحت حتى يقرأ أحدٌ الجدول. صار يُرفَض بـ400.

الخاصيّة الأمنية لم تتغيّر — النسبة لا تأتي من الجسم أبدًا — لكن **الجواب صار صادقًا**. وحُوِّل الاختبار الذي كان يثبّت الإهمال إلى اختبار يثبّت الرفض، مع شرح التغيير في نصّه.

#### استثناء واحد معلَن ومعدود

`POST /billing/google-play/rtdn` يستقبل **مغلَّف Google Pub/Sub** بعد تحقّق OIDC، ويحلّه `parseGoogleRtdn`. شكله شكل Google لا شكلنا، وإعلانه في مخطَّط يعني نسخةً ثانية من عقدٍ لا نملكه — تتقادم مع أوّل تغيير عنده.

والحرس لا يقبل الاستثناء بل **يعدّه**: `DIRECT_READS = { 'billing.ts': 1 }`. قراءةٌ ثانية في نفس الملف تُفشل الجولة.

#### وقرارٌ في الاتجاه الآخر

`bubble_id` مُعلَن في مخطَّط جلسة سرد الكتب **ولا يُستخدَم فيها**: العميل يبني جسمًا واحدًا للكتب والقصص معًا، فيرسله حين يكون مضبوطًا. وإعلانه مقبولًا-مُهمَلًا أصدق من رفضٍ يقطع سردًا لأجل حقل لا يضرّ — والقرار **مكتوب** بدل أن يكون سهوًا.

وكذلك `params` في التتبّع و`answers` في الحلقات: حرّة الشكل بـ`opaque()` مع سقف، لأن مُحقِّقها الخاص (`screenParams`) يعرف ما لا يعرفه المخطَّط — أي مفاتيح مسموحة لكل شاشة. وهذه دلالة لا شكل.

#### وتحقّق من العميل قبل الإغلاق

راجعتُ أجسام الطلبات في `majarra_api_client.dart` مقابل المخطَّطات الثلاثة عشر — بعد ما كشفته الدفعة 18 من انكسار صامت في مسار التقدّم. كلّها متطابقة، ولا رفض جديد على نسخة التطبيق الحالية.

#### معايير القبول

| المعيار | الحالة |
|---|---|
| كل مسار كتابة على مسار العميل يعلن مخطَّطًا ويرفض غير المعروف | ✅ ١٣ موجّهًا، والقائمة المُدرَجة فارغة |
| رسائل الأخطاء موحّدة ولا تكشف بنية داخلية | ✅ (الدفعة 18) |
| اختبار يفشل إن أُضيف مسار كتابة بلا مخطَّط | ✅ والحرس صار يعدّ الاستثناءات لا يسمح بها |

#### الملفات

`src/lib/requestSchema.ts` (`bodyOr400` + `NO_FIELDS`) · `routes/{episodes,books,stories,downloads,notifications,billing,partnerships,analyticsIngest,creations}.ts` · `test/requestSchema.test.mjs` (القائمة والحرس) · `test/analyticsIngest.test.mjs` (تحويل موثَّق) · `test/hlsManifest.test.mjs`.

#### التالي

`API-105` (ثلاثة جداول تُقرأ ولا يكتبها شيء) بالترتيب من §6، أو `APP-101` (ملفات آلهة بلا اختبار، XL).

---

### الدفعة الرابعة والعشرون (2026-08-26) — `API-105` جداول تُقرأ ولا يكتبها شيء

الفحوص بعد الدفعة: Worker `npm test` **1336/1336** (+12) · `tsc` نظيف (الطرفان) · ترحيل `0086` طُبِّق محليًّا · `vitest` 310/310 · Flutter 607/607.

#### ما كان

`children_profiles` و`watch_progress` و`child_screen_time_daily`: **صفر صفًّا، وصفر `INSERT` في المصدر كلّه**. سلطة الحقيقة انتقلت إلى الكائن الدائم، والإسقاط لم يُكتب قطّ.

والأثر ليس «ميزة ناقصة» بل **رقمٌ كاذب**: شاشةٌ تقرأ جدولًا فارغًا تعرض صفرًا يُقرأ «لا نشاط» ومعناه «لا بيانات». و`GET /admin/analytics/children/:id` كان يُعيد **404 لكل طفل قائم فعلًا** — «طفل غير موجود» عن طفل يشاهد الآن.

#### القرار: الطابور يكتب، وجدولٌ جديد لا إحياء القديم

البند طلب قرارًا صريحًا بين اتجاهين (الطابور يُسقِط، أو الشاشات تقرأ من الكائن). اخترت **الطابور**: الحدث موجود أصلًا (`progress.updated`/`content.completed`) ويحمل كل ما يلزم، وقراءة الشاشات من الكائن تعني نداءً لكل أسرة في كل تقرير إداري — وهي عملية عبر آلاف الكائنات.

**ولم أُحيِ `watch_progress`.** شكله من عصر ما قبل الكائن ويخالف ما يُنتجه:

| هو | والكائن يُنتج |
|---|---|
| مفتاح `(child_id, episode_id)` | `(child_id, content_type, content_id)` — والكتب والقصص والألعاب صارت نصف المحتوى |
| ثوانٍ | مللي ثانية — والتحويل عند الكتابة يُفقد ما يحتاجه استكمال المشاهدة |
| مفتاحان أجنبيان إلى `children_profiles` و`episodes` | والأوّل جدولٌ بلا كاتب أيضًا |

فأحيا `child_progress_projection` بنمط `0008` المُعلَن: بلا مفاتيح أجنبية، وبعلامة `last_event_at_ms`.

#### `child_screen_time_daily` أُسقط بلا بديل

**صفر قارئ في المصدر كلّه.** وقت الشاشة يُحتسَب داخل الكائن لأن الحدّ اليومي قراءة-تعديل-كتابة تحتاج تسلسلًا لكل أسرة، **ولا حدث في الـoutbox يُصدره**. فالجدول كان وعدًا بتقرير لم يُطلَب بعد — وحين يُطلب يُنشأ بنمط الإسقاط لا بشكل 2019. وإبقاؤه كان سيبقي الفخّ: عمودٌ صحيح يُقرأ يومًا فيُعطي صفرًا.

#### وعطل ثالث لم يكن في نصّ البند

`child.updated` و`child.track_transitioned` كانا يُصدَران ويُدقَّقان **ولا يصلان إلى الإسقاط إطلاقًا**. أي أن تعديل اسم مستعار أو انتقال طفل من `kids` إلى `junior` لا يظهر في `child_projection` — فتبقى اللوحة تعرض الاسم القديم والمسار القديم إلى الأبد، وتُخدَم للطفل مكتبةُ عمرٍ تجاوزه.

وحمولة `child.updated` كانت تحمل **أسماء الحقول وحدها** بلا قيَمها، فلم يكن الإسقاط يستطيع تطبيق شيء أصلًا. صارت تحمل القيَم المُعلَنة، و`redactForAudit` يمسح `nickname` قبل سجلّ التدقيق كما كان.

والتطبيق `COALESCE` لا إسناد مباشر: الحدث **جزئيّ بطبيعته** — يحمل ما تغيّر وحده — فإسناد `NULL` لما لم يُرسَل كان سيمحو حقلًا لم يُطلَب تعديله.

#### قواعد الإسقاط التي تستحقّ ذكر سببها

- **حدث أقدم يُهمَل** (`excluded.last_event_at_ms >= …`): الطابور لا يضمن ترتيبًا، ونبضةٌ متأخّرة كانت سترجع الموضع إلى الوراء فيبدأ الطفل من أوّل الحلقة بعد أن شاهد نصفها.
- **المدّة تُثبَّت بالأكبر**: نبضةٌ مبكّرة قد تعلن مدّة صفرًا قبل أن يقرأ المشغّل الملف كاملًا.
- **الإكمال لا يُنزَع**: من أكمل حلقةً ثم أعاد مشاهدتها من أوّلها أكملها.
- **علامات الحذف تُحترَم**: صفٌّ جديد بعد حذف طفل يُعيد بناء ما طُلب محوه.
- **حدث بلا هوية محتوى يرفع** لا يُكتب ناقصًا: الرفع يُعيد المحاولة ثم يظهر في الـDLQ، والصفّ الناقص يبقى صامتًا.

#### معايير القبول

| المعيار | الحالة |
|---|---|
| إكمال طفل لحلقة يُنتج صفًّا في إسقاط التقدّم خلال ثوانٍ | ✅ الطابور يكتبه، ومُختبَر بالحمولة الحقيقية |
| «تابع المشاهدة» يعرض عنصرًا حقيقيًّا في بيئة اختبار | 🟡 المسار قائم (`GET /family/progress` من الكائن) **ولم أتحقّق منه من طرف إلى طرف**: لا بيئة تشغيل لي هنا |
| لا شاشة تقرأ جدولًا بلا كاتب؛ واختبار يمنع العودة | ✅ الجدولان مُسقَطان، و`children_profiles` بلا قارئ، ومسحٌ للمصدر يفرض ذلك |

#### حدود موثَّقة

**١. `children_profiles` لم يُسقَط.** مفاتيح أجنبية من `attempts` و`favorites` و`parental_consents` و`data_requests` تشير إليه، وإسقاطه سلسلة قرارات مستقلّة. المُنفَّذ أن **لا قارئ يبقى له** — وهو ما يجعل إسقاطه لاحقًا تنظيفًا لا تغييرًا في السلوك.

**٢. بلوكا `mostWatched` و`becauseYouWatched` ما زالا مخفيَّين** في التطبيق (`home/domain/feed_blocks.dart:260`). الإسقاط جعلهما **ممكنَين** — النسبة إلى كوكب أو الترتيب بالأكثر إكمالًا صارت استعلامًا حقيقيًّا — لكن لا نقطة نهاية تخدمهما بعد. وكشفهما بلا ذلك يعني ترتيبًا مُختلقًا، وهو ما أُخفيا لأجله.

#### الملفات

`migrations/0086_child_progress_projection.sql` (جديد) · `src/queue/familyEvents.ts` (ثلاثة معالجات) · `src/do/FamilyState.ts` (قيَم `child.updated`) · `routes/{adminAnalytics,adminMastery,childSettings,recommendations,adminPlanets}.ts` · `test/progressProjection.test.mjs` (جديد، 12) · `test/masteryReports.test.mjs` (اسم العمود).

#### التالي

`APP-101` (أربعة ملفات آلهة بلا اختبار، XL) أو `API-106` (النشر يتخطّى البوابة) بالترتيب من §6.

---

### الدفعة الخامسة والعشرون (2026-08-26) — `API-106` مسار نشر واحد يفشل مغلقًا

الفحوص بعد الدفعة: Worker `npm test` **1337/1337** · `tsc` نظيف (الطرفان) · `vitest` 310/310 · Flutter 607/607.

#### ما كان

موجّه النشر الجديد (القصص، الكتب، الألعاب، المشروعات) **يفشل مغلقًا** بشكل صحيح. ومعالجا السلسلة والحلقة الأقدم في `routes/admin.ts` كانا يتعاملان مع نتيجة بوابة **فارغة** كـ«غير مُقيَّمة» — `readiness: 'not evaluated'` — **وينشران على أي حال**.

أي أنهما يتجاوزان البوابة **بالضبط في الحالة التي لم تستطع فيها البوابة أن تقول شيئًا**. وهذه ليست حالة حميدة: `evaluateFor` تعود فارغةً حين لا تستطيع جمع الحقائق عن الكيان، والوجود مُتحقَّق منه قبلها بسطرين — فالفراغ يعني **أن البوابة لم تعمل**. ونشرُ ما لم تُقيّمه البوابة هو العطل الذي أُنشئت البوابة لمنعه.

والتعليق في `adminPublish.ts` كان يقرّ بذلك ويسجّله متابعةً «لم تُنفَّذ هنا لأن تشديد مسار نشر حيّ يستحقّ تغييرًا خاصًّا به». هذا هو ذلك التغيير.

**والأثر مقيس:** عشرون حلقة منشورة اليوم **بلا أصل فيديو واحد** وبمدّة زمنية فارغة. أي أن المسار الذي نُشرت به لم يمنع نشر محتوى **لا يمكن تشغيله** على جهاز طفل.

#### القرار: دالّة واحدة، لا مواءمة نسختين

كان يمكن «مواءمة» المعالجَين بإضافة فرع الفشل المُغلَق إلى كل منهما. ورفضتُ ذلك: نسختان من مسار نشر تعنيان **تشدّدًا في واحدة وتساهلًا في الأخرى** — وهو ما حدث فعلًا. فصار الستّة يستدعون `publishEntity` واحدة.

والخريطة الحرفية هي ما يجعل ذلك ممكنًا بلا خطر: كل نوع يعلن جدوله وأعمدته، فلا اسم جدول يأتي من طلب. و`is_published` عمود على `episodes` وحدها — وضبطُ `status` بلا ضبطه يُنتج حلقةً «منشورة» **لا يراها أحد**، لأن القراءة العامة تفحص العمودين.

وبقيت الفحوص الوجودية في موضعها (`404` و`archived` و`published` المُتكرّر) لأن رسائلها مُختبَرة، والمشترك هو **البوابة والفشل المُغلَق وصفوف التدقيق وأعمدة النشر**.

وتصحيح صغير في الطريق: `pathParam` بدل `c.req.param('id') ?? ''`. مسارٌ فقد مقطعه بعد تعديل توجيه كان يُنتج «400 id required» تُقرأ خطأً في الطلب، فيبحث المشغّل في المكان الخطأ.

#### الاختبارات: من تثبيت الموضع إلى تثبيت الخاصيّة

كان اختباران يثبّتان أن **منطق البوابة داخل معالجَي `admin.ts`** — أي أنهما كانا يحرسان الشكل الذي أنتج العطل. صارا يثبّتان:

- أن كل مسار من المسارَين **يفوّض** إلى الدالّة المشتركة، وأنه لا يحتفظ بفرع بوابة خاص (`not evaluated` ممنوعة نصًّا).
- أن الدالّة المشتركة تُقيّم، وتفرّع على الحكم، وتردّ قائمة المانعين، **وتفشل مغلقًا** — وأن كل ذلك يسبق `UPDATE`.
- أن الأنواع الستّة كلّها في الخريطة، وأن الحلقة تحمل `hasIsPublished`.

#### معايير القبول

| المعيار | الحالة |
|---|---|
| نشر حلقة بلا أصل فيديو جاهز يُرفض بـ409 مع سبب | ✅ بالتركيب: البوابة تُصنّفه مانعًا (مُختبَر وظيفيًّا في `publishGate.test.mjs`)، والدالّة تردّ `gateRefusal` بـ409 قبل الكتابة |
| المسارات الستة تستخدم نفس دالّة النشر والبوابة | ✅ ومُختبَر |
| كل رفض ونشر يُسجَّل في `audit_logs` مع تحذيرات الجهوزية | ✅ `publish` و`publish_blocked` من الدالّة المشتركة |

**حدّ في التحقّق:** لا تجهيزة اختبار على مستوى المسار مع D1 في هذا المستودع، فالربط مثبَّت بمسح المصدر كما في كل حرس هنا. والمنطق نفسه — «حلقة بلا فيديو ولا صورة مصغّرة تُنتج مانعَين» — مُختبَر وظيفيًّا على `evaluatePublishGate`.

**وما لم تفعله هذه الدفعة:** العشرون حلقة المنشورة أصلًا **بقيت منشورة**. المسار صار يمنع الجديد، وسحبُ ما نُشر قرار محتوى لا هندسة — ويُعالَج في `CNT-108` (حلقات منشورة بمدة فارغة) مع `CNT-101` (صفر فيديو).

#### الملفات

`src/routes/adminPublish.ts` (خريطة الستّة + `publishEntity` + أعمدة النشر) · `src/routes/admin.ts` (المعالجان يفوّضان) · `test/publishGate.test.mjs` · `test/routeGuards.test.mjs`.

#### التالي

`APP-101` (أربعة ملفات آلهة بلا اختبار، XL) أو `DB-101` (سجل ترحيلات غير متسق) بالترتيب من §6.

---

### الدفعة الثامنة والستّون (2026-08-31) — `API-107`: التشخيص معكوس، والبوابة تحكم على عمودٍ ميت

الفحوص بعد الدفعة: Worker `npm test` **1440/1440** (+4) · `tsc --noEmit` نظيف.

#### ما ظنّه البند نقصًا في المخطَّط كان موجودًا

البند: «لا جدول `book_pages`. الصفحات نص JSON داخل `books.pages`… لا يمكن ربط صورة أو سرد بصفحة كتاب، ولا تشغيل بوابة الجهوزية على الكتب بمعنى حقيقي».

والمقيس: صفحات الكتب في **`story_pages`** بمفتاح `story_id = book.id`، ومعها `story_page_localizations` للنصّ والسرد، وربطُ أصولٍ للرسم. و`GET /books/:id/pages` يقرأ ذلك ويُخرج `translated_pages` و`narrated_pages` و`renderable` و`with_audio`. والكود يقول ذلك بنفسه في `books.ts`: «`books.pages` holds a legacy JSON blob and is deliberately **not selected**».

فلم يكن الحلّ بناء جدول — كان موجودًا. ولو بنيتُه لأضفتُ مصدرًا ثالثًا لصفحةٍ واحدة.

#### والعطل الحقيقي أخطر مما وُصف

ثلاثة مواضع كانت تحكم على **العمود الميت**:

| الموضع | ما كان |
|---|---|
| `adminPublishGate.ts` | يقرأ `b.pages` ويمرّره كـ`facts.pages` |
| `publishGate.ts:evaluateBook` | `parseJson(pages)` ثمّ `pages.length` |
| `catalogueValidation.ts:bookPublishError` | نفس الفحص على مسار الكتابة |

والمقيس: العمود `'[]'` في **الاثنين والعشرين** كتابًا. فالحكم خاطئٌ في الاتجاهين معًا:

- **لا كتاب يُنشَر** وإن كانت صفحاته كاملة في `story_pages` — لأن العمود فارغ دائمًا.
- وكتابٌ تُكتب في عموده مصفوفةٌ **يُنشَر بلا صفحة واحدة** في المصدر الذي يقرأه التطبيق.

#### الإصلاح: مصدرٌ واحد، ومعه ما كانت البوابة تفوته

استُخرجت قراءة الصفحات من فرع القصص إلى `loadPageFacts` مشتركة، فالكتاب يُقاس بما يُقاس به القصة **ومن نفس الجدول الذي يقرأه الطفل**. و`bookPublishError` صار `bookPagesReleaseError(db, id)` — سؤالٌ عن حالة لا عن شكل قيمة.

وبما أن المصدر صار حقيقيًّا، صار ممكنًا فحصُ ما لم يكن يُفحَص: **رسمُ كل صفحة ونصُّها**. كتابٌ مصوَّر بصفحةٍ بلا رسم ينشر صفحةً بيضاء، وذلك كان يمرّ لأن الصفحات لم تُقرأ أصلًا. والصفحات الناقصة **تُسمّى بأرقامها** لا تُعَدّ فقط.

ثلاثة قرارات في التفاصيل:

- **العمود القديم يبقى للتشخيص لا للحكم**: إن كانت `story_pages` فارغة والعمود غير فارغ، فالرسالة تقول «صفحاتك في المكان الخطأ وهذا مكانها» بدل «الكتاب بلا صفحات». محرّرٌ كتب عمله يستحق أن يُقال له أين هو. ولم يُحذف العمود: كل قيمة محلية `'[]'` لكنّي لا أرى الإنتاج، وحذفُ عمودٍ قد يحمل بيانات ليس قراري (`DB-102` بالمنطق نفسه).
- **إنشاء كتابٍ بحالة نشر صار مرفوضًا صراحةً**: صفحاته تُضاف بعد وجوده، فطلبُ نشرٍ لحظةَ الإنشاء لا يمكن أن يكون صادقًا. وكان الفحص القديم يُجيزه إن حمل الطلبُ مصفوفةً في العمود.
- **استعلامان لا واحدٌ لكل صفحة**: كتابٌ من أربعين صفحة كان سيكلّف أربعين رحلة لجوابٍ واحد.

#### واختبارٌ كان يُثبّت المصدر الخطأ

`publishGate.test.mjs` كان يمرّر `pages: '[]'` — نصَّ JSON من العمود القديم — فيُثبّت أن البوابة تقرأه. أُبدل بالمصدر الحقيقي، ومعه ثلاث حالات تُثبّت العطل بالاتجاهين: صفحاتٌ في العمود وحده تُنتج رسالةً تقول أين هي · وكتابٌ له صفحات حقيقية **لا يُحجَب** · وصفحةٌ بلا رسم أو بلا نصّ تُحجَب **وتُسمّى**.

#### الملفات

`dashboard/api/src/routes/adminPublishGate.ts` (`loadPageFacts`) · `dashboard/api/src/lib/publishGate.ts` (`evaluateBook` و`BookFacts`) · `dashboard/api/src/lib/catalogueValidation.ts` · `dashboard/api/src/routes/adminContent.ts` (مسارَا الإنشاء والتحديث) · `dashboard/api/test/publishGate.test.mjs` · `dashboard/api/test/catalogueValidation.test.mjs`.

#### التالي

`OPS-107` هو آخر ما بقي من الأودت خارج ما يحتاج قرارك أو اعتمادًا خارجيًّا، وهو يمسّ ملفاتك بسعةٍ (حذف أرشيفات ومخلَّفات) فسأقيسه وأعرض قبل أن أحذف شيئًا.

---

### الدفعة السابعة والستّون (2026-08-31) — بندٌ جديد `I18N-102`: نصٌّ عربي مُشوَّه الترميز، ومنه رسائل يراها مشغّل

الفحوص بعد الدفعة: Worker `npm test` **1436/1436** (+1) · `tsc --noEmit` نظيف · وبوابة التشويه خضراء بعد التحقّق من فشلها.

> **هذا ليس بندًا من الأودت الأصلي.** رُصد في ذيل الدفعة 66 كملاحظة، وقِيس هنا فوُجد أوسع من ملفَّين. أُدرج بمعرّف `I18N-102` لأنه عطلٌ مقيسٌ له أثر على المستخدم.

#### الكاشف، ثمّ القياس

نصٌّ عربي كُتب UTF-8 ثمّ قُرئ cp1252 وأُعيدت كتابته يصير رطانة. وبايتات العربية في UTF-8 تبدأ بـ`D8`/`D9`، فتظهر بعد التشويه محرفًا يتبعه محرفٌ في `U+0080..U+00BF` — **تتالٍ لا يقع في نصٍّ سليم**، لا عربيًّا ولا إنجليزيًّا. فالكاشف دقيق ولا يحتاج قائمة كلمات.

المقيس: **15 ملفًا**. والتفريق هو جوهر الأمر — تشويهٌ في تعليق قبحٌ يعيق القراءة، وتشويهٌ في بيانٍ **يُعرَض لطفل أو لمشغّل**:

| الملف | أسطر في بيانات |
|---|---:|
| `migrations/0088_games_voice_assets.sql` | **150** (`title_ar`) |
| `KIRO_REPORT_HISTORY.md` | 97 |
| **`src/routes/adminUsers.ts`** | **26 رسالة خطأ** |
| أربعة سكربتات + `_audit_apply.sql` | 9 |

#### الأخطر: ستٌّ وعشرون رسالة خطأ في لوحة الإدارة

`adminUsers.ts` كان يُعيد `error: '<رطانة>'` في ستٍّ وعشرين موضعًا — أي أن مشغّلًا يُمنع من عمل يقرأ حروفًا لا معنى لها بدل «لا تملك صلاحية إدارة المستخدمين». أربعة عشر نصًّا مميَّزًا، وكلها 400/403/404/409.

**وأُصلحت بالكتابة اليدوية لا بالاستعادة الآلية**، وهذا قرارٌ مقيس: التشويه **مزدوج**. جرّبتُ فكّه بأربع طرق — Latin-1، وcp1252 بجدولٍ عكسيّ، ومقطعًا مقطعًا مع جولةٍ مغلقة للإثبات — والنتيجة: **1097 محرف إبدال** في ترحيلة `0088`، و«استعادة» **37%** من مقاطع الملفات الأخرى ورفض الباقي. وترقيعُ 37% يُنتج ملفًا **نصفَ صحيح**، وهو أسوأ من مشوَّهٍ معروف.

فكُتبت النصوص يدويًّا، ومعناها مؤكَّد من شيئين معًا: الفكّ الجزئي للأصل، وسياقُ الشرط الذي يُطلق كل رسالة (`!canManage` → صلاحية، `!target` → غير موجود، `!body` → صيغة الطلب). والاستبدال **بمطابقة بصمة** لا بنسخ رطانة إلى سكربت — فلا خطأ نسخٍ ممكن. النتيجة المقيسة: **26 مُستبدَلًا، وصفر تشويه في بيانات هذا الملف**.

#### البوابة: خطُّ أساسٍ مُجمَّد، لا صفرٌ مُتعذَّر

الدَّين الباقي **لا يُصلَح آليًّا** والصحيح إعادةُ توليده من مصادره — وهو فعل مالك. فبوابةٌ تطلب صفرًا اليوم تُطفأ غدًا، وذاك عين `DEBT-102`.

فالبوابة تُجمّد المقيس: ملفٌ جديد أو عددٌ أكبر **يُفشل الجولة**. وتفشل أيضًا على **تحسّنٍ غير مُسجَّل** — لأن خطّ أساسٍ أكبر من الواقع يسمح بعودة التشويه في صمت. وقد خُفِض `adminUsers.ts` من 26 إلى صفر، فحُذف من القائمة.

وتحقّقتُ من الفشل: ملفٌ جديد فيه سطرٌ مشوَّه أوقعها، وحذفُه أعادها خضراء.

#### وإيجابيةٌ كاذبة في كاشفي أوقعتُها بنفسي

أبلغ الفحص أوّلًا عن **34 سطرًا «في بيانات»** في `front/src/types/api.ts`. وبالفحص: عنوانُ قسمٍ عربي داخل `/* --- ... --- */`، وتصنيفي كان يعدّ `*` تعليقًا ولا يعدّ `/*`. فأُضيف — وبلا ذلك كانت البوابة تحمل رقمًا لا يراه مستخدم، وهو أوّل ما يُفقد الثقة في بوابة.

ومثلُه بالعكس: `LEDGER.md` و`content_dtos.dart` فيهما سطرٌ مشوَّه **مقصود** — مثالٌ يشرح العطل. وهما في التعليقات فلا تمسّهما البوابة، ومعهما استثناءٌ ضيّق مُعلَن للملفات التي موضوعها التشويه (منها هذا الأودت) — وإلّا رصد الفحص شرحَ نفسه، وهو الفخّ الذي تكرّر هنا سبع مرّات.

#### الملفات

`tools/ci/mojibake-scan.mjs` (جديد) · `.github/workflows/ci.yml` · `dashboard/api/test/ciGates.test.mjs` (+1) · `dashboard/api/src/routes/adminUsers.ts` (26 رسالة).

#### التالي

`HUMAN-112` صار أوسع: إعادةُ توليد `0088` **وأربعة سكربتات و`_audit_apply.sql`** بترميز صحيح. وما بقي من الأودت يحتاج قرارك: `API-107` و`OPS-107` (يمسّان بياناتك وملفاتك)، و`API-103`/`API-104`/`API-108` مزايا تحتاج قرارًا أو اعتمادًا خارجيًّا.

---

### الدفعة السادسة والستّون (2026-08-31) — `DB-103`: القيد مفروضٌ فعلًا، والعطل في الفرضية

الفحوص بعد الدفعة: Worker `npm test` **1435/1435** (+7) · `tsc --noEmit` نظيف · والمِسحة الجديدة خضراء على القاعدة المحلية.

#### أربع تجارب نقضت الفرضية

البند بُني على «D1/SQLite لا يفرض `FOREIGN KEY` افتراضيًّا، والمخطط يعتمد عليها». فقستُ:

| التجربة | النتيجة |
|---|---|
| `PRAGMA foreign_keys` | **1** |
| إدراج مرجعٍ غير موجود | **مرفوض** — `SQLITE_CONSTRAINT_FOREIGNKEY` |
| `PRAGMA foreign_keys=OFF` ثمّ إدراج | **مرفوض أيضًا** — الأمر لا يُطاع |
| جدولٌ يرجع إلى جدولٍ غير موجود | **مرفوض عند الإنشاء** |
| `PRAGMA foreign_key_check` | **صفر صفٍّ يتيم** |

ويؤكّده حادثٌ في هذا الأودت نفسه: `DB-104` كان ترحيلةً **توقّفت** على انتهاك مفتاح أجنبي — أي أن الإنفاذ كان يعمل وقتَها بما يكفي لإيقاف البناء.

والمثال الذي استشهد به البند يُقرأ الآن على وجهه: `granted_by TEXT REFERENCES admin_users(id)` — المفتاح **مُعلَن**. فمرورُ `'admin-api-key'` لم يكن لغياب الإنفاذ، بل لأن العمود **قابل للعدم** والقيد لا يُفحَص على `NULL`. والتعليق في الكود الذي يشرح الإصلاح كان يحمل الفرضية الخاطئة، فصُحّح.

#### فالخطر ليس ما وصفه البند، بل ثلاثة أشياء أخرى

1. **أن يُطفأ الإنفاذ** — بتغيّرٍ في المنصّة لا بأمرٍ في SQL. فيُقاس صريحًا ولا يُفترض.
2. **صفوفٌ يتيمة سابقة** — القيد لا ينظر إلى الماضي، و`foreign_key_check` ينظر.
3. **مراجع بلا قيد أصلًا** — وهذه **ما لا يراه أي إنفاذ**: عمودٌ يحمل معرّفًا ولا `REFERENCES` عليه. والجرد: **120** عمودًا.

#### المِسحة، وثلاث مرّات صحّحتُ فيها نفسي

`tools/ops/referential-integrity.mjs`، مربوطة بوظيفة `migrations` في CI **بعد** بناء قاعدةٍ من الصفر — وموضعها مقصود: القاعدة هناك نتاج الترحيلات وحدها، فأي عطلٍ يُنسَب إلى ما يمكن إصلاحه لا إلى بيانات بيئةٍ قديمة. وتقبل `--remote` لقياس الإنتاج، وذاك فعل مالك.

وثلاثة تصحيحات في الطريق تستحق التسجيل لأن كلًّا منها كان سيُنتج رقمًا كاذبًا أو أداةً لا تُشغَّل:

- **جرد التغطية بتعبير نمطي أعطى 139 عمودًا، والصحيح 120.** حلّلتُ DDL من `sqlite_master` بحثًا عن `REFERENCES`، فوصفتُ **تسعة عشر** عمودًا لها قيدٌ بأنها بلا قيد (الصيغ متعدّدة: قيدٌ على مستوى الجدول، وأعمدةٌ متعدّدة الأسطر). فرجعتُ إلى أن **SQLite تُجيب** لا تعبيري.
- **لكن سؤال SQLite بنداءٍ لكل جدول كان ثلاثمئة نداء `wrangler` وأكثر من دقيقة** — وفحصٌ بهذا الثمن لا يُشغَّل. فصار **بيانًا لكل جدول في نداءٍ واحد**: أربعة نداءات، والجواب 120 مطابقًا للمرجع.
- **وفرع «صفوف يتيمة» لا يمكن تلفيقه.** جرّبتُ ثلاث طرق فرفضها D1 كلَّها. فبقي أن يُختبَر **الحكم** لا القاعدة: `evaluateIntegrity` دالّةٌ نقيّة مُصدَّرة، و`test/referentialIntegrity.test.mjs` يُغذّيها صفوفًا مُصطنَعة — ومنها أن `undefined` من `PRAGMA` تُقرأ **إطفاءً لا نجاحًا** (وهو نفس عطل «قراءةٌ فاشلة تُشحن صفرًا» في `ADM-106`)، وأن اسم العمود المحجوز `table` يصل مُقتبَسًا أحيانًا فلا يُعرَض «(مجهول)» في تقريرٍ غرضه أن يقول **أين** العطل.

#### وما لم أفعله: تحقّقٌ تطبيقي مكرِّر

المعيار الأول يطلب «كل مرجع حساس يُتحقَّق منه قبل الكتابة». والقيد **يتحقّق**، مفروضًا ومقيسًا. فإضافة فحصٍ تطبيقي ثانٍ إلى جانبه تُنتج مصدرين للقاعدة نفسها ينحرف أحدهما — وقد رصد هذا الأودت ذلك النمط مرارًا. فالمكتوب: القيد هو المتحقِّق، ويعلوه تحقّقٌ تطبيقي **حيث لا يبلغ القيد** — أي على القيم القابلة للعدم، كما يفعل `verifiedGrantedBy`.

#### الملفات

`dashboard/api/tools/ops/referential-integrity.mjs` (جديد) · `dashboard/api/test/referentialIntegrity.test.mjs` (جديد، 6) · `dashboard/api/test/ciGates.test.mjs` (+1) · `.github/workflows/ci.yml` · `dashboard/api/src/routes/adminTeams.ts` (تصحيح تعليق).

#### التالي

بقي من المتبقّي: `API-107` (صفحات الكتب عمود JSON) و`OPS-107` (نظافة الشجرة) — وكلاهما يمسّ بيانات أو ملفات المالك بسعةٍ تستحق قرارًا. و`API-103`/`API-104`/`API-108` مزايا غائبة تحتاج قرار مالك أو اعتمادًا خارجيًّا. وفي `تشفير المحتوي.md` وترحيلة `0088` نصوصٌ **مُشوَّهة الترميز** — لُوحظ أن ملف `adminTeams.ts` فيه تعليقٌ عربي مُشوَّه كذلك، وهو نمطٌ يستحق مسحًا مستقلًّا.

---

### الدفعة الخامسة والستّون (2026-08-31) — `DB-102`: الجدول الميت كان **مقروءًا**، وفحصٌ كنتُ أُصدّقه لا يفحص شيئًا

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **712/712** · Worker `npm test` **1428/1428** (+2) و`tsc --noEmit` نظيف · اللوحة **`tsc -b` نظيف** و`vitest` أخضر.

#### البند توقّع الأثر، والقياس وجده واقعًا

`0010_cleanup_dead_d1_tables.sql` أعلن سبعة جداول ميتة وقال إنها «تُبقى كـno-op … وسيتم إسقاطها في **0011** بعد التأكد». نحن عند **0089**. والبند توقّع: «مطوّر يقرأ الوثيقة **يكتب** استعلامًا على جدول لا يُكتب أبدًا، فيحصل على نتيجة فارغة **بلا خطأ**».

والمقيس: **قد كُتب**.

| الموضع | ما كان يُعيده |
|---|---|
| `adminCustomer.ts` قائمة العملاء | `device_count` = **صفر لكل أسرة** |
| `adminCustomer.ts` مساحة العميل | قائمة أجهزة «الإسقاط» فارغة أبدًا · وقائمة شراءات فارغة أبدًا |
| `adminAppExperience.ts` قائمة الأجهزة | `data: []` و`total: 0` — «صفر جهاز على المنصّة» |
| `adminAppExperience.ts` دعم الأسرة | قائمة أجهزة فارغة |

و**صفر كاتب** لـ`account_devices` ولـ`google_play_purchases` في `src` و`scripts` و`migrations` كلّها.

#### وتعليقان كانا يؤكّدان وجود كاتب

`adminDevices.ts` كان يقول إن `account_devices` «إسقاطٌ يُحدَّث بالحدث الذي يبعثه هذا الأمر»، و`adminAppExperience.ts` إنه «إسقاط D1 يُغذّيه الطابور» و«الأداة الصحيحة للسرد والتصفية عبر الحسابات». كلاهما غير صحيح. وصُحّحا مع الإبقاء على ما بقي صحيحًا في تحليلهما — أن قراءة إسقاطٍ لا تجيب «هل لوح هذا الأب متّصل الآن؟».

#### الجواب: عدم التوفّر مُعلَنًا، لا صفرٌ ولا فراغ

- `device_count` صار `NULL` ومعه `meta.unavailable` بسببٍ مكتوب، واللوحة تعرض «—» بتلميح. الصفر ادّعاءُ قياسٍ لم يحدث.
- قائمتا الأجهزة والشراءات في مساحة العميل صارتا `Unavailable` بنفس الشكل الذي تستعمله هذه الصفحة أصلًا لقراءة السلطة. **والأجهزة الحقيقية كانت تُقرأ من السلطة في نفس الطلب** — فالقائمة الثانية كانت زائدة قبل أن تكون خاطئة.
- قائمة أجهزة المنصّة صارت `total: null` + سبب. وشقيقُها `/devices/:id/revoke` يرفض بصراحة لهذا السبب نفسه منذ دفعاتٍ سابقة، فالقائمة كانت **تخالف شقيقها بصمتها**.
- والفراغ في الشراءات كان يبدو صادقًا بالحادث (لا شراء لأن مسار الشراء غير موصول — `API-104`)، لكن سببه «لا كاتب». والفرق يهمّ يوم يُوصَل المسار: مشغّلٌ اعتاد «لا شراءات» لا يشكّ يوم تبقى القائمة فارغة خطأً.

#### الحرس، ولماذا لا تعليق ولا إسقاط

المعيار يقبل «تُسقَط بترحيل **أو** تُوسَم بتعليق لا يمكن تفويته». الإسقاط يمحو صفوفًا قد تكون في الإنتاج من قبل انتقال الملكية، ولا أرى الإنتاج. والتعليق **يمكن تفويته** — والدليل في يدي: `0010` نفسه تعليقٌ فُوِّت تسعًا وسبعين ترحيلة، وأربعة استعلامات كُتبت بعده.

فـ`test/deadTables.test.mjs`: يفشل على أي `FROM|JOIN|INTO|UPDATE` لأحد السبعة خارج `src/do/`. والاستثناء ليس ثقبًا: `playback_leases` و`used_refresh_tokens` هناك **مِلكٌ شرعي** لمخطَّط `FamilyState` الداخلي (27 و5 إشارات). وقائمة السبعة **مُشتقّة** من سطر الإعلان في `0010` ومُقارَنة به، فلا تنحرف عنه. والتعليقات تُقشَّر قبل الفحص — وإلّا رصد الحرس ملفات هذه الدفعة نفسها، وهي تشرح الأسماء بالضرورة.

وتحقّقتُ أنه يفشل: بإضافة استعلامٍ واحد في مسارٍ إداري.

#### واختبارٌ كان يُثبّت العطل

`routeGuards.test.mjs` كان يشترط `SELECT id, display_name, platform, status FROM account_devices` **حرفيًّا**. غرضه سليم (منع `SELECT *` وتسريب أعمدة)، لكنه بذلك **جعل قراءة الجدول الميت شرطًا للنجاح**. صار التأكيد على **غيابه**، والخاصّية الأمنية محفوظة في تأكيدات الأعمدة الباقية و`doesNotMatch(SELECT *)`.

#### وتصحيحٌ يخصّني: فحصٌ نقلتُه في دفعاتٍ كثيرة وهو لا يفحص شيئًا

كنتُ أُبلّغ «`npx tsc --noEmit` نظيف (الطرفان)». وبعد أن كتبتُ خطأً نوعيًّا حقيقيًّا في اللوحة (`.length` على قسمٍ صار كائنًا) ومرّ الفحص، قِستُ: أدخلتُ خطأً متعمَّدًا فأخرج **صفر رسائل وخروج 0**.

السبب: `dashboard/front/tsconfig.json` ملفُّ حلٍّ (`"files": []` + `references`)، فـ`tsc --noEmit` بلا `-p` يفحص **صفر ملف**. والصحيح `tsc -b` — وقد أوقع الأخطاء الثلاثة فورًا.

و**CI بريء**: يشغّل `npx tsc --noEmit -p tsconfig.app.json`، وهو صحيح. الخطأ كان في أمري المحلّي وفي نقلي إياه كدليل. وأُضيف `npm run typecheck` = `tsc -b` حتى لا يعتمد الصحّ على تذكّر عَلَم. والمقيس أيضًا: فحص Worker **حقيقي** (أدخلتُ خطأً متعمَّدًا فأوقعه).

#### الملفات

`dashboard/api/src/routes/adminCustomer.ts` · `.../adminAppExperience.ts` · `.../adminDevices.ts` (تعليق) · `dashboard/api/test/deadTables.test.mjs` (جديد، 2) · `dashboard/api/test/routeGuards.test.mjs` · `dashboard/front/src/types/api.ts` · `.../pages/CustomersPage.tsx` · `.../pages/CustomerDetailPage.tsx` · `dashboard/front/package.json` · `تشفير المحتوي.md`.

#### التالي

`DB-103` (المفاتيح الأجنبية غير مفروضة) — وهو التالي في الترتيب بين المتبقّي القابل للتنفيذ.

---

### الدفعة الرابعة والستّون (2026-08-31) — مصالحة انحدارَين خارجيَّين: «اعمل الصح»

الفحوص بعد الدفعة — **كلّها خضراء**: `flutter analyze --fatal-warnings` صفر · `flutter test` **712/712** · Worker `npm test` **1426/1426** · `tsc --noEmit` نظيف (الطرفان) · اللوحة `vitest` **354/354** (27 ملفًا) · `docs-classified` و`assets-declared` و`feature-matrix` بـ`--check` صفر مشاكل.

#### الأوّل: صفوف الرئيسية — أُعيد ما حُذف بقرار، فأُزيل ثانيةً

تعديلٌ خارج الجلسة أعاد إلى `home_feed.dart` ثلاث حالات لأنواع حذفها `APP-104`. وقرأتُ ما أعاده قبل أن أحكم، فوجدتُه **حرفيًّا** ما وُصف في البند: ثلاث حالات تُرجِع `SizedBox.shrink()` وتعليقاتها تُقرّ بالعطل — «Disabled: hardcoded 0.34 progress with no endpoint» و«Hidden until trustworthy analytics transport exists» و«Hidden until rule-based recommendation engine». أي نوعٌ في التعداد، وصفٌّ تعرضه لوحة الإدارة، **ولا شيء يظهر للطفل**.

فالصحّ إزالتها لا إعادة قيم التعداد: إعادتها تعيد الوعد الفارغ. وأُزيل معها أربعة أصناف ميتة كانت تُغذّيها (`_LearningJourneyCard` و`_MostWatchedRail` و`_ExploreMajarraSection` ومعه `_ExploreDest`/`_ExploreCard`) — **473 سطرًا** بلا منادٍ، ثلاثة منها كانت مُسكَتة بـ`// ignore: unused_element`. وحرس `lib_hygiene_test.dart` هو الذي أوقع الثالث: إسكاتُ تحذير كودٍ ميت ممنوع خارج المولَّد.

#### وعطلٌ ثالث وجدته في الطريق: `قريباً` كانت تكذب

`BlockType.comingSoon` كان له **تطبيقان** في نفس الـ`switch`، والثاني غير قابل للوصول (`unreachable_switch_case` — وهو ما رصده `DEBT-101`). وكلاهما يفعل الشيء نفسه: الأوّل `series.reversed.take(n)` والثاني `series.take(5)` — أي **أحدث السلاسل المتاحة للمشاهدة** تحت عنوان «قريباً».

وهذا هو كذب `CNT-106` بعينه على الصفّ لا على البطاقة: طفلٌ يهمل ما هو متاح لأنه مكتوبٌ عليه «قريبًا»، أو ينقر ما يظنّه غير متاح. ولا حقل «قادم» في `SeriesItem` — لكن `episodesCount == 0` إشارةٌ **صادقة وموجودة**: مجلَّدٌ أُعلن ولم يُملأ. فصار الصفّ يعرض ذلك، **ولا يُعرض إطلاقًا** عند غياب ما هو قادم — كما تفعل بقيّة الصفوف في الملف. صفٌّ فارغ بعنوانٍ يَعِد أسوأ من غيابه.

#### الثاني: ترحيلتان تتصادمان في الرقم، ومعهما نصوص معطوبة

أُضيفت `0082_games_voice_assets.sql` و`0083_link_games_voice_manifest.sql` خارج الجلسة، **فتصادمتا** مع `0082_self_produced_rights.sql` و`0083_offline_licensing.sql` القائمتين. فصارت الأرقام المكرَّرة خمسة بدل الثلاثة الموثَّقة، وحرس `DB-101` أوقعها.

وأُعيد ترقيمهما إلى `0088`/`0089`، وهذا **لا ينقض** قاعدة السجلّ «لا يُعاد ترقيم ترحيل مُطبَّق»: قِستُ قبل أن أفعل — `d1_migrations` المحلي آخر ما فيه `0087`، ولم تكونا في السجلّ. أي أنهما غير مُطبَّقتين، والقاعدة لا تبلغهما. والترتيب النسبي محفوظ لأن `0089` يربط ما يُدرجه `0088`.

**وما لم أفعله، ولماذا:** النصوص العربية في `0088` (`title_ar` لـ150 صفّ) **مُشوَّهة الترميز**. جرّبتُ فكّها بفرضية «UTF-8 مقروءًا كـLatin-1» فأنتج **1097 محرف إبدال** — أي أن السلسلة مزدوجة. والمولِّد `tools/gen_0082.py` يكتب بـ`utf-8` صحيحًا، فالعطل **في مصدره**. فلم أُصلح المحتوى بالتخمين ولم أطبّقه: سُجّل التحفّظ في `LEDGER.md` صريحًا، والصحيح إعادة توليد الملف بعد إصلاح مصدره.

ولم أُسجّلهما «مُطبَّقتين» على شيء: السجلّ يزعم ما طُبِّق فعلًا، وزعمي عن بيئةٍ لا أراها هو عين ما بُني الحرس ليمنعه.

#### الدرس المتكرّر، وهذه ثالث مرّة في جلسةٍ واحدة

`content_dtos.dart` (الدفعة 62) و`home_feed.dart` (هذه) — كتابةُ ملفٍ كاملًا من خارج المسار تُسقط إصلاحاته السابقة **بلا إشعار**، والحرس هو ما يجدها. وفي الحالتين لم أُرجِع الملف إلى نسخةٍ سابقة: أعدتُ **الخاصّية** المفقودة جراحيًّا وأبقيتُ الإضافات الجديدة. والفرق ليس أدبًا: إرجاعُ الملف يمحو عمل ساعة، وإعادةُ الخاصّية تحفظ الاثنين.

#### الملفات

`app_main/lib/features/home/presentation/widgets/home_feed.dart` (ثلاث حالات محذوفة · أربعة أصناف ميتة · `comingSoon` صار مقيسًا) · `dashboard/api/migrations/0088_games_voice_assets.sql` و`0089_link_games_voice_manifest.sql` (إعادة ترقيم) · `dashboard/api/migrations/LEDGER.md`.

#### التالي

القائمة المرتَّبة (§5 ثم §6) انتهت. ما بقي **لم يُلمس بقرار**: `ENC-002/013/015` و`API-103/104/107/108` و`DB-102/103` و`OPS-107` و`APP-101`. وأفعال المالك في §12 — ومنها الآن **إعادة توليد `0088`** بترميز صحيح قبل تطبيقه.

---

### الدفعة الثالثة والستّون (2026-08-31) — `DOCS-101`: تصنيفٌ في الرأس، لا نقلٌ يكسر ثلاثمئة رابط

الفحوص الخاصّة بهذه الدفعة: `docs-classified --check` أخضر (بعد التحقّق من فشله في حالتيه) · `assets-declared --check` أخضر · `feature-matrix --check` أخضر · `ciGates` **13/13**.

> **ولا يمكنني أن أزعم خضرة المجموعتين الكبيرتين**: `flutter analyze` و`flutter test` و`npm test` **حمراء الآن بتغييراتٍ من خارج هذه الجلسة** وصلت أثناء الدفعة. التفصيل في آخر هذه الفقرة، وهو غير ناتج عن هذه الدفعة (لم تلمس ملف كودٍ واحدًا).

#### الأرقام صُحّحت، والتصنيف صار موجودًا

المقيس: **409** ملف `.md` (لا 405) · **4022** مربّعًا غير محدَّد (لا 4026) في **174** ملفًا · و**158** محدَّدًا (لا 112) · و**36** في الجذر · و`docs/` فيه **275**.

`DOCS_INDEX.md`: ملفات الجذر الستّة والثلاثون مُصنَّفة — **16 مصدر حقيقة** و**20 لقطة منقضية** — ولكلٍّ سببٌ وما يُقرأ بدلًا منه. ووسمٌ في **رأس** كل ملف تاريخي، حتى يراه من يفتح الملف مباشرةً بلا المرور من الفهرس.

وفي `النواقص_المتبقية.md` تحذيرٌ محدَّد لأنها ما استشهد به البند: لا تزال تقول «الكود لا يُبنى — P0» وتقدّر تطبيق Flutter بـ«20-25%». وهذا **قِسته** قبل كتابته لأن الملف عُدِّل في 2026-08-30، فربّما صُحّح: لم يُصحَّح.

وحُذف `KIRO_IDE_LAST.md` — صفر بايت، وغير متعقَّب.

#### والنقل إلى `_archive/` مرفوضٌ بقياس

البند طلبه صراحةً. فقستُ المراجع الداخلة إلى خمسة عشر من هذه الملفات: **304 مرجعًا** من ملفاتٍ أخرى في المستودع (`KIRO_LAST_REPORT.md` وحده **48**، و`FLUTTER_APP_STATUS.md` **37**، و`النواقص_المتبقية.md` **32**).

فالنقل يستبدل «توثيقًا مُضلِّلًا» بـ«304 روابط مكسورة» — تبادلٌ لا مكسب فيه. والأثر الذي يذكره البند (قارئٌ يصدّق لقطةً منقضية) يحلّه الوسم في الرأس بلا كسر شيء. والنقل مع إعادة كتابة كل مرجع عملٌ مستقلّ يستحق قرارًا صريحًا لا تمريرًا داخل دفعة.

#### والـ4022 مربّعًا لم تُلمس، بقصد

أكثرها قوائم عمل **داخل** وثائق صارت الآن تاريخية موسومة — أي أن سياقها صار معلنًا. وشطبها بالجملة يزعم إنجازًا لم يحدث، وحذفها يمحو سجلّ ما كان يُخطَّط له. والعدد نفسه ليس عطلًا: العطل كان أن يُقرأ كقائمةِ عملٍ حاضرة، وذلك ما زال الوسم يمنعه.

#### الحرس

`tools/ci/docs-classified.mjs`، مربوطٌ بوظيفة `feature-matrix` في CI ومحروسٌ ربطُه في `ciGates.test.mjs`. يفشل على: ملف جذرٍ بلا تصنيف · تصنيفٍ بلا ملف · ملفٍ تاريخيّ بلا وسم في **أوّل** 400 محرف منه.

و«الوسم لا النثر» مقصود: الفحص على `<!-- doc-status: historical -->` لا على عبارةٍ عربية. حرسٌ على النثر يرصد الملف الذي **يشرح** التصنيف — وهو الفخّ الذي وقعتُ فيه في الدفعة السابقة نفسها. وتحقّقتُ من الفشل في الحالتين: وثيقةٌ جديدة بلا تصنيف، ووسمٌ محذوف من ملفٍ مُصنَّف.

#### تغييراتٌ من خارج هذه الجلسة أوقفت المجموعتين الكبيرتين

هذه الدفعة لم تلمس ملف كودٍ واحدًا (وثائق + `ci.yml` + اختبار الربط). ومع ذلك:

1. **`app_main/lib/features/home/presentation/widgets/home_feed.dart`** كُتب في **20:07** من خارج الجلسة، وأعاد الإشارة إلى `BlockType.learningJourney` و`mostWatched` و`becauseYouWatched` — وهي **ثلاثة أنواع حُذفت بقرارٍ موثَّق** في `APP-104` (كانت مبنيّة بالكامل ثم مُعطَّلة بثابت `=> false` لأن مصدرها لا وجود له). فالتحليل يفشل بثلاثة **أخطاء ترجمة**، والحلّ اختيارٌ بين نقض قرار `APP-104` أو نقض عمل الساعة 20:07 — وليس قراري.
2. **ترحيلتان جديدتان** `0082_games_voice_assets.sql` (19:29) و`0083_link_games_voice_manifest.sql` (19:37) أُضيفتا من خارج الجلسة **بلا تسجيلهما في `LEDGER.md`** (آخر لمسة له 08-28). وحرس السجلّ المبنيّ في الدفعة 26 (`DB-101`) يفشل — أي أنه يعمل كما وُضع له. ولم أُسجّلهما نيابةً: السجلّ يزعم ما طُبِّق على الإنتاج، وزعمُه عن ترحيلةٍ لا أعرف حالتها هو عين ما بُني الحرس ليمنعه.

الاثنان مسجَّلان هنا كحالة مستودعٍ لا كبنود أودت، لأن مصدرهما عملٌ جارٍ لا عيبٌ في التصميم.

#### الملفات

`DOCS_INDEX.md` (جديد) · `tools/ci/docs-classified.mjs` (جديد) · `.github/workflows/ci.yml` · `dashboard/api/test/ciGates.test.mjs` · ووسمٌ في رأس **20** ملفًا تاريخيًّا · وحُذف `KIRO_IDE_LAST.md`.

#### التالي

انتهت القائمة المرتَّبة (§5 ثم §6). ما بقي **لم يُلمس بقرار**: `ENC-002/013/015` (تنتظر منظومة الترخيص XL) · `API-103/104/107/108` · `DB-102/103` · `OPS-107` · `APP-101` (XL بلا قرار مالك). وأفعال المالك المعلَّقة مجموعةٌ في §12.

---

### الدفعة الثانية والستّون (2026-08-31) — `DEBT-102`: إسكاتٌ يحمي لا شيء، وانحدارٌ من خارج الأودت

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر **بالقواعد الأربع مُعادةً** · `flutter test` **712/712** (+2) · ولم تُلمس ملفات الخادم ولا اللوحة.

#### الرقم لم يكن قائمًا

التعليق كان يقول: «لا تُفشِل CI على لينت الأسلوب حتى تُصلَح الـ58 info الحالية (متابعة C3)». فأعدتُ تفعيل الأربع الأسلوبية وقستُ:

| القاعدة | المشكلات عند إعادتها |
|---|---:|
| `use_key_in_widget_constructors` | **2** (ملفٌ واحد) |
| `prefer_const_constructors` | صفر |
| `prefer_const_declarations` | صفر |
| `avoid_print` | صفر |

الدين سُدِّد ضمنًا في دفعاتٍ سابقة، وبقي الإسكات وحده **يحمي لا شيء**. وهذا هو خطر التأجيل بلا تاريخ: الثمن يصير مجهولًا، فلا أحد يجرؤ على إعادة القاعدة، فتبقى مُطفأة سنةً على عملٍ يستغرق دقائق.

**والستّ ليست ستًّا**: اثنتان من المُسكَتات ليستا دينًا أسلوبيًّا أصلًا — `invalid_annotation_target` إنذارٌ كاذب على وسومٍ يولّدها `freezed`/`json_serializable` في كودٍ لا نملك شكله، و`dangling_library_doc_comments` تخالف أسلوب رؤوس الملفات المتَّبع في مئات الملفات. فخلطُهما مع الأربع في تعليقٍ واحد يعتذر عن «دَين» هو ما جعل الكلّ يبدو مؤجَّلًا.

#### وطبعٌ بلا شرط في مسار إنتاج

`avoid_print` أعطى صفرًا لأن الثلاث `print` في `lib/` مُسكَتة **سطرًا بسطر** بـ`// ignore: avoid_print` — أي أن إسكات القاعدة عامًّا كان يخفي أن هناك إسكاتات خاصّة أيضًا. واحدةٌ منها هي العطل:

- في `crash_reporter.dart` طبعتان داخل `kDebugMode`: مشروعتان، وصارتا `debugPrint`/`debugPrintStack` — قناة Flutter المعتمدة، فزالت الحاجة إلى الإسكات.
- في `creative_catalogue_provider.dart` طبعةٌ **بلا أي شرط**: تعمل في الإصدار على جهاز الطفل. وتوثيقها كان يقول «Observe via AnalyticsService» — **وعدٌ لم يُنفَّذ**، لا شيء يرسل هذا إلى التحليلات. صارت محروسة بـ`kDebugMode`، والعدّاد `fallbackActivations` هو المرصد الحقيقي، والنصّ يقول ما هو صحيح.

فلم يبقَ إسكاتٌ سطريّ لـ`avoid_print` في التطبيق.

#### الحرس، ومصيدةٌ وقعتُ فيها للمرّة الثانية في جلسةٍ واحدة

`lint_suppressions_test.dart`: مجموعة المُسكَتات **مُعلَنة** في الاختبار (فنموّها فعلٌ يُرى في المراجعة)، وكل مدخل يجب أن **يسبقه تعليق**. ولا يفحص جودة السبب — لا يمكن — لكنه يجعل الإسكات الصامت مستحيلًا.

وكتبتُ فيه تأكيدًا ثالثًا: «نصّ التعليق المُعتذِر لم يعد موجودًا». فسقط فورًا — لأن الملف **يقتبس العبارة القديمة ليوثّق ما كان**. حرسٌ يمنع ذكر العطل يمنع شرحه. أسقطتُ التأكيد وتركتُ مكانه شرحًا: هذا هو الفخّ الذي رصده هذا الأودت سبع مرّات، ووقعتُ فيه هنا مرّة أخرى بعد أن وقعتُ فيه في الدفعة 54.

ولم أضف حزمة `yaml` لأجل الحرس: قراءة الأسطر تكفي، والتعليقات — وهي نصف المفحوص — يُسقطها أي مُحلِّل.

#### انحدارٌ من خارج هذا الأودت، وُجد بالفحوص

`flutter test` بعد تغييراتي أخرج **ثلاثة إخفاقات لا علاقة لها بها**. والمقيس: `content_dtos.dart` عُدِّل خارج هذه الجلسة (طابعه الزمني 18:47، وتغييراتي 19:03–19:04)، بنسخةٍ أوسع من المُلتزَم بـ258 سطرًا — **وفقدت إصلاحين موثَّقين**:

1. **الدفعة 30 (`APP-103`)**: `_cdnBase = 'https://cdn.majarra.app'` عاد مُعلَنًا حرفيًّا في الملف بدل `AppConfig.assetBaseUrl`. فتغييرُ النطاق يعني مواضع متعدّدة من جديد.
2. **الدفعة 48 (`CNT-106`)**: `PlanetDto` لم يعد يحلّل `published_series` و`published_openable`، فعاد `publishedOpenable` إلى `null` دائمًا — أي أن «قريبًا» لا تظهر على كوكبٍ فارغ مقيس.

أُعيد الاثنان **جراحيًّا** مع الإبقاء على الإضافات الجديدة كلّها؛ لم أُرجِع الملف إلى نسخةٍ سابقة. وموضع التسجيل: الدفعات 60 و61 لم تلمس Flutter، فلم يُشغَّل `flutter test` بينها وبين الدفعة 59 — ولذلك مرّ الانحدار أيامًا. الدرس هو دقّة الفحوص لا سعتها: مجموعةٌ خضراء **قبل** الدفعة هي ما يميّز عطلي من عطلٍ وجدته.

#### الملفات

`app_main/analysis_options.yaml` · `lib/core/errors/crash_reporter.dart` · `lib/features/games/application/creative_catalogue_provider.dart` · `lib/features/games/presentation/pages/creative_studio_page.dart` (`super.key` في مضيفَين) · `test/lint_suppressions_test.dart` (جديد، 2) · و**إصلاح الانحدار**: `lib/features/home/data/content_dtos.dart`.

#### التالي

`DOCS-101` بالترتيب من §6 — وهو آخر بند في القائمة المرتَّبة.

---

### الدفعة الحادية والستّون (2026-08-29) — `PRIV-103`: التبعية كانت منحلّة، والتعليق لم يعلم

الفحوص بعد الدفعة: Worker `npm test` **1425/1425** (+5) · `npx tsc --noEmit` نظيف · ولم تُلمس ملفات Flutter ولا اللوحة.

#### البند كان محجوبًا على شيءٍ أُنجز قبل سبعٍ وثلاثين دفعة

`ownsChild` كان يستدعي `/state` — حالة الأسرة **كاملة** — لكل حدث تحليلات، وتعليقه يشرح السبب: الإسقاط في D1 بلا كاتب. والسبب كان صحيحًا **وقتَ كتابته**، لكن `API-105` أُنجز في الدفعة 24: الجدول العاطل هو `children_profiles`، أمّا `child_projection` فيكتبه مستهلك الطابور على الإنشاء والتحديث والأرشفة، ويحمل `parent_id` و`status`.

والأدلّ من ذلك: **أربعة مسارات تتحقّق من الملكية به بالفعل** — `recommendations.ts` و`childSettings.ts` و`adminMastery.ts` و`adminAnalytics.ts` — وهي تُقدّم **بيانات طفل حقيقية**، أي أشدّ حساسية من كتابة صفّ قياس. فلم يكن هذا المسار يحمل معيارًا أعلى، بل تعليقًا لم يُحدَّث. وهذه هي الحالة التاسعة من «توثيقٍ يخالف الكود» في هذا الأودت، بصيغتها الأخرى: توثيقٌ كان صادقًا ثم تخلّف عن إصلاحٍ في مكان آخر.

#### التصميم: الإسقاط أوّلًا، والسلطة عند الغياب وحده

قراءة D1 واحدة على المفتاح الأوّليّ، مقيَّدة بـ`parent_id` و`status='active'`. وعند غياب الصفّ **تُسأل السلطة**.

ثلاثة أسباب لهذا الشكل بالذات:

- **لا يمكن أن يكون أغلى من قبل.** كان **كل** حدث يجرّ `/state`؛ صار النداء في حالة الغياب فقط. حتى معرّفٌ مُختلَق يقصد استدعاء السلطة لا يزيد على الحالة السابقة، وهو محدودٌ بحصّة المسار نفسها (240 حدثًا/دقيقة) — الحصّة التي كانت تحمي النداء الكامل في كل حدث أصلًا.
- **الاحتياطي مقصود لا احتياط.** الإسقاط قد يتأخّر خلف الطابور، وطفلٌ أُنشئ قبل لحظة قد لا يكون صفّه قد كُتب — وأوّلُ أحداثه هي بالضبط ما يقيس رحلة التهيئة. فالاقتصار على الإسقاط كان سيفقد أنفعَ القياسات بصمت.
- **ولا ذاكرة مؤقتة.** قرارُ تخويلٍ من ذاكرةٍ قديمة يقبل طفلًا نُقل أو أُرشِف. الرخص هنا يُدفَع من صحّة التخويل، وهذا ثمن لا يُدفَع.

#### الحرس، ومصيدةٌ فيه لم تكن بديهية

الشكوى **كلفة** لا صحّة، فالقياس على الكلفة: هل استُدعيت `/state`. لكن `authenticateParent` نفسه ينادي `/sessions/resolve` للتحقّق من الجلسة والحقبة — فعدُّ «كل نداءات الكائن الدائم» كان سيُثبت شيئًا غير المقصود ويفشل دائمًا. العدّاد يفرّق بالمسار.

وتأكيدٌ أضفته بعد أن رأيتُ ثغرته: «صفٌّ لأبٍ آخر يُرفض» **يمرّ في الحالتين** — لأن الرفض يأتيه من السلطة لا من الاستعلام، فحتى لو سقط `parent_id` من الشرط لبقي أخضر. فصار التأكيد على **الاستعلام نفسه**: شكلُه يحمل `parent_id = ?` و`status = 'active'`، ومعاملاته هما معرّف الطفل ومعرّف الأب **المُوثَّق**. وتحقّقتُ أنه يفشل: بحذف الشرطين من الاستعلام سقط تأكيد.

#### الملفات

`dashboard/api/src/routes/analyticsIngest.ts` (`ownsChild`) · `dashboard/api/test/analyticsIngest.test.mjs` (+5).

#### التالي

`DEBT-102` بالترتيب من §6.

---

### الدفعة الستّون (2026-08-29) — `PRIV-101`: نقلُ الرقم إلى الإعداد، لا اختيارُ رقم

الفحوص بعد الدفعة: Worker `npm test` **1420/1420** (+6) · `npx tsc --noEmit` نظيف · ولم تُلمس ملفات Flutter ولا اللوحة.

#### ما فعلتُه وما امتنعتُ عنه

البند `HUMAN-REVIEW`: 180 يومًا رقمٌ هندسي ينتظر مراجعة خصوصية طفل لم تحدث. والتعليق فوق الثابت كان يقول ذلك بنفسه، ويصف الحلّ: «ينبغي أن ينتقل إلى الإعداد إن قرّرت المراجعة رقمًا آخر».

فُعِل **النقل** لا الاختيار: `ANALYTICS_RETENTION_DAYS` في `Env`، والافتراض **180 كما هو بحرفه**. تغييرُ الرقم هو القرار القانوني عينه (COPPA / GDPR-K)، وأي رقمٍ أختاره كان سيُنتج نفس العطل الذي يرصده البند — سياسةٌ مصدرها مهندس. النقلُ يجعل قرار المراجعة **قابلًا للتنفيذ بلا نشر كود**، ولا ينفّذه.

#### الإخفاق المُصمَّم: إعدادٌ مشوّه لا يُعطّل الحذف

قيمةٌ غير صالحة (`abc`, `0`, `-5`, `3651`, `12.5`) تعود إلى الافتراض وتُسجَّل بمفتاح `cleanup_retention_invalid`. والاتجاه مقصود: لو عطّلتُ الحذف على خطأ إعداد، لحُفظت بيانات الأطفال **إلى الأبد** بسبب حرفٍ مطبعيّ — وهو أسوأ إخفاق ممكن في هذا الموضع بالذات. والحدّان 1 و3650 ليسا سياسة بل حرسٌ على قيمةٍ مشوّهة: صفرٌ يحذف كل شيء في كل تشغيل، وعشرُ سنوات تُبطل وجود النافذة.

والاختبار على **السلوك**: يشغّل المهمّة المجدولة ويتأكّد أن المُعامل المُمرَّر إلى SQLite هو `'-45 days'` عند إعداد 45 — لا أن الكود يذكر اسم المتغيّر.

#### المعيار الثاني: جردٌ مقيس، لا سياسةٌ أكتبها

المعيار يطلب «سياسة خصوصية أطفال مكتوبة في المستودع تطابق ما ينفّذه الكود». صياغةُ سياسةٍ قانونية ليست عملي، لكن **الجهل بما ينفّذه الكود** هو ما يمنع المراجعة من الحدوث أصلًا. فكُتب `docs/DATA_RETENTION.md`: جردٌ مقيسٌ من الكود بعنوانٍ يقول ما هو وما ليس هو — ما يُحذف تلقائيًّا، وما يُحذف بانتهاء الصلاحية، وما يُحذف بطلب المستخدم، وما يبقى على الجهاز، **وما لا نافذة له**.

وتحقّقتُ من صفحة الخصوصية في التطبيق قبل ذلك، متوقّعًا تناقضًا: لم يكن. تقول عن نفسها صراحةً إنها ملخّص لما ينفّذه التطبيق «وليس نصًا قانونيًا أو شروط استخدام معتمدة». صدقٌ يستحق التسجيل في أودتٍ سجّل ثماني حالات من «توثيقٍ يخالف الكود».

#### ووُجد جدولٌ بلا نافذة

`notifications` يحمل `parent_id` و`child_id` وعنوانًا ونصًّا وارتباطًا عميقًا — و**لا شيء في الكود يحذفه بمرور الوقت**. لا مهمّة مجدولة تمسّه؛ يُحذف فقط بحذف الأب أو الطفل عبر `ON DELETE CASCADE`. فلحسابٍ نشط تتراكم صفوفه بلا حدّ. ومعه `audit_events` وصفوف الرصد بلا نافذة كذلك (وأولها سجلّ إداري حذفُه يتعارض مع غرضه، فالسؤال هو الحدّ الأعلى).

ولم أقترح رقمًا لأيٍّ منها. هذا هو القرار المطلوب من المراجعة، وقد سُجّلت الأربعة في §4 من الجرد كأسئلةٍ صريحة تُغلق البند حين تُجاب.

#### الملفات

`dashboard/api/src/scheduled/cleanup.ts` (`analyticsRetentionDays`) · `dashboard/api/src/lib/db.ts` (`Env`) · `dashboard/api/.env.example` · `docs/DATA_RETENTION.md` (جديد) · `dashboard/api/test/retentionConfig.test.mjs` (جديد، 6).

#### التالي

`PRIV-103` بالترتيب من §6.

---

### الدفعة التاسعة والخمسون (2026-08-29) — `PERF-102`: الأداة كانت مبنيّة، وسبعةٌ من واحدٍ وعشرين يستعملونها

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **710/710** (+5) · ولم تُلمس ملفات خارج `app_main`.

#### العدّ صُحّح، والعطل انتقل موضعه

المقيس اليوم: `Image.asset` **53** (لا 56) · `Image.network` **12** و`CachedNetworkImage` **صفر** (لا 13 مجموعًا) · `cacheWidth` **3** (لا 5) · `cacheHeight` **صفر** · `ResizeImage` **صفر**.

والأهمّ: `CinematicImage` **يقبل `decodeWidth`** ويحوّله إلى `cacheWidth` بالكثافة الحقيقية، منذ ما قبل هذه الدفعة. والمقيس: **21 موضع نداء، سبعة تُمرّرها**. فالعطل ليس «لا توجد أداة» بل «أداةٌ لا تُستعمل» — وهو نمطٌ تكرّر في هذا الأودت حتى صار متوقَّعًا.

#### الخلط الذي يجعل هذا صامتًا

`width`/`height` على `Image` تحدّدان **التخطيط** ولا تمسّان فكّ الترميز. فصورة 768×1376 مُعطاة `width: 72` تُفكّ بكامل أبعادها، والشاشة تبدو صحيحة تمامًا. لا خطأ، ولا تحذير، ولا شيء في التحليل الساكن. الأثر يظهر على جهاز طفلٍ منخفض الذاكرة بعد التمرير: تقطيعٌ ثم إغلاقٌ من النظام.

#### الأثر بالميغابايت لا بالعدّ

| السطح | الأصول | لو فُكّت بكاملها | الحوض | بعد السقف |
|---|---|---:|---|---:|
| أفاتارات الشخصيات | 50 صورة حتى 768×1376 | **200 MB** | خلايا 72–78 | 216 بكسلًا |
| رموز الكواكب | 10 صور 768×768 | **22.5 MB** | دوائر 58–72 | 174 بكسلًا |
| شبكات التلوين و«ارسم مثلي» | حتى 50 صورة 1024×1024 | **200 MB** | مصغَّرات | حسب الحوض |

شبكةُ اختيار الأفاتار وحدها كانت تعرض خمسين صورةً بأربعة ميغابايتات لكلٍّ منها في خلايا 72 بكسلًا.

#### القاعدة في موضع واحد، وأربعة أسطح مشتركة

`decodeCapFor(context, logicalWidth)` في `core/widgets/decode_cap.dart`، مطبَّقًا في `PlanetSymbol` و`ChildAvatarView` و`DrawingAsset` و`smartImage`. أربعةُ أسطح تغطّي مئات البطاقات، بدل تعديل 53 موضع نداء واحدًا واحدًا. وثلاثة أحواض صغيرة معلومة العرض أُعطيت `decodeWidth` صريحًا (120، 120، 84).

وثلاث ملاحظات في التصميم:

- **البكسل الفيزيائي لا المنطقي**: خليّة 72 على شاشة 3× تحتاج 216. تمريرُ 72 كان سيُنتج صورةً ضبابية — أي «تحسين أداء» يُدفَع من جودة الصورة بلا أن يُلاحَظ.
- **`null` لا صفر عند الحوض المجهول**: `smartImage` يُنادى أحيانًا بعرضٍ `infinity` (خلفية ممتدّة). سقفٌ مُخترَع هناك ضبابٌ في مكانٍ لم يُقَس، فالسقف يعود `null` صراحةً ومُختبَرًا.
- **`Builder` في `smartImage`**: الدالّة بلا `BuildContext`، وتغليفُها أرخص من تمرير سياقٍ إلى كل منادٍ. وليس تزيينًا: بلا سياقٍ لا تُعرف الكثافة، ومع كثافةٍ مفترضة يعود عطلُ الضباب.

#### الحرس على ما يفكّ الترميز، لا على النصّ

`cacheWidth` غير صفريّة تجعل Flutter يلفّ المزوّد في `ResizeImage`. فالاختبار يمشي في الشجرة المبنيّة ويقرأ `provider is ResizeImage ? provider.width : null` لكل `Image`. مسحُ المصدر كان سيمرّ على `cacheWidth: null` وعلى تعليقٍ يذكر الكلمة — وهو الفخّ الذي تكرّر في هذا الأودت ستّ مرّات.

وتحقّقتُ أن الحرس يفشل: بحذف السقف من ملف الأفاتار سقط تأكيد.

#### الملفات

`lib/core/widgets/decode_cap.dart` (جديد) · `lib/core/widgets/cinematic_image.dart` (`PlanetSymbol`) · `lib/features/child/presentation/widgets/child_avatars.dart` · `lib/features/games/presentation/widgets/drawing_asset.dart` · `lib/features/games/presentation/pages/studio_v2/image_slot.dart` · `series_details_page.dart` (موضعان) · `downloads_page.dart` · `test/decode_cap_test.dart` (جديد، 5).

#### ما بقي مقيسًا

أربعة عشر موضع `CinematicImage` بلا `decodeWidth`، وأكثرها أحواضٌ كبيرة (هيرو، مشغّل، ملء الشاشة) لا يفيد فيها السقف ويكلّف تحجيمًا — كما يقول تعليقٌ قائم في `studio_home_widgets.dart` عن حوضٍ نسبته 1:1. لم تُطارَد بالجملة لأن سقفًا على حوضٍ كبير عملٌ بلا مقابل.

#### التالي

`PRIV-101` (مدة الاحتفاظ تنتظر مراجعة خصوصية) بالترتيب من §6 — وهو `HUMAN-REVIEW`، فالمتوقَّع أن يكون نقلَ الرقم إلى الإعداد وتوثيقَ انتظار المراجعة لا اختيارَ رقمٍ.

---

### الدفعة الثامنة والخمسون (2026-08-29) — `PERF-101`: الرقم كان يقيس القرص، والعطل كان في اتجاهٍ آخر

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **705/705** · Worker `npm test` **1414/1414** (+1) · وحرسا الحجم الجديدان أخضران بعد التحقّق من فشلهما.

#### الأرقام صُحّحت في الاتجاهين

الـ358 MB صحيحة على القرص، **وليست ما يُنزِّله الأهل**. إعلان المجلد في Flutter غير تعاودي، والسياسة في `pubspec.yaml` تعتمد على ذلك نصًّا. فبقياس التوسيع الصحيح:

| | ملفات | حجم |
|---|---:|---:|
| مُبندَل فعلًا | 389 | **116.12 MB** |
| على القرص غير مُعلَن | 630 | **242.06 MB** |
| **متعقَّب في git** | **104** | **8.85 MB** |

و«المستودع نفسه ثقيل بلا سبب» **غير صحيح**: `.gitignore` ينفّذ قائمة سماح صريحة، فمن 496 ملف قصص على القرص **22 فقط متعقَّب**. الـ242 MB مخرَجاتُ خطّ إنتاج محلية قابلة للتوليد.

#### ولذلك لم أنقل شيئًا

المعيار الأول («`assets` يحتوي فقط المُعلَن») غرضُه المكتوب «الأصول المصدرية خارج `assets`» — وهو **مُستوفًى للمستودع** بالفعل. ونقلُ 242 MB من ملفات **غير متعقَّبة** على جهاز المالك تغييرٌ واسع الأثر بلا مقابل هندسي، وموضعُها قرارُه لا قراري. فالمعيار جزئيّ بعلامة `[~]` ومعه سببه، بدل «إنجاز» يُقاس بعدد الملفات المُزَحزَحة.

والخطر الحقيقي الذي يذكره البند بنفسه ليس وجود الملفات بل **إعلان مجلدٍ بالخطأ**: سطرٌ واحد يضيف مئات الميغابايتات إلى ما يُنزِّله أهلٌ لطفلهم. فذاك ما حُرِس.

#### الحرس: ثلاثة أحكام، وحدٌّ خارجيّ لا مُختار

`tools/ci/assets-declared.mjs` (جديد) و`tools/ci/aab-size.mjs` (جديد)، وكلاهما مربوطٌ بوظيفة `flutter` في CI:

1. **حِمل الأصول** لا يتجاوز حدًّا **مقيسًا** (116.12 → 125 MB). الهامش صغير مقصودًا: حدٌّ فضفاض لا يرصد إعلان مجلد، وحدٌّ ملتصق يفشل على إضافة أيقونة.
2. **حجم الحزمة** لا يتجاوز **200 MB** — وهذا ليس رقمًا اخترتُه بل عتبة Google Play التي يرى فوقها المستخدم على بيانات الجوّال تحذير حجمٍ عند التثبيت. أثرٌ ملموس على أهلٍ يثبّتون التطبيق، لا ذوقٌ هندسي. وغيابُ ملف الحزمة **فشل**: خطوةٌ تقيس ما لا وجود له وتُبلّغ نجاحًا أسوأ من غيابها.
3. **كل مسار `assets/…` حرفيّ في `lib/`** مُبندَلٌ فعلًا.

والحدّان قابلان للتجاوز بـ`--max-mb=` **لأجل رؤيتهما يفشلان** بلا تعديل الأداة. تحقّقتُ من الثلاثة: تجاوُز الحِمل، وغياب الحزمة، وحزمةٌ فوق الحدّ، ومسارٌ وهميّ في `lib/`.

وفي `ciGates.test.mjs` حرسٌ على **الربط** نفسه، ومعه ترتيبُ الخطوتين: قياس الحزمة قبل بنائها يقيس حزمةً قديمة أو لا شيء. أداةٌ موجودة وغير مربوطة ليست بوابة — وهو ما وقع فعلًا مع `feature-matrix --check` في الدفعة 42.

#### الحكم الثالث أوقع أحد عشر مسارًا ميتًا

وهذا ما لم يكن في البند:

- **سبعة** مسارات في `assets/images/tracing/v2/` — مجلدٌ **غير موجود على القرص ولا مُعلَن**. والأثر لم يكن انكسارًا (`errorBuilder` يُعيد `SizedBox`) بل أسوأ في صمته: `hasImg` تصير `true` لمجرّد وجود المسار، فيُبنى المصغَّر ويفشل ويُخفى، **ويُتجاوَز** فرع `if (!hasImg)` الذي يرسم الأيقونة المصمَّمة. فبطاقات فئات التتبّع تفقد صورتها **وأيقونتها**. صار `assetPath` قابلًا للعدم، فعادت الأيقونة.
- **هيرو شاشة التتبّع** `assets/images/studio/v2/trace-hero.png` — مسارٌ **مُعلَّق بقرار مكتوب** في `pubspec.yaml` («dev fallback فقط: لا يُبندل في release») وغير موجود على القرص. فكان الهيرو يسقط **دائمًا** إلى `coloring/v2/bird.png`: عصفورُ التلوين في رأس شاشة التتبّع، في كل بناء إصدار. صار الأصل المُبندَل هو المُمرَّر.
- ومطابقتان كانتا `'assets/'` وحدها — مقارنةُ بادئة لا تحميل. استُثنيت، فحرسٌ يصرخ على الصحيح يُطفأ.

#### منظومةٌ كاملة لا تعمل ولا يناديها أحد

`bundledStoryAssetForUrl` و`loadBundledStoryEnvelope`: توثيقهما يقول إنهما يحلّان القصص المُبندَلة للقراءة دون اتصال. والمقيس: **صفر منادٍ** لكلٍّ منهما في المستودع كلّه، والمسارات التي يحلّانها (`assets/images/stories/` و`assets/data/bundled_stories/`) **غير مُعلَنة بقرار مكتوب**، فما كان يمكن أن توجد في الحزمة. أي أنها ما عملت يومًا ولا كان يمكن أن تعمل. والأطرف أن دفعةً سابقة «أصلحت» فيها مقارنةَ المضيف (`APP-103`) — إصلاحٌ في دالّةٍ لا ينادها أحد.

حُذفت الاثنتان. والقراءةُ دون اتصال يخدمها مسار التنزيلات المشفَّرة والذاكرة المؤقتة، وهما مُختبَران. والبديل — ربطُها وإعلان 222 MB — مرفوضٌ لأنه يناقض سياسة الحزمة المكتوبة ويضاعف مسارًا قائمًا.

#### حدٌّ في الحرس يستحق التسجيل

الفحص يرى المسارات **الحرفية** فقط. و`bundledStoryAssetForUrl` كانت تبني مسارها بـ`segments.join('/')` — أي أن هذا الحرس **لم يكن سيوقعها**. وُجدت بتتبّع المنادين لا بالمسح. فمن يقرأ خضرة هذا الحرس لا يقرأ «لا مسار أصلٍ مفقود في التطبيق»، بل «لا مسار حرفيّ مفقود».

#### الملفات

`tools/ci/assets-declared.mjs` (جديد) · `tools/ci/aab-size.mjs` (جديد) · `.github/workflows/ci.yml` (خطوتان) · `dashboard/api/test/ciGates.test.mjs` (حرس الربط والترتيب) · `app_main/lib/features/games/presentation/pages/studio_tracing/trace_home_v2.dart` · `.../studio_v2/trace_home_page.dart` · وحُذف `app_main/lib/core/media/bundled_story_assets.dart` و`bundled_story_pages.dart`.

#### التالي

`PERF-102` (56 `Image.asset` مقابل 5 استخدامات لـ`cacheWidth`) بالترتيب من §6.

---

### الدفعة السابعة والخمسون (2026-08-29) — `A11Y-102`: البوابة كانت موجودة، والعطل في المسار الذي لا يعرفها

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **705/705** (+8) · وبقيّة الفحوص كما هي: الدفعة لم تلمس ملفًا خارج `app_main`.

#### الدليل نُقض جزئيًّا

البند قال «لا يوجد بديل بلوحة مفاتيح أو D-pad» وأثره «طفل على التلفاز لا يستطيع استخدام الاستوديو مطلقًا». الشقّ الثاني صحيحٌ في نتيجته وخاطئٌ في تفسيره: التلفاز **مخدوم بثلاث بوابات مقيسة**، لا مهمَل.

1. `/studio` يفحص `deviceProfileProvider` ويعرض «الاستوديو يحتاج شاشة لمس».
2. بطاقة الاستوديو في سطح الملف مُخفاة على التلفاز (`if (!isTelevision)`)، و`tv_home_shell` لا يحمل رابطًا إليه أصلًا.
3. `evaluateAvailability` يرفض بـ`requiresTouch`، و`TraceColorEngine` يُعلن `supportsDpad => false`، وكل حزمة استوديو تُعلن `supports_dpad: false`.

فالحجب **قرارٌ منفَّذٌ برسالةٍ صادقة**، لا صمتٌ.

#### والعطل كان في الرابعة الغائبة

ثلاثة روابط عميقة — `/studio/coloring/:id` و`/studio/reference/:id` و`/studio/trace/:id` — تفتح **الأسطح نفسها** ولم تفحص شيئًا. ولا تمرّ بـ`GameScreen` فلا يعصمها `requiresTouch` (حزمها تُعلن `supports_dpad: false` بلا من يقرأه في هذا المسار). فرابطٌ عميق على التلفاز — من إشعار أو مشاركة أو استئناف — كان يُنزل الطفل على لوحةٍ تُدار بالإصبع وحده: لا رسالة، ولا سبيل إلى الرسم بالريموت.

هذا هو نمط `ADM-102` نفسه للمرّة الثانية: فحصٌ محفورٌ في **موضع واحد** بينما المواضع الأخرى التي تحتاجه لا تعرف بوجوده.

#### الإصلاح: البوابة داخل بانيَة المسار

`TouchOnlySurface` (جديد) + `_touchOnlyRoute`. الأربعة تُعلَن عبرها، فمن يضيف مسار استوديو خامسًا **يحمل الفحص بالبناء** لا بتذكّره. وثلاث ملاحظات في التصميم:

- **دالّة لا ودجت**: على التلفاز لا يُبنى السطح إطلاقًا، فلا يُحمَّل أصلٌ ولا يُفتح مخزنٌ لشيءٍ لن يُعرض. والاختبار يؤكّد `built == 0` لا «غير مرئي» — لأن «لا يُرى» كان سيمرّ على سطحٍ يعمل خلف الرسالة.
- **البوابة أوّلًا**: كانت رسالة «اختر طفلًا أولًا» تسبق فحص الجهاز في `/studio`. قدرةُ الجهاز غير مشروطة بطفل، فصارت أوّلًا.
- **فرع الخطأ يبني السطح، بقرار موثَّق**: `DeviceProfileService` يلتقط `PlatformException` و`MissingPluginException` ويُعيد «ليس تلفازًا»، فالفرع غير قابل للوصول عمليًّا — وحجبُ السطح عليه كان سيمنع الهواتف (الغالبية) على فشلٍ لا يحدث. مكتوبٌ في الكود ومُثبَّتٌ باختبار.

#### الحرس، ولماذا يقشّر التعليقات

الخاصّية شاملة على الملف («كل مسار استوديو»)، وهي من نوع ما لا يُقاس بطلبٍ واحد، فحرسها مسحٌ بنيويّ: لا `GoRoute(path: '/studio…'` عارٍ. والتعليقات **تُقشَّر أوّلًا** لأن حرسًا على النصّ الخام يرصد التعليق الذي يشرح العطل فينجح كذبًا — وهو خطأ وقعتُ فيه بنفسي في الدفعة 54. ومعه تأكيدٌ مضادّ أن المسارات الأربعة المعروفة **لا تزال** مغلَّفة، لئلّا يمرّ الحرس بصمتٍ لو حُذف الغلاف كلّه. وتحقّقتُ أنه يفشل: بفكّ غلاف واحد سقط التأكيد.

#### رقمٌ مُختلَق وُجد في الطريق

لوحة التلوين V2 كانت تعرض للطفل `'${_ops.length} من 8 مناطق'`. البسط عددُ النقرات لا المناطق (نقرتان على الموضع نفسه تُحسبان اثنتين)، والمقام **8** رقمٌ حرفيّ لا علاقة له برسمةٍ بعينها — والتعليق فوقه كان يقرّ بذلك: «تقدير… بسيط حالياً». واللوحة تُعبّئ بـ`_floodFill` على بكسل لا على قائمة مناطق، فلا يوجد في وقت التشغيل ما يُقاس عليه مقامٌ صادق. صار الشريط يعرض ما يُعرف — عدد التلوينات — بصياغةٍ عربية صحيحة (مفرد ومثنّى وجمعان: «1 تلوينات» و«2 تلوينات» كانتا ستخرجان من `'$n تلوينات'` وحدها).

#### وما **لم** يُعمَل، وهو بقيّة البند

بديل الإدخال نفسه. الجرد المقيس في `features/games/**`: إشارة **واحدة** إلى لوحة المفاتيح في المنطقة كلّها (`CallbackShortcuts` لإغلاق طبقة مقارنة)، وصفر `FocusableActionDetector`/`Shortcuts`/`Actions`/`onKeyEvent`، وصفر `Semantics(onTap:)` على أي لوحة، ولا مفهوم «مؤشّر حاليّ» في الحالة. الأدوات والألوان مخدومة بـ`Semantics(button:, selected:)`؛ اللوحة وحدها ليست كذلك.

وسطح الأفعال الأدنى **محدودٌ ومقيس**: تحريك، `_begin`، `_extend`، `_end` في الرسم الحرّ؛ ونقطة تعبئة واحدة في التلوين. أي أن البناء ممكن. لكن الفجوة الباقية بعد إصلاح اليوم هي الطفل ذو الإعاقة الحركية **على الهاتف**، والسؤال «هل الرسم الحرّ بمؤشّرٍ منطقي تجربةٌ يُراد تقديمها، أم البديل نشاطٌ آخر؟» تجربةُ منتَج تُصمَّم لا تُستنتَج — فسُجّل `DECIDE-111` بدل اختراع الجواب. ومعه فرصةٌ مقيسة: `ColorRegion` نموذجٌ قائم بمعرّفات ثابتة و`label` و`hitRegionAt` و`regionPaths`، فتلوينُ مناطق بعُقَد `Semantics` قابلة للتركيز رخيص — **إن** حُوّلت اللوحتان النقطيّتان إليه.

#### الملفات

`lib/app/router/touch_only_surface.dart` (جديد) · `lib/app/router/app_router.dart` (`_touchOnlyRoute` + أربعة مسارات) · `lib/features/games/presentation/pages/coloring/coloring_board_v2.dart` (`coloringFillCountLabel`) · `test/touch_only_routes_test.dart` (جديد، 5) · `test/coloring_fill_count_label_test.dart` (جديد، 3).

#### التالي

`PERF-101` (358 ميغابايت أصول بصيغ مزدوجة) بالترتيب من §6.

---

### الدفعة السادسة والخمسون (2026-08-29) — `A11Y-101`: الإشارة الواحدة لتكبير النص لم تكن مجازًا

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **697/697** (+7) · Worker `npm test` **1413/1413** · وبقيّة الفحوص كما هي: هذه الدفعة لم تلمس ملفًا واحدًا خارج `app_main` (التعديل ملفّان: صفحة الكواكب واختبار جديد).

#### قياسٌ أوّل أوقع عطلًا

البند رصد **199** إشارة `Semantics` مقابل **إشارة واحدة** إلى `textScaler`. أخذتُ ذلك على أنه احتمال ثم قِسته: مِسحة على ثلاث نِسَب (1.0 / 1.3 / 2.0) لشاشتين تُبنيان بلا شبكة — `PlanetsPage` و`StudioHomeView`.

النتيجة المقيسة: **6 من 7 تأكيدات نجحت، وواحد فشل** — `A RenderFlex overflowed by 29 pixels on the bottom` عند 200% فقط. 130% نظيفة، والاستوديو نظيف على النِّسَب الثلاث.

#### السبب: رقمان حرفيّان ومقياسٌ ثالث لا يعرفهما

شريط اختيار الكواكب كان:

- `_PlanetChooser` يفرض `SizedBox(height: 128)` (و152 على التلفاز) — رقمٌ ثابت.
- `_PlanetChoice` داخله يكتب `fontSize: 12` للاسم و`fontSize: 10` لوسم «قريبًا» — رقمان حرفيّان.

فحين رفع المستخدم حجم الخط، نمت التسميتان ولم ينمُ الشريط، وامتصّ `Expanded` النقص حتى نفد فتجاوز العمود. والمقدار المقيس **29** يطابق نموّ كتلة النصّ عند 200% بالضبط: `(12 + 10) × 1.0 × 1.35 ≈ 29.7`.

**الإصلاح** الحجمان صارا ثابتين مُعلَنين (`_choiceLabelFontSize` و`_choiceComingSoonFontSize`) يقرأهما **الاثنان**: البطاقة تنسّق بهما، والشريط يحسب ارتفاعه منهما عبر `MediaQuery.textScalerOf`. والمضاف هو **نموّ** كتلة النصّ وحدها لا الارتفاع كلّه — الرمز والحواشي لا تتأثّر بحجم الخط، فمضاعفتها معه كانت ستوسّع الشريط 12 بكسل عند 100% وتغيّر تخطيطًا يعمل اليوم بلا سبب.

#### ولماذا هذه الدفعة **لا** تشطب المعيار الأول

المعيار نصّه «كل شاشة رئيسية». المِسحة تغطّي **شاشتين**. باقي الشاشات تُبنى عبر مزوّدي Riverpod وشبكة، وكلّ واحدة تحتاج مضيفًا يحقن حالتها — وهو عملٌ حقيقي لا يُطوى في هذه الدفعة. تسجيلها «مُنجَزة» لأن اختبارًا مرّ على شاشتين هو نوع الادّعاء الذي يجعل الأودت نفسه غير موثوق. فالمعيار **جزئيّ بعلامة `[~]`** ومعه ما يغطّيه بالعدد.

#### حدٌّ في الأداة يستحق التسجيل

تجاوزٌ صفريّ **لا يعني نصًّا مقروءًا**. الاسم يحمل `maxLines: 1` و`TextOverflow.ellipsis`، فعند تكبيرٍ عالٍ يتقلّص إلى حروفٍ ناقصة وثلاث نقاط: تخطيطٌ سليم وقراءةٌ مفقودة، والمِسحة تمرّ. هذا مكتوبٌ في رأس ملف الاختبار حتى لا يُقرأ نجاحه أكثر مما يعني، والمعيار الثالث (تقنيات مساعدة على جهاز حقيقي) يبقى `HUMAN-107`.

#### الملفات

`lib/features/planets/presentation/planets_page.dart` (ثابتان مُعلَنان + ارتفاع مشتقّ من المقياس) · `test/text_scaling_layout_test.dart` (جديد، 7 تأكيدات).

#### التالي

`A11Y-102` (الاستوديو والتلوين بلا بديل وصول) بالترتيب من §6.

---

### الدفعة الخامسة والخمسون (2026-08-29) — `I18N-101`: «دالّة الإجبار» كانت داخل شرطٍ يُلغيها

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **690/690** (+1) · Worker `npm test` **1413/1413** · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **354/354** · بناء اللوحة ينجح · `feature-matrix --check` صفر · وحرس التغطية أخضر.

#### البند XL ومعلَّق على قرار مالك، ومعياره الثالث غير مشروط

المعيار الأوّل قرار المالك (`DECIDE-105`)، والثاني مشروط به. والثالث — «لا واجهة تعرض
خيار لغة لا تعمل فعلًا» — غير مشروط. فقستُ السطوح الثلاثة:

* **التطبيق**: اللغة مثبَّتة بسببٍ مكتوب على `AppLocales.fallback`، ويحرسها
  `locale_catalog_test`.
* **نموذج ملفّ الطفل**: حقل اللغة **للقراءة فقط** بتعليقٍ يشرح أن «عرض مُنتقٍ بخيار
  واحد يزعم اختيارًا لا وجود له».
* **صفحة الهبوط**: لها فاحصٌ يفتح متصفّحًا ويتحقّق أن المبدّل **يترجم الصفحة فعلًا**
  «لا أن الزرّ يستجيب فقط»، ويرفض بقاء أي نصّ عربي في الواجهة الأجنبية.

فالمعيار الثالث مُستوفًى في الثلاثة، وكلٌّ بحرسه.

#### وفي الحرس نفسه كان العطل

`locale_catalog_test.dart`:

```dart
// If EN ever reaches full coverage this test will fail, prompting the flag
// to be flipped — which is exactly the intended forcing function.
final cov = coverage(enKeys);
if (cov < 0.99) {
  expect(AppLocales.english.isSelectable, isFalse, ...);
}
```

التأكيد **داخل** `if`. فحين تبلغ التغطية 99% يُتخطّى الجسم و**يمرّ الاختبار بلا أن
يؤكّد شيئًا** — أي أن «دالّة الإجبار» التي يصفها التعليق هي عكس ما يفعله الكود.

وبالقياس: **التغطية 100% اليوم** (255 مفتاحًا في كل ملفات ARB الثلاثة، و**صفر قيمة
عربية** في `app_en.arb`). فالاختبار كان قد **توقّف عن التأكيد أصلًا**، بصمت، وتعليقه
ما يزال يصف حرسًا.

#### والمقياس نفسه كان خطأً

255 مفتاحًا مترجَمة بالكامل، وفي الواجهة **2,683** نصًّا عربيًّا حرفيًّا في **147 من
198** ملفًا لا يمرّ بأي كتالوج (والأودت قاس 2,526 في 102 ملفًا — أي أن العدد **نما**،
لأن لا بوابة تمنعه). فتغطية ARB تقرأ 100% والشاشات عربية.

ولو قُلبت الراية على ذلك الرقم لعُرضت على المستخدم **لغةٌ لا يستطيع التطبيق رسمها** —
وهو العطل الذي يوجد هذا البند لمنعه.

#### فصارت دالّة الإجبار على المانع الحقيقي

اختبارٌ يسجّل أن ARB مكتمل **ويؤكّد بلا شرط** أن الإنجليزية غير قابلة للاختيار.
وثانٍ يقيس النصوص الحرفية ويمنع أي لغة غير العربية ما دامت موجودة — وهي دالّة الإجبار
الصادقة: تخضرّ فقط حين تُستخرَج النصوص فعلًا، **ولا تفترض قرار السياسة**: إن بقي
المنتج عربيًّا وحده بقيت صحيحة بلا كلفة.

وتحقّقتُ أنها تفشل فعلًا: قلبتُ الإنجليزية إلى `complete` مؤقّتًا فسقط اختباران، ثم
أُعيدت. حرسٌ لم أرَه يفشل ليس حرسًا — وهذا هو بالضبط ما فات على الاختبار السابق.

#### ما بقي من `I18N-101`

القرار (`DECIDE-105`) وما يتبعه: استخراج 2,683 نصًّا (**XL**)، وبوابة CI تمنع نصًّا
حرفيًّا جديدًا — **ولم أبنِ البوابة**: تحت سياسة «العربية وحدها» هي احتكاكٌ بلا مقابل،
وبناؤها الآن افتراضٌ لقرارٍ ليس لي. والرقم مقيسٌ الآن في اختبارٍ يُظهره في سبب الفشل،
فمن يقرأه يعرف حجم العمل.

#### الملفات

`app_main/test/locale_catalog_test.dart` (استبدال الاختبار الفارغ باثنين، وعدّاد النصوص الحرفية).

#### التالي

`A11Y-101` بالترتيب من §6.

---

### الدفعة الرابعة والخمسون (2026-08-29) — `QA-104`: «لا تأكيد على المصدر» قاعدةٌ تُفقد صرامةً لو أُخذت حرفيًّا

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **689/689** · Worker `npm test` **1413/1413** (+4) · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **354/354** · بناء اللوحة ينجح · `feature-matrix --check` صفر · وحرس التغطية أخضر.

#### القياس: 123 تأكيدًا على المصدر، وأكثرها في موضعه

مسحتُ المجموعة كلّها: **43 ملفًا** يقرأ المصدر، وفيها نحو **123** تأكيدًا. وتحويلها
كلّها إلى سلوك **يُخسر** صرامةً لا يربحها، والفرق الذي يحدّده البند نفسه بعبارة
«لإثبات سلوك وقت التشغيل»:

* **سلوك وقت تشغيل** — هل يعمل الوسيط؟ هل تُفرَض الحصة؟ هذا بطلبٍ حقيقي، وتأكيدُ
  المصدر عليه يبقى أخضر لو غُيّر مسار التركيب إلى مسار لا يطابق أي طلب حقيقي.
* **خاصّية بنيوية شاملة** — أن **كل** موجّه متحوّل في 36 ملفًا يُعلن
  `requirePermission` (31 تأكيدًا في `routeGuards`). تحويلها إلى سلوك يعني ضرب 497
  نقطة واحدةً واحدة؛ ومسح المصدر هو الأداة الصحيحة لها لا تنازلًا عنها.

#### و`cors.test.mjs` مذكورٌ في الدليل وهو **مُصلَحٌ أصلًا**

طبقاته الثلاث صحيحة: **طلبات preflight حقيقية** على التطبيق المُركَّب · ومحتوى العقد
مُؤكَّدٌ على القائمة **المستوردة** (`ALLOW_HEADERS`) لا على نصّ · وتأكيدٌ بنيويّ واحد
مع `doesNotMatch` يمنع **نسخةً ثانية** من قائمة الترويسات داخل `index.ts`. والنسخة
الثانية تتصرّف تصرّفًا مطابقًا اليوم وتتباعد غدًا، فلا يراها أي اختبار سلوكي. فلم
أغيّر فيه شيئًا.

#### والعطل الحقيقي كان عُذرًا مكتوبًا

في `analyticsIngest.test.mjs`:

```js
// The quota lives in index.ts middleware, which this suite cannot mount, so
// the registration is asserted on the source.
assert.match(source, /app\.use\('\/api\/v1\/analytics\/\*', analyticsLimit\)/);
```

والعذر **غير صحيح**: `rateLimit.test.mjs` و`entrypoint.test.mjs`
و`homeBuilderE2E.test.mjs` تُركّب `src/index.ts` فعلًا، وفي الأولى منها مِنصّة كاملة
(`worker.fetch` مع `RateLimiter` حقيقي في فضاء أسماء وهمي).

فانتقل الإثبات إلى طلبٍ حقيقي — **وبلا 241 طلبًا**: الحاصر يَطبع `X-RateLimit-*` على
كل استجابة يتولّاها، فطلبٌ واحد يُثبت أنه عمل على هذا المسار، و**قيمة `Limit`
تُعرِّف أيّ إعداد عمل** (240 = إعداد التحليلات لا غيره). ويُقرأ اسم الدلو من فضاء
الأسماء فيُثبت أنه `analytics:` لا دلوٌ مشترك.

ومعه **حالة ضابطة**: قراءة كاتالوج لا تُنفق ميزانية التحليلات. فاختبارٌ يرى الترويسة
دائمًا لا يفرّق بين «الحاصر عمل» و«كل استجابة تحمل هذه الترويسة».

#### وحرسٌ على العُذر لا على التمييز

التمييز بين البنيويّ والسلوكيّ لا يُكتشَف آليًّا، فلا أدّعي حرسه. والمحروس هو
**العبارة التي تُبرّر الخطأ**: مجموعةٌ تزعم أن التطبيق خارج متناولها. ويسبقه اختبارٌ
يُثبت الحقيقة التي يستند إليها الحرس (ثلاث مجموعات تُركّب التطبيق) بدل افتراضها.

#### ودرسٌ أوقعتُ نفسي فيه للمرّة السادسة

أوّل تشغيل للحرس **رصد تعليقاتي أنا**: الشرح الذي كتبتُه عمّا أُزيل كان يقتبس العبارة
المحروسة حرفيًّا، في ملفَين. وهو الدرس الذي تكرّر خمس مرّات قبل هذه في هذا الأودت:
حرسٌ على نصّ يرصد **التعليق الذي يشرح العطل**. والعلاج أن يُوصَف العطل بغير عبارته
المحروسة — **لا أن يُخفَّف الحرس**. ووُثّق ذلك في رأس ملف الحرس ليجده مَن يليه.

#### اختبارات (+4)

`rateLimit.test.mjs` (+2): مسار التحليلات وراء حصّته وبإعدادها بعينه (وبدلوٍ باسمها) ·
والحالة الضابطة. و`sourceAssertionDiscipline.test.mjs` (جديد، +3، بحذف واحدٍ من
`analyticsIngest`): ثلاث مجموعات تُركّب التطبيق فعلًا · ولا مجموعة تزعم العكس · وحصّة
التحليلات لا تعود إلى نصّ المصدر.

#### ما بقي من `QA-104`

المعيار الأوّل («كل تأكيد على وسيط أو حصة أو CORS بطلب حقيقي») **مُستوفى**: الحصة
والـCORS صارا سلوكيّين. والـ123 تأكيدًا الباقية بنيويّةٌ بحكم القاعدة المُعلَنة أعلاه،
ومراجعتها واحدًا واحدًا للتأكّد من أن أيًّا منها لا يخفي خاصّية وقت تشغيل عملٌ ورقيّ
يحتاج قائمة مُصنَّفة — وهي نفس فئة «تصنيف 370 نقطة» في `ADM-101`.

#### الملفات

`dashboard/api/test/rateLimit.test.mjs` · `test/analyticsIngest.test.mjs` (حُذف تأكيد المصدر) · `test/sourceAssertionDiscipline.test.mjs` (جديد، 3).

#### التالي

`I18N-101` بالترتيب من §6.

---

### الدفعة الثالثة والخمسون (2026-08-29) — `QA-103` (المعيار الثاني): رقمُ تغطيةٍ لا يحرسه أحد يُقرأ مرّةً ثم يُنسى

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **689/689** · Worker `npm test` **1409/1409** (+5) · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **354/354** · بناء اللوحة ينجح · `feature-matrix --check` صفر · و**حرس التغطية أخضر**.

#### ما لم أفعله، ولماذا

المعيار الأوّل يطلب `integration_test/` بمسار عدم اتصال كامل (تنزيل → تشفير → تشغيل
→ انتهاء ترخيص) **على جهاز أو محاكٍ في CI**. وهو محجوب بشيئين: محاكي Android في CI
استثمارٌ كبير بطيء ومتقلّب، **ومحتوى الفيديو غير موجود** (`CNT-101`: صفر أصل فيديو) —
فمسار «تنزيل ثم تشغيل» لا شيء يسير فيه. لم أبنِ هيكلًا يمرّ بلا أن يقيس شيئًا.

#### والمعيار الثاني كان قابلًا للتنفيذ كاملًا

قِستُ أوّلًا: `flutter test --coverage` أعطى **31.92%** من 28,834 سطرًا في **193**
ملفًا (من 203 في `lib/`) على 689 اختبارًا.

والحدّ **31%** — مقيسٌ لا مُختار، بتاريخ قياسه مكتوبًا بجانبه. وحدٌّ أعلى من الواقع
يُفشل كل جولة فيُطفَأ أو يُخفَّض، وهو الدرس الذي أعادته هذه الجلسة مرّاتٍ: حرسٌ يصرخ
بالباطل يُطفَأ. وحدٌّ صفريّ يحرس لا شيء، فيرفضه اختبار.

#### والمصيدة التي كان الحرس سيُصفِّق لها

`lcov` **لا يذكر إلا الملفات التي حمّلها اختبار**. فلو توقّف اختبارٌ عن استيراد ملفٍ
كبيرٍ ضعيف التغطية، يخرج الملف من التقرير فترتفع النسبة — **والتغطية الحقيقية تنخفض**.
حرسُ النسبة وحده كان سيقول «تحسّن».

فيُحرَس **عدد الملفات** كما تُحرَس النسبة (`minFiles: 190`). وتحقّقتُ من الحالتين
فعلًا: تقريرٌ بنسبة **100% في ملفٍ واحد** يُرفَض، وتقريرٌ بـ1% يُرفَض، و**تقريرٌ غائب
يُرفَض** — لأن خطوةً لم تُنتج تقريرًا تمرّ بلا قياس، وذلك أخفى من انخفاض النسبة.

#### والنسبة مرجَّحة بالأسطر لا متوسّط نسب

متوسّط النسب يمنح ملفًا من عشرة أسطر وزن ملفٍ من ألف، فيتحسّن الرقم بإضافة ملفات
صغيرة مغطّاة. والاختبار يقيس الفرق بحالةٍ تفرّق بينهما: ملفٌ من سطرين مغطّى تمامًا
وملفٌ من 98 سطرًا بلا تغطية — المتوسّط 50% والحقيقة **2%**.

وتقريرٌ بلا أسطر قابلة للتنفيذ **لا يدّعي نسبة** (`null`): صفرٌ كان سيُقرأ «لا تغطية»
و100 كان سيُقرأ «كاملة».

#### والسكربت نفسه محروس — لأن هذا بالضبط ما فشل في الدفعة 42

الدفعة 42 وجدت مولِّدًا كُتب لمنع التقادم **فتقادم هو** لأن `--check` فيه لم يُشغَّل.
فسكربتُ CI بلا اختبار هو النمط نفسه. وحين كتبتُ الاختبار **لم يكن الملف قابلًا
للاستيراد**: كوده الأمريّ يعمل عند `import` فيُنهي العملية. فأُضيف شرط «نقطة الدخول»،
وهو إصلاحٌ لقابلية الاختبار كشفه الاختبار نفسه.

وفي CI: **الفحص الذاتي يسبق القياس**، كما يفعل `scan-secrets` — حاسبٌ حسابه خاطئ
يُبلّغ رقمًا مريحًا.

#### اختبارات (+5)

النسبة مرجَّحة بالأسطر · وتقريرٌ بلا أسطر لا يدّعي نسبة · وكل حدٍّ يحمل تاريخ قياسه
(وحدٌّ صفريّ مرفوض) · وحدُّ الملفات موجود وقريبٌ من الشجرة المقيسة لا رمزيًّا · وCI
يُشغّل الحرس **ومعه الفحص الذاتي** وبـ`--coverage` (فخطوةٌ تُنتج تقريرًا بلا حرس
تُقرأ مرّة وتُنسى، وحرسٌ بلا تقرير يفشل دائمًا).

#### ما بقي من `QA-103`

المعيار الأوّل كلّه (محاكٍ في CI + مسار عدم اتصال حقيقي): محجوب على محتوى الفيديو
وعلى قرار استثمارٍ في CI. وتغطية الـWorker واللوحة **غير مقيسة بعد** — ولم أُدرجها في
الحدود لأن حدًّا بلا قياس هو رقمٌ مُختار، وهو ما يرفضه هذا البند أصلًا.

#### الملفات

`tools/ci/coverage-floor.mjs` (جديد) · `.github/workflows/ci.yml` (خطوة التغطية) · `.gitignore` · `dashboard/api/test/coverageFloor.test.mjs` (جديد، 5).

#### التالي

`QA-104` بالترتيب من §6.

---

### الدفعة الثانية والخمسون (2026-08-29) — `CNT-110`: التنزيل كان يُمارس حقًّا لا يقرؤه

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **689/689** · Worker `npm test` **1404/1404** (+7) · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **354/354** · بناء اللوحة ينجح · و`feature-matrix --check` صفر مشاكل.

#### دعوى البند عن البوابة قديمة

يقول: «ولا بوابة نشر تفحص وجود صفّ الحقوق». و`rightsChecks` في `lib/publishGate.ts`
تفحص بثلاث درجات: **ترخيصٌ منتهٍ يحجب** بلا شرط («المحتوى منتهٍ، والجميع راضٍ،
والنشر مع ذلك غير قانوني — فهو بالضبط الفحص الذي لا يجوز أن يكون تحذيرًا»)، وغيابُ
السجلّ يُحذِّر، وأقاليمٌ فارغة تُحذِّر. فالبوابة تفحص.

#### والمعيار الثاني بعد قرار «بلا DRM» له معنى واحد قابل للتنفيذ

المعيار: «المحتوى المرخَّص لا يُنشر قبل توثيق مصفوفة DRM/Offline/Watermark». وقرار
المالك `DECIDE-101`: **بلا DRM**. فما يبقى منه: `content_rights.licenses` تحمل
**الاستعمالات المسموح بها** — قِيسَت فعلًا: `["streaming","offline"]` أو
`["streaming"]` — أي أن المصفوفة **موجودة في البيانات**.

و`routes/downloads.ts` **لم يكن يقرؤها أصلًا**: صفر إشارة إلى `content_rights` في
الملف. فمنصّةٌ تُنزِّل عنصرًا حقوقه المُعلَنة «بثّ فقط» تُمارس حقًّا لم تُعلنه —
لا تشفير، لكن ما يُعلَن يجب أن يُحترَم.

#### والقياس منعني من ارتكاب «حجب الكلّ»

أوّل قاعدة خطرت لي: «ترخيصٌ مُعلَن يجب أن يشير إلى عقده (`contract_url`)». وبالقياس:
**125 صفًّا كلّها بلا `contract_url`** — فالقاعدة كانت ستحجب المكتبة كلّها فورًا.

والقياس الثاني منعني من خطأٍ أخفى: الحلقات (33) والكتب (13) كلّها تُعلن `offline`،
والألعاب (56) «بثّ فقط» **وليست قابلة للتنزيل** أصلًا، و**القصص لا يمكن أن تحمل صفّ
حقوق** لأن قيد CHECK في `content_rights.entity_type` يستثنيها. فرفضٌ عند **غياب**
الحقوق كان سيُعطّل تنزيل القصص كلّها بسبب قيدٍ في المخطوطة لا بسبب حقوق.

فالقاعدة المُنفَّذة أضيق وأصدق: **الرفض حين تُعلَن الحقوق وتستثني `offline`** فقط.
والغياب ليس منعًا (وبوابة النشر تُحذِّر عليه)، والقائمة المعطوبة تُعامَل كالغياب لا
كالمنع — فخطأُ بياناتٍ واحد لا يُوقف تنزيل عنصرٍ حقوقه سليمة. و403 لا 404: العنصر
موجود ومنشور، والسبب مُسمّى (`offline_right_not_declared`) بدل إخفائه في «غير متاح».

#### واختباري كشف موضعًا ثانيًا كنتُ سأتركه

كتبتُ اختبارًا يؤكّد أن الفحص **يسبق** إصدار الترخيص، فأخفق — لأن أوّل ظهور لاسم
`signOfflineLicense` هو **سطر الاستيراد**. تصحيحُ المقارنة إلى موضع النداء كشف
**موضعَي إصدار** لا واحدًا: الإصدار، و**التجديد**.

والتجديد يُمارس الحقّ من جديد. فحصُ الإصدار وحده كان يجعل حقًّا **سُحب بعد التنزيل**
يبقى ممتدًّا شهرًا بعد شهر: صاحب الحقوق يُغيّر «بثّ وعدم اتصال» إلى «بثّ فقط»، ولا
شيء يمنع التجديد. وهي نفس علّة «البوابة تعمل مرّةً ولا تُعاد» التي عالجتها الدفعة 43.
فحُرِس الموضعان، ويؤكّد اختبارٌ أن **عدد مواضع الإصدار يساوي عدد مواضع الحرس** وأن كل
حرسٍ يسبق إصداره — فموضع إصدار ثالث يُضاف لاحقًا يُفشل الجولة.

#### اختبارات (+7)

قيد CHECK مقروءٌ من المخطوطة نفسها (فإن صارت القصص ذات حقوق يُفشل الاختبار ويُراجَع
`RIGHTS_BEARING`) · والأنواع المفحوصة محصورة فلا تُحجَب القصص بقيدٍ هندسيّ · والغياب
لا يرفض · والإعلان الذي يستثني `offline` يرفض بـ403 وسببٍ مُسمّى · والفحص يسبق
الإصدار · **والتجديد محروس أيضًا** · والقائمة المعطوبة كالغياب.

#### ما بقي من `CNT-110`

`content_availability` بصفٍّ واحد فالرفض الإقليمي غير مُهيَّأ — **بيانات** (وله شاشة
منذ الدفعة 38). ومعيار «مالك ونوع ترخيص وتاريخ انتهاء لكل أصل»: المالك والتراخيص
مسجَّلة، و`expiry` فارغ في كل الصفوف — وهو صحيح لمحتوًى مملوك بلا انتهاء، ويصير
واجبًا عند أوّل ترخيصٍ خارجي.

#### الملفات

`dashboard/api/src/routes/downloads.ts` · `dashboard/api/test/offlineRightsDeclared.test.mjs` (جديد، 7).

#### التالي

`QA-103` بالترتيب من §6 (آخر بنود `CNT-*` انتهت).

---

### الدفعة الحادية والخمسون (2026-08-29) — `CNT-109`: تبويبٌ يجيب «لماذا لا دليل؟» بعناوين أعمدته

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **689/689** · Worker `npm test` **1397/1397** (+4) · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **354/354** (+3) · بناء اللوحة ينجح · و`feature-matrix --check` صفر مشاكل.

#### البند محتوى، وشاشته كانت تكذب

«57 هدفًا وصفر سؤال» محتوى لا كود. لكن أثره المكتوب — «كل شاشة إتقان ستعرض *لا
أدلة*» — دفعني إلى الشاشة، وفيها تبويب **تشخيص** كُتب ليجيب: لماذا لا دليل لهذا
الهدف؟ وكان جسم جدوله:

```jsx
<td>{text.whyContent}</td><td>{text.whyGames}</td><td>{text.runtimeStatus}</td>
```

أي **عناوين الأعمدة مكان قيمها**: ثلاث عبارات ثابتة («محتوى مرتبط»، «ألعاب قادرة على
توليد دليل»، «حالة التشغيل») تتكرّر في كل صفٍّ من السبعة والخمسين.

وهذا **أسوأ من خليةٍ فارغة**: المسؤول يقرأ منه أن لكل هدفٍ محتوًى مرتبطًا وألعابًا
قادرة على القياس — وهو **عكس** ما يوجد التبويب لكشفه. تبويبٌ كامل يبدو أنه يشخّص ولا
يقرأ حرفًا من قاعدة البيانات.

#### والقياس الصحيح ليس عدد الألعاب المرتبطة

«قادرة على توليد دليل» شرطها المحرّك: `memory_flip` و`rhythm_tap` لا يكتبان إتقانًا
(`ENGINES_WITHOUT_MASTERY`)، فلعبةٌ عليهما مرتبطةٌ بهدفٍ **لا تُنتج دليلًا أبدًا** —
وهي خطأ ربطٍ لا مصدر قياس، وبوابة النشر تحجبها أصلًا (الدفعة 47).

فصار `by-objective` يُعيد أربعة أعداد **مقيسة**: `linked_episodes`، `linked_games`،
`questions_count`، و`evidence_capable_games` (ألعاب منشورة على محرّكٍ يكتب إتقانًا).
والفرق بين الأخيرين هو الجواب الذي يحتاجه المحرِّر: «ثلاث ألعاب مرتبطة وصفر قياس»
تعني **أعِد الربط**، لا **أنتِج محتوى**.

#### وصفرٌ يُقال بالكلمات لا برقم

في خلية جدول، «0» تُقرأ كقياسٍ ناجح قيمته صفر. فالخليتان الحرجتان تعرضان عبارة: «لا
لعبة منشورة على محرّك يقيس الإتقان» و«لا أسئلة» — والعدد يظهر حين يكون له معنى.

#### ومصيدة في المَعلَمات

قائمة المحرّكات تُربَط بـ`?` داخل `SELECT`، أي **قبل** مَعلَمات `WHERE`. وترتيبٌ
مقلوب هنا لا يُنتج خطأً بل **أرقامًا خاطئة بصمت** — أسوأ أنواع الأعطال في تقرير.
فيحرسه اختبارٌ يقرأ المَعلَمات المُربوطة فعلًا ويؤكّد أن أوّلها قائمة المحرّكات.

وفي الطريق سقط تعليقٌ داخل نصّ SQL (بين `LIMIT ? OFFSET ?` والعلامة الخلفية) فكان
سيُرسَل إلى D1 كجزءٍ من الاستعلام. أظهره الفحص فورًا وصُحّح.

#### اختبارات (+7)

الخادم (4): قائمة المحرّكات العاجزة **مقروءة من نفس المصدر** الذي تقرأه النقطة (فلا
تصير قائمةً ثانية تتباعد) · وكل هدف يحمل أعدادًا مقيسة (ومعها `success_rate = null`
لا صفر) · والاستعلام يستثني المحرّكات العاجزة **وترتيب مَعلَماته صحيح** · وهدفٌ
مرتبطٌ بثلاث ألعابٍ عاجزة يُبلَّغ بصفر قادرة.

اللوحة (3): الأعداد المقيسة تظهر **ولا تتكرّر عناوين الأعمدة كقيم** · وصفر قادرة
يُقال بالكلمات مع بقاء عدد الألعاب المرتبطة مرئيًّا (فيُرى أن العلّة ربطٌ لا نقص) ·
و«لا أسئلة» بالكلمات.

#### ما بقي من `CNT-109`

تأليف الأسئلة وإنتاج الألعاب القادرة: **L محتوى**. والشاشة صارت تقول أين الفجوة
بالضبط ولمن، وهي شرط البدء لا نتيجته.

#### الملفات

`dashboard/api/src/routes/adminMastery.ts` · `dashboard/front/src/pages/MasteryPage.tsx` · `dashboard/api/test/masteryEvidenceDiagnostics.test.mjs` (جديد، 4) · `dashboard/front/src/test/masteryDiagnostics.test.tsx` (جديد، 3).

#### التالي

`CNT-110` بالترتيب من §6.

---

### الدفعة الخمسون (2026-08-29) — `CNT-108`: البطاقة لم تكن تعرض مدةً فارغة، بل تُقرّ بأن الحلقة «قصيرة»

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **689/689** (+6) · Worker `npm test` **1393/1393** · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **351/351** · بناء اللوحة ينجح · و`feature-matrix --check` صفر مشاكل.

#### الأثر المكتوب في البند أهدأ من الحقيقة

يقول البند: «كل بطاقة حلقة تعرض **مدة فارغة**». والكود يقول غير ذلك:

```dart
String get durationLabel {
  final minutes = durationSeconds ~/ 60;
  return minutes <= 0 ? 'قصيرة' : '$minutes د';
}
```

فحين لا تُسجَّل مدة تُعيد **«قصيرة»**. وكل حلقة منشورة بلا مدة، أي أن كل بطاقة كانت
**تُقرّ بأن الحلقة قصيرة** — وهي ليست حقلًا فارغًا بل **دعوى عن المحتوى لم يقِسها
أحد**، تُعرض لوليّ أمرٍ يبحث عن شيءٍ قصير قبل النوم. وذلك أسوأ من الفراغ، وهو نفس
عائلة «رقمٌ مُختلَق» التي أُصلحت في الدفعة 40 (عدد الجلسات) وفي الدفعة 41 (صفرٌ مكان
المجهول).

والكلمة كانت تخدم **معنيين**: «مقيسٌ وقصير» و«لا أعرف». وأي كلمة تخدم معنيين لا
تُبلّغ أيًّا منهما.

#### والنمط الصحيح كان في المستودع نفسه

`home_feed.dart:1649` تفعل الصواب أصلًا: `durationSeconds != null && durationSeconds > 0 ? … : null`
ثم تعرض الشارة شرطيًّا. فـ`durationLabel` كانت **الشاذّة**، لا الحالة العامّة.

الآن نوعها `String?`: المجهول `null`، وستة مواضع استهلاك تُسقط التسمية بدل اختلاقها
— ومنها **التسمية الوصفية** لقارئ الشاشة، فلا يسمع طفلٌ وصفًا لم يقِسه أحد. وما دون
الدقيقة **مقيس**، فله تسمية دقيقة («أقل من دقيقة») لا الكلمة الغامضة.

#### وأمّا الاشتقاق من الأصل فمحجوب لا مؤجَّل

نطاق البند «يُشتق من الأصل عند الاستيراد». وبالقياس: `content_assets` **لا يحمل عمود
مدة** أصلًا، و`content_assets WHERE kind='video'` = صفر (`CNT-101`). فلا مصدر يُشتقّ
منه، ولا يمكن استخراجها في Worker بلا معالجة وسائط. فهي محجوبة على إنتاج الفيديو، لا
عملٌ مؤجَّل بلا سبب.

والبوابة **تُحذِّر** على المدة الغائبة بسببٍ دقيق («فحدّ وقت الشاشة والتقدّم يُحسبان
بلا مرجع») — وذلك التحذير صار **مرئيًّا** في مسح البوابة بالأمس (الدفعة 49). فالمتابعة
قائمة، والاشتقاق ينتظر الملفات.

#### اختبارات (+6)

`test/episode_duration_label_test.dart`: صفرٌ يعني «لا أعرف» لا «قصيرة» · ومدة سالبة
أو معطوبة تبقى مجهولة · وما دون الدقيقة مقيسٌ فيُسمّى بدقّة · والمقيس بالدقائق ·
ونتيجة البحث بلا مدة تبقى اسم السلسلة وحده **ولا تحتوي كلمة «قصيرة»** · ومع المقيس
تُضاف بعد الفاصل.

#### الملفات

`app_main/lib/features/home/domain/content_models.dart` · و`presentation/widgets/content_cards.dart` · و`features/details/presentation/series_details_page.dart` · و`features/playback/presentation/playback_page.dart` · و`features/search/domain/search_engine.dart` · و`features/shorts/presentation/shorts_page.dart` · `app_main/test/episode_duration_label_test.dart` (جديد، 6).

#### التالي

`CNT-109` بالترتيب من §6.

---

### الدفعة التاسعة والأربعون (2026-08-29) — `CNT-107`: التحذير المُتجاهَل كان غير مرئي — في أداةٍ كتبتُها قبل ستّ دفعات

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **683/683** · Worker `npm test` **1393/1393** (+2) · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **351/351** (+1) · بناء اللوحة ينجح · و`feature-matrix --check` صفر مشاكل.

#### «السلوك المتوقع» يخالف قرارًا موثَّقًا في الكود، والقرار أصحّ

طلب البند: «لا نشر بلا مراجعة معتمدة للنوع المطلوب». والبوابة **تقرأ حالة المراجعة
فعلًا** (`reviewChecks` في `lib/publishGate.ts`)، وتفرّق:

* مراجعة **مرفوضة** أو «تحتاج تغييرًا» → **حاجب**. والتعليق يشرح: «شخصٌ نظر في هذا
  وقال لا، والنشر فوقه ليس ثغرةً في قائمة تحقّق بل **تجاوزٌ صامت لمراجع**».
* مراجعة **معتمدة** → نجاح.
* **غائبة أو معلّقة** → تحذير، بسببٍ مكتوب: تحويل الغياب إلى حاجب «يجعل المكتبة كلّها
  غير قابلة للنشر بين ليلة وضحاها — فيتعلّم الناس **تجاوز** البوابة لا تعبئتها».

وهذا هو منطق هذا الأودت نفسه: حرسٌ يحجب كل شيء يُطفَأ. فلم أحوّل التحذير إلى حاجب.

#### وتصحيحٌ لما كتبتُه في الدفعة 48

قلتُ عن `CNT-106` إن بوابة المراجع الشرعي «مراجعة بشرية لا كود». **خطأ**:
`religiousChecks` تحجب النشر على المحتوى الديني بثلاثة فحوص —
`religious_source` بلا `source_type`، و`religious_review` بلا تاريخ موافقة،
و`review_sharia` بلا قرارٍ معتمد — وهي **حواجب لا تحذيرات**، خلافًا لكل الأدوار
الأخرى. التوقيع بشريّ، والإنفاذ في الكود. صُحّح في موضعه.

#### والعطل الحقيقي كان في أداةٍ كتبتُها أنا

مسح البوابة (الدفعة 43، ثم عُمّم في 44) كان يُعيد **الحواجب فقط**
(`result.blockers`). والتحذيرات تُلقى.

فما رصده البند — 43 سجلّ مراجعة كلّها `pending`، ومحتوى منشور بلا اعتماد واحد
مكتمل — كان يعود من أداتي **«لا شيء محجوب»**. أي أن الأداة التي بنيتُها لإظهار
«المنشور المعطوب» كانت تُخفي بالضبط الصنف الذي يتحدّث عنه هذا البند.

الآن `warned` و`warned_count` **منفصلان**: طيُّهما في `blocked` يسمّي التحذير رفضًا،
وإسقاطهما يجعل التحذير المُتجاهَل غير مرئي — وليس أيٌّ منهما الحقيقة. وصفٌّ محجوب لا
يُعدّ مرّتين: هو مُبلَّغ بالحاجب الأقوى، وإدراجه في الاثنين يضخّم عدّادين لمشكلةٍ
واحدة فيقرأ المشغّل ضعف حجم العمل.

وعبارة اللوحة «**يمرّ** البوابة ويحمل تحذيرًا» لا «سليم»: البوابة لا تحجبه، وذلك لا
يعني أنه مكتمل.

#### اختبارات (+3)

الخادم (2): سلسلة **مكتملة الحقول** (غلاف، حلقات منشورة، نمط بصري، وصف) ومراجعاتها
غير مسجَّلة → `blocked_count = 0` و`warned_count > 0` بمعرّف يبدأ بـ`review_` ·
وصفٌّ محجوب لا يُعدّ في التحذيرات أيضًا.

وفي بناء الأوّل درسٌ عمليّ: أوّل تجربةٍ لي أعطت `blocked_count = 1` — لأن السلسلة
الوهمية كانت بلا غلاف وبلا حلقات. فالاختبار الذي أردته كان **يقيس شيئًا آخر**، ولو
مرّ بذلك الشكل لكان حرسًا على حالةٍ لا تخصّه.

واللوحة (1): «لا شيء محجوب» يبقى صحيحًا **ومعه** التحذير المُتجاهَل بالاسم.

#### ما بقي من `CNT-107`

تعاقدُ المراجعين وتشغيل المراجعات (`HUMAN-105`): بشريّ. والقدرة على رؤية «منشورٌ
وكل مراجعاته معلّقة» صارت موجودة، وهي شرط المتابعة لا نتيجتها. وأمّا `workflow_runs`
= 0 فهي حالة «لم يبدأ أحد» لا «لا كاتب»: الكاتب موجود (`adminWorkflow.ts:192`) كما
قِيس في الدفعة 41.

#### الملفات

`dashboard/api/src/routes/adminPublishGate.ts` · `dashboard/front/src/types/api.ts` + `src/pages/QualityPage.tsx` · `dashboard/api/test/publishSweep.test.mjs` (+2) · `dashboard/front/src/test/publishSweep.test.tsx` (+1).

#### التالي

`CNT-108` بالترتيب من §6.

---

### الدفعة الثامنة والأربعون (2026-08-29) — `CNT-106`: الطفل كان يدفع ثمن النقر ليعرف أن الغرفة فارغة

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **683/683** (+8) · Worker `npm test` **1391/1391** (+2) · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **350/350** · بناء اللوحة ينجح · و`feature-matrix --check` صفر مشاكل.

#### أوّلًا: الخلل ليس غرفةً بيضاء

قياسُ ما يفعله التطبيق فعلًا: صفحة الكواكب **تُعلن الفراغ بصراحة أصلًا** داخل الكوكب
— «لا توجد سلاسل منشورة لهذا الكوكب الآن.» و«لا توجد حلقات…» و«لا توجد ألعاب…» في
ثلاثة أقسام.

فالخلل الحقيقي أن **الاختيار كان أعمى**: `GET /api/v1/planets` تُعيد كل كوكب نشِط
بلا أي إشارة إلى وجود محتوى، فتسع بطاقات متشابهة تُعرض لطفل وبعضها لا شيء فيه.
والطفل يدفع ثمن النقر ليعرف.

ولم أُخفِ البطاقة: الإخفاء يمحو خريطة الطريق ويجعل كوكبًا مؤجَّلًا **بقرار** يشبه
كوكبًا لم يُخطَّط له. فالخادم يقول الحقيقة والبطاقة تعرضها: «قريبًا».

#### ودليل البند كان يعدّ الشيء الخطأ — في الاتجاهين

البند بنى جدوله على **السلاسل والحلقات المنشورة**، فاستنتج ثلاث «غرف فارغة»:
`maharat` و`tarikh` و`islamic`.

والسلسلة **مجلَّد**: نشرها لا يفتح شيئًا. والحلقات ليست كل ما يُفتح: القصص والألعاب
والكتب تتفرّع من `series` لا من الحلقات. فقستُ «ما يمكن فتحه فعلًا» على D1 المحلية:

* `maharat` = **3** عناصر تُفتح، و`tarikh` = **3** — أي أنهما **ليسا** فارغين.
* و`qiyam` = **صفر**، وهو الكوكب الذي وصفه البند بأن فيه 3 سلاسل و4 حلقات.

فعدُّ الحلقات وحده يخطئ في الاتجاهين: يصف الممتلئ فارغًا والفارغ ممتلئًا. والعدد
المشحون الآن `published_openable`: حلقات + قصص + ألعاب + كتب منشورة، عبر
`series.planet_id`. و`published_series` يبقى منفصلًا للوحة، فلا يختلط بما يُفتح.

#### و`null` ليست صفرًا

النسخة المحزومة (`LocalCatalog`) لا تحمل عددًا، وخادمٌ أقدم لا يرسل الحقل. فقراءة
الصمت «فراغًا» كانت ستَطبع «قريبًا» على كل كوكب **لحظة انقطاع الشبكة** — إعلانُ غيابٍ
هو في الحقيقة غيابُ معرفة، وهو الكذب نفسه في الاتجاه المعاكس. فالحقل `int?`،
و`isMeasuredEmpty` تشترط عددًا مقيسًا. نفس قاعدة الدفعة 41: شرطةٌ للمجهول ورقمٌ
للمقيس.

#### والعلامة مسموعة لا مرئية فقط

«قريبًا» أُضيفت إلى تسمية `Semantics` للبطاقة، لا إلى النصّ وحده: علامةٌ بصرية فقط
تُخفي الحقيقة عن طفلٍ يستعمل قارئ شاشة، ويحرسه اختبار.

#### اختبارات (+10)

Flutter (8): صفرٌ مقيس يعني «قريبًا» · وغياب العدّ «غير معروف» لا «فارغ» · وسلاسل
منشورة بلا شيء يُفتح تبقى «قريبًا» (وهي الحالة التي يخطئ فيها عدّ السلاسل) · وكوكبٌ
فيه ما يُفتح ليس «قريبًا» · وأربعة على **الشاشة الحقيقية** لا على غلافٍ للاختبار
(العلامة تظهر، ولا تظهر للممتلئ، ولا تظهر بلا عدّ، وتُسمَع في التسمية الوصفية).

الخادم (2): القائمة تشحن الحقلين، والاستعلام يعدّ **ما يُفتح** — مقروءًا من نصّ
الاستعلام المُنفَّذ: يجب أن يضمّ `episodes` و`stories` و`games` و`books`، وأن يُبقي
`published_series` منفصلًا.

#### ما بقي من `CNT-106`

المعيار الثاني كلّه: **لا يُنشر عنصرٌ من كوكب الإيمان قبل توقيع مراجعٍ شرعي معتمد**
(`HUMAN-103`). وهو بوابة مراجعة بشرية لا كود، ولم أقترب منها. وإنتاجُ محتوى الكواكب
الفارغة **XL إنتاج**.

#### الملفات

`dashboard/api/src/routes/planets.ts` · `app_main/lib/features/home/domain/content_models.dart` + `data/content_dtos.dart` + `features/planets/presentation/planets_page.dart` · `app_main/test/planet_content_counts_test.dart` (جديد، 8) · `dashboard/api/test/planetContentCounts.test.mjs` (جديد، 2).

#### التالي

`CNT-107` بالترتيب من §6.

---

### الدفعة السابعة والأربعون (2026-08-29) — `CNT-105`: معياران أحدهما مُنجَزٌ بأفضل، والآخر يطلب حشو حقلٍ لا قارئ له

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **675/675** · Worker `npm test` **1389/1389** (+4) · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **350/350** · بناء اللوحة ينجح · و`feature-matrix --check` صفر مشاكل.

#### المعيار الثاني مُنجَزٌ **وأفضل من صياغته**

طلب البند: «كل لعبة إمّا لها هدف تعليمي أو وسم *ترفيهية* صريح». والموجود أدقّ من
الوسم: فحص `objective` في `lib/publishReadiness.ts` **مشتقٌّ من عقد المحرّك**
(`writesMastery`) بأربعة فروع:

* محرّك لا يكتب إتقانًا → الهدف `not_applicable` بسببٍ مكتوب: «ترفيهي أولًا: يكتب
  محاولات ولا يكتب إتقانًا».
* ومحرّك كهذا **مربوطٌ** بهدف → **حاجب**. أي عكس القاعدة الساذجة تمامًا: الهدف هنا
  ليس نقصًا يُملأ، بل خطأٌ يُرفع.
* حزمة بلا تقييم → `not_applicable`.
* يكتب إتقانًا وبلا هدف → حاجب.

فـ«ترفيهية» **تصنيفٌ مُشتَقّ** لا وسمًا يُكتب بيد. ووسمٌ يدويّ كان سيصير مصدر حقيقة
ثانيًا يكذب حين يتغيّر المحرّك — وهي العائلة التي يطاردها هذا الأودت في كل دفعة.
فرفضتُ إضافته.

#### والمعيار الأوّل مبنيٌّ على أثرٍ **غير صحيح**

يقول البند: «طفل في الثالثة أو الرابعة يفتح لعبة بلا تعليمات **ولا صوت يشرح**».
والقياس يقول غير ذلك: `gamePackValidation.ts` قاعدة 5 تجعل
`vo.intro` و`vo.instruction` و`vo.instruction_repeat` **مفاتيح إلزامية**، وغيابها
**خطأ يحجب النشر** (`ctx.forPublish`) لا تحذيرًا. ويحرسه اختبار قائم بالاسم: «rule 5:
mandatory voice keys block publish but only warn on a draft». و`repeat_instructions_button`
مثبَّت `const: true` في المخطَّط.

فالتعليمات المسموعة **مضمونة ومحروسة**، والطفل لا يُفتح له لعبة بلا صوت يشرح.

#### وأمّا `instructions_ar` فحقلٌ لا يقرؤه أحد

الخادم يشحنه في حِزمة اللعبة (`instructions: localization?.instructions ?? game.instructions_ar`)،
و**تطبيق Flutter لا يحلّله في أي موضع**: صفر إشارة إلى `instructions` في
`app_main/lib`، مقابل خمس إشارات تستهلك `vo.instruction`/`vo.intro` المسموعة.

فحشو الحقل في 36 لعبة منشورة — أو جعله حاجب نشر — عملٌ يُقاس بعدد الصفوف لا بأثرٍ على
طفل. **ورفضتُ إضافة فحص `instructions`** لهذا السبب: البند نفسه يطارد «واجهةً تَعِد
بما لا يحدث»، وحقلٌ إلزاميّ لا قارئ له هو الطقس نفسه في الاتجاه المعاكس. وسواءُ
استعماله في التطبيق أو إسقاطه قرار منتج، لا قرارٌ أتّخذه بدل المالك.

#### وبالبحث في نفس المسار ظهر عطلٌ حقيقيّ

`resolveLanguage` تختار الترجمة بوجود **صفٍّ** لا بوجود نصٍّ فيه. فصفٌّ فرنسيّ
عمودُ `title` فيه `NULL` كان يُشحَن بـ`language: 'fr'` و`language_fell_back: false`
**والعنوان العربي**: خلطُ لغةٍ يُعرَض كلغةٍ مُحلَّلة، وحقلٌ واحد بحرفٍ قد لا يقرؤه
الطفل، ولا شيء في الحِزمة يقول ذلك.

والسابقة على بعد حقلين في الملف نفسه: `missing_prompt_keys` و`missing_voice_keys`
موجودان بالنصّ «فيكون النقص مرئيًّا لا صامتًا». والعنوان والتعليمات هما الحقلان
اللذان تُركا خارج القاعدة — وهما أوّل ما يقرؤه طفل.

أُضيف `text_fell_back` في الحِزمة، والقاعدة في `textFallbacks` **بجوار سببها**
(`resolveLanguage`) لا في الموجّه. والعربية أساسٌ فلا تُبلَّغ: تقريرٌ يُطلق على كل
لعبة عربية يُدرَّب الناس على تجاهله — وهو نفس درس «حرسٌ يصرخ بالباطل يُطفَأ».

#### اختبارات (+4)

في `test/gameDelivery.test.mjs`، بوحدةٍ صافية لا بطلبٍ يحتاج جلسة عائلة: لغةٌ
مُحلَّلة بنصٍّ فارغ تُسمّي الحقول الساقطة (الثلاث حالات) · وصفٌّ مترجَم كاملًا لا
يُبلّغ شيئًا (فتبقى الإشارة ذات معنى) · والعربية ليست سقوطًا · ولا ترجمةَ أصلًا يعني
لا شيء يُسمّى (لأن الموجّه لا يشحن لعبةً في تلك الحالة، فتسميةُ حقولٍ تصف حِزمةً لا
تُرسَل).

#### ما بقي من `CNT-105`

قرارٌ لا عمل: إمّا أن يقرأ التطبيق `instructions` فيصير الحقل واجبًا، أو يُسقَط من
العقد. سُجّل `DECIDE-110`. وأمّا «21 لعبة بلا هدف» فالبوابة تجيب عنها بالتصنيف
المشتَقّ اليوم، ومن كان بلا هدف **وله محرّكٌ يكتب إتقانًا** فهو محجوبٌ عن النشر أصلًا.

#### الملفات

`dashboard/api/src/lib/gameDelivery.ts` (`textFallbacks`) · `src/routes/games.ts` (`text_fell_back`) · `test/gameDelivery.test.mjs` (+4).

#### التالي

`CNT-106` بالترتيب من §6.

---

### الدفعة السادسة والأربعون (2026-08-29) — `CNT-104`: «مسارَا ربط» كانا ستةَ عشر

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **675/675** · Worker `npm test` **1385/1385** (+6) · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **350/350** (+4) · بناء اللوحة ينجح · و`feature-matrix --check` صفر مشاكل.

#### البند رصد المبدأ الصحيح وأخطأ في مقداره

قال إن **مسارَي ربط** يتعايشان — `asset_links` للحلقات وعمودٌ مباشر للقصص — وإن أي
تقرير «أصول غير مستخدمة» يجب أن يعرف الاثنين وإلّا أعطى نتيجة خاطئة. والمبدأ صحيح
تمامًا. والمقدار: **ستة عشر مفتاحًا أجنبيًّا في ثلاثة عشر جدولًا**
(`asset_links`, `asset_uploads`, `blog_authors`, `blog_posts`,
`episode_audio_tracks`, `episode_renditions`, `episode_subtitle_tracks`,
`playback_leases`, `questions`, `seo_meta`, `story_page_localizations`,
`story_pages` بعمودين، `web_page_sections`).

فتقريرٌ يُبنى على وصف البند نفسه يبقى خاطئًا — **وخاطئًا في الاتجاه الذي يُحذَف فيه
محتوى مستخدَم**.

#### فالمسارات تُكتشَف ولا تُكتَب

قائمةٌ بثلاثة عشر جدولًا تكون صحيحة اليوم وخاطئة عند الترحيل التالي، وهو العطل الذي
يجده هذا الأودت في كل مرآةٍ تُصان بيد. فالتقرير يقرأ كتالوج SQLite في كل نداء
(`sqlite_master.sql LIKE '%REFERENCES content_assets%'`) ويستخرج أسماء الأعمدة من نصّ
المخطَّط، فعمودٌ جديد يُغطّى **لحظةَ وصول ترحيله** بلا ما يُتذكَّر. الكلفة استعلامٌ
واحد إضافي.

والاستخراج مُثبَّت على الإشارة نفسها (`<عمود> TEXT … REFERENCES content_assets`) لا
على لاحقةٍ معروفة: قاعدة `*_asset_id` كانت تُسقط ثمانيةَ أعمدة اسمها `asset_id`
مجرَّدًا، وأيّ اسمٍ يأتي لاحقًا.

#### وأخطر حالة في هذا التقرير: أن يفشل الاكتشاف

لو أعاد الاكتشاف صفرًا لظهر **كل أصل** يتيمًا، فتُدعى إلى حذفٍ جماعي بثقةٍ كاملة.
فالنقطة **ترفض** بـ500 ورسالةٍ صريحة بدل أن تُبلّغ. ويحرسه اختبار.

#### وقياسٌ على المخطَّط الحقيقي لا على بديل

بنيتُ الاستعلام المُركَّب وشغّلته على D1 المحلية: **14 أصلًا صوتيًّا و87 صورة** بلا
أي إشارة. والصوت يطابق عدد البند، والصور **ستة أضعافه** ولم تكن في نطاقه.

ومع ذلك لا أرفع ذلك إلى حقيقةٍ عن الإنتاج: القاعدة المحلية بلا صفحات قصص، فصورةٌ
تُربَط في الإنتاج عبر `story_pages.image_asset_id` تظهر يتيمة هنا. القيمة المؤكَّدة
أن **التقرير يعمل ويغطّي المسارات كلّها**؛ وأرقام الإنتاج فعلُ مالكٍ مُعلَّق.

#### والمؤرشَف مستثنًى بقرار

معيار القبول نفسه يقول «مرتبط بكيان **أو** مُصنَّف صراحةً كأرشيف»، و`content_assets.status`
يحوي `archived` أصلًا. فالمؤرشَف قرارٌ اتُّخذ لا طرفٌ سائب، و`?include_archived=1`
يُظهره لتدقيق المساحة.

#### وله شاشة

قسمٌ في `MediaLibraryPage` يعرض العدد والتوزيع بالنوع والمساحة — **ومعه عدد مسارات
الربط المفحوصة**. وذلك ليس تفصيلًا تقنيًّا: تقريرٌ لا تُرى تغطيته لا يُوثَق به،
وغيابُ الرؤية هو كيف صار «مسارَان» وصفًا مقبولًا لثلاثة عشر جدولًا. ونقطةٌ بلا
مُستهلِك هي `ADM-101` بثوب آخر.

#### اختبارات (+10)

الخادم (6): المسارات مكتشفة من نصّ المخطَّط (والعمودان في `story_pages` كلاهما) ·
وكل مسار مكتشف يصير شرط `NOT EXISTS` (مقروءًا من الاستعلام المُنفَّذ) · والمؤرشَف
مستثنًى افتراضًا ويُضَمّ بطلب · و**صفر مسارات يعني رفضًا** لا تقريرًا بكل الأصول ·
والـ`limit` مسقوفة و`kind` مربوطة كمَعلَمة · والتقرير يقول ما فَحَص.

واللوحة (4): لا يُفحَص إلا بطلب · والعدد مع عدد المسارات المفحوصة · وصفرٌ يُعرض «لا
أصل يتيم» ومعه التغطية · وفشل الفحص لا يُعرض «لا أصل يتيم».

#### ما بقي من `CNT-104`

المعيار الأوّل: **تصنيف** الأصول اليتيمة فعليًّا — ربطٌ أو أرشفة. وهو قرار محتوى على
الإنتاج، وصار له الآن ما يسبقه: قائمةٌ صحيحة بما هو يتيم حقًّا.

#### الملفات

`dashboard/api/src/routes/adminAssets.ts` · `dashboard/front/src/lib/api.ts` + `src/types/api.ts` + `src/pages/MediaLibraryPage.tsx` · `dashboard/api/test/unreferencedAssets.test.mjs` (جديد، 6) · `dashboard/front/src/test/unreferencedAssets.test.tsx` (جديد، 4).

#### التالي

`CNT-105` بالترتيب من §6.

---

### الدفعة الخامسة والأربعون (2026-08-29) — `CNT-103`: التقرير كان موجودًا، والعرض كان يُلقي نصفه

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **675/675** · Worker `npm test` **1379/1379** · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **346/346** (+1) · بناء اللوحة ينجح · و`feature-matrix --check` صفر مشاكل.

#### القياس: البيانات إنتاجية، والبوابة أدقّ من الاستعلام

القاعدة المحلية: **صفر صفحة قصة** وصفر ترجمة و**14 أصلًا صوتيًّا**. فأرقام البند
(194 صفحة، 178 بلا رسم، 16 مسرودة) إنتاجية لا أؤكّدها من هنا.

والمعيار الثاني — «تقرير جهوزية لكل قصة يُنتَج آليًا من الاستعلام أعلاه» — تبيّن أنه
**منجَزٌ في الخادم أصلًا**، وأدقّ من الاستعلام المقترح: `evaluateStory` لا تعدّ
`image_asset_id IS NULL` فقط، بل تفصل «بلا رسم» عن «رسمها غير جاهز»
(`image_status !== 'ready'`) — وأصلٌ مرتبطٌ غير جاهز يُقرأ في الاستعلام كأنه رسمٌ
موجود. وتفعل مثله للنصّ والسرد، وتُرجع **المواضع بالاسم**: «صفحة 3: بلا رسم».

#### والعطل كان في العرض، وهو ما وجدته بمراجعة عملي أمس

لوحة مسح البوابة التي أضفتُها في الدفعة 44 كانت تعرض `label_ar` وحده:
**«رسوم الصفحات»**. والبوابة تُعيد معه `detail` بالعدد و`items` بالمواضع، وكنتُ
أُلقيهما. فالمحرِّر يرى اسم الفحص ولا يرى كم ولا أيّها — وهو نصفُ تقرير، أي رقمٌ بلا
خطوةٍ تالية: نفس العطل الذي أصلحته الدفعة 44 نفسها، على بعد طبقة.

الآن: العدد والتفصيل، وأوّل ثمانية مواضع بالاسم، **والباقي بعددٍ صريح** — «و170
موضعًا آخر» لا «وغيرها». فـ«غيرها» تُخفي الفرق بين موضعين ومئة وسبعين، وهو الفرق بين
«أكمل صفحتين» و«القصة لم تُرسم».

#### اختبار (+1)

قصة بـ178 موضعًا: يؤكّد أن العدد يظهر (`178 صفحة بلا رسم`)، وأن أوّل المواضع يظهر
بالاسم (`صفحة 1: بلا رسم`)، وأن الباقي يظهر بعددٍ صريح (`and 170 more`) — أي أن
الثلاثة معًا لا اسم الفحص وحده.

#### ما بقي من `CNT-103`

المعيار الأوّل كلّه: رسمٌ وسردٌ لكل صفحة في كل قصة تُنشَر. **XL إنتاج، قرار مالك.**
والقدرة على معرفة ما ينقص — بالعدد والموضع — صارت كاملة من الخادم إلى الشاشة.

#### الملفات

`dashboard/front/src/pages/QualityPage.tsx` · `dashboard/front/src/test/publishSweep.test.tsx` (+1).

#### التالي

`CNT-104` بالترتيب من §6 — و**دليله قابل للتحقّق محليًّا** خلافًا للثلاثة قبله: 14 أصلًا صوتيًّا موجودة محليًّا، وصفر صفحة قصة، أي أن الأربعة عشر كلّها يتيمة هنا أيضًا.

---

### الدفعة الرابعة والأربعون (2026-08-29) — `CNT-102`: «اثنتا عشرة قصة عند جاهزة» رقمٌ بلا خطوةٍ تالية

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **675/675** · Worker `npm test` **1379/1379** (+4) · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **345/345** (+1 صافي، والملف صار 6) · بناء اللوحة ينجح · و`feature-matrix --check` صفر مشاكل.

#### القياس أوّلًا، والنتيجة أن الأرقام إنتاجية

قاعدة البيانات المحلية: **8 قصص `writing`** و13 كتابًا `draft` و22 سلسلة `draft`
وواحدة منشورة. أي **صفر قصة `ready`** محليًّا. فأرقام البند (12 قصة `ready`، صفر
منشورة، 22 كتابًا) حقائق **إنتاجية** لا أؤكّدها من هنا — كما في `CNT-101`.

والمعيار الأوّل «القصتان المكتملتان منشورتان» **قرار محتوى** على الإنتاج، لا كود.

#### والمعيار الثاني كان قابلًا للتنفيذ، وهو الذي يجعل الأوّل ممكنًا

«كل قصة `ready` لها تقرير بوابة يوضح ما ينقصها بالاسم» — وهذه **قدرة** لم تكن
موجودة: النقطة الوحيدة تفحص عنصرًا واحدًا، و`QualityPage` تفحص عيّنة من ثمانية لكل
نوع **أيًّا كانت حالتها**. فمعرفة أن اثنتي عشرة قصة تقف عند `ready` رقمٌ بلا خطوةٍ
تالية؛ ومعرفة أن ثمانيًا منها تنقصها الرسوم **خطوة**.

فعُمّم مسح الدفعة الذي أُضيف في الدفعة 43: صار `?status=` بقائمة مغلقة
(`published|ready|scheduled|review`)، وأُعيد تسميته من `/drift` إلى
**`/publish-readiness/sweep`** ومفاتيحه من `drifted` إلى `blocked`.

وسبب التسمية أنها صارت **سؤالين لا واحدًا**، و«انحراف» يصف أحدهما فقط:

* `published` — ما هو حيٌّ الآن وسيفشل لو نُشر اليوم.
* `ready` — ما ينتظر النشر وما ينقصه بالاسم.

ونقطةٌ واحدة لا نقطتان، لأنه سؤالٌ واحد بمرشّح — ونقطتان تصيران نسختين من الحلقة
نفسها تتباعدان. (والتسمية غُيّرت بعد دفعةٍ واحدة ومُستهلِكها الوحيد في هذا المستودع،
فالثمن معدوم؛ وإبقاء اسمٍ يصف نصف ما يفعله أغلى.)

#### والحالة تصل إلى `WHERE`

فهي **مُقيَّدة بقائمة** ومربوطة كمَعلَمة لا مُدرَجة في النصّ، ورسالة الرفض تسمّي
المسموح فلا يُخمّن المتصل. ويحرس ذلك اختبارٌ يقرأ الاستعلام المُنفَّذ فعلًا ويؤكّد
أن `status = ?` مع `ready` في المَعلَمات — لا أن الرفض حدث.

#### والشرح يتبع السؤال

عبارتا اللوحة مختلفتان لأن الحالتين مختلفتان: «لا انحراف: 15 صفًّا منشورًا فُحص»
مقابل «12 صفًّا فُحص ولا شيء يحجبها — كلّها قابلة للنشر الآن». وقول «لا انحراف» عن
صفوف **لم تُنشر** كلامٌ لا معنى له، ويحرسه اختبار.

#### اختبارات (+15 في ملفين، أعوَض 10 سابقة)

الخادم (9، بطلبٍ حقيقي على الموجّه): نوعٌ غير مدعوم يُرفض · وحالةٌ غير مدعومة تُرفض
**والمسموح مُسمّى في الرسالة** · والحالة مربوطة كمَعلَمة (مقروءة من الاستعلام
المُنفَّذ) · و`published` هي السؤال الافتراضي · وصفرُ صفوف يُعرض مع عدد المفحوص ·
وحلقة منشورة بلا فيديو تُبلَّغ بحاجب البوابة `video` · و**قصة `ready` تُمسَح فتأتي
بأسماء ما ينقصها** · ونوعٌ فشلت قراءته يُسمّى ولا يُبلَّغ نظيفًا · والـ`limit`
مسقوفة صعودًا وهبوطًا.

واللوحة (6): لا يُفحَص إلا بطلب · و«لا شيء محجوب» مع عدد المفحوص · والمحجوب مسمّى
بحاجبه · و**تغيير الحالة يسأل الخادم سؤالًا آخر ويشرحه شرحًا آخر** · وصفٌّ تعذّر
تقييمه «غير معروف» لا مطويّ · وفشل الفحص لا يُعرض «لا شيء محجوب».

وفي اختبار الخادم قرارٌ صغير مقصود: تأكيدُ **وجود** أسماء للحواجب لا أسماءٍ بعينها.
قائمةٌ محدّدة تجعل الاختبار مرآةً ثانية لقواعد البوابة، وهي العائلة التي يطاردها هذا
الأودت.

#### ما بقي من `CNT-102`

نشر القصتين (قرار محتوى على الإنتاج) وإكمال رسوم وسرد العشر الباقية (**XL إنتاج**).
والقدرة على معرفة ما ينقص كلَّ واحدةٍ صارت موجودة، وهي شرط البدء لا نتيجته.

#### الملفات

`dashboard/api/src/routes/adminPublishGate.ts` · `dashboard/front/src/lib/api.ts` + `src/types/api.ts` + `src/pages/QualityPage.tsx` · `dashboard/api/test/publishSweep.test.mjs` (9) · `dashboard/front/src/test/publishSweep.test.tsx` (6).

#### التالي

`CNT-103` بالترتيب من §6.

---

### الدفعة الثالثة والأربعون (2026-08-29) — `CNT-101` (الشطر البرمجي) و`QA-105`: اختبارٌ كان يمرّ بالحظّ

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **675/675** في **عشر جولات كاملة متتابعة** · Worker `npm test` **1375/1375** (+5) · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **344/344** (+5) · بناء اللوحة ينجح · و`feature-matrix --check` صفر مشاكل.

#### ما لم أفعله، ولماذا

`CNT-101` حجمه **XL وإنتاجٌ لا كود**: لا ملفات فيديو. ومعياراه الأوّل والثالث
(«الاستعلام يعيد عددًا يساوي المنشور» و«تشغيل حلقة على جهاز حقيقي بدليل») يحتاجان
محتوًى لا أستطيع اختلاقه، وقرار مالك. فلم أقترب منهما.

وقِستُ ما يمكن قياسه: قاعدة البيانات **المحلية** فيها 33 حلقة كلّها `draft` وصفرٌ
لها رابط فيديو. فرقم «20 حلقة منشورة» في البند حقيقةٌ **إنتاجية** لا أستطيع تأكيدها
من هنا؛ تأكيدها فعلُ مالكٍ مُعلَّق أصلًا (تشغيل الأدوات بـ`--remote`).

#### وما تبيّن أن الكود يمنعه أصلًا

مسار نشر الحلقات **واحد** (`publishEntity(c, 'episode')`)، والبوابة تحجب صراحةً
حلقةً بلا فيديو (`publishGate.ts:598`). فلا يمكن اليوم نشر حلقة معطوبة. المعيار
الثاني «لا حلقة منشورة بلا أصل جاهز» مضمونٌ **للمستقبل**.

#### والفجوة الحقيقية: البوابة تعمل مرّةً ولا تُعاد

وهي لا تُجيب السؤال التشغيلي: **ما هو منشورٌ الآن وسيفشل لو نُشر اليوم؟** صفٌّ نُشر
قبل وجود فحصٍ ما، أو فُصل أصله بعد نشره، يبقى حيًّا ومعطوبًا. والنقطة الوحيدة تفحص
عنصرًا واحدًا (`/publish-readiness/:type/:id`)، فالجواب كان يقتضي طلبًا لكل صفّ
وإنسانًا يعدّ.

`GET /admin/publish-readiness/sweep` تُجيبه، و**تُعيد تشغيل البوابة نفسها** لكل صفّ
منشور لا تُقلّد قاعدتها في SQL: نسخةٌ ثانية من القاعدة تنحرف عنها كما انحرف كل
مرآةٍ يدوية في هذا الأودت. أبطأ، وصحيحٌ بالبناء. ويحرسه اختبارٌ يؤكّد أن معرّف
الحاجب العائد هو `video` **من البوابة** لا نصًّا كتبتُه.

وثلاثة قرارات في التفصيل: جدول كل نوع **مُعلَن** لا مُخمَّنًا بـ`${type}s` (لكانت
`storys` فيُبلَّغ عن نوعٍ لم يُستعلَم أصلًا كأنه نظيف) · و`limit` مسقوفة لأن الكلفة
`صفوف × استعلامات` · وصفٌّ تعذّر تقييمه يُسمّى في `unavailable` ولا يُطوى في «لا
انحراف» — وهو خطأ «الصفر مكان المجهول» نفسه في موضعٍ أخطر.

ولها **مُستهلِك**: قسم في `QualityPage` — الشاشة تفحص عيّنةً من كل نوع أيًّا كانت
حالتها، وهذا القسم يسأل المعاكس: ما هو **حيّ** ومعطوب. لا يُحمَّل تلقائيًّا (يُعيد
تقييم البوابة لكل صفّ، فهو أثقل من قراءة). ونقطةٌ بلا مُستهلِك هي `ADM-101` بثوب آخر.

#### `QA-105`: التقلُّب لم يكن تقلُّبًا، بل مرورًا بالحظّ

فشل الاختبار في جولتي هذه الدفعة أيضًا — **الثالثة والرابعة** — فتوقّفتُ عن إعادة
التشغيل وفتحتُ السبب.

المساعد `settle` كان ينتظر **120 مللي ثانية من زمن الحائط** ويفترض أن فكّ ترميز
الصورة انتهى. منفردًا يكفي؛ وفي جولةٍ كاملة تتنافس فيها عمليات كثيرة على الجهاز لا
يكفي، فيؤكّد السطر التالي على لوحةٍ ما تزال تُحمّل. أي أن **حُكم الاختبار كان رهنَ
حِمل الجهاز**.

فصار الانتظار على **الشرط** لا على الساعة: استقصاءٌ بخطوات قصيرة حتى يتحقّق ما
سيؤكّده الاختبار، بمهلةٍ حقيقية تفشل بجملةٍ مفهومة.

**وهنا ظهر ما كان مخفيًّا.** بعد التحويل فشل اختبارٌ آخر بصراحة: «إعادة» لا تُبلّغ
حدثًا ثالثًا خلال عشر ثوانٍ. والسبب أن انتظار الحدث **لازمٌ وغير كافٍ**: زرّ
«إعادة» يكون `onTap: null` حتى إعادة البناء التي تلي الالتزام، والحدث يصل قبلها.
و`tester.tap` على زرّ معطَّل **ينجح ولا يفعل شيئًا** — فالنقرة كانت تضيع، ويُلام
الأثر. أي أن الاختبار كان يمرّ لأن النومة الـ120 صادفت أن تسمح بإعادة البناء.

فصار كل انتظار على **شرط الخطوة التالية**: الحدث **و**أن يكون الزرّ الذي سيُنقَر
قابلًا للنقر (`InkResponse.onTap != null`). وأُفرد للحالة الوحيدة التي تُثبت
**غيابًا** مساعدٌ باسمه `quiet`: إثباتُ ألّا شيء حدث لا شرطَ له يُنتظَر، فهو الموضع
الوحيد الذي تكون المدّة فيه الأداة الصحيحة — ولا يُخفى داخل `settle` حيث يظنّه
القارئ محدودًا بحقيقة.

والقبول بالقياس الذي طلبه البند: **عشر جولات كاملة متتابعة** بلا فشل (ومعها خمس
جولات منفردة)، بعد أن كان يفشل قريبًا من جولةٍ من كل جولتين.

#### اختبارات (+10)

الخادم (5): نوعٌ غير مدعوم يُرفض لا يُفحَص كلا شيء · وصفرُ صفوف منشورة يُعرض **مع
عدد المفحوص** · وحلقة منشورة بلا فيديو تُبلَّغ بحاجب البوابة `video` · ونوعٌ فشلت
قراءته يُسمّى ولا يُبلَّغ نظيفًا · والـ`limit` مسقوفة صعودًا وهبوطًا.

واللوحة (5): لا يُفحَص إلا بطلب · و«لا انحراف» يُعرض مع عدد المفحوص (15 = 12+3) ·
والصفّ المنحرف يُسمّى ومعه حاجبه · وصفٌّ تعذّر تقييمه يُعرض «غير معروف» ولا يُطوى ·
وفشل الفحص لا يُعرض «لا انحراف».

#### ما بقي من `CNT-101`

الشطر الإنتاجي كلّه: إنتاج الفيديو وربطه، ثم قياس الانحراف على الإنتاج بالنقطة
الجديدة، ثم تشغيلٌ من طرف إلى طرف بدليل. **قرار مالك** (XL).

#### الملفات

`dashboard/api/src/routes/adminPublishGate.ts` (نقطة الانحراف) · `dashboard/front/src/lib/api.ts` + `src/types/api.ts` + `src/pages/QualityPage.tsx` (المُستهلِك) · `app_main/test/coloring_board_test.dart` (`QA-105`) · `dashboard/api/test/publishSweep.test.mjs` (جديد، 5 — عُمّمت في الدفعة 44) · `dashboard/front/src/test/publishSweep.test.tsx` (جديد، 5 — عُمّمت في الدفعة 44).

#### التالي

`CNT-102` بالترتيب من §6.

---

### الدفعة الثانية والأربعون (2026-08-29) — `ADM-107`: مكوّنٌ ميت قاد إلى مولِّدٍ عاطل وحرسٍ لم يُشغَّل

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **675/675** · Worker `npm test` **1370/1370** · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **339/339** · وبناء اللوحة ينجح.

#### البند كان XS، وما تحته لم يكن

`NotImplementedPage` مُعرَّف وغير مستخدَم — صحيح، وحذفه سطران. لكن الاسم كان
**مذكورًا دليلًا** في أربع وثائق: `docs/FEATURE_MATRIX.md`
و`FEATURE_MATRIX_VERDICTS.json` و`النواقص_المتبقية.md` وتعليقٍ في
`AdminRoutes.tsx`. فالكود الميت لم يكن فضلةً تُزال، بل **مصدرَ حُكمٍ خاطئ عن حالة
المنصّة**: «ثماني وجهات غير مُنفَّذة».

وقياس الثمانية: كلّها شاشات حقيقية، بين نداءين وستّة نداءات لكل واحدة (94–343
سطرًا)، وتحقّقتُ أن موجّهًا خادميًّا يُجيب **كلّ** نداء من الثمانية والعشرين.
فحالتها صارت `PARTIAL` لا `COMPLETE`: وجودُ نداءات ليس دليل اكتمال، ورفعُها إلى
`COMPLETE` يكرّر العطل في الاتجاه المعاكس.

#### والاستثناء المعماري كان مستندًا إلى الوصف الباطل

تسع صفحات كانت **ساكنة** (غير `lazy`) بحُجّة مكتوبة في `AdminRoutes.tsx`: «غير
مُنفَّذة، وكل واحدة ٢–٣ كيلوبايت وتتشارك `NotImplementedPage`». والحُجّة سقطت
بكاملها. فصارت `lazy` مثل الـ115 الأخرى — والنتيجة **مقيسة** لا مُقدَّرة: حزمة
`AdminRoutes` من **297.91 ك.ب إلى 152.82** (مضغوطة: 69.71 → 37.68)، أي 145 ك.ب
تُنزَّل مع كل فتح للوحة مقابل شاشات لا يفتحها معظم المستخدمين.

#### ثم ظهر ما هو أكبر من البند كلّه

`tools/dashboard-audit/feature-matrix.mjs` مولِّدٌ كُتب **لهذا السبب بعينه**:
«كل أودت سابق لهذه اللوحة كُتب بيدٍ، وكلٌّ منها تقادم في جلسته». ونصفه الاستدلالي
مُشتَقّ من المصدر في كل تشغيل. وفيه بالفعل وضع `--check` يرفض حُكمًا يناقض الكود.

**ولم يكن مربوطًا بـCI.** فأصابه العطل الذي وُجد لمنعه:

* مُحلِّل الصفحات يفهم `import { X } from './pages/X'` **فقط**. وحين صارت كل
  الصفحات `lazy` في دفعةٍ سابقة، صار يحلّ **صفرًا من 123**. فنصف الجدول
  الاستدلالي **فرغ تمامًا** وهو يُقرأ كأنه دليل.
* وأسوأ: انقلب حرسه إلى كاذبٍ في الاتجاه المعاكس. تشغيل `--check` أعطى **163
  مشكلة**، منها «مسجَّلة COMPLETE والصفحة لا تنادي أي API» لصفحاتٍ تنادي كثيرًا.
* وبقي `MISSING` في ثمانية أحكام لأن لا شيء قابَلها بالكود.

أُصلح المُحلِّل ليفهم شكل `lazy`، فصار **123 من 123**، والمشاكل من 163 إلى **2**.

#### والاثنتان الباقيتان كانتا اتّهامين باطلين

الحرس الذي يشتكي بالباطل يُطفَأ، وهو تفسيرٌ معقول لعدم ربطه بـCI أصلًا:

1. `/quality: publishReadiness resolve to no mounted server route` — والنقطة
   موجودة (`adminPublishGate.ts:450`). العلّة أن خريطة التركيب لا تفهم
   `import X, { a } from '…'`، فثلاثة موجّهات مركَّبة كانت **غير مرئية** لها
   (`adminPublishGate`, `publicSite`, `publicRender`) وكل نقاطها «غير مركَّبة».
2. `/calendar: calls rescheduleCalendarEvent which the API client does not
   define` — وهي مُعرَّفة، لكن مسارها يأتي **من صفّ الحدث** لا من المصدر
   (`event.reschedule.route`، وترفض إن غاب). كان المُحلِّل يُسقطها بصمت ثم يتّهم
   الملف الخطأ. صارت تُسجَّل `dynamic` وتُعرَض بذلك الوصف: موجودة، وغير قابلة للحلّ
   ساكنًا. وإظهارها شرط: نقطةٌ لا يستطيع الجدول حلّها يجب أن تبقى مرئية، وإلّا
   قلّل الجدول من حجم ما يدّعي تدقيقه بصمت.

وبعد الإصلاحين: **صفر مشاكل**.

#### تصحيح خطأٍ أدخلتُه أنا في الدفعة 38

قلتُ عن `ADM-101` إن نقاط الخادم **370** لا 508، «بطريقة عدّ مُعلَنة:
`route.<method>(` في `src/routes/*.ts`». وطريقةُ العدّ نفسها كانت معطوبة: تُطابق
موجّهًا اسمه `route` حرفيًّا فقط، فتُسقط `adminUsersRoute` و`familyRoute`
و`episodesRoute` وغيرها. العدّ الصحيح **497** (مخرَج المولِّد، وهو الرقم
القابل لإعادة الإنتاج بأمر واحد)، أي أن رقم الأودت الأصلي 508 كان أقرب من
«تصحيحي». والدرس على نفسي: **تصحيحُ رقمٍ بطريقة عدٍّ غير مُختبَرة ينتج رقمًا
أسوأ**، ولذلك صار الرقم يُقتبس من الأداة لا من بحثٍ في سطر أوامر.

#### الربط بـCI هو العلاج كلّه

مهمّة `feature-matrix` جديدة: `--check` (ترفض الحكم المخالف)، **ثم** إعادة
التوليد ومقارنة الناتج بالمُلتزَم — فلا يبقى الجدول متخلّفًا عن الكود.

وأزلتُ من الناتج سطر «Last generated: <تاريخ>»: كان يجعل الملف يختلف **بالساعة**،
فمهمّة «المُلتزَم يطابق ما يولّده المولِّد» كانت ستفشل غدًا على كل تغيير غير ذي
صلة — وحرسٌ يصرخ بالباطل يُطفَأ. و`git log -1 -- docs/FEATURE_MATRIX.md` يجيب
«متى» بصدقٍ أكبر. وتحقّقتُ أن تشغيلين متتاليين يُنتجان الملف نفسه بالبايت.

#### ما بقي، مقيسًا

**39** مسارًا بحالة `UNVERIFIED` — لا حُكم مسجَّلًا لها، وهو ما يُخرجه المولِّد
بصدق بدل اختراع حالة. وتصنيفها حُكمٌ بشريّ لكل مسار، لا استنتاجٌ من تعبير نمطي.

#### وتقلُّبٌ ثانٍ في نفس ملف الاختبار

`coloring_board_test.dart` فشل مرّةً أخرى في جولةٍ كاملة، وهذه المرّة في اختبارٍ
**آخر** وبإشارة أوضح: `Expected: no matching candidates / Actual: Found 1
CircularProgressIndicator` — الشاشة كانت ما تزال تُحمّل عند التأكيد. رُفع ذلك إلى
`QA-105` كدليلٍ ثانٍ يرجّح «إطارٌ غير منتظَر». والجولة التالية `675/675`.

#### الملفات

`dashboard/front/src/components/PageState.tsx` (حذف المكوّن) · `src/AdminRoutes.tsx` (تسع صفحات إلى `lazy`) · `tools/dashboard-audit/feature-matrix.mjs` (ثلاثة إصلاحات في المُحلِّل + إخراج مستقرّ) · `tools/dashboard-audit/refresh-missing-verdicts.mjs` (جديد) · `docs/FEATURE_MATRIX_VERDICTS.json` (8 أحكام) · `docs/FEATURE_MATRIX.md` (مُعاد توليده) · `النواقص_المتبقية.md` · `.github/workflows/ci.yml` (مهمّة `feature-matrix`).

#### التالي

`CNT-101` بالترتيب من §6.

---

### الدفعة الحادية والأربعون (2026-08-29) — `ADM-106`: البند كان خطأً، والمبدأ الذي يحمله صحيحٌ في موضع آخر

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **675/675** · Worker `npm test` **1370/1370** (+4) · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **339/339** (+3) · وبناء اللوحة ينجح.

#### القياس نقض الدليل

البند يقول: اثنا عشر جدولًا **لا يكتبها شيء**، فالشاشات تعرض صفرًا لا يُميَّز عن حالة
سليمة. ودليله عدد الصفوف في D1 المحلية — وهو **قياسٌ للخطأ**: جدولٌ فارغ ليس جدولًا
بلا كاتب. الأوّل يعني «لم يفعلها أحد بعد»، والثاني يعني «لا يمكن أن تُعرَف».

فقستُ الكاتب لا الصفوف (`INSERT INTO <table>` في `src/**`):

* **أحد عشر من الاثني عشر لها كاتب** في موجّه إداري يبلغه إنسان: `campaigns` من
  `adminCampaigns.ts:65`، و`support_tickets` من `adminSupport.ts:223`،
  و`access_grants` من ثلاثة مواضع، وهكذا. فصفرها **صادق**: لا حملة أُنشئت بعد.
* و`ops_health_checks` كاتبها `scheduled/healthChecks.ts:90` مربوطٌ بجدول
  `*/5 * * * *` وبفرعٍ صريح في `scheduled` — أي أنه يكتب كل خمس دقائق فعلًا.
* والمثال الذي بُني عليه البند («يعرض صفر حوادث بينما `failed_family_events` = 24»)
  **أُصلح في `OPS-106`**: النقطة تقرأ الأحداث الفاشلة وتُنزل `overall_health` إلى
  `degraded` إن وُجدت.
* والوحيد بلا كاتب `translation_units` (INSERT = صفر، UPDATE = 2). ومع ذلك لا تعرض
  شاشته أصفارًا: `adminTranslation.ts` **يتفرّع صريحًا** — إن كان الجدول فارغًا
  حسب الطابور من `story_page_localizations` وبيانات حقيقية.

#### والمبدأ الذي يحمله البند صحيح — في الملف نفسه

المبدأ («الفرق بين *لا يوجد* و*لا نعرف* يجب أن يظهر للمشغّل») لم يكن مكسورًا في
الجداول الفارغة، بل في **`/admin/ops/overview`**: العدّادات تبدأ من `0`، وكل استعلام
محاطٌ بـ`catch {}` **فارغة**. فقراءةٌ فاشلة تُبقي الصفر ويُشحَن كأنه قياس.

وأخطر من الصفر أن `overall_health` يبقى `'healthy'`: الشاشة تُعلن السلامة اعتمادًا
على فشلٍ صامت — في الشاشة التي **لا تُفتَح إلا حين يُشتبه في وجود خلل**.

والدليل أن هذا سهوٌ لا قرار: كتلة `business` في نفس الدالة تفعل الصحيح
(`.catch(()=> null)`)، فالنمطان كانا متجاورين بأربعين سطرًا.

#### الإصلاح على الطرفين، لأن أحدهما وحده لا يُغيّر شيئًا

الخادم: العدّادات تبدأ `null` ولا تصير رقمًا إلا بقراءة ناجحة، و`catch` تسجّل اسم
المسبار وسببه في `unavailable_probes`، وإن فشل أي مسبار فالحالة `unknown` لا
`healthy`. **وترتيب الشدّة محفوظ**: `degraded` لا يُخفَّف إلى `unknown`، لأن ما قِيس
وظهر سيّئًا أقوى دليلًا من قراءةٍ لم تنجح.

واللوحة كانت تكتب `?? 0` على كل عدّاد — أي أنها **تُعيد إنتاج العطل بعد إصلاحه على
بعد طبقة**. صارت `metric()` تطبع شرطةً لغير الرقم، و`reasonFor()` تُسمّي المسبار
الفاشل: شرطةٌ بلا سبب تُقرأ «لا شيء»، وهو الخلل نفسه بشكل ألطف.

#### اختبارات (+7)

الخادم (4، بطلبٍ حقيقي على الموجّه لا بقراءة نصّ المصدر): **الصفر المقيس يبقى صفرًا**
(حرسٌ يحوّل كل صفر إلى شرطة يُخفي الحقيقة من الجهة الأخرى) · القراءة الفاشلة `null`
ومعها اسم مسبارها · الحالة `unknown` لا `healthy` عند فشل مسبار · ومشكلةٌ مقيسة
(24 حدثًا) تفوز على «غير معروف» فتبقى `degraded`.

واللوحة (3): الصفر المقيس يُعرض صفرًا بلا سبب · و`null` يُعرض شرطةً **مع** «تعذّرت
القراءة» ولا رقم فيها · والحالة العامّة «غير معروف» لا «سليم».

#### ما بقي، بقياسٍ لا بعبارة

معيار «كل حالة فارغة تفرّق بين لا نتائج ولا مصدر» يبقى جزئيًّا: **85 موضع `?? 0`** في
32 ملفًا في `src/pages`. وأكثرها صادق (طول مصفوفة، أو عدد من استجابة ناجحة)، فسحبها
بالجملة يقلب العطل إلى ضدّه: شرطةٌ مكان صفرٍ حقيقي. فهي تحتاج **قاعدةً تُميّز المصدر
القابل لأن يكون مجهولًا** ثم مرورًا موضعًا بموضع — كتصنيف الـ370 نقطة في `ADM-101`،
لا بحثًا واستبدالًا.

#### الملفات

`dashboard/api/src/routes/adminOpsReliability.ts` · `dashboard/front/src/pages/OpsPage.tsx` · `dashboard/api/test/opsOverviewUnknown.test.mjs` (جديد، 4) · `dashboard/front/src/test/opsUnknownMetrics.test.tsx` (جديد، 3).

#### التالي

`ADM-107` بالترتيب من §6.

---

### الدفعة الأربعون (2026-08-29) — `ADM-105`: نتيجةٌ خضراء تصف تسعًا وثمانين صفحة من مئة وثماني عشرة

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **675/675** · Worker `npm test` **1366/1366** · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **336/336** (+6) · وبناء اللوحة ينجح.

#### القياس أوّلًا: الثمن كان أرخص بكثير من التقدير

البند حجمه **L** وسببٌ معقول: تسعة وعشرون ملفًا خارج الفحص. رفعتُ التعطيل عن
الـ29 كلّها في خطوة واحدة وشغّلتُ `tsc` لأرى الرقم لا لأقدّره: **ثمانية وأربعون
خطأً في تسعة عشر ملفًا**.

فـ**عشرة من التسعة والعشرين لم يكن فيها خطأ واحد**. تعطيلٌ نُسخ من ملف إلى ملف بلا
سبب، وحمل معه تسعًا وثمانين صفحةً في تقرير أخضر لا يصفها.

#### وما كان تحته ليس أسلوبًا

ستّة وثلاثون من الثمانية والأربعين كانت «مُعرَّف ولا يُقرأ». وهي الفئة التي تُقرأ
كترتيب، فكانت تُخفي:

* **`ObjectiveWorkspacePage`**: `masteryByObjective` يُنادى **ثلاث مرّات** في كل
  فتح — مرّتان في `Promise.allSettled` تُهمَل نتيجتهما (`mRes` بلا قارئ)، وثالثة
  تقرأ. نداءان زائدان إلى الإنتاج مقابل لا شيء.
* **`OpsSlaPage`**: `api.supportFamily('test')` — نداءٌ بمُعرَّف عائلة **مُختلق**
  مُعلَّم `// placeholder`، نتيجته تُهمَل، يُطلق في كل فتح للتبويب.
* **`SchoolAccountsPage`** حالةٌ تُكتب من `meta` ولا تُقرأ، و**`RemoteConfigPage`**
  حسابٌ لمفاتيح الإيقاف بلا عرض، و`window.__ENV__` يُقرأ بلا مستهلك.

وثلاثة أخطاء كانت **أعطالًا يراها المستخدم**:

1. **`RecommendationsPage`**: `persona.age === '9-12'` — رقمٌ يُقارن بنصّ، فالشرط
   **أبدًا غير صحيح**، فمسار الروّاد ٩–١٢ يعرض «لا مرشَّحين» دائمًا. وتصحيح الشرط
   وحده لم يكن يكفي: قائمة المعاينة نفسها بلا صفٍّ للروّاد، فالشاشة تبقى كما كانت.
   صُحّح الاثنان معًا، وإلّا لأصلحتُ الكود وأبقيتُ العطل.
2. **`RemoteConfigPage`**: `text.loadError` مُستخدَمة في ثلاثة مواضع و**غير موجودة**
   في نصوص الصفحة. فشلٌ ليس `Error` كان يُنتج `setError(undefined)`، فتُرسَم
   الصفحة **فارغةً كأن القراءة نجحت**، والحفظ الفاشل يُغلق بلا رسالة.
3. **`BillingPage`**: `<Icon style={...}>` و`Icon` لا يقبل `style`. فالتنسيق يُلقى
   صامتًا: الأيقونة تبقى في مسار المحتوى بينما للمُدخَل حاشية 30px — فراغٌ ثم
   أيقونة في غير موضعها. (ومعها `left` صار `insetInlineStart`: اللوحة عربية أوّلًا.)
   و**`SchoolAccountsPage`** تطلب أيقونة `shield` غير الموجودة في `IconName`، فلا
   تُرسَم أيقونة أصلًا في تنبيه الخصوصية.

#### ورقمٌ مُختلَق في عملية أمنية

`TeamAccessPage` كان يكتب `notice` في ثلاثة مواضع — إنشاء حساب، وإصدار كلمة مرور
مؤقّتة، وسحب الجلسات — **ولا يعرضه في أيٍّ منها**. والسحب بلا تأكيد أسوأ من غيره:
المسؤول لا يعرف أنه حدث فيُعيده.

وحين عُرض ظهر ما هو أسوأ: النصّ «`— 2 sessions`» و«سيُسحب 2 جلسة»، والنقطة تُعيد
`revoked: true` **بلا عدد**. رقمٌ تخترعه الواجهة في عملية أمنية يُبنى عليها قرار.
صار التأكيد يقول ما حدث فقط.

#### الحرس: المطابقة على التعليمة لا على النصّ

`src/test/typeSuppression.test.ts` يمسح **كل** `src` (لا `src/pages` وحدها) ويمنع
تعطيل الملف وتجاهل السطر. وثلاث نقاط في بنائه:

* **المطابقة على شكل التعليمة** (تعليقٌ يبدأ بها) لا على النصّ المجرّد. الدرس
  تكرّر خمس مرّات في هذا الأودت: حرسٌ على النصّ يرصد **التعليقات التي تشرح العطل**
  — وهي مكتوبة في ثلاثة من الملفات المُصلَحة — فتُقشَّر الشروح ويبقى الحرس.
* **الاسم مُركَّب من جزأين** في ملف الاختبار، فلا يرصد الحرس نفسه.
* **اختبارٌ أن الشجرة غير فارغة** (`> 200` ملفًا): حرسٌ يقيس صفرًا من صفر يمرّ
  دائمًا. وتحقّقتُ أنه **يفشل فعلًا**: أُدخلت التعليمة في ملف، فسمّاه الاختبار
  بالاسم، ثم أُزيلت.

و`@ts-expect-error` مسموحة عن قصد: **تفشل عندما يزول الخطأ**، فتُنظّف نفسها. أمّا
تعطيل الملف فيبقى بعد أن يزول سببه بلا أن يُخبر أحدًا. (عددها اليوم صفر على أي حال.)

#### اختبارات (+6)

`typeSuppression.test.ts` (3) · و`typeSuppressionFallout.test.tsx` (3) تحرس ما وُجد
تحت التعطيل لا سكوت المترجم: عمر 10 يُظهر مرشَّح الروّاد ولا يُظهر مرشَّحي ٦–٨ ·
ورفضٌ ليس `Error` يعرض «تعذّر قراءة الضبط البعيد» لا صفحةً فارغة · وسحب الجلسات
يعرض تأكيدًا **بلا أي رقم** (`not.toMatch(/\d/)`).

#### وجدتُ اختبارًا يتقلّب

`coloring_board_test.dart` — «فرشاة تُلوّن والتراجع يستردّ» فشل في جولةٍ كاملة
(`+674 -1`) ونجح منفردًا ونجح في الجولة التالية (`675/675`). ولا سطر Dart في هذه
الدفعة، فهو تقلُّب سابقٌ لها لا انحدارٌ منها. سُجِّل `QA-105`: اختبارٌ يتقلّب هو
حرسٌ لا يُوثَق به، وهو بالضبط عائلة العطل التي يعالجها هذا الأودت.

#### الملفات

19 ملفًا في `dashboard/front/src/pages/` (رفع التعطيل + الإصلاحات) · `src/lib/labels.ts` (`trackLabel`) · `src/test/typeSuppression.test.ts` (جديد، 3) · `src/test/typeSuppressionFallout.test.tsx` (جديد، 3).

#### التالي

`ADM-106` بالترتيب من §6.

---

### الدفعة التاسعة والثلاثون (2026-08-29) — `ADM-102`: التصاريح مفروضة في الخادم وغائبة عن الواجهة

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **675/675** · Worker `npm test` **1366/1366** · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **330/330** (+13) · وبناء اللوحة ينجح.

#### العطل ليس أمنيًّا، والأمان هو ما يجعله سيّئًا

الخادم يحرس بجدية: **196** استدعاءً لـ`requirePermission` في ستّة وثلاثين موجّهًا.
واللوحة كانت تفتح كل شاشة لأي حساب مُصادَق. فلا تسريب — والرفض يقع في الخادم — لكن
الرحلة تنتهي حيث لا يمكن إصلاحها: مراجعٌ يفتح شاشة، يملأ نموذجًا، **يفقد عمله على
403**. والنتيجة التي تتكرّر في كل فريق أن يتعلّم المستخدمون تجاهل الأخطاء، وهي أغلى
من الشاشة التي سبّبتها.

#### نقطة إنفاذ واحدة، لا مئة وثلاث وعشرون

البند اقترح مكوّنًا يغلّف مسارات `AdminRoutes`. رفضتُ التغليف لكل مسار لسببٍ واحدٍ
يكفي: مئة وثلاثة وعشرون موضعًا يعني أن **المسار الرابع والعشرين بعد المئة سيُكتب بلا
حرس**، ولن يلاحظه أحد لأن غياب الحرس يظهر كشاشة تعمل.

`PermissionGate` يُركَّب مرّةً واحدة في `AdminLayout` حول `Outlet`، فيقرأ الموقع من
الموجّه ويسري على كل مسار قائم ومُضاف. وهو **داخل** حدود `Suspense` لا خارجها: عند
الحجب لا يُرسَم `Outlet`، فلا تُنزَّل حزمة الصفحة أصلًا.

#### القاعدة: `null` لكل شاشة قراءة

الصلاحية تُطلَب حيث تكون الشاشة **فعلًا كاملًا** لا يملكه الحساب: الفريق والأدوار
والمنح، ومزوّدو الذكاء الاصطناعي، والفوترة والأسعار، وسجل التدقيق، ووضع الموقع
والضبط البعيد، وخطّ النطق. هناك يكون الفتح وعدًا كاذبًا كاملًا.

وما عداه مفتوح: القوائم وصفحات التفاصيل ولوحات المتابعة. الخادم يحرس الكتابة على أي
حال، و**حجب القراءة يمنع مراجعًا من أن يفهم ما يراجعه**. ومسارٌ غير معروف لا يُحجَب:
شاشة بيضاء لخطأ في الخريطة أسوأ من شاشة يحرسها الخادم أصلًا.

وأسماء الصلاحيات مقروءة من مواضع `requirePermission` لا مُختلقة (ثمانية عشر اسمًا)،
لأن اسمًا مخترعًا يعني حجبًا دائمًا لا يفسّره أحد: لا حساب يملك ما لا يُمنح.

#### والقائمة الجانبية كانت قائمةً ثانية تُصان بيدٍ

`Sidebar` كان يحمل `permission` مكتوبًا على كل عنصر — وقد **خالف الخريطة في خمسة
مواضع**: `team-access` و`governance` و`narration` و`remote-config` و`failed-events`.
عنصرٌ مخفيّ ومساره مفتوح، أو ظاهرٌ ومساره سيُحجَب. صار الحقل محذوفًا والصلاحية
**مشتقّة** من `permissionForPath(item.to)`، فمعيار «القائمة تعكس صلاحيات الحساب»
صحيحٌ بالبناء لا بالمواظبة.

وحيث اختلفت القائمتان أخذتُ **بالأشدّ المشحون** (قرار القائمة الجانبية) لا بالأوسع:
توسيعُ الظهور بالسهو تغييرٌ في السلوك لا إصلاحٌ له. وواحدٌ منها مقيس لا مُقدَّر:
`GET /admin/grants` نفسه محروس بـ`manage_permissions`، فشاشة «الموظفون والتصاريح»
لا تُحمَّل بلا الصلاحية أصلًا.

#### اختبارات (+13)

`src/test/routePermissions.test.tsx`: كل مسار في `AdminRoutes.tsx` معلَن في الخريطة
(يفشل عند إضافة مسار) · **ولا مفتاح بلا مسار** (قاعدةٌ ميتة تُقرأ كأنها سارية) · كل
اسم صلاحية يعرفه الخادم · النمط الأكثر حرفيةً يفوز (`plans/:id` لا يطابق
`blog/posts`) · مسار غير معروف لا يُحجَب · الحجب يعرض الشاشة **ولا يرسم الصفحة**
ويسمّي الصلاحية المطلوبة · المالك يتجاوز بلا صلاحيات مُعدّدة · شاشة قراءة مفتوحة
لحساب بلا صلاحيات · والقائمة تُخفي «سجل التدقيق» عن المراجع وتُظهره لمن يملك
`view_audit_log`.

#### ما لا يفعله هذا الحرس

لا يحمي شيئًا. السلطة في `requirePermission`، وهي ما يبقى إن عُدِّل العميل في
المتصفح. وظيفة الحرس أن يقول «لا تملك هذا» **قبل** أن يُملأ نموذجٌ يُفقَد.

#### الملفات

`dashboard/front/src/lib/routePermissions.ts` (جديد: 123 مسارًا + `SERVER_PERMISSIONS` + `permissionForPath`) · `src/components/PermissionGate.tsx` (جديد) · `src/components/AdminLayout.tsx` (نقطة الإنفاذ) · `src/components/Sidebar.tsx` (حذف الحقل اليدوي + الاشتقاق) · `src/test/routePermissions.test.tsx` (جديد، 13).

#### التالي

`ADM-105` بالترتيب من §6.

---

### الدفعة الثامنة والثلاثون (2026-08-29) — `ADM-101` (جزئي): سؤالٌ لا تُجيبه اللوحة

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **675/675** · Worker `npm test` **1366/1366** · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **317/317** (+7) · وبناء اللوحة ينجح.

#### طريقة الدليل كانت معطوبة، فتضخّمت الفجوة

البند استنتج «ثلاثة موجّهات بلا أي شاشة» من أن البحث في `AdminRoutes.tsx` عن
`availability` و`backup` و`publish` لا يعطي نتيجة. لكن سطوح اللوحة **تبويبات
وحوارات داخل مساحات العمل** لا مسارات عليا:

* `AvailabilityPanel` مركّب في **خمس** مساحات عمل ويستخدم ثلاث نقاط من أربع.
* `publish-readiness` مُستخدَم في `QualityPage` و`PublishReadinessDialog`
  و`PublishReadinessPanel`.

فبحثٌ عن كلمة في ملف مسارات لا يقيس التغطية. والعدد نفسه كان خطأً: **370** نقطة لا
508 (بطريقة عدّ مُعلَنة: `route.<method>(` في `src/routes/*.ts`).

#### والفجوة الحقيقية كانت السؤال المعاكس

لوحة الإتاحة تُجيب: «هل هذه السلسلة ظاهرة في فرنسا؟». والسؤال الذي **لم يكن له
جواب** هو: «ما المحجوب على المنصّة كلّها، وأين؟».

`GET /admin/availability` تُجيبه، وكانت **بلا أي مُستهلِك**: مُنفَّذة في الخادم،
ومُعلَنة في `lib/api.ts` باسم `availabilityPolicies`، ولا شاشة تنادِيها. فالجواب
يقتضي استعلام D1 بيدٍ.

وذلك ليس ترفًا تنظيميًّا: **مسار التشغيل يرفض بـ451 اعتمادًا على هذا الجدول**. فشكوى
«الفيديو لا يعمل عندي» لم يكن يمكن ردّها إلى سببها إلا بالحدس.

#### الشاشة تسرد ولا تحرّر — بقرار

التحرير يبقى في `AvailabilityPanel` داخل مساحة العمل، حيث تظهر **سلسلة الوراثة
كاملة**. وقيدٌ يُحرَّر بلا رؤية ما يورثه الأب قرارٌ نصفُ معلوم: «عالمي» على السلسلة
لا يعني شيئًا إن كان الكوكب محجوبًا. فكل صفّ في القائمة رابطٌ إلى موضع تحريره.

والافتراض العام (`global`) لا رابط له: ليس عنصرًا له صفحة، ورابطٌ يؤدّي إلى 404 أسوأ
من غيابه.

#### ورفضتُ معيارًا من معايير القبول

المعيار الأوّل يطلب «شاشة استعادة تنفّذ استعادة خلف تأكيد». و`POST /restore`
**يُعيد 501 صراحةً**: دفعةٌ سابقة استبدلت بها كتلةً كانت تُبلّغ بنجاح استعادة لم
تحدث («وقد يبني عليها مسؤولٌ قرارًا بحذف مصدر آخر»).

فبناء زرّ استعادة اليوم هو **بالحرف** العطل الذي يُصلحه هذا الأودت في كل دفعة: واجهة
تَعِد بما لا يحدث — وفي أخطر عملية تشغيلية. أُعيد تحديد المعيار: تُنفَّذ الاستعادة
خادميًّا أوّلًا، ثم تُبنى شاشتها.

#### اختبارات

+7 في `src/test/availabilityPolicies.test.tsx`: القائمة تُقرأ من النقطة التي كانت
بلا مُستهلِك · النمط والسبب **بالكلمات لا بالرمز** (‏`unavailable` و`worldwide`
يفترقان في الأثر تمامًا) · النافذة تُعرض حين توجد و«دائمًا» حين لا · كل صفّ يفتح موضع
تحريره · الافتراض العام بلا رابط · **قائمة فارغة تُقرأ «لا قيود» لا «تعذّر القراءة»**
(على شاشة حقوق، الأولى تعني أن المحتوى عالميّ والثانية لا تعني شيئًا) · وفشل القراءة
يُعرض بزرّ إعادة.

وفي بناء الاختبار درسٌ صغير: تأكيدٌ على «من» في الصفحة كلّها نجح بمطابقة النصّ
التعريفي أعلى الشاشة. صار النطاق داخل الصفّ — تأكيدٌ يمرّ بمطابقة لا تعني شيئًا
اختبارٌ لا يحرس شيئًا.

#### ما بقي من البند

الشطر الأكبر: **تصنيف 370 نقطة** — لكل واحدة شاشة أو «برمجي فقط» بسبب مكتوب. وهذا
يحتاج **أداةً تعدّ وتُقابل** لا وثيقةً تُكتب بيدٍ: وثيقةٌ يدوية لثلاث مئة وسبعين
نقطة تتقادم قبل أن تُكمَل — كما تقادم هذا البند نفسه. ومعه بندان صغيران:
`GET /backup` و`GET /quality` بلا مُستهلِك (والثانية تجاوزتها `publish-readiness`).

#### الملفات

`dashboard/front/src/pages/AvailabilityPoliciesPage.tsx` (جديد) · `src/AdminRoutes.tsx` (مسار) · `src/components/Sidebar.tsx` (مدخل + تسميتان) · `src/test/availabilityPolicies.test.tsx` (جديد، 7).

#### التالي

`ADM-102` (التصاريح مفروضة في الخادم وغائبة عن الواجهة في 101 من 118 صفحة) بالترتيب من §6.

---

### الدفعة السابعة والثلاثون (2026-08-29) — `APP-110`: تعليقٌ يقول «ينتظر الخادم» والخادم شحن

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **675/675** (+8) · Worker `npm test` **1366/1366** · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **310/310**.

#### البند نفسه كان أقدم من الكود

`APP-110` عنوانه «نواقص موسومة في الكود بانتظار الخادم». والقياس قلبه:

* `intro_start_ms` و`intro_end_ms` و`recap_*` و`credits_start_ms` و
  `preview_sprite_url` و`preview_sprite_vtt_url` — **كلّها في القاعدة** منذ
  `0047_episode_streaming_contract.sql`، و`0048` أضاف `episode_renditions`.
* `routes/episodes.ts` يُرسلها، و`adminEpisodeStreaming.ts` يملك CRUD كاملًا
  للثلاثة.
* `EpisodeDto.fromJson` **يحلّلها كلّها**، و`playback_page.dart` **يستخدمها**:
  `introRange` تُظهر زرّ «تخطي المقدمة»، و`previewSpriteUrl` تُغذّي معاينة التمرير،
  و`uiSubtitleTracks` تبني قائمة الترجمات، و`uiQualityRenditions` قائمة الجودة.

ومع ذلك بقي في أعلى `playback_page.dart` تعليقٌ يسرد تلك الحقول ويطلب إضافتها إلى
`EpisodeDto.fromJson` «عندما يشحنها الخادم».

#### وهذا أخطر أنواع التوثيق الخاطئ

توثيقٌ يصف عطلًا لا وجود له **يُنتج عملًا**: يقرؤه المطوّر فيبني ما هو مبنيّ، أو
يخبر صاحب المنتج أن الميزة «تنتظر الخادم» — وهي تنتظر **محرّرًا** يضع علامة مقدمة.
والفرق بينهما في الجدول أسابيع.

وهذا خامس موضع من هذه العائلة في الأودت (بعد `content_repository` في 19، و`triage`
في 26، و`gameCatalogProvider` في 31، ومهلة «الخمس دقائق» في 34). ويبدو أن القاعدة
المستخلصة تستحق تسميتها: **كل تعليق يصف حالة نظامٍ آخر يتقادم بصمت، فيجب أن يُربط
باختبار أو يُحذف.**

#### فما هو مُظلم؟ البيانات

القياس على القاعدة المحلية: **٣٣ حلقة**، وصفرُ صفٍّ في `episode_renditions` و
`episode_audio_tracks` و`episode_subtitle_tracks`، وصفرُ حلقةٍ لها
`preview_sprite_url`، وصفرُ حلقةٍ لها `intro_start_ms`.

فالسلسلة كاملة من القاعدة إلى الزرّ، ولا أحد وضع القيمة. وذلك `CNT-101` — عملُ
تحرير على لوحةٍ جاهزة — لا بندُ تطبيق.

#### والحرس يمنع الوهم من الجهتين

`test/streaming_contract_test.dart` يثبّت أن التحليل قائم (فلا يُحذف فيُقال «لم
يُشحن»)، وأن التعليق المُضلِّل لم يعد في المصدر. ومعه حالتان تستحقّان الذكر:

* **مدى مقدمة مقلوب لا يُشتقّ**: نهايةٌ قبل بداية بياناتُ محرِّر خاطئة، ولا يجوز أن
  تُنتج زرًّا يقفز بالطفل إلى الخلف.
* **حلقة بلا أي من الحقول تبقى صالحة للتشغيل** — وهي حالة الإنتاج اليوم، فيجب أن
  تكون مُثبَّتة لا مُفترَضة.

#### ومرّة رابعة: حرسي رصد تعليقي

أوّل صياغة للاختبار ترفض وجود النصّ `Backend TODO` في الملف — و**شرحي الجديد كان
يقتبسه**. لم أخفّف الحرس (فالهدف هنا تعليقٌ لا كود، فلا تُقشَّر التعليقات): أعدتُ
صياغة الشرح. النمط يتكرّر بما يكفي ليكون قاعدة: **حرسٌ على نصٍّ يجب أن يُكتب شرحُه
بغير ذلك النصّ.**

#### ما بقي، بأصحابه لا كبندٍ عائم

| الباقي | صاحبه |
|---|---|
| تعبئة علامات المقدمة والجودة والصوت والترجمة | `CNT-101` — تحرير على لوحة جاهزة |
| إقران التلفزيون | `API-103` — لا نقطة نهاية؛ والشاشة تعلن عدم التوفّر بصدق بدل رمز ثابت |
| ناقل الأحداث التحليلية | قرار مزوّد (`OPS-106`/`PRIV-101`)؛ شكل الحدث الآمن مكتوب |
| فحوص الإقليم ونافذة العرض | **ليست نقصًا**: السلطة للخادم (451/403) والعميل يعرض ما يقوله |

#### معايير القبول

البند لم يحمل معايير قبول (كان جردًا). والمُنجَز: تصحيح الوصف بالقياس، وحذف
التوثيق المُضلِّل، وربط الباقي بأصحابه، وحرسٌ يمنع رجوع أي من الاثنين.

#### الملفات

`app_main/lib/features/playback/presentation/playback_page.dart` (حُذف التعليق المُتقادم وصُحِّح `[AC7]`) · `test/streaming_contract_test.dart` (جديد، 8).

#### التالي

انتهت `APP-*` كلّها عدا `APP-101` (**XL**، أربعة ملفات آلهة — يحتاج قرارك) وشطر
`APP-102` الترتيبيّ. فالتالي بالترتيب من §6: `ADM-101`.

---

### الدفعة السادسة والثلاثون (2026-08-29) — `APP-109`: مهلةٌ واحدة لعمليات لا تتشابه

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **667/667** (+11) · Worker `npm test` **1366/1366** · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **310/310**.

#### ثماني ثوانٍ صحيحة لطلبٍ واحد، خاطئة لطلبين

`Duration(seconds: 8)` كانت المهلة الوحيدة، مطبَّقة على **ستّة عشر موضعًا**. وهي
صحيحة لجلب صفوف الكاتالوج — طفلٌ يقف أمام شاشة، وثمانٍ حدُّ صبره. وخاطئة تمامًا
لموضعين:

* **تصدير بيانات الحساب كلّها** — وليّ أمر طلبها ويعرف أنها تأخذ وقتًا.
* **رفع رسمة الطفل** — حمولةٌ على شبكة جوّال، وكل فشل يخسر عملًا رسمه طفل.

فصارت مهلتان مُصرَّحتان بأسمائهما: `_timeout` (ثمانٍ) و`_extendedTimeout` (خمس
وأربعون). ولا «بلا حدّ» لأي طلب: الحدّ الغائب يُنتج انتظارًا أبديًّا على شبكة تقبل
الاتصال ولا تُجيب — وهو بالحرف ما وجدتُه في تحميل الترجمة في الدفعة 29.

#### والقرار الذي يخالف ظاهر المعيار

المعيار يطلب «إعادة محاولة بتراجع أسّي **للقراءات الآمنة**». وتطبيقه على عمومه كان
سيُسيء: مسار الكاتالوج له بديلٌ **أرخص من الانتظار** — `ContentRepository` يقرأ
الكاش ثم الحزمة عند الفشل. فإعادة محاولةٍ هناك تُجلس الطفل أمام دوّارة سبع عشرة
ثانية بدل أن يرى رفًّا محفوظًا بعد ثمانٍ.

فالإعادة **تُطلَب صريحةً** (`attempts: 2`) حيث لا بديل: قائمة الأجهزة، وسياق
الفوترة الذي يسبق دفعًا. وهناك بديلُ الفشل رسالةُ خطأ، فمحاولةٌ ثانية أرخص منها.
والافتراض `attempts: 1` — أي أن السلوك العام لم يتغيّر، والتغيير محدود بموضعين
يُقرأ سببهما عندهما.

القاعدة المستخلصة: **إعادة المحاولة تنفع حيث لا بديل، وتضرّ حيث البديل جاهز.** وهي
معلومةٌ لا يملكها العميل، فتُمرَّر إليه من موضع النداء.

#### وما لا يُعاد، مكتوبًا

* **أي حالة HTTP**: 4xx طلبٌ مرفوض يُرفَض ثانيةً، و5xx خادمٌ يترنّح تضاعف المحاولةُ
  حمله.
* **كل كتابة**: إعادة إرسالها قد تُنشئ طفلًا ثانيًا أو تخصم مرّتين؛ ومسارات الحذف
  تحمل مفتاح تكرار **لكنها مدمِّرة**، فلا تُعاد آليًّا بحال.

#### تصنيفٌ واحد لا اثنان

أوّل تطبيق التقط `TimeoutException` و`http.ClientException` بنوعيهما، فسقط اختباري:
`SocketException` ليس أيًّا منهما. والحلّ لم يكن إضافة النوع الثالث —
`SocketException` من `dart:io` فلا وجود له على الويب — بل **إعادة استخدام
`AppFailure`**: هي المرجع الواحد لمعنى «شبكة» و«مهلة» في التطبيق، وهي ما تقرأه
الشاشات لتختار رسالتها.

فالعميل يعيد المحاولة على ما تسمّيه الشاشةُ عابرًا. ولو كرّرتُ التصنيف لظهر تفارقٌ
لاحقًا: عميلٌ يعيد المحاولة على خطأٍ تعرضه الشاشة دائمًا، أو العكس.

#### اختبارات

+11 في `test/request_budget_test.dart`: التراجع الأسّي مقيس بأرقامه · قراءةٌ بلا
بديل تُعاد مرّة ثم تنجح · الفشل المتكرّر يُرفَع بعد محاولتين **لا حلقة** · المهلة فشلُ
نقلٍ فتُعاد · **حالة HTTP لا تُعاد أبدًا** · **مسار الكاتالوج لا يُعاد** · الكتابة لا
تُعاد · المهلتان مُعلَنتان · الثقيلان يستخدمان الممتدّة · **لا طلب بلا مهلة** (يعدّ
الطلبات مقابل المهلات في المصدر) · والرسالتان مختلفتان.

#### معايير القبول

| المعيار | الحالة |
|---|---|
| مهلة لكل نوع عملية | ✅ مهلتان مُصرَّحتان، والثقيلان منقولان، ولا طلب بلا حدّ |
| إعادة محاولة بتراجع أسّي للقراءات الآمنة | ✅ **بقرار**: مُعلَنة حيث لا بديل، مُستثناة حيث البديل أرخص من الانتظار |
| رسالة مختلفة للانقطاع عن البطء | ✅ كانت موجودة، وصارت محروسة |

#### الملفات

`app_main/lib/features/home/data/majarra_api_client.dart` (المهلتان، `retryBackoff`، `_retryTransport`، `_isTransient`، وثلاثة مواضع نداء) · `test/request_budget_test.dart` (جديد، 11).

#### التالي

`APP-110` (نواقص موسومة في الكود بانتظار الخادم) بالترتيب من §6 — وهي في الغالب **جردٌ وقرار** لا تنفيذ، لأن كل بند فيها ينتظر عقدًا خادميًّا.

---

### الدفعة الخامسة والثلاثون (2026-08-29) — `APP-108`: شرطٌ `null` دائمًا يُغطّي المُشغِّل

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **656/656** (+6) · Worker `npm test` **1366/1366** · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **310/310**.

#### البند قال «هشّ»، والقياس يقول «مكسور في اتجاه واحد»

الشرط كان:

```dart
ModalRoute.of(context)?.settings.name?.contains('playback')
```

داخل `MaterialApp.router(builder:)`. والبند وصفه بأنه «هشّ وقد يفشل في الاتجاهين».
والحقيقة أضيق وأسوأ: هذا الموضع **فوق الـNavigator**، فلا `ModalRoute` أعلاه —
والقيمة `null` **دائمًا**. فالنتيجة `false` في كل جولة، وأثرها **واحد ومحدَّد**:

> شاشة المُشغِّل تُغطّى بدعوة «أدِر الجهاز إلى العمودي» في الوضع الأفقي على هاتف
> مضغوط — وهو الوضع الذي يفتحه المُشغِّل **بنفسه** في `initState` لمشاهدة الفيديو.

أي أن الحاجز الذي كُتب ليستثني الفيديو كان يمنعه وحده تقريبًا. والفرق بين «هشّ» و
«مكسور دائمًا» ليس لفظيًّا: الأوّل يُجدوَل، والثاني عطلٌ ظاهر لكل من فتح حلقة وأدار
جهازه.

#### وعطلٌ ثانٍ لم يذكره البند

القرار كان معلَّقًا على `MediaQuery` وحده. فحتى لو صحّ الشرط، لا يُعاد حسابه عند
**التنقّل** — يبقى على قيمته حتى يتغيّر حجم أو اتجاه. فالحاجز يستمع الآن إلى
`router.routerDelegate` عبر `ListenableBuilder`: القرار تابعٌ للمسار، فيجب أن
يُعاد بناؤه بتغيّره.

#### القرار من بيانات المسار

`RouteMatchList.fullPath` هو **نمط** المسار الذي طابقه `go_router`
(`'/playback/:episodeId'`) لا الموقع (`'/playback/ep-1'`). فالمقابلة تجري على النمط
في مجموعة مُعلَنة واحدة في `app_router.dart`:

```dart
const landscapeCapableRoutes = <String>{'/playback/:episodeId'};
```

فلا تحليل نصّي لعنوان، ولا `contains` تُصيب أي مسار يحوي الكلمة. والمُشغِّل غير مُسمّى
كمسار (`name:`) في هذا الموجّه، فالنمط هو أثبت ما يُتاح.

**والاختبار يثبّت الافتراض نفسه** لا النتيجة فقط: يبني موجّهًا حقيقيًّا، ينتقل إلى
`/playback/ep-1`، ويوازن `fullPath` بـ`'/playback/:episodeId'`. لو غيّرت ترقية
`go_router` دلالة `fullPath` إلى الموقع، سقط هذا الاختبار قبل أن يسقط المستخدم.

#### اختبارات

+6 في `test/orientation_gate_test.dart`: الرئيسية تمنع · المُشغِّل يسمح **بأي معرّف
حلقة** · `fullPath` نمطٌ لا موقع · **الخروج من المُشغِّل يُعيد المنع** (قرارٌ يُمنَح ولا
يُسحَب أسوأ من قرارٍ لا يُمنَح، لأن كل شاشة بعده تُعرَض بتخطيط مكسور) · لا `ModalRoute`
ولا مطابقة نصّية في المصدر · والحاجز يستمع إلى المُفوِّض.

ودرسٌ مكرّر للمرّة الثالثة: أوّل صياغة للحرس رصدت **التعليق الذي يقتبس الكود
القديم** فبلّغت عن الملف الذي أُصلح. التعليقات تُقشَّر.

#### معايير القبول

| المعيار | الحالة |
|---|---|
| القرار من بيانات المسار لا من مطابقة نصّ | ✅ `fullPath` + مجموعة مُعلَنة، ويثبّت الاختبار دلالة `fullPath` نفسها |
| اختبار يغطّي الدخول والخروج | ✅ ومعه أي معرّف حلقة، وإعادة البناء على التنقّل |

#### الملفات

`app_main/lib/app/router/app_router.dart` (`landscapeCapableRoutes` + `routeAllowsLandscape`) · `app_main/lib/app/majarra_app.dart` (الحاجز يقرأ المسار ويستمع للمُفوِّض) · `test/orientation_gate_test.dart` (جديد، 6).

#### التالي

`APP-109` (مهلة شبكة واحدة ثابتة ٨ ثوانٍ لكل الطلبات) بالترتيب من §6، ثم `APP-110`.

---

### الدفعة الرابعة والثلاثون (2026-08-29) — `APP-107`: نافذة وليّ الأمر تبقى مفتوحة في الخلفية

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **650/650** (+7) · Worker `npm test` **1366/1366** · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **310/310**.

#### تنازلٌ صحيح في سببه، خاطئ في مداه

إبطال إثبات وليّ الأمر كان مقصورًا على `AppLifecycleState.detached` — أي على إغلاق
التطبيق فعليًّا. والسبب المكتوب صحيح ومقيس: على الويب تُطلق `inactive` عند كل فقدان
تركيز، فتُبطل الإثبات وتُنتج **حلقة** في شاشة الرمز (إدخال → تنقّل → فقدان تركيز →
إبطال → عودة إلى الشاشة).

لكن العلاج شمل الجوال بلا حاجة. فوليّ أمر يفتح منطقة الوالدين ثم ينتقل إلى تطبيق
آخر أو يضع الجهاز جانبًا يترك الوصول الأبوي **حيًّا** حتى ينتهي المؤقّت. والمنطقة
تحمل وقت الشاشة، ووقت النوم، وحذف الحساب — والجهاز في تلك الدقائق قد يكون في يد
الطفل.

#### والتعليق كان يجعل التنازل مقبولًا بقيمةٍ خاطئة

آخر سطر في ذلك التعليق: «مؤقّت **الخمس** دقائق يحدّ النافذة أصلًا». و
`AuthGuard.parentAccessDuration` = **خمس عشرة دقيقة**.

فالقارئ يوازن خطرًا مقداره ثلث الحقيقة. وهذا نوع خاصّ من العطل: التعليق لا يكذب في
وصف السلوك، بل في **حجم التنازل**، فيقبله المراجع لأنه يبدو صغيرًا. وهو رابع موضع
من عائلة «توثيق يخالف الكود» في هذا الأودت (بعد `content_repository` في 19، و
`triage` في 26، و`gameCatalogProvider` في 31).

#### القرار: `paused` تُبطل على الجوال، والويب يبقى على `detached`

`paused` هي اللحظة التي لم يبقَ فيها التطبيق في المقدّمة — وهي بالضبط لحظة انتقال
الجهاز إلى يدٍ أخرى. وكلفتها معروفة ومقبولة: وليّ الأمر الذي يعود بعد ثوانٍ يُعيد
إدخال الرمز، **وإعادة إدخال رمزٍ أرخص من نافذةٍ مفتوحة على ضوابط طفل**.

والويب مستثنًى **لسببٍ مقيس لا احتياطًا**: العطل الأصلي كان حلقةً موصوفة، ولا نملك
اليوم بيئة ويب مُختبَرة نتحقّق فيها أن `paused` لا تُطلقها. فيبقى على `detached` حتى
يُختبَر، والاستثناء مكتوب بسببه — استثناءٌ بلا سبب مكتوب يُنسَخ لاحقًا إلى موضع لا
يخصّه.

و`inactive` غير مشمولة قصدًا: تُطلقها إشعارات النظام ومركز التحكّم على iOS والتطبيق
ما زال في يد وليّ الأمر.

#### الحرس: يوازن النصّ بالقيمة

الاختبار يستخرج **كل** مدّة بالدقائق مذكورة في `majarra_app.dart` ويوازنها بـ
`AuthGuard.parentAccessDuration`. فأي تعليق يذكر رقمًا مخالفًا يُفشل الجولة — وهو
الخطأ نفسه الذي رصده البند.

ومعه شرطٌ أن تُكتب المدّة **بالكلمات** أيضًا («خمس عشرة دقيقة»): الأرقام تُعدَّل بحثًا
واستبدالًا فتنجو الكلمات وتُقرأ، والعكس يُنتج ما كان.

#### اختبارات

+7 في `test/parent_access_lifecycle_test.dart`: لا مدّة في التعليق تخالف الثابت ·
المدّة مكتوبة بالكلمات · `paused` تُبطل على غير الويب و`detached` في كل مكان ·
`inactive` لا تُبطل · استثناء الويب مكتوب بسببه · والإبطال يُسقط الإثبات · والمدّة
القصوى في موضع واحد.

#### معايير القبول

| المعيار | الحالة |
|---|---|
| الإبطال على `paused` في الجوال، والويب باستثناء مشروح | ✅ |
| التعليق والقيمة متطابقان، واختبار يربطهما | ✅ الاختبار يقرأ الرقم من المصدر ويوازنه بالثابت |

#### الملفات

`app_main/lib/app/majarra_app.dart` (شرط دورة الحياة + التوثيق) · `test/parent_access_lifecycle_test.dart` (جديد، 7).

#### التالي

`APP-108` (حاجز الاتجاه يعتمد على مطابقة نصّ المسار) بالترتيب من §6، ثم `APP-109` (مهلة شبكة واحدة ثابتة لكل الطلبات).

---

### الدفعة الثالثة والثلاثون (2026-08-29) — `APP-105`: تحليلٌ نظيف فوق ٦٠٠ سطر ميت

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **643/643** (+3) · Worker `npm test` **1366/1366** · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **310/310** · و`flutter pub get` ينجح بعد إسقاط الاعتمادية.

#### العلّة: التحذيرات لم تُصلَح، أُسكتت

`flutter analyze --fatal-warnings` كان يمرّ بصفر مشكلة **وفي الشجرة نحو ٦٠٠ سطر لا
يناديها أحد**. السبب أن أحد عشر `// ignore: unused_element`/`unused_field` موزّعة على
خمسة ملفات كانت تُخفيها، وكلٌّ منها بتعليقٍ يبرّر البقاء:

* «retained as canonical fallback builder (offline safety)» — على سبع دوالّ في
  `creative_studio_page.dart`.
* «kept for API compatibility» — على ثلاثة حقول في `reference_catalogue_page.dart`.
* «retained for audit trail» — على حقل يحمل **قدرة وصول إلى وسائط** في
  `playback_page.dart`.
* «Reserved for Phase 2 discovery layout» — على قسم كامل في `home_feed.dart`.

ودالّةٌ لا يناديها أحد **ليست بديلًا**: هي غير قابلة للوصول. والتعليق الذي يسمّيها
بديلًا أسوأ من الكود الميت نفسه، لأنه يمنع القارئ التالي من حذفها ويجعله يظنّ أن
هناك مسارًا ثانيًا للأمان.

#### والحذف هنا حافظٌ للسلوك بحكم البناء

هذا ما يفرّق هذه الدفعة عن `APP-101`: `unused_element` تعني أن **لا مرجع** في
الوحدة، فما لا يُنادى لا يتغيّر سلوكه بحذفه. فلا حاجة إلى تغطية اختبارية أوّلًا —
المحلّل هو البرهان. وحُذف بالتتالي: كل حذفٍ يكشف ما كان يستند إليه فقط
(`_ExploreMajarraSection` → `_ExploreCard` → `_ExploreDest`، و`_showComparison` →
`_captureCurrentDrawing` → `_parseHex` → استيراد `dart:ui`).

#### وإسكاتان كانا يحرسان تحذيرًا لم يعد موجودًا

أخطر ما في التراكم ظهر عند الحذف: `_offlinePath` و`_filterAge` كانا **مُسكَتين وهما
مقروءان فعلًا**. أي أن ثلاثة إسكاتات متجاورة كان أحدها ميتًا، ولا شيء يقول أيُّه لا
يزال لازمًا. وقد كشفني ذلك بنفسه: حذفتُ `_filterAge` ثقةً بإسكاتها فسقط البناء
بثلاثة مراجع حيّة، فأُعيدت.

فالدرس المكتوب في الاختبار: **الإسكات لا يُراجَع، والحذف يُراجَع نفسه**.

#### والحرس القائم رصد استثناءاته الميتة

`engine_content_separation_test.dart` فيه قائمة سماح لمعرّفات مسموحة، ومعها اختبار
«لا استثناء متقادم». وبعد الحذف فشل **مرّتين** بأسماء استثناءات لأشياء لم تبقَ
(`_categories`, `_ages`, `quickColors`). حرسٌ يحرس نفسه — وهو ما يفصل قائمة سماح
مقبولة عن قائمة تتحوّل إلى قمامة.

#### تصحيح: «الصفحات المتوازية المكرَّرة» ليست مكرَّرة

البند يقول: «لا يمكن معرفة أي نسخة هي الحقيقية، وأي تعديل يجب تكراره مرتين». عددتُ
مراجع الملفات الثمانية: **لا واحد منها مهجور**. `coloring_home_v2.dart` يحمل النماذج
المشتركة ويستورده ٨ مواضع، و`trace_home_page.dart` **يستورد** `trace_home_v2.dart`
ويُغلِّفه، و`my_boards_page.dart` و`my_boards_v2.dart` **كلاهما** مستورَد من نفس
الصفحة في موضعين.

فالبنية طبقةٌ حيّة (`_v2` نماذج وعرض · `_live` مُغلِّف شبكي) لا نسخٌ مهجورة. ودمجها
إعادة هيكلة في شيفرة بلا تغطية — فأُعيد تحديد المعيار وربطه بـ`APP-101` بدل أن
يُشطَب بحذفٍ لا يجوز.

#### والاعتمادية الميتة

`hive_flutter` مُعلَنة بصفر استخدام في الشجرة كلّها: وزنٌ في الحزمة، وسطحُ توريد،
وتحديثٌ يُراجَع كل مرّة بلا سبب. حُذفت، و`pub get` ينجح.

#### اختبارات

+3 في `test/lib_hygiene_test.dart`: لا ملف غير `.dart` في `lib/` (باستثناء
`lib/l10n/*.arb` — **مصدر** الترجمة باصطلاح Flutter لا مخلَّف) · لا `ignore` لتحذيرات
الكود الميت خارج ملفات `l10n` المُولَّدة (وهي تُعاد توليدًا فيُعاد إسكاتها) ·
و`hive_flutter` ليست في `pubspec.yaml`.

#### معايير القبول

| المعيار | الحالة |
|---|---|
| لا ملف غير `.dart` ولا `.new` في `lib/` | ✅ ويحرسه اختبار |
| `hive_flutter` محذوفة | ✅ ويحرسه اختبار |
| صفر `unused_*` في التحليل | ✅ **وصفر إسكاتٍ لها** — وهو ما كان يُخفي ٦٠٠ سطر بتحليل أخضر |
| نسخة واحدة لكل شاشة استوديو | ⛔ أُعيد تحديده: لا نسخة مهجورة؛ الدمج إعادة هيكلة مع `APP-101` |

#### الملفات

حُذف `app_main/lib/features/playback/presentation/playback_page.dart.new` · نُقل `README_V2.md` → `docs/COLORING_V2_DESIGN.md` · `pubspec.yaml` (‏−`hive_flutter`) · `home/presentation/widgets/home_feed.dart` (‏−٢٠٤) · `games/presentation/pages/creative_studio_page.dart` (‏−٤٠٧) · `games/presentation/pages/reference_drawing_page.dart` (‏−٧١) · `games/presentation/pages/reference_catalogue_page.dart` (حقلان + دالّة) · `playback/presentation/playback_page.dart` (‏`_capabilityToken` + إسكات متقادم) · `test/lib_hygiene_test.dart` (جديد، 3) · `test/engine_content_separation_test.dart` (ثلاثة استثناءات متقادمة).

#### التالي

`APP-107` (تنازلات دورة الحياة وتوثيق يخالف الكود في بوابة وليّ الأمر) بالترتيب من §6، ثم `APP-108`/`APP-109`.

---

### الدفعة الثانية والثلاثون (2026-08-29) — `APP-104`: لوحة تَعِد بصفٍّ لا يظهر أبدًا

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **640/640** · Worker `npm test` **1366/1366** (+3) · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **310/310**.

#### البند وصف دَينًا داخليًّا، والواقع وعدٌ عبر ثلاث طبقات

`APP-104` مكتوب كـ«كود مُعطَّل في التطبيق»: نوعان يعيدان `false`، وبلوكٌ يعيد
`SizedBox.shrink()`. وهذا صحيح وناقص. الأنواع الثلاثة — «الأكثر مشاهدة»، «لأنك
شاهدت»، «رحلة التعلّم» — كانت **مبنيّة ومُعلَنة في الطبقات الثلاث**:

* **اللوحة** تعرضها في مُنتقي الأنواع بأسماء عربية.
* **الخادم** يقبلها في `HOME_BLOCK_TYPES` ويصنّفها في `SYSTEM_BLOCK_TYPES`.
* **التطبيق** يخطّطها إلى أنواع مُعطَّلة بثابت.

فمحرّرٌ يستطيع إضافة «الأكثر مشاهدة»، وتسميتها، وجدولتها، واستهدافها، ونشرها — **ولا
تظهر على أي جهاز، ولا خطأ في أي مكان**. وهذا ليس دَينًا داخليًّا يُقاس بكلفة الصيانة:
هو واجهةٌ تَعِد بما لا يحدث. وفي التخطيط الافتراضي للتطبيق نفسه كانت **ثلاثة صفوف**
منها.

#### السبب الجذري لم يكن `API-105`

البند يعلّق نفسه على `API-105`، وهو **مُنجَز** (الدفعة 24: `child_progress_projection`
بكاتب فعلي). ومع ذلك لا مصدر للثلاثة:

| النوع | لماذا لا يُكمَل |
|---|---|
| `most_watched` | يحتاج **تجميعًا بين الأسر** — قرارُ خصوصية، لا نقطة نهاية ناقصة |
| `learning_journey` | لا مصدر له أصلًا؛ كان يعرض تقدّمًا ثابتًا `0.34` |
| `because_you_watched` | يقرأ **نفس** مصدر `recommended` العاملة — صفٌّ ثانٍ من مصدر واحد اختيارُ منتج لا فجوة |

فالقرار الحذف. والبند نفسه يقول: «الوضع الحالي أسوأ الاثنين».

#### السحب دون كسر ما هو محفوظ

هذه هي العقدة الحقيقية: `HOME_BLOCK_TYPES` **مرآةٌ لقيد CHECK** في القاعدة، ويثبّت
ذلك اختبارٌ يقرأ ملف الترحيل ويوازن. فإسقاط الثلاثة منها يُفشل الاختبار، وتضييق
القيد بترحيلٍ يجعل الصفوف المحفوظة في الإنتاج **غير مقروءة**.

فالحلّ فصلُ سؤالين كانا مدموجين في قائمة واحدة:

* `HOME_BLOCK_TYPES` — **ما تقبله القاعدة**. لم تُلمس، والاختبار باقٍ.
* `OFFERED_BLOCK_TYPES` — **ما يجوز إنشاؤه**، مشتقّة بالطرح لا مكتوبة ثانيةً: قائمتان
  تُصانان بيدٍ تفترقان، والافتراق هنا يظهر كصفٍّ يضبطه محرّر ولا يراه أحد.

فالكتابة تُفحَص على المعروض، والقراءة تبقى على المقبول. ورسالة الرفض مستقلّة: «نوعٌ
سُحب: لا إصدار من التطبيق يعرضه» لا «نوع غير صالح» — الثانية تُرسل المشغّل يبحث عن
خطأ إملائي في اسمٍ كان صحيحًا بالأمس.

#### وفي اللوحة: تُسحب من المُنتقي، وتبقى مقروءة

المُنتقي يُبنى من `meta.block_types` (قرار سابق صحيح)، فتوقّف عرضها **بلا تعديل في
اللوحة**. والأسماء العربية **بقيت** عن قصد: صفٌّ قائم في الإنتاج يجب أن يُقرأ
بعنوانه لا بمعرّفه. ومعها شارة `role="alert"` تقول إنه نوع مسحوب وتطلب حذفه أو
تغييره — فالمحرّر يرى **سبب** غياب الصفّ بدل أن يظنّه معطوبًا.

#### والصفوف المحفوظة تنكسر بأمان بحكم التصميم

`_blockTypeMap` في التطبيق كان أصلًا يُسقط ما لا يعرفه ويُدرجه في
`HomeLayout.unsupportedTypes` — «فالفجوة مرصودة لا صامتة» كما يقول تعليقه. فحذف
التخطيطات الثلاثة **يستخدم** ذلك المسار لا يخالفه: صفٌّ محفوظ بنوع مسحوب يُتجاهَل
ويُذكَر بالاسم.

والاختبار الذي كان يثبّت السلوك القديم (أن الثلاثة «تُخفى») لم يُحذف بل **حُوّل** إلى
تثبيت هذه الخاصيّة: تخطيطٌ فيه الثلاثة يُنتج بلوكًا واحدًا ويُبلّغ عن ثلاثة أنواع غير
مدعومة. نمطٌ متكرّر في هذا الأودت: تحويل الحرس من تثبيت الموضع إلى تثبيت الخاصيّة.

#### ومعها ٢٢٢ سطرًا ميتة

`_LearningJourneyCard` و`_MostWatchedRail` كانا مُبقيَين بـ`// ignore: unused_element`
— أي أن أحدًا رأى التحذير وأسكته بدل أن يحذف. حُذفا.

#### ما بقي من البند، بدقّة

* شارة «جديد» محيَّدة لعدم وجود حقل تاريخ إصدار — يحتاج عمودًا في المخطَّط وكاتبًا له.
* أفاتار طفل placeholder في `home_destination_spec.dart` — **صار قابلًا للحلّ** بعد
  الدفعة 30: `familyChildrenProvider` في `child/application/` يحمل `avatarId`.

#### اختبارات

+3 في `test/homeExperience.test.mjs`: النوع المسحوب يبقى مقبولًا في القاعدة ولا
يُعرَض · القائمة المعروضة **مشتقّة** لا مكتوبة ثانيةً · والمسحوب لا يُصنَّف «نظامًا»
(نوعٌ بلا مُنتقي محتوًى **وبلا عارض** ليس نظاميًّا، هو غائب). وفي Flutter: الاختبار
المحوَّل أعلاه.

#### معايير القبول

| المعيار | الحالة |
|---|---|
| إمّا إكمال المصدر أو حذف الكود المُعطَّل | ✅ حُذف من الطبقات الثلاث، والسبب لكل نوع مكتوب |
| لا يستطيع محرّر إنشاء صفٍّ لا يُعرَض | ✅ رفضٌ على الكتابة برسالة تشرح السحب |
| الصفوف المحفوظة تبقى مقروءة | ✅ القيد لم يُضيَّق، والتطبيق يُبلّغ عن النوع غير المدعوم |

#### الملفات

`app_main/lib/features/home/domain/feed_blocks.dart` · `home/application/home_layout.dart` · `home/presentation/widgets/home_feed.dart` (‏−٢٢٢ سطرًا) · `dashboard/api/src/lib/homeExperience.ts` (‏`RETIRED_BLOCK_TYPES` + `OFFERED_BLOCK_TYPES`) · `api/src/routes/adminAppExperience.ts` (المعروض + رسالة السحب) · `dashboard/front/src/types/api.ts` · `front/src/pages/AppExperiencePage.tsx` (شارة النوع المسحوب) · `api/test/homeExperience.test.mjs` (+3) · `app_main/test/home_layout_test.dart` (حرس محوَّل).

#### التالي

`APP-105` (مخلَّفات وتكرار داخل `lib/`: ملف `.new`، ووثيقة داخل الكود، و`hive_flutter` ميتة، وأربع صفحات استوديو مكرَّرة) بالترتيب من §6.

---

### الدفعة الحادية والثلاثون (2026-08-29) — `APP-103`: كاتالوج مبندل يُحشَر والشبكة سليمة

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **640/640** (+12) · Worker `npm test` **1363/1363** · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **310/310**.

#### أوّلًا: تصحيح البند نفسه

البند قال «الأخطر أن الفشل صامت — الطفل لا يعرف أنه يرى نسخة قديمة». وذلك **غير
صحيح على الرئيسية**: `_FallbackNotice` موجود منذ قبل هذه الدفعة، بنصّ صريح وزرّ
تحديث و`liveRegion` للقارئ الصوتي.

وقال إن ثلاثة اختبارات تفشل في هذا المسار. **الثلاثة تنجح**، وهي أصلًا تقيس
كاتالوج الأنشطة المرجعية بصيغة JSON لا `local_catalog.dart`. دليلٌ قديم بقي في
البند شهورًا.

وكذلك كدتُ أبني هذه الدفعة على تعليقٍ في `content_repository` يقول إن معرّفات
الحزمة «ليست معرّفات صفوف `games` في الخادم، وإخفاؤها يمنع 404 مضمونًا». فحصتُ
الستّة والثلاثين معرّفًا مقابل ملفات الترحيل: **كلّها موجودة**. التعليق كان صحيحًا
يومًا ثم سبقته الترحيلات. ولو صدّقته لكتبتُ في هذا التقرير عطلًا لا وجود له.

#### وثانيًا: ما كان أخطر ممّا وصفه البند

البند يقول «عند أي انقطاع». والحقيقة أن `play_page.dart` كان يدمج
`LocalCatalog.experiences` **بلا أي شرط**، ثم `catalog.experiences`، ثم إن خلا الكل
أضاف **ثلاث ألعاب مكتوبة في الكود** كـ«بديل مطلق». وتعليقه يقول عن المبندل
«always exists» كأن ذلك ميزة.

فالطفل يرى ألعابًا من الحزمة **والشبكة سليمة تمامًا**. وردّ `fetchGames(childId:)`
هو ما يطبّق حالة النشر والمسار العمري والاستحقاق، و`ExperienceItem` المبندل لا
يحمل حالةً ولا عمرًا — فلا شيء منها يُطبَّق عليه. أي أن سياسة النشر كانت تُتجاوز في
**كل** جولة، لا في الانقطاع.

#### وثالثًا: توثيقٌ يصف عكس ما يفعله الكود

فوق `gameCatalogProvider` مكتوب:

> «الأخطاء الشبكية والمصادقة تبقى أخطاءً … فطلبٌ فاشل **لا يُحيي أبدًا** الشرائح
> المبندلة.»

وتحته مباشرةً `catch (_) { }` يُعيد `const []`، وتعليقه الداخلي يقول إن الصفحة
«ستعود إلى LocalCatalog». أي أن الوصف يصف النقيض. وهذا ثالث موضع من نفس النوع في
هذا الأودت (بعد `content_repository` في الدفعة 19 و`triage` في 26): **توثيقٌ يُطمئن
قارئه فيمتنع عن الفحص، وهو أسوأ من غياب التوثيق**.

فالفشل يُرفَع الآن كما يقول وصفه، وتقرّر الشاشة.

#### القاعدة: الخادم مصدر، والمبندل بديل انقطاعٍ مُعلَن

`resolvePlayableGames` دالّة نقية تُوحّد المصادر بشرطين معًا: **لا شيء حيّ**،
**و**الكاتالوج يعلن أنه مبندل (أو مزوّد الألعاب أخفق). فردٌّ فارغ صحيح — والطفل
التجريبي يُعيد `[]` بقصد — لا يُعتبر انقطاعًا فتُحشَر له الحزمة.

وحُذف «البديل المطلق»: مصدرٌ رابع لا يزيد يقينًا، يزيد احتمال أن يُعرَض على الطفل ما
لا يعرفه الخادم ولا يستطيع أحد سحبه.

وشاشة «العب» صارت تُعلن استخدام الحزمة **بنفس نصّ الرئيسية وزرّها**: نفس الحالة
تُقال بنفس الكلمات، وإلا قرأ وليّ الأمر رسالتين لسببٍ واحد.

#### نطاق الأصول: أربعة مواضع لا اثنين

البند ذكر موضعين. والجرد وجد **أربعة**: `_cdnBase` في `content_dtos.dart`،
و`kR2Base` في `image_slot.dart`، و**إحدى وخمسون** نصًّا حرفيًّا في
`local_catalog.dart`، ومقارنةَ مضيفٍ مسموح في `bundled_story_assets.dart`.

والأخير أخطرها: **موضع تحقّق**. فتغيير النطاق مع نسيانه لا يكسر الصور فحسب، بل
يجعل كل أصلٍ صحيح **مرفوضًا** بلا سبب ظاهر.

والمصدر الآن `AppConfig.assetBaseUrl` و`assetHost`. و`const` لا getter عن قصد:
خرائط الأغلفة `const`، وgetter كان يُجبر على إسقاط `const` عن كاتالوج كامل. ولا
تجاوز من بيئة التشغيل: عنوان الأصول ليس نقطة تكامل مع مشغّل، وكل تجاوزٍ مقبول هنا
يفتح بابًا لتوجيه صور الأطفال إلى أصل آخر.

#### ما لم أفعله — وهو قرارك

الحزمة **لا تحمل حالة نشر ولا عمرًا**، فلا يمكن الضمان الكامل بأن عنصرًا سُحب من
النشر لا يظهر فيها. والحلول الثلاثة (هيكل فارغ · مكتبة معلَّمة · حزمة تحمل حالة
تُراجَع) تختلف في **ما يراه طفل بلا إنترنت** لا في الكود. مُسجَّلة في §5 كقرار مالك.

والوضع الحالي هو الأوسط: يرى محتوًى، ويعرف أنه محليّ.

#### اختبارات

+12: `test/play_catalog_test.dart` — الحزمة لا تُضاف حين ينجح الخادم · ولا حين
يكون الكاتالوج مبندلًا والألعاب حيّة · وردٌّ فارغ صحيح يبقى فارغًا · والمبندل
يُستخدم عند الانقطاع **ويُعلن نفسه** · وحزمةٌ فارغة لا تُعلن إشعارًا بلا محتوى ·
والتطبيع يعدّ `letter-tracing` و`game-letter-tracing` لعبةً واحدة · ولا ألعاب
مكتوبة في الكود في الصفحة · والصفحة تقرأ `hasError` صريحًا · ولا نصّ حرفيّ لنطاق
الـCDN في `lib/` خارج ملف الإعداد.

#### معايير القبول

| المعيار | الحالة |
|---|---|
| استخدام البديل المبندل مرئي لا صامت | ✅ الرئيسية كانت تُعلنه، و«العب» تُعلنه الآن |
| نطاق CDN واحد من `AppConfig` | ✅ ويحرسه اختبار يرفض أي نصّ حرفي في `lib/` |
| لا محتوى غير منشور في أي وضع | 🟡 لم يعد يُحشَر مع الحيّ ولا يُستخدم إلا عند فشلٍ فعلي؛ والضمان الكامل قرار مالك |

#### الملفات

`app_main/lib/features/games/application/play_catalog.dart` (جديد) · `games/application/game_providers.dart` (حُذف `catch (_)`) · `home/presentation/pages/play_page.dart` (الدمج المشروط + إشعار + حذف الألعاب المكتوبة) · `core/env/app_environment.dart` (`assetBaseUrl`/`assetHost`) · `home/data/content_dtos.dart` و`home/data/local_catalog.dart` و`games/presentation/pages/studio_v2/image_slot.dart` و`core/media/bundled_story_assets.dart` (من المصدر الواحد) · `test/play_catalog_test.dart` (جديد، 12).

#### التالي

`APP-104` (بلوكات مبنية ثم مُعطَّلة بثابت في الكود لأن بياناتها غير موجودة) بالترتيب من §6.

---

### الدفعة الثلاثون (2026-08-29) — `APP-102` (تكملة): ضابط أمان للطفل يفشل صامتًا

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **628/628** (+10) · Worker `npm test` **1363/1363** · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **310/310**.

#### العطل: وليّ الأمر يضبط حدًّا، ولا يُحفَظ، ولا يُخبَره أحد

ستّ كتابات لضوابط رقابية — وقت الشاشة اليومي، بداية نافذة النوم ونهايتها ومسحها،
السماح بتغيير السرعة، التشغيل التلقائي — كانت مكتوبة داخل `onChanged`/`onPressed`
في شجرة العناصر. **خمسٌ منها بلا أي `catch`**، والسادسة بـ`try/finally` بلا
`catch`.

وأثره ليس تقنيًّا: يسحب وليّ الأمر الشريط إلى ٣٠ دقيقة، يفشل الطلب، فيهرب
الاستثناء من مُعالِج حدثٍ غير متزامن إلى منطقة أخطاء التطبيق، **وتُعيد الشاشة رسم
القيمة القديمة بلا كلمة**. فيخرج وهو يظنّ الحدّ ساريًا، والطفل يشاهد بلا حدّ.

وهذا أخطر ما في `APP-102`: البند يصف «طبقات غير متسقة»، والأثر الفعلي **ضابط أمان
للطفل يفشل صامتًا**.

#### مسار واحد، لأن ستّة مسارات تعني ستّ فرص للاختلاف — وقد اختلفت

`ChildControlsController` يملك المسار كلّه: علَم الحفظ، ورسالة الفشل، والإبطال.
والقرارات فيه:

* **لا يُعيد رفع الاستثناء.** نداءٌ من مُعالِج حدث لا يملك من يلتقطه، ورفعُه هو ما
  جعله صامتًا أوّلًا. يضع رسالةً في حالته لتعرضها الشاشة، ويسجّل عبر قناة
  `APP-106`.
* **`childSettingsProvider` لا يُبطَل إلا عند النجاح.** إبطاله بعد فشلٍ يُعيد جلب
  نفس القيَم القديمة، فيبدو أن الشاشة «حدّثت» ولم يتغيّر شيء — وهو أسوأ من عدم
  التحديث لأنه يُقرأ نجاحًا.
* **الحالة لكل طفل** (`family` على معرّفه). كانت في العنصر، فأسرةٌ بأكثر من طفل
  تشترك في علَم حفظٍ واحد.
* **الرسالة من `AppFailure` لا من `toString()`**، وتُمسَح عند أوّل نجاح: رسالة فشلٍ
  باقية بعد نجاحٍ تجعل وليّ الأمر يشكّ في ضبطٍ حُفِظ فعلًا.

#### واسم الحقل يُكتب مرّة واحدة

`updateChildSettings` تقبل `Map<String, Object?>` أيًّا كان، والصفحة كانت تبنيها في
ستّة مواضع بمفاتيح نصّية حرفية. ومفتاحٌ مكتوب بيدٍ في ستّة مواضع **لا يخطئ مرّة
فيُكتشف**: يخطئ في موضع واحد فتبقى الخمسة شاهدةً على أنه «يعمل».

وهذا نفس شكل العطل الذي أخفى حفظ تقدّم المشاهدة شهورًا (`childId` مقابل `child_id`
— `SEC-110`). فصار لكل ضبط دالّة مسمّاة في `ChildControlsRepository`، واسمُ الحقل
في موضع واحد يثبّته اختبار. ومسحُ نافذة النوم في **طلب واحد**: بطلبين تبقى نافذةٌ
نصفها محدَّد إن فشل الثاني — حالةٌ لم يقصدها وليّ الأمر ولا يراها.

ومفتاح تكرار الحذف يُولَّد في المستودع لا في الصفحة: هو ما يمنع حذفًا ثانيًا عند
إعادة المحاولة، وتركُه في عنصر واجهة يجعله سهل النسيان في أي نداء جديد.

#### `familyChildrenProvider` لم يكن تفصيلًا في صفحة

كان مُعلَنًا في `child_switcher_page.dart`، ويقرأه `app_router.dart` **ليقرّر ما إذا
كانت الأسرة أكملت التهيئة**. أي أن قرار التوجيه الأوّل في التطبيق يعتمد على مزوّدٍ
يسكن ملفَ صفحة.

والدليل ظهر عند النقل: **أربعة ملفات كانت تستورد ملفَ الصفحة ولا تستخدم منه شيئًا
غير المزوّد**، فسقط استيرادها بعد النقل. ومعه انتقل `ChildProfile` إلى
`child/domain/` — نموذج نطاق تقرأه ثلاث ميزات، وقد كان في ملف عرض.

#### والحرس وجد ما لم يجده جردي

كتبتُ اختبارًا يمنع رجوع المزوّدات الثلاثة التي نقلتها، فأشار إلى **ثلاثة لم أكن
أعرفها**: `accountProfileProvider` و`accountChildrenProvider` في
`account_data_page.dart`، و`familyDevicesProvider` في `devices_page.dart`. فنُقلت
معها، و`profile` كسبت `application/`.

هذا هو الفرق بين حرسٍ يثبّت **خاصيّة** وحرسٍ يثبّت قائمةً كتبها إنسان: الثاني كان
سيمرّ بثلاثة مخالفين وأنا أظنّ البند مُغلقًا.

والفحص على `FutureProvider`/`StreamProvider` وحدهما: مزوّدٌ متزامن لحالة عرضٍ محليّة
موضعه الصفحة، ومنعُه تزمُّتٌ بلا سبب.

#### فشلٌ صامت ثالث، بالطريق

`earnedStickersProvider` كان `catch (_) { return const []; }`. القائمة الفارغة تبقى
هي السلوك — «مجموعتي» مساحة الطفل ويجب أن تُفتح دون إنترنت — لكن الفشل يُسجَّل الآن،
فلا يستوي «لا ملصقات بعد» بـ«فشل الجلب». ورفٌّ فارغ دائمًا لا يشتكي منه أحد ولا
يظهر في أي عدّاد.

#### ما لم أفعله، ومُعلَن

`EarnedSticker` و`AccountProfile` و`FamilyDevice` نماذج نطاق ما زالت في ملفات
صفحات، فطبقة `application/` تستوردها بـ`show` صريح. نقلُها سحبٌ لعشرة مواضع لا
يخدم هذه الدفعة، والاستيراد الضيّق يجعل الدَّين **مرئيًّا** بدل أن يبدو تصميمًا.

وبقي نقل ١٥ نداء `MajarraApiClient` من طبقة العرض (كانت ٢١). كلّها تمرّ بالعميل
المثبَّت، فمكسبها ترتيبيّ وقابلية اختبار لا أمن، وهي في صفحات بلا تغطية.

#### اختبارات

+10: `test/child_controls_controller_test.dart` (٩) — كل ضبط يُرسل مفتاحه المتوقَّع
· المسح في طلب واحد · فشل الشبكة يضع رسالةً عربية **ولا يرفع** · الفشل مُسجَّل
بموضعه · رفض الخادم فشلٌ لا نجاح صامت · النجاح بعد الفشل يمسح الرسالة · الإقرار
يُخفيها · علَم الحفظ يعود في الحالتين · وفشل طفلٍ لا يظهر على آخر. و+١ في حرس
الطبقات: لا مزوّد جلبٍ في ملف صفحة.

وفي بناء الاختبار درسٌ صغير: أوّل صياغة قارنت **كل** الطلبات فسقطت، لأن إبطال
`childSettingsProvider` يُنتج قراءةً مصاحبة. صار الفحص على طلبات `PUT` وحدها —
اختبارٌ يفشل لسببٍ لا يقيسه هو اختبارٌ يُحذَف لاحقًا.

#### معايير القبول

| المعيار | الحالة |
|---|---|
| كتابة ضابطٍ رقابي لا تفشل صامتة | ✅ رسالة ظاهرة + تسجيل + إبطال عند النجاح وحده |
| لا نصّ استثناء خام على وليّ أمر | ✅ في مسار الحذف وفي تحميل الإعدادات |
| لا مزوّد جلبٍ في ملف صفحة | ✅ صفر، ويحرسه اختبار (ستّة نُقلت) |
| كل ميزة تنادي الشبكة تملك طبقاتها | 🟡 أربع ميزات بالطبقات الأربع بدل اثنتين؛ الباقي في §5 |

#### الملفات

`app_main/lib/features/parent/data/child_controls_repository.dart` (جديد) · `parent/application/child_controls_controller.dart` (جديد) · `parent/presentation/pages/parent_dashboard_page.dart` (الكتابات الستّ + الحذف + رسالة التحميل) · `child/domain/child_profile.dart` (جديد، منقول) · `child/application/family_children_provider.dart` (جديد، منقول) · `profile/application/account_providers.dart` (جديد، منقول) · `games/application/rewards_providers.dart` (جديد، منقول + تسجيل الفشل) · `child_switcher_page.dart` و`account_data_page.dart` و`devices_page.dart` و`my_collection_route.dart` (حُذفت الإعلانات) · `app_router.dart` و`onboarding_flow_page.dart` و`child_profile_form_page.dart` و`age_transition_review_page.dart` (استيرادات) · `test/child_controls_controller_test.dart` (جديد، 9) · `test/presentation_network_layering_test.dart` (+1) · `test/my_collection_navigation_test.dart` (استيراد).

#### التالي

`APP-103` (كاتالوج مبندل يحلّ محل الحقيقي عند أي انقطاع) بالترتيب من §6، أو تكملة `APP-102` من بند 4 في §5.

---

### الدفعة التاسعة والعشرون (2026-08-28) — `APP-102` (جزئي): مسارا شبكة بلا تثبيت شهادات

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **618/618** (+11) · Worker `npm test` **1363/1363** · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **310/310**.

#### البند يُقرأ كترتيب مجلّدات، وفيه ثقب أمني

`APP-102` مكتوب بلغة معمارية: «تسع ميزات بلا `data/` ولا `domain/`». وجردُه
أظهر أن عدم الاتساق **أنتج** شيئًا أخطر من نفسه: موضعان ينفّذان `http.get`
عاريًا، أي بمخزن ثقة النظام لا بالعميل المثبَّت (`SEC-105`).

وما يحمّلانه ليس تفصيلًا: أحدهما **نصّ الترجمة الذي يُعرَض على الطفل**
(`playback_page.dart:790`)، والآخر صورة التلوين (`coloring_board_v2.dart:178`).
فأي جذر مزروع على الجهاز — أداة تحليل، وكيل مؤسسي، اعتراض على شبكة عامّة —
يستطيع تقديم نصٍّ من عنده يُقرأ للطفل. وهذا تحديدًا ما بُني `SEC-105` لمنعه،
متجاوَزًا في موضعين بقيا خارج نظره لأنهما لا يمرّان بـ`MajarraApiClient`.

ومعه علّتان: طلب الترجمة **بلا مهلة** أصلًا (فشبكةٌ تقبل الاتصال ولا تُجيب تُعلّقه
إلى الأبد)، وطلب الصورة بمهلة ثالثة (٢٠ ثانية) لا تشبه مهلة العميل (٨).

#### وأسوأ ما فيه أن العلاج كان مكتوبًا

`CreativeRemoteAssetsService.fetchImageBytes` موجودة في `games/data/` منذ
`SEC-105`، **مثبَّتةً وبمهلة**، وفوقها تعليق يقول: «العميل المثبَّت هو الافتراضي في
كل موضع، وإلا بقي مسار واحد غير مثبَّت يكفي لقراءة ما يمرّ به». ولوحة التلوين
تجاهلتها وكتبت الطلب بنفسها.

فهذا هو أثر `APP-102` بالحرف: **تطبيقان لعملٍ واحد بيدين مختلفتين، وأحدهما انحرف
إلى ما هو أقل أمنًا**. والبند لا يقول ذلك، لأنه يصف المجلّدات لا ما بينها.

#### وثلاث صفحات تسرّب عميلًا مثبَّتًا لكل زيارة

`final _service = CreativeRemoteAssetsService()` مكتوبة في ثلاث صفحات، وكل نسخة
تُنشئ عميلًا مثبَّتًا جديدًا (سياق ثقة محمَّل + اتصالات) **لا يُغلَق**. صار للخدمة
مزوّدٌ واحد يقرأ `httpClientProvider` — العميل الذي يُبنى مرّة ويُغلَق مع
الـProviderScope، وهو المسار نفسه الذي تمرّ به بقيّة طلبات التطبيق. فلا يبقى مسار
شبكة ثانٍ بسياسة ثقة خاصّة.

وصفحتان حُوّلتا إلى `ConsumerStatefulWidget` لتقرآ من مزوّدها. تغييرٌ ميكانيكي
(نوع الأداة وحده) على صفحةٍ لها موضع إنشاء واحد.

#### القرار: فشل الترجمة يبقى ترجمةً فارغة، ويُسجَّل

لم أحوّل فشل تحميل الترجمة إلى خطأ يوقف التشغيل. الطفل يشاهد حلقةً، وإسقاط
المشاهدة لأن ملف ترجمة لم يُحمَّل مقايضةٌ خاطئة في وجهه. لكن `catch (_) {}` صار
`reportIgnoredError` (قناة `APP-106` التي لا ترفع ولا تعرض)، فنقصُ الترجمة يصير
**قابلًا للرصد** بدل أن يُستنتَج من شكوى.

و`load` تُعيد `null` لا نصًّا فارغًا: الفارغ يمرّ إلى `WebVTTCaptionFile` كملفٍّ
صالح بلا أسطر، فيستوي عند كل قارئ لاحق «لا ترجمة لهذه الحلقة» و«ترجمة فشل
تحميلها». والتحويل إلى `''` يحدث في موضع واحد ظاهر في الصفحة.

وفكّ الترميز `utf8.decode` صريحًا لا `response.body`: الأخير يتبع ترويسة النوع،
وملفات VTT تُخدَم أحيانًا بلا `charset` فتُقرأ latin-1 **فيرى الطفل رموزًا**.
ولذلك اختبارٌ مستقلّ.

#### الحرس: خاصيّة لا موضع

`test/presentation_network_layering_test.dart` يفحص **كل** ملف تحت
`lib/features/*/presentation/` ويرفض استيراد `package:http`. اليوم صفر مخالف، وأي
ملف عرضٍ جديد يفتح مسارًا شبكيًّا بنفسه يُفشل الجولة. وهذا هو معيار قبول `APP-102`
بصيغة تُنفَّذ لا بصيغة تُقرأ.

وكتابة هذا الحرس علّمتني ما تعلّمته في الدفعة 27: أوّل صياغة رصدت **التعليق الذي
يشرح العطل** فبلّغت عن الملف الذي أُصلح. تعليقات السطر تُقشَّر الآن — حرسٌ يصرخ في
السليم يُعطَّل في أوّل أسبوع.

#### وخطأٌ في اختباري كشف أن الإصلاح صحيح

أوّل صياغة للاختبار السعيد بنت الردّ بـ`http.Response('…عربي…', 200)`، فسقطت:
`http.Response(String, …)` يرمّز بـlatin-1 حين لا `charset` في الترويسة، فلا يقبل
عربيًّا أصلًا. أي أن المكتبة نفسها تُظهر العلّة التي يعالجها المستودع. الاختبار
يبني البايتات الآن، وهو الشكل الصادق لما يصل من الشبكة.

#### ما لم أفعله، ولماذا

نقل نداءات `MajarraApiClient` الـ٢١ الباقية من طبقة العرض. كلّها تمرّ بالعميل
المثبَّت أصلًا، فالمكسب **ترتيبيّ وقابلية اختبار لا أمن**. وهي في صفحات بحجم ٣٥٣٨
و٢٢٢٤ و١٤٨٠ سطرًا **بلا اختبار واحد يغطّيها** — أي أن نقلها هو `APP-101` بثوب آخر:
مقايضة عطلٍ صامت بعطلٍ صريح.

والباقي مُحدَّد بأسمائه في §5 ومُرتَّب **بالضرر لا بالطبقة**، وأوّله: ستّ كتابات
`updateChildSettings` في `parent_dashboard_page.dart` **بلا أي `catch`** — فشلُ
كتابة وقت الشاشة أو وقت النوم لا يُخبِر وليَّ الأمر، وهو يظنّ الضبط ساريًا.

#### اختبارات

+11 في `test/presentation_network_layering_test.dart`: لا `package:http` في أي
ملف عرض · لا بناء للخدمة في طبقة العرض · المزوّدان يقرآن العميل المثبَّت · والمستودع:
يعيد VTT كما هو · يفكّ utf8 لا حسب الترويسة · غير‑200 → `null` ومُسجَّل · فشل شبكة
→ `null` ومُسجَّل ولا يُرفَع · **فشل التحقّق من هوية الخادم يُهمَل بلا إعادة محاولة
ويُسجَّل** · المهلة تُطبَّق فلا تعليق أبدي · عنوان فارغ أو بلا مخطَّط لا يُطلَب أصلًا ·
`null` لا نصّ فارغ.

#### معايير القبول

| المعيار | الحالة |
|---|---|
| لا `http` مُستدعى مباشرة من `presentation/` | ✅ صفر ملف، ويحرسه اختبار |
| كل ميزة تنادي الشبكة تملك `data/` و`domain/` | 🟡 جزئي: `playback` كسبت `data/`+`application/`، و`games` كسبت `application/`. الباقي بأسمائه في §5 |
| لا `MajarraApiClient` من `presentation/` | ⛔ باقٍ: ٢١ ملفًا. مكسبه ترتيبيّ لا أمنيّ، وهو مشروط بتغطية اختبارية لا توجد بعد |

#### الملفات

`app_main/lib/features/playback/data/caption_repository.dart` (جديد) · `playback/application/playback_providers.dart` (جديد) · `games/application/creative_providers.dart` (جديد) · `playback/presentation/playback_page.dart` (‏`http.get` → المستودع، وحُذف استيرادان ميتان) · `games/presentation/pages/coloring/coloring_board_v2.dart` (‏`http.get` → الخدمة، و`ConsumerStatefulWidget`) · `coloring/coloring_home_v2_live.dart` و`pages/reference_catalogue_page_live.dart` (الخدمة من مزوّدها) · `test/presentation_network_layering_test.dart` (جديد، 11).

#### التالي

`APP-102` تكملةً بالترتيب المذكور في §5 (وأوّله كتابات ضوابط وليّ الأمر بلا `catch`)، أو `APP-103` (كاتالوج مبندل يحلّ محل الحقيقي عند أي انقطاع).

---

### الدفعة الثامنة والعشرون (2026-08-28) — `OPS-105` النشر من CI

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **607/607** · Worker `npm test` **1363/1363** (+12) · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **310/310**. وحزمة الإنتاج تُبنى (`wrangler deploy --dry-run --env production`، ‏2183 KiB / gzip 436).

#### العلّة الحقيقية: طلبُ تغييرٍ متنكّرًا في صورة مسار

`deploy-gate` كان مثبَّتًا على `--dry-run`، وفوقه تعليق يشرح أن التوكن مطلوب وأن
تمكينه قرار مالك. يبدو ذلك أمانةً، وهو في الحقيقة **بابٌ مغلق بمفتاحٍ في جيب أحد
آخر**: يضيف المالك السرَّين، ثم لا يُنشَر شيء حتى يتذكّر أحدٌ تحرير الملف. وبين
الخطوتين فجوةٌ لا يملأها إلا الحظّ.

فالتوكن صار **شرطًا في وقت التشغيل**: خطوةٌ تقرأ وجود السرَّين وتُخرج علَمًا، وخطوتان
تتفرّعان عليه — نشرٌ حقيقي إن وُجدا، وبناءُ حزمةٍ إن غابا. إضافة السرَّين **هي كلّ
المفتاح**. و`secrets` لا تُقرأ في `if:`، فالوجود يُحلّ عبر `env` بلا طبع أي قيمة.

#### «أيّ إصدارٍ يعمل الآن؟» — يُسأل الإنتاج، لا سجلٌّ بجانبه

`API_VERSION` قيمتها `v1` قبل النشر وبعده: هي **عقد الـAPI** لا هوية البناء. وكانت
هي كل ما تعرضه `/`، فتُقرأ إجابةً وهي ليست إجابة.

فبصمة الـcommit تُخبَز في الحزمة (`tools/ci/write-release.mjs` → `src/lib/release.ts`)،
وتُعلَن في `GET /version` مع وقت البناء ورقم الجولة. والجواب من الإنتاج نفسه لا من
سجلٍّ يُكتب بجانبه، لأن السجل يفترق عن الواقع أوّل مرّة يُنشر بيدٍ.

**ولم تُمرَّر بـ`wrangler deploy --var`.** العلَم موجود ويبدو أقصر، لكن دلالة دمجه مع
`vars` المُعلَنة في الإعداد ليست شيئًا نقامر عليه في مسار النشر: لو استبدل الخريطة بدل
أن يضيف إليها، سقطت `ENVIRONMENT` و`API_VERSION` وانكسر الإنتاج **بعد** خضرة كل
الفحوص. والملف المُولَّد لا دلالة فيه تُفاجئ: ما يُقرأ هو ما كُتب.

والقالب في السكربت هو **المصدر الوحيد**، والملف المتتبَّع مخرَجُه بقيَم النائب —
يثبّت ذلك اختبارٌ يوازن الاثنين. فلا نسخة تنحرف بتحرير يدوي، ولا تنبيه «الملف تغيّر»
في كل جولة.

وقيَم النائب `unknown` لا فراغ ولا تاريخ اللحظة: بناءٌ محلي أو نشرٌ يدوي **لا يعرف**
رقم الإصدار. و`''` تُقرأ حقلًا ناقصًا، وتاريخُ اللحظة يُقرأ حقيقةً — والصدق أنفع من
كليهما.

#### النشر لا يُصدَّق على كلمته

الخطوة التي تليه تسأل `https://api.majarra.app/version` وتوازن البصمة بالـcommit،
ستّ محاولات بينها عشر ثوان، ثم تُفشل الجولة. فالحالة التي تمنعها ليست نظرية: «نشرٌ
نجح» ونسخةٌ قديمة تخدم الطلبات هي أسوأ نتيجة ممكنة، لأن كل مؤشّر أخضر.

والاقتطاع إلى ١٢ محرفًا يحدث في **موضع واحد** (الكاتب)، ويوازن المسار بنفس الطول.
ولو اقتطع أحدهما بطولٍ آخر، فشل التحقّق على نشرٍ سليم — وهو نوع الفشل الذي يُعلَّم
بأنه «متقلّب» فيُهمَل.

#### السجل: وسمٌ في المستودع لا سطرٌ في جولة تنتهي

`release/<تاريخ>-<sha7>` مشروحٌ برسالتين: معرّف إصدار Cloudflare، ورابط الجولة.
الوسم يبقى بعد انقضاء سجلات الجولات، وهو ما يُترجم «نسخة كذا» إلى commit ساعةَ
حادثة. ومُتسامِح: إعادة نشر نفس الـcommit لا تُفشل الجولة.

ومعرّف الإصدار يُقرأ من مخرَج `wrangler deploy`، وإن تغيّرت صياغته فمن
`wrangler versions list --json`. أي أن تغييرًا في نصّ أداة خارجية **يُضعف السجل ولا
يُسقط النشر** — والعكس كان سيجعل تحديث wrangler حادثةَ إطلاق.

#### الترحيلات لا تركب مع النشر — بقرار

نشرٌ سيّئ يُلغيه `wrangler rollback` في ثوانٍ. **وترحيلٌ سيّئ لا يُلغيه شيء** يملكه
هذا المسار: لا `rollback` يعيد عمودًا حُذف. فربطُهما يعني أن تغييرًا في المخطَّط يركب
مع إصلاح نصّ عابر فيُطبَّق بلا أن ينظر إليه أحد. `migrate:remote` يبقى فعلًا منفصلًا
مقصودًا، ومعه `migration-ledger --remote` للفحص قبله، والبناء النظيف مضمون بمهمّة
`migrations` (`DB-104`).

#### حرسٌ قائم كان يثبّت الصياغة القديمة

`test/ciGates.test.mjs` كان يفرض `deploy-gate:` و`wrangler deploy --dry-run`. لم
يُخفَّف: حُدِّث إلى العقد الجديد ليطلب **المسارَين** معًا — الحقيقي والجاف. لأن مهمّةً
تكتفي بالجاف تمرّ فراغًا، ومهمّةً تنشر دائمًا تفشل كل جولة حتى يوجد توكن. وأُضيف
شرطٌ يمنع عودة تعليق «كيف تجعله حقيقيًّا»: الوعد بتحرير ملف لا يعيش كتوثيق.

وقاعدة «لا خطوة تُخفي فشلها بـ`||`» بقيت قاعدة: الاستثناءات **معدودة بأسمائها** (قراءة
معرّف الإصدار، وقراءة البصمة الحيّة داخل حلقةٍ تفشل بنفسها). فأي `||` جديد في أي
مكان يُفشل الاختبار.

#### اختبارات

+12 في `test/release.test.mjs`: الملف المُولَّد = مخرَج القالب · النائب `unknown` لا
فراغ · الاقتطاع في موضع واحد · القالب يرفض قيمة تكسر ملف TypeScript · `/version`
يقرأ من الحزمة لا من الإعداد · **و`/version` مُركَّب قبل عارض الصفحات العامة** —
ترتيبٌ دلالته الأسبقية: لو سبقه العارض لأجاب بصفحة HTML، فيفشل التحقّق في كل جولة
بلا سبب ظاهر · والمسار ينشر بوجود التوكن، ويتحقّق، ويوسم، ولا يُطبِّق ترحيلات.

#### معايير القبول

| المعيار | الحالة |
|---|---|
| نشر من CI على `master` بعد خضرة كل الوظائف | ✅ وبما فيها `migrations`؛ ولا يحدث من جهاز أحد |
| كل نشر يترك سجلًا يربط الإصدار بالـcommit | ✅ وسم مشروح + `/version` من الإنتاج + موازنة تُفشل الجولة عند الاختلاف |
| إجراء تراجع مُختبَر مرّة واحدة | 🟡 **موثَّق بأوامره، وغير مُنفَّذ على الإنتاج** — يحتاج نشرًا حقيقيًّا. فعلٌ للمالك بعد السرَّين |

#### الملفات

`.github/workflows/ci.yml` (‏`deploy-gate` → `deploy`: بصمة، تفرّع على التوكن، تحقّق، وسم) · `dashboard/api/tools/ci/write-release.mjs` (جديد) · `dashboard/api/src/lib/release.ts` (جديد، مُولَّد) · `dashboard/api/src/index.ts` (`/version` + `release` في `/`) · `test/release.test.mjs` (جديد، 12) · `test/ciGates.test.mjs` (العقد الجديد) · `SETUP_SECRETS.md §8` (التشغيل، التراجع، وسبب فصل الترحيلات) · `.gitignore` (`.wrangler-ci/`).

#### التالي

`APP-101` (أربعة ملفات آلهة بلا اختبار، **XL** — يحتاج قرار مالك قبل البدء) أو `APP-102`، بالترتيب من §6.

---

### الدفعة السابعة والعشرون (2026-08-28) — `DB-104` البناء النظيف

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` صفر · `flutter test` **607/607** · Worker `npm test` **1351/1351** (+5) · `tsc --noEmit` نظيف (الطرفان) · `vitest run` **310/310**.

#### السبب: ابنٌ يُدرَج قبل أبيه بثلاثة بيانات

`0074_wave4_closure_36_games.sql` يُدرج في بيانه السابع صفَّ ترجمة لـ
`game-trace-color-advanced-3`، ويُدرج **اللعبة نفسها** في بيانه الثامن.
و`INSERT OR IGNORE` لا يُنقذ الموقف: خيار `ON CONFLICT` في SQLite **لا يشمل
`FOREIGN KEY`**، فالمخالفة خطأٌ لا تجاوُز. فيسقط الملف كلّه، ويتوقّف كل ما بعده:
٧١ مُطبَّقًا و١٨ معلَّقًا.

أي أن **بيئة جديدة لم يكن يمكن إنشاؤها من المستودع**. وعاش هذا شهورًا لأن كل بيئة
قائمة بُنيت تدريجيًّا، فلم يمرّ أحد على المسار من الصفر.

#### أداتي أنا كانت تكذب، فأضلّتني دورةً كاملة

قبل أن أجد السبب، شغّلتُ أداة التنصيف التي كتبتها في الدفعة 26. أعلنت «بيانًا
واحدًا» في ملفٍ فيه تسعة، وسمّت البيان الأخير — وهو سليم — فاشلًا. عطلان:

1. **قطع التعليقات تُسقط بياناتها.** كل قطعة تبدأ بالتعليق الذي كان يعلو بيانها،
   والمرشّح كان يُسقط ما يبدأ بـ`--` — أي البيان معه. التعليق يُقشَّر الآن سطرًا
   سطرًا ولا تُقصّ الكتلة به.
2. **`cmd.exe` يُفسد كل استعلام فيه سطر جديد.** كان الاستعلام يُقتبَس يدويًّا
   ويُمرَّر عبر صدفة Windows. الاستعلام يُكتب الآن في ملف مؤقّت ويُمرَّر بـ`--file`،
   فلا يمرّ بصدفة أصلًا. (و`npx.cmd` بلا `shell` يرفضه Node بـ`EINVAL`، فالملف هو
   المخرج لا تبديل المُصدِّف.)

الدرس المكتوب في الأداة وفي `LEDGER.md`: **أداة تشخيص تكذب أسوأ من غيابها** —
الغائبة تُبقيك تبحث، والكاذبة تُوقفك عند بريء.

ودرسٌ ثانٍ يخصّني: البند نفسه كان يقول إن مفاتيح `game_localizations.game_id`
الثمانية عشر «كلّها مُدرَجة داخل الملف». صحيحٌ وغير كافٍ — **المهم موضعها لا
وجودها**. وذلك الفحص جرى على قاعدة مكتملة كان الصفّ فيها موجودًا سلفًا، فأجاب عن
سؤال آخر بثقة.

#### تحرير ترحيل مُطبَّق — استثناء بشرطين، لا نقض للقاعدة

قاعدة `LEDGER.md` (الدفعة 26): لا يُحرَّر ترحيل مُطبَّق، والإصلاح بترحيل جديد. وهنا
**لا يمكن**: انهيارٌ في منتصف السلسلة لا يصلحه لاحقٌ لأن اللاحق **لا يُشغَّل**.
فأُعلن الاستثناء وشرطاه في `LEDGER.md`:

1. التحرير **أصغر ما يُمكن** ويقتصر على إزالة سبب الفشل.
2. حالة البيئات التي طبّقته سابقًا **تُقارَب بترحيل جديد**، لأن التحرير لا يعمل
   فيها: اسم الملف مسجَّل عندها كمُطبَّق ولن يُقرأ ثانيةً.

فحُذف من `0074` صفُّ الترجمة المُبكِّر وحده (وكانت حالته `draft`)، فترجمة اللعبة
النهائية بحالة `ready` صارت البيان الأخير بعد إدراجها. **لا بيان أُضيف ولا قيمة
تغيّرت.**

#### وتحت العطل عطلٌ ظاهر للطفل

الترجمة كانت مكتوبة **مرّتين بقيمتين**: `0074` بمفتاح `game.trace.word.amal`،
و`0074d_final6_games.sql` بمفتاح `game.trace.free`. وكلتاهما `INSERT OR IGNORE`،
فالفائز **من يصل أوّلًا** لا من يكون صحيحًا. ولمّا كان `0074` يتوقّف قبل صفّه، فازت
قيمة `0074d` في كل بيئة قائمة — وكانت ستفوز قيمة `0074` في كل بناء نظيف بعد
الإصلاح، فتفترق البيئات في صفٍّ واحد.

والصحيح مُحدَّد لا مُختار: حزمة اللعبة (كما ثبّتها `0081_fix_wave4_truncated_packs.sql`)
لها ثلاثة مستويات بمفاتيح `letter.alif` و`letter.baa` و`word.amal`. فـ
`game.trace.free` مفتاحٌ **لا يطلبه أي مستوى** — منقول عن `game-shape-trace-3` —
ومفتاح المستوى الثالث **غائب**. أي أن المستوى الثالث يُعرَض في الإنتاج بنصّ مفقود.
فـ`0087` ليس تسويةَ فرقٍ بين بيئتين، بل إصلاح عطل يراه طفل.

و`0074d` لم يُلمس: تعديله لا يصلح بيئةً طبّقته، والقيمة الصحيحة تُثبَّت في `0087`
بعده فتَغلِب في كل مسار.

#### مقابلة المخطَّطين: الفرق في القاعدة القائمة لا في المستودع

بُنيت قاعدة نظيفة، ودُمِج `sqlite_master` منها ومن القاعدة المُطبَّقة تدريجيًّا:
**٣٣٣ كائنًا مقابل ٣٣٠**. والفرق:

| الفرق | القراءة |
|---|---|
| `family_deletion_watermarks` و`child_deletion_watermarks` وفهرسها — **في النظيفة فقط** | يُنشئها `0051_identity_account_lifecycle.sql` ويقرأها `familyEvents.ts` في ١٩ موضعًا. أي أن **قاعدة التطوير القائمة كانت ناقصةً** لا المستودع؛ والبناء النظيف هو الأصحّ |
| نصّ تعريف `creative_drawings` | فراغات وحدها — متطابق بعد التطبيع. أثرُ تنسيقٍ لُمس بعد التطبيق، لا اختلاف مخطَّط |

ولهذا صار البناء النظيف هو قاعدة التطوير الفاعلة، والقديمة محفوظة إلى جانبها
(`.wrangler/state/v3/d1_applied`) لأن الحفظ أرخص من الأسف. **ومقابلة الإنتاج فعلٌ
للمالك** لأنها تحتاج بيانات اعتماد.

#### البوابة: عدّاءٌ يبدأ بلا حالة

مهمّة `migrations` جديدة في `.github/workflows/ci.yml`: العدّاء نظيف بحكم
التعريف، فتطبيق الترحيلات عليه **هو** اختبار البناء النظيف. ثم
`migration-ledger.mjs --local --require-applied` يُفشل الجولة إن بقي ملفٌ واحد
معلَّقًا. والعلَم صريحٌ لا مُستنتَج: على الإنتاج «المعلَّق» يعني أن المالك لم ينشر
بعد، وفي بناءٍ نظيف يعني انهيارًا. والمهمّة مضافة إلى `needs` بوابة النشر، وإلا
فشلت بلا أثر.

#### اختبارات

+5 في `test/migrations.test.mjs` (9 → 14): البيان المخالف لم يعد في `0074` ·
ترحيل التقريب `UPDATE` بلا إدراج (فلا يُعيد العطل الذي جاء يصلحه) · السجل يعلن
البناء محروسًا لا فاشلًا · CI يبني من الصفر ويمنع المرور بملف معلَّق · ولا ترجمة
لعبة تُدرَج قبل لعبتها في تسلسل الترحيلات كلّه.

والأخير هو الحرس الثابت للخاصيّة التي انكسرت. وكتابته درسٌ في نفسها: بُني أوّلًا
بتعبير نمطي، فأخطأ مرّتين — مرّةً باشتراط أن يكون قوس الصفّ في بداية سطر (ونصف
ملفات البذر تكتب `VALUES (` ثم تنزل سطرًا) فبلّغ عن ستّ ألعاب سليمة، ومرّةً بالنظر
**داخل** نصوص JSON فحسب مفاتيح الترجمة معرّفاتِ ألعاب. فاستُبدل بماسحٍ يتتبّع
الاقتباس والعمق ولا يقرأ ما داخل النصوص أصلًا. **حرسٌ يصرخ في السليم يُعطَّل في أوّل
أسبوع.**

#### معايير القبول

| المعيار | الحالة |
|---|---|
| قاعدة فارغة تطبّق كل الترحيلات | ✅ **٩٠/٩٠**، ببناءَين نظيفَين (قبل `0087` وبعده)، وصفر معلَّق وصفر صفٍّ بلا ملف |
| مخطَّط البناء النظيف يطابق المُطبَّق تدريجيًّا | ✅ قُوبل بـ`sqlite_master`: الفرق الجوهري الوحيد **نقصٌ في القاعدة القائمة** (جدولا الشواهد وفهرسهما)، والباقي فراغات. مقابلة الإنتاج فعلٌ للمالك |
| اختبار في CI يمنع رجوع الانكسار صامتًا | ✅ مهمّة `migrations` + `--require-applied` + مضافة إلى `needs` بوابة النشر |

#### الملفات

`migrations/0074_wave4_closure_36_games.sql` (إسقاط الصفّ المخالف — استثناء مُعلَن) · `migrations/0087_db104_trace_localization_convergence.sql` (جديد) · `migrations/LEDGER.md` (الاستثناء + حالة البناء النظيف + سببه) · `tools/ops/migration-ledger.mjs` (`--file` بدل `--command`، تقشير التعليقات، `--require-applied`) · `tools/ops/triage-failed-events.mjs` (نفس إصلاح الصدفة) · `.github/workflows/ci.yml` (مهمّة `migrations` + `needs`) · `test/migrations.test.mjs` (+5).

#### التالي

`OPS-105` (النشر لا يحدث من CI) بالترتيب من §6، ثم `APP-101` (XL، ملفات آلهة).

---

### الدفعة السادسة والعشرون (2026-08-26) — `DB-101` سجل الترحيلات

الفحوص بعد الدفعة: Worker `npm test` **1346/1346** (+9) · `tsc` نظيف · `vitest` 310/310 · Flutter 607/607.

#### ما وجدته أخطر ممّا وصفه البند

البند رصد أرقامًا مكرَّرة وفجوات وصيغًا فرعية. والفحص الفعلي كشف شيئين لم يكونا فيه:

**١. ثلاثة ترحيلات نُفِّذت مرّتين.** القاعدة المحلية فيها **٩٢ صفًّا مقابل ٨٩ ملفًا**، والثلاثة الزائدة:

| صفّ في السجل | صار اسمه |
|---|---|
| `0051_wave1_games.sql` | `0054_wave1_games.sql` |
| `0052_wave2_depth.sql` | `0055_wave2_depth.sql` |
| `0053_wave3_final.sql` | `0056_wave3_final.sql` |

اسم الملف هو **مفتاح** `d1_migrations`. فإعادة ترقيمه بعد تطبيقه تجعله يبدو غير مُطبَّق، **فيُطبَّق ثانيةً** — والصفّ القديم يبقى يشير إلى ملف لا وجود له. ونجا ذلك لأن بذورها `INSERT OR IGNORE`/`IF NOT EXISTS`: **حظًّا لا تصميمًا**. ولو كانت `ALTER TABLE` أو `UPDATE` لكانت النتيجة عمودًا مزدوجًا أو عدًّا مضاعفًا.

وهذا أيضًا تفسير «الفجوتين» عند 0052 و0053: ليستا فجوتين بل رقمين هُجرا في إعادة الترقيم. أي أن الفجوة والتكرار **حادثة واحدة** لا عيبين.

**٢. بناء قاعدة نظيفة من الترحيلات مستحيل اليوم.** نقلتُ حالة D1 المحلية جانبًا وأعدت التطبيق من الصفر: يتوقّف عند `0074_wave4_closure_36_games.sql` بـ`FOREIGN KEY constraint failed` — **٧١ مُطبَّقًا و١٨ معلَّقًا**.

ومعناه أن **مخطَّط الإنتاج لا يمكن إعادة إنتاجه من المستودع**: لا بيئة اختبار من الصفر، ولا مقارنة بيئتين، ولا استرداد يبدأ من الترحيلات. وهذا معيار القبول الثاني للبند، وجوابه **لا**.

وفحصتُ كل مفاتيح 0074 الأجنبية على القاعدة النظيفة — السلاسل التسع، والمحرّكات الاثنا عشر (يبذرها الملف نفسه)، والهدفان التعليميان، والثمانية عشر تعريبًا — **وكلّها تحلّ**. فالمخالفة لا تُفسَّر من الملف وحده. فتحتُ لها بندًا مستقلًّا: **`DB-104`**، بأداة تنصيف جاهزة، ومعايير قبول تشمل اختبارًا في CI يبني قاعدة نظيفة.

#### القرار: السجل مصدر حقيقة يحرسه اختبار، لا وثيقة مساعدة

البند نصّ على «لا تُعِد ترقيم ترحيلات مُطبَّقة». وهذه قاعدةٌ تُنقَض عند أوّل ضيق وقت **إن كانت في وثيقة وحدها**.

فـ`migrations/LEDGER.md` يحمل **قائمة الملفات الـ89 نفسها**، و`test/migrations.test.mjs` يقارنها بالمجلد. أي أن إضافة ملف أو حذفه أو **إعادة تسميته** تُفشل الجولة حتى يُحدَّث السجل — وتحديثه يعني أن أحدًا قرأ القاعدة وسببها.

وهذا هو الفرق العملي: القاعدة صارت **غير قابلة للنقض بالسهو**.

ويحرس الاختبار أيضًا: أن الأرقام المكرَّرة هي الثلاثة الموثَّقة ولا غيرها، وأن كل فجوة مذكورة بسببها، وألّا تُضاف صيغة فرعية بحرف لاحق (`0074b` وأختاها مسموحتان بالاسم وحده)، وأن السجل يذكر الحادثة وأرقامها المقيسة لا القاعدة مجرَّدة.

#### المخطَّط الثاني المتناقض — حُذف

`migrations/catalog/0001_catalog_init.sql` كان **خارج** `migrations_dir`: لا wrangler يطبّقه ولا سكربت ينادِيه، وبحثتُ في المستودع فلا مرجع له. ومع ذلك كان يُعلن `series` و`episodes` بأربعة أعمدة لكلٍّ منهما، مقابل التعريفين الحقيقيين بثلاثين عمودًا وأكثر.

أي أنه **مخطَّط ثانٍ متناقض ينتظر من يطبّقه بالخطأ**، وسكتشُ «قاعدة كاتالوج للقراءة فقط» الذي كُتب لأجله لا وجود له في البنية. حُذف، ويفرض الاختبار ألّا يعود ترحيلٌ خارج المجلد.

#### عطل في أداة الدفعة العشرين، كشفه بناء هذه

أداة السجل فشلت أوّل تشغيل بـ`Unknown arguments: name, FROM, d1_migrations…`. السبب أن `shell: true` — اللازم لتشغيل `npx` على Windows — يمرّر السطر كنصّ واحد، فيُقسَّم استعلامٌ فيه فراغات إلى وسائط.

**وأداة تشخيص الأحداث الفاشلة (`triage-failed-events.mjs`، الدفعة 20) فيها نفس العطل بالحرف** — استعلامها أطول، فكانت ستفشل بنفس الرسالة التي تبدو خطأً في wrangler لا فينا. أُصلحت الاثنتان.

#### معايير القبول

| المعيار | الحالة |
|---|---|
| لا رقم مكرَّر، وكل فجوة موثَّقة بسبب | ✅ الثلاثة الموثَّقة مغلقة باختبار، والفجوات الثلاث بأسبابها |
| إعادة بناء قاعدة نظيفة تنجح وتُنتج نفس المخطَّط | ⛔ **لا** — مُتحقَّق منه بالتشغيل، ومُسجَّل بندًا مستقلًّا `DB-104` |
| `migrations list` على الإنتاج موثَّق في المستودع | 🟡 الأداة والإجراء جاهزان (`--remote > migrations/APPLIED_PRODUCTION.md`)؛ التشغيل فعلٌ للمالك لأنه يحتاج بيانات اعتماد |

#### الملفات

`migrations/LEDGER.md` (جديد) · `tools/ops/migration-ledger.mjs` (جديد) · `test/migrations.test.mjs` (جديد، 9) · `tools/ops/triage-failed-events.mjs` (إصلاح الاقتباس) · حُذف `migrations/catalog/0001_catalog_init.sql`.

#### التالي

`DB-104` (البناء النظيف يتوقّف عند 0074 — بأداة تنصيف جاهزة) أو `APP-101`.

---

### الدفعة العاشرة (2026-08-26) — قرار مالك: **`DECIDE-101` الإطلاق بلا DRM**

> **القرار:** كل المحتوى المرئي من إنتاج «مجرة» نفسها. لا جهة ترخيص تفرض Multi-DRM، فالإطلاق يمضي بالوضع المرحلي غير DRM.

هذه دفعة **قرار** لا دفعة كود، ومعها ترحيل بيانات واحد يجعل القرار قابلًا للتحقّق. الفحوص لم تتغيّر: `flutter analyze` صفر مشكلة · `flutter test` 546/546 · Worker 1204/1204 · `vitest` 310/310 · `tsc` نظيف.

#### القرار متوافق مع الخطة، لا استثناء منها

المعيار 5 في `تشفير المحتوي.md` §26 ينصّ بنفسه: «الوضع المؤقت غير DRM يستثني هذا المحتوى ويقتصر على **فيديو أصلي يملكه المنتج**». وشرط المعيار متحقّق بعد إقرار المالك، فالبند يُعاد تصنيفه «مستوفى بالوضع المرحلي» لا «غير مستوفى».

#### ما لا يعنيه القرار — وهذا أهمّ من القرار نفسه

طُرح التبرير بأن «نشفّر ونمنع التصوير ونعرض على الموبايل فقط»، وثلاث نقاط فيه غير دقيقة وتبقى قائمة بعد القرار:

**1. التشفير عندنا ليس بديلًا عن DRM.** مفتاحنا مفتاح **تطبيق** يعيش في مخزن جهاز يملكه المستخدم: من يشغّل التطبيق يملك المفتاح بالتعريف. DRM يضع المفتاح وفكّ التشفير داخل بيئة موثوقة في المعالج (TEE) لا يراها التطبيق ولا المستخدم. فطبقتنا تمنع «انسخ الملف واذهب» لا «استخرج المحتوى».

**2. منع التصوير سطحي.** `FLAG_SECURE` يهزم واجهات المنصّة وحدها: لا يصمد أمام جهاز مفتوح الجذر، ولا بناء معدَّل، ولا بطاقة التقاط HDMI، ولا كاميرا خارجية. وعلى منصّات Apple **لا منع أصلًا** — رصد فقط (الدفعة التاسعة).

**3. «موبايل فقط» لا تقلّل الخطر.** الهاتف أسهل منصّة للاختراق أدواتيًّا: جهاز Android مفتوح الجذر مع أداة تعليق دوالّ يصل إلى دالة الفكّ مباشرة.

**النتيجة العملية:** القرار سليم لأنه **تجاري** — لا أحد يفرض DRM على محتواك، وخطر تسريب حلقة خطرك أنت وحقّك أن تقبله — لا لأن ما لدينا يعادل DRM. وهذا الفرق يجب أن يبقى مكتوبًا حتى لا يُبنى عليه قرار لاحق خاطئ (مثل «نرخّص محتوى من غيرنا، فحمايتنا كافية»).

#### ما يرتفع في الأولوية بسبب القرار

**`ENC-004`** — التشغيل يفكّ المحتوى إلى **ملف صريح** على القرص ويسلّمه للمشغّل. بلا DRM صار هذا أسهل طريق استخراج في المنظومة: أسهل من كسر التشفير، وأسهل من التقاط الشاشة. كان مؤجَّلًا لتصميم منفصل، وصار أول بند في §5 بعد هذه الدفعة.

**`ENC-001`** — لم يكن محجوبًا بـDRM بل بالقرار، والآن **غير محجوب**. «بلا DRM» لا تعني «بلا ترخيص»: منظومة الترخيص هي ما يفرض المدفوع من المجاني، وحدود الأجهزة، والإبطال عند سحب جهاز. يبقى حجمه XL وحده سببًا لتخصيص دفعة له.

#### الترحيل `0082` — لماذا لم يكفِ سطر في مستند

شرط صلاحية القرار أن **كل** منشور مملوك، و`content_rights` كان **صفر صفًّا**: لا شيء في النظام يقول من يملك ماذا، فالقرار كان رأيًا غير قابل للفحص آليًّا. الترحيل سجّل **248 صفّ ملكية** بمالك `majarra`:

| النوع | صفوف |
|---|---:|
| `series` | 40 |
| `episode` | 81 |
| `book` | 22 |
| `game` | 105 |

`territories = ["*"]` و`licenses` تشمل البث والتنزيل — هذا ما يعنيه أن تملك عملك، لا ترخيصًا مقيَّدًا. و`expiry = NULL` عن قصد: حقوق المنتج على عمله لا تنتهي بتاريخ، وكتابة تاريخ وهمي كانت ستجعل بوابة نشر مستقبلية ترفض محتواك عند حلوله. و`INSERT OR IGNORE` فلا يُكتب فوق صفّ أدقّ مكتوب يدويًّا.

وبعد الترحيل صار السؤال «هل هذا المحتوى مملوك؟» استعلامًا لا اجتهادًا، وصار **ظهور محتوى بلا صفّ حقوق** إشارةً إلى شيء دخل من خارج الإنتاج الداخلي — وهو بالضبط الحدث الذي يعيد DRM إلى الطاولة. مُتحقَّق: صفر حلقة بلا صفّ حقوق.

#### الشرط الذي يُبطل القرار

**أول محتوى مرخَّص من طرف ثالث يُعيد `ENC-009` نشطًا و`HUMAN-101` شرطًا مسبقًا.** لا استثناء ولا «مؤقتًا»: عقود المرخِّصين تفرض مصفوفة DRM/HDCP/Watermark قبل النشر، وخرقها مسؤولية قانونية لا نقص ميزة.

#### ما تغيّر في هذا الملف

`DECIDE-101` مشطوب ومحسوم · `ENC-009` صار `ACCEPTED-TRADEOFF` بشرطه · المعيار 5 في §4 صار ✅ «بالوضع المرحلي» والمعيار 25 «غير منطبق» · صفّ «DRM للفيديو» في §2 · `CNT-110` نصفه مُغلَق · `HUMAN-101` صار شرطيًّا · `DATA-007` في §13.

#### التالي

`ENC-004` (الملف الصريح أثناء التشغيل) صار أعلى ما في §5 بعد القرار، ويحتاج قرارًا هندسيًّا في طريقة التشغيل (خادم محلي يقدّم تدفقًا مفكوكًا، أم تكامل native). وبعده — أو بالتوازي — `ENC-001` كدفعة مستقلّة.

---

### الدفعة الحادية عشرة (2026-08-26) — `ENC-004` تشغيل بلا ملف صريح

الفحوص بعد الدفعة: `flutter analyze --fatal-warnings` **صفر مشكلة** · `flutter test` **562/562** (16 اختبارًا جديدًا) · Worker `npm test` 1204/1204 · `vitest` 310/310 · `tsc` نظيف.

#### ما كان

التشغيل يفكّ الحزمة إلى `.play_<id>.mp4` **صريح** على القرص ويسلّمه للمشغّل. ممنوع في §12 من الخطة، وبعد قرار «بلا DRM» صار أسهل طريق استخراج في المنظومة كلّها: ملف كامل قابل للقراءة، عمره عمر الجلسة، وقد يبقى بعدها إن مات التطبيق قبل الحذف.

#### القرار: خادم على loopback لا تكامل native

`video_player` و`just_audio` يبنيان على ExoPlayer وAVPlayer، وكلاهما يقرأ من **رابط** لا من تدفق في الذاكرة. فالخيارات اثنان: خادم HTTP داخل التطبيق، أو مصدر بيانات مخصَّص بكود Kotlin وSwift لكل مشغّل. اختير الأول: يعمل على المنصّتين بكود Dart واحد، ولا يضيف سطحًا native ثالثًا يُصان.

`LocalMediaSource` يربط على `127.0.0.1` بمنفذ يختاره النظام، ويفكّ الأجزاء المطلوبة عند الطلب. **لا بايت صريح يلمس القرص** — يثبّته اختبار يقايس محتوى المجلد قبل التشغيل وبعده.

#### القفز يعمل، وهذه ثمرة الصيغة المقسَّمة

المشغّل يطلب `Range` عند كل قفزة، والخادم يجيب من الجزء المطابق مباشرة: فهرس الجزء = الموضع ÷ حجم الجزء، وكل جزء مستقلّ بمفتاحه وnonce وAAD. بلا `ENC-006` كانت كل قفزة تعني فكّ الملف من أوّله. الاختبارات تغطّي: نطاقًا في المنتصف، ونطاقًا مفتوح النهاية، و`bytes=-N`، ونطاقًا يبدأ **داخل** جزء لا على حدّه.

وثلاثة تفاصيل بلا أي منها لا يعمل المشغّل:
- **`Content-Type` مشتقّ من نوع المحتوى** (`video/mp4` / `audio/mp4`): ExoPlayer يختار الحاوية من الترويسة قبل قراءة البايتات، و`octet-stream` تجعله يرفض ملفًا سليمًا.
- **الطول يُحسب بلا فكّ**: `plainLengthOf` يحسبه من طول الملف وحجم الجزء. المشغّل يسأل عن الطول أولًا، وإجابة خاطئة = شريط تقدّم كاذب وقفز لا يصل.
- **نطاق خارج الملف يُعيد 416** لا 200 بجسم فارغ: الثاني يجعل المشغّل يظنّ الملف انتهى فيتوقف بلا رسالة.

#### الحدّ الأمني، بصراحة

على Android يستطيع أي تطبيق آخر على الجهاز الوصول إلى `127.0.0.1`. لذلك: **مسار سرّي لكل جلسة** (128 بت من `Random.secure()`، لا فهرسة ولا مسار افتراضي يستجيب)، و**عمر الخادم عمر التشغيل** (آخر رابط يُغلق الخادم — يثبّته اختبار)، و**loopback فقط** فلا وصول من الشبكة.

وهذا **أضيق** من الحالة السابقة لا أوسع: الملف الصريح كان مقروءًا من أي تطبيق على جهاز مفتوح الجذر، ومن أي نسخة احتياطية، وبعمر أطول من الجلسة. والمسار السرّي لا يُخمَّن.

#### أثر على مسار الشبكة أيضًا

المشغّل صار يستعمل `networkUrl` في الحالتين، فسقط فرع `VideoPlayerController.file` كلّه من صفحتَي الفيديو والصوت. الفرق الوحيد الباقي هو ترويسة التخويل: المصدر المحلي لا يحتاجها والشبكي يحملها.

#### حدّ في الاختبار يستحق التسجيل

`TestWidgetsFlutterBinding` يستبدل `HttpClient` فيُعيد 400 لكل طلب. لذلك التحقّق الشبكي الحقيقي — الطلب الكامل والنطاقات والترويسات والرفض — في `local_media_source_test.dart` وحدها (لا تستعمل ذلك الربط)، وفي `download_engine_test.dart` يُتحقَّق من شكل الرابط وغياب الملف الصريح وأن الحزمة على القرص مشفَّرة وتُفَك إلى الأصل. الفصل مقصود: اختبار يزعم فحصًا شبكيًّا وهو يفحص 400 مصطنعة أسوأ من غيابه.

#### الملفات

`lib/core/media/local_media_source.dart` (جديد) · `lib/core/crypto/file_crypto.dart` (`decryptRange` و`plainLengthOf` و`_PackageLayout`) · `lib/features/downloads/data/download_repository.dart` · `lib/features/downloads/application/download_manager.dart` · `lib/features/playback/presentation/playback_page.dart` · `lib/features/audio/presentation/pages/audio_player_page.dart` · `test/local_media_source_test.dart` (جديد، 16 اختبارًا) · `test/download_engine_test.dart` · `test/file_crypto_chunked_test.dart`.

#### التالي

بعد هذه الدفعة لم يبقَ في §5 بند قابل للإنجاز بلا منظومة الترخيص: `ENC-002` و`ENC-005` و`ENC-007` و`ENC-008` و`ENC-010` و`ENC-015` كلها تنتظر `ENC-001` — وهو **غير محجوب الآن** بعد حسم `DECIDE-101`، وحجمه XL يستحق دفعة مخصَّصة. البديل بالترتيب: `ENC-013` (موقوف على `DECIDE-107`: حصص التنزيل) ثم §6 من `SEC-107`.
