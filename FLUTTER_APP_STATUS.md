# تقرير حالة تطبيق مجرة (Flutter) — المنجز والناقص

> تطبيق `majarra` — تجربة تعليمية ترفيهية عربية سينمائية للعائلة (أطفال 3–12).
> المسار: `app_main`. Flutter 3.35.2 · Dart ^3.9.
> آخر تحديث: 9 أغسطس 2026 (بعد جولة إكمال الإنتاج).

الرموز: ✅ مكتمل · ⚠️ جزئي / محجوب خارجيًا · ❌ غير منجز.

---

## 1. الخلاصة

البنية ناضجة ومتصلة بـ API حقيقي على `https://api.majarra.app`. في هذه الجولة أُنجزت ميزات إنتاجية حقيقية (لا واجهات وهمية): تنزيلات مشفّرة دون اتصال، بحث عربي متعدد الأنواع + بحث صوتي، تقارير لوحة الوالدين الحقيقية، الدخول بالبصمة، منتقي الصورة الرمزية، وضع «اقرأ لي»، تفاعلات Shorts حقيقية، تقييد `API_BASE_URL`، وبنية لغات مبنية على اكتمال الترجمة الفعلي. جميعها مغطّاة باختبارات (المجموع 257 اختبارًا تمرّ، وتحليل نظيف).

السياسة المتّبعة: **لا رقم مختلَق ولا زر يتظاهر بالعمل** — الميزات المحجوبة بالخادم تُعرض بحالة صريحة.

---

## 2. حالة الميزات

| الميزة | الحالة | ملاحظة |
|---|---|---|
| المصادقة (دخول/تسجيل/خروج) | ✅ | متصلة بالخادم |
| PIN الوالدين (خادمي) | ✅ | `parent_pin_hash` + throttle/lock في FamilyState؛ العميل يتحقّق خادميًا أولًا |
| توكن قدرة قصير لعمليات الوالدين الحسّاسة | ⚠️ | يتطلّب تعديل FamilyState (منطقة يعمل عليها وكيل آخر) — مؤجّل |
| الدخول بالبصمة (راحة محلية) | ✅ | `local_auth`، يفتح البوابة المحلية فقط، fallback للـ PIN |
| ملفات الأطفال (إنشاء/تبديل) | ✅ | متصلة `/family/children` |
| منتقي الصورة الرمزية | ✅ | كتالوج ثابت `ChildAvatars` + منتقي |
| تعديل/حذف ملف الطفل | ❌ | لا endpoints (GET/POST فقط) + تعارض FamilyState — مؤجّل |
| لوحة الوالدين (تقارير حقيقية) | ✅ | نشاط/تعلّم/أوسمة من `/family/progress`,`/mastery`,`/rewards` |
| حدود الوقت / نافذة النوم | ⚠️ | يجب فرضها في الخادم (FamilyState) — مؤجّل بصدق |
| التنزيلات دون اتصال (مشفّرة) | ✅ | AES-256-GCM + بوابات اشتراك/تخزين/Wi-Fi + حالات + تشغيل offline |
| تنزيل المحتوى المحمي (فيديو/سرد خاص) | ⚠️ | يحتاج endpoint ترخيص offline في الخادم — محجوب |
| صفحة `/downloads` | ✅ | عناصر حقيقية + حالات + حذف + تخزين مستخدم |
| مشغّل الفيديو | ✅ | (كما كان) استئناف + heartbeat + حماية شاشة |
| مشغّل الصوت | ✅ | + تشغيل offline-first للتنزيلات + زر تنزيل |
| القارئ «اقرأ بنفسي/صامتة» | ✅ | |
| القارئ «اقرأ لي» | ✅ | سرد لكل صفحة عبر توكن قدرة + تقليب تلقائي؛ حالة صريحة عند غياب السرد |
| القارئ «اقرأ معي» (تظليل) | ⚠️ | لا يوجد توقيت كلمات في عقد المحتوى — محجوب |
| الألعاب (12 محرّك) | ✅ | (كما كان) |
| البحث النصي (عربي مُطبَّع + متعدد الأنواع) | ✅ | سلاسل/حلقات/كتب/ألعاب/كواكب + عمليات بحث أخيرة |
| البحث الصوتي | ✅ | `speech_to_text`، مؤقّت، بلا تخزين صوت + إذن ميكروفون |
| تفاعلات Shorts | ✅ | حفظ (قائمة المشاهدة) + مشاركة نظام؛ أُزيل الإعجاب/التعليق |
| تقييد `API_BASE_URL` | ✅ | allowlist لكل بيئة + رفض http/بيانات اعتماد/مضيف غير مدرج |
| بيئات dev/staging/prod | ✅ | `AppConfig` + شارة بيئة؛ backend staging منفصل = محجوب |
| بنية اللغات (AR/EN/FR) | ✅ | متاحة حسب اكتمال الترجمة الفعلي (اختبار يفرض ذلك)؛ AR فقط قابلة للاختيار |
| RTL/LTR | ✅ | AR = RTL، البنية جاهزة لـ LTR؛ استخدام Directional متّسق |
| اتّساق الخط | ✅ | `ThemeData.fontFamily = Readex Pro` |
| نموذج الاتصال + أخطاء آمنة للطفل | ✅ | `ConnectionStatus` + `AppErrorView`/`OfflineNotice` |
| اقتران التلفزيون | ❌ | لا endpoint — محجوب خادميًا |
| نسيت كلمة المرور | ❌ | لا مسار استعادة ذاتي + مزوّد بريد — محجوب |
| الدعم (CRM) | ⚠️ | لا API موجّه للتطبيق بعد (بُني الجانب الإداري) — مؤجّل |
| ترقية/شراء الاشتراك | ⚠️ | قراءة فقط؛ الشراء يحتاج مزوّد دفع — محجوب |

---

## 3. أبرز ما أُضيف في هذه الجولة (كلها باختبارات)

- **الأمان**: `core/env/app_environment.dart` — allowlist لـ `API_BASE_URL` + بيئات + شارة. (`app_environment_test`)
- **الأخطاء/الاتصال**: `core/net/connection_status.dart` + `core/widgets/app_error_view.dart`. (`connection_status_test`)
- **البصمة**: `core/security/biometric_auth.dart` + دمج في صفحة PIN. (`parent_pin_biometric_test`)
- **الأفاتار**: `features/child/.../child_avatars.dart`. (`child_avatars_test`)
- **البحث**: `core/text/arabic_search.dart` + `search_engine.dart` + `recent_searches_store.dart`. (`arabic_search_test`)
- **القارئ**: `features/reader/application/reader_narration.dart` + «اقرأ لي». (`reader_narration_test`)
- **الوالدان**: `features/parent/application/parent_reports.dart`. (`parent_reports_test`)
- **التنزيلات**: `core/crypto/file_crypto.dart` + `features/downloads/**`. (`download_engine_test`)
- **البحث الصوتي**: `core/speech/voice_search.dart`. (`voice_search_test`)
- **اللغات**: `core/l10n/locale_catalog.dart`. (`locale_catalog_test`)

---

## 4. المحجوبات الخارجية / الخلفية (بصدق)

| # | البند | ما يلزم |
|---|---|---|
| 1 | ترخيص التنزيل offline للمحتوى المحمي | endpoint خادمي يصدر ترخيصًا/بايتات طويلة الأمد (التوكنات الحالية قصيرة للبث) |
| 2 | توكن قدرة قصير لعمليات الوالدين | إرجاعه من `/parent-pin/verify` وفحصه في كل تعديل — داخل FamilyState (تعارض) |
| 3 | تعديل/حذف ملف الطفل | `PATCH/DELETE /family/children` — داخل FamilyState (تعارض) |
| 4 | حدود الوقت/نافذة النوم | تخزين وفرض على مستوى الأسرة في الخادم |
| 5 | اقتران التلفزيون | endpoints كود مؤقّت/تحقّق/إبطال |
| 6 | نسيت كلمة المرور | دورة توكن استعادة + مزوّد بريد |
| 7 | «اقرأ معي» (تظليل متزامن) | توقيت كلمات في عقد صفحات القصة |
| 8 | سرد القصص وvoice-over الألعاب | تسجيلات صوتية فعلية (محتوى) |
| 9 | الدعم داخل التطبيق | API دعم موجّه للتطبيق (بُني الجانب الإداري) |
| 10 | الشراء/الترقية | مزوّد دفع معتمد |
| 11 | بيئة staging منفصلة | بنية تحتية staging (route/D1/KV/R2/queues) |

> ملاحظة تنسيق: البنود المؤجّلة بسبب «التعارض» هي ملفات backend يعمل عليها وكيل آخر بنشاط (`dashboard/api`, وخاصة `FamilyState.ts`)؛ أُجّل الجزء الخلفي فقط تفاديًا للتعارض، مع إبقاء عقد/واجهة Flutter جاهزة حيث أمكن.

---

## 5. التحقّق

- `flutter analyze` → **No issues found**.
- `flutter test` → **257 اختبارًا تمرّ** (منها ~9 مجموعات جديدة أُضيفت هذه الجولة).
- لم تُشغّل تجارب E2E على جهاز حقيقي في هذه البيئة؛ التدفّقات مغطّاة باختبارات وحدة/تكامل منطقية. راجع KIRO_LAST_REPORT.md.
