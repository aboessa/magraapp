# أودت الألعاب والرسم وتجربة الواجهة — 2026-09-23

> **ما هذا.** أودت مقيس لثلاثة محاور طُلبت بعينها: محرّكات الألعاب، وموديل
> الرسم، وتجربة الواجهة على الهاتف والجهاز اللوحي والتلفاز. لا تقدير فيه: كل
> بند يحمل ملفًّا وسطرًا، أو استعلامًا على قاعدة الإنتاج.
>
> **كيف قيس.** قراءةُ كود، وعدٌّ بـ`Select-String` على `app_main/lib`، وأربعة
> استعلامات على `majarra-db --env production --remote`. ما لم يُقس موسومٌ بذلك
> صراحةً في §7.
>
> **علاقته بـ`AUDIT_2026-09-23.md`.** ذاك خطّ الأساس العامّ للمستودع. هذا يعمّق
> ثلاثة محاور لم يفصّلها، ولا يخالفه. §6 هنا يُسقط سبعة ادّعاءات كانت في خريطة
> عملي الأولى لهذه المحاور ولم تصمد للقياس.

---

## §0 الخلاصة بالأرقام

| القياس | الرقم | المرجع |
|---|---|---|
| محرّكات ألعاب مُنفَّذة في التطبيق | **12** | `game_screen.dart` → `buildDefaultRegistry()` |
| محرّكات في جدول `game_engines` على الإنتاج | **17** | استعلام §1.2 |
| محرّكات في الكتالوج **لا شيء يشغّلها** | **5** | استعلام §1.2 |
| ألعاب منشورة على الإنتاج | **39** | استعلام §1.2 |
| ألعاب منشورة تُعلن `supports_dpad: true` | **33** | استعلام §3.1 |
| مواضع قراءة مفتاح لوحة/ريموت في كل `features/games` | **1** (زرّ Escape في صفحة رسمٍ لا تُفتح على التلفاز) | §3.1 |
| ألعاب `draft` على محرّكات لا وجود لها | **13** | استعلام §1.2 |
| إشارات `layoutClass`/`horizontalPagePadding` | **56** في **16** ملفًّا | §2.1 |
| إشارات `isTelevision` | **253** في **28** ملفًّا | §2.1 |
| مواضع احترام تكبير الخطّ (`textScalerOf`) | **2** في **2** ملفَّين | §2.3 |
| مستهلكو `AppInputMode` خارج ملفه | **0** | §3.4 |
| لوحات رسم مستقلّة يصل إليها الطفل | **6** | §4.1 |

**الحكم في سطر واحد.** العمود الفقري للألعاب سليم ومُنفَّذ فعلًا — اثنا عشر
محرّكًا حقيقيًّا لا واجهات فارغة. والعطل الأكبر ليس في المحرّكات بل في
**التلفاز**: ثلاث وثلاثون لعبةً من التسع والثلاثين تُعرَض على التلفاز بوعدٍ
مكتوب في حزمتها (`supports_dpad: true`) لا يقابله **سطرٌ واحد** يقرأ زرًّا من
الريموت. أمّا الرسم فمستثنى من التلفاز بالبناء ومحروسٌ باختبار (§5)، وعطله في
مكان آخر: **ستّ لوحات مستقلّة** تفعل الشيء نفسه بستّ طرائق، وفي إحداها شريط
أدوات كامل لا يعمل.

---

## §1 محرّكات الألعاب

### §1.1 ما هو مُنفَّذ — وهو أكثر مما تقوله السمعة

اثنا عشر محرّكًا، كلّها بأجسام حقيقية، مسجَّلة في خريطةٍ لا في `switch`:

| المحرّك | ملفّه | `supportsDpad` |
|---|---|---|
| `trace_color` | `trace_color_engine.dart:21` | `false` |
| `memory_flip`, `match_pairs`, `sort_bins`, `sequence_order` | `wave_one_engines.dart:51,271,414,540` | `true` |
| `count_quantity`, `logic_pattern`, `word_build` | `wave_two_engines.dart:79,563,848` | `true` |
| `rhythm_tap` | `rhythm_tap_engine.dart:67` | `true` |
| `block_code` | `block_code_engine.dart:351` | `true` |
| `sim_lab` | `sim_lab_engine.dart:131` | `true` |
| `timeline_map` | `timeline_map_engine.dart:165` | `true` |

والاختيار بالبيانات لا بالكود: `GameEngineRegistry.resolve` (`game_engine_registry.dart:47`)
قراءةُ خريطة، و`evaluateAvailability` (`:105-142`) يُفرِّق **خمسة** أسباب عدم
توفّرٍ بأسمائها — محرّك غير معروف، إصدار محرّك أحدث، إصدار حزمة أحدث، محتوى
لمسيّ على تلفاز، حزمة تالفة — فالطفل يرى رسالةً تقول شيئًا صحيحًا لا خطأً
عامًّا. هذا تصميم جيّد، ويستحق أن يُقال.

### §1.2 كتالوج الإنتاج يعرض خمسة محرّكات لا شيء يشغّلها

استعلام `game_engines` على الإنتاج يُعيد **17** صفًّا. خمسة منها لا محرّك لها في
التطبيق ولا مخطَّط عقدٍ على الخادم (`ENGINE_SCHEMAS` في
`dashboard/api/src/lib/gamePackGate.ts:50-59` يعرف **12** فقط):

| `engine_id` | `name_ar` | ألعاب | منشور | بلا حزمة |
|---|---|---|---|---|
| `engine-match` | المطابقة | 6 | 0 | 6 |
| `engine-maze` | المسار والمنطق | 3 | 0 | 3 |
| `engine-sequence` | الترتيب | 2 | 0 | 2 |
| `engine-memory` | الذاكرة | 1 | 0 | 1 |
| `engine-builder` | البناء والتجربة | 1 | 0 | 1 |

ثلاثة عشر صفًّا، كلّها `draft` وكلّها بلا `content_pack`. وأربعة من الخمسة
**توائم بالاسم** لمحرّكات قائمة: «المطابقة» هي `match_pairs`، و«الذاكرة» هي
`memory_flip`، و«الترتيب» هي `sequence_order`، و«البناء» قريبٌ من `word_build`.
أي أن هذه بقايا نظام معرِّفاتٍ أقدم (`engine-*` بالشرطة) بقيت في الجدول بعد
الانتقال إلى `snake_case`. وحدَه `engine-maze` بلا مقابل: لا محرّك متاهة في
التطبيق إطلاقًا.

**الأثر محدود ومعروف، وهذا هو المهم:** لا طفل يراها — كلّها `draft`. والنظام
**يسمّي** المشكلة بنفسه: `gamesOps.ts:268-273` يحسب
`engine_coverage.missing` بمقابلة الكتالوج بالمُنفَّذ، و`publishReadiness.ts:418-424`
يضع فحص «المحرّك» على `blocked` بنصٍّ صريح، و`:437-441` يفرّق **عقد الخادم** عن
**تنفيذ العميل** ويرفض أن يجعل «لم ننظر» و«مُنفَّذ» حالةً واحدة. فالدَّين هنا
ليس عمًى في الأدوات، بل تقريرٌ لم يقرأه أحد.

لكن ثغرةً واحدة تستحق الذكر: `validatePackForGame` (`gamePackGate.ts:194-196`)
يُعيد `{validated: false, errors: []}` لمحرّكٍ بلا مخطَّط — **صفر أخطاء**. فبوابة
الحزمة تمرّ بصمت على هذه الثلاثة عشر، والذي يحجب النشر هو `publishReadiness`
وحده. حاجزان مستقلّان أحدهما ساكت.

و`adminContent.ts:744-748` يعرض **الصفوف السبعة عشر كلّها** في منتقي المحرّك،
بلا تمييزٍ لخمسةٍ منها، مع أن `enginesWithRuntimeSchema()` مُصدَّرةٌ ومتاحة
لذلك بالضبط (`gamePackGate.ts:66-68`).

### §1.3 حقول حزمة تُحلَّل ولا تُقرأ

`game_pack.dart` يحلّل حقولًا لا يستهلكها أحد. مقيسٌ بعدّ الإشارات في كلّ
`app_main/lib`:

| الحقل | الإشارات | الحكم |
|---|---|---|
| `GamePack.producesCreations` | **1** (`game_pack.dart:417`، تعريفُه) | مُعلَن ولا يُقرأ |
| `PackAccessibility.reducedMotionSupported` | **3**، كلّها في `game_pack.dart:214,227,234` | مُعلَن ولا يُقرأ |
| `PackAccessibility.sequentialTapAlternative` | **4**؛ يُقرأ في `trace_session.dart:244` وحده | يعمل في `trace_color` فقط |

`reducedMotionSupported` هو الأخطر من الثلاثة: حزمةٌ تقول «هذا المحتوى يحترم
تقليل الحركة» والتطبيق لا يسأل الحقل قطّ. وعدٌ في البيانات لا يقابله سلوك.

### §1.4 مساحات اللمس — أفضل مما ظننت

`effectiveTouchTarget` (`game_services.dart:277-281`) يفرض أرضية **48dp** ولا
يقبل أقلّ منها مهما قالت الحزمة. وهي مستعملة **16** مرّة، وموزَّعة على
**أحد عشر** محرّكًا من اثني عشر.

الغائب الوحيد `timeline_map_engine.dart` — و**غيابه صحيح**: سطحه
`GestureDetector` واحد يغطّي الخريطة كلّها (`:513-546`)، فالنقر في أي موضع يضع
الدبّوس. لا هدف صغيرًا لتُفرض عليه أرضية. (هذا يُسقط بندًا كان في خريطتي
الأولى؛ انظر §6.)

### §1.5 الصوت يفشل بصمت

المستخدم فعلًا هو `CapTokenGameAudioService`: يُنشأ في `game_providers.dart:309`
ويُمرَّر في `game_route.dart:109`. أمّا `RealGameAudioService` فإشاراته الستّ
كلّها **داخل ملفه** `game_audio_service.dart` (`:38,39,87,101,121`) وتعليقٌ في
`media_audio_player.dart:20` — صِنفٌ لا موضع إنشاء له.

والفشل صامت في كل فرع: `unawaited` مُظلَّلةً محليًّا في
`media_audio_player.dart:174-177` و`game_audio_service.dart:167` لا تفعل شيئًا،
فصوتُ تعليمةٍ لا يُشغَّل لا يُعلِم أحدًا — لا الطفل ولا سجلّ. لعبةٌ تعليماتها
صوتية تصير لعبةً بلا تعليمات، وتبدو «تعمل».

### §1.6 لا حفظ لتقدّم اللعب

لا مسار حفظ محلّي لتقدّم اللعب. المحاولات تُبلَّغ إلى الخادم
(`reportEngineAttempt`)، فانقطاعُ الشبكة وسط مستوىً يعني البدء من الأوّل. وهذا
يخالف نمط الرسم، حيث `local_creation_store.dart` يحفظ محلّيًّا أوّلًا.

### §1.7 `ScoringMode` ثلاثة أسماء وسلوكان

`sequence` و`discrete` تتصرّفان مثل `geometric`. وفي الأحد عشر محرّكًا عدا
`trace_color` لا أثرَ للنمط إطلاقًا: تُحسب النقاط وتُبلَّغ، ولا يتغيّر شيء في
اللعب. ثلاثة أسماء في العقد، سلوكٌ واحد في البرنامج.

---

## §2 الهاتف والجهاز اللوحي

### §2.1 الطبقة المتجاوبة موجودة ومستعملة

`app_layout.dart` (36 سطرًا) يُعرِّف ثلاث فئات — `compact ≤ 599`،
`medium ≤ 1023`، `expanded` — و**الارتفاع يفوز أوّلًا**: `fromSize` يُرجِع
`compact` إذا كان `height ≤ 479` قبل النظر إلى العرض (`:16-21`). قاعدةٌ صائبة:
هاتفٌ أفقيٌّ عريض يبقى بملاحةٍ مضغوطة.

والاستعمال حقيقي: **56** إشارة في **16** ملفًّا.
`content_cards.dart:16-33` يقيس عرض البطاقة بالفئة، و`series_details_page.dart:109-141`
يفرّع **خمس** قيم (أقصى عرض، نسبة البطل، حجم العنوان، ارتفاع الرفّ)، و`cinematic_hero.dart:130`
و`home_feed.dart:436` كذلك. والتعليق في `content_cards.dart:12-15` يذكر السبب
بنفسه: قبل هذا كان الجهاز اللوحي يأخذ عرض الهاتف.

المقابلة مع التلفاز تبقى غير متكافئة — **253** إشارة `isTelevision` في **28**
ملفًّا — لكن ذلك متوقَّع: التلفاز قشرةٌ منفصلة (`TvHomeShell`) لا مجرّد نقطة
كسر.

### §2.2 بوابة الاتجاه تحرس بعتبة `900` عمياء عن النسبة

`main.dart:26-34` يقفل العمودي. ثم `_OrientationGate` (`majarra_app.dart:357-414`)
يقيس:

```dart
final isLandscapeTooWide =
    media.size.width > media.size.height && media.size.width < 900;
if (isLandscapeTooWide && !routeAllowsLandscape(widget.router)) {
  return const _PortraitRequiredScreen();
}
```

والمسارات المستثناة من العمودي **مسار واحد**:
`landscapeCapableRoutes = {'/playback/:episodeId'}` (`app_router.dart:249`).

وفي هذا عطلان مقيسان:

1. **الشرط على العرض لا على النسبة.** هاتفٌ حديث بمنطقيّ `914×411` أفقيًّا
   يمرّ (‏`914 ≥ 900`) فلا يرى شاشة «أدِر الجهاز»، ويُبنى بفئة `compact`
   (لأن `411 ≤ 479`) داخل ارتفاعٍ **411** — وفيه عناصر بارتفاعات ثابتة تفوقه
   (§2.4). نجا من البوابة ليقع في التخطيط.
2. **الاستوديو مقفولٌ عموديًّا.** `/studio` ليس في `landscapeCapableRoutes`،
   فالرسم على جهازٍ لوحيّ صغير (‏`1024×600` مثلًا) يُحجَب بشاشة «أدِر الجهاز»
   — والرسم هو أكثر نشاطٍ يستفيد من العرض.

وأمرٌ يُحسب للكود: التعليق في `app_router.dart:249-268` يسرد عطلَين سابقَين
في هذا الموضع بعينه (‏`builder` فوق الـNavigator يُعيد `null` دائمًا، و
`contains('playback')` مطابقةً نصّية) ويشرح لماذا صار القرار من
`RouteMatchList.fullPath`. الموضع مُراجَعٌ مرّةً، والعتبة `900` بقيت.

### §2.3 تكبير الخطّ غير محترَم إلّا في موضعَين

`MediaQuery.textScalerOf` يظهر في **ملفَّين** في كل التطبيق:

- `planets_page.dart:363-380` — ويُحسن: يُضيف **نموّ كتلة النصّ وحدها** إلى
  الارتفاع ولا يضاعف الرمز والحواشي، وعليه تعليقٌ يشرح ذلك، ومحروسٌ باختبار
  ثلاث نِسَب.
- `watchlist_page.dart:168-180`.

وما عداهما — البطاقات والرِفاف وأزرار المشغّل والاستوديو كلّه — يُقاس بأرقام
ثابتة. طفلٌ (أو ولي أمرٍ) رافعٌ حجم الخطّ إلى 130% في نظام التشغيل يحصل على نصٍّ
مقطوعٍ داخل صناديق لم تنمُ معه.

### §2.4 ارتفاعات ثابتة

| الموضع | القيمة |
|---|---|
| `content_rail.dart:175` | `height: 320` |
| `home_destination_spec.dart:140` | `390` / `340` |
| `my_boards_v2.dart:298` | `360` |
| رِفاف `series_details_page.dart:136-141` | `190` / `208` / `220` (وهذه **مفرَّعة بالفئة**، فمقبولة) |

الثلاثة الأولى أرقام عارية لا تسأل عن الفئة ولا عن الارتفاع المتاح. مع بوابة
`900` في §2.2 يصير الناتج `320px` داخل `411px`.

### §2.5 أرضية 48dp داخل الألعاب فقط

`effectiveTouchTarget` مفروضٌ في المحرّكات (§1.4)، ولا نظير له خارجها. وأصغر
الأهداف المقيسة خارج الألعاب:

- `complete_board_page.dart:106` — أحد عشر قرصَ لونٍ بـ`30×30` (وهي غير قابلة
  للنقر أصلًا، §4.3).
- `studio_design.dart` و`login_page.dart` و`playback_page.dart` تُصرِّح
  `minimumSize` بقيمٍ دون 48 في مواضع متفرّقة.

والمفارقة في `complete_board_page.dart:56`: حزمة الصفحة نفسها تُعلن
`'min_touch_target_dp': 48`، ولا شيء في الصفحة يقرأها.

---

## §3 التلفاز

### §3.1 ثلاث وثلاثون لعبةً معروضة على التلفاز، وصفر مفتاح

هذا أهمّ بندٍ في الأودت، وقياسه قاطع من طرفَين.

**من طرف المحتوى** — استعلام على الإنتاج، عن الألعاب المنشورة وحدها:

| `engine_id` | منشور | يُعلن `supports_dpad: true` |
|---|---|---|
| `block_code`, `count_quantity`, `logic_pattern`, `match_pairs`, `memory_flip`, `rhythm_tap`, `sequence_order`, `sim_lab`, `sort_bins`, `timeline_map`, `word_build` | 3 لكلٍّ = **33** | **33** |
| `trace_color` | 6 | **0** |

**من طرف الكود** — شرط العرض على التلفاز يحتاج الاثنين معًا:

```dart
// game_engine_registry.dart:61-65
bool playableOnTelevision(String engineId, {required bool packSupportsDpad}) {
  final engine = resolve(engineId);
  if (engine == null) return false;
  return engine.supportsDpad && packSupportsDpad;
}
```

والأحد عشر محرّكًا المقابلة تُعلن `supportsDpad => true` (§1.1). فالشرطان
مستوفيان، والثلاث والثلاثون **تُعرَض** في كتالوج التلفاز
(‏`home_page.dart:60` يمرّر `requireDpad: isTelevision`).

**وعدد مواضع قراءة مفتاحٍ في كلّ `features/games`: واحد.** عدٌّ على
`LogicalKeyboardKey|Shortcuts(|CallbackShortcuts|FocusNode|KeyboardListener|RawKeyboard`
في الشجرة كلّها يُرجع مطابقتَين، وكلتاهما في السطر نفسه:

```
reference_drawing_page.dart:488  return CallbackShortcuts(
reference_drawing_page.dart:489  bindings: {const SingleActivator(LogicalKeyboardKey.escape): close},
```

زرّ «رجوع» في صفحة رسمٍ مرجعيّ — وهي صفحةٌ لا تُفتح على التلفاز إطلاقًا (§5).
أي أن **لا محرّكَ واحدًا من الاثني عشر يقرأ زرًّا**.

**الأثر:** طفلٌ على التلفاز يتصفّح ثلاثًا وثلاثين لعبة، يفتح أيًّا منها، ولا
يستطيع تحريك شيء. لا رسالةَ تفسير — لأن النظام يعتقد أن اللعبة مدعومة —
فتُقرأ الحالة على أنها تعطّلُ تطبيق. و`supports_dpad: true` مكتوبٌ في الحزم
الثلاث والثلاثين في قاعدة الإنتاج، فالإصلاح ليس في الكود وحده.

**والسبيلان المحتملان مختلفان في الكلفة جذريًّا**، ويستحقّان قرار مالك:

- إمّا تنفيذ إدخال D-pad في أحد عشر محرّكًا (بؤرة، تنقّل، تأكيد، رجوع) —
  وهو عملٌ حقيقيّ لا تعديل سطر.
- وإمّا قلب `supports_dpad` إلى `false` في الثلاث والثلاثين — فيختفي كلّ
  اللعب من التلفاز، ويصير صادقًا. وهذا ما يفعله `trace_color` الآن بالضبط.

لا أوصي بأحدهما من دون قرارك، لأن الثاني يحذف ميزةً مُعلَنة والأوّل يُنفق
أسابيع.

### §3.2 لا بحث على التلفاز

مدخلات `/search` اثنان فقط: `watch_page.dart:79` و`explore_page.dart:29,44` —
وكلتا الصفحتَين داخل `AdaptiveHomeShell`، أي **غير التلفاز** (`home_page.dart:66-68`
يختار `TvHomeShell` أو `AdaptiveHomeShell` ولا يخلط). وما يُوصله `TvHomeShell`
هو: `/planets`، `/tv-pairing`، `/watchlist`، القارئ، الصوت، السلسلة، اللعبة
(`tv_home_shell.dart:40-177`). لا بحث.

و`SearchPage` و`ShortsPage` يُمرَّران `isTelevision: false` **حرفيًّا**
(`app_router.dart:355,369`). فهذا **كامنٌ لا حيّ**: الصفحتان لا تُفتحان على
التلفاز اليوم، ولو فُتحتا (رابط عميق، أو مدخل تلفاز يُضاف غدًا) لظهرتا بتخطيط
الهاتف — وفي `search_page.dart:108` `autofocus: !isTelevision`، فلوحة مفاتيح
تنبثق على شاشة تلفاز.

### §3.3 `/tv-pairing` مدخلٌ حيّ إلى صفحةٍ ناقصة

`tv_home_shell.dart:90` يضع زرّ «اقتران التلفزيون» في شريط التطبيق، أي أنه
**قابلٌ للوصول من الشاشة الأولى**. و`tv_pairing_page.dart` بلا `autofocus`
وبلا نقطة نهاية خادم. زرٌّ بارزٌ إلى طريقٍ مسدود.

### §3.4 `AppInputMode` بلا مستهلك واحد

`core/input/input_mode.dart` يُعرِّف نموذج نمط الإدخال. إشاراته الأربع عشرة
**كلّها داخل الملف نفسه** (`:6,8,9,11,12,13,16,17,18,23,25,26,41,51`). صفر
مستهلك. والتجريد الذي يفصل «تلفاز» عن «لا مؤشّر» — وهو بالضبط ما يحتاجه §3.1 —
مكتوبٌ وغير موصول.

وبؤرة التنقّل ثلاثة مواضع في التطبيق كلّه: `FocusTraversalGroup` في
`tv_home_shell.dart:49` و`audio_player_page.dart:447` و`story_reader_page.dart:894`.

### §3.5 كشف التلفاز بإشارة واحدة

`DeviceProfileService` (`device_profile.dart:18-35`) يعتمد
`UiModeManager.currentModeType` من `MainActivity.kt` وحده — لا حجمًا، ولا نوع
إدخال. والترتيب مقصود ومُعلَّق عليه: `kIsWeb` **قبل** تجاوز التطوير، فلا يصير
Chrome تلفازًا. والنتيجة الصحيحة بحدودها: iOS وسطح المكتب والويب ليست تلفازًا
أبدًا، ولو وُصِل جهاز Android بشاشة ومِقود.

---

## §4 موديل الرسم

### §4.1 ستّ لوحات مستقلّة، والطفل يصل إليها كلّها

| اللوحة | الملف | السطور | الحفظ | الأدوات |
|---|---|---|---|---|
| الكانفاس المشترك | `engine/free_draw_surface.dart` | 1094 | ✔ عبر المستدعي | 4 فرش + ممحاة حقيقية + تراجع/إعادة |
| تلوين (v1) | `engine/coloring_board.dart` | 768 | ✔ (`creative_studio_page.dart:2644`) | فرشاة + ملء |
| تلوين (v2) | `pages/coloring/coloring_board_v2.dart` | 693 | ✔ بمسارٍ خاصّ | ملء نقطيّ فقط، بلا فرشاة ولا ممحاة |
| تتبّع (v2) | `pages/studio_tracing/trace_board_v2.dart` | 419 | — | تتبّع |
| أكمل الرسمة | `pages/complete/complete_board_page.dart` | 120 | ✔ | **ستّ أدوات كلّها معطَّلة** (§4.3) |
| صِل النقاط | `pages/connect_dots/connect_dots_board_page.dart` | 676 | **✘ لا حفظ** | نقرٌ مرتَّب |

الستّ كلّها حيّة: `/studio` → `CreativeStudioPage` →
`CompleteCataloguePage:153` → `CompleteBoardPage`، و
`ConnectDotsCataloguePage:445` → `ConnectDotsBoardPage`،
و`creative_studio_page.dart:376,379,496,2644`.

وهناك ازدواج `v1/v2` صريح: لوحتا تلوين، وصفحتا «لوحاتي»
(`my_boards_page.dart` 268 سطرًا و`studio_v2/my_boards_v2.dart` 326)، وصفحتا
بيتِ تتبّع (`trace_home_page.dart` 312 و`studio_tracing/trace_home_v2.dart` 431
— والأولى تُنيب عن الثانية في `:75`). و**كلتا** صفحتَي «لوحاتي» مستدعاةٌ من
`creative_studio_page.dart`: `MyBoardsV2Page` في `:166,387` و`MyBoardsPage` في
`:459`. مسارٌ واحد في الواجهة يُوصل إلى تطبيقَين مختلفَين للفكرة نفسها بحسب
الطريق المأخوذ.

### §4.2 الكانفاس المشترك جيّد، وتعليقه بائت

`free_draw_surface.dart` هو الجزء الأمتن في الرسم: ممحاة حقيقية بـ
`BlendMode.clear` على طبقةٍ محفوظة لا ضربةً بلون الخلفية — والتعليق في
`:14-17` يشرح لماذا: ضربةُ الخلفية تنكسر لحظةَ وجود `background_asset`، وليست
صحيحةً في التراجع مع الشفافية. وهو يقرأ `level.backgroundAsset` فعلًا
(‏`:474`) فصورة التحدّي تظهر على الكانفاس.

عطلٌ صغير وقابل للإصلاح فورًا: التعليق في `:20` يقول «Bounded to 50 strokes»
والثابت في `:70` هو `kMaxStrokes = 120`. توثيقٌ يخالف الكود في الملف نفسه.

### §4.3 «أكمل الرسمة» — صفحةٌ تُظهر ما لا تفعله

`complete_board_page.dart` 120 سطرًا، وفيها خمسة أمور مقيسة:

1. **شريط أدوات كامل معطَّل.** `:103` — ستّ أدوات (فرشاة، قلم، ممحاة، ملء
   اللون، تحديد، تكبير)، وكلّ واحدة `SnackBar('أداة … قريباً')`. و«فرشاة»
   مُلوَّنة بـ`#6A3DF2` أي **تبدو مُختارة** وهي معطَّلة مثل البقية.
2. **أحد عشر قرص لونٍ لا يُنقَر.** `:106` — `Container` بـ`decoration` فقط،
   بلا `InkWell` ولا `GestureDetector`. زينةٌ بهيئة أداة. ومقاسها `30×30`، دون
   الأرضية التي تُعلنها حزمة الصفحة نفسها في `:56` (`min_touch_target_dp: 48`).
3. **زرّان بنصَّين مختلفَين وفعلٍ واحد.** `:109` — «إعادة / مسح الرسم» و«من
   جديد / ابدأ من جديد» كلتاهما `_drawCtrl.clear()`.
4. **عدّاد نجومٍ ثابت.** `:84` — `Text('120')` رقمٌ حرفيّ في شريط التطبيق، لا
   صلة له بما فعله الطفل.
5. **ولا تقدّم ولا صوت.** `:61` — `SilentGameAudioService()` و`_NoopReporter()`.
   فالصفحة تعرض «120 نجمة» ولا تُبلِّغ محاولةً واحدة.

الحفظ وحده يعمل: `:71` يستدعي `saveFromBoundaryWithDocument`.

وعطلٌ تخطيطيّ مستقلّ: التخطيط `Row` بلوحٍ جانبيّ ثابت `120px` (`:92`) والكانفاس
`Expanded`. على هاتفٍ بعرض 360 يبقى للكانفاس ‏`360 − 12 − 12 − 120 − 8 ≈ 208px`.
سطحُ رسمٍ بعرض مئتَي بكسل.

### §4.4 «صِل النقاط» لا يحفظ

`connect_dots_board_page.dart` (676 سطرًا): عدُّ الإشارات إلى
`save|Save|store|Store` في الملف = **صفر**. الطفل يُكمل النشاط ولا يبقى منه
شيء. وثلاثة أزرارٍ فيه معطَّلة بـ`SnackBar`: الصوت (`:146`)، والتكبير
(`:408`)، والتصغير (`:413`). و`_hint()` يُنقص عدّادًا بلا أثرٍ بصريّ.

### §4.5 نموذج المناطق المُعلَن لا تستعمله اللوحة الشغّالة

`engine/coloring_regions.dart` (161 سطرًا) يُعرِّف `ColorRegion` — نموذج
مناطق التلوين. و`coloring_board_v2.dart`، وهي اللوحة التي يفتحها الطفل، تملأ
**نقطيًّا** بـ`tol = 120` ولا تعرف المناطق. فالنموذج مُصدَّرٌ ومستورَد في
`creative_studio_page.dart:20` ويخدم `coloring_board.dart` (v1) وحده.

والأثر ليس نظريًّا: الملء النقطيّ لا يعرف حدود الشكل، فهو يُسيل اللون عبر أي
فتحةٍ في الخطّ الخارجيّ — وهو ما يحدث في رسوم SVG المولَّدة عند حوافّ مضادّة
التعرّج.

### §4.6 الأصول والكتالوج — نصفه بلا شبكة

`creative_catalogue_provider.dart` (522 سطرًا) فيه أحد عشر مزوّدًا، **ستّة
منها بلا شبكة** (‏`coloring`، `reference`، `referenceSteps`، `copy`، `prompt`
ومشتقّاتها) وخمسةٌ بشبكة. و`fetchColoringFromApi` و`fetchReferenceFromApi`
**بلا مستدعٍ**: دالّتان مكتوبتان لجلبٍ لا يُطلَب.

وفي حلّ الأصول مسارَان لا يُفرَّق بينهما بالنوع:
`drawing_asset_map.dart` (~160 مدخلًا) يحلّ `asset-*` إلى SVG مبندلة، و
`creative_remote_assets.dart` (364 سطرًا) يحلّ بـURL. و`DrawingAsset` يأخذ
`assetIdOrPath` — سلسلةً واحدة قد تكون معرِّفًا أو مسارًا أو رابطًا. في
`complete_board_page.dart:34` يصير `widget.item.assetId ?? ''`: سلسلةٌ فارغة
تجتاز حرس `!= null` في `free_draw_surface.dart:450`.

### §4.7 هندسة تتبّع مُختلَقة

`trace_home_page.dart:214+` يبني `TraceLevelSpec` بنقاطٍ مكتوبة يدويًّا:
`letters_ar` فيه أربع نقاطٍ لا تُشكّل حرف «أ». والطفل يتتبّع مسارًا لا علاقة
له بالحرف المعروض اسمه. ثم `StudioSuccessPage` في `:164` يعرض **الرمز** بخطّ
`96px` لا رسمة الطفل — فالمكافأة على عملٍ لا تُري العمل.

### §4.8 الحفظ: محلّيٌّ يعمل، وسحابيٌّ لا يستدعيه أحد

- `local_creation_store.dart`: PNG بـbase64 في `SharedPreferences`، سقف
  `1024px` و`2MiB` للصورة و**200** عملًا. يعمل.
- `creation_cloud_service.dart`: المسار الوحيد للرفع، محروسٌ بموافقة
  `child_creations` **fail-closed** (لا موافقة ⇒ لا رفع). سليم كسياسة.
  و**لا شيء يستدعيه تلقائيًّا**: رسوم الطفل تبقى على الجهاز، ويفقدها بتغيير
  الهاتف.
- `coloring_board_v2.dart` يتجاوز `LocalCreationStore` في `_saveToPhone`
  ويشارك بـ`pixelRatio: 3.0` بمساره الخاصّ — فسقوف §4.8 الأولى لا تسري عليه.

---

## §5 الرسم مستثنى من التلفاز — بالبناء ومحروسٌ باختبار

هذا البند مُتحقَّق منه بثلاث طبقات مستقلّة، وهو **صحيح**:

1. **بوّابة في بانيَة المسار.** `_touchOnlyRoute` (`app_router.dart:277-284`)
   يغلّف كل مسار استوديو بـ`TouchOnlySurface`، ومستعمَلة في **أربعة** مسارات:
   `/studio` (`:388`)، `/studio/coloring/:id` (`:424`)،
   `/studio/reference/:id` (`:443`)، `/studio/trace/:id` (`:462`).
2. **السطح لا يُبنى على التلفاز.** `TouchOnlySurface`
   (`touch_only_surface.dart:36-47`) يأخذ **دالّة** لا ودجت، فعلى التلفاز لا
   يُبنى السطح إطلاقًا — لا أصلَ يُحمَّل ولا مخزنَ يُفتح لشيءٍ لن يُرى — ويظهر
   `TouchRequiredMessage`: «الاستوديو يحتاج شاشة لمس … افتح الاستوديو على
   الهاتف أو الجهاز اللوحي للرسم». رسالةٌ صادقة لا شاشة خطأ.
3. **حرسٌ آليّ.** `test/touch_only_routes_test.dart` يُوقع من يضيف مسار
   استوديو بـ`GoRoute` عاريًا.

ورابعًا، من طبقة المحرّكات: `trace_color` وحده يُعلن `supportsDpad => false`
(`trace_color_engine.dart:21`)، و`evaluateAvailability` يرفضه على التلفاز
بـ`requiresTouch` (`game_engine_registry.dart:139`). وقياس الإنتاج يُطابق:
ستّ ألعاب `trace_color` منشورة، و`dpad_true = 0` لجميعها (§3.1).

والتعليق في `touch_only_surface.dart:1-17` يسجّل لماذا صارت البوّابة هنا:
الروابط العميقة الثلاثة كانت تُنزل الطفل على لوحةِ رسمٍ تُدار بالإصبع على
تلفاز، بلا رسالة ولا سبيل. أُصلح، ومحروس.

**استثناءٌ واحد يستحق الذكر:** فرع `error` في `TouchOnlySurface:42` يبني السطح
عند تعذّر التعرّف على الجهاز. وهو مقصودٌ ومُعلَّق عليه — `DeviceProfileService`
يلتقط `PlatformException` و`MissingPluginException` ويُعيد «ليس تلفازًا»، فالفرع
غير قابل للوصول عمليًّا، وحجبُ السطح عليه كان سيمنع الهواتف وهي الغالبية.
قرارٌ صائب موثَّق.

---

## §6 سبعة ادّعاءات أسقطها القياس

كانت في خريطة عملي الأولى لهذه المحاور. أُثبِت خلافها، وتُسجَّل هنا لأن
ادّعاءً خاطئًا في أودت يُنفق وقتًا على إصلاح ما ليس مكسورًا:

| الادّعاء | المقيس |
|---|---|
| «خمسة ملفات فقط تتفرّع على `layoutClass`، بـ11 إشارة» | **16** ملفًّا و**56** إشارة. الطبقة المتجاوبة مستعملة فعلًا (§2.1) |
| «`effectiveTouchTarget` غائب في `trace_color` و`rhythm_tap` و`timeline_map`» | موجود في `trace_color` (3 مواضع) و`rhythm_tap`. وغيابه في `timeline_map` **صحيح**: سطحه `GestureDetector` واحد (§1.4) |
| «`/search` و`/shorts` تموت على التلفاز» | لا تُفتحان على التلفاز أصلًا — لا مدخل لهما في `TvHomeShell`. العطل الحقيقي: **لا بحث على التلفاز** (§3.2) |
| «`autofocus` في `search_page.dart:108` يُظهر لوحة المفاتيح على التلفاز» | كامنٌ لا حيّ، لأن الصفحة لا تُفتح هناك (§3.2) |
| «`isTelevision: false` الحرفيّة في صفحات المسارات العمرية عطل» | صحيحة بالبناء: `home_page.dart:66-68` لا يختار `AdaptiveHomeShell` إلّا على غير التلفاز |
| «`sequentialTapAlternative` مُعلَن ولا يُقرأ» | يُقرأ في `trace_session.dart:244`. القاعدة تسري على `reducedMotionSupported` و`producesCreations` وحدهما (§1.3) |
| «كانفاس `complete_board_page` أبيض بلا صورة التحدّي» | `free_draw_surface.dart:474` يقرأ `level.backgroundAsset`، فالصورة تظهر (§4.2) |

---

## §7 الترتيب بالأثر

### يُقرَّر قبل أن يُبرمَج

| # | البند | المرجع |
|---|---|---|
| 1 | **33 لعبةً على التلفاز بلا إدخال.** تنفيذ D-pad في 11 محرّكًا، أو قلب `supports_dpad` إلى `false` في 33 حزمةَ إنتاج. لا ثالث. | §3.1 |
| 2 | ازدواج `v1/v2`: لوحتا تلوين، وصفحتا «لوحاتي» كلتاهما مستدعاة، وبيتا تتبّع. أيُّ نسخةٍ هي الحقّ؟ | §4.1 |

### يُصلَح بلا قرار

| # | البند | المرجع |
|---|---|---|
| 3 | «أكمل الرسمة»: 6 أدوات معطَّلة، و11 قرص لونٍ لا يُنقَر، وزرّان بفعلٍ واحد، و«120» ثابتة، وصفر تبليغ محاولات | §4.3 |
| 4 | «صِل النقاط» لا يحفظ شيئًا | §4.4 |
| 5 | صفر حفظٍ سحابيّ فعليّ: رسوم الطفل تموت مع الهاتف | §4.8 |
| 6 | هندسة تتبّع مُختلَقة: `letters_ar` أربع نقاطٍ لا تُشكّل حرف «أ» | §4.7 |
| 7 | تكبير الخطّ محترَمٌ في موضعَين من كل التطبيق | §2.3 |
| 8 | بوابة الاتجاه: عتبة `900` على العرض، والاستوديو مقفول عموديًّا | §2.2 |
| 9 | فشل الصوت صامتٌ في كل فرع؛ و`RealGameAudioService` بلا موضع إنشاء | §1.5 |
| 10 | 5 محرّكات في كتالوج الإنتاج لا شيء يشغّلها + 13 لعبةً عليها؛ وبوابة الحزمة تمرّ بصفر خطأ | §1.2 |
| 11 | `AppInputMode` بلا مستهلك — وهو التجريد الذي يحتاجه البند 1 | §3.4 |
| 12 | `reducedMotionSupported` و`producesCreations` مُعلَنان ولا يُقرآن | §1.3 |
| 13 | `/tv-pairing` زرٌّ في الشاشة الأولى إلى صفحةٍ بلا نقطة نهاية | §3.3 |
| 14 | ملء نقطيّ يُسيل اللون؛ و`coloring_regions.dart` لا تستعمله اللوحة الشغّالة | §4.5 |
| 15 | كانفاس بعرض ~208px على هاتفٍ بعرض 360 في «أكمل الرسمة» | §4.3 |
| 16 | `ScoringMode`: ثلاثة أسماء وسلوكان، ولا أثر في 11 محرّكًا | §1.7 |
| 17 | ارتفاعات ثابتة: 320 و390/340 و360 | §2.4 |
| 18 | لا حفظ محلّي لتقدّم اللعب، خلافًا لنمط الرسم | §1.6 |
| 19 | `fetchColoringFromApi` و`fetchReferenceFromApi` بلا مستدعٍ؛ و6 مزوّدين بلا شبكة | §4.6 |
| 20 | تعليق «50 strokes» والثابت 120 في الملف نفسه | §4.2 |

---

## §8 ما لم يُقس

قيلَ صراحةً لأن أودتًا يزعم شمولًا لم يبلغه أسوأ من أودتٍ يحدّ نفسه:

- **لا تشغيلَ على جهاز.** كل ما فوق قراءةُ كودٍ واستعلامُ قاعدة. لم أفتح
  التطبيق على هاتف ولا جهازٍ لوحيّ ولا تلفاز Android، فلا لقطةَ شاشة تُدعم
  بها أرقام التخطيط.
- **بوابة `900` لم تُختبر بأحجامٍ حقيقية.** الحسابان (`914×411`،
  `1024×600`) استنتاجٌ من الشرط لا قياسُ جهاز.
- **لا قياس أداءٍ.** لا زمن إطارٍ ولا ذاكرة على كانفاس 120 ضربة، ولا على
  الملء النقطيّ.
- **لا تحقّقَ من صحّة أهداف التتبّع** إلّا `letters_ar`. باقي مجموعات
  `TraceLevelSpec` لم تُقابَل بالحروف والأشكال التي تزعمها.
- **صوت `game-letter-tracing`** (تسعة مراجع، لعبةٌ `draft` واحدة) بقي معطّلًا
  بقرارٍ سابق: كلماته عن الأشكال لا الحروف، وصوتٌ يقول غير ما يفعله الطفل
  أسوأ من الصمت. إصلاحه محتوى لا كود.
- **التلفاز لم يُقس إلّا من الكود والبيانات.** لم يُجرَّب ريموتٌ فعليّ على
  الثلاث والثلاثين، وعليه فالبند 1 في §7 مُثبَتٌ بالغياب (صفر مفتاح) لا
  بالمشاهدة.
